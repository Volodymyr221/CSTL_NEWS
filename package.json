{
  "name": "cstl-news",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "test": "node tests/run.mjs"
  },
  "dependencies": {
    "esbuild": "^0.20.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}

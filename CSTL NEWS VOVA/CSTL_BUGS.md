# CSTL NEWS — Відомі баги (помилки)

> Один баг за раз. Виправив — переміщуй у "Закриті".
> Додавай нові баги одразу як знайдеш.
> Формат ID: **B-XX** — номер, сортуються хронологічно.
> Використання: `/fix B-XX` — скіл Claude прочитає цей файл і знайде опис бага.

---

## 🔴 Критичні (зламана функціональність)

| # | Файл | Опис | Рішення |
|---|------|------|---------|
| — | — | Відкритих критичних багів нема. | — |

> B-01…B-04, **B-23**, **B-24**, **B-26**, **B-27** закриті — див. розділ «Закриті».

---

## 🟡 Середні (є обхідний шлях або рідко трапляється)

| # | Файл | Опис | Рішення |
|---|------|------|---------|
| ~~**B-05**~~ | ~~`src/tabs/buses.js`~~ | ~~`getNextDeparture()` ігнорує поле `days`.~~ | **Закрито 2026-04-18.** `isDayActive(route.days)` в `matchesSearch()` фільтрує рейси за `щодня / пн-сб / пн-пт` через `kyivDayOfWeek()`. |
| ~~**B-08**~~ | ~~`sw.js`~~ | ~~`logo.png` не в `STATIC_ASSETS`, в офлайні splash порожній квадрат.~~ | **Закрито** (фактично у Фазі 1 А+, підтверджено 2026-05-17). `sw.js:22` має `'./logo.png'` у списку precache. |
| **B-14** | Немає тематичного фільтра у `news.js` | Документація обіцяє "географія + тематика", але `news.js` має тільки `GEO_FILTERS`. `category` виводиться в картку, але фільтра нема. | Фаза 2.1. Додати `TOPIC_FILTERS` і другий рядок чіпів. |
| **B-25** | `src/core/keyboard.js` — верх смикається при ПІДЙОМІ клавіатури | 🆕[26.07] Лист коментарів «Стрічки», встановлений PWA на iPhone. У момент появи клавіатури верхня частина екрана смикається на кілька кадрів; при зникненні клавіатури — плавно. Кінцевий стан правильний (числа з панелі діагностики: `стиск=413 зсув=0`, `ФОН зсув: +0`, `аркуш top=157 = top0`) — дефект **транзитний**. Причина: iOS прокручує webview, щоб показати поле вводу, **раніше** ніж віддає нам `visualViewport.resize`, а подія `scroll` приходить уже **після** малювання кадру. | ⚠️ **Дві спроби провалились — читай хендовер у `_ai-tools/SESSION_STATE_VOVA.md` перед тим як братись.** Спроба 1 (PR #638) — прибрав «дубль» замка, зламав усе, відкочено. Спроба 2 (PR #641) — стискав аркуш на `focus`, через що тап у поле **закривав лист коментарів**, відкочено (PR #642). Не пробували: `interactive-widget=resizes-content` у `<meta viewport>`. **У нативному додатку проблеми не буде** — там розкладка синхронізована з клавіатурою системою. Вова знає і погодився поки лишити. |
| **B-22** | `scripts/parse_rss.py` — семантичні дублі новин | 🆕[В 20.07] Та сама подія з 2 джерел потрапила у стрічку двічі (напр. загибель О.Квача: «На війні загинув 42-річний захисник…» Волинь Post + «Прийняв останній бій на Сумщині: загинув 42-річний захисник…» Конкурент). Дедуп `is_dup_title` рахує **Jaccard** (перетин÷об'єднання) з порогом `0.65`; тут 6 спільних ÷ 11 усіх = **0.55** < 0.65 → пропустив, бо друге джерело додало «прикрасу» до заголовка. | **Не робити зараз — частина ширшого «налаштувати парсер по нових критеріях» (Вова, критерії дасть).** Рекомендація: додати **containment**-перевірку (менший заголовок майже весь у більшому: перетин÷min(len)≥0.8 → дубль; тут 6/7=0.86 зловило б) поряд із Jaccard. Опційно: дедуп за ключовою сутністю (ім'я+факт). |

---

## 🟢 Дрібні (косметика, не ламає функціонал)

| # | Файл | Опис | Рішення |
|---|------|------|---------|
| ~~**B-15**~~ | ~~`src/tabs/news.js`~~ | ~~Патерн `onclick="fn('${data}')"` — XSS-ризик.~~ | **Закрито 2026-05-17.** 3 inline onclick → event delegation (`#geo-filters` + `#news-list`) + `data-geo` / `data-article-id` атрибути. `setGeoFilter` / `openArticle` більше не у `window`. |
| ~~**B-21**~~ | ~~`src/tabs/community-blocks.js` + `community.js`~~ | ~~7× `onclick="switchTab('...')"`~~ | **Закрито 2026-05-17.** `data-switch-tab="X"` атрибути + один делегований listener у `attachSwitchTabDelegation()` на `#cm-content`. Аналогічно B-15. |

---

## ✅ Закриті баги

| # | Опис | Дата закриття | Коміт |
|---|------|--------------|-------|
| **B-27** | 🆕[В 26.07] **Лічильник коментарів показував більше, ніж видно на екрані.** Картка поста писала «2 коментарі», у заголовку листа теж 2, а в листі був ОДИН. Не синхронізовано ні між екранами, ні між користувачами. **Корінь (доведено живими даними, пост 14):** коментар `111` — відповідь на коментар `110`, який ВИДАЛЕНИЙ. Хронологія: 13:45:24 створено 110 → 13:45:41 видалено 110 → **13:45:48 створено 111 як відповідь на нього**. Такий рядок живий, але недосяжний: подання `page_comment_counts` рахувало все живе (`count(*) where deleted_at is null`), а `fetchPostComments` спускається лише від ЖИВИХ коренів. Ніщо не забороняло відповідь під видалений коментар (жоден із 4 тригерів цього не перевіряв), а каскад «одна сітка» цього закрити не може — він прибирає гілку на момент видалення, про майбутніх дітей не знає. ⚠️ Звірка лічильника з базою (PR #651, 26.07) була правильна, але марна — синхронізувала клієнта з джерелом, яке брехало. | 2026-07-26 | Міграції `forbid_reply_under_deleted_parent` (заборона на вставці, маркер `parent_deleted` + людський текст «Цей коментар уже видалено» + прапорець `gone` → «Стрічка» перемальовує гілку) і `page_comment_counts_only_reachable` (подання рахує лише досяжні: спуск від живих коренів, `security_invoker` збережено) + ремонт даних (1 сирота мʼяко видалений). SQL: `scripts/supabase_comment_counts_reachable.sql`. **Доведено на живій базі:** відповідь під видаленого → відмова з правильної причини · відповідь під живого і кореневий коментар проходять (без регресу) · батько з іншого поста → відмова · число = «за логікою екрана» на всіх постах. |
| **B-24** | Фото у приватному чаті Дошки не доставляло сповіщення. Задеплоєна версія `send-chat-push` (v2, від 19.06) робила `msg.text.length`, а повідомлення тільки з фото має `text = null` (`src/core/supabase.js:690` пише `text: text \|\| null`) → `null.length` → 500 → отримувач без пуша. Фікс лежав у репо незадеплоєним 15 днів. **Масштаб доведено запитом до бази: 17 фото-повідомлень**, останнє 21.07 — кожне впало. | 2026-07-25 | **Деплой Edge Function через MCP `deploy_edge_function` → v3, ACTIVE, `verify_jwt: true`.** Git-коміту фікс не потребував — код у репо вже правильний з 10.07 (PR #321), не збігався лише прод. Звірено зворотним читанням задеплоєного коду (`get_edge_function`): `select('… text, photo_url')` + `bodyText = msg.text \|\| (msg.photo_url ? '📷 Фото' : '')` на місці. ⚠️ Помітку аудиту «перевірити `verify_jwt`, у робочих `false`» знято як хибну — `true` тут правильний (клієнтський виклик зі звіркою автора), `false` мають лише функції, які кличе база. |
| **B-26** | 🆕[знайдено Claude 25.07 при додаванні редагування коментаря] **Редагування ОБХОДИЛО антиспам.** `trg_page_comments_antispam` («Стрічка») і `trg_comments_antispam` (Дошка) стояли лише на `BEFORE INSERT`. Тобто фільтр перевіряв текст при створенні коментаря, але не при його правці — переписати вже опублікований коментар на матюк можна було вільно. У Дошці редагування існувало давно, тож дірка була відкрита й там. Доведено наживо: до фіксу правка на «хуйло» проходила в обох таблицях. | 2026-07-25 | Текст проганяється через `text_abuse_reason` і при UPDATE: у «Стрічці» — всередині `page_comments_guard_update` (щоб не плодити другий тригер на тій самій таблиці), у Дошці — новий `trg_comments_guard_update_antispam`. Міграція `comment_edit_support_and_antispam_on_update`. Після фіксу — заблоковано в обох, нормальна правка проходить. ⚠️ Номер саме B-26: B-25 зайняв баг клавіатури 26.07. |
| **B-23** | Дірка `pcom update` на `page_comments`: `with check (true)` дозволяла через API змінити **будь-яке поле** — підробити `author_uid` (чуже авторство), переписати чужий текст з-під адміна сторінки, підробити згадку `reply_to_uid`, перенести коментар на інший пост і **«воскресити» видалений (`deleted_at = null`) в обхід каскаду «одна сітка»**. | 2026-07-25 | Тригер `trg_page_comments_guard_update` (BEFORE UPDATE, функція `page_comments_guard_update`) — той самий патерн, що `messages_guard_own_edit` 08.07. Міняти можна лише `text` (і лише автору) та `deleted_at` (лише null → час); видалений коментар заморожено. Заразом `with check (true)` → той самий вираз, що в `using`. SQL: `scripts/supabase_comment_guard.sql`, міграція `guard_page_comments_update_fields`. **Доведено тестом: 4 дірки відкриті ДО фіксу → 9/9 зелених ПІСЛЯ**, каскад не зачеплено. |
| **B-01** | Деплой падав з помилкою "non-fast-forward" після зміни нікнейму GitHub (старий deploy.yml робив `git commit bundle.js → push main`, а main рухався). | 2026-04-10 | Закрито через архітектурне рішення А+ (новий підхід GitHub Pages Deploy Action не комітить у main взагалі — див. `docs/ARCHITECTURE.md`). Крок 3 Фази 1. |
| **B-04** | `.github/workflows/deploy.yml` видалено в `5763587`, автодеплой не працював. | 2026-07-04 | Закрито формально — підхід А+ (`deploy.yml`) давно на проді, деплой стабільно працює. Був помилково у «Критичні · в процесі». |
| **B-02** | `bundle.js` видалено (`5763587`). | 2026-04-10 | Вирішено Кроком 2 Фази 1 (`0e9dcd2`) — `bundle.js` відновлено, у git як робоча база. |
| **B-03** | `build.js` видалено (`5763587`). | 2026-04-10 | Вирішено Кроком 1 Фази 1 (`2e7309e`) — `build.js` відновлено. |
| **B-05** | `buses.js` ігнорував поле `days` — показував рейси у неділю для «пн-сб». | 2026-04-18 | `isDayActive()` + `kyivDayOfWeek()` у `matchesSearch()`. |
| **B-10** | Два набори слеш-команд (root `.claude/commands/` + дубль у `CSTL NEWS VOVA/.claude/commands/`). Claude Code підхоплює тільки root, sub-копія була джерелом розсинхронізації. | 2026-05-12 | Видалено sub-копію (6 файлів: `audit`, `fix`, `mockup`, `new-file`, `start`, `gemini`). Root лишається єдиним джерелом правди. |
| **B-07** | `src/tabs/submit.js` — форма «Подати новину» через `mailto:` з оманливим toast «Надіслано!». | 2026-05-12 | Файл `submit.js` видалено повністю. Замінено крутішим UX: кнопка-тригер у Дошці громади (`community.js`) → bottom-sheet модалка → submit-handler. Бекенд (Supabase) — у Фазі 3. |
| **B-18** | `src/tabs/submit.js` — PII (телефон/Instagram) зберігалися у localStorage без TTL. | 2026-05-12 | Файл `submit.js` видалено повністю. Нова форма у модалці нічого не зберігає у localStorage; дані підуть напряму у Supabase у Фазі 3. |
| **B-11** | `.DS_Store`×3 і `CSTL NEWS VOVA.zip` (36 KB) закомічені в git. | 2026-05-12 | Розширено `.gitignore` (`.DS_Store`, `*.zip`, `*.log`, `.idea/`, `.vscode/`, `.env`). Файли прибрано з git tracking через `git rm --cached`. |
| **B-09** | Мертвий CSS `.submit-form` / `.form-input` / `.submit-btn` (~32 рядки), стара картка `.news-card`. | 2026-05-13 | Прибрано при розбитті style.css на 8 модулів (`style/*.css`) — мертві блоки не перенесено. |
| **B-13** | `fetch` до Open-Meteo у `weather.js` без таймаута і AbortController. Висить безкінечно при поганому з'єднанні. | 2026-05-13 | Додано `AbortController` з 5-секундним `setTimeout(() => ac.abort(), 5000)`. `clearTimeout` у success і catch. |
| **B-12** | `news.js:42` featured = `articles[0]` без сортування — перша у JSON може бути стара. | 2026-05-13 | У `getFiltered()` додано `.sort((a,b) => (b.ts || 0) - (a.ts || 0))`. Featured тепер завжди найсвіжіша. |
| **B-17** | `events.js` сортування тільки за датою — при однакових датах події у порядку JSON. | 2026-05-13 | Додано вторинне сортування `(a.time || '').localeCompare(b.time || '')`. |
| **B-06** | `events.js` крихкий парсинг дати `new Date(now.toDateString())` — на iOS Safari ламається. | 2026-05-13 | Уже закрито у коді: `now.setHours(0,0,0,0)` + `new Date(e.date + 'T00:00:00')`. Підтверджено пошуком — крихких патернів нема. |
| **B-16** | `boot.js setupPWA()` створював дубль `<link rel="manifest">` через Blob URL — конфлікт із статичним з index.html, на iOS Safari нестабільно. | 2026-05-13 | Функцію `setupPWA()` повністю видалено з `boot.js`. Лишився тільки статичний `<link rel="manifest" href="manifest.json">` у index.html (повний з PNG-іконками). |
| **B-19** | Граматика "Вови" у документації: `"Воваа"`, `"Вовау"`, `"Воваом"`. | 2026-05-13 | `sed` по `docs/RULES.md` і `docs/CONCEPT.md`. Виправлено на правильні відмінки. |
| **B-20** | `sync.sh` робив `git pull origin main --no-rebase -X ours` — мовчки відкидав чужі зміни. | 2026-05-13 | Файл `sync.sh` видалено. Workflow тепер через робочу гілку → auto-merge → deploy. |
| **B-08** | `sw.js` — `logo.png` мав не бути в `STATIC_ASSETS`. | 2026-05-17 | Фактично закрито давно — `sw.js:22` має `'./logo.png'` у списку precache. Аудит підтвердив, перенесено у Закриті. |
| **B-15** | `src/tabs/news.js` — 3× inline `onclick`. | 2026-05-17 | Event delegation: один listener на `#geo-filters` (через `data-geo`) і один на `#news-list` (через `data-article-id`). `setGeoFilter` / `openArticle` прибрано з `window.*`. |
| **B-21** | `src/tabs/community.js` + `community-blocks.js` — 7× inline `onclick="switchTab(...)"`. | 2026-05-17 | `data-switch-tab="X"` атрибути + один делегований listener у `attachSwitchTabDelegation()` на `#cm-content`. `switchTab` лишається глобальною (потрібна tab-bar). |

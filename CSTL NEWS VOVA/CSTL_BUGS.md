# CSTL NEWS — Відомі баги (помилки)

> Один баг за раз. Виправив — переміщуй в Закриті.
> Додавай нові баги одразу як знайдеш.
> Формат ID: **B-XX** — номер, сортуються хронологічно.
> Використання: `/fix B-XX` — скіл Claude прочитає цей файл і знайде опис бага.

---

## 🔴 Критичні (зламана функціональність)

| # | Файл | Опис | Деталі |
|---|------|------|--------|
| **B-01** | `.github/workflows/deploy.yml` | **Деплой падає з помилкою "non-fast-forward"** після зміни нікнейму GitHub з `Volodymyr221` на `VShevchukkk`. CI (автодеплой) не може запушити bundle.js бо main вже має новіші коміти. | Фікс: додати `git pull --rebase origin main` перед `git push origin main` в deploy.yml. Файл вже виправлено локально але не запушено — потрібен токен. |

---

## 🟡 Середні (є обхідний шлях або рідко трапляється)

*Поки порожньо — додавай баги сюди коли знайдеш*

---

## 🟢 Дрібні (косметика, не ламає функціонал)

*Поки порожньо — додавай баги сюди коли знайдеш*

---

## ✅ Закриті баги

| # | Опис | Дата закриття |
|---|------|--------------|
| — | — | — |

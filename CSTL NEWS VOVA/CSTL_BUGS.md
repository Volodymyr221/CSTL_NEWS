# CSTL NEWS — Відомі баги (помилки)

> Один баг за раз. Виправив — переміщуй у "Закриті".
> Додавай нові баги одразу як знайдеш.
> Формат ID: **B-XX** — номер, сортуються хронологічно.
> Використання: `/fix B-XX` — скіл Claude прочитає цей файл і знайде опис бага.

---

## 🔴 Критичні (зламана функціональність)

| # | Файл | Опис | Рішення |
|---|------|------|---------|
| 🟡 **B-33** | `src/core/account-ui.js` (`NOTIF_KEYS`) + усі Edge Functions | 🆕[аудитом «Питань» 23.08] **Усі чотири вимикачі сповіщень у кабінеті були неробочі.** `notif_prefs` писався в `localStorage` і **не читався ніким** — `grep` по `src/`, `supabase/`, `scripts/` давав рівно одного споживача (той самий файл, що писав). Функції слали push на всі пристрої, ні про які налаштування не питаючи. 📐 **Випадки різні:** «Автобуси» — підписка справжня, але вимикалась ІНШИМ місцем (два вимикачі на одну річ, кабінетний брехав); «Дошка» — не перевірялась; **«Світло» і «Новини» вимикали те, чого не існує** — таких push у проєкті немає, тобто вада з ФАЛЬШИВИМ ПІДТВЕРДЖЕННЯМ. ⚠️ Слово Вови: «Декоративного в нас нічого не має бути, у нас все має бути робоче». | 🟡 **ЗРОБЛЕНО 24.08, чекає живої перевірки.** Таблиця `notif_prefs` (RLS «лише свої») · кабінет пише в базу · читають `send-bus-push` v16 · `send-answer-push` v5 · `send-comment-push` v4 · `send-page-push` v5. Теми: **Автобуси · Питання · Дошка · Стрічка**; «Світло»/«Новини» прибрані. Персональні звернення («вам відповіли», чати, групи) вимикача не мають навмисно. Повний розбір — `docs/QA_CONCEPT.md` і журнал `_session-log/vova-2026-08-23b.md`. |
| — | — | Інших відкритих критичних немає. | — |

> ✅ **B-32 ЗАКРИТО 22.08** — коментарі Стрічки не надсилались ДВІ ДОБИ у всіх.
> Опис лишено повністю: він показує, як помилка вказувала не на ту політику.

<details><summary>B-32 — «Коментар не надіслано» (закрито 22.08)</summary>

| # | Файл | Опис | Рішення |
|---|------|------|---------|
| ~~**B-32**~~ | база: політика `pcom read` + `src/core/supabase.js:addPageComment` | 🆕[знайшов Вова 22.08 на проді, знімок з другого акаунта] **Жоден коментар у Стрічці не надсилався з 20.08.** Тост «Коментар не надіслано — спробуй ще раз», у базі за дві години **0 нових коментарів**. **Корінь:** `addPageComment` робить `.insert().select().single()`, тобто `INSERT … RETURNING`. RETURNING мусить ПРОЧИТАТИ вставлений рядок, а це йде через SELECT-політику `pcom read`, яка з міграції 20.08 звучала `using (public.page_comment_visible(id))`. Функція оголошена **STABLE** і бачить знімок бази на ПОЧАТОК оператора — щойно вставленого рядка в тому знімку ще немає, `exists(…)` = false, і весь INSERT відкочується з `42501`. ⚠️ **Найпідступніше: помилка називає INSERT**, тому розслідування природно йде до політики вставки, яка ЦІЛКОМ СПРАВНА — той самий обман, що 16.08 у `push_subscriptions`. 📐 **Доведено на живій базі** (транзакція з rollback, підставлені роль `authenticated` і JWT справжнього профілю): INSERT **без** `RETURNING` → **OK**; **з** `RETURNING` → **42501**; той самий рядок окремим оператором → **видимий**. Тобто рядок законний, ламався рівно момент RETURNING. **Масштаб:** вражені три таблиці — `posts`, `page_posts`, `page_comments`, тобто рівно ті, чиї політики питали про **власний** рядок; політики, що питають БАТЬКА (`comments read`, `preact read`, `pcomreact read`), працювали — тому Дошка й Питання не ламались. 🛑 **Чому не спіймав сторож:** `tests/visibility-model.mjs` мав у списку рядок `['page_comments', 'page_comment_visible(id)']` — тобто **сам вимагав тієї форми, яка ламала вставку**. Він міряв ФОРМУ ЗАПИСУ, а не наслідок. | ✅ **ЗАКРИТО 22.08.** `scripts/supabase_visibility_returning_fix.sql`, накатано міграцією `visibility_policies_stop_self_query_returning_fix`. Правило винесено у `*_row(колонки)`: політика таблиці кличе його з колонками СВОГО рядка (RLS має їх напряму, знімок не потрібен), а `*_visible(id)` кличе ТУ САМУ `*_row()` — тож модель видимості 20.08 ціла й джерело правди лишається одне на сутність. **Доведено після накату:** коментар з RETURNING → OK · чуже авторство → відхилено (42501) · чужі чернетки → 0 видно. Сторож виправлено + доданий сторож самого КЛАСУ: **жодна SELECT-політика не сміє питати `*_visible(id)` про власний рядок** (26/26, з контролем). Заразом заведено `logDbRefusal` у ядрі `netCall` і блок «🚨 Збої» в адмінці — щоб наступна така поломка була видна нам, а не чекала випадкової спроби Вови. |

</details>

> ✅ **B-31 ЗАКРИТО 09.08** (див. «Закриті»). Опис нижче лишено в повному вигляді
> як історію знахідки — він показує, ЯК саме баг знайшовся.

<details><summary>B-31 — повний опис (закрито)</summary>

| # | Файл | Опис | Рішення |
|---|------|------|---------|
| ~~**B-31**~~ | `src/core/sidebar.js:180` | 🆕[знайдено Claude 09.08 приладом `apple-audit`] **Пункт бургер-меню «Особистий кабінет» не відкриває нічого.** Тап — і тиша: ні екрана, ні тосту, ні помилки в консолі. **Корінь:** гілка `kind === 'account'` робить `document.getElementById('account-btn')?.click()`, а елемента з id `account-btn` у застосунку **більше немає** — кнопку кабінету перенесли на `[data-account-btn]` біля привітання Громади (`src/core/account-ui.js:30`, коментар там прямо це фіксує: «раніше була одна `#account-btn` у шапці»). `?.click()` на `null` мовчки не робить нічого, тому поломка беззвучна. 🔑 **Як знайшлось:** прилад аудиту заміряв «Кабінет» і видав числа **один-в-один** із вкладкою Громада (78 цілей, 130 матеріалів, 32 тексту) — саме ця однаковість і виказала, що екран не відкривався. **Доведено наживо** (залогінений житель, Chromium 390×844): `#account-btn` не існує · пункт меню → кабінету немає · клік по `[data-account-btn]` → `#acc-cab` зʼявляється. ⚠️ Другий шлях у кабінет живий, тож людина не замкнена — але пункт меню бреше. | ✅ **ЗАКРИТО 09.08.** `document.getElementById('account-btn')` → `document.querySelector('[data-account-btn]')`. ⚠️ Шукаємо саме за **атрибутом**, а не за новим id: делегат у `account-ui.js` слухає ВСІ `[data-account-btn]`, і прибивати цвяхами конкретне місце — рівно та помилка, яку лагодили. **Доведено вимірюванням:** новий поведінковий сторож `tests/sidebar-account.mjs` (5 перевірок) відкриває меню й тисне пункт, а далі міряє наявність шару `#acc-cab`; після фіксу **5/5**, **контроль** `BUNDLE_REV=origin/main` — **4/5**, і падає РІВНО шлях через меню, тоді як «кнопка біля привітання відкриває кабінет» лишається зеленою. Саме ця пара й доводить, що зламаний був шлях, а не кабінет. |

</details>

> B-01…B-04, **B-23**, **B-24**, **B-26**, **B-27**, **B-29**, **B-30**, **B-31** закриті — див. розділ «Закриті».

---

## 🟡 Середні (є обхідний шлях або рідко трапляється)

| # | Файл | Опис | Рішення |
|---|------|------|---------|
| ~~**B-05**~~ | ~~`src/tabs/buses.js`~~ | ~~`getNextDeparture()` ігнорує поле `days`.~~ | **Закрито 2026-04-18.** `isDayActive(route.days)` в `matchesSearch()` фільтрує рейси за `щодня / пн-сб / пн-пт` через `kyivDayOfWeek()`. |
| ~~**B-08**~~ | ~~`sw.js`~~ | ~~`logo.png` не в `STATIC_ASSETS`, в офлайні splash порожній квадрат.~~ | **Закрито** (фактично у Фазі 1 А+, підтверджено 2026-05-17). `sw.js:22` має `'./logo.png'` у списку precache. |
| **B-14** | `src/tabs/news.js` + `src/tabs/news-hub.js` | Немає тематичного фільтра. Документація обіцяє «географія + тематика» — географія є, тематики нема. 🔄 **Адресу виправлено аудитом 06.08:** тут стояло `community-blocks.js:1155`, чіпи `CM_NEWS_FILTERS`. **Цієї змінної в коді більше немає** (`grep` по всьому `src/` дає 0 збігів) — гео-групи переїхали в `news.js` як `NEWS_GEO_GROUPS` (одне місце правди, 31.07), а глибина новин — у повноекранний хаб `news-hub.js`. | Додати фільтр за категорією в хабі новин. ⛔ **ЗАБЛОКОВАНО B-28** — фільтр буде порожній. Спершу класифікатор. = **Н-5** в аудиті. |
| 🆕 **B-28** | `scripts/parse_rss.py` — класифікатор категорій | **Класифікатор майже не проставляє категорії.** 📐 **Перезаміряно 06.08** по `data/articles.json` (400 статей): «Суспільство» — **373 = 93.2%**, Економіка 11 (2.8%), Культура 6 (1.5%), Спорт 5 (1.2%), Історія 5 (1.2%). _(26.07 було 381 = 95%; тобто трохи краще, але суть та сама.)_ Наслідок: уся кольорова система бейджів марна, тематичний фільтр (B-14) неможливий. | Полагодити класифікатор у парсері. ⚠️ Врахувати, що Вова 21.07 звів набір до **4 базових** категорій (Суспільство · Культура · Спорт · Економіка) + `CATEGORY_ALIAS` для старих (`news.js`) — класифікатор має розкладати саме по цих 4. ⚠️ У живих даних досі трапляється п'ята, «Історія» (5 статей) — це стара категорія, яку `CATEGORY_ALIAS` мав би зводити. = **Н-10** в аудиті. |
| **B-25** | `src/core/keyboard.js` — верх смикається при ПІДЙОМІ клавіатури | 🆕[26.07] Лист коментарів «Стрічки», встановлений PWA на iPhone. У момент появи клавіатури верхня частина екрана смикається на кілька кадрів; при зникненні клавіатури — плавно. Кінцевий стан правильний (числа з панелі діагностики: `стиск=413 зсув=0`, `ФОН зсув: +0`, `аркуш top=157 = top0`) — дефект **транзитний**. Причина: iOS прокручує webview, щоб показати поле вводу, **раніше** ніж віддає нам `visualViewport.resize`, а подія `scroll` приходить уже **після** малювання кадру. | ⚠️ **Дві спроби провалились — читай хендовер у `_ai-tools/SESSION_STATE_VOVA.md` перед тим як братись.** Спроба 1 (PR #638) — прибрав «дубль» замка, зламав усе, відкочено. Спроба 2 (PR #641) — стискав аркуш на `focus`, через що тап у поле **закривав лист коментарів**, відкочено (PR #642). Не пробували: `interactive-widget=resizes-content` у `<meta viewport>`. **У нативному додатку проблеми не буде** — там розкладка синхронізована з клавіатурою системою. Вова знає і погодився поки лишити. |
| **B-22** | `scripts/parse_rss.py` — семантичні дублі новин | 🆕[В 20.07] Та сама подія з 2 джерел потрапила у стрічку двічі (напр. загибель О.Квача: «На війні загинув 42-річний захисник…» Волинь Post + «Прийняв останній бій на Сумщині: загинув 42-річний захисник…» Конкурент). Дедуп `is_dup_title` рахує **Jaccard** (перетин÷об'єднання) з порогом `0.65`; тут 6 спільних ÷ 11 усіх = **0.55** < 0.65 → пропустив, бо друге джерело додало «прикрасу» до заголовка. | **Не робити зараз — частина ширшого «налаштувати парсер по нових критеріях» (Вова, критерії дасть).** Рекомендація: додати **containment**-перевірку (менший заголовок майже весь у більшому: перетин÷min(len)≥0.8 → дубль; тут 6/7=0.86 зловило б) поряд із Jaccard. Опційно: дедуп за ключовою сутністю (ім'я+факт). |

---

## 🟢 Дрібні (косметика, не ламає функціонал)

| # | Файл | Опис | Рішення |
|---|------|------|---------|
| ~~**B-15**~~ | ~~`src/tabs/news.js`~~ | ~~Патерн `onclick="fn('${data}')"` — XSS-ризик.~~ | **Закрито 2026-05-17.** 3 inline onclick → event delegation (`#geo-filters` + `#news-list`) + `data-geo` / `data-article-id` атрибути. `setGeoFilter` / `openArticle` більше не у `window`. |
| ~~**B-21**~~ | ~~`src/tabs/community-blocks.js` + `community.js`~~ | ~~7× `onclick="switchTab('...')"`~~ | **Закрито 2026-05-17.** `data-switch-tab="X"` атрибути + один делегований listener у `attachSwitchTabDelegation()` на `#cm-content`. Аналогічно B-15. |

---

## ✅ Закриті баги

> ℹ️ **Про шляхи в цьому розділі (звірено 06.08).** Тут згадуються файли, яких у
> репозиторії вже немає — `src/tabs/submit.js` (B-07, B-18), `sync.sh` (B-20),
> `.github/workflows/auto-merge.yml`. Це **не помилки**: саме видалення цих файлів і
> було виправленням. Розділ читається як історія, а не як карта коду.

| # | Опис | Дата закриття | Коміт |
|---|------|--------------|-------|
| 🆕 **B-30** | 🆕[В 07.08] **Пункт FAB «Повідомлення» на Дошці не відкривав нічого.** Тап — і тиша: ні екрана, ні тосту, меню навіть не згорталось. ⚠️ **Перший діагноз був хибний** і записаний сюди ж: «привид у стопці екранів `chat-core.js` після виходу з чату». Уточнення Вови зняло його одним реченням: *«я не виходив, я заходив у вкладку "повідомлення" через меню FAB в дошці і тапав на цю вкладку і воно не відкривалось»* — чат тут узагалі ні до чого. **Корінь:** `closeFab` було оголошено як `const` **усередині `renderAll()`**, а викликала його `syncMsgFab()` — функція рівня модуля. Різні області видимості; у зібраному `bundle.js` esbuild навіть перейменував локальну на `closeFab2`, і виклик усередині `syncMsgFab` лишився звертанням до неіснуючого глобального імені → **`ReferenceError` у момент тапу**, обробник падав на ПЕРШОМУ рядку, тож ні `requireAuth`, ні `openThreadsList` не виконувались ніколи. 🔑 **Чому не відтворювалось у пісочниці і чому 46 стендів були зелені:** ламався ЛИШЕ пункт, вставлений `syncMsgFab()` — тобто в людини **без власних оголошень, але з розмовами** (саме другий акаунт Вови); пункт домальовується асинхронно, коли приїжджають треди. Той самий пункт, намальований `renderFab()`, отримував обробник із правильної області видимості й працював, а будь-який наступний повний рендер (тап по фільтру, вихід на вкладку і назад) баг «лікував». Наявний сторож `board-fab-icon.mjs` дивиться на ТЕКСТ коду і каже лише «пункт малюється за правилом» — рівно знахідка **A-1** аудиту Дошки: сторожі стерегли вигляд, а не поведінку. | 2026-08-07 | `src/tabs/board.js` — `closeFab()` піднято на рівень модуля (одне місце правди; вузли шукає НАЖИВО через `getElementById`, а не тримає в замиканні від моменту рендера), локальний `const` прибрано, `toggleFab` лишився локальним. **Доведено вимірюванням:** новий поведінковий сторож `tests/board-fab-messages.mjs` (9 перевірок) відтворює сцену Вови — залогінений житель без своїх оголошень, розмови приїжджають ПІСЛЯ першого рендера (`slow: { threads: 2200 }` у фікстурі), передумова «пункт вставив саме `syncMsgFab`» міряється **MutationObserver**, а не таймінгом. Після фіксу **9/9**; **контроль** `BUNDLE_REV=origin/main` на коді до фіксу — **3/9** з дослівним `closeFab is not defined`. Заразом розширено `tests/_board-fixture.mjs`: підроблений ЗАЛОГІНЕНИЙ житель (`opts.user`), повільні таблиці (`opts.slow`) і метод `.not` у ланцюгу (без нього `fetchUnreadByThread` рвався і розмови не приїжджали зовсім). |
| 🆕 **B-29** | 🆕[В 26.07] **Антиспам блокував звичайну українську мову.** Вова не міг опублікувати пост про туристичну Олику — «🚫 Пост містить заборонені слова», хоча матюків у ньому нема. **Корінь:** крок 2 `containsProfanity` (`src/core/utils.js`) прибирав з тексту **всі** пробіли й розділові знаки і шукав стеми **підрядком** у злитому полотні. Задум — зловити рознесене «х у й»; наслідок — у злитому тексті шматками матюків стають звичайні слова. У пості спрацювало на `…та ро[блят]ь усе…` — слово **«роблять»**. Заміряно прогоном справжнього `utils.js` у node: **21 звичайна фраза → 15 заблоковано** (роблять/люблять/зроблять — `блят` усередині слова; «свій обовʼязок», «який обрав», «твій образ», «стій обережно», «тримай оборону», «хай обирає» — склейка `…й`+`об…` = `йоб`; «під орудою», «під ореолом» = `підор`; «купи здоровя» = `пизд`). Било не лише по постах: той самий фільтр стоїть на коментарях «Стрічки», Дошці, обговореннях і оголошеннях — слово «роблять» не проходило **ніде**. Додатково знайдено прогоном по живому корпусу (400 статей `data/articles.json`): **«підорлик»** (птах Aquila pomarina) ловився стемом `підор`, **«ЄБРР»** — стемом `єб`, а «…два дні й обʼєднає…» розпадалось на `й`+`об`, бо апостроф вважався роздільником. | 2026-07-26 | `src/core/utils.js` + дзеркало `scripts/supabase_antispam_shared.sql`. **Три зміни:** (1) склейка лише ланцюжків **одиночних** літер (`spacedRuns`, у SQL — барʼєр `\|` замість кожного слова 2+ літер) — обхід «х у й» ловиться далі, звичайні слова не склеюються; (2) апостроф прибирається ДО розбору (усі 4 накреслення) — «обʼєднає» ціле, а обхід «хʼуʼй» навпаки схлопується в «хуй»; (3) allow-list `підорлик`/`єбрр`. Заразом **рішення Вови «послаб фільтр тільки на прямі матюки»** — прибрано образи (ідіот, кретин, дебіл, лох, дурень, тупий, козел, даун, бовдур, скотина, гнида, тварюка, мразь, падло, сволота, педик) і вульгаризми (гівно, срака, жопа, сцикло); `довбо`→повні форми (блокувало «довбати стіну»). 🛑 `nigger`/`nigga`/`faggot` лишені свідомо — мова ворожнечі, не «різкість у розмові». **Доведено:** клієнт — 57/57 тестів + 400 статей 0 хибних. **База — міграція `relax_antispam_to_direct_profanity_only` НАКАТАНА НА ПРОД** (проєкт `uabyfecseqnemvcqhdem`): до накату жива база відхиляла всі 7 нормальних фраз, після — **38/38** тестів, пост Вови «✅ чисто», а прогін **усіх 64 реальних текстів** `page_comments`+`comments` відхилив лише 2 справжні матюки і 5 порожніх рядків (старе правило) — **нових хиб нуль**. Звірено: одна сигнатура функції, всі 4 обгортки через спільне ядро, 5 тригерів на місці. |
| **B-27** | 🆕[В 26.07] **Лічильник коментарів показував більше, ніж видно на екрані.** Картка поста писала «2 коментарі», у заголовку листа теж 2, а в листі був ОДИН. Не синхронізовано ні між екранами, ні між користувачами. **Корінь (доведено живими даними, пост 14):** коментар `111` — відповідь на коментар `110`, який ВИДАЛЕНИЙ. Хронологія: 13:45:24 створено 110 → 13:45:41 видалено 110 → **13:45:48 створено 111 як відповідь на нього**. Такий рядок живий, але недосяжний: подання `page_comment_counts` рахувало все живе (`count(*) where deleted_at is null`), а `fetchPostComments` спускається лише від ЖИВИХ коренів. Ніщо не забороняло відповідь під видалений коментар (жоден із 4 тригерів цього не перевіряв), а каскад «одна сітка» цього закрити не може — він прибирає гілку на момент видалення, про майбутніх дітей не знає. ⚠️ Звірка лічильника з базою (PR #651, 26.07) була правильна, але марна — синхронізувала клієнта з джерелом, яке брехало. | 2026-07-26 | Міграції `forbid_reply_under_deleted_parent` (заборона на вставці, маркер `parent_deleted` + людський текст «Цей коментар уже видалено» + прапорець `gone` → «Стрічка» перемальовує гілку) і `page_comment_counts_only_reachable` (подання рахує лише досяжні: спуск від живих коренів, `security_invoker` збережено) + ремонт даних (1 сирота мʼяко видалений). SQL: `scripts/supabase_comment_counts_reachable.sql`. **Доведено на живій базі:** відповідь під видаленого → відмова з правильної причини · відповідь під живого і кореневий коментар проходять (без регресу) · батько з іншого поста → відмова · число = «за логікою екрана» на всіх постах. |
| **B-24** | Фото у приватному чаті Дошки не доставляло сповіщення. Задеплоєна версія `send-chat-push` (v2, від 19.06) робила `msg.text.length`, а повідомлення тільки з фото має `text = null` (`src/core/supabase.js:690` пише `text: text \|\| null`) → `null.length` → 500 → отримувач без пуша. Фікс лежав у репо незадеплоєним 15 днів. **Масштаб доведено запитом до бази: 17 фото-повідомлень**, останнє 21.07 — кожне впало. | 2026-07-25 | **Деплой Edge Function через MCP `deploy_edge_function` → v3, ACTIVE, `verify_jwt: true`.** Git-коміту фікс не потребував — код у репо вже правильний з 10.07 (PR #321), не збігався лише прод. Звірено зворотним читанням задеплоєного коду (`get_edge_function`): `select('… text, photo_url')` + `bodyText = msg.text \|\| (msg.photo_url ? '📷 Фото' : '')` на місці. ⚠️ Помітку аудиту «перевірити `verify_jwt`, у робочих `false`» знято як хибну — `true` тут правильний (клієнтський виклик зі звіркою автора), `false` мають лише функції, які кличе база. |
| **B-26** | 🆕[знайдено Claude 25.07 при додаванні редагування коментаря] **Редагування ОБХОДИЛО антиспам.** `trg_page_comments_antispam` («Стрічка») і `trg_comments_antispam` (Дошка) стояли лише на `BEFORE INSERT`. Тобто фільтр перевіряв текст при створенні коментаря, але не при його правці — переписати вже опублікований коментар на матюк можна було вільно. У Дошці редагування існувало давно, тож дірка була відкрита й там. Доведено наживо: до фіксу правка на «хуйло» проходила в обох таблицях. | 2026-07-25 | Текст проганяється через `text_abuse_reason` і при UPDATE: у «Стрічці» — всередині `page_comments_guard_update` (щоб не плодити другий тригер на тій самій таблиці), у Дошці — новий `trg_comments_guard_update_antispam`. Міграція `comment_edit_support_and_antispam_on_update`. Після фіксу — заблоковано в обох, нормальна правка проходить. ⚠️ Номер саме B-26: B-25 зайняв баг клавіатури 26.07. |
| **B-23** | Дірка `pcom update` на `page_comments`: `with check (true)` дозволяла через API змінити **будь-яке поле** — підробити `author_uid` (чуже авторство), переписати чужий текст з-під адміна сторінки, підробити згадку `reply_to_uid`, перенести коментар на інший пост і **«воскресити» видалений (`deleted_at = null`) в обхід каскаду «одна сітка»**. | 2026-07-25 | Тригер `trg_page_comments_guard_update` (BEFORE UPDATE, функція `page_comments_guard_update`) — той самий патерн, що `messages_guard_own_edit` 08.07. Міняти можна лише `text` (і лише автору) та `deleted_at` (лише null → час); видалений коментар заморожено. Заразом `with check (true)` → той самий вираз, що в `using`. SQL: `scripts/supabase_comment_guard.sql`, міграція `guard_page_comments_update_fields`. **Доведено тестом: 4 дірки відкриті ДО фіксу → 9/9 зелених ПІСЛЯ**, каскад не зачеплено. |
| **B-01** | Деплой падав з помилкою "non-fast-forward" після зміни нікнейму GitHub (старий deploy.yml робив `git commit bundle.js → push main`, а main рухався). | 2026-04-10 | Закрито через архітектурне рішення А+ (новий підхід GitHub Pages Deploy Action не комітить у main взагалі — див. `docs/ARCHITECTURE.md`). Крок 3 Фази 1. |
| **B-04** | `.github/workflows/deploy.yml` видалено в `5763587`, автодеплой не працював. | 2026-07-04 | Закрито формально — підхід А+ (`deploy.yml`) давно на проді, деплой стабільно працює. Був помилково у «Критичні · в процесі». |
| **B-02** | `bundle.js` видалено (`5763587`). | 2026-04-10 | Вирішено Кроком 2 Фази 1 (`0e9dcd2`) — `bundle.js` відновлено, у git як робоча база. |
| **B-03** | `build.js` видалено (`5763587`). | 2026-04-10 | Вирішено Кроком 1 Фази 1 (`2e7309e`) — `build.js` відновлено. |
| **B-05** | `buses.js` ігнорував поле `days` — показував рейси у неділю для «пн-сб». | 2026-04-18 | `isDayActive()` + `kyivDayOfWeek()` у `matchesSearch()`. |
| **B-10** | Два набори слеш-команд (root `.claude/commands/` + дубль у `CSTL NEWS VOVA/.claude/commands/`). Claude Code підхоплює тільки root, sub-копія була джерелом розсинхронізації. | 2026-05-12 | Видалено sub-копію (6 файлів: `audit`, `fix`, `mockup`, `new-file`, `start`, `gemini`). Root лишається єдиним джерелом правди. |
| **B-07** | `src/tabs/submit.js` — форма «Подати новину» через `mailto:` з оманливим toast «Надіслано!». | 2026-05-12 | Файл `submit.js` видалено повністю. Замінено крутішим UX: кнопка-тригер у Дошці громади (`community.js`) → bottom-sheet модалка → submit-handler. Бекенд (Supabase) — у Фазі 3. |
| **B-18** | `src/tabs/submit.js` — PII (телефон/Instagram) зберігалися у localStorage без TTL. | 2026-05-12 | Файл `submit.js` видалено повністю. Нова форма у модалці нічого не зберігає у localStorage; дані підуть напряму у Supabase у Фазі 3. |
| **B-11** | `.DS_Store`×3 і `CSTL NEWS VOVA.zip` (36 KB) закомічені в git. | 2026-05-12 | Розширено `.gitignore` (`.DS_Store`, `*.zip`, `*.log`, `.idea/`, `.vscode/`, `.env`). Файли прибрано з git tracking через `git rm --cached`. |
| **B-09** | Мертвий CSS `.submit-form` / `.form-input` / `.submit-btn` (~32 рядки), стара картка `.news-card`. | 2026-05-13 | Прибрано при розбитті style.css на 8 модулів (`style/*.css`) — мертві блоки не перенесено. |
| **B-13** | `fetch` до Open-Meteo у `weather.js` без таймаута і AbortController. Висить безкінечно при поганому з'єднанні. | 2026-05-13 | Додано `AbortController` з 5-секундним `setTimeout(() => ac.abort(), 5000)`. `clearTimeout` у success і catch. |
| **B-12** | `news.js:42` featured = `articles[0]` без сортування — перша у JSON може бути стара. | 2026-05-13 | У `getFiltered()` додано `.sort((a,b) => (b.ts || 0) - (a.ts || 0))`. Featured тепер завжди найсвіжіша. |
| **B-17** | `events.js` сортування тільки за датою — при однакових датах події у порядку JSON. | 2026-05-13 | Додано вторинне сортування `(a.time || '').localeCompare(b.time || '')`. |
| **B-06** | `events.js` крихкий парсинг дати `new Date(now.toDateString())` — на iOS Safari ламається. | 2026-05-13 | Уже закрито у коді: `now.setHours(0,0,0,0)` + `new Date(e.date + 'T00:00:00')`. Підтверджено пошуком — крихких патернів нема. |
| **B-16** | `boot.js setupPWA()` створював дубль `<link rel="manifest">` через Blob URL — конфлікт із статичним з index.html, на iOS Safari нестабільно. | 2026-05-13 | Функцію `setupPWA()` повністю видалено з `boot.js`. Лишився тільки статичний `<link rel="manifest" href="manifest.json">` у index.html (повний з PNG-іконками). |
| **B-19** | Граматика "Вови" у документації: `"Воваа"`, `"Вовау"`, `"Воваом"`. | 2026-05-13 | `sed` по `docs/RULES.md` і `docs/CONCEPT.md`. Виправлено на правильні відмінки. |
| **B-20** | `sync.sh` робив `git pull origin main --no-rebase -X ours` — мовчки відкидав чужі зміни. | 2026-05-13 | Файл `sync.sh` видалено. ⚠️ **Опис потоку тут застарів (звірено 06.08):** було «робоча гілка → **auto-merge** → deploy». `auto-merge.yml` вимкнено й видалено 05.07 — пуш у `claude/**` більше нічого не деплоїть. Чинний потік: **робоча гілка → PR через `/finish` → squash-merge у `main` → `deploy.yml`**. |
| **B-08** | `sw.js` — `logo.png` мав не бути в `STATIC_ASSETS`. | 2026-05-17 | Фактично закрито давно — `sw.js:22` має `'./logo.png'` у списку precache. Аудит підтвердив, перенесено у Закриті. |
| **B-15** | `src/tabs/news.js` — 3× inline `onclick`. | 2026-05-17 | Event delegation: один listener на `#geo-filters` (через `data-geo`) і один на `#news-list` (через `data-article-id`). `setGeoFilter` / `openArticle` прибрано з `window.*`. |
| **B-21** | `src/tabs/community.js` + `community-blocks.js` — 7× inline `onclick="switchTab(...)"`. | 2026-05-17 | `data-switch-tab="X"` атрибути + один делегований listener у `attachSwitchTabDelegation()` на `#cm-content`. `switchTab` лишається глобальною (потрібна tab-bar). |

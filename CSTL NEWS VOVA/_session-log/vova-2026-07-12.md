# Журнал сесії — Вова — 2026-07-12

## /startuem
- Гілка сесії: `claude/startup-uem-vzafm0` (харнес закріпив цю назву; звичайна конвенція проекту — `vova/session-<дата>`, але змінювати гілку без потреби не буду, поки нема команди «Роби»).
- git identity: user.name/user.email виставлено на Вову.
- BYYOU_PLAN.md: paused-потік — це Ромина робота (Потоки 5-9), немає збігу гілки з Вовою → не підхоплюю, стартую звичайно.
- Прочитано: HOT_RULES, START_HERE, BOARD.md, SESSION_STATE_VOVA (хендовер 12.07 вечір), CSTL_BUGS (0 критичних), AUDIT (топ Вовиної зони).
- Monitor `owner-watch.sh` (стеження за Ромою) запущено — перший запуск видав повний дамп історії (baseline-файл `/tmp/cstl_watch_seen.txt` не існував) → зупинив, засіяв baseline поточними ref'ами, перезапустив чисто.
- BOARD.md: заявив активність (конфлікт-мердж з Роминим свіжим комітом #390 — вирішено, запушено в main напряму).

## /byyou Потік 10 — Д-5: двигун Обговорень з board.js (кроки 1-14 ✅)
- Знахідка планування: board.js — ОДИН конвеєр з if(type)-гілками, не два шматки; прямий виріз = циклічний імпорт (клас бага TDZ, як у Роминому C2). Рішення: 3 файли, односторонні імпорти + ін'єкція `getPosts` + подія `cstl-posts-changed` замість прямого renderBoard().
- Знахідка кроку 1: «спільних» хелперів менше ніж думав план — реально спільні лише закладки+шер; renderLoc/renderCardFoot/pluralAds — тільки оголошення; authorAvatar/clockTime — тільки обговорення. Мінімальний shared = менше ризику.
- Мертвий код: `partWord` (0 викликів), імпорт `submitPost` у board.js (реальний виклик у community-modal.js) — прибрано, не перенесено.
- Результат: board.js 2166→1241р; board-discussions.js 949р; board-shared.js 85р. `199afd9b` + CACHE `cstl-20260712-2010` (`1c0b6bd1`).
- Смоук Playwright (гість, 390px, fallback JSON): Дошка+Обговорення+модалки+фільтри+FAB — усі зелені, 0 pageerror. 2 фейли тесту по дорозі — інфраструктура тесту (клік у центр перекритий модалкою; ✕ пошуку рендериться лише з перебудовою шапки — поведінка ДО рефактору та сама), код не змінювався.
- Слова Вови: «Так. Тільки дуже детально, нічого не зламай» (Брама старту) → «Роби все детально. Нічого не зламай і не поруш».
- ⏸ Стою на Брамі деплою (крок 15) — чекаю слово «деплой». Пам'ятати: origin/main пішов уперед (Ромині #390-392, зокрема icons.js stroke-width) — перед PR merge origin/main, CACHE 2010 > їхнього 1946.
- «Деплой» отримано: merge origin/main (конфлікт лише sw.js CACHE — лишив 2010), rebuild, повторний смоук зелений, push, PR #393 → squash `72b0c39f`. Доки оновлено (BYYOU done, SESSION_STATE, AUDIT Д-5 закрито, BOARD + позначка Ромі про емодзі в новому файлі).

## Д-21 — фон Дошки: «темний корок» ЗАДЕПЛОЄНО (PR #395)
- Показав 6 живих варіантів фону (скріншоти реального застосунку з CSS-override): поточний корок / темний корок / бордовий оксамит / графітовий фетр / зелений фетр / нічний перелив. Вова обрав №2 «темний корок» («Давай темний корок подивимось, роби»).
- CSS-only (`style/community.css`): .board-bg = затемнення rgba(24,14,8,.62) + теплий radial-відблиск згори поверх ТОГО САМОГО cork2.png (фото збережено, відкат = 1 зміна); .bd-group-title/.bd-group-empty/.bd-empty(scoped через .board-bg ~) → кремові; .bm-preview-section (прев'ю форми) — те саме затемнення.
- Перевірено Playwright: дошка + групи НП (Дерно) + порожній НП (Котів) — текст читабельний, 0 pageerror.
- PR #395 → squash `ca761a95`, деплой #2728 success (звірено через api.github.com — прямий curl до github.io з пісочниці блокується). CACHE `cstl-20260712-2120`.

## Фікс свайп-закриття модалки (відрив липкої шапки на iOS) — PR #396
- Баг Вови (скрін): у модалці «Нове оголошення» прокрутив контент вниз → не відпускаючи розвернув палець і потягнув аркуш → липка шапка відривалась від тіла.
- Корінь: свайп застосовує transform:translateY() до .app-modal-sheet, що є ОДНОЧАСНО скрол-контейнером зі sticky-шапкою. iOS WebKit: transform на прокрученому контейнері зі sticky-дітьми рве їх. Охоронець scrollTop стояв лише на touchstart.
- Фікс (`core/modal.js`, спільний примітив): зсув-закриття лише при scrollTop===0 (як рідні аркуші iOS); прокручений контент спершу довертається нативним скролом. ⚠️ Дрібна зміна поведінки Д-17 (граб-за-шапку при прокрученому спершу довертає) — узгоджено з Вовою.
- Перевірено Playwright touch-жестами: регрес закриття з верху + фікс + довертання + ✕ + 0 pageerror.
- PR #396 → squash `f60eaca3`, CACHE `cstl-20260712-2135`.

## Приклади фону модалки «Подати оголошення» (обговорення, не деплой)
- Спершу зрозумів як «темний корок» під фон Дошки — показав 4 варіанти (білі/темні поля, теплий). Потім Вова уточнив: мав на увазі ПОТОЧНИЙ кремовий колір модалки, лише глибший/насиченіший. Показав 4 кремові (P0 #FAF8EF поточний / P1 #F2EBD8 / P2 #EDE3C9 / P3 #E7DABB). Питання закрито Вовою («Стоп, це питання закриваєм»), нічого не задеплоєно.
- Техніка макетів: реальна модалка через динамічний імпорт `/src/tabs/community-modal.js` (оминає requireAuth-гейт FAB) + override-CSS. 1:1 структура/іконки.

## Іконка автобуса у віджеті відстеження → нова (як таб-бар) — PR #397
- Після зміни таб-бар іконки Ромою (#390) стара прямокутна bhv4-bus-icon лишалась неузгодженою. Замінив на Tabler bus (ICONS.bus) у `buses.js`: bhv4-bus-icon (віджет відстеження, зверху-зліва) + bhv4-empty-icon (порожній стан). Лише path, класи/стилі збережено.
- Рухомий маркер 🚌 на карті маршруту — НЕ чіпав (рішення Вови).
- Перевірено Playwright: віджет = таб-бар, стара форма 0 у DOM, 0 pageerror.
- PR #397 → squash `97123e37`, CACHE `cstl-20260713-0625`.

## Соцмережі Olyka Castle у футері сайдбару — PR #398
- Уточнення Вови: це соцмережі ГОЛОВНОГО бренду Olyka Castle, не застосунку; без підпису — лише іконки. Посилання дав Вова (Instagram olyka_castle + Facebook share-лінк).
- icons.js: +brandInstagram/+brandFacebook (Tabler). sidebar.js: SOCIAL-константа + футер у renderNav, target=_blank rel=noopener, тап закриває сайдбар. sidebar.css: .sidebar-nav flex:1 + .sb-social-foot margin-top:auto (до низу), кнопки 44×44 бордовий контур.
- Мокап показано (2 ітерації: «Ми в соцмережах» → «Olyka Castle» → без підпису). Живий тест: href/aria/попап/закриття — ок, 0 pageerror.
- PR #398 → squash `eaecb3a3`, CACHE `cstl-20260713-0700`.

## FAB «Мої оголошення»: олівець → стандартний ICONS.pencil — PR #400
- Скарга Вови: FAB-олівець у «Мої оголошення» не стандартизований. Корінь: board-chat.js мав локальний EDIT_ICON_SVG (стара форма M12 20h9…), а меню/FAB Дошки — ICONS.pencil. Замінив на ICONS.pencil (вже імпортований).
- «Видимість незалежно від оголошень»: FAB рендериться безумовно в buildScreen (position:absolute до .pm-screen fixed) — уже завжди видно, у всіх 3 вкладках. Підтверджено скріном порожнього стану (fabVisible/matchesPencil true).
- Пауза по дорозі: Bash-класифікатор платформи ~хв був недоступний (інфраструктура) — правка лежала у файлі, добудував коли ожив.
- PR #400 → squash `92871ad6`, деплой #2745, CACHE `cstl-20260713-1122`.

## Парсер новин: без дубля заголовка/часу + абзаци як в оригіналі — PR #411
- Вова (скріни IMG_3235/3236, стаття Волинь Post «балістика 5000 км»): (1) опис в додатку починався з дубля заголовка + «Сьогодні, 13:45» — сторінки видавців повторюють h1+час у контейнері статті; (2) абзаци рвались на кожному вкладеному елементі — «РБК-Україна» (посилання) і «Володимир Зеленський» (жирний) окремими абзацами.
- Фікси `scripts/parse_rss.py`: (1) `clean_article_text(title=...)` зрізає перший абзац=заголовок (нормалізоване порівняння + хвіст « - ВолиньPost» ≤20 симв.), час зрізає наявний regex; title прокинуто через `fetch_full_article` (3 виклики) і застосовано до RSS-контенту; (2) новий `_paragraphs_fallback` замість `get_text('\n\n')` — межа абзацу ЛИШЕ блокові теги (div/p/h*/li/br), inline (_INLINE_TAGS: a/b/strong/em/span…) лишається всередині абзацу.
- Перевірка: 6 юніт-тестів + наскрізна симуляція розмітки volynpost (4/4 абзаци цілі, без заголовка/часу). Живий фетч з пісочниці неможливий (проксі 403 на зовнішні домени). API-тригер workflow_dispatch — 403 (обмежений токен, відома пастка) → фікс підхопить крон (кожні 30 хв). Старі статті лишаються як були (дедуп по URL).
- ⚠️ Зона формально Роми (новини) — Вова доручив напряму; Ромі на око: parse_rss.py змінено (clean_article_text має новий параметр, з дефолтом — зворотно сумісно).
- CACHE не чіпався (CI-скрипт, не код сайту). PR #411 → squash `a585177a`.

## Новини: іконки модалки — векторні + бордовий ✕ — PR #410
- Вова (скрін IMG_3233): у модалці новини кнопки share/save = емодзі → векторні у стилі додатку; ✕ закриття оформити як у модалці «Нове оголошення».
- share 📤/save 🔖/bell 🔔 → ICONS.share/bookmark/bell (news.js підставляє innerHTML у openArticle); `.modal-icon-btn` колір var(--red), svg 1em, активна = бордова заливка + біла іконка; index.html — прибрано емодзі зі статичних кнопок.
- ✕: спершу зробив ink-soft (сірий) → Вова уточнив скріном IMG_3234, що в «Нове оголошення» ✕ БОРДОВИЙ (`.app-modal--board-compose` малює #722F37 SVG-X) → `.modal-close-btn` color var(--red).
- Playwright: share/save svg (без емодзі), ✕ rgb(114,47,55), 0 pageerror.
- PR #410 → squash `93c834e6`, деплой #2783 success, CACHE `cstl-20260714-1116`.

## Громада: порядок екстрених контактів — PR #409
- Вова (скрін IMG_3231): екстрені контакти впорядкувати 101→102→103→104→112→Волиньобленерго (було 103,101,102,104,обленерго,112). Набір той самий, плитки вже клікабельні (tel:) — змінився лише порядок.
- Сортування в коді `renderContactsBlock` за `EMERG_ORDER=['101','102','103','104','112']`, решта (Волиньобленерго) — після. Дані `data/community.json` не чіпав (щоб не конфліктувати з Ромою).
- Playwright: порядок 101/102/103/104/112/обленерго, усі tel:.
- PR #409 → squash `6e2964e5`, деплой #2780 success, CACHE `cstl-20260714-1047`.

## Дошка: іконки в кнопках картки трохи більші — PR #408
- Вова (скрін IMG_3230): іконки дзвінка/чату в кнопках футера картки збільшити, кнопки лишити.
- `.cm-board-foot-actions .cm-board-call/.cm-board-msg-btn svg` 16→19px; кнопки 34px без змін.
- Модальні варіанти цих кнопок (окремі svg-розміри) — не чіпав.
- Playwright: кнопка 34px, іконка 19px.
- PR #408 → squash `f4f4c811`, деплой #2779 success, CACHE `cstl-20260714-1031`.

## Дошка: заголовок вкладки + лінія → темний корок #4E3820 — PR #407
- Вова (скрін IMG_3229): заголовок «ДОШКА ОГОЛОШЕНЬ» кремовий #F2E7D0 зливався зі світлою шапкою. Ітерації: показав v1-4 (світла карамель→бронза) → обрав вохру #B99562 (`8322f03a`) → «ще темнішим, як шапка віджета» → показав v5-7 → обрав v7 «корок» #4E3820. Заодно попросив лінію-роздільник під заголовком перевести з бордового в цей же колір.
- `.bd-title` color #F2E7D0→#4E3820, text-shadow 0.45→0.25 (м'якша, темний текст на світлій шапці); `.bd-title::after` градієнт rgba(92,33,40)→rgba(78,56,32).
- Playwright: колір rgb(78,56,32), лінія-градієнт у тому ж кольорі.
- PR #407 → squash `804162c4`, деплой #2778 success, CACHE `cstl-20260714-1024`.

## Віджет: назва в шапці по центру + ширший інтервал — PR #406
- Вова: назву в шапці розташувати по центру + збільшити інтервал між буквами.
- `.cmbw-head justify-content:center` (назва+іконка по центру); `.cmbw-title letter-spacing` 0.6→1.5px. Зміряв межу: 1.6px ще влазить (297px при доступних 303px), взяв 1.5px із запасом.
- Playwright: не обрізається, група по центру (зсув 0px).
- PR #406 → squash `4771d265`, деплой #2776 success, CACHE `cstl-20260714-0921`.

## Віджет: назва «Актуальні оголошення громади» + крапки під картки + шапка/дата — PR #405
- Пакет дрібних правок за фідбеком Вови (скрін IMG_3228 + обговорення назви):
  - **Назва** «ДОШКА ОГОЛОШЕНЬ» → «АКТУАЛЬНІ ОГОЛОШЕННЯ ГРОМАДИ» (віджет = швидке інформування, не дублікат вкладки; Вова накидав варіанти, обрав цей). `.cmbw-title` 14→13.5px, letter-spacing 1.1→0.6px — довша назва одним рядком.
  - **Крапки-індикатори** з правого верху шапки → ВНИЗ під картки по центру (класичний патерн каруселі); 5→6px; `:empty{display:none}` коли пар <2. У markup `.cmbw-dots` перенесено між стрічку і `.cmbw-foot`.
  - **Шапка/іконка** трохи більші (іконка 17→19px).
  - **Дата в один рядок** (`.cmbw-time` white-space:nowrap + flex:0 0 auto; `.cmbw-loc` min-width:0) — не переноситься коли локація «Олицька громада» довга.
- Показував макети (назва+«ГРОМАДИ», крапки вниз) — Вова підтвердив «Роби».
- Playwright: назва не обрізається, крапки під картками по центру, тап→стрибок, дата в один рядок при довгій локації, карусель/автоскрол цілі, 0 pageerror.
- PR #405 → squash `e5c00b48`, деплой #2774 success, CACHE `cstl-20260714-0903`.

## Віджет дошки: затемнені краї — рамка каруселі — PR #404
- Задум Вови (скрін IMG_3225): боки стрічки затемнити як шапку/низ → суцільна «рамка телевізора»; центральна пара яскрава й «виступає» (акцент), бічні картки заходять за темний край.
- Показав 3 сили (v1 делікатна / v2 середня / v3 сильна) — Вова обрав v2.
- Реалізація: 2 absolute-краї `.cmbw-edge--l/--r` у `#cm-board-content` (position:relative), градієнт `rgba(0,0,0,0.34)`→прозорий, ширина 32px; `z-1` під шапкою/низом (`z-2`), `pointer-events:none`. Разом з карусель-масштабом (бічні ~0.87) підсилює глибину.
- Playwright: краї не перехоплюють тапи (тап по картці→модалка), карусель/крапки/автоскрол цілі, 0 pageerror.
- PR #404 → squash `152c0949`, деплой #2773 success, CACHE `cstl-20260714-0838`.

## Дошка: кремовий заголовок вкладки + обрамлення віджета — PR #403
- **Заголовок вкладки Дошка** «ДОШКА ОГОЛОШЕНЬ»: бордовий `#5C2128` → кремовий `#F2E7D0` (як шапка віджета на Громаді), тінь бордова→темна. Вова просив «замінити бордовий на колір як на віджеті». Показав 4 темніші тони (v1-4) — не обрав, лишили кремовий.
- **Обрамлення віджета Дошки на Громаді** (Вова: стрілку з шапки вниз як кнопку «переглянути всі оголошення»; шапку затемнити в тон віджета + лінія-роздільник; смуги мають прилягати впритул і повторювати заокруглення блоку):
  - шапка (назва+крапки) → затемнена смуга `rgba(0,0,0,0.22)` + `border-bottom` кремовий 0.16; прилягає до верху, кути обрізає `.cm-block` overflow:hidden+radius.
  - **ключ до «впритул»:** контейнер `#cm-board-content` мав `.cm-loading` (padding 8/14/14) що лишався після рендеру → додав `el.classList.remove('cm-loading')` → padding 0, смуги на всю ширину.
  - стрілку прибрано з шапки; more-картку зі стрічки прибрано; нижня смуга-кнопка `.cmbw-foot` «Переглянути всі оголошення»+стрілка, симетрична (border-top), прилягає до низу; тап → вкладка Дошка (data-bw-more, уже в хендлері).
  - мертвий CSS `.cmbw-arrow`/`.cmbw-more`/`.cmbw-more-in` прибрано.
- Playwright: смуги затемнені й впритул (getBoundingClientRect vs блок <1px), лінії, кнопка→Дошка, карусель/крапки/автоскрол цілі, 0 pageerror. Повторно зелений після мерджу origin/main.
- Мердж main: конфлікти у 3 файлах віджета + журнал (взято наші; журнал — origin не мав запису #402 бо docs-коміт після squash). Маркери чисті.
- PR #403 → squash `561c0a8f`, деплой #2772 success, CACHE `cstl-20260714-0750`.

## Віджет Дошки, ітерації 2-3 за фідбеком Вови — PR #402
- **Іт. 2 (скріни IMG_3214/3221):** (а) випадковий порядок оголошень при кожному рендері (Fisher-Yates) — просуває і старі, не дублює «свіжі вгорі» вкладки; (б) перша пара стартує біля лівого краю; (в) плейсхолдер-обкладинки для карток без фото (макет затверджено).
- **Іт. 3 (нова вказівка):** (а) плейсхолдери СКАСОВАНО — картка без фото просто нижча, як на Дошці (стрічка `align-items:flex-start`); (б) карусель: масштаб картки = частка її видимої ширини (центральна пара scale 1, обрізані бічні ~0.87, плавно перетікає при гортанні; скейлиться внутрішня `.cmbw-in`, щоб не рухати снап-геометрію); (в) замість лічильника «N оголошень» — крапки-індикатори пар у шапці (стиль автобусного віджета), тап по крапці = стрибок на пару + пауза; (г) максимум 16 випадкових карток.
- **Пастки по дорозі:** `offsetLeft` рахується від offsetParent (+14px зсув) — снап-цілі рахувати різницею з першою карткою; mandatory-снап ВИКИДАЄ недосяжні (відʼємні) цілі — великий `scroll-padding-left` зробив ціль першої пари -12 і старт стрибав на пару 2 → фікс: `scroll-padding-left:16px` + `scroll-margin-left:12px` на непарних крім першої.
- Перевірено Playwright: 14 пунктів зелені (висоти, крапки, синхронізація активної, перетікання масштабів, тапи, пауза), 0 pageerror; повторно зелений після мерджу origin/main.
- Мердж main перед PR: конфлікти у 3 файлах віджета (squash #401 переписав історію) — взято наші новіші; звірка маркерів чиста (2 збіги `=======` — декоративні розділювачі коментарів CSS).
- PR #402 → squash `4e83ee5b`, деплой #2754 success, CACHE `cstl-20260713-1741`.

## Новий віджет Дошки на Громаді (повна переробка) — PR #401
- Задача Вови: старий віджет видалити повністю, новий «правильний з фоном», детально продумати UI/UX. Показав поточний + 2 макети (A стрічка / B колаж) — Вова обрав A з уточненнями: УСІ оголошення громади, автопрокрутка по 2, peek минулих зліва і наступних справа.
- Реалізація: renderBoardBlock переписано (community-blocks.js); темний корок Д-21 на .cm-block--board; стрічка scroll-snap на непарних картках (пари), відступи 30px+gap 12 → peek ~18px обидва боки; автопрокрутка 5с/пара, пауза-на-дотик 8с, цикл; картки: пін/фото/тег(реюз cm-board-cat)/заголовок/локація/дата; тапи: картка→openAdModalStandalone, шапка+«Всі»→вкладка. Слайд «Розмови» видалено; мертвий код ~170р JS + ~223р CSS вичищено; шапка секції «Дошка громади» прибрана (шапка тепер у віджеті).
- По дорозі зловив: шапка переносилась на 2 рядки (вузько) → title 13px/ls1.1 + nowrap.
- Playwright: рендер/peek/зсув рівно на пару за 5с/пауза/обидва тапи — зелені, 0 pageerror.
- PR #401 → squash `b9800667`, CACHE `cstl-20260713-1218`.

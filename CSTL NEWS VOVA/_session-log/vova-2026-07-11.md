# Журнал сесії — Вова, 2026-07-11 (гілка `claude/startup-uem-k2i4o6`)

## Старт
- `/startuem`: власник = Вова (пошта `illiabogdanets041@gmail.com` — незнайома, не Ромина → метод виключення). Прапорець власнику виставлено в 1-й відповіді.
- Прочитано обов'язкові: HOT_RULES, NEW_SESSION_PROMPT, BOARD, BYYOU_PLAN (done), SESSION_STATE_VOVA, AUDIT (розділ Дошки), CSTL_BUGS (🔴 нема), CLAUDE.
- Автостеження Роми (Monitor `owner-watch.sh`) запущено, git config = Вова.

## Робота: FAB Дошки
- **Задача (слова Вови дослівно):** «переходимо до кнопки FAB … Для початку трішки збільши кнопку FAB трішки». Погоджено конкретику: 50→56px. Вова: «Роби».
- **Зроблено:** `style/community.css` — `.board-trigger--fixed` width/height 50→56px; іконка `.cm-board-trigger-icon/close svg` 26→28px. CACHE `cstl-20260711-2030`. Коміт `0aeac112`.
- Чистий CSS — `bundle.js` не залежить (community.css підключається окремо через style.css).

## Інцидент координації — Рома задеплоїла PR #349 (перетин зон)
- Автостеження зловило: Рома змерджила в `main` **PR #349 «Обговорення: FAB-стандартизація, векторні іконки, лічильник лайків, фікс клавіатури»** (byyou Потік 5).
- **Перетин:** Д-22 (стандартизація ВСІХ FAB) — спільний механізм `.board-fab*`/`.board-trigger--fixed`. Рома **прибрала** окремий клас `.cm-board-trigger.disc-fab-plus` → FAB Обговорень тепер на спільному класі. Тобто моя зміна розміру автоматично зачепить і FAB Обговорень (для стандартизації — добре).
- **Рішення Вови:** «Роби, Деплой» → синхронізувати main і задеплоїти.
- **Синк:** `git merge origin/main` (усталений патерн проекту, не rebase — force-push у робочу гілку заблокований політикою). Конфлікт лише `CACHE_NAME` у sw.js (мій `2030` новіший за main `1845` → лишив 2030). community.css авто-змерджилось чисто. Merge-коміт `d680a91d`.
- `bundle.js` перезібрано (esbuild `npm install` щоразу заново) — збігається з main (JS не чіпав). node build.js exit 0, check-imports 27 файлів чисто.
- Delta проти origin/main: лише `community.css` (FAB) + `sw.js` (CACHE) — 4 рядки.

## Деплой FAB
- PR #350 → squash-merge у main (sha 7f6679d3). CACHE `cstl-20260711-2030`. Deploy-workflow запущено.

## Д-17: липка шапка модалки «Подати оголошення»
- **Слова Вови:** «Тепер задачі в кнопці FAB в вкладці подати оголошення, які там?» → «Починай з першої задачі» → спершу обрав Варіант B, потім «Ні, варіант А треба. Роби».
- **Знахідка:** опис Д-17 в AUDIT застарів — модалку Рома вже мігрувала на спільний примітив `core/modal.js` (Потік C2, PR #328), старих класів `.cm-board-modal-handle/close` нема. Скрол-контейнер = `.app-modal-sheet`; рисочка `.app-modal-handle` + ✕ `.app-modal-close` (absolute) їдуть зі скролом.
- **Варіанти:** A — scoped через `.app-modal--board-compose` (безпечно, примітив не чіпаємо); B — перебудувати примітив (усі sheet-модалки, зачіпає C1 Роми). Вова обрав **A**.
- **Зроблено:** `community.css` (біля наявного блоку `.app-modal--board-compose` Роми): `.app-modal-handle` → `position:sticky;top:0` full-bleed смуга (margin -14/-18px скасовує padding аркуша, фон `--bg-card`, бар через `::before`); `.app-modal-close` → `position:sticky;float:right;top:12px` + `margin-bottom:-32px` (прибрати висоту з потоку). Спільний `core/modal.js` НЕ чіпано.
- **Жива перевірка Playwright** (390px): тест-харнес `_d17_test.html` (видалений після) + скрін до/після скролу — рисочка+✕ лишаються зверху, форма їде під ними, контент не визирає над смугою. Геометрія: handleWidth 390 (full), closeVisible true.
- CACHE `cstl-20260711-2046`. Коміт `a7628f0f`. Синк main (merge `83a5691b`, конфлікт лише CACHE). Деплой — «Деплой» → /finish (PR# — див. BOARD).
- **Лишилось по модалці «Подати»:** Д-23 (категорія без автовибору), Д-24 (контакт автопідстановка+маска), Д-6 (прев'ю-картка, косметика).

### 🐛 Д-17 фікс (жива скарга Вови, скрін iPhone) — прозорість зверху
- **Скарга Вови (дослівно):** «Зверху прозорість, цей блок має повністю прилягати до верху модалки і фіксуватись». Скрін: чіпи категорій + затемнена «ДОШКА ОГОЛОШЕНЬ» просвічували НАД липкою шапкою.
- **Корінь:** iOS Safari не пінить sticky-елемент впритул при **негативному верхньому margin** (`margin-top:-14px` на рисочці). У Chromium-тесті не проявилось (тому пропустив), + перший харнес мав спрощену заглушку чіпів замість реальних 3 рядів.
- **Фікс:** `.app-modal--board-compose .app-modal-sheet` `padding-top` 14→**0**; рисочка `margin` `-14px -18px 12px` → `0 -18px 12px` (прибрано негативний верхній margin). Шапка тепер флеш до верху (`handleTop == sheetTop`, перевірено Playwright з РЕАЛЬНИМИ чіпами).
- CACHE `cstl-20260711-2340`. Коміт `ade0eed7` (+ merge). Деплой PR #352.

### ✨ Д-17 розширення (Вова, скрін iPhone) — заголовок теж у липку шапку + 🐛 прибрано дубль CSS
- **Слова Вови:** «все окей, тільки правильно працює зверху… тільки прикріпи ще [заголовок]… От я тобі виділив червоною лінією все те, що зверху має бути статичне». → «Деплой».
- **Зроблено:** `.app-modal--board-compose .cm-board-modal-title` → `position:sticky;top:22px` (липне під баром рисочки), full-bleed смуга + `border-bottom` (лінія-роздільник). Рисочка z-index 6, заголовок 5, ✕ 7 (top:24, на рядку заголовка). Тепер уся шапка (рисочка+заголовок+✕) статична, підзаголовок+форма скроляться під нею.
- **🐛 Знайдено й прибрано дубль:** блок `.app-modal--board-compose .app-modal-handle/::before/close` був **задвоєний** (наслідок squash-merge PR #351/#352 + merge-back — типова пастка; стара `-14px` версія перекривала фікс). Був **на проді**. Прибрано → детерміновано.
- **Текст заголовка:** лишили «Нове оголошення» (рішення Вови — деплой без зміни тексту).
- **Жива перевірка Playwright** (реальні чіпи): рисочка флеш 84.4, заголовок липне під баром (повна ширина), ✕ на рядку заголовка, підзаголовок+форма під шапкою. CACHE `cstl-20260712-0030`. Коміт `7b541580` + merge `05482a95`.

### 🎚️ Д-17 полиш лінії (фідбек Вови) — лінія лише при скролі
- **Слова Вови:** «коли моделька повністю розкрита… ця лінія трішки збиває… зроби так, щоб лінія з'являлася тільки тоді, коли… весь нижній блок з даними починає заходити за шапку… має плавно з'являтися». Спершу просив прибрати зовсім (коміт `34db0626`), потім уточнив — показувати лише при скролі.
- **Зроблено:** слухач скролу в `openBoardModal` (`community-modal.js`) ставить `.is-scrolled` на `.app-modal-sheet` при `scrollTop>2`; `community.css` — `border-bottom:transparent` за замовч. (місце зарезервоване, без стрибка) + `transition:border-color .2s` + `.is-scrolled .cm-board-modal-title{border-bottom-color:rgba(0,0,0,.07)}`. JS+CSS → bundle перезібрано.
- **Жива перевірка Playwright:** спокій — border `rgba(0,0,0,0)` (нема), скрол — `rgba(0,0,0,0.07)` (є), плавно. CACHE `cstl-20260712-0100`. Коміт `<scroll-line>` + merge Роминого #354 (`90c06cb9`, конфлікти: CACHE + мій scroll-варіант title vs завжди-лінія main + BOARD; лишив свій).

### 🎨 Д-17 шапка — полиш вигляду (Вова, скрін)
- **Слова Вови:** «олівець зробив векторним; нове оголошення щоб великими буквами було, розмір не змінюй; справа іконка хрестик… додай їй трішки тіні бо вона зливається з фоном». → «Деплой».
- **Зроблено:** (1) ✏️ emoji → вектор-олівець — локальний `PENCIL_ICON_SVG` у `community-modal.js` (мірор `EDIT_ICON_SVG` з board.js; НЕ імпортую звідти — board.js імпортує цей файл → був би циклічний імпорт), у заголовку `<span class="cm-board-title-ic">`. (2) `.cm-board-modal-title` `text-transform:uppercase` + `display:flex;align-items:center;gap:8px` (розмір 18px не змінено). (3) `.app-modal--board-compose .app-modal-close` `box-shadow:0 1px 5px rgba(0,0,0,.16)`.
- **Жива перевірка Playwright:** textTransform=uppercase, svg 22px (не emoji), closeShadow присутня. CACHE `cstl-20260712-0115`. Merge #356 Роми (тільки CACHE-конфлікт).
- ⚠️ **Інцидент:** тест-харнес `_d17_test.html` випадково закомічений (через `git add -A` у merge) → протік на прод через squash #357. Прибрано (PR #358). Урок: тест-харнеси надалі в scratchpad (поза репо), не в корені.

## Д-23: категорія без автовибору (сесія Вови, гілка `claude/startup-uem-k2i4o6`)
- **Слова Вови:** «Д-23» → «Роби» → «Деплой».
- **Зроблено (`community-modal.js` + `community.css`):** (1) `state.category` `'продам'`→`''` на старті (жоден чіп не активний). (2) Сабміт: перевірка `!state.category` **першою** — тост «Оберіть категорію оголошення» + `scrollIntoView` до `#bm-chips`, блокує публікацію. (3) `renderPreview`: приглушений плейсхолдер-тег `.cm-board-cat--placeholder` («Категорія», сірий курсив) поки не обрано; після вибору — кольоровий тег категорії.
- **Жива перевірка Playwright** (харнес у scratchpad, абсолютні CSS-шляхи — не в репо): 0 активних чіпів на старті, плейсхолдер color rgb(154,154,154). CACHE `cstl-20260712-0130`.
- **Лишилось по формі:** Д-24 (контакт автопідстановка+маска), Д-6 (прев'ю-картка косметика).

## Д-23 полиш + плавне свайп-закриття (сесія Вови)
- **Слова Вови (скрін):** «біля категорії додай малесеньку зірочку як біля заголовка… обов'язково»; «рисочка зверху = свайп-закриття… дуже важливо щоб при закритті НЕ дьоргалась, плавно; неважливо чи до половини дотягну, має бути плавно».
- **Зроблено:** (1) `community-modal.js` label «Категорія *» (реюз `.bm-label-req`). (2) 🤝 **Спільний примітив `core/modal.js`** — свайп touchend: при закритті (dy>90) панель тепер плавно доїжджає `translateY(100%)` з відновленою CSS-transition ПЕРЕД `close()`; раніше close() лишав інлайн `transform:translateY(dy)`+`transition:none` → панель застигала й різко зникала (дьоргання). Відскок (<90) вже був плавний.
- **Жива перевірка Playwright** (синтетичні touch на справжньому примітиві через http+ES-модуль): close → `translateY(100%)`+transition 0.25s+removed; snap → transform очищено+лишається відкрита; 0 помилок.
- **⚠️ Ромі:** зачепив її C1-примітив `core/modal.js` — покращення закриття для ВСІХ sheet-модалок (Погода/Сайдбар/Світло/Акаунт), не конфлікт. CACHE `cstl-20260712-0145`.

## Edge Function ai-analytics-summary (прохання Роми через Вову)
- Вова: «чи ти можеш зробити щоб я не заходив». → Задеплоїв Ромину Edge Function `ai-analytics-summary` (Потік 2, AI-кнопка статистики) з репо-файлу через Supabase MCP `deploy_edge_function`. Status ACTIVE, v1, verify_jwt:true. Таблиці `admins`(2)/`analytics_events`(96) на місці; пересланий Ромою текст мав копіпаст-артефакт (розрив у `api.anthropic.com`) — репо-файл чистий, деплоїв його. ⚠️ Секрет ANTHROPIC_API_KEY з коду не перевірити (Вова казав що додав). Позначено Ромі в BOARD (її ручна дія закрита).

## Д-24: поле Телефон — автопідстановка + маска (сесія Вови)
- **Рішення Вови:** «Додати колонку [phone]»; поле = **тільки телефон, Telegram прибрати** («поки не треба»).
- **БД:** `apply_migration add_phone_to_profiles` — `alter table profiles add column if not exists phone text` (success). Заразом кабінет («Телефон (для оголошень)») почне зберігати (раніше губився, був тост-попередження).
- **Код `community-modal.js`:** `maskUaPhone`/`phoneDigits` хелпери; state.contact дефолт `+380`; автопідстановка `p.phone` у getProfile-блоці (guard `phoneDigits===0` — не перебиває введене); label «Телефон (необов'язково)»; input type=tel + маска на `input` (формат+обмеження 9 цифр); сабміт блокує неповний (1-8 цифр); прев'ю+payload показують/шлють лише повний номер (9), інакше null. Прибрано мертву `isPhone`.
- **Перевірка:** node --check+build+юніт-тест маски (9 кейсів: `+380`/`050…`/`+380…`/overflow — усі коректні). CACHE `cstl-20260712-0200`.
- 🔴🐛 **Вова знайшов на проді 2 баги свайпу** (регрес/експозиція після мого PR #360 в примітиві): (1) при закритті свайпом контент рухається 2× і відривається; (2) прогорнув модалку → свайп від шапки не закриває, лише рухає текст. Корінь: слухач `passive:true` не блокує нативний скрол (transform+scroll разом=2×) + touchstart виходить якщо scrollTop>0. **Фікс — наступним заходом після деплою Д-24** (примітив, passive:false+preventDefault на драг-вниз + граб від шапки незалежно від скролу).

# Журнал сесії — 2026-07-29 (сесія N)

**Гілка:** `claude/startuem-jxjipg` · **Production:** `main`
**Старт:** `/startuem`

---

## Крок 0 — старт

- `git config user.name "Вова"` + `user.email vitocorleone191@outlook.com` — виконано.
- `git fetch origin` + `git pull origin main` → `Already up to date`, HEAD = `3a0fa124`
  (`auto(vopas): розклад автобусів 29.07 19:23 UTC`).
- Прочитано: `HOT_RULES.md`, `START_HERE.md`, `SESSION_STATE_VOVA.md`, `AUDIT_2026-07.md`,
  `CSTL_BUGS.md`, `CLAUDE.md`, `ВОВА_ПРОФІЛЬ.md`, `NEW_SESSION_PROMPT.md`, `BYYOU_PLAN.md`.

## Знайдена розбіжність у документах

`BYYOU_PLAN.md` каже **`Статус: active`**, гілка `claude/startuem-yxqfd9`, кроки 1-14
«усі локальні коміти, **БЕЗ push**». Насправді робота **вже в `main`**:
- `f8621d0f feat(chat): розмова = людина, оголошення — контексти всередині (#715)`
- `605a7caf fix(ci): check-imports читав /* у HTML-атрибуті як блок-коментар (#716)`

Перевірка: `git merge-base --is-ancestor 47c4198e HEAD` → NO (бо squash-мердж створює
новий коміт), але зміст потоку в `main` є. Тобто відкритим лишився лише **крок 15 —
смоук на iPhone**, а статус файлу треба перевести в `done`.

---

## Хід сесії

### Контекст задач прийшов СКРІНШОТАМИ, не з пам'яті
У Вови закінчились ліміти в попередньому акаунті, той чат недосяжний. Він надіслав два
знімки з формулюваннями. Дослівно з них:
- «В категорії "ГРОМАДА" новини повинні зберігатись місяць»
- «Також всі кремові кольори, які знаходяться в вкладці дошка і в всіх її пунктах
  підменю треба замінити в такому стилі як ми міняли фон, десь такі самі десь інша
  тональність якщо треба але суть ти зрозумів я думаю» · **«І роби все разом»**

Я той чат НЕ бачу — тому все перевірив по коду наживо, а не «згадував».

### Задача 1 — Громада живе місяць (коміт `445e5e1b`)
**Це виявився БАГ, не нова фіча.** Правило вже було написане в `prune_by_age`
(`scripts/parse_rss.py`), але одразу після нього стояв `all_articles[:MAX_ARTICLES]` —
сліпий зріз за ДАТОЮ, без огляду на розділ.

Заміряно на живому `data/articles.json`:

| Що | Число |
|---|---|
| Статей у файлі | 400 з 400 (стеля вперта) |
| Приплив за добу | 130 · 132 · 121 |
| Найстарша стаття | 3.2 дні |
| Статей Громади | 4 (з них 3 ексклюзиви) |
| Найстарша Громади | позиція **365 з 400**, вік 2.7 дні → мала померти наступної доби |

Тобто фактичне зберігання було ≈3 доби для ВСІХ розділів — не 30 для Громади і навіть
не 7 для Волині. Ексклюзиви `prune_by_age` свідомо не чіпає, а зріз їх усе одно вбивав.

Фікс: `cap_articles()` + `MAX_COMMUNITY = 120` — квота Громади ВСЕРЕДИНІ тих самих 400
(вага завантаження не зростає: клієнт тягне файл цілком, `src/tabs/news.js:97`, 1.4 МБ).

Стенд `scripts/test_retention.py` — 16 перевірок. **Контроль:** підмінив `cap_articles`
на старий зріз і прогнав той самий стенд → впало рівно 5 перевірок (ті, що про Громаду
й ексклюзиви). Тобто стенд справді ловить цей баг, а не малює зелене.

⬜ **Свідомо НЕ зроблено:** Волинь/Україна досі не доживають своїх 7 днів — стеля тисне
і їх. Це окреме рішення з реальною ціною: 7 днів × 130/добу ≈ 900 статей ≈ 3 МБ на
кожне відкриття. Виніс окремим пунктом, чекає слова Вови.

### Задача 2 — кремові поверхні Дошки і підменю (коміт `051d8812`)
Родина токенів у `base.css`: `--board-card #FFFFFF` · `--board-line #C4C4C1` ·
`--board-press #E4E4E1`. **Натиск підібраний числом, не на око:** стара пара
білий↔`--paper-deep` давала контраст 1.271, нейтральний `#E4E4E1` до білого дає 1.274.

Змінено: меню категорій/локації · кружечок × у пошуку · натиски «написати»/«подзвонити»/
слота фото · бар вводу · бар «Відповідь/Редагування» · чіпи-фільтри · кружечок
«відповісти» · смужка чіпів контекстів + картка під нею · лист дій над повідомленням.

Наслідок, закритий тут же: чіпи контекстів були БІЛІ на кремовій смужці — на білій
смужці вони б зникли. Тому неактивний чіп тепер заглиблений (`--board-bg`).

Лист дій узяв `--board-bg`, а не `--board-line`: його фон видно у щілинах `gap:1px` між
кнопками І у 8px просвіті над «Скасувати» — суцільний #C4C4C1 дав би там сіру плиту.

**Обговорення НЕ зачеплені** — усе спільне (`.bd-*`) правлено scoped під `#board-content`.

Сторож `tests/board-cream.mjs` — 28 перевірок. Міряє ЖИВУ теплоту (R−B) обчислених
кольорів, а не наявність слова в CSS. **Контроль:** на старих кремових значеннях ловить
13 теплих поверхонь; Обговорення при цьому лишаються теплими (`#EAE4D2`, теплота 24).

### ⬜ Знайдено, але НЕ чіпав (HOT_RULES №9 — сказати, а не прибрати)
Мертвий код, який теж кремовий, але його ніхто не бачить:
- `.bd-tab` / `.bd-tab--active` (`community.css`) — 0 входжень у `src/` та `index.html`
- `.board-bg--plain` — 0 входжень
- `.chats-entry` + `.gr-avatar` / `.gr-mbr` (`messages.css`) — хаб «Чати» розформовано,
  кнопка «Групи» має `style="display:none"` (`index.html:109`)

### Здача
- `node build.js` → exit 0 (`check-imports`: 40 файлів, проблем нема)
- `npm test` → **23 стенди / 300 перевірок** зелені
- `python scripts/test_retention.py` → 16/16
- `CACHE_NAME = cstl-20260729-2126`
- `bundle.js` перегенерований: у git лежала версія ДО фіксу `47c4198e` (CI не комітить
  збірку назад), тепер збіг зі `src/`
- Знімки: `tests/tools/_out/board-1-top.png`, `chat-3-три-контексти.png`, `chat-4-список.png`

### Публікація (`/finish`)
Вова: **«Продовжуй»** (після мого питання «Робити /finish?») — читаю як дозвіл публікувати.

- `git merge origin/main` — 8 комітів, ВСІ автопарсери (новини/автобуси/`vopas-fetched`).
  Конфліктів нема, `sw.js` не зачеплений (звичний конфлікт CACHE_NAME цього разу не спрацював).
- **Діагноз підтвердився ВДРУГЕ і терміново:** після мерджу найстаріша стаття Громади
  («Костел Пресвятої Трійці») зсунулась з позиції 365 на **388 з 400** за кілька годин.
  До видалення лишалось 12 позицій — наступний прогін парсера її б зніс. Фікс встиг.
- Перепрогін після мерджу: `node build.js` exit 0 · `npm test` 23/300 · `test_retention.py` 16/16.
- `CACHE_NAME` переставлений на фактичний час: **`cstl-20260730-0134`** (був `2126` з
  попереднього прогону — працював би, бо відрізняється від `main`, але як позначка часу брехав).
- `CLAUDE.md` оновлено: числа стендів були **20/237** (стало 23/300) і `--board-bg` стояв
  `#E1E5EB`, хоча чинний **`#E6E6E3`** — обидва відставали. Заразом описана нова родина токенів.

🟡 **Чекає Вову:** живий смоук на iPhone + слово, чи виносимо Волинь/Україну.

---

## Друга частина сесії (30.07) — АУДИТ ДОШКИ + потік /byyou на 14 кроків

**Замовлення Вови дослівно:** «Тепер проведи повний детальний аудит дошки, включаючи як
головну сторінку так і кнопку FAB разом зі всіма вкладками підменю і кожній деталі в ній.
З технічної сторони та з сторони UI UX, скажи що ми не взяли до уваги, що пропустили або
що могли зробити краще, якщо все окей то так і напиши, але май свою думку, не піддакуй
мені і оціни свою роботу в цій вкладці від 1 до 10.» + `/byyou`.

### Аудит → `_ai-tools/AUDIT_DOSHKA_2026-07.md` (коміт `76f05400`)
Самооцінка **6/10**. Три дефекти виявились МОЇМИ, з кремового проходу кількома годинами
раніше: межі на листі подачі з контрастом **1.007** (тобто їх не існувало). Я заявив у
коміті, що «пройшов хвіст» — і не пройшов.

Окремим розділом виписав те, що **перевірив і НЕ підтвердив** (згортання шапки надійне,
XSS немає, кнопки 36px — мертвий код), щоб не роздувати список знахідок.

### Перебіг потоку
Вова: **«Стартуємо»** → зробив усі 🟢 і зупинився перед двома 🔴.
Далі **«Крок 7 роби»** і на крок 9 — заперечив моєму варіанту питанням «а куди вмістити
остальні пункти меню?» та запропонував свій: іконка FAB міняється за станом.
Я не погодився, виклав чотири аргументи, запропонував варіант В (винести «Повідомлення»
у шапку). Вова: **«Мій давай»** → зробив його.

### 🔴 Дві мої помилки в самому аудиті
1. **Д-В4** «фокус не переїжджає» — неправда, `.focus()` там уже був.
2. **Д-В1** «кнопка бреше текстом + провал WCAG 2.5.3» — теж неправда: текст на кнопці
   схований (`display: none`), а `display:none` прибирає вузол і з дерева доступності.
   Тобто мій найсильніший аргумент проти ідеї Вови стосувався брехні, якої не існувало.
   **Його інстинкт виявився точнішим за мій аналіз** — реальна нестача була саме та, що
   іконка не відповідала головній дії.

### Що ловило мене по дорозі (щоб не повторювати)
- **Чотири рази** — зворотні лапки в коментарі всередині шаблонного рядка (закривають
  рядок). Тричі в стендах, раз у `board.js`.
- **Один раз закомітив ЗЛАМАНУ збірку** (порушення HOT_RULES №5): `node build.js | grep
  | tail` віддає статус `tail`, не `node`, тож `&&` пропустив падіння. Виправив амендом,
  далі писав `node build.js > log; echo $?`.
- **Контроль стенда FAB** спершу через `HEAD~1..3` — бомба з таймером, за кілька комітів
  падав би сам. Переписав на синтез стану «до» з чинного джерела.
- **Регулярка** читала `calc()` через `[^)]*` і спинялась на першій дужці — не збігалась
  ніколи. Читати до `;`.
- **Сцена стенда з 9 картками** відтворила ту саму випадковість, що на проді → контроль
  провалився. Потрібна парна кількість.

### Публікація
Вова: **«Деплой»**. Мердж `main` → три конфлікти (`sw.js`, `base.css`, `board-cream.mjs`),
усі «моє нове проти нічого в main» (PR #717 прийшов squash-ом, у гілці лежали оригінали).
Розвʼязано вручну, `-X theirs` НЕ використовувався. `npm test` 26/331 після розвʼязання.
PR **#718** → squash `8686cd93` → деплой **#3545 success** (07:16:44 → 07:17:26 UTC).
`CACHE_NAME = cstl-20260730-0532`.

---

## Третя частина (30.07, після деплою #3545) — фікс модалки + позначки

### Скарга 1: відрив шапки модалки (PR #719)
Вова зі скріншотом: «якщо скролю донизу і не відпускаючи палець від екрану починаю
скролити до верху, то верхня шапка починає відриватись від верху модалки».

**Корінь.** Лист подачі — сам собі скролер, шапка (рисочка/заголовок/✕) — `sticky`
ВСЕРЕДИНІ нього. Запобіжник `if (panel.scrollTop > 0)` тримав, ПОКИ контент іще
прокручений. Коли контент доходив до верху посеред жесту, `scrollTop` ставав 0,
перевірка вимикалась — і код ставив `transform` на скролер зі sticky-дітьми. Тобто
він не ПРИБИРАВ відрив, а ВІДКЛАДАВ його рівно до тієї миті, коли Вова його й бачив.

**Фікс:** замок `wasScrolling` — жест, що хоч раз був прокруткою, до `touchend` не
стає закриттям. У СПІЛЬНОМУ примітиві `core/modal.js`, не копією.
Плюс `overscroll-behavior-y: contain` на лист подачі.
⚠️ Наслідок: граб за шапку прокрученого аркуша більше не закриває одразу. Чекає
слова Вови на телефоні.

### Прохання 2 і 3 (той самий PR)
- Число в FAB-меню → накладка на кут кружечка. Заміряно: у плашці пункт 198→233px
  (+35px), тепер 198→198px.
- Крапка «є нове» 8px на іконці вкладок **Дошка** і **Обговорення**. Мережі не
  додано: Дошка з кешу `_unreadChats`, Обговорення — локальний підрахунок.
- 🔴 **Стрічку НЕ позначаємо.** Я запропонував три варіанти, Вова спитав «як краще
  і надійніше». Відповів не «(а) чи (б)», а що питання інше: `fd-bell` у Стрічці це
  перемикач push-підписки, а НЕ вхідні — крапці нікуди вести. Спершу екран
  «Відповіді». Вова: **«То давай без стрічки»**.

### Що ловило мене (щоб не повторювати)
- **Зворотні лапки в коментарі всередині шаблонного рядка** — ПʼЯТЬ разів за сесію.
  Запропонував механічну перевірку в `check-imports.js`, чекає слова Вови.
- **Контроль стенда двічі виправив МЕНЕ, не код:** (1) сценарій жесту стартував палець
  у тілі вже прокрученого аркуша, де жест узагалі не починається (`touchstart` має
  свій `return`) — нічого не рухалось навіть без замка; (2) регулярка мала вікно 400
  символів проти заміряних 488.
- **Force-push заборонений** → мердж застарілої віддаленої гілки. Перед розвʼязанням
  довів через `git diff origin/main HEAD`, що різниця — рівно нова робота.
  Автомердж дав **дубль `refreshUnreadBadge`** — збірка впала, прибрав руками.

### Публікація
PR **#719** → squash `0fac23f4`. ⚠️ Прогін мого мерджу **#3548 = cancelled**: за пів
хвилини прийшов автокоміт парсера і GitHub витіснив деплой новішим. Не втрата —
`0fac23f4` предок `7b8c55c3`, і деплой **#3549 success** повіз мою роботу
(звірено: `CACHE_NAME`, `tab-dot`, `wasScrolling` у `origin/main`).
`CACHE_NAME = cstl-20260730-0827` · **28 стендів / 355 перевірок**.

### ⬜ Знайдено, НЕ чіпав (HOT_RULES №9)
1. `board-discussions.js` ~211: `t > dividerTs`, де `t` — ISO-РЯДОК, а `dividerTs` —
   число мс. `Number(ISO)` = NaN → умова ЗАВЖДИ false, тобто роздільник
   «Нові повідомлення» там, найпевніше, не зʼявляється ніколи.
2. `_session-log/vova-2026-07-28c.md` містить ЗАКОМІЧЕНІ маркери конфлікту
   `<<<<<<< HEAD` з 28.07 — сміття в тексті документа.

# Журнал сесії — 2026-07-24 (c)

> Живий журнал (веду по ходу). Гілка `claude/startuem-sq0h2w`. Production: `main`.

## Старт
- `/startuem` виконано. git config → Вова. `git pull origin main` = up to date (свіжий).
- Прочитано: HOT_RULES, START_HERE, SESSION_STATE_VOVA, CSTL_BUGS, BACKLOG, AUDIT_2026-07, CLAUDE.md, NEW_SESSION_PROMPT.
- `/byyou` статус = `idle` (нема paused-потоку).
- Останній живий CACHE: `cstl-20260724-1651`. Усе задеплоєно в main.
- Активна тема останніх сесій: візуальний редизайн «Стрічки» (feed) — верхній блок кружечків + сторінка спільноти.

## Кроки

### 1. Фото в постах Стрічки — динамічна пропорція (задача Вови зі скріна IMG_3545)
- **Слова Вови (дослівно):** «Я закинув пост з фотографією, і ця фотографія вже не така вертикально довга… Її можна було вмістити в повному розмірі, але її обрізали чогось по боках. Треба налаштувати динамічно, щоб такі фотографії… показувались у нормальну ширину-висоту. А фотографії, які вже геть високі вертикально, потрібно показувати найкращим способом… Так, як можливо зараз вони.»
- **Корінь:** `style/feed.css:124` — `.fd-photo--single img { aspect-ratio: 3/4; object-fit: cover }`. Кожне одиночне фото силою заганялось у рамку 3:4; фото ≈1:1 ширше за рамку → cover масштабує по висоті → зрізає боки («К» і «Я» у «КУЛЬТУРА ПОЧИНАЄТЬСЯ»). Галерея (2+): `max-height:520px` + cover — та сама хвороба + різна висота слайдів.
- **Рішення Вови (AskUserQuestion):** стеля **3:4 — як зараз** (високі фото не міняють вигляду), «Роби».
- **Зроблено:**
  - `src/tabs/feed.js` — `PHOTO_MIN_AR = 3/4`, `applyPhotoRatio(box,img)` (читає `naturalWidth`/`naturalHeight`, пише `--fd-ar` = `max(w/h, 0.75)`), `wirePhotoRatios(root)` (guard `data-ar-wired`, гілка `img.complete` для кешу + `load` once). Виклик — першим рядком `wireGalleries` → усі 3 наявні call-site (стрічка `renderFeed`, екран сторінки ×2) підхоплюють без змін.
  - `style/feed.css` — `.fd-photo--single img` → `aspect-ratio: var(--fd-ar, 3 / 4)`; `.fd-gal-slide img` → те саме + прибрано `max-height:520px` (це був кап 3:4 лише для екрана 390px; тепер кап робить пропорція на будь-якій ширині). Карусель бере пропорцію ПЕРШОГО фото → слайди однакової висоти, свайп не стрибає.
  - Fallback `3/4` поки фото вантажиться → рамка тримає місце, стрічка не стрибає.
- **Перевірка:** `node --check` OK · `node build.js` чисто (35 файлів, check-imports 0) · Playwright 390px, 0 помилок консолі. Заміри рамки: 1:1 → 390×390 (ціле), 4:5 → 390×488 (ціле), 9:16 → 390×520 (кап 3:4, обрізка лише верх/низ), 16:9 → 390×219 (ціле). Скріншот: сині маркери країв фото видно на ВСІХ 4 → боки не ріжуться ніде.
- CACHE `cstl-20260724-1755`.
- ⚠️ Не робив: збереження розмірів фото в БД при завантаженні (прибрало б і мікро-осідання рамки при першому показі) — потребує міграції `page_posts`; поточне рішення працює і для старих постів без міграції.

### 2. /byyou з іншого акаунта
- Сліду в репозиторії НЕ БУЛО: жодної гілки з новою роботою, `BYYOU_PLAN.md` = `idle`.
- Темою потоку став дзвіночок сповіщень (крок 4 нижче).

### 3. Смужка скролу — прибрана глобально (скрін IMG_3541)
- `.app-main` ховав її з 20.07, але скролерів у додатку 25+ → сторінка спільноти показувала. Одне глобальне правило в `base.css` замість точкових. CACHE `cstl-20260724-1808`.

### 4. /byyou — надійний дзвіночок сповіщень «Стрічки» (12 кроків)
- **Слова Вови:** «цей дзвіночок повинен працювати дуже чітко… одразу запросити дозвіл… сповіщення мають надходити в будь-якому випадку… Це все ніби працює над цим команда Instagram.»
- Рішення Вови на брамах: тригер у базі (не клієнт) · режим /byyou · стеля фото 3:4 · сповіщення лише на НОВІ пости (не на редагування).
- Знайдено 6 причин збою + 1 незнайдена дірка (in-app банер не показував пости). Деталі — `BYYOU_ARCHIVE_2026-07-24_bell-push.md`.
- **Корінь мовчання на iPhone:** дозвіл питався після `await` → жест користувача згорав.
- **Зміна плану по ходу:** відмовився від `service_role`+Vault (довелося б просити ключ у переписці) на користь власного секрету в `app_secrets`.
- **Власний баг, спійманий на живій перевірці:** виходи «нема підписників/пристроїв» не прибирали журнал → гонка губила сповіщення. Фікс `bail`.
- **Мій же тест спершу впав** (очікування `null` замість тексту про відхилений дозвіл) — помилка тесту, не коду.
- Деплой: PR #574 (клієнт, run #3262 success) + #575 (сервер). Тригер і функція v3 накатані через MCP після авторизації конектора.
- **⏸️ За Вовою:** живий тест на 2 акаунтах з телефона.

### Інциденти процесу
- Форс-пуш і `git reset --hard` заборонені харнесом → замість обходу зробив зустрічний мердж старої вершини гілки у пересаджену. Пуш пройшов без форсу.
- Хук `pre-edit-read-check` зупинив Edit файлу, який я сам щойно створив — виконав Read, як велить правило.
</content>
</invoke>

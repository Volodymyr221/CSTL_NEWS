# Живий журнал — Вова, 2026-07-15

## Сесія
- Гілка: `claude/startup-uem-ty2znc` (харнес закріпив; у main тільки через /finish).
- Акаунт: `illiabogdanets041@gmail.com` → за виключенням = Вова.

## Задача: Редизайн екрана «Мій кабінет»
**Бриф Вови (скрін = ціль, поля inline):** повний UI/UX редизайн `openAccount()` без зміни структури/порядку/логіки/елементів. iOS-26/Apple HIG стиль, бордова айдентика лишається.

**Рішення власника:**
- Скрін — ціль (не поточний стан; поточний код на main — проста форма без іконок/стрілок).
- Поля лишаються inline-редагування, стрілка `›` — декоративна.
- «Роби» дано після ASCII-макета.

### Файли
- `src/core/account-ui.js` — розмітка `.acc-f` (іконка-плитка + підпис/значення + вектор-стрілка), блок «Моє» (іконка+назва+опис), «Сповіщення» (консистентні плитки). id полів і хендлери НЕ чіпав.
- `style/account.css` — переписано hero (аватар 72→88, badge), `.acc-cab-sec`, `.acc-f*`, `.acc-cab-save`, `.acc-cab-row*`.
- `sw.js` — bump CACHE_NAME.

### Іконки (з core/icons.js, Tabler): user, clipboard, calendar, phone, pin, home, fileText, chevronRight, megaphone, bookmark, message.

### Хід
- ASCII-макет показано → Вова «Роби».
- `account-ui.js`: додав локальні хелпери `field(ic,label,control)` (рядок анкети: плитка-іконка + `.acc-f-body`[підпис+поле] + `.acc-f-chev` вектор-стрілка) і `navRow(go,ic,name,desc)` (блок «Моє»: плитка + назва+опис + стрілка). Секцію «Мої дані» переписав через `field()` — **id полів (cf-name…cf-bio) і всі хендлери НЕ чіпав**. Блок «Моє» → `navRow()` з описами. «Сповіщення» — обгорнув у `.acc-cab-row-ic`/`.acc-cab-row-body` для консистентності (тумблери працюють як були).
- `account.css`: hero аватар 72→88 + тінь; камера 28→30; типографіка (ім'я 24 / email 14 / place 12.5); trust → badge зі скло-блюром. `.acc-cab-sec` r16→20, більший padding. `.acc-f*` повністю новий — flex-рядок iOS Settings, плитка 36px `rgba(114,47,55,.08)`, роздільник-псевдоелемент `left:65px` (під текст, не на всю ширину). `.acc-cab-save` height 52 / r16 / тінь. `.acc-cab-row*` — плитка+назва+опис. Приховав нативний глиф date-пікера (`::-webkit-calendar-picker-indicator{opacity:0}`).
- Іконки: user/clipboard/calendar/phone/pin/home/fileText + chevronRight (усі реальні з `core/icons.js`).
- `sw.js`: CACHE `cstl-20260715-0746` → `cstl-20260715-1336`.
- **Перевірка:** `node --check` ок; `node build.js` — 31 файл, imports чисто; bundle містить нові класи (8×), синтаксис ок. **Візуальний смоук:** ізольований прев'ю (реальний account.css + icons.js, мок даних, 390px) → Playwright скрін, **0 pageerror**, вигляд 1-в-1 з ціллю (плитки/стрілки/роздільники/badge/аватар).
- ⏳ Не задеплоєно — робоча гілка. У main через `/finish` коли Вова скаже «готово».
- 🔎 Реальний ручний тест на iPhone за Вовою (клавіатура в inline-полях, збереження анкети, тапи «Моє», тумблери).
- ✅ ЗАДЕПЛОЄНО: PR #419 → main → деплой #2822 success. CACHE `cstl-20260715-1336`.

## Задача 2 (той самий день): 3 фікси зі скріна Вови (кабінет)
**Вова (дослівно):** «МІЙ КАБІНЕТ в шапці … зроби великими буквами, та я не можу поставити фото в профіль, вибиває помилку … Також це сповіщення не видно на сторінці кабінету, бо воно під сторінкою … зроби всі такі технічні сповіщення перед всіма сторінками».
1. **Заголовок «МІЙ КАБІНЕТ» великими** — `style/account.css` `.acc-cab-top b` +`text-transform:uppercase; letter-spacing:0.6px`. Звірено скріном.
2. **🔴 Фото/анкета не зберігались — «permission denied for table profiles».** Корінь у БАЗІ, не фронті: `supabase_reputation_hardening.sql` (08.07) зробив `revoke update on profiles` + повернув грант лише на `(uid,name,email,birth_date)`; розширені колонки (surname/phone/settlement/street/bio/avatar_url) не додали (хоч коментар це передбачав). Звірено на живій БД (`information_schema.role_column_grants`): UPDATE був лише 4 колонки. **Фікс:** `grant update (surname,phone,settlement,street,bio,avatar_url) on public.profiles to authenticated, anon` — застосовано через MCP apply_migration (`profiles_grant_extended_update_columns`, project `uabyfecseqnemvcqhdem`). `trusted`/`approved_count` НЕ чіпав (захист від самопризначення довіри лишається). Звірено після: UPDATE-грант тепер 10 колонок, без trusted/approved_count. Запис у репо: `scripts/supabase_profiles_grant_extended.sql` + позначка в hardening-SQL. **Клієнт не змінювався.**
3. **Тости ховались під кабінетом** — `style/base.css` `.toast` z-index `3000→4000` (кабінет `.acc-cab`=3100 перекривав; тост=прямий child body, тож z-index достатньо). Тепер над усіма повноекранними сторінками.
- `sw.js`: CACHE `cstl-20260715-1336` → `cstl-20260715-1431` (змінились account.css+base.css).
- Перевірка: `node build.js` чисто; Playwright-прев'ю — заголовок великими, 0 pageerror.
- ✅ ЗАДЕПЛОЄНО: PR #420 → main → деплой #2823 success. CACHE `cstl-20260715-1431`.

## Задача 3: Картка профілю (тап по аватару) + перегляд/видалення фото
**План затверджено (ExitPlanMode).** Рішення Вови: картка чужого = фото+ім'я+громада+badge «Довірений автор»+«у громаді з рік» (без «про себе»); видалення свого фото — меню по камері; тап відкриває картку скрізь де є аватар.
- **БД (RPC):** новий `get_public_profile(p_uid)` — SECURITY DEFINER, віддає РІВНО 6 полів (uid,name,avatar_url,settlement,trusted,created_at), НІКОЛИ phone/email/birth_date. `scripts/supabase_public_profile.sql`. 🔴 **НЕ застосовано — MCP apply/execute просять апрув після рестарту; Вова застосовує вручну (блок дано).** Фронт fail-soft: без RPC картка показує фото з кешу + «Житель громади».
- **supabase.js:** `fetchPublicProfile(uid)` (обгортка RPC, fail-soft→null).
- **utils.js:** винесено спільний `openPhotoLightbox(url)` (з board-chat `openPhoto`, переюз `.pm-lightbox`); board-chat кличе спільний.
- **profile-card.js (НОВИЙ):** `openProfileCard(uid)` (fetch→`openModal` sheet: велике фото тап→lightbox, ім'я, 📍громада, badge, «У громаді з рік») + `initProfileCardTaps()` — ОДИН делегований слухач на document по `[data-av-uid]`. Імпорт+init у `app.js`.
- **account-ui.js:** кнопка-камера → меню (нема фото: одразу вибір; є: «Змінити/Видалити»). Видалення = `saveProfile({avatar_url:null})` + літера + `updateHeaderBtn` + тост.
- **account.css:** `.pcard*` (картка) + `.acc-avmenu*` (меню камери). Дошка не чіпана (автор там текстом, без аватар-кружечка → «скрізь де є аватар» = обговорення+чати).
- `sw.js`: CACHE → `cstl-20260715-1502`.
- **Перевірка:** `node build.js` чисто (32 файли; лінтер хибно ловив метод-шорткат `onMount(){}` → змінив на `onMount:()=>`); Playwright-прев'ю картки+меню — 0 pageerror, вигляд ок (фото/громада/badge/«у громаді з», літера-fallback, меню Змінити/Видалити).
- ⏳ Публікується цим `/finish` (RPC застосовано власником). Живий тест на 2 акаунтах за Вовою.

## Задача 4: 2 фікси картки профілю (z-index) — репорти Вови зі скрінів
1. **Меню камери ховалось під кабінетом** + **картка не відкривалась у чаті дошки** — один корінь: `.app-modal` (примітив) z-index 1250 < кабінет 3100 / чат `.pm-screen` 2401 → sheet рендерився ПІД ними. Фікс: новий модифікатор `.app-modal--top { z-index:3500 }` (modal.css) на картці (profile-card.js) і меню камери (account-ui.js). `.pm-lightbox` 2600→3600 (фото поверх картки). Укладання: чат2401/кабінет3100 → картка3500 → лайтбокс3600 → тост4000.
2. **Ім'я в шапці чату не відкривало картку** (лише аватар мав `data-av-uid`) — додав `data-av-uid` на `.pm-head-titles` (board-chat.js). + делегований слухач тепер ПРОПУСКАЄ рядки списку розмов (`[data-thread]`) — там тап відкриває саму розмову, не картку (інакше після z-index-фіксу було б подвійне спрацювання).
- CACHE `cstl-20260715-1848`. `node build.js` чисто.

## Задача 5: Доробка контенту картки профілю (план затверджено)
**Рішення Вови:** дата приєднання = місяць+рік («з липня 2026»); вік = показувати число (RPC віддає лише обчислене число, не дату народження); «Про себе» = ЛИШЕ якщо користувач щось написав.
- **RPC розширено:** `get_public_profile` тепер віддає +`bio` +`age` (`extract(year from age(birth_date))` — лише число, birth_date НЕ віддається). `scripts/supabase_public_profile.sql`. 🔴 Вова застосовує вручну (MCP-write блокується).
- **profile-card.js:** мета-лінія `📍громада · N років` (кожне опційне); bio-блок «ПРО СЕБЕ» (лише якщо непорожнє); футер «Учасник CSTL LIFE з <місяць> <рік>». Хелпери `pluralYears` (1рік/2-4роки/5+років, виняток 11-14) + `joinDate` (реюз `MONTHS_GEN` з chat-core.js).
- **account.css:** `.pcard-meta`/`.pcard-dot`/`.pcard-bio`/`.pcard-bio-h` (bio — м'який блок, pre-wrap, overflow-wrap:anywhere).
- CACHE `cstl-20260715-2114`. Перевірка: `node build.js` чисто; Playwright-прев'ю (повна+мінімальна картка, відмінки віку) — 0 pageerror, вигляд за макетом.
- ⏳ Чекає: (1) Вова застосує RPC, (2) «Фініш». Фронт fail-soft: без RPC показує ім'я+громаду+badge+футер (bio/вік з'являться після RPC).

## Задача 6: Громада — привітання з кабінетом + «ШО В СЕЛІ?» + фокус-скрол (план затверджено, «Роби»)
**Рішення Вови:** секція = «ШО В СЕЛІ?» попри збіг зі вкладкою (перейменування вкладки — відкрите); іконку кабінету з шапки ПРИБРАТИ зовсім; ефект — делікатний.
- **index.html:** `#account-btn` видалено з шапки (лишились бургер+лого+закладка).
- **account-ui.js:** `updateHeaderBtn` → узагальнений `refreshAccountButtons()` (усі `[data-account-btn]`, кожна кнопка тримає свою дефолтну іконку в dataset; export для community.js); клік — делегований слухач на document (покриває кнопку, що рендериться пізніше).
- **community.js:** `.cm-greeting` → flex-рядок (колонка дата+текст зліва, `button.cm-greet-account[data-account-btn]` справа — скляне коло 46px, фото профілю через refreshAccountButtons). `fitGreeting()` — один рядок: nowrap + зменшення шрифту 27→мін.19px по scrollWidth (виклик у initCommunity + updateGreetingName). Секція `<header class="cm-sec-head">ШО В СЕЛІ? / Ось що головне у нас сьогодні</header>` перед першим блоком. `initCenterFocus()` — фокус-скрол: passive scroll на `.app-main` + rAF-guard; на кожен блок t=dist/(vh·0.55) → scale 1−0.05t, opacity 1−0.22t (безперервно, transform+opacity=композитор); найближчий до центру отримує `.cm-block--focus` (глибша тінь); скидання для блоків поза екраном; guard по dataset.tab='community'; вимкнено при prefers-reduced-motion; перерахунок на resize/cstl-tab-changed.
- **community.css:** `.cm-greeting` flex+`.cm-greeting-col{min-width:0}`; `.cm-greet-account` (скло: rgba .13 фон, бордер .55, blur 6, тінь; av-мініатюра на всю площу); `.cm-sec-head` (h2 21px/800/ls 2.2/scaleY 1.12 білий+тінь — прийом .bd-title; p 13.5 приглушений; z-index:1 над обоями); `#cm-content .cm-block{will-change:transform,opacity; transform-origin:center; transition:box-shadow}` + `--focus` тінь.
- `sw.js` CACHE `cstl-20260715-2345`.
- **Перевірка (E2E на РЕАЛЬНОМУ застосунку, python http.server + Playwright, 390px, гість):** headerAccountBtn=false; greetBtn=true; привітання 1 рядок (27px; «Доброї ночі, Володимир!» → 26px, влазить); secHead/сабтайтл ок; 6 блоків; скрол → центр scale 0.997+focus, далекі 0.95/op 0.78; тап по колу → «Приєднайтесь»; 0 pageerror. Скрін збігається з фото-прикладом Вови.
- ⏳ На гілці, чекає «Фініш». Живий тест плавності на iPhone за Вовою. 🤝 Ромі: зона Громада (розмітка привітання/секції) + шапка (видалено кнопку) + підзаголовок «Ось що головне…» повернувся як підзаголовок СЕКЦІЇ (вона прибирала його з привітання 08.07) — рішення Вови.

## Задача 7 (16.07, ті самі зміни Громади — 3 виправлення Вови після живого тесту)
**Вова (дослівно):** «Блоки не потрібно робити прозорими вони повинні бути такими як є… що в селі Це неправильне місце розташування Це треба буде переддумати… бургер перемістив вправо, в самий кінець, зліва біля нього іконку збереження а назву CSTL LIFE вліво».
1. **Фокус-скрол без прозорості** — прибрано `style.opacity` з `initCenterFocus` (лишились масштаб −5% + тінь фокуса); `will-change: transform` (без opacity).
2. **Секцію «ШО В СЕЛІ?» прибрано** (розмітка + CSS) — місце розташування передумаємо окремо (коментарі-мітки лишено в коді).
3. **Шапка:** лого CSTL LIFE вліво (бургер з header-left прибрано), справа — закладка, бургер у САМИЙ правий край (порядок header-right: saved-hub → sidebar-toggle).
- CACHE `cstl-20260715-2357`. Смоук: порядок шапки ✓, зліва лише лого ✓, secHead відсутній ✓, скрол — scale ставиться/opacity порожня ✓, бургер на новому місці відкриває сайдбар ✓, 0 pageerror. 📌 Знахідка: у сайдбарі вже є пункт «Особистий кабінет» → вхід у кабінет доступний з усіх вкладок через меню.

## Задача 8 (16.07): «ШО В СЕЛІ?» — липкий заголовок секції (хореографія Вови, «Роби» після концепту)
**Вова:** заголовок = сукупність нижніх блоків; при скролі підіймається і «гальмує в шапку», блоки ховаються ПІД нього; зверху лишаються «ШО В СЕЛІ?» + іконка кабінету.
- **community.js:** секція повернулась як `header#cm-sec-head` (h2+p+друга кнопка `data-account-btn`) + вартовий `#cm-sec-sentinel` перед нею. Детект прилипання в `initCenterFocus`: розрив панель↔вартовий > живого flex-gap контейнера (+2px) → клас `--stuck`. Працює і при prefers-reduced-motion (скляний фон = читабельність, не рух; фокус-масштаб і далі вимикається).
- **community.css:** `.cm-sec-head` position:sticky top:0 z-index:2 (блоки z-index:1 пірнають під); у потоці — 21px по центру + підзаголовок, прозорий фон поверх фото; `--stuck` — компактна панель: бордове скло rgba(92,37,44,.82)+blur(10), h2 17px, підзаголовок згортається (max-height 0), кнопка кабінету 38px справа проявляється (до того opacity:0 — щоб не дублювалась з кнопкою привітання). Переходи 0.25s.
- **🐛 Знайдено і виправлено 2 пастки в ході тесту:** (1) детект «top панелі ≤ top .app-main» хибний — sticky пінить на 56px ПІД фіксованою шапкою, а .app-main починається з 0 → перейшов на вартового; (2) розрив у спокої ≠ 0 — це flex-gap (14px) контейнера #cm-content між УСІМА дітьми → поріг = живий rowGap+2 (не магічне число).
- CACHE `cstl-20260716-0017`. E2E: верх — не прилипло/21px/кнопка прихована ✓; глибина — прилипло/17px/підзаголовок 0/кнопка видима, тап → «Приєднайтесь» ✓; 0 pageerror. Скрін панелі = концепт.

## Задача 9 (16.07): «ШО В СЕЛІ?» — друга ітерація хореографії («пограємось», Вова)
**Вова:** фраза на місці підпису фото (підпис ТИМЧАСОВО прибрати); скролиться з контентом БЕЗ зміни розміру; доїжджає до місця привітання → призупиняється; під нею легенький стильний блюр на всю ширину без різких країв; блоки ховаються під блюр; іконка кабінету «нікуди не дівається».
- **community.js:** підпис фото `.cm-hero-sub` закоментовано (повернути легко); кнопка кабінету ВИНЕСЕНА з привітання в окремий прибитий елемент `.cm-acc-pin` (sticky нульової висоти, перший child #cm-content, `margin-bottom:-14px` гасить flex-gap) — кнопка стоїть у правому верхньому куті ВЕСЬ скрол; `.cm-sec-head` без кнопки (лише h2+p).
- **community.css:** `.cm-acc-pin{sticky; top:6px; z3; height:0}` (top:6px = стартовій позиції в потоці — кнопка не пливе ні на піксель, звірено координатами 322,74 в обох станах); `.cm-sec-head` — розмір НЕ міняється (21px завжди), кольори адаптивні: у потоці бордовий на світлому фрості (перший прогін показав: білий на фрості вицвітає), при `--stuck` білий+тінь; `--stuck` = blur(9px) + rgba(30,16,18,.3) + маска-градієнт (низ розчиняється до прозорого — «не обрізається по краях») на всю ширину. Привітання: `padding-right:64px` під прибиту кнопку.
- CACHE `cstl-20260716-0037`. E2E: розмір 21px в обох станах ✓; блюр лише при stuck ✓; підпис фото прихований ✓; iconStaysPut=true (координати ідентичні) ✓; тап по кнопці → вхід ✓; 0 pageerror.
- Домовленість: якщо вигляд не сподобається після ігор — повертаємо як було (блоки + «Олика» з підписом).

## Задача 10 (16.07): 4 деталі «ШО В СЕЛІ?» (Вова після живого тесту v2)
1. **Підпис фото повернено** у стилі підпису фотографії: курсив, serif (Georgia), 12.5px, приглушений (`.cm-hero-sub`).
2. **«Білий блок» заокруглено:** фрост-стрічка `.cm-hero-blurband` — border-radius 26px зверху; верх площини тепер = верх «ШО В СЕЛІ?» (wireHeroBlur: block = #cm-sec-head, fallback стара картка); верхній край маски різкіший (6%→3%) щоб кути читались.
3. **3D-тінь фрази:** h2 подвійна делікатна тінь (0 1px 2px + 0 3px 8px, бордово-темна ~0.18/0.12), p — легша; у stuck-стані білі тіні як були.
4. **Відступи:** `.cm-sec-head` padding 10/26 → 8/12 (фраза ближче до верху блока, блоки ближче до фрази); у `--stuck` padding-bottom 26 повертається (зона розчинення маски не зачіпає текст), transition padding.
- CACHE `cstl-20260716-0105`. E2E: heroCaption=true, iconStaysPut=true, блюр лише stuck, тап → вхід, 0 pageerror. Скрін: кути блока видно, підпис-курсив, тінь делікатна.

## Задача 11 (16.07): ВІДКАТ регресії фрост-смуги (моя помилка в задачі 10)
**Вова (дослівно):** «Чого бобра [фрост-смуга] тепер піднімається не з блоками як раніше, а під надписом «ШО В СЕЛІ?»? Я це не просив». Далі: «Виправи, Деплой».
- **Корінь:** у задачі 10 я перечепив `wireHeroBlur` за `#cm-sec-head` — фрост підіймався ПІД фразу, наповзаючи на фото костелу, а не разом з першою карткою як завжди.
- **Фікс community.js:** `wireHeroBlur` `cache()` → `block = document.getElementById('cm-news-board')` (стара робоча поведінка — фрост іде РАЗОМ з блоками).
- **Фікс community.css:** `.cm-hero-blurband` — прибрано `border-radius:26px 26px 0 0` (повноширинна смуга left:0/right:0 не «білий блок», заокруглення не мали сенсу поза карткою); край маски повернено 3%→6% (щільніший верх, як до задачі 10).
- **Лишено недоторканим** з задачі 10 (Вова НЕ скаржився): підпис фото курсивом, 3D-тінь фрази, тісні відступи sec-head.
- Rebuild OK (32 файли), bundle звірено (`block = document.getElementById("cm-news-board")`). CACHE bump цим `/finish`.

## Задача 12 (16.07): блоки ближче до «ШО В СЕЛІ?» (скрін Вови)
**Вова:** «підніми нижні блоки трохи вище, щоб була така відстань як на скріні».
- **community.css:** `.cm-sec-head` padding-bottom 12→4px (у потоці). Перша картка (ТАБЛО НОВИН) піднялась на 8px ближче до підзаголовка: загальна відстань фраза↔картка ≈26px → ≈18px (padding 4 + flex-gap контейнера 14). Проміжки МІЖ блоками (14px) не чіпав. `--stuck` padding-bottom 26px (під блюр-маску) недоторканий.
- CSS-only, bundle без змін. CACHE `cstl-20260716-0638`.

## Задача 13 (16.07): фраза «ШО В СЕЛІ?» вище + глибша тінь (скрін Вови)
**Вова:** «саму фразу підніми трішки вище, разом зі всім що є знизу... таких самих відступів як на фото. Та добав цій фразі глибоку тінь, щоб вона не зливалась з фоном».
- **community.css (CSS-only):**
  1. `.cm-hero-spacer` height 180→150px — уся група (фраза + підзаголовок + усі блоки) піднялась на 30px вище разом; внутрішні відступи фраза↔блоки (4px) незмінні. Спейсер = розпірка в потоці під фіксоване фото (560px); менша розпірка → контент стартує вище.
  2. Тінь h2 фрази: `0 1px 2px .18 + 0 3px 8px .12` (світло-бордо) → `0 2px 3px rgba(40,15,20,.42) + 0 5px 16px rgba(40,15,20,.28)` (темніша, глибша — фраза піднялась ближче до фото, не зливається). Підзаголовок p: тінь .10 → .30.
- Bundle без змін (CSS). CACHE `cstl-20260716-0657`.

## Задача 14 (16.07): фразу опущено на 18px впритул до блоків + 3D-emboss тінь
**Вова:** «опусти на 18px фразу... блоки, починаючи з блоку новин, залиш на місці. Добав більше тіні... щоб не зливалась з фоном, типу як 3д вигляд, може по іншому».
- **community.css (CSS-only):**
  1. `.cm-hero-spacer` 150→168px (фраза+блоки вниз 18px) + `#cm-content #cm-news-board { margin-top: -18px }` (блоки назад угору 18px). Підсумок: фраза сідає впритул над блоком новин (проміжок був рівно 4px padding + 14px flex-gap = 18px), блоки не зрушились. Stuck не чіпається (spacer впливає лише на стартову позицію).
  2. Тінь h2 — 3D-emboss (по-іншому, як Вова припустив): 3 шари — світла верхня грань `rgba(255,250,245,.55)` + близька різка `rgba(35,12,16,.52)` + дальній ореол `rgba(35,12,16,.34)`. Літери «підняті» над фоном. Підзаголовок p — світла грань + тінь .34.
- Bundle без змін. CACHE `cstl-20260716-0720`.

## Задача 15 (16.07): stuck-фраза по центру іконки + 3-шарова тінь у білому стані
**Вова:** «тих самих три шари зберігається коли фраза міняє колір на білий? Зроби щоб фраза зупинялась рівно по центру іконки».
- **community.css (CSS-only):**
  1. `.cm-sec-head--stuck` padding-top 8→18px. Геометрія: іконка `.cm-acc-pin` sticky top:6 + кнопка absolute top:12 + висота 46 → центр іконки 41px. Фраза при padding-top 18 центрується на ~41px = рівно центр іконки. Плавно (transition padding 0.3s). Resting (у потоці) не зачеплено.
  2. Тінь у білому (stuck) стані — раніше ОДНА (`0 2px 8px .55`), тепер 3 шари як у бордовому: `0 1px 0 rgba(0,0,0,.28)` (верхня грань темна — на білих літерах світла не читається) + `0 2px 6px .55` + `0 7px 18px .42`. Підзаголовок p — темна грань + тінь. 3D-об'єм зберігається в обох станах.
- Bundle без змін. CACHE `cstl-20260716-0748`.

## Задача 16 (16.07): 3-шарова тінь верхній зоні + іконка кабінету більша/замінена
**Вова (обвів верхню зону):** «добав таку трьохшарову тінь і верхньому заголовку і підзаголовку зверху, також іконці особистого кабінету. Але вигляд іконки кабінету трохи збільшити та замінити іконку людини».
- **community.css:**
  1. `.cm-greeting-text` (привітання) і `.cm-greeting-date` (дата) — тінь стала 3-шарова (темні шари: грань + близька + дальня), як у «ШО В СЕЛІ?». Було по одній тіні.
  2. `.cm-greet-account` (іконка кабінету): 46→50px; box-shadow → 3 шари (плаваюче коло). `.cm-acc-pin .cm-greet-account` top 12→10 — центр іконки лишається 41px (щоб stuck-центрування task 15 не збилось: 6+10+50/2=41).
- **community.js:** SVG іконки замінено з тонкого контурного user (stroke) на заповнений filled-user (fill=currentColor, 26px) — чистіше на малому розмірі.
- Rebuild OK, bundle звірено (нова svg). CACHE `cstl-20260716-0808`.

## Задача 17 (16.07): сайдбар відкривається справа + легкий блюр фону
**Вова:** «відкриття сайдбару перемісти на сторону бургера [справа], та коли відкривається сайдбар фон повинен бути легенько заблюрений».
- **sidebar.css (CSS-only, JS без прив'язки до сторони):**
  1. `.sidebar` left:0→right:0, transform translateX(-102%)→translateX(102%) (виїжджає з правого краю, з боку бургера), box-shadow 6px→-6px (тінь на лівому краю панелі).
  2. `.sidebar-overlay` + `backdrop-filter: blur(3px)` (+webkit); затемнення rgba(0,0,0,.42)→.32 (легше, щоб блюр читався).
- Bundle без змін. CACHE `cstl-20260716-0941`.
- Відкрите: хрестик ✕ лишився у правому куті панелі — запропонував Вові перенести вліво (до внутрішнього краю), чекаю рішення.

## Задача 18 (16.07): табло новин — повний розмір знизу, звуження лише вгору за центр
**Вова:** «перше табло новин щоб коли вилазить знизу до верху — вже звичайного розміру як по центру; а коли з центру ховається за верхню шапку — звужувалось. Решта віджетів — як зараз».
- **community.js `initCenterFocus`:** для `#cm-news-board` поки blockCenter > viewCenter (нижче центру, піднімається знизу) — `scaleDist=0` → scale 1 (не звужується). Вище центру — звичайна dist → звужується. Окрема `scaleDist` (лише масштаб) vs справжня `dist` (детект фокуса) — щоб табло не хапало підсвітку сидячи внизу. Решта блоків симетрично як було.
- Rebuild OK, bundle звірено (scaleDist×2). CACHE `cstl-20260716-1008`.

## Задача 19 (16.07): заокруглені внутрішні кути сайдбару
**Вова:** спершу «кути зправа», потім уточнив: «не права, а ліва частина — там де sidebar кінчається і починається блюр; та що прилягає до екрана не заокруглювати, заокругли протилежну».
- **sidebar.css (CSS-only):** `.sidebar` border-radius `0 18px 18px 0` (перша спроба — праві кути) → `18px 0 0 18px` (ліві/внутрішні кути верх+низ, де панель межує з блюром; правий край до екрана прямий).
- Bundle без змін. CACHE `cstl-20260716-1031`.
- Урок: «зправа/зліва» для right-drawer плутається — уточнив у Вови, з другого разу правильно.

## Задача 20 (16.07): блоки трохи нижче від «ШО В СЕЛІ?»
**Вова:** «опусти трохи всі блоки починаючи з ТАБЛО НОВИН і всі що знизу, від ШО В СЕЛІ?».
- **community.css (CSS-only):** `#cm-content #cm-news-board` margin-top -18→-4px. Уся група блоків опустилась на 14px від фрази; проміжок фраза↔перша картка ~14px (= flex-gap між рештою блоків, рівномірно). Скасовує частину задачі 14 (де я підтягнув впритул margin -18).
- Bundle без змін. CACHE `cstl-20260716-1047`.

## Задача 21 (16.07): редизайн «лист + язичок» — iOS-стиль налягання на фото («Роби, будем дивитись»)
**Вова:** фраза на випуклому язичку з заокругленими кутами; тілесний блок налягає на фото з тінню; заокруглити кути блока; «має виглядати не як-небудь, а як сучасний iOS додаток». Перед кодом — макет (правило mockup): 6 ітерацій макета (компактніший язичок → глибша тінь → знахідка: затемнення фото маскувало тінь → варіант «тільки назва в язичку» відкладено на потім).
- **community.js:** розмітка — сентінел+sec-head+усі блоки обгорнуто в `.cm-sheet`; фрост-смугу видалено повністю (div з hero, функцію `wireHeroBlur`, прапорець `_heroBlurWired`, виклик) — непрозорий лист її закривав, backdrop-filter палив би GPU даремно.
- **community.css:**
  - `.cm-sheet`: margin 0 -14px (повна ширина), paper, radius 26px зверху, тінь `0 -4px 10px .42 + 0 -22px 46px .38`, flex column gap 14, padding 0 14 4.
  - `.cm-sec-head` → margin -46px 0 0 (язичок виступає над листом на фото), padding 0; `.cm-sec-head-in` = язичок: paper, radius 18 18 0 0, padding 8/20/5, тінь `0 -3px 7px .42 + 0 -12px 28px .40`; h2 21→19px, p 13.5→12.5.
  - `--stuck`: пігулка розчиняється (bg transparent, тінь/radius/padding 0, transition) — блюр-панель як була; padding-top 18 (центр = іконка) збережено.
  - Видалено: `.cm-hero-blurband`, `.cm-hero::after` (беж-перехід), `#cm-news-board margin-top` (тепер gap листа). Оверлей: затемнення+«Олика» підняті (padding 70→100, градієнт до 88%) — низ фото чистий під тінь листа.
- **E2E (реальний застосунок, 390px):** лист 0..390 ✓; язичок на фото (top 579 vs лист 611, виступ 32px) ✓; paper/radius 18 ✓; blurband відсутня ✓; stuck: блюр 9px, білий h2, пігулка transparent ✓; центр фрази ≈ центр іконки (95.75 vs 97) ✓; 0 pageerror. Скріни resting/stuck = макет.
- CACHE `cstl-20260716-1136`.
- ⏭️ Далі за Вовою: спробувати варіант 2 — у язичку лише «ШО В СЕЛІ?», підзаголовок у листі (макет v6 схвалений як напрям).

## Задача 22 (16.07): фото на весь екран + язичок одне-тіло з листом (правки після живого тесту)
**Вова (3 правки + «Роби»):** 1) тінь пропала бо фото обривалось вище листа → зробити фото повноекранним заднім тлом з обʼєктом угорі (+ дав промпт для ChatGPT); 2) додати листу помірну тінь налягання (не перебор); 3) язичок = одне ціле з листом, ободок лише зверху/боки до зʼєднання, знизу під назвою рамки НЕ будувати.
- **Макет ПЕРЕД кодом:** повноекранне фото + лист + язичок що налягає вниз (mockup-fullbg) — схвалено «Роби».
- **community.css (CSS-only):**
  1. `.cm-hero` height:560 → bottom:0 (повноекранне заднє тло). Причина: на високому iPhone фіксовані 560px обривались ВИЩЕ листа → тінь листа падала на бежеве тло (=невидима). Повне фото → лист завжди на фото → тінь читається.
  2. `.cm-hero-img` object-position 55%→28% (костел угорі кадру).
  3. `.cm-hero-overlay`: justify flex-start, «Олика» на фікс. 430px (не flex-end — бо на повний екран пішло б у самий низ під таб-бар); градієнт у ПІКСЕЛЯХ (не % змінної висоти).
  4. `.cm-sheet`: тінь → помірна `0 -5px 12px .30 + 0 -18px 40px .26` (не перебор); radius 26→24; padding-top 0→8 (паперова зона для занурення язичка).
  5. `.cm-sec-head`: margin -46→-52 (виступ). `.cm-sec-head-in`: radius 20, padding 9/22/6, тінь узгоджена з листом; низ сідає в паперову зону листа → одне тіло без ободка знизу.
- **E2E (430×932, реальний iPhone-розмір):** hero=932 (весь екран) ✓; язичок налягає на лист 25px безшовно ✓; язичок↔картка 14px ✓; stuck-залипання (блюр+білий+пігулка розчинена) ✓; 0 pageerror. Скрін resting = макет.
- **Розміри фото для Вови:** 1290×2796 (9:19.5), JPG ≤500КБ, обʼєкт у верхніх 55-60%, низ спокійний; папка photos/, назви olyka.day-1..4 / olyka.evening-1..4.
- CACHE `cstl-20260716-1512`.

## Задача 23 (16.07): фраза без стрибка на центр іконки + горбик = одне тіло з блоком
**Дві правки (обидві CSS-only, community.css):**
1. **Стрибок при залипанні** — Вова: фраза пришпилювалась зависоко, потім «пригала» на центр іконки. Причина: у `--stuck` padding-top стрибав 8→18. Фікс: padding-top пігулки 22px ОДНАКОВИЙ в обох станах → текст-центр 97px = центр іконки і при пінінгу, і при блюрі, без розриву. E2E покроково: tc 97 наскрізь через перехід (y540 пін → y580 stuck), 0 ривка.
2. **Горбик = одне тіло** — Вова (уточнення через кілька ітерацій макета): язичок і блок одне ціле, горбик = просто випуклість верху блоку; не окрема накладена пігулка; без дуг/філе; без окремого ободка/тіні знизу; ТАБЛО не перекривати. Рішення: `.cm-sheet` box-shadow → `filter: drop-shadow` (×2 шари) — обводить лист РАЗОМ із пігулкою (`.cm-sec-head-in`, всередині листа) як ОДИН силует; пігулці прибрано власну box-shadow → жодного шва. У `--stuck` пігулка розчиняється (папір→прозоро), текст лишається на блюрі. 
   - ⚠️ Перевірено емпірично: `filter` на листі НЕ зламав sticky-залипання тексту всередині (пінить на 56px). Картки на власних шарах (`will-change:transform`) → drop-shadow не мав би ре-растеритись на фокус-скролі (спостерегти на iPhone).
   - Макети: mockup-cap (окрема пігулка — відкинуто), mockup-one (drop-shadow одне тіло — схвалено «Роби»).
- E2E 430×932: одне тіло ✓, ТАБЛО не перекрите ✓, tc 97=іконка без стрибка ✓, залипання пінить ✓, 0 pageerror.
- CACHE `cstl-20260716-1555`. (Раніше в сесії також: фото повернено як було до нових фото — задача revert.)

# Журнал сесії — Вова, 2026-07-20 (гілка `claude/startuem-jikkf5`)

> Нова harness-сесія (`/startuem`), окремо від `vova/session-2026-07-19`. Гілка вже містила весь актуальний `main` (перевірено `git merge-base --is-ancestor origin/main HEAD`).

## Старт (`/startuem`)
- Прочитано: HOT_RULES, START_HERE, BOARD.md (повністю, 390р), BYYOU_PLAN.md, SESSION_STATE_VOVA.md (перші ~325р), CSTL_BUGS.md, AUDIT_2026-07.md (перші ~150р), CLAUDE.md.
- Констатовано: проект одноосібний (Рома вийшов 18.07), BOARD/AUDIT ще не приведені у відповідність — окрема задача.
- Owner-watch Monitor НЕ запускав — сенсу нема (нема кого стежити).
- ⚠️ Гілка сесії — `claude/startuem-jikkf5` (harness), не `vova/session-<дата>` як зазвичай радить `/startuem`: зовнішні інструкції цієї конкретної сесії явно вимагали розробляти й пушити саме в цю гілку. Позначено користувачу в першій відповіді.

## Задача 29 (20.07) — 2 фікси Громади зі скріна Вови
**Скрін від Вови:** вкладка Громада, «ШО В СЕЛІ?», з червоною стрілкою на смужку скролу праворуч.

**Запит Вови (дослівно):** «підпис фотографії «Олика / Колегіальний костел Святої Трійці» - ці два рядки підпису, потрібно чучуть на низ перемістити» + «цей повзунок сторінки справа... треба забрати з додатку, це ж все таки додаток а не сайт».

**Дослідження коду (перед зміною):**
- `.cm-hero-overlay` (`style/community.css:1134`) — flex-колонка `justify-content:flex-end`, підпис анкериться до `padding-bottom`. Було `100px`.
- `.app-main` (`style/base.css:183`) — головний скролер сторінки (`overflow-y:auto`), нативна WebKit-смужка скролу рендериться навіть у standalone PWA.

**Зроблено:**
1. `style/community.css` — `.cm-hero-overlay` `padding-bottom: 100px → 86px` (−14px, підпис нижче; текст/шрифт не чіпано).
2. `style/base.css` — `.app-main`: додано `scrollbar-width:none` + `-ms-overflow-style:none` + `::-webkit-scrollbar{display:none;width:0;height:0}` (приховує нативну смужку скролу; сам скрол функціонально не зачеплено).
3. `sw.js` `CACHE_NAME` → `cstl-20260720-1211`.
4. `node build.js` — потрібен був `npm install esbuild` (node_modules був відсутній у сесії) → після встановлення білд пройшов чисто (32 файли, `check-imports` без проблем). JS не змінювався — `bundle.js` не чіпав.
5. Побічний ефект `npm install`: `package.json`/`package-lock.json` зсунули версію esbuild `^0.20.0→^0.20.2` — **відкочено** (`git checkout -- package.json package-lock.json`), не стосується задачі.
6. `SESSION_STATE_VOVA.md` — додано запис «Задача 29».

**Слова Вови:** «Роби і фініш» → підтвердження на обидва пункти + запуск `/finish`.

## Логічний аудит вкладки «ШО В СЕЛІ?» (обговорення, без коду)
- Вова попросив логічний аудит додатка; конкретно — розібратись що Рома залишав про «Шо в селі» + накинув свою ідею.
- **Знайдено (код+доки Роми):** «Шо в селі» (id `shotam`, `events.js`) = жива стрічка подій+свят, календар/фільтри Рома прибрав 06.07; наповнення через `events.json`/`holidays.json` + кабінет редактора (Алла) + AI-редактор + `sync_cms`. Назва — ТИМЧАСОВА, Рома лишив фінал за Вовою. Концепт Роми «Громада = рамка Шо в селі».
- **Ідея Вови:** перетворити вкладку на стрічку сторінок-каналів місцевих кураторів (ті самі, що ведуть FB-групи Олики). Модель — «канали для читання» (легкий варіант, житель читає/лайкає/коментує/підписується). UI: головна стрічка + горизонтальні кружечки-аватарки груп → тап відкриває екран групи (банер+аватар+дзвіночок-підписка+стрічка постів групи). Збіг з концептом Claude ~95%; кружечки Вови кращі за чіпи-фільтри — взято його варіант.
- **Дано Вові:** промпт для ChatGPT (фотовізуал 2 екранів, з айдентикою бренду).
- **Збережено концепт:** `CSTL NEWS VOVA/docs/SHOTAM_PAGES_PLAN.md` (повна концепція + 3 сутності + реюз кабінету + ризики + таблиця варіантів назви).
- **Відкрите:** назва вкладки (варіанти в документі, рекомендація — «Спільноти» в таб-барі + гасло «Шо в селі?» усередині); фотовізуал від Вови; технічна розкладка на етапи перед «Роби».

## Задача 30 (20.07) — прямі кути блюр-рядка під «ШО В СЕЛІ?»
- **Скрін Вови:** обведено верхній блюр-рядок під назвою «ШО В СЕЛІ?» коли вона прикріплюється до шапки — нижні кути заокруглені, «інколи при певних тінях кидаються в очі».
- **Зроблено:** `.cm-topbar-blur` (`style/community.css`) `border-radius: 0 0 18px 18px` → `border-radius: 0` (нижні кути прямі/гострі, як раніше). Нижній край і так м'яко розсіюється маскою-градієнтом — різкої лінії нема. CSS-only.
- CACHE `cstl-20260720-1411`. Слова Вови: «Фініш».

## Дослідження автобусних маршрутів через Олику (задача А-1 + А-7, без коду)
- Вова: мало тижневих лімітів (відновляться післязавтра) — зробити максимум зараз. Основні задачі — зворотні маршрути автобусів (А-1) + додати «Рівне–Довгошиї» (через Олику, рівненський сайт, не VOPAS) + скласти список усіх маршрутів через Олику.
- **Діагноз А-1 (код):** `parse_vopas.py` УЖЕ запитує зворотні пари (`MARSHRUTI` рядки 45-53: Олика→Луцьк, Личани→Луцьк тощо), але VOPAS не віддає рейси «з Олики» (проміжна станція). Тому в `schedule.json` лише напрямки «В Олику». Код правильний — блок у джерелі.
- **Мережа середовища блокує всі .ua** (403 на curl/WebFetch — vopas/olexrada/data.gov.ua/bus.com.ua; перевірено `$HTTPS_PROXY/__agentproxy/status` → connect_rejected). Працює лише WebSearch (сніпети). Офіційні PDF/XLSX звідси не завантажити — Вова відкриває руками.
- **Знайдено (WebSearch):** Рівне→Довгошиї 4:25/6:40/8:00/12:50/15:30/17:10, перевізники Яцишин/Санрайз/Мохнюк (= ті самі що в Олиці → траса через Олику підтверджена); маршрут №304 Жорнище-Луцьк/Цумань-Луцьк(ч/з Олику)/Цумань-Ківерці; порядок з Луцька Дідичі→Олика→Метельне.
- **Джерела офіційних даних** (для Вови): olexrada.gov.ua/doc/bus_schedule.pdf ⭐, data.gov.ua реєстр Волині XLSX ⭐, westua.net Олика-Луцьк, bus.com.ua?as=560100 (Рівне табло), тел. Луцької АС +380332246047.
- **Збережено:** `docs/BUS_ROUTES_RESEARCH.md` (повне дослідження, список маршрутів, план на 3 кроки, що заблоковано). AUDIT: А-1 деталізовано діагнозом, додано А-7 (Рівне–Довгошиї).
- **Код НЕ чіпано** — правило проекту: км/час/зупинки не вигадувати, лише з першоджерела; даних поки нема.

## Інциденти
- **git-merge конфлікт при /finish (задача 30):** `git merge origin/main` дав конфлікти в `sw.js` (CACHE 1411 vs 1211) і `_session-log/vova-2026-07-20.md` (add/add — origin вже містив journal з PR #487 без секції аудиту). Вирішено вручну: узято мою новішу версію в обох. `style/community.css` авто-змерджився чисто.
- Задача 29: немає (build.js спрацював чисто після встановлення залежності).

# BYYOU_PLAN — стан потоку /byyou (CSTL)

**Статус:** `active` — потік **09.08 «Security pass по Supabase»**
**Гілка:** `claude/startuem-xzx19p`
**Архів попереднього потоку:** `BYYOU_ARCHIVE_2026-08-08_weather-day.md` (PR #845)

---

## Ціль

Комплексний прохід безпеки по базі за наслідками живого аудиту 09.08
(розбір — у відповіді Вові + рекомендації ChatGPT). Мета — **коректна модель
доступу**, а не «нуль попереджень у раднику Supabase»: з 88 WARN радника
реально дієвих виявилось два, решта — тригерні функції і функції зі сторожем
`is_admin()` усередині.

🔑 **Правило потоку:** база — прод, відкату «одним рядком» немає. Кожна
міграція йде окремим кроком, з перевіркою ДО і ПІСЛЯ від імені ролі `anon`.

---

## 🔴 Що перевірено проти порад ChatGPT (і де він помиляється)

| порада | вердикт |
|---|---|
| «`REVOKE SELECT ON ads` і від `authenticated`» | ⚠️ **Пастка.** `REVOKE` діє нижче за RLS — адмін теж `authenticated`, політика `"Admins manage ads"` перестане його рятувати. Безпечно **лише тому**, що адмінка `ads` не читає (звірено: 0 входжень у `src/` і `admin.html`). Робимо свідомо, а не «бо порадили». |
| «Закрити `page_reactions` для public повністю» | ❌ **Зламає Стрічку.** Клієнт читає таблицю напряму (`src/core/supabase.js:1450`) і має живу підписку (`:1359`). Realtime поважає RLS → лічильник лайків перестане оновлюватись наживо, а гість побачить 0 лайків усюди. Потрібен інший розкрій (крок 9). |
| «`profiles.display_name`» | ❌ Колонки нема, вона `name`. Плюс є `surname`, якого ChatGPT не бачив. |
| «UID enumeration через `get_avatars`» | 🟡 Перебільшено: uuid 128-бітний, перебрати не можна. Реальна вада інша — **немає стелі на довжину масиву**. |
| «Тригерні SECURITY DEFINER функції — теж діра» | ❌ Ні. Postgres не дає викликати їх як RPC. 13 із 76 WARN — саме такі. |

**Мішені, які ChatGPT пропустив:** `threads.author_name`, `threads.buyer_name`,
`chat_group_members.name` — той самий клас підробки, що й `posts.author`.

---

## 📐 Заміряно ДО початку (жива база, роль `anon`)

| що пробував прочитати анонім | віддала база |
|---|---|
| `profiles` · `messages` · `chat_group_messages` · `ads` | **0** ✅ |
| `posts` | 24, з них **12 із телефонами** 🔴 |
| `page_reactions` (з колонкою `user_id`) | 11 лайків, **5 унікальних uid** 🟡 |

`posts`: розходжень `author` ↔ `profiles.name` — **0** (`mismatch = 0`).
Тобто серверний тригер авторства на живих даних нічого не змінить.

---

## Кроки — ПОТІК 1 (P0, блокуюче перед масштабуванням)

Чиста база, інтерфейсу не торкається. Клієнт після цих кроків працює як раніше.

| # | крок | тип | статус |
|---|---|---|---|
| 1 | Інвентар RPC-поверхні: усі public-функції → хто виконує / читає / пише / чи є сторож. Файл `_ai-tools/AUDIT_SUPABASE_2026-08-09.md` | 🟢 | ⬜ |
| 2 | Стенд безпеки `tests/security-rls.mjs` — прогін від 4 ролей (anon / user A / user B / admin) **ДО змін**, фіксує чинний стан як еталон | 🟢 | ⬜ |
| 3 | Міграція **A** — підробка автора: тригер `enforce_denorm_identity()` на `posts`, `comments`, `threads`, `chat_group_members` | 🔴 | ⬜ |
| 4 | Міграція **B** — `ads`: `revoke select` від anon+authenticated, подання `ads_public` лише з безпечними колонками | 🔴 | ⬜ |
| 5 | Міграція **C** — `revoke execute` на `heal_orphan_page_comments`, `flush_page_comment_push` від anon+authenticated | 🟢 | ⬜ |
| 6 | Міграція **D** — `search_path` для 7 функцій + стеля на масив у `get_avatars` | 🟢 | ⬜ |
| 7 | Прогін стенда безпеки ПІСЛЯ + `npm test` (регресія 54 стенди) | 🟢 | ⬜ |
| 8 | Дзеркалення міграцій у `scripts/*.sql` + аудит-файл + реліз-нотатки | 🟢 | ⬜ |

## Кроки — ПОТІК 2 (P1, чіпає інтерфейс — окремо)

| # | крок | тип |
|---|---|---|
| 9 | `page_reactions`: подання `page_reaction_counts` для гостей + повний рядок лише авторизованим; клієнт `fetchPageReactions` | 🔴 |
| 10 | `posts.contact` — номер по запиту: RPC `get_post_contact(post_id)` + журнал + рейт-ліміт | 🔴 |
| 11 | Клієнт: кнопка «Показати номер» для гостя (Дошка) | 🟢 |
| 12 | `get_public_profile` — синхронізувати прод із файлом (`bio` + `age`); спершу рішення Вови, чи потрібен вік публічно | 🟡 |
| 13 | Сценарій запрошень у групи (`get_group_by_invite` / `join_group_by_token`) — перевірити, що працює при 0 політик на `chat_group_invites` | 🟢 |

**Чому розділено:** потік 1 — тільки база, нуль ризику для інтерфейсу, можна
деплоїти окремо. Потік 2 міняє екран Дошки і Стрічки → потребує смоуку на
iPhone. Разом це ~13 кроків × важкі SQL-відповіді — не влізе в одне вікно
без втрати якості на останніх кроках.

---

## Де зупинились

Потік створено, чекаю «ок» на браму старту. Нічого ще не змінено.

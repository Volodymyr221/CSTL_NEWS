# BYYOU_PLAN — стан потоку /byyou (CSTL)

**Статус:** `done` — потік **06.08 вечір «Аудит документації»** виконано і змерджено
(PR **#824**, squash-коміт `541d8ffb`). Незавершеного немає, push-замок знято.

Архів потоку (усі 14 кроків, знахідки, реліз-нотатки) →
`_archive/BYYOU_PLAN_2026-08-06b_аудит-документації.md`.
Реєстр знахідок із числами → `AUDIT_DOCS_2026-08.md`.

Попередній завершений: **06.08 — Автор · Профіль · Кабінет · Скарги** →
`_archive/BYYOU_PLAN_2026-08-06_автор-профіль-кабінет-скарги.md`.

---

## Підсумок останнього потоку

Виправлено 6 класів поломок документації: **65** битих посилань, **5** прямих
суперечностей між документами, **4** неправдиві факти в головних файлах, **4**
застарілі розділи карт файлів, структурний дубль із двох тек `_session-log/`.

Найдорожча знахідка — `HOT_RULES.md` (читається першим) обіцяв сторожі клавіатури
`kb-guard.test.js` / `kb-tapinput.test.js`, яких **ніколи не було в git**.

Заведено сторож **`tests/docs-refs.mjs`** (10 перевірок, доведено падінням на двох
навмисних поломках): падає на битому посиланні, на розходженні кількості стендів із
диском і на появі другої теки журналів. `npm test` → **45 / 767**, усе зелено.

⬜ **Відкрите питання до Вови:** заводити 12-те правило в `HOT_RULES.md`
(«документація — теж код; не посилайся на файл, не перевіривши, що він є»).
Не завів сам — правила створює тільки Вова.

---

## Як почати новий потік

`/byyou <ціль>` — Claude прочитає код, складе план на 10-15 кроків, покаже його і
чекатиме «ок». Між брамами — тільки локальні коміти, публікація на слово «деплой».

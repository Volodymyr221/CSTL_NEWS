# BYYOU_PLAN — живий потік /byyou

**Статус:** `active`
**Ціль:** редизайн блоку новин у Громаді — сучасний і читабельний і для молоді, і
для старших. Потік 1 із двох: **вигляд** (віджет, картки, хаб). Медіа (галереї,
відео, парсер) — окремий потік 2.
**Гілка:** `claude/startuem-5nmz43`
**Старт:** 26.08.2026

---

## 📐 Заміряно ДО плану (живий `data/articles.json`, 400 статей, 1.6 МБ)

| Що | Число | Чому це важливо |
|---|---|---|
| Статей **без обкладинки** | **114 з 400 = 29%** | Картка без фото — не крайній випадок, а майже третина стрічки |
| Перші 6 у розділі, з фото | Громада 4/6 · Волинь 4/6 · Україна 3/6 · Світ 4/6 | Два-три місця з шести завжди без фото |
| Статей з **описом** (`excerpt`) | **400 з 400 = 100%**, медіана 315 символів | Опис у великій картці ніколи не буде порожнім — безпечно додавати |
| `<img>/<figure>/<iframe>/<video>` у `content` | **0 з 400** | Парсер їх **навмисно** викидає (`_blocks_to_html`, аллоулист p/h3/ul/li/strong/em/br/blockquote) |
| Обкладинки | гарячі посилання на чужі хости | `img.konkurent.ua`, `uimg.pravda.com.ua`, `static.rayon.in.ua`, `rada.info` |

---

## ⛔ Чого я НЕ зміг перевірити звідси

Мережева політика цього середовища відбиває **всі** домени джерел (403 на CONNECT:
`volynpost.com`, `konkurent.ua`, `pravda.com.ua`, `rayon.in.ua`, `img.konkurent.ua`).
Тобто **я не бачив жодної живої сторінки-джерела** і не знаю, як там насправді
влаштована галерея. Парсер живе в GitHub Actions, де мережа є — перевірка медіа
можлива лише там або з надісланого Вовою збереженого HTML. Це причина, чому
парсер винесено в окремий потік, а не робиться наосліп зараз.

---

## Кроки — Потік 1: ВИГЛЯД

| # | Крок | Стан | Хеш |
|---|---|---|---|
| 1 | Замір «до»: висота віджета новин і сусідніх віджетів Громади | ✅ | `—` |
| 2 | Велика картка вища: 3 рядки заголовка + 2 рядки опису поверх фото, вуаль перерахувати | ✅ | `—` |
| 3 | Два нижні рядки → повноцінні малі картки: вищі, мініатюра фото коли є, джерело + час | ✅ | `—` |
| 4 | **Картка без фото — власний дизайн** (29% стрічки). Показую 2-3 варіанти, Вова обирає | 🔴 | |
| 5 | Провести ту саму мову в хаб «Усі новини» (`.nc--row`, `.nc--tile`, `.nc--lead`) | 🟢 | |
| 6 | Замір «після»: віджет має влазити; порівняти з «до» і назвати число вголос | 🟢 | |
| 7 | Стенд `news-widget.mjs` доповнити на нові форми; `/qa-explore` + `/audit` | 🟢 | |
| 8 | Смоук на iPhone | 🟡 | |

**Потік 2 (наступний): МЕДІА** — контракт медіа-блоків у санітизованому HTML,
клієнтський рендер (фото з підписом, галерея-свайп, відео-постер), повноекранний
перегляд, і тільки потім парсер. Відкрите питання №1 нижче блокує його старт.

---

## 📐 Замір «ДО» (крок 1, `tests/tools/news-redesign-measure.mjs`, екран 390×844)

Видима зона — **731px** (екран мінус шапка 56 і таб-бар 57).

| Віджет Громади | Висота |
|---|---|
| Телефони | **576px** ← найбільший на сторінці |
| **Новини** | **357px = 49% видимої зони** |
| Оголошення | 343px |
| Автобуси | 225px |

Усередині віджета: `nc--hero` **161px** (пропорція 2:1), `nc--line` **60px** × 2,
сторінка каруселі **296px**. Уся Громада — 1772px.

🔴 **Поправка до того, що я казав до заміру.** Я назвав найбільшим сусідом
«Актуальний збір» 450px — це число з коментаря в коді від 11.08, а не з живого
екрана. Насправді найбільший блок — **Телефони, 576px**, а збору зараз на сторінці
немає взагалі. Тобто стеля зростання новин ширша, ніж я сказав: до ~500px віджет
лишається меншим за Телефони.

⚠️ **Побічна знахідка, важлива для потоку 2.** У трьох великих картках із чотирьох
`<img>` уже замінений на `.img-fallback` — чужі фото не завантажились (мережа
середовища їх ріже). Це рівно той випадок, який на проді трапляється постійно, і
він відтворюється тут сам собою. Тобто **стенд уміє міряти вигляд без фото**, не
підробляючи дані.

**Ціль редизайну (з цих чисел):** hero 161→~230px, рядки 60→~78px, віджет
357→**~450px = 62% видимої зони** — усе ще менше за Телефони.

## 📐 Замір «ПІСЛЯ КРОКУ 2»

| | було | стало |
|---|---|---|
| Велика картка | 161px (2:1) | **242px (4:3)** |
| Заголовок | 43px, 2 рядки, 17px | **69px, 3 рядки, 18px** |
| Опис поверх фото | немає | **38px, 2 рядки** |
| Вуаль | 62% | **74%**, точки перераховані від початку тексту |
| Віджет новин | 357px = 49% | **438px = 60%** |
| Карусель | 7 сек | **10 сек** |

Текст влазить у вуаль у всіх чотирьох картках (153px тексту проти 179px вуалі),
чистої фотографії зверху **88px**. Найбільший блок сторінки — Телефони 576px —
лишається більшим за новини.

🧪 `news-widget` 68/68 · `carousel-shared` 15/15 · `clamp` 17/17 · `dead-css` 8/8
· `css-alive` 3/3 · `tap-targets` 5/5.

⚠️ **Знайдено дорогою, вирішується на кроці 3.** Сторож `news-widget.mjs` тримає
стелю віджета магічним числом: `w.h < 480`. Зараз 438px, після кроку 3 буде ~474 —
тобто наступний же крок упреться в число, взяте до того, як Вова попросив блок
збільшити. Замінити треба не число на більше, а число на **виміряне відношення**:
«віджет новин менший за найбільший інший блок Громади» — це той самий критерій,
але він міряє наслідок, а не запис.

## 📐 Замір «ПІСЛЯ КРОКУ 3»

| | до потоку | зараз |
|---|---|---|
| Велика картка | 161px | **242px** |
| Нижній рядок | 60px, лише заголовок + час | **80px**, мініатюра 56px + заголовок + джерело · час |
| Сторінка каруселі | 296px | **416px** |
| Віджет новин | 357px = 49% | **477px = 65%** |

Найбільший блок Громади — Телефони 576px — усе ще більший.

🔴 **Давня вада, знайдена заміром.** Рядки виходили різної висоти: **104px і 80px**
в одному блоці. Причина не в новому коді — у `.img-fallback` стоїть
`min-height: 80px`, хоч підпис над ним стверджує «успадковує розмір від класу
картинки». Не успадковує: 80px перебиває будь-який менший розмір варіанта. Тобто
висота картки залежала від того, **чи доїхало чуже фото**. `.nc--mini` (мініатюра
72px) має той самий розрив на 8px увесь час — просто в тихому рядку картинки
раніше не було взагалі, тож ваду ніде було побачити. Лагодиться
`.nc-img.img-fallback { min-height: 0 }` — загальне правило лишається для
плейсхолдерів подій, де картинка своєї висоти не задає.

🔴 **Стеля сторожа перестала бути числом.** Було `w.h < 480` — заміряне 04.08
плюс «запас 50px». Воно не міряло того, що називає: «не зʼїдає головний екран» —
це відношення до інших блоків сторінки, а не абсолют. І воно вже заважало: після
кроків 2-3 віджет став 477px, тобто робота на пряме замовлення Вови впиралась у
число, поставлене до того, як замовлення прозвучало. Стало: **«віджет новин не
найбільший блок Громади»** + грубий запобіжник «менше 75% видимої зони». Обидва
з контролем: роздутий віджет і порожній список сусідів мусять червоніти.

🧪 `news-widget` **71/71** (було 68) · `clamp` 17/17 · `dead-css` 8/8 ·
`css-alive` 3/3 · `tap-targets` 5/5 · `article-image` 7/7 · `article-banner` 6/6.

## Де зупинились

Кроки 1-3 закриті. Далі крок 4 🔴 — картка без фото, показати варіанти Вові.

---

## Реліз-нотатки

_(заповнюються у Фазі 3)_

---

## Архів завершених потоків

Лежать поруч, файли `BYYOU_ARCHIVE_<дата>_<тема>.md`. Останній —
`BYYOU_ARCHIVE_2026-08-20_lantsyugy.md` (аудит ланцюгів «створено → видно»,
12 кроків, закрито 22.08).

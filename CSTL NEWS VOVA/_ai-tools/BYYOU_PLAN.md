# BYYOU_PLAN — стан потоку /byyou (CSTL)

**Статус:** active
<!-- idle=вимкнено · active=потік іде (push-замок УВІМКНЕНО) · paused=пауза · done=завершено -->

**Ціль:** БЛОК 3 AI-редактора — семантичний дедуп новин (AI-кластер) + новини як місія в editor/ (хірургічно, реюз).
**Власник:** Рома · **Гілка:** roma/editor-block3 · **Rollback-tag:** byyou-start-block3

## Рішення (від Роми)
- **Підхід:** хірургічно — НЕ переписувати робочий новинний код. Реюз за інтерфейсами (як Блоки 1-2).
- **Дедуп-рушій:** AI-кластер — один додатковий виклик Anthropic за прогін (наявний ключ), групує «та сама подія іншими словами». Без нового ключа/залежності.

## Знахідки коду (реальні)
- Поточний дедуп = за словами заголовка (Jaccard ≥0.65, `pr.is_dup_title`) — НЕ ловить ту саму подію іншими словами.
- Точка інтеграції = `ai_news_agent.enqueue()` (рядок 455): семантичний дедуп вставляється на початку, ФЕЙЛ-СОФТ (без ключа/помилка → поведінка як сьогодні).
- Ядро дедупу житиме в `editor/core/dedup.py`; агент його імпортує (як імпортує `parse_rss`). `record_spend` рахує вартість.
- `editor/` пайплайн (Source→Reader→Filter→Writer→Image→Sink) уже возить свята; новини стануть місією реюзом `build_prompt/call_agent/extract_json_array`.

## Принципи
Найпростіше=найкраще · корінь не симптом · мінімум у фічах, максимум у фундаменті · реюз не переписування · фейл-софт (жива стрічка не має зламатись).

## Кроки
| # | Крок | Стан |
|---|------|------|
| 1 | Дочитати точні сигнатури (`enqueue`, `record_spend`, `build_prompt/call_agent/extract_json_array`, `spend.record`) | 🟢 |
| 2 | `editor/core/dedup.py` — `cluster_duplicates(new, recent, api_key)`: 1 виклик Anthropic (заголовок+лід), JSON-групи, фейл-софт, spend-облік | 🟢 |
| 3 | Прапорець `semantic_dedup` у `hromada_config.json` (декларативний вимикач, дефолт ON) | 🟢 |
| 4 | Вшити в `ai_news_agent.enqueue`: імпорт + 1 гардований виклик (≥2 кандидати + ключ + прапорець) | 🟢 |
| 5 | `editor/sources/news.py` (тонкий, реюз агента) + `missions/news.json` + `missions/volyn.json` + рядок у registry — новини як місія editor/ | 🟢 |
| 6 | Тести: стаб-мок Anthropic → групування дублікатів + фейл-софт + гард; `editor.run --mission news --dry-run`; `py_compile` усього | 🟢 |
| 7 | Оновити `docs/AI_EDITOR_ARCH.md` (компонент дедупу + новинна місія) + BOARD | 🟢 |
| 8 | Тест-блок + реліз-нотатки → БРАМА ДЕПЛОЮ | 🟡 |

## Де зупинились
Старт: план складено, чекаю «ок» Роми. Гілка roma/editor-block3 (від origin/main), rollback-tag byyou-start-block3.

## Реліз-нотатки
(заповнюється у Фазі 3)

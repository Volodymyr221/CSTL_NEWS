# BYYOU_PLAN — стан потоку /byyou (CSTL)

**Статус:** paused
<!-- ⏸ 09.07 ПАУЗА на 76% контексту. Новий чат → /byyou → продовжити з «Де зупинились». -->

**Ціль:** Майстер-план 18 задач Роми (`PLAN_2026-07-09_batch.md`), 8 батчів. Виконуємо по черзі, кожен = окремий деплой.
**Власник:** Рома · **Гілка:** `claude/new-session-3k5u9n` · **Rollback-теги:** byyou-start-agent / -tablo2 / -weather

## Прогрес батчів
- ✅ **Батч 1 (AI-агент витрати/якість)** — ЗАДЕПЛОЄНО PR #296. Корінь витрат вбито (idempotent backoff), кап на всю чергу, свято-дедуп, фото поважає holidays.json. 🔴 Вові: `scripts/supabase_cms_dedup.sql` (чистка 16 дублів) — інструкція дана.
- ✅ **Батч 2 (Табло+Hero)** — ЗАДЕПЛОЄНО PR #301 (кути, hero-кроп 55%, блюр 14px). + **item 10 v2 (зона скролу)** — коміт `61d6c7f0` ЛОКАЛЬНО, CACHE `cstl-20260709-1920`, ЧЕКАЄ «деплой» (не запушено). Інсет стрічки через `margin: 6px 12px 0` на `.cm-news-feed` → винна рамка навколо = скрол сторінки.
- 🟡 **Батч 3 (Модалка погоди)** — В ПРОЦЕСІ. `wxLineChart` вже переписано (WX-геометрія + wxGeom + 2год-таймінг `i%2` + права шкала °) — **збережено в git stash «batch3-weather-wip»** (community-blocks.js). НЕ зроблено: wxBarChart сині бари+2год, скрабер (снап по годинах, pointer-events на `.wx-chart-svg-wrap`, спільна лінія+readout+іконка `weather_code`), свайп-вниз закриття (реюз community-modal.js:130-165), CSS (wx-grid/wx-cursor/wx-readout/precip темніша підкладка).

## ▶️ ЯК ПРОДОВЖИТИ (новий чат)
1. `git stash list` → `git stash pop` (поверне weather WIP у community-blocks.js). Якщо конфлікт — WIP лише в wxLineChart-зоні.
2. Спершу: якщо Рома ще не сказав «деплой» на item 10 v2 (коміт 61d6c7f0) — нагадати; після «деплой» → push+PR+merge (CACHE 1920).
3. Дописати Батч 3 (див. «НЕ зроблено» вище) — карта коду в `PLAN_2026-07-09_batch.md` (Батч 3) + агент-звіт був: openWeatherDayModal/wxLineChart/wxBarChart у `community-blocks.js` (~162-271), геометрія WX, `weather_code` вже в hourly. Скрабер снапить по годинах; swipe-close тач-хендлери на `.wx-sheet` (не на графіку — там скрабер, `setPointerCapture`).
4. Далі за планом: Батч 4 (події-фото+перенаправлення) → 5 (модалка-іконки) → 6 (FAB-анімація) → 7 (спільні Збережені+автобуси) → 8 (Push P-5/8/9 фронт + P-2 код).

## Ще у майстер-плані (не батчі): контент п.4 (переписати 4 свята-статті) — чекає уточнення Роми (де/які); зона-принцип у BOARD п.5.

## Реліз-нотатки (item 10 v2, для деплою)
Зона внутрішнього скролу Табло звужена (інсет margin) → палець за межами карток тягне сторінку, всередині — стрічку. CACHE cstl-20260709-1920.

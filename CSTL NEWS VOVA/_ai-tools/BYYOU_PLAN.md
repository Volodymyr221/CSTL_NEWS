# BYYOU_PLAN — живий потік /byyou

**Статус:** `active` — Вова сказав «Так» на брамі старту (10.08, сесія U-20).

> 🆕 **ЯКЩО ТИ НОВИЙ ЧАТ — ЧИТАЙ ЦЕ ПЕРШИМ.** Вова просив, щоб новий чат
> «зразу підхопив byyou». Порядок дій:
> 1. `/byyou` **без аргументів** → скіл побачить цей файл (`paused`) і ввійде в
>    режим ПРОДОВЖЕННЯ, Фаза 2. Планувати наново НЕ треба — план нижче готовий.
> 2. Перечитай увесь цей файл: розбір двох механізмів важливіший за таблицю.
> 3. Перевір гілку: `git branch --show-current` → має бути `claude/startuem-*`
>    (або `vova/*`), НЕ `main`.
> 4. Скажи Вові одним екраном, що саме збираєшся робити, і **чекай «ок»**.
>    Брама старту — правило скіла, не формальність.
> ⚠️ Жодного `git push` до слова «деплой» (брама деплою).
> 🛑 Пряма вимога Вови: «не поламай ту логіку плавності і тд як зараз».
**Потік:** 10.08 — «Бургер: меню згортається саме, блюр лишається — закрити КОРІНЬ»
**Гілка:** `claude/startuem-1rgetk` _(сесія U-20; попередня `claude/startuem-a75x6z` уже злита в `main`)_
**Замовлення Вови (дослівно):** «проблема з бургером та тим що це меню пропадає не
виправлена, розбери це комплексно, можливо подивись на це з іншої сторони… але не
поламай ту логіку плавності і тд як зараз… можливо щось не до кінця згортається чи
виключається».

---

## Що вже пробували (і чому цього не вистачило)

| захід | що робив | чому не закрив |
|---|---|---|
| 09.08 | прибрано `requestAnimationFrame` і `setTimeout` зі стану | лікувало відкладені виклики, не сам шар |
| 10.08 (#884) | `paintState()` — один писар; `:has()` не дає затемненню пережити панель | керує ВИДИМІСТЮ, але не тим, ЧОМУ меню закривається, і не композитним шаром блюру |

## 🔍 Розбір з іншого боку — ДВА незалежні механізми, не один

Симптом Вови складається з **двох** різних речей, і кожна має свою причину. Саме
тому «один фікс» щоразу закривав половину.

**(1) «Меню відкривається і ВІДРАЗУ згортається» — привид тапу.**
Затемнення отримує `pointer-events: auto` НЕГАЙНО при відкритті, а панель у цю мить
ще за краєм екрана (`translateX(102%)`, їде 0.28с). Тобто в перші кадри під пальцем
у точці бургера лежить САМЕ ЗАТЕМНЕННЯ, а тап по ньому = `closeSidebar()`.
На iOS після повернення з чужого застосунку система часто **дублює/доганяє** дотик —
і цей другий клік влучає в затемнення. Звідси «відкрилось і зразу закрилось», і
звідси ж привʼязка саме до сценарію з Instagram.

**(2) «Блюр залишається» — застиглий композитний шар.**
`.sidebar-overlay` оголошує `backdrop-filter: blur(3px)` у БАЗОВОМУ стані, тобто
фільтр існує завжди — і поки меню закрите теж. У проєкті вже записаний урок про
Safari: «`will-change` + `backdrop-filter` дає **застиглий blur**» (`style/board.css`).
🔑 Заміряно по всьому проєкту: `.sidebar-overlay` — **ЄДИНИЙ** повноекранний шар,
який має `backdrop-filter` І вмикається/вимикається. Унікальна комбінація = унікальний
баг. Решта блюрів у застосунку або статичні, або без перемикання.
⚠️ Механізм (2) — гіпотеза, підперта уроком із цього ж репозиторію і відомими
багами `backdrop-filter` у Safari; довести його в Chromium неможливо. Тому фікс
робиться так, щоб прибрати САМУ УМОВУ (фільтра в закритому стані не існує), а не
покладатись на правильність пояснення.

## 📐 КРОК 1 — ЗАМІРЯНО ПРИЛАДОМ (10.08, Chromium 390×844)

**Механізм (1) підтверджено, але він ВДВІЧІ ширший, ніж описано вище.**

Питання приладу було одне: що лежить під пальцем у точці бургера після відкриття.
Відповідь по кадрах (бургер — центр `351×28`):

| час від відкриття | під пальцем | привид-тап закриває меню? |
|---|---|---|
| 0–25мс | **затемнення** | **так** |
| ~30–120мс | панель (`#sidebar`, глухе місце) | ні |
| **≥150мс** | **`#sidebar-close` — хрестик ✕** | **так** |
| 266мс | панель доїхала (`right` = 390 = край екрана) | — |

🔴 **ГОЛОВНА ЗНАХІДКА, ЯКОЇ В ПЛАНІ НЕ БУЛО: хрестик меню стоїть РІВНО НА
БУРГЕРІ.** Заміряно прямокутники: бургер `332,9 · 38×38`, ✕ `332,14 · 44×44`,
перекриття **1235px² = 86% площі бургера**.

➡️ Тобто закрити щойно відкрите меню може **два різні елементи**: у перші кадри
затемнення, а далі — сам хрестик. Фікс А з плану («затемнення не приймає тап»)
закрив би **лише перше вікно з двох**, і симптом лишився б живим. Саме це й
пояснює, чому два попередні заходи не допомогли: обидва дивились на затемнення.

🔑 **Одне правило замість двох латок:** поки панель не доїхала, **жоден** тап не
рахується рішенням закрити меню — байдуже, у що він фізично влучив. Межа не
вигадана числом, а міряється: панель стоїть, коли її правий край збігся з краєм
екрана.

⚠️ **Прилад збрехав з першого разу, і це записано навмисно:** перша редакція
показала, що під пальцем лежить `#splash` (заставка ще не зникла), а умова «панель
доїхала» була записана не в той бік (`right >= innerWidth` істинне і тоді, коли
панель ще ЗА екраном) — виходило «доїхала» на 4-й мілісекунді. Обидві помилки
дали б хибний висновок «бага немає». Дванадцятий випадок брехливої мірки в проєкті.

## Принцип цього заходу

Не «ще одна латка», а **прибрати умови**: тап у перші кадри не має досягати
затемнення; фільтра в закритому стані не має існувати; закритий шар не має
компонуватись узагалі. Кожен рубіж самодостатній.

---

## Кроки

> 🔵 **Легенда стану — це НЕ прогрес.** 🟢 = роблю сам · 🟡 = потрібен Вова
> (смоук на iPhone) · 🔴 = стоп, спершу питаю. Виконаний крок позначається ✅ і
> хешем коміту в останній колонці. Зараз колонка «коміт» порожня **у всіх**
> рядках — тобто не зроблено ще нічого.

| # | крок | стан | коміт |
|---|---|---|---|
| 1 | ✅ Прилад: відтворити «привид тапу» в Chromium — довести, що зараз меню закривається | 🟢 | `2f9ed57` |
| 2 | ✅ Фікс А: поки панель не доїхала, меню не закриває НІЩО — ні затемнення, ні ✕ (переписано за замірами кроку 1: вікон два, а не одне; замість числа 350мс — вимір доїзду) | 🟢 | `c29534ff` |
| 3 | ✅ Фікс Б: `backdrop-filter` лише у відкритому стані (у закритому фільтра НЕМА), зникає із затримкою — щоб не рвати згасання | 🟢 | `796eacd8` |
| 4 | Фікс В: запобіжник `:has()` додає `display: none` — закритий шар не компонується | 🟢 | |
| 4б | 🆕 Низ меню («Слідкуйте за нами» + соцмережі) — СТАТИЧНИЙ, як шапка; скролиться лише список. Замовлення Вови 10.08 зі знімка | 🟢 | |
| 5 | Сторож: привид тапу НЕ закриває меню (+ контроль на старому коді) | 🟢 | |
| 6 | Сторож: у закритому стані немає ні фільтра, ні шару (+ контроль) | 🟢 | |
| 6б | Сторож на крок 4б: підвал не їде зі списком, «Політика і приватність» не ховається під ним | 🟢 | |
| 7 | Перевірити, що плавність НЕ змінилась: `sidebar-overlay`, `motion`, `sheet-visible` | 🟢 | |
| 8 | Повний `npm test` + `node --check` | 🟢 | |
| 9 | Браузерний смоук зачеплених екранів | 🟢 | |
| 10 | `/audit` по змінених ділянках (дублі, мертвий код, баланс) | 🟢 | |
| 11 | Оновити `CACHE_NAME`, документи, реліз-нотатки | 🟢 | |
| 12 | 🟡 Смоук на iPhone: Instagram → назад → бургер ×5 | 🟡 | |
| 13 | Брама деплою — чекати слово «деплой» | 🔴 | |

**Оцінка:** 13 кроків, більшість дрібні (CSS + 2 сторожі) ≈ 30-35% вікна.

## Де зупинились

Фаза 1 завершена, план складено. **Жоден крок ще не виконано** — 🟢 в таблиці
означає «дозволено робити самому», а не «зроблено». Чекаю «ок» на брамі старту.

**Що сталося між планом і цим рядком** (щоб новий чат не шукав): Вова перервав
потік окремою дрібною задачею — не сподобались дві іконки в меню. Її закрито й
задеплоєно (див. `SESSION_STATE_VOVA.md`, пункт «3-біс»): «Громада» → гурт
людей `ICONS.usersGroup`, «Стрічка» → `list-details` у таб-барі. Коду бургера ця
задача **не торкалась**, тож план нижче чинний як є.

**Стан коду на момент паузи:** гілка `claude/startuem-a75x6z` злита в `main`,
`CACHE_NAME = cstl-20260810-2340`, `npm test` — 66 стендів · 1119/1119.

**Що вже перевірено і НЕ треба переробляти:** `paintState()` — єдиний писар
стану меню (жодних `requestAnimationFrame`/`setTimeout`); запобіжник
`body:not(:has(.sidebar--open)) .sidebar-overlay` не дає затемненню пережити
панель; сторожі `sidebar-menu.mjs` (41) і `sidebar-overlay.mjs` (34) зелені.
Це фундамент, на який кроки 2-4 лягають зверху, а не альтернатива їм.

## Реліз-нотатки
_(заповнюються на кроці 11)_

# BYYOU_PLAN — стан потоку /byyou (CSTL)

**Статус:** done
<!-- idle=вимкнено · active=потік іде (push-замок УВІМКНЕНО) · paused=пауза · done=завершено -->
<!-- ✅ 07.07 ЗАДЕПЛОЄНО: PR #235 (squash 0bfe6398). Лічильник витрат + гард на повну чергу + prompt caching. Одноразове від Вови: поповнити баланс Anthropic (~$20) — без цього агент мовчить; лічильник оживе після поповнення+запуску. -->

**Ціль:** Фаза 0 оптимізації AI-автопостингу новин — прилад вимірювання витрат + прибрати некешований вхід + гард на повну чергу.
**Власник:** Рома · **Гілка:** roma/ai-spend-counter · **Rollback-tag:** byyou-start-spend-counter

## Контекст (реальні дані з консолі Anthropic, липень)
- Вхід 382k · вихід 22.5k · пошуків 24 · разом ≈ **$1.7/міс** (НЕ дорого; вмер від порожнього балансу −$0.04).
- Профіль **17:1 вхід:вихід** → головна неефективність = некешований вхід у циклі `pause_turn`.
- Модель `claude-sonnet-5` ($3/$15 за 1млн; web_search $10/1000). Розклад 4×/добу.

## План (12 кроків)
| # | Крок | Файл | Стан |
|---|------|------|------|
| 1 | Константи цін + SPEND_PATH + load_spend/save_spend | ai_news_agent.py | 🟢 |
| 2 | call_agent → акумулює usage (in/out/cache/web_search) крізь pause_turn, повертає (text, usage) | ai_news_agent.py | 🟢 |
| 3 | Prompt caching: cache_control ephemeral на префіксі + останньому блоці перед кожним викликом | ai_news_agent.py | 🟢 |
| 4 | record_spend: рахує $ → дописує data/ai_spend.json (runs[] + totals місяць/усього) | ai_news_agent.py | 🟢 |
| 5 | main: гард «черга повна → skip API»; виклик record_spend після місії | ai_news_agent.py | 🟢 |
| 6 | Початковий data/ai_spend.json (порожній каркас) | data/ai_spend.json | 🟢 |
| 7 | py_compile + --dry-run перевірка | — | 🟢 |
| 8 | admin.html: таб «💸 Витрати AI» (admin-only) + маршрут | admin.html | 🟢 |
| 9 | admin.html: renderSpend(el) — fetch data/ai_spend.json, картки+журнал | admin.html | 🟢 |
| 10 | yml: git add data/ai_spend.json у крок commit | ai-news-agent.yml | 🟢 |
| 11 | Тест-блок: py_compile + браузер-смоук admin.html + /audit | — | 🟡 |
| 12 | Реліз-нотатки | BYYOU_PLAN.md | 🟢 |

## Де зупинились
✅ Усі 12 кроків виконано, 2 локальні коміти (`d85c7760` python, `3fc5976b` admin+yml). Тести зелені (py_compile, node --check renderSpend, структура admin.html). CACHE_NAME не потрібен (admin.html не в SW). **Чекаю слово «деплой».**

## Реліз-нотатки — Фаза 0 (лічильник + оптимізація автопостингу)
**ЩО ЗМІНИЛОСЬ:**
- Новий блок в адмінці **«💸 Витрати AI»** (лише адмінам): місяць / усього / останній запуск + журнал останніх запусків ($, статей, пошуків, кеш).
- Агент новин тепер **рахує реальну вартість** кожного запуску → пише `data/ai_spend.json`.
- **Гард економії:** агент не викликає дорогий API, якщо черга новин уже повна (30).
- **Кешування** повторюваного вводу в циклі пошуку → менше вхідних токенів (профіль був 17:1).

**ЩО МОЖЕ ЗЛАМАТИСЬ:**
- Адмінка: новий таб. Перевірити що інші таби (Статті/Модерація/Налаштування) не з’їхали.
- Агент новин: змінено `call_agent` (тепер повертає usage) — перевіряється першим живим запуском після поповнення балансу.

**ЩО ПЕРЕВІРИТИ (Вова/адмін, після деплою):**
1. Зайти в адмінку → таб «💸 Витрати AI» відкривається, показує $0 (поки запусків нема).
2. Решта табів адмінки працюють як раніше.
3. Після поповнення балансу + ручного запуску агента — у блоці з’явиться рядок з $ і кеш-читанням > 0 (доказ що кешування працює).

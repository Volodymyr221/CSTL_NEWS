# BYYOU_PLAN — стан потоку /byyou (CSTL)

**Статус:** done
<!-- ✅ 12.07 ПОТІК 6 ЗАДЕПЛОЄНО: PR #354, мердж 0d28b6ad, live v2717 · 12.07 00:44, CACHE cstl-20260712-0055. -->
<!-- 🔜 ПОТІК 2 (AI-кнопка «Оновити статистику») — окремий /byyou, чекає ANTHROPIC_API_KEY у Supabase secrets. -->

## 🧩 ПОТІК 6 (11.07) — Статистика: події+профіль(вік)+дашборд адмінки (Потік 1 з 2)

**Джерело:** розмова з Ромою (не Вовин список) — статистика/аналітика застосунку. Домовлено: тільки свій Supabase (без Google Analytics/Plausible), вік — самозаповнення в профілі (не вираховується), село — вже є (SETTLEMENTS), AI-кнопка «Оновити статистику» — окремий **Потік 2** (чекає ключ `ANTHROPIC_API_KEY` у Supabase secrets — Вова робить). Іконки адмінки — Tabler Icons (MIT), копіюю SVG в код (без нової залежності збірки).

**Досліджено (реальний код):** `admin.html` (1400р, окремий standalone-файл, свій Supabase-клієнт, БЕЗ спільних ES-модулів із сайтом) — вкладка `renderSpend()` (~795) найближчий шаблон для нової «Аналітика». `account-ui.js` — форма профілю (`openAccount`, 112-241), `SETTLEMENTS` вже підключено. `supabase.js` — `getAnonId()` (140-153) вже дає анонімний id, реюзаю для трекінгу гостей. `scripts/*.sql` — конвенція `supabase_<фіча>.sql`, idempotent, явний RLS. `supabase/functions/` — 3 готові Edge Functions (Deno/TS), новий буде за тим самим шаблоном (`send-chat-push` як приклад).

**🤝 Зона:** `admin.html` формально «Системне» (Вова), але фіча — спільна ініціатива (Вова просив статистику в своєму списку). Позначу в PR, роблю впевнено (принцип BOARD.md п.5).

### Кроки (13 кроків)
| # | Крок | Файли | Стан |
|---|------|-------|------|
| 1 | SQL: `analytics_events` (visitor_id, event_type, tab, ts, meta jsonb) — insert відкритий (анонім+акаунт), select лише `admins`. **Виправлено на кроці 4:** `profiles.age` НЕ додаю — `birth_date` вже є (`supabase_profiles.sql`, збирається в анкеті), вік рахую з нього на льоту | `scripts/supabase_analytics.sql` (новий) | ✅ `bee55ff8`, виправлено далі |
| 2 | `supabase.js`: `logEvent(visitorId,type,{tab,meta})` (fire-and-forget), `fetchAnalyticsSummary()` (агрегати: унікальні, по вкладках, пристрій, час доби) | `src/core/supabase.js` | ✅ `f1c1d8d4` |
| 3 | `logEvent` у точці зміни вкладки (`window.switchTab`, `app.js`) + перший перегляд дефолтної вкладки при `init()` + `PWA install` (`appinstalled`, `boot.js`) | `src/app.js`, `src/core/boot.js` | ✅ `da582d38` |
| 4 | ~~Профіль: поле «Вік»~~ — **не потрібно**, `birth_date` вже збирається (`openProfile`/`openAccount`, `account-ui.js:59-83,112-241`). Виправила SQL кроку 1 (прибрала зайву колонку `age`) | `scripts/supabase_analytics.sql` | ✅ `69f9f563` |
| 5 | `admin.html`: нова вкладка «Аналітика» — stat-cards (мірор `renderSpend`), прямі запити до `analytics_events` (унікальні сьогодні/7д/усього, PWA-встановлення, по вкладках/пристрою/годині). **Знахідка:** RLS `profiles` (own-row) не дає читати чужі профілі — нова SECURITY DEFINER `admin_profile_stats()` (мірор `is_admin()`) віддає лише агреговані кошики (село/вік), не сирі профілі | `admin.html`, `scripts/supabase_analytics.sql` | ✅ `b94e5032` |
| 6 | `admin.html`: 9 вкладок емодзі→Tabler SVG (реальні path з github.com/tabler/tabler-icons, MIT), кнопка «Оновити статистику» — задизейблена заглушка (Потік 2 підключить) | `admin.html` | ✅ `88a6dbaf` |
| 7 | Тест-блок: `node --check` усіх `.js` + `node build.js`, `admin.html` (вилучений inline-скрипт синтаксично чистий, баланс div/svg) | — | ✅ `96f196dd` |
| 8 | Playwright-смоук: перемикання вкладок з `logEvent` — 0 `pageerror`, `analytics_events` (ще не в БД) фейлить тихо (console.warn, не ламає UI) | — | ✅ зелено |
| 9 | CACHE_NAME bump | `sw.js` | ✅ `d3646bd0` — `cstl-20260712-0039`, фінально `cstl-20260712-0055` після мерджу |
| 10 | Реліз-нотатки + BOARD.md позначка (зона «Системне», Вова в курсі) | — | ✅ `14e07f06` |
| 11 | Брама деплою — чекати «деплой» | — | ✅ **ЗАДЕПЛОЄНО PR #354, `0d28b6ad`, live v2717 · 12.07 00:44, CACHE `cstl-20260712-0055`** |
| 12 | SQL-міграцію (`scripts/supabase_analytics.sql`) застосувати в Supabase — **MCP `supabase` без авторизації в цій сесії**, готовий SQL-блок для SQL Editor власнику | — | 🟡 дано Вові готовий SQL-блок у чаті — чекаю підтвердження запуску |
| 13 | Хендовер у Потік 2 (AI-кнопка) — чекає `ANTHROPIC_API_KEY` у Supabase secrets | — | 🟡 ключ уже додав Вова (за його словами) — Потік 2 можна планувати наступним /byyou |

**Оцінка:** 13 кроків × ~35K ≈ 450K токенів (~46% вікна) — великий потік, стежу за контекст-хуком (35%/55%/75%).
**Ризик:** середній — нова SQL-схема (застосовується вручну, MCP Supabase без авторизації в цій сесії), `admin.html` без бандлера (правки вручну, без esbuild-перевірки синтаксису як для `src/`).

### 📋 РЕЛІЗ-НОТАТКИ ПОТОКУ 6 (Фаза 3, перед деплоєм)

**ЩО ЗМІНИЛОСЬ:**
- Застосунок тепер рахує власну (без Google/Plausible) статистику: перегляди вкладок, встановлення `PWA`, тип пристрою, час доби — тихо, у фоні, не видно користувачу.
- Нова вкладка «Аналітика» в адмінці (`admin.html`) — унікальні відвідувачі сьогодні/7д/усього, розподіл по вкладках/пристрою/годині доби, розподіл по селах і віку (з наявних профілів, агреговано — жодних імен/дат конкретних людей адмінка не показує).
- Кнопка «Оновити статистику» (AI-висновок) — видно, але задизейблена: чекає ключ (Потік 2).
- 9 іконок вкладок адмінки: емодзі → векторні (Tabler Icons).

**ЩО МОЖЕ ЗЛАМАТИСЬ:**
- До ручного запуску `scripts/supabase_analytics.sql` в Supabase — вкладка «Аналітика» покаже нулі/помилку RPC (таблиця/функція ще не існують у БД). Сайт для звичайних відвідувачів не постраждає — `logEvent` ковтає помилку мовчки (`console.warn`, не кидає далі).
- `admin.html` редагувався вручну (без збірки/бандлера) — синтаксис перевірено вилученням inline-скрипту через Node, але живого кліку по новій вкладці в реальній адмінці не робила (потрібен адмінський акаунт, як і в попередніх потоках).

**ЩО ПЕРЕВІРИТИ:**
1. **Вова/Рома, першим ділом:** запустити `scripts/supabase_analytics.sql` у Supabase SQL Editor (інструкція — окремим блоком нижче після деплою).
2. Адмінка → вкладка «Аналітика» — цифри зʼявляються (після кроку 1 і кількох реальних відвідувань сайту).
3. Будь-яка вкладка сайту (Новини/Дошка/Автобуси) — перемикання працює як завжди, без затримок/помилок.
4. Встановити застосунок як PWA на телефон → в адмінці лічильник «встановлень PWA» +1.
5. Адмінка → всі 9 вкладок — іконки векторні, не емодзі, вирівняні з текстом.

---

## 🧩 ПОТІК 5 (11.07) — Обговорення: FAB, іконки, лічильники, клавіатура композ-модалки

**Джерело:** список Вови (💬 Обговорення, 6 пунктів → згруповано у 4 задачі, Статистика-блок відкладено як окрема розмова). Прочитано реальний код: `board.js` (FAB 955-1006, `renderChatCard` 911-941, `openDiscussionCompose` 456-498, `openChatModal` keyboard-pattern 600-630, click-делегація 1716-1780, realtime 1780-1822), `core/modal.js`, `style/community.css` (FAB-блоки, footer-картки).

**⚠️ Знахідка перед стартом (важливо для кроку 3):** сьогодні ж (`ce94a75f`, «Д-13») Вова ПОВНІСТЮ прибрав реакції з оголошень Дошки («на маркетплейсі не доречні») — видалено весь UI (тригер/попап/CSS ~130 рядків), АЛЕ дата-шар лишився недоторканим: `supabase.js` має готові `fetchAllReactions`/`setReaction`/`subscribeReactions`/`getAnonId` (таблиця `reactions`, RLS уже налаштовано — лайк лише для залогінених). План — переюзати цю ІСНУЮЧУ інфраструктуру для лічильника лайків Обговорень (без нової схеми/ключів, тому не 🔴). Але це та сама концепція, яку щойно навмисно прибрали з сусіднього типу постів — якщо це небажаний збіг, скажи «зміни крок 3».

### Кроки (10 кроків)
| # | Крок | Файли | Стан |
|---|------|-------|------|
| 1 | Задача 1 — стандартизувати FAB: спільний базовий CSS (розмір/заливка/тінь) для `.board-trigger--fixed` (Дошка) і `.disc-fab-plus` (Обговорення) через один блок; лишити label-текст+бейдж лише на Дошці (функціонально потрібні — підменю з 4 діями+непрочитані), Обговорення — той самий вигляд без них (просте «+», 1 дія) | `board.js:955-1006`, `community.css:1046-1067,1608-1623,2937-2954` | ✅ `79593764` |
| 2 | Задача 2 (частина А) — емодзі→SVG: порожній стан (302), лічильник у шапці чат-модалки (399,519), заголовок «Обговорення» (1068). Реюз наявного невикористаного `COMMENT_ICON_SVG` (💬) + нова іконка-мегафон для заголовка | `board.js:47,302,399,519,1068` | ✅ `3aa5dfc1` |
| 3 | Задача 3 — лічильник лайків: новий стан `reactionsByPost` (Map, вантажиться в `renderBoard()` через `fetchAllReactions`), клік-делегація (`data-like-id`, гейт `requireAuth`, single emoji ❤️, upsert/delete через `setReaction`), realtime (`subscribeReactions`) — мірор патерну коментарів (`commentsByPost`/`onCommentRealtimeEvent`) | `board.js:108-138,911-941,1213-1235,1716-1822` | ✅ `a4c09696` |
| 4 | Задача 2 (частина Б) + Задача 4 — у тому ж `renderChatCard`: замінити 👥 на нову SVG-іконку, перемістити лічильник учасників у верхній правий кут (над `.bd-chat-last`, навпроти 💬), лайк-кнопка (♡/♥+число) стає на місце учасників (низ футера, зліва) | `board.js:911-941`, `community.css:2860-2954` | ✅ `a4c09696` (разом з кроком 3) |
| 5 | Задача 4 — клавіатура композ-модалки: прибрати синхронний `ta?.focus()` (клавіатура більше не вилітає одразу); додати `openDiscSheet` проброс `onClose` (для коректного знімання listener'ів); у `openDiscussionCompose` — visualViewport-логіка зсуву аркуша під клавіатуру (мірор `applyKb`-патерну з `openChatModal`, спрощено під `app-modal-sheet`) | `board.js:419-432,456-498` | ✅ `2e3bc24e` |
| 6 | `sw.js` — підняти `CACHE_NAME` (формат `cstl-YYYYMMDD-HHMM`) | `sw.js` | ✅ `2e3bc24e` — `cstl-20260711-1830` |
| 7 | Тест-блок: `node --check` усіх змінених + `node build.js` + `check-imports.js` | — | ✅ 27 файлів чисто |
| 8 | Playwright-смоук: FAB відкриття/закриття (Дошка+Обговорення), рендер нових іконок (0 літеральних емодзі), лайк-клік+лічильник+позиція учасників, композ-модалка відкривається без миттєвої клавіатури + зсув при фокусі на textarea (мок `visualViewport`) | — | ✅ усе зелено (деталі нижче) |
| 9 | `/audit`-погляд змінених ділянок (баланс div, дублі, мертвий код) + реліз-нотатки в цьому файлі | — | ✅ diff чистий, дублів/мертвого коду нема |
| 10 | Брама деплою — реліз-нотатки, чекати «деплой» | — | ✅ **ЗАДЕПЛОЄНО PR #349, `eb13f807`, live v2702 · 11.07 19:40, CACHE `cstl-20260711-1845`** |

### 📋 РЕЗУЛЬТАТИ СМОУКУ (крок 8, Playwright, гість-режим, fallback JSON-дані)
- **FAB Обговорень**: клас `cm-board-trigger board-trigger--fixed` (той самий що Дошка, без окремого `disc-fab-plus`), меню відкривається (3 пункти), 0 помилок консолі.
- **FAB Дошки (регресія)**: клас незмінний, label+бейдж на місці, 4 пункти меню працюють — уніфікація FAB Обговорень Дошку не зачепила.
- **Іконки**: у корені `#disc-content` — 0 літеральних 💬/👥/📢 (перевірено `TreeWalker` по текстових вузлах); єдине лишнє входження 💬 — іконка вкладки в таб-барі (поза скоупом задачі).
- **Картка обговорення**: `.bd-chat-topline` (💬-лічильник зліва, 👥-учасники справа) + `.bd-chat-like` кнопка в футері — усі присутні в DOM.
- **Лайк-клік (гість)**: `requireAuth` гейтить коректно — тост «увійдіть», стан кнопки НЕ змінюється, 0 помилок.
- **Композ-модалка «Створити обговорення» (гість)**: клік по FAB-пункту гейтиться `requireAuth` (тост, модалка не відкривається) — очікувано, живий тест самого відкриття/клавіатури потребує тестового акаунта (як і в Потоці C2).

**Оцінка:** 10 кроків × ~35K ≈ 350K токенів (~36% вікна).
**Ризик:** середній — крок 3 чіпає realtime+click-делегацію (спільний код з коментарями/збереженням), крок 5 чіпає клавіатурну логіку (iOS PWA специфіка, важко перевірити без живого iPhone).

### 📋 РЕЛІЗ-НОТАТКИ ПОТОКУ 5 (Фаза 3, перед деплоєм)

**ЩО ЗМІНИЛОСЬ:**
- FAB-кнопка «+» Обговорень тепер виглядає так само, як FAB Дошки (той самий скляний стиль/розмір) — раніше була візуально іншою (суцільна заливка).
- 💬/👥/📢 (порожня тема, лічильник повідомлень у шапці чату, заголовок «Обговорення») — тепер векторні іконки замість емодзі.
- Картка теми обговорення: зверху над прев'ю останніх повідомлень — лічильник повідомлень (зліва) і лічильник учасників (справа, раніше був унизу картки).
- Новий ❤️ лічильник лайків — на місці, де раніше був лічильник учасників (унизу картки зліва). Лайк ставить лише залогінений (як і збереження) — гість побачить пропозицію увійти.
- Модалка «Створити обговорення»: клавіатура більше не вилітає одразу при відкритті аркуша — з'являється лише при тапі в поле, і аркуш тоді сам підіймається над клавіатурою.

**ЩО МОЖЕ ЗЛАМАТИСЬ:**
- Лайк реюзає ту саму Supabase-таблицю `reactions`, яку сьогодні ж повністю прибрали з оголошень Дошки (Д-13, окреме рішення) — концептуально інше застосування (одна емоція ❤️ для тем, не набір реакцій для товарів), але якщо це небажаний збіг — легко відкотити лише крок 3-4.
- Живий тест композ-модалки (клавіатура/зсув) не робила — вхід гейтований `requireAuth`, немає тестового акаунта в цій сесії (та сама відома межа, що й у Потоці C2). Логіка мірорить вже перевірений `applyKb`-патерн з `openChatModal`.

**ЩО ПЕРЕВІРИТИ НА IPHONE:**
1. Обговорення → «+» — кнопка виглядає як на Дошці (скло, не суцільний червоний круг).
2. Будь-яка картка обговорення → лічильники повідомлень/учасників зверху, ❤️-лайк внизу зліва — тап лайкає (стає червоним, число +1), повторний тап знімає.
3. Обговорення → «+» → «Створити обговорення» → аркуш виїжджає знизу БЕЗ клавіатури → тап у поле «Тема» → клавіатура з'являється, аркуш підіймається так, щоб поле+кнопка «Створити» лишались видимими.
4. Відкрити тему з повідомленнями → у шапці чату лічильник (векторна іконка, не 💬).
5. Дошка (не Обговорення) → «+» — переконатись що виглядає й працює як раніше (label «Подати оголошення», 4 пункти меню).

---

## 🧩 ПОТІК 4 (10.07) — Потік C2 звужено: Board ad modal + Disc sheets

**Джерело:** `docs/PLAN_MODALS_STANDARDIZATION.md`, підпотік C2 («sheets/compose»). План від 07.07 називав 4 цілі — прочитала реальний код board.js, 2 з них виявились архітектурно непридатні для спільного примітиву:
- **Ad zoom sheet** — насправді ДВІ дубльовані реалізації (`expand()` — FLIP-морфінг картки в модалку за координатами джерела; `openAdModalStandalone()` — той самий вигляд без картки-джерела). Морфінг несумісний з примітивом (немає підтримки «росту з елемента»).
- **Discussion chat modal** (`openChatModal`) — keyboard-aware realtime чат: динамічно рухає top/height/bottom під клавіатуру, жести на бульбашках, скрол-пігулка. Конфліктує з фіксованою `position:fixed;inset:0` моделлю примітиву.

**Рішення (підтверджено Ромою):** мігрую лише 2 архітектурно прості — Board ad modal + Disc sheets. Морфінг і realtime-чат лишаю як є (задокументую причину в PLAN_MODALS_STANDARDIZATION.md, не спроба-і-відкат).

### Кроки (9 кроків)
| # | Крок | Файли | Стан |
|---|------|-------|------|
| 1 | Мігрувати Board ad modal (`openBoardModal`) на примітив, sheet-варіант. Заголовок+підзаголовок лишаються в bodyHtml (власний шрифт, не generic `.app-modal-title`) — лише chrome (backdrop/panel/handle/close/swipe) на примітив, свій custom-свайп прибрати (primitive's вбудований еквівалентний) | `src/tabs/community-modal.js` | ✅ `8c091cbb` — знайдено й зберіг спец. z-index:2700 (модалка відкривається і з екрана «Мої оголошення», z-index 2401 — інакше ховалась би під ним) через модифікатор `.app-modal--board-compose` |
| 2 | Мігрувати Disc sheets (`openDiscSheet`) — тонка обгортка над примітивом, зберегти сигнатуру `openDiscSheet(opts)→close` і `onMount(sheet,close)` (2 аргументи) щоб не чіпати 4 виклики (`openMyDiscussions`/`openSavedDiscussions`/`openDiscussionCompose`+`openDiscussionList`) | `src/tabs/board.js` | ✅ `8c091cbb` |
| 3 | Прибрати мертвий CSS: `.cm-board-modal/backdrop/panel/handle/close` (НЕ чіпати `.cm-board-modal-note/gallery/dots/...` — то Ad zoom sheet, інша функція, лишається), `.disc-sheet-backdrop/handle/head/close` (лишити `.disc-sheet-title/body/list/empty` — контент) | `style/community.css` | ✅ `8c091cbb` |
| 4 | Тест-блок: `node --check`+build+check-imports, Playwright-смоук (відкрити форму оголошення, заповнити поле, свайп-закрити; відкрити «Мої обговорення»/«Створити обговорення», перевірити submit), регресія Дошки | — | ✅ 26 файлів чисто; обидва входи в реальному UI за `requireAuth` (гість не пройде) — протестувала ПРЯМИМ ES-module викликом (як юніт-тест). **Знайшла й виправила реальний баг:** `openDiscSheet` мав temporal dead zone — `close` читався в `onMount` до завершення деструктуризації, кидало `ReferenceError` при КОЖНОМУ відкритті. Виправлено (`let`+відкладена стрілка), обидва тести зелені (position:fixed, z-index:2700 збережено, форма+прев'ю+close працюють) |
| 5 | Оновити `docs/PLAN_MODALS_STANDARDIZATION.md`: C2 — 2/4 done, задокументувати архітектурне рішення не чіпати morph/realtime-чат (з поясненням чому) | `docs/PLAN_MODALS_STANDARDIZATION.md` | ✅ `d978df1c` |
| 6 | Брама деплою — реліз-нотатки, чекати «деплой» | — | ✅ **ЗАДЕПЛОЄНО PR #328, `eaf6824c`, live v2654 · 10.07 22:48, CACHE `cstl-20260710-2246`** |

### 📋 РЕЛІЗ-НОТАТКИ ПОТОКУ C2 (Фаза 3, перед деплоєм)

**ЩО ЗМІНИЛОСЬ:**
- Форма «Нове оголошення» на Дошці тепер використовує спільний компонент модалки (той самий що Погода/Акаунт/Сайдбар із Потоку C1) — менше дубльованого коду, той самий стиль закриття (свайп/✕/поза формою/ESC).
- Bottom-sheet «Мої обговорення» / «Збережені обговорення» / «Створити обговорення» — те саме.
- Візуально майже непомітно — форми виглядають і поводяться так само, як і раніше.

**🐛 ЗНАЙДЕНО Й ВИПРАВЛЕНО (реальний баг, не з цієї міграції — існував би в БУДЬ-якому коді з такою структурою):**
`openDiscSheet` (обгортка над примітивом) мала помилку TDZ (temporal dead zone) — змінна `close` читалась усередині `onMount` РАНІШЕ, ніж завершувалось її присвоєння. Це кидало `ReferenceError` при КОЖНОМУ відкритті «Мої обговорення»/«Створити обговорення»/«Збережені обговорення». Спіймано власним тестом до деплою (прямий виклик функції, оминаючи гейт логіну), виправлено (`let` + відкладена стрілка-обгортка).

**ЩО МОЖЕ ЗЛАМАТИСЬ:**
- Мінімальний ризик — обидва входи гейтовані `requireAuth` (лише залогінені бачать ці екрани), тому живий UI-клік не можу перевірити в цій сесії (немає тестового акаунта) — перевірила прямим викликом функцій (юніт-тест), поведінка ідентична.
- Форма «Нове оголошення» з екрана «Мої оголошення» (не лише з Дошки) — перевірено з коду що z-index (2700) збережено, живого кліку з того екрана саме не робила.

**ЩО ПЕРЕВІРИТИ НА IPHONE (потрібен залогінений акаунт):**
1. Дошка → «+» → «Подати оголошення» → форма відкривається знизу, заповнити поле «Заголовок» → прев'ю оновлюється одразу.
2. Свайп вниз по формі (не по полю вводу) → закриває.
3. Обговорення → «+» → «Створити обговорення» — відкривається без помилки (раніше падало з ReferenceError).
4. Обговорення → «+» → «Мої обговорення» / «Збережені обговорення» — список відкривається.
5. З «Мої оголошення» (кабінет) → «+ нове оголошення» → форма відкривається ПОВЕРХ екрана (не ховається під ним).

**Оцінка:** 6 кроків × ~35K ≈ 210K токенів (~22% вікна) — менше за C1, менший обсяг.
**Ризик:** низький — обидві цілі вже мають майже ідентичну до примітиву логіку свайпу/backdrop. `board.js` активно редагує Вова (зона Дошки) — торкаюсь лише 2 конкретні функції (`openDiscSheet`+4 виклики), не структуру шапки/скролу що він щойно міняв.

---

## 🧩 ПОТІК 3 (10.07) — Потік C1: спільний примітив модалки + 4 прості overlay

**Джерело:** `docs/PLAN_MODALS_STANDARDIZATION.md` (відкладено 07.07, Рома обрав продовжити 10.07). Інвентар звірено з реальним кодом сьогодні — план застарів: `.sr-modal` видалено (Батч 7), з'явився новий патерн `.shub-sheet` (хаб «Збережені»), «Disc actions sheet» тепер дублюється у 2 файлах. Разом 15 модалок (не 14), 3 підпотоки (C1/C2/C3) — це C1, найменший ризик.

**Досліджено (реальний код, не з пам'яті):** `account-ui.js` вже має власний міні-примітив `openModal(innerHtml)` (center-варіант, `scale()`); `power.js`/`sidebar.js`/`community-blocks.js` (погода) — кожен свій bottom-sheet (`translateY()`) з майже ідентичною структурою (backdrop+panel+handle+close). Article-modal (`style/modal.css`) — найближче до готового sheet-еталону (sticky header з іконками, вже зроблено Б5.1).

### Кроки (10 кроків)
| # | Крок | Файли | Стан |
|---|------|-------|------|
| 1 | Новий `src/core/modal.js`: `openModal({title,bodyHtml,variant,onMount,swipeClose})` → `{close,el}`. Variant `sheet` (bottom, handle, swipe-down) і `center` (scale-in). Реюз патернів з article-modal + account-ui.js | `src/core/modal.js` (новий) | 🟢 |
| 2 | CSS: `style/modal.css` — генеричні `.app-modal*` класи (backdrop/sheet/center/close), кольори з існуючих реалізацій (не вигадувати нові) | `style/modal.css` | 🟢 |
| 3 | Мігрувати Power help-модалку (`openQueueHelpModal`) на примітив, sheet-варіант | `src/tabs/power.js` | 🟢 |
| 4 | Мігрувати Sidebar info-модалку (`openInfoModal`, враховуючи `--doc` варіант для правового тексту) | `src/core/sidebar.js` | ✅ `d57d9589` — смоук зелений (Підтримка + Політика --doc, max-height 776px = 92svh) |
| 5 | Мігрувати Account-модалку (`openModal` у account-ui.js) на примітив, center-варіант — власний API `openModal(innerHtml)` лишається (тонка обгортка над новим) | `src/core/account-ui.js` | ✅ `9d74a4d0` + критичний фікс `249fe534` |

**⚠️ Знахідка кроку 5 (критичний баг у `style/modal.css`, вплинув на КРОКИ 4+5):** коментар `(pw-help-*/acc-*)` у шапці файлу містив буквальний `*/`, який передчасно закрив CSS-коментар і "з'їв" саме правило `.app-modal { position: fixed; ... }`. Без нього sidebar-модалки (крок 4) і account-модалка (крок 5) рендерились у звичайному потоці сторінки (нижче видимого екрана), а НЕ як overlay поверх контенту — Playwright-смоук кроку 4 це не зловив (перевіряв лише класи/текст, не позицію), скріншот тоді показав "порожній" екран, що я помилково списав на таймінг. Знайдено зараз через діагностику чому кнопка «Поки пропустити» не клікалась. Виправлено `249fe534`, живою перевіркою підтверджено: усі 3 вже мігровані модалки (Sidebar Support, Sidebar Policy --doc, Account Join) тепер `position:fixed` і коректно в межах екрана (скріншоти). Крок 3 (Power) тим самим фіксом теж захищений (спільний файл), але живого смоуку Power через дата-залежний онбординг (потребує мережі) в цій сесії не було — **рекомендую Вові перевірити Power-довідку наживо на iPhone окремо**.
| 6 | Мігрувати Weather-модалку (`openWeatherDayModal`), sheet-варіант — **графіки/скрабер НЕ чіпати**, лише chrome (backdrop/sheet/close/swipe) | `src/tabs/community-blocks.js` | ✅ `e345611a` — додано модифікатор `.app-modal--weather` (синій градієнт, власні відступи/max-height), `wireWeatherSwipe` переведено на `.app-modal-sheet`+`close()` від примітиву; `wxLineChart`/`wxBarChart`/`wireWeatherScrubber` НЕ чіпав. Смоук: мокнутий Open-Meteo API, обидва графіки рендеряться, скрабер працює (курсор+бульбашка), close-X закриває |
| 7 | Прибрати мертвий CSS після міграції — sidebar.css/account.css/community.css вже прибрані інлайн у кроках 4/5/6. Лишилось `.pw-help-modal/backdrop/panel/handle/close/title` (крок 3 не чіпав) | `style/power.css` | ✅ прибрано, `node --check`+build+check-imports зелені |
| 8 | Тест-блок: `node --check` усіх, Playwright-смоук (відкрити/закрити кожну з 4 модалок, перевірити контент+свайп+ESC, регресія інших вкладок) | — | ✅ усі перевірки зелені (деталі нижче) |

**📋 Результати тест-блоку кроку 8:**
- `node --check` + `node build.js` + `check-imports.js` — 26 файлів, без помилок.
- **Account Join (center):** `position:fixed`, картка в межах екрана (y≈279, центровано), закриття (Escape) прибирає модалку, body-скрол-лок знімається коректно.
- **Sidebar Support (sheet):** `position:fixed`, аркуш знизу в межах екрана, закриття/скрол-лок ОК.
- **Sidebar Policy --doc (sheet):** те саме + вища панель (max-height 92svh), контент (TOC/заголовки) рендериться.
- **Weather (sheet --weather):** мокнутий Open-Meteo API, синій градієнт-аркуш, обидва графіки (температура+опади) рендеряться, **скрабер працює** (курсор+бульбашка з іконкою/годиною/значенням), close-X закриває, body-скрол-лок знімається.
- **Power help (sheet):** код мігровано (крок 3) і використовує той самий примітив що й 4 вище перевірені — АЛЕ **кнопку вкладки «Світло» приховано з таб-бару ще 16.05.2026** (`index.html:192` — закоментовано, «не актуально, світло не відключають»), тому живий UI-смоук фізично неможливий у цій сесії. Не регресія цього потоку — попередній стан, знайдено під час діагностики.
- **Регресія інших вкладок:** Громада/Шо в селі/Дошка/Автобуси — перемикаються і рендерять нормально.
| 9 | Оновити `docs/PLAN_MODALS_STANDARDIZATION.md` — C1 ✅, інвентар звірено, C2/C3 лишаються | `docs/PLAN_MODALS_STANDARDIZATION.md` | ✅ статус змінено з «відкладено» на «C1 done», інвентар звірено (Saved-routes прибрано з батчу 7, застарілий запис виправлено) |
| 10 | Брама деплою — реліз-нотатки, чекати «деплой» | — | ✅ **ЗАДЕПЛОЄНО PR #322, `a0521717`, live v2643 · 10.07 20:10, CACHE `cstl-20260710-1929`** |

### 📋 РЕЛІЗ-НОТАТКИ ПОТОКУ C1 (Фаза 3, перед деплоєм)

**ЩО ЗМІНИЛОСЬ:**
- Створено спільний компонент модалки (`core/modal.js`) — 4 модалки тепер використовують одну й ту саму логіку відкриття/закриття/свайпу замість 4 окремих копій коду.
- Мігровано: довідка «Як дізнатись чергу» (Світло — UI зараз прихований), «Підтримка»/«Політика і приватність» (бургер-меню), «Приєднайтесь до громади»/«Доповніть профіль» (акаунт), «Погода по годинах» (Громада → клік на день прогнозу).
- Прибрано ~150 рядків дубльованого CSS-коду (backdrop/панель/handle/close-кнопка більше не копіюються в кожному файлі).
- Побічний ефект (косметичний, узгоджений з дизайн-системою): модалки «Приєднайтесь»/«Доповніть профіль» і «Погода» тепер мають кнопку ✕ у верхньому правому куті (раніше закривались лише тапом повз/свайпом) — консистентно з іншими модалками застосунку.

**🐛 ЗНАЙДЕНО Й ВИПРАВЛЕНО (критичний баг, не видно було без глибокого тесту):**
Коментар у новому `style/modal.css` випадково містив символи `*/` всередині тексту — це передчасно закрило CSS-коментар і «з'їло» ключове правило, яке робить модалку накладенням поверх екрана. Наслідок: усі 4 модалки рендерились би НИЖЧЕ видимої частини екрана (як частина звичайної сторінки), а не як спливаюче вікно — кнопки фізично не влізали в екран і не натискались. Знайдено й виправлено в цій же сесії до деплою, живим тестом підтверджено що всі 4 модалки тепер відкриваються правильно.

**ЩО МОЖЕ ЗЛАМАТИСЬ:**
- Мінімальний ризик — усі 4 модалки перевірені живим браузерним тестом (позиція на екрані, відкриття/закриття, вміст, для Погоди — ще й перетягування графіка пальцем).
- Модалка «Довідка про чергу» (Світло) технічно мігрована, але кнопка вкладки «Світло» прихована в UI ще з 16.05.2026 (не моя зміна) — живий смоук на iPhone неможливий, поки цю вкладку не повернуть.

**ЩО ПЕРЕВІРИТИ НА IPHONE:**
1. Бургер-меню → «Підтримка» — модалка знизу, кнопка «✉️ Написати в підтримку» працює.
2. Бургер-меню → «Політика і приватність» — довгий документ, скролиться, є навігація по розділах зверху.
3. Іконка 👤 (гість) → «Приєднайтесь до громади» — картка по центру екрана, кнопка ✕ закриває, кнопка Google працює.
4. Громада → блок погоди → тап на будь-який день прогнозу → аркуш знизу з 2 графіками (температура/опади), перетягни пальцем по графіку — з'являється бульбашка з годиною і значенням.

**Оцінка:** 10 кроків × ~35K ≈ 350K токенів (~36% вікна). Сесія вже довга (кілька потоків сьогодні) — якщо контекст-хук покаже 🟡35%/🟠55% по дорозі, зупинюсь і задокументую «де зупинились» як завжди.

**Ризик:** мінімальний — 4 НАЙПРОСТІШІ overlay з 15 (свідомий вибір плану), складні (chat/lightbox/popover) — окремі підпотоки C2/C3 пізніше.

---

## 🚀 ПОТІК 2 (10.07) — Батч 5-8 + аудит перенаправлень, розбито на 2 сесії (за проханням Роми — вміститись у 75-80% контексту)

**Рішення Роми (дослівно, 10.07):** «Вові не передавай роботу. Склади план під /byyou і роби сам в цій сесії.» → Батч 6 і 7 (позначені в майстер-плані як «крос-зона Вові») роблю САМА — торкаюсь `board.js`/`community.css`/`buses.js`, позначаю це в PR як завжди (BOARD п.5 «зона = хто головний, не стіна»), не чекаю Вову.
Пізніше того ж дня: «Можеш частину задач розділити для наступної сесії щоб вміститися в 75-80% контексту» → розбито на СЕСІЮ А (ця, Батч 5+6+7) і СЕСІЮ Б (наступний чат, Батч 8+аудит). Межа обрана за логікою домену (UI-візуал vs push-інфра), не довільно по номеру кроку.

### СЕСІЯ А (ця сесія) — Батч 5+6+7, 10 кроків
| # | Крок | Файли | Стан |
|---|------|-------|------|
| 1 | Б5.1 — 3 іконки (📤🔔🔖) у `.modal-sticky-header` | `index.html:151-160`, `style/modal.css` | ✅ `36f0b597` |
| 2 | Б5.2 — events.js: підключити іконки, прибрати `.ev-ics-btn`+`.share-btn--inline` знизу+мертвий CSS | `events.js:170-197`, `style/events.css` | ✅ `59ba2510` |
| 3 | Б5.3 — news.js: підключити іконки (share+save, нове `localStorage cstl_saved_articles`), прибрати нижній `.share-btn--inline` | `news.js:200-215` | ✅ `9d3aff49` |
| 4 | Б5.4 — saved-hub.js: секція «Збережені статті» (реюз хаба) | `saved-hub.js` | ✅ `7ed9aad6` |
| 5 | Б6.1 — прибрати `transform` з `.board-fab-item` (лишити opacity), пом'якшити white-glass кнопки | `community.css:1718-1788` | ✅ `b2d47623` |
| 6 | Б7.1 — export `getSavedRoutesForUI` (вже є, рядок 1845) + нова `openSavedRouteOnBuses(rid,date,from,to)` | `buses.js:1845+` | ✅ `cde6e98d` |
| 7 | Б7.2 — saved-hub.js: секція 🚌 автобуси, фікс гілки `if(!ids.length)return` | `saved-hub.js:46-49` | ✅ `51e1829b` |
| 8 | Б7.3 — прибрати `#saved-routes-btn`+DOM-частину `initSavedRoutesHeader` (⚠️ зберегти завантаження `loadTrackedRoute`/`hydrateTrackedFromDB`/`onAuthChange`), прибрати `openSavedModal/renderSavedRows/updateSavedBadge`+CSS `.sr-*` | `buses.js:1922-2075`, `index.html:37-43`, `app.js:213` | ✅ `84f950ee` |
| 9 | Тест-блок: `node --check` усіх, Playwright-смоук по зачеплених екранах (модалка статті, FAB, автобуси+хаб) | — | ✅ усі перевірки зелені |
| 10 | Брама деплою — реліз-нотатки, чекати «деплой» | — | ✅ **ЗАДЕПЛОЄНО PR #319, `bb75f97a`, live v2639, CACHE `cstl-20260710-1547`** |

**Оцінка СЕСІЇ А:** 10 кроків × ~35K ≈ 350K токенів — укладається в 75-80% разом з уже використаним контекстом сесії.

### 📋 РЕЛІЗ-НОТАТКИ СЕСІЇ А (Фаза 3, перед деплоєм)

**ЩО ЗМІНИЛОСЬ:**
- Модалка статті/події: 3 нові іконки зверху (📤 поділитись · 🔔 нагадування — лише події/свята · 🔖 зберегти — лише статті, нове). Старі великі кнопки внизу прибрані.
- Хаб «Збережені» (🔖 в шапці): +2 нові секції — 📰 Статті (доступно й гостю) і 🚌 Автобуси.
- Окрема іконка-закладка «Збережені рейси» в шапці зникла — той самий функціонал тепер у хабі 🔖.
- FAB-меню Дошки (+): анімація появи пунктів менш смикана (тільки fade, без ковзання).

**ЩО МОЖЕ ЗЛАМАТИСЬ:**
- Якщо у когось у localStorage лишився стан старої кнопки-закладки — нешкідливо, просто не використовується.
- Перехід «Автобуси» з хаба на дуже старий/майбутній (>2 тижні) збережений рейс — тижнева смужка покаже сторінку 0 (не знайде точну), сам рейс все одно спробує проскролити.

**ЩО ПЕРЕВІРИТИ НА IPHONE:**
1. Відкрити будь-яку новину → 3 іконки зверху, 🔖 зберігає (стає бордовим), у хабі зʼявляється.
2. Відкрити подію/свято в «Шо в селі» → 🔔 нагадування зверху працює (завантажує .ics), 🔖 не показується.
3. Хаб 🔖 → секція «Статті» видно навіть БЕЗ входу в акаунт.
4. Зберегти рейс в Автобусах → відкрити хаб → тап по рейсу → переходить на Автобуси, потрібний день+рейс підсвічується.
5. Дошка → кнопка «+» (FAB) → відкриття меню плавне, без «привида»/подвійного контуру.

### СЕСІЯ Б (Батч 8 + аудит) — 7 кроків
| # | Крок | Файли | Стан |
|---|------|-------|------|
| 11 | Б8.1 — `ensureChatPush()` в `openChat`, спільний модуль VAPID+urlBase64 (реюз buses.js:17-141) | `board-chat.js:58`, `core/push.js` (новий) | ✅ `bccd2494` |
| 12 | Б8.2 — банер/тост на chat/group push (P-8) + title/body/thread_id у postMessage | `sw.js:159`, `board-chat.js:1141` | ✅ `72bd6bd5` |
| 13 | Б8.3 — thread_id у notification data (P-9), notificationclick→postMessage, `openThreadById` | `sw.js:162-176`, `board-chat.js`, `app.js` (hash-роутинг холодного старту) | ✅ `4dff69c6` |
| 14 | Б8.4 — код P-2 (photo_url select+fallback body) — **комічу, живий деплой Edge Function НЕ можу** (нема Supabase CLI доступу з цієї сесії — інфраструктурне обмеження, не хендофф) | `supabase/functions/send-chat-push/index.ts:49,73` | ✅ код `ba5679c0` / 🔴 деплой (за Вовою) |
| 15 | Аудит перенаправлень інших блоків Громади (Дошка-віджет/автобус/погода/контакти) | `community-blocks.js`, `board.js` (export `openAdModalStandalone`) | ✅ `a79239b7` — 2 реальні знахідки: Дошка-віджет тепер відкриває конкретний пост (не вкладку); автобус-картка тепер відкриває конкретний рейс (раніше не робила НІЧОГО при тапі) |
| 16 | Тест-блок: `node --check` усіх, Playwright-смоук | — | ✅ усі перевірки зелені (скріншот: зум-модалка відкрила саме тапнутий пост) |
| 17 | Брама деплою — реліз-нотатки, чекати «деплой» | — | 🟡 чекаю слово |

### 📋 РЕЛІЗ-НОТАТКИ СЕСІЇ Б (Фаза 3, перед деплоєм)

**ЩО ЗМІНИЛОСЬ:**
- Приватний чат Дошки: тап «відкрити чат» тепер реально просить дозвіл push-сповіщень (раніше мовчав, якщо ти ніколи не вмикав сповіщення автобусів) — P-5.
- Push про нове повідомлення чату: якщо застосунок відкритий — з'являється банер зверху (тап відкриває розмову), раніше було тихо (тільки бейдж) — P-8.
- Тап по системній нотифікації чату (коли застосунок закритий/у фоні) — тепер відкриває САМЕ ту розмову, раніше просто відкривав застосунок на головній — P-9.
- Push про фото-повідомлення в чаті — код виправлено (раніше падав з помилкою, отримувач взагалі не отримував сповіщення) — P-2. **Живий ефект з'явиться лише після ручного `supabase functions deploy send-chat-push` (Вова).**
- Громада → міні-віджет Дошки: тап по конкретному оголошенню/обговоренню відкриває САМЕ його, не просто вкладку «Дошка».
- Громада → картка автобуса: тап тепер веде на вкладку Автобуси з підсвіченим САМЕ цим рейсом (раніше тап нічого не робив).

**ЩО МОЖЕ ЗЛАМАТИСЬ:**
- P-2 (фото-пуш) лишається зламаним на проді, доки Вова не задеплоїть Edge Function вручну.
- Банер push (P-8) — новий DOM-елемент `#chat-push-banner`, з'являється лише коли `document.visibilityState==='visible'` — на iOS PWA це може поводитись специфічно (не тестовано наживо).

**ЩО ПЕРЕВІРИТИ НА IPHONE:**
1. Ще НЕ вмикав push (жоден дозвіл) → відкрити чиюсь розмову в Дошці → має з'явитись системний запит дозволу.
2. Отримати повідомлення в чаті, коли застосунок відкритий → банер зверху, тап відкриває розмову.
3. Отримати повідомлення, коли застосунок закритий → тап по системній нотифікації → відкриває САМЕ ту розмову (не головну).
4. Громада → тап по картці Дошки/Автобуса → веде на конкретний пост/рейс, не просто вкладку.
5. (Після деплою Edge Function Вовою) — фото-повідомлення в чаті доходить пушем отримувачу.

**Ціль:** Майстер-план 18 задач Роми (`PLAN_2026-07-09_batch.md`), 8 батчів. Виконуємо по черзі, кожен = окремий деплой.
**Власник:** Рома · **Гілка (поточна сесія):** `claude/new-session-gj49q9` (харнес цієї сесії закріпив саме її, не `3k5u9n` — деталі в SESSION_STATE_ROMA.md 10.07) · **Rollback-теги:** byyou-start-agent / -tablo2 / -weather

## Прогрес батчів
- ✅ **Батч 1 (AI-агент витрати/якість)** — ЗАДЕПЛОЄНО PR #296. Корінь витрат вбито (idempotent backoff), кап на всю чергу, свято-дедуп, фото поважає holidays.json. 🔴 Вові: `scripts/supabase_cms_dedup.sql` (чистка 16 дублів) — інструкція дана.
- ✅ **Батч 2 (Табло+Hero)** — ЗАДЕПЛОЄНО PR #301 (кути, hero-кроп 55%, блюр 14px).
- ✅ **item 10 (зона скролу Табло)** — ЗАКРИТО Вовою на main (padding-підхід). Мій margin-інсет (коміт 61d6c7f0) відкинуто при мерджі PR #310 на користь уже задеплоєної версії Вови — результат ідентичний (винна рамка боків = скрол сторінки). Партнерство: не клобрив живу версію.
- ✅ **Батч 3 (Модалка погоди)** — ЗАДЕПЛОЄНО PR #310 (CACHE `cstl-20260710-0807`). wxLineChart (2год+права шкала°), wxBarChart (сині бари+права шкала%+2год), скрабер (снап по годинах, спільний курсор на обох графіках, бульбашка іконка+година+значення), свайп-вниз закриття, CSS (wx-grid/wx-cursor/wx-readout).
- ✅ **Батч 4 (Події: фото+перенаправлення)** — ЗАДЕПЛОЄНО PR #314 (CACHE `cstl-20260710-1339`). `renderEventBlock` пробрасує `id`+`image`; мініатюра 44×44 у картці карусель (тільки коли є фото); тап відкриває САМЕ цю подію/свято (`openShotamModal` export) замість глухого перемикання вкладки. Побічно: фікс `weatherCodeInfo()` WMO 85/86 (снігові зливи, `🌨️`) — знайдено brain-контролем. ⏸️ Дошка-віджет (тап→вкладка, не на пост) — свідомо не робив, лишається окремим пунктом.

## ▶️ ЯК ПРОДОВЖИТИ (новий чат)
1. `git fetch origin main` → перевірити чи Вова щось не змінив у зоні наступного батчу.
2. Якщо СЕСІЯ А (кроки 1-10, Батч 5+6+7) ще не задеплоєна — доробити/задеплоїти її спершу.
3. Якщо СЕСІЯ А задеплоєна — продовжити з **СЕСІЯ Б** (кроки 11-17 вище: Батч 8 Push + аудит перенаправлень). Карта коду батчів — `PLAN_2026-07-09_batch.md`.

## Ще у майстер-плані (не батчі): контент п.4 (переписати 4 свята-статті) — чекає уточнення Роми (де/які); зона-принцип у BOARD п.5.

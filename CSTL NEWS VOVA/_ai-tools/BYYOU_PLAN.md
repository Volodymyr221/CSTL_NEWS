# BYYOU_PLAN — стан потоку /byyou (CSTL)

**Статус:** `active` — потік іде (push-замок увімкнено).
**Гілка:** `claude/startuem-ru50qv` · **Точка відкату:** тег `byyou-clamp-start`
**Ціль:** згортання довгих текстів постів «Стрічки» — «… Показати більше» / «Згорнути»,
рішення по ФАКТИЧНІЙ висоті рендера, як у Facebook/Instagram.

---

## Що знайдено в коді (прочитано, не з памʼяті)

- Текст поста малюється в ОДНОМУ місці: `postCardHtml()` → `src/tabs/feed.js:447`
  `<div class="fd-text">${escapeHtml(post.text)}</div>`.
- `postCardHtml` викликається з ТРЬОХ точок: головна стрічка `renderFeed()` (`:515`),
  екран сторінки — вкладка «Події» (`:1632`) і вкладка постів (`:1636`).
- CSS `.fd-text` — рівно один рядок (`style/feed.css:184`): `font-size:15px`,
  `line-height:1.5`, `white-space:pre-wrap`, `word-break:break-word`.
- Уже є ГОТОВИЙ ПАТЕРН «пройтись по DOM після рендера»: `wireGalleries(root)`
  (`:204`), яку кличуть рівно в тих самих трьох точках (`:516`, `:1725`, `:1734`).
  → мій `wireClamps(root)` стає поруч, окремої інфраструктури вигадувати не треба.
- Делегування кліків — `wireCards(root)` (`:2356`), кличеться і для вкладки, і для
  екрана сторінки (`.fd-screen` живе в `document.body`, тому має свій виклик).
- ⚠️ `renderFeed()` робить `listEl.innerHTML = …` — тобто ПЕРЕМАЛЬОВУЄ весь список
  (лайк, новий пост, видалення). Стан «розгорнуто» злетить, якщо тримати його в DOM.

---

## Рішення (чому саме так)

| Питання | Рішення | Чому |
|---|---|---|
| Чим обрізати | `max-height` + `overflow:hidden` | `-webkit-line-clamp` не анімується, а вимога — плавність 250-300мс |
| Скільки рядків | `--fd-text-lines: 7`, висота в `em` | в `em` переживає масштаб шрифту iOS; вимога 5 — не рахувати символи |
| Коли НЕ згортати | ховається < 2 рядків → кнопки нема | вимога 1 і 8: не мучити людину заради 1-2 рядків |
| Як анімувати | `max-height` у px зі `scrollHeight`, після `transitionend` зняти | з `auto` перехід не працює |
| Де тримати стан | `Set` id у модулі `feed.js` | `innerHTML` перемальовує картки — у DOM стан не виживе |
| Скрол при згортанні | тримати верх картки на місці | вимога 4: інтерфейс не має стрибати |

---

## Кроки

| # | Крок | Тип |
|---|---|---|
| 1 | Тег відкату `byyou-clamp-start` | 🟢 |
| 2 | CSS: `.fd-text--clamped` (max-height у em) + `.fd-more` (кнопка) + плавність | 🟢 |
| 3 | `postCardHtml`: обгортка `.fd-textwrap` навколо `.fd-text` (місце під кнопку) | 🟢 |
| 4 | `wireClamps(root)`: міряє `scrollHeight`, вішає клас і кнопку лише де треба | 🟢 |
| 5 | Виклики `wireClamps` у ті самі 3 точки, де є `wireGalleries` | 🟢 |
| 6 | Розгортання: max-height→scrollHeight, `transitionend`→зняти, текст «Згорнути» | 🟢 |
| 7 | Згортання назад + утримання позиції скролу (верх картки на місці) | 🟢 |
| 8 | Стан у `Set` — переживає `renderFeed()` і перемикання вкладок екрана | 🟢 |
| 9 | Делегат `[data-toggle-text]` у `wireCards` (не новий listener) | 🟢 |
| 10 | Крайні випадки: порожній текст · пост із фото · подія · `resize`/поворот | 🟢 |
| 11 | `node --check` + `node build.js` + браузерний смоук (Chromium, реальні тексти) | 🟢 |
| 12 | `/audit` + реліз-нотатки + `CACHE_NAME` | 🟢 |
| 13 | Брама деплою — чекаю слово «деплой» | 🟡 |

**Оцінка обсягу:** 13 кроків, 2 файли коду (`feed.js` +~55 рядків, `feed.css` +~20).
Кроки дрібні й однорідні → орієнтовно 35-45% вікна. Розбивати на два потоки не треба.

---

## Де зупинились

Брама старту — план показано, чекаю «ок» від Вови.
Не почато нічого, жодного файлу не змінено.

---

## 🔴 Питання на браму старту

**Конфлікт із паралельним потоком.** Уся робота — у `src/tabs/feed.js`, тобто в тому
самому файлі, де на іншому акаунті відкритий `/byyou` по модалках «написати допис».
Зони РІЗНІ (там композер ~2050-2200, тут картка ~416-462 + делегат ~2356), але файл один
→ при злитті двох гілок буде конфлікт, який доведеться розводити руками.

---

## 🔴 Що чекає ЖИВОГО тесту Вови (з попередніх потоків)
- **Лист коментарів під клавіатурою (PR #660):** плавний підйом · відступ згори ·
  плавне повернення вниз після згортання клавіатури · свайп шапки.
  ⚠️ Завеликий відступ → один рядок `--fd-kb-top-gap` у `style/feed.css`.
- Синхронізація між ДВОМА пристроями: коментар, видалення, новий пост → пігулка «↑ N нових».

---

## Архів завершених потоків
Сесія F (26.07): `BYYOU_ARCHIVE_2026-07-26b_net-islands.md` (PR #653) ·
`..._26c_comment-counts.md` (PR #654, #655) · `..._26d_feed-ux.md` (PR #656).
Сесія G (26.07): `..._26e_kb-sheet-motion.md` (PR #660, `CACHE_NAME cstl-20260726-1815`).
Сесія H (26.07): B-29 антиспам — не через `/byyou`, звичайним потоком (PR #666, run 3414).

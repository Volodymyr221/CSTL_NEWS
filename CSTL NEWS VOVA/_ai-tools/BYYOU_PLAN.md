# BYYOU_PLAN — стан потоку /byyou (CSTL)

**Статус:** `active`

> Проєкт одноосібний (власник Вова). Коли стартує новий напівавтономний потік через `/byyou`,
> цей файл заповнюється планом (10-15 кроків) і статусом `active`/`paused`. Архіви завершених
> потоків — файли `BYYOU_ARCHIVE_*.md` у цій же папці.

> Попередній завершений потік: **2026-07-25 — Коментарі, Потік 1/2 (клієнтський UX)** → PR #611/#612,
> далі поза потоком: #613, #614, #615, #616 (клавіатура). Архів — нижче в цьому файлі при завершенні.

---

**Гілка:** `claude/startuem-7564sq`
**Ціль:** Коментарі — відповіді і клавіатура: (1) при появі клавіатури НІЩО, крім смуги вводу, не рухається (зараз шапка і кружечки підскакують угору); (2) коментар, на який відповідаєш, візуально позначений; (3) цей коментар підтягується у видиму зону, якщо його закрила клавіатура.

> Орієнтир — логіка коментарів Instagram (Вова: «має бути така сама логіка як в інстаграмі»).

### Кроки
| # | Крок | Файли | Стан |
|---|------|-------|------|
| 1 | Аналіз коду коментарів, межі задачі | feed.js, feed.css, keyboard.js, base.css | ✅ |
| 2 | `data-com-id` на рядку + `data-reply-id` на кнопці «Відповісти» | feed.js | ✅ `36c8bae0` |
| 3 | `replyTarget` +`commentId`; підсвітка переживає перемалювання | feed.js | ✅ `36c8bae0` |
| 4 | CSS підсвітки: рисочка + заливка псевдоелементом, БЕЗ стрибка розкладки | style/feed.css | ✅ `36c8bae0` |
| 5 | Зняття підсвітки: ✕, надсилання, закриття, інший «Відповісти», видалення адресата | feed.js | ✅ `36c8bae0` |
| 6 | Підтягування у видиму зону: спільний `revealInScroller` + гачок `onOpen` | feed.js, keyboard.js | ✅ |
| 7 | Тести Playwright 13/13 + доказ, що падають без фіксу | скретчпад | ✅ |
| 8 | 🔴 Зсув усього застосунку: ЧЕКАЄ скрін `#kbdebug` від Вови (значення `offTop`) | — | 🔴 |
| 9 | Реалізація компенсації зсуву за підтвердженими числами | keyboard.js (+index.html?) | 🔴 |
| 10 | Тест-блок: node --check · браузер-смоук · аудит | — | 🟢 |
| 11 | Реліз-нотатки + брама деплою (блок 1: кроки 2-7) | BYYOU_PLAN.md | ✅ PR #617, v3458 |
| 12 | Фікси з живого тесту: рисочку обрізало · список «плавав» вбік · смуга «Відповідь для…» зливалась | style/feed.css | ✅ |
| 13 | Тести на ці 3 баги + доказ падіння (20/20) | скретчпад | ✅ |

---

## 📐 ЛОГІКА КОМЕНТАРІВ — повна специфікація (узгоджується з Вовою ПЕРЕД кодом)

> Вова: «треба продумати повністю детально логіку», орієнтир — Instagram.
> Це джерело правди для кроків 2-7. Кожен пункт має бути перевірюваним.

### А. Стани листа коментарів
| Стан | Що видно | Як увійти | Як вийти |
|---|---|---|---|
| **Спокій** | заголовок «N коментарів», список, смуга вводу внизу | відкрити лист | свайп вниз / тап по фону |
| **Ввід** | те саме + клавіатура; лист коротшає, верх на місці | тап у поле | згорнути клавіатуру |
| **Відповідь** | те саме + рядок-адресат підсвічений + смуга «Відповідь для Імʼя ✕» | тап «Відповісти» | ✕ · надсилання · закриття листа |

### Б. Що рухається при появі клавіатури (правило Вови, дослівно)
> «щоб нічого зверху не виїзджало і не зміщувалось, крім клавіатури і плавного підтягування нижнього блоку вводу і все прилегле до нього»

- ❌ НЕ рухаються: шапка застосунку, кружечки каналів, пости за листом, верх листа, заголовок «N коментарів».
- ✅ Рухаються: смуга вводу (їде вгору разом з клавіатурою) і смуга «Відповідь для…» (прилегла до неї).
- ✅ Список коментарів **стискається** знизу (не їде), його вміст лишається на тій самій прокрутці — крім випадку Г.

### В. Позначення коментаря-адресата
- Підсвічується **саме той рядок, на якому натиснули «Відповісти»** (не корінь гілки).
  ⚠️ Технічна пастка: `setReply` зберігає `parent_id || id` — корінь гілки, бо відповіді у нас 2 рівні.
  Для підсвітки потрібен ОКРЕМИЙ `commentId` самого рядка, інакше підсвітиться не той.
- Вигляд: вертикальна бордова рисочка ліворуч (3px, `--fd-accent`) + мʼяка заливка рядка.
- Розкладка не стрибає: рисочка малюється всередину відступу, висота рядка не змінюється.
- Підсвітка **переживає перемалювання списку** (realtime додав/видалив коментар → `renderCommentSheet` перебудовує `innerHTML`).

### Г. Підтягування адресата у видиму зону
- Умова: після появи клавіатури рядок-адресат повністю або частково закритий смугою вводу / поза списком.
- Дія: плавно прокрутити **список коментарів** (не сторінку) так, щоб рядок став видимим з полем 12px над смугою вводу.
- Якщо рядок і так видимий — **не рухати нічого** (порожня дія краще за зайвий рух).
- Прокрутка — `behavior: smooth`, всередині `.fd-com-list`.

### Д. Життєвий цикл відповіді
1. Тап «Відповісти» → підсвітка + смуга «Відповідь для Імʼя» + фокус у поле + (клавіатура) + підтягування (Г).
2. Тап «Відповісти» на іншому рядку → підсвітка переходить, стара знімається.
3. ✕ на смузі → підсвітка знята, режим відповіді скинуто, **клавіатура лишається** (людина може писати звичайний коментар).
4. Надсилання → відповідь додається у гілку, режим скидається, підсвітка знята, **клавіатура лишається відкритою** (як в Instagram), список показує нову відповідь.
5. Адресата видалили (свій/realtime) поки писали → режим тихо скидається (нема кому відповідати).
6. Закриття листа → усе скидається, фон відпускається на ту саму позицію.

### Е. Питання, які треба підтвердити (мої рекомендації позначено ✅)
1. Після надсилання клавіатура: ✅ лишається відкритою (IG) / згортається.
2. Після надсилання список: ✅ прокручується до нової відповіді / лишається де був.
3. Підсвітка: ✅ рисочка + мʼяка заливка / лише рисочка / лише заливка.
4. Підтягування: ✅ лише коли адресат закритий / завжди ставити його над смугою вводу.

---

**Де зупинились:** блок 1 (кроки 2-7) задеплоєно — PR #617, v3458. Живий тест Вови виявив 3 баги, усі виправлені (крок 12) і покриті тестами (крок 13, 20/20 + доказ падіння). CACHE `cstl-20260725-1739`. Чекаю «деплой» на блок 2.

### 🐞 Фікси після живого тесту (IMG_3604/3605/3606)
- **Рисочку підсвітки не було видно на кореневих коментарях, а на вкладених — було.** Один корінь із наступним багом: підкладка `::before` виступає на 8px, а список не мав бічного відступу → у кореневих рядків її обрізало краєм списку; у вкладених є `margin-left: 44px`, тому там вона вміщалась.
- **Коментарі «плавали» горизонтально.** Той самий виступ створював переповнення вбік: `overflow-y: auto` робить другу вісь не-visible, і список отримав горизонтальну прокрутку.
- **Фікс обох:** `.fd-com-list` → `padding: 12px 10px` (поле під підкладку) + явний `overflow-x: hidden`.
- **Смуга «Відповідь для Імʼя» зливалась зі списком** (прохання Вови «зроби гарно»): додано власне тло, лінію-волосинку згори і мʼяку тінь угору — читається як частина низу, а не як ще один коментар.
- Тести доводять симптоми: без фіксу падають з «список ПЕРЕПОВНЮЄТЬСЯ вбік (366 > 358)» і «рисочку кореневого коментаря ОБРІЗАЄ».
🔴 Кроки 8-9 (зсув усього застосунку під клавіатурою) СВІДОМО не почато — чекають скрін `#kbdebug` від Вови зі значенням `offTop`. Без реальних чисел не вгадую вчетверте.
Далі: реліз-нотатки → брама деплою цього блоку (кроки 2-7).

**Реліз-нотатки:** (заповнюються у Фазі 3)

# BYYOU_PLAN — стан потоку /byyou (CSTL)

**Статус:** `idle` — активного потоку /byyou немає.

> Проєкт одноосібний (власник Вова). Коли стартує новий напівавтономний потік через `/byyou`,
> цей файл заповнюється планом (10-15 кроків) і статусом `active`/`paused`. Архіви завершених
> потоків — файли `BYYOU_ARCHIVE_*.md` у цій же папці.

> **Останній завершений потік: 2026-07-25 — Коментарі, Потік 1/2 (клієнтський UX).**
> PR #611, CACHE `cstl-20260725-1402`, деплой run 3320. Зроблено:
> (1) клавіатура підіймає лише смугу вводу, верх коментарів стоїть (`visualViewport` → `--kb` →
> `padding-bottom` `.fd-com-sheet`); (2) картка профілю по тапу на аватар/імʼя коментаря
> (`data-av-uid`); (3) курсор-підказка. Коміти: `29eb84b3`, `48a366fc`, `641d6afd`.
> ⏸️ Живий тест Вови на iPhone (5 пунктів — див. SESSION_STATE / реліз-нотатки PR #611).

> **🔜 НАСТУПНИЙ ПОТІК — Коментарі, Потік 2/2 (🔴 бекенд, Вова застосовує SQL/Edge вручну):**
> push-сповіщення (коментар→адмінам сторінки, відповідь→автору коментаря) · колонка
> `reply_to_uid` + «@Імʼя» у вкладених відповідях · deep-link на конкретний коментар
> (відкрити пост → лист коментарів → підсвітити). Стартувати: `/byyou <ціль Потоку 2>`.

---

## Шаблон активного потоку (заповнюється при старті /byyou)

_Статус потоку — один із: active / paused / done (рядок нижче у форматі `**Статус:** <значення>`)._
**Гілка:** `claude/startuem-...` або `vova/<тема>`
**Ціль:** <що робимо>

### Кроки
| # | Крок | Файли | Стан |
|---|------|-------|------|
| 1 | … | … | 🟢/🟡/🔴 |

**Де зупинились:** <для продовження в новому чаті>

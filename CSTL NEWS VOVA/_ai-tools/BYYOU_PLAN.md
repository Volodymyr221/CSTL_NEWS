# BYYOU_PLAN — стан потоку /byyou (CSTL)

**Статус:** `active` — потік **07.08 «Живе оновлення публічних даних»**.

**Гілка:** `claude/startuem-xzx19p` · **Стартова точка:** `3f0e5391` (фікс B-30)

> 🔄 07.08, друга сесія дня: гілку перенесено з `claude/startuem-gsp3ct` — та вже
> влита в `main` (PR #833/#834), незлитого коду на ній немає.

---

## Ціль (замовлення Вови, дослівно)

*«Зайшов з другого акаунту і змінив імʼя та встановив фото на профіль, але мені не
оновило це відразу в додатку, приходиться закрити додаток повністю і зайти… ми можемо
це і все інше де відображається публічна інформація оновлювати відразу… хоча б коли
вони перейшли на іншу вкладку і назад… Це питання треба комплексно закрити раз і
назавжди»*.

**Формулювання задачі:** застосунок має **самооновлювати публічні дані** без
перезапуску. Мінімальний контракт — **повернення на вкладку/екран показує свіже**.

---

## 🔴 РОЗВІДКА 07.08 ПЕРЕВЕРНУЛА ПЛАН. Читай перш ніж щось робити

Чернетка кроків (нижче, в архівному блоці) виходила з того, що дані **недоступні** і
їх треба відкривати. Читання коду показало інше.

### 1. Публічне читання імен і фото ВЖЕ Є — і краще, ніж відкрита RLS

`scripts/supabase_avatars_public.sql` → `get_avatars(uids uuid[])`, `SECURITY DEFINER`,
grant для `anon` і `authenticated`. Віддає **рівно** `uid, name, avatar_url`.
Поруч — `get_public_profile(p_uid)` (6 несекретних полів для картки профілю).

➡️ **Питання №1 до Вови («робимо імена і фото публічно читабельними?») закривається
відповіддю «вже зроблено».** Вова сказав «Так» — і це підтверджує чинну конструкцію.
🛑 **RLS на `profiles` НЕ відкривати.** Вузький RPC уже дає рівно те, що треба, і при
цьому фізично не може віддати телефон, пошту чи дату народження. Відкриття таблиці
розширило б поверхню без жодної нової користі.

### 2. СПРАВЖНІЙ корінь — вічний кеш у памʼяті, а не доступ

`src/core/supabase.js:413-414`:
```
const _avatarCache = new Map();   // uid -> url
const _nameCache   = new Map();   // uid -> живе імʼя
```
`fetchAvatars()` бере **лише невідомі** uid: `.filter(u => u && !_avatarCache.has(u))`.
Тобто дізнались імʼя один раз — тримаємо його **до кінця життя вкладки**. Негативи
(«профілю нема» = `''`) кешуються так само назавжди.
Другий поверх того самого: `hydrateAvatars`/`hydrateNames` ставлять вузлу
`data-av-done` / `data-name-done` і більше до нього не повертаються.

➡️ Це **дослівно** пояснює слова Вови «приходиться закрити додаток повністю і зайти»:
повне закриття скидає памʼять — інших способів очистити ці дві `Map` у коді немає.

### 3. Міграція старих розмов НЕ потрібна

Заморожені `threads.buyer_name` / `author_name` уже перекриваються живим імʼям:
рядок списку розмов (`board-chat.js:777`) і шапка чату (`:141`) несуть `nameUid(...)`,
а `hydrateNames` підміняє текст. Плюс у базі є тригер `sync_profile_denorm`
(`scripts/supabase_identity_sync.sql`), який оновлює денормалізовані копії.
➡️ **Крок 8 чернетки знято.** Лишається лише звірити, що тригер справді накатано.

### ⚠️ Обмеження цієї сесії
MCP Supabase **не авторизований** — перевірити RPC і тригери прямо на проді з чату
неможливо. Це нічого не блокує (міграцій потік не потребує), але **крок 3 — звірка
руками Вови**, а не «вважаємо що там усе гаразд».

---

## Кроки

| # | Крок | Стан |
|---|------|------|
| 1 | Карта: які публічні дані показуються, звідки беруться, чим оновлюються (таблиця «жива/мертва») | 🟢 |
| 2 | Довести **вимірюванням**, що корінь — вічний кеш + одноразова гідрація (сцена «імʼя змінилось у базі → застосунок показує старе») | 🟢 |
| 3 | Звірка на проді: `get_avatars`, `get_public_profile`, тригер `sync_profile_denorm` — живі? | 🟡 |
| 4 | Кеш профілів: строк придатності (TTL) + явна інвалідація `invalidateProfiles()`; негативи протухають швидше | 🟢 |
| 5 | Гідрація за **поколінням** (`data-name-gen`), а не одноразовим `done` — інакше свіжий кеш нікуди не потрапить | 🟢 |
| 6 | Спільний примітив `src/core/refresh-on-return.js`: «повернувся на вкладку / у застосунок» + антифлуд | 🟢 |
| 7 | Підключити Дошку (спиратись на наявний `refreshBoardKeepingPlace`, не заводити другий механізм) | 🟢 |
| 8 | Підключити Стрічку (через наявні `patchPostCard` / `patchPageScreen`) | 🟢 |
| 9 | Підключити Повідомлення + відкритий чат (наявні `paintUnreadBadge` / `refreshUnreadBadge`) | 🟢 |
| 10 | Підключити Громаду (привітання на імʼя, віджети) | 🟢 |
| 11 | Сторож `tests/live-profile.mjs`: змінили імʼя і фото в базі → повернення на вкладку показує нове. **З контролем** (на коді до фіксу мусить упасти) | 🟢 |
| 12 | Смоук `/qa-explore` + реліз-нотатки → брама деплою | 🟡 |

🛑 **Цеглини, які НЕ дублювати** (є в проєкті, потік мусить на них спиратись):
`refreshBoardKeepingPlace()` · `core/list-patch.js` (`keepScroll`) · `patchPostCard` /
`patchPageScreen` · `hydrateAvatars` / `cachedAvatar` · `paintUnreadBadge` vs
`refreshUnreadBadge` · подія `cstl-tab-changed` (`src/app.js:74`).

---

## Де зупинились

Розвідку (Фаза 1) завершено, план переписано під знайдений корінь.
**Чекає «ок» Вови на брамі старту.**

⬜ **Крок 3 потребує Вови:** звірити в Supabase → SQL Editor, що `get_avatars`,
`get_public_profile` і тригер `sync_profile_denorm` справді на проді (з чату
не видно — MCP не авторизований).

---

## Архів: чернетка кроків від 07.08 (до розвідки)

Виходила з припущення «дані недоступні, треба відкривати RLS». Розвідка показала,
що доступ уже є, а ламається оновлення. Лишено, щоб не проходити те саме коло вдруге.

| # | Крок | Що з ним стало |
|---|------|----------------|
| 3 | 🔴 Рішення про доступ: публічне подання імен і фото (зміна RLS) | ❌ не потрібно — `get_avatars` уже це дає |
| 8 | Міграція старих розмов із «Житель» | ❌ не потрібно — `hydrateNames` + `sync_profile_denorm` |

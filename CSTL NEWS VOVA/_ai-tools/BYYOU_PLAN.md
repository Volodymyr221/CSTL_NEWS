# BYYOU_PLAN — стан потоку /byyou (CSTL)

**Статус:** `active` — потік заведено 26.07 (сесія F), чекає «ок» на брамі старту.
**Гілка:** `claude/startuem-7g0why`
**Ціль (Вова, 26.07):** «Продовжуєм byyou те яке робили в минулому чаті заново» —
тобто та сама ціль: **банальні технічні помилки не мають доходити до користувача, а обрив
зв'язку не має коштувати людині написаного тексту**. Минулий потік (26.07, PR #639, деплой
3372) поклав ФУНДАМЕНТ і накрив ним «Стрічку» + Дошку. Цей потік добиває **острівці**, куди
ядро не дійшло.

> Архів минулого потоку → `BYYOU_ARCHIVE_2026-07-26_network-errors.md`.

---

## Замір на вході (факти, не здогади)

| Що | Число | Звідки |
|---|---|---|
| `alert()` у `src/` | **0** ✅ | минулий потік прибрав усі 7 |
| Записів у базу всього (`insert/update/upsert/delete/rpc`) | **55** у `supabase.js` | grep |
| З них проведено через ядро `netCall` | **6** | grep `netCall(` |
| **Не накрито ядром** | **≈49** | різниця |
| `netErrorText` (людський словник) використовується | `supabase.js` ×4, `account-ui.js` ×3, `auth.js` ×2 | grep |
| Сира технічна помилка ще тече людині | `account-ui.js:254`, `account-ui.js:289` (`+ err.message`) | grep |
| `sendMessage` (приватний чат) повертає | `error: error.message` — **сирий текст** | `supabase.js:752-753` |

**Мутації поза `supabase.js` — перевірив, це хибні збіги** (`Set.delete`, `swReg.update`,
`dbg.update`), окрім `auth.js:122/130` — справжній `upsert` профілю без ядра.

---

## 🔴 ДВІ РЕЧІ, ЯКІ ТРЕБА ВИРІШИТИ ДО СТАРТУ

### 1. Повтор ВСТАВКИ може створити дубль — і в минулому потоці цей ризик уже внесено
`netCall` повторює запит при обриві. Для `update`/`delete`/`upsert` це безпечно (повтор дає
той самий результат). Для **вставки** — ні: якщо запит доїхав до бази, а відповідь загубилась
по дорозі назад, повтор створить **другий коментар / друге повідомлення / другий пост**.
Минулий потік обгорнув `createPagePost` — тобто ризик подвійного поста вже в проді (шанс
малий: треба саме втрату відповіді, а не втрату запиту, — але він не нульовий).

Три шляхи:
- **(а) Звірка перед повтором** — перед другою спробою читаємо базу: чи запис уже там?
  Повторюємо лише якщо нема. Без зміни схеми БД. Працює одразу для **чату** — там уже є
  `client_tag` (uuid від клієнта, `supabase_chat_hardening.sql`).
- **(б) Унікальний ключ у базі** (`client_tag` + `unique index` на коментарях/постах) — база
  сама відкидає дубль. Найнадійніше, але **це зміна схеми Supabase** → 🔴 стоп за правилами
  `/byyou`, і **MCP Supabase у цій сесії не авторизований** → SQL накатуєш ти руками.
- **(в) Вставки не повторювати взагалі** — лише людський текст + збережена чернетка.
  Нуль ризику дублів, але при кліпанні мережі людина тисне «Надіслати» вдруге сама.

**Моя рекомендація: (а) для чату зараз + (в) для решти вставок, (б) — окремим заходом
пізніше.** Причина: (а) вже має ключ і нічого не вимагає від тебе; (в) не гірше за
теперішній стан, бо чернетка й так не губиться; (б) чекає SQL і твоїх рук.

### 2. MCP Supabase у цій сесії не авторизований
Docs проєкту кажуть «MCP працює», але цього разу сервер просить авторизацію. Тобто **я не можу
цієї сесії ні накатувати міграції, ні читати базу**. Усе, що потребує SQL — на тебе, руками.

---

## Кроки

| # | Крок | Тип | Стан |
|---|------|-----|------|
| 1 | Карта всіх ≈49 записів: класифікувати на **ідемпотентні** (update/delete/upsert) · **вставки контенту** · **тихі фонові** (реакції/закладки/лічильники) | 🟢 | ⬜ |
| 2 | Рішення по вставках (блок 🔴 №1 вище) — зафіксувати вибраний шлях у цьому файлі | 🔴 | ⬜ |
| 3 | Ідемпотентні записи «Стрічки» через ядро: коментарі (правка/видалення), реакції постів і коментарів, підписка на сторінку, модератори | 🟢 | ⬜ |
| 4 | **Приватний чат** — головний острівець: `sendMessage`/`editMessage`/`deleteMessage`/`markThreadRead`/`setThreadState`/`getOrCreateThread` через ядро; прибрати `error.message` з повернень (людський текст) | 🟢 | ⬜ |
| 5 | Чат: звірка за `client_tag` перед повтором вставки (шлях «а») + перевірити, що оптимістична бульбашка не роздвоюється | 🟢 | ⬜ |
| 6 | **Групи** (10 записів): створення, інвайт, вступ, вихід, схвалення/відхилення, передача власника, повідомлення груп | 🟢 | ⬜ |
| 7 | **Пости Дошки**: `bumpPost`/`closePost`/`deleteMyPost`/`restorePost` + коментарі Дошки (`addComment`/`editComment`/`deleteComment`) | 🟢 | ⬜ |
| 8 | **Push-підписки** (`savePushSubscription`/`deletePushSubscription`/`saveUserPushDevice`) — обрив тут = людина думає, що підписалась на рейс, а насправді ні. Плюс дзвіночок рейсу в `buses.js` | 🟢 | ⬜ |
| 9 | **Профіль/вхід**: `auth.js` upsert профілю через ядро + прибрати два останні сирі `err.message` в `account-ui.js` (254, 289) | 🟢 | ⬜ |
| 10 | **Тихі фонові** (реакції, закладки, `logEvent`, `markThreadRead`): повтор БЕЗ тостів — не смітити людині через лайк | 🟢 | ⬜ |
| 11 | Стенд: обірвана мережа (`route.abort`) по кожній родині — A/B доказ «без фікса сира помилка / з фіксом людський текст + повтор» | 🟢 | ⬜ |
| 12 | `node --check` усіх змінених + `/audit` + браузерний смоук (`/qa-explore`) | 🟢 | ⬜ |
| 13 | Реліз-нотатки → Вова (що змінилось / що може зламатись / що перевірити на iPhone) | 🟡 | ⬜ |
| 14 | Деплой за словом «деплой» | 🔴 | ⬜ |

**Оцінка обсягу:** 14 кроків, більшість — механічна заміна виклику в одному файлі
(`supabase.js`), плюс один стенд. ≈50% вікна. В один потік влазить.

**Чого свідомо НЕ роблю в цьому потоці:** унікальний ключ у базі (шлях «б») — це зміна схеми
Supabase, потребує твоїх рук і окремого рішення; міграція Чату на `core/keyboard.js` (стоїть
у черзі після того, як клавіатура «Стрічки» тебе влаштує).

---

## Де зупинились

Фаза 1 завершена: замір зроблено, план складено, статус `active` (push-замок увімкнений).
**Чекаю «ок» на брамі старту** + рішення по блоку 🔴 №1 (як чинити зі вставками).

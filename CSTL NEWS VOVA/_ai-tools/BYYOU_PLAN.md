# BYYOU_PLAN — стан потоку /byyou (CSTL)

**Статус:** active
<!-- idle=вимкнено · active=потік іде (push-замок УВІМКНЕНО) · paused=пауза (продовжити /byyou) · done=завершено -->

**Ціль (БЛОК C):** вкладка «Шо там» — свята з AI-статтею + картинкою (контент Алли — пізніше).
**Власник:** Рома · **Гілка:** roma/shotam-tab · **Rollback-tag:** byyou-start-shotam
**Дорожня карта:** `docs/NEWS_REDESIGN_PLAN.md` · **Блоки A/B — ✅ на проді** (див. нижче історію).

## Кроки — БЛОК C (уточнено 06.07 з Ромою — див. «Рішення» нижче)
> **Назва слоту: «Шо в селі»** (тимчасова, ще працюємо над нею). Раніше — «Шо там».
> **Рішення навігації:** слот «Новини» в таб-барі ЛИШАЄТЬСЯ на місці → перейменовується на «Шо в селі» (іконка-газета лишається). Сегмент «📰 Новини» прибираємо (новини живуть блоком у Громаді). Сегмент «📅 Події» лишається і ТРАНСФОРМУЄТЬСЯ у стрічку «Шо в селі» (news-стиль картки + модалка). Окремої вкладки «Події» більше нема — вона і є «Шо в селі». Свята (holidays.json) лишаються у цій стрічці.

| # | Крок | Стан | Коміт |
|---|------|------|-------|
| 1 | `events.js` → картки в news-стилі + модалка повного прочитання (реюз `#article-modal` зі свайпом); прибрати expand-in-card + IntersectionObserver | 🟢 | ☐ |
| 2 | `index.html` — прибрати сегмент-перемикач + пейн новин з `page-news`; лишити стрічку (календар+фільтри+список); перейм. на `page-shotam` | 🟢 | ☐ |
| 3 | Таб-бар: «Новини»→«Шо в селі» (data-tab, назва; газета-іконка лишається); `switchTab` — прибрати редирект events→news+сегмент, додати `shotam`; legacy виклики `events`/`news` перенаправити | 🟢 | ☐ |
| 4 | CSS — перевдягання карток у news-стиль | 🟢 | ☐ |
| 5 | node --check + /qa-explore смоук + реліз-нотатки → брама деплою | 🟡 | ☐ |
| — | **Баг-сміття у статтях** (parse_rss `fetch_full_article` тягне навігацію/футер/«Вибір редактора») — окремий блок після Шо-в-селі, у цьому ж потоці | 🟢 | ☐ |

## Історія блоків A/B (✅ на проді)
- Блок A — AI-агент новин (черга+крапельна публікація, анти-SSRF, og:image, max_tokens 8192-фікс).
- Блок B — новини у вкладці Громада (3 фільтри, вкладена прокрутка, червоний фон).
- Бонуси: фікс замерзлого ліміту, фікс заторів деплою (cancel-in-progress:true).

## Де зупинились (ПАУЗА на 77% контексту — продовжити /byyou у новому чаті)
Гілка `roma/shotam-tab` (від main 08b6547), rollback-тег `byyou-start-shotam`. Кодом ще нічого не зроблено.

**🔑 ГОЛОВНА ЗНАХІДКА (перечитати перед продовженням):** Крок 1 і 3 фактично ВЖЕ Є:
- `data/holidays.json` — 25 свят на 2026 з описами-статтями, датами (рухомі теж: Великдень 12.04, Трійця 31.05) і **cover-картками** (`cover_emoji` + `cover_gradient`) замість фото. → CF-ключі для свят НЕ потрібні.
- Свята ВЖЕ показуються у вкладці **Події** (`src/tabs/events.js` ~р.386-400: зливає events.json + holidays.json у allEvents).

**Отже Блок C стиснувся до:** (2) вкладка «Шо там» (стрічка свят із cover + пости Алли пізніше), (5) навігація (прибрати «Новини» з таб-бару, додати «Шо там» — Вова погодив). AI-збагачення тексту — опційно (описи вже пристойні).

**✅ РІШЕННЯ РОМИ (отримано):**
- **Свята → у «Шо там»** (варіант A): перенести з «Події» у «Шо там». «Події» лишаються для афіші заходів.
- **Вигляд «Шо там» = як стрічка новин** (картки + cover_emoji/gradient) + **модалка для повного прочитання** (description).

**Наступний крок після відповіді:** побудувати вкладку «Шо там» (новий `src/tabs/shotam.js` + секція в index.html + таб-бар + `switchTab`), рендер свят із cover_emoji/gradient, модалка з description. Потім навігація (🔴 таб-бар — узгоджено з Вовою).

**Статус побічних задач:** агент новин — фікс max_tokens 8192 задеплоєно (08b6547), оживе на авто-запуску ~12:15 UTC; перевірити лог наступного запуску ai-news-agent.yml.

## Реліз-нотатки
- ЩО ЗМІНИЛОСЬ:
- ЩО МОЖЕ ЗЛАМАТИСЬ:
- ЩО ПЕРЕВІРИТИ НА IPHONE:

# BYYOU_PLAN — стан потоку /byyou (CSTL)

**Статус:** `active`
**Гілка:** `claude/startuem-sq0h2w`
**Ціль:** сторінки «Стрічки» стають керованими: власник може призначати модераторів, обирати від чийого імені публікує, і нормально кадрувати банер/аватар при завантаженні.

> Попередній завершений потік: 2026-07-24 дзвіночок сповіщень → `BYYOU_ARCHIVE_2026-07-24_bell-push.md`.

## Вхідні дані (з реального коду і бази)
- `page_admins(page_id, uid, role)` уже існує; `can_edit_page()` **ролі не дивиться** — доступ дає сам факт запису.
- Писати в `page_admins` може ЛИШЕ глобальний адмін (`padmins admin all` = `is_admin()`) → власник сторінки не може додати помічника.
- `pposts insert` вимагає `can_edit_page(page_id) AND author_uid = auth.uid()`, а `pages update` = `is_admin() OR can_edit_page(id)` → **розсинхрон**: глобальний адмін редагує сторінку, але опублікувати на ній не може.
- `fetchMyEditablePageIds()` читає `page_admins` без фільтра і покладається на RLS; для глобального адміна політика `padmins read own` віддає ВСІ рядки → композер показується на всіх сторінках, хоч публікація впаде.
- Банер: `.fd-screen-top` 168px висоти на всю ширину ≈ **2,3:1**. Зараз фото просто заливається `object-fit: cover` → квадратне фото ріжеться по боках.
- Завантаження банера/аватара — `uploadImageReliable` (`core/upload.js`), стиснення на canvas уже є.

## Кроки
| # | Крок | Файли | Стан |
|---|------|-------|------|
| 0a | 🔴 **БАГ НАВІГАЦІЇ (скрін IMG_3557):** свайп від лівого краю закриває сторінку спільноти І одночасно відкочує додаток назад по історії браузера (вискакує Дошка). Корінь: `attachScreenSwipeBack` ловить жест у зоні `clientX ≤ 24` — рівно там, де iOS забирає власний системний жест «назад»; `preventDefault()` системний жест не скасовує. Рішення: повноекранні шари підключити до історії (відкриття → `pushState`, `popstate` → закрити верхній шар); наш свайп замість прямого `close()` кличе `history.back()` | `feed.js` | 🔴 |
| 0b | Той самий патерн на решту повноекранних шарів: приватний чат, «Мої оголошення», «Збережені», Обговорення | `board-chat.js`,`chat-core.js` | 🟢 |
| 1 | 🔑 Полагодити розсинхрон прав: `can_edit_page()` враховує `is_admin()` → глобальний адмін реально може публікувати там, де бачить композер | SQL (функція) | 🔴 |
| 2 | RPC керування модераторами: `add_page_moderator(page_id,email)` · `remove_page_moderator` · `list_page_moderators` (SECURITY DEFINER, лише власник сторінки/глобальний адмін; чужі дані не віддає) | SQL | 🔴 |
| 3 | Колонка `page_posts.show_author boolean default true` (від чийого імені) | SQL | 🔴 |
| 4 | Дата-шар: 3 функції модераторів + передача `show_author` | `supabase.js` | 🟢 |
| 5 | Екран «Модератори» у меню «⋯»: список, додати за поштою, видалити, ролі власник/модератор | `feed.js`,`feed.css` | 🟢 |
| 6 | Композер: вибір «від спільноти / від себе» + збереження вибору | `feed.js` | 🟢 |
| 7 | Картка поста: підпис автора лише коли обрано «від себе» | `feed.js` | 🟢 |
| 8 | Новий `core/cropper.js`: рамка кадрування (зсув пальцем, масштаб щипком/повзунком, обрізка на canvas) | `cropper.js` (новий) | 🟢 |
| 9 | Стилі кроппера | `style/crop.css` (новий) + `style.css` | 🟢 |
| 10 | Підключити кроппер до банера сторінки (рамка 2,3:1) | `feed.js` | 🟢 |
| 11 | Підключити кроппер до аватара сторінки (кругла рамка 1:1) | `feed.js` | 🟢 |
| 12 | Тест-блок: `node --check` · `node build.js` · браузерний смоук кроппера і композера | — | 🟢 |
| 13 | Реліз-нотатки + живий тест на iPhone | — | 🟡 |

## Рішення, зафіксовані до старту
- Модератора додають **за поштою** (рішення Вови). RPC відповідає лише «додано» / «такого користувача нема» — жодних чужих даних не віддає.
- Ролі: **власник** (усе + керує людьми) · **модератор** (пише й редагує пости, людей не додає).
- У композері за замовчуванням — **від імені спільноти** (як зараз). Старі пости не змінюються.
- Кадрування — справжня обрізка (фото ріжеться до пропорції банера перед завантаженням), а не збереження позиції: менший файл і передбачуваний результат.

## Де зупинились
Фаза 1 завершена. Чекаю «ок» на Брамі старту.

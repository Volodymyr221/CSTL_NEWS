# BYYOU_PLAN — живий потік /byyou

**Статус:** `planning` — чекає «ок» Вови на брамі старту.

**Потік:** 11.08 — «Обговорення → ПИТАННЯ ГРОМАДИ: Q&A замість чату»
**Гілка:** `claude/startuem-9cf4gf` _(сесія U-21; попередній потік U-20 про бургер — `done`, задеплоєно PR #887/#888)_
**Замовлення Вови:** стратегічне переосмислення вкладки «Обговорення» (текст-бриф від ChatGPT,
повністю в історії чату). Ключова теза замовника: *«Мені не подобається не стільки зовнішній
вигляд, скільки сама семантика»*.

---

# ЧАСТИНА 1 — АУДИТ (заміряно, не припущено)

## 1.1 🔴 ЖИВІ ДАНІ — головна знахідка, вона перевертає постановку задачі

Заміряно **прямим запитом до продової бази** (Supabase MCP, `posts` + `comments`), а не
з памʼяті й не з документації.

| тема (`posts.type='chat'`) | повідомлень | різних людей | остання активність |
|---|---|---|---|
| «Коли плануються наступні вибори?» | **44** | **2** | 07.07 |
| «Коли концерт?» | 3 | 1 | 09.08 |
| «Чи буде концерт на день міста?» | 2 | 1 | 20.06 |
| «Бачиш його?» | **0** | 0 | — |
| «?» | **0** | 0 | — |

**Разом: 5 тем · 49 повідомлень · 11 профілів у застосунку.**

### 🔑 Три висновки, які змінюють рішення

**(1) «Живої активності», яку можна зламати, НЕ ІСНУЄ.** Тема з 44 повідомленнями —
це не громада, це **дві людини**, і зміст їх повідомлень: `Прил` · `Пр` · `Алат` ·
`Дад` · `Сда` · `Бабдссжажаажажа` · `В` · `В` · `С` · `Аааааа` · `Ало` · `Хурма`.
Це прогін інтерфейсу, а не розмова. **Отже головний контраргумент проти редизайну
(«не вбий те, чим уже користуються») знято даними — користуватись нічим.**

**(2) Люди інстинктивно пишуть ПИТАННЯ, а не «теми».** 3 з 5 тем — питання за
формою і зі знаком питання в кінці: «Коли плануються наступні вибори?», «Чи буде
концерт на день міста?», «Коли концерт?». Тобто гіпотеза замовника підтверджується
**поведінкою**, а не смаком. Дошка збирає оголошення, Обговорення — питання; продукт
уже сам сказав, чим він є.

**(3) Механізм відповідей майже не використано:** `reply_to_id` заповнений у **1**
коментарі з 54. Тобто «гілки в чаті» — це вкладена робота, яку ніхто не спожив.
У Q&A той самий стовпчик отримує чесну роботу: відповідь на відповідь.

⚠️ **Наслідок для приймання:** якщо базу не почистити, новий екран «Питання» на проді
першими двома картками покаже **«?»** і **«Бачиш його?»** — і виглядатиме зламаним
незалежно від якості коду. Це окреме 🔴-питання до Вови (нижче, крок 0).

## 1.2 Що реалізовано зараз (по коду, не з памʼяті)

| шар | де | суть |
|---|---|---|
| сторінка вкладки | `index.html:153` `#page-discussions` → `#disc-content` | справжня сторінка, не overlay (таб-бар видимий) |
| рендер списку | `src/tabs/board.js` (`renderBoard` при `activeType='chat'`) | той самий рушій, що Дошка; шапка = **лише пошук** (заголовок прибрано 21.07 як дубль таб-бару) |
| картка теми | `board-discussions.js:790` `renderChatCard` | тема · `💬 N повідомлень` · `👥 N учасників` · **2 останні повідомлення з іменами й часом** · ❤️ лайк · автор · закладка |
| відкриття | `board-discussions.js:433` `openChatModal` | **повноекранна модалка** з власним свайпом-вниз (~50 рядків) і власним хаком клавіатури (~30 рядків) |
| стрічка | `chatMessagesHtml` | **бульбашки** `.pm-bubble`, мої справа / чужі зліва, час `HH:MM`, роздільники днів «Сьогодні/Вчора», роздільник «Нові повідомлення», пігулка «↓ N нових повідомлень» |
| ввід | форма в модалці | плейсхолдер **«Написати повідомлення…»**, кнопка **«↑»** |
| створення | `openDiscussionCompose` | аркуш «Створити обговорення» → поле «Тема обговорення» → кнопка «Створити» |
| дані | `posts.type='chat'` + `comments` | коментарі: `reply_to_id`, `edited_at`, `deleted_at`, `client_tag`, `sender_uid` |
| realtime | `attachDiscussionsRealtime` | `subscribeComments` + `subscribeReactions`, refetch-і-перемалюй |
| антиспам | `core/utils.js` + тригер `comments_antispam` | клієнт і база дзеркалять один одного, `RATE_SCOPE='disc'` |

## 1.3 У чому конкретна UX-проблема (називаю механізм, не відчуття)

**Не в кольорах.** У застосунку **чотири незалежні сигнали**, кожен з яких сам по собі
каже «це месенджер», і разом вони перекривають будь-який напис:

1. **Бульбашки з вирівнюванням «мої справа / чужі зліва»** — це найсильніший
   візуальний маркер месенджера в світі. Жоден Q&A ним не користується.
2. **Час `HH:MM` + роздільники «Сьогодні/Вчора»** — годинникова вісь. Q&A живе на
   вісі «давно / нещодавно», а не «о котрій».
3. **Прев'ю двох останніх повідомлень на картці списку** — це рядок списку чатів
   (Viber/Telegram), а не анонс питання.
4. **«Написати повідомлення…» + «↑»** — дослівна мова месенджера.

**Плюс семантика входу.** Слово «Обговорення» називає ФОРМАТ («тут відбувається
обговорення»), а не ПОТРЕБУ («я не знаю — спитаю»). Форма без потреби не запускається:
щоб створити «обговорення», людині треба спершу вигадати, що вона хоче обговорити.
Щоб поставити питання — не треба нічого вигадувати, питання вже є в голові.
**Це і є корінь: вкладка вимагає від людини креативності замість того, щоб
обслуговувати наявну потребу.**

## 1.4 Що дозволяє і чого не дозволяє база (заміряно `pg_policies`)

✅ **Уся Q&A-модель реалізовна БЕЗ жодної зміни схеми і RLS.** Питання = `posts.text`,
відповіді = `comments`, вкладені відповіді = наявний `reply_to_id`, автор редагує/видаляє
свою відповідь (політики `Author updates own comment` / `Author deletes own comment` є).

🔴 **Два обмеження, які треба знати ЗАРАЗ, а не спіткнутись потім:**
- `posts UPDATE` — **тільки `is_admin()`**. Автор не може ні редагувати, ні позначити
  своє питання. Отже **«вирішено» і «корисна відповідь» неможливі без міграції**
  (нова колонка + нова політика) → виносяться в окремий блок (див. 3.2).
- `bumped_at` через це теж ніхто не оновлює → **сортування «за останньою активністю»
  роблю клієнтськи** (max `created_at` відповідей). Безкоштовно: коментарі й так усі
  в памʼяті (`fetchAllComments`).

---

# ЧАСТИНА 2 — МОДЕЛЬ, ЯКУ ПРОПОНУЮ

## 2.1 Де я НЕ згоден із брифом (і чому)

**Бриф пропонує назву вкладки «ПИТАННЯ» або «ЗАПИТАТИ» і просить не вибирати механічно.**

🛑 **«ЗАПИТАТИ» у таб-барі — відхиляю.** Дві причини:
1. Решта чотирьох пунктів — іменники-місця (Стрічка · Громада · Дошка · Автобуси).
   Дієслово серед них читається як кнопка дії, а не як розділ; людина 60+ очікує,
   що тап по ньому одразу щось надішле.
2. Ярлик бреше про вміст для найчастішого сценарію: **більшість заходить ЧИТАТИ
   чужі відповіді, а не питати.** «Запитати» обіцяє форму, а показує список.

✅ **«ПИТАННЯ» — беру,** але сама по собі назва проблему НЕ вирішує (вона теж лише
називає). **Дію несе екран, а не ярлик:** велика кнопка «Запитати громаду» першим
елементом сторінки, а не схована у FAB. Розподіл чесний: таб-бар відповідає «де я»,
екран — «що я тут роблю».

🛑 **Ще одне заперечення до брифу: одного перейменування замало, і навпаки —**
**перейменування без зняття чотирьох маркерів чату (1.3) не дасть НІЧОГО.** Якщо
лишити бульбашки й «Написати повідомлення…», людина побачить «Питання», відкриє —
і потрапить у месенджер. Тому назва стоїть у плані **останнім** кроком змісту, а не
першим: спершу міняється те, що людина бачить усередині.

## 2.2 Фінальна структура (пункти A-N з брифу)

**A. Назва вкладки:** `ПИТАННЯ` · іконка — бульбашка **зі знаком питання** (зараз
бульбашка з рядками тексту = чат).

**B. Purpose одним рядком (він же слоган екрана):** «Не знаєте — запитайте громаду».

**C. Список:** назва «ПИТАННЯ ГРОМАДИ» + слоган → **кнопка «Запитати громаду»** →
пошук → картки, відсортовані **за останньою активністю**.

**D. Картка** (те, що потрібно для рішення «відкривати чи ні» — і нічого більше):
```
┌────────────────────────────────────┐
│ Коли буде концерт на День міста?   │  ← ПИТАННЯ домінує (17px/700, до 3 рядків)
│                                    │
│ ⬤ Олена · 2 год тому          🔖  │
│ ────────────────────────────────   │
│ 💬 8 відповідей · остання 10 хв    │
└────────────────────────────────────┘
```
🗑 **Прибирається:** прев'ю двох повідомлень (маркер чату) · «👥 учасників» (метрика
чату) · ❤️ з картки.
🆕 **Стан «без відповіді»** — окремий рядок замість лічильника: **«Ще ніхто не
відповів»**. Це не порожнеча, а **заклик**: єдиний спосіб оживити Q&A — показати тому,
хто знає, де він потрібен.

**E. Відкрите питання — ПОВНОЕКРАННИЙ ШАР** (бриф пропонував, я підтверджую, але
**через наявний `core/layers.js`**, а не власний):
```
←  Питання                                🔖
──────────────────────────────────────────
КОЛИ БУДЕ КОНЦЕРТ НА ДЕНЬ МІСТА?
⬤ Олена · 9 серпня
[ 🙋 Мене теж цікавить · 3 ]
──────────────────────────────────────────
8 ВІДПОВІДЕЙ
┌────────────────────────────────────────┐
│ ⬤ Віктор                  9 серпня     │
│ Начебто 24 серпня.                     │
│                          ↩ Відповісти  │
└────────────────────────────────────────┘
   └ ⬤ Марія · Так, підтверджую.
──────────────────────────────────────────
[ Ваша відповідь…              ] [Надіслати]
```
🔑 **Чому шар, а не модалка:** `core/layers.js` віддає закриття **системному жесту
iPhone**. У `CLAUDE.md` і `HOT_RULES` це записано прямою забороною писати власний
свайп для повноекранного шару (02.08 це вже коштувало окремого блока роботи). Заразом
з `board-discussions.js` зникає ~50 рядків власного свайпу.

**F. Створення** — один крок, без категорій/локації/фото (кожне поле = бар'єр для 60+):
«ЩО ВИ ХОЧЕТЕ ДІЗНАТИСЬ?» → поле з живим прикладом → кнопка **«ЗАПИТАТИ ГРОМАДУ»**.

**G. Відповіді:** картка на всю ширину (аватар · імʼя · дата · текст), **без бульбашок,
без «мої справа», без `HH:MM`, без роздільників днів**. Вкладена відповідь — з відступом
під батьківською (наявний `reply_to_id`). Редагування/видалення свого — меню «⋯».

**H. Статуси:** «без відповіді» / «є відповіді» — **з даних, безкоштовно** ✅.
«Вирішено» / «корисна відповідь» — 🔴 **потребує міграції, окремий блок** (3.2).

**I. Лайки / закладки:** ❤️ → **«🙋 Мене теж цікавить»**, переїжджає з картки
всередину питання. 🔑 **У базі значення лишається `❤️`** — міняється лише підпис та
іконка, тож **9 наявних реакцій не пропадають**. Закладки лишаються як є (`saved_posts`,
5 записів, хаб «Збережені» вже їх читає).

**J. Пошук** — лишається (наявний рядок), плейсхолдер → «Пошук серед питань…».

**K. Фільтри** — **один** чіп «Без відповіді» + «Усі». Малюється, **лише якщо є хоч
одне без відповіді** (той самий патерн, що `bd-types` на Дошці: мертва кнопка вбиває
екран). «Мої» лишається у FAB — другого шляху не заводжу.

**L. Realtime** — **не чіпаю**: ті самі `subscribeComments`/`subscribeReactions` і той
самий refetch-і-перемалюй. Змінюється лише функція, що малює.

**M. Мобільний UX 60+:** мінімальний текст 15px (зараз прев'ю чату 12-13px) · тап-цілі
44px · головна дія — кнопка на екрані, не жест · жодного прихованого свайпу як
єдиного шляху.

**N. Майбутні спільноти:** розділення закладається **вже зараз, самою назвою**:
«Питання» = публічний Q&A громади · «Спільноти» (майбутнє) = чат села. Оскільки чат-мова
з Q&A знімається повністю, майбутній чат спільноти не конкуруватиме з цією вкладкою
і не дублюватиме її.

---

# ЧАСТИНА 3 — ПЛАН

## 3.1 Кроки цього блоку

| # | крок | стан | файли |
|---|---|---|---|
| 0 | 🔴 **Питання Вові до старту** (тестові дані · «вирішено» · назва) | 🔴 СТОП | — |
| 1 | Каркас Q&A у двигуні: `renderQuestionCard` (картка) + сортування за активністю | 🟢 | `board-discussions.js` |
| 2 | Екран питання на `core/layers.js`: шапка, тіло, ввід; зняти власний свайп | 🟢 | `board-discussions.js` |
| 3 | Відповіді без бульбашок: `answersHtml` (картки, вкладеність, дати) | 🟢 | `board-discussions.js` |
| 4 | Ввід і дії: «Ваша відповідь…», «Надіслати», меню «⋯», «Мене теж цікавить» | 🟢 | `board-discussions.js` |
| 5 | Створення: «Що ви хочете дізнатись?» → «Запитати громаду» | 🟢 | `board-discussions.js` |
| 6 | Шапка екрана: назва + слоган + кнопка «Запитати громаду» + чіп «Без відповіді» | 🟢 | `board.js` |
| 7 | Порожній стан вкладки (перше знайомство) + плейсхолдер пошуку | 🟢 | `board.js` |
| 8 | Стилі `.qa-*` (картка, шар, відповіді) у мові CSTL LIFE | 🟢 | `style/board.css` |
| 9 | Перейменування наскрізь: таб-бар + іконка · бургер · «Збережені» · капсула Громади | 🟢 | `index.html`, `sidebar.js`, `saved-hub.js`, `home-caps.js` |
| 10 | Оновити 4 наявні сторожі, що тримались за слово «Обговорення» | 🟢 | `tests/sidebar-menu.mjs`, `tabbar-press.mjs`, `tab-dots.mjs`, `board-cream.mjs` |
| 11 | 🆕 сторож `tests/questions.mjs` + **контрольний прогін на `origin/main`** (мусить упасти) | 🟢 | `tests/questions.mjs` |
| 12 | Тест-блок: `npm test` · `/qa-explore` · `/audit` · реліз-нотатки | 🟢 | — |
| 13 | 🟡 Смоук на iPhone: питання → відкрити → відповісти → назад → створити | 🟡 ТВОЯ ЧЕРГА | — |

**Оцінка обсягу:** 13 кроків ≈ **55-65% вікна**. Близько до стелі, тому «вирішено»
і чистка даних свідомо винесені в блок 2 — інакше потік не доїде до деплою.

## 3.2 Що НЕ входить у цей блок (і чому саме так)

| відкладено | причина |
|---|---|
| статус «Вирішено» + «корисна відповідь» | 🔴 міграція: колонка `posts.solved_comment_id` + UPDATE-політика для автора. Змішувати зміну схеми з редизайном UI = два ризики в одному деплої |
| чистка 2 сміттєвих тем і 49 тестових повідомлень | 🔴 видалення даних незворотне — тільки з прямого дозволу Вови |
| локальні спільноти сіл (чат Митильного) | окремий продукт, у брифі теж позначений як «стратегічний напрям, не зараз» |
| категорії питань | бар'єр при створенні; при 5 питаннях фільтрувати нічого |

## 3.3 Чого НЕ ламаю (перевірено перед стартом)

realtime-підписки · оптимістична вставка з відкатом · антиспам/антифлуд/фільтр
матюків (клієнт ↔ тригер бази — дзеркало, `RATE_SCOPE='disc'` не чіпаю) · RLS ·
авторизація і гейт «увійдіть, щоб відповісти» · закладки `saved_posts` і хаб
«Збережені» · наявні 9 реакцій (значення `❤️` у базі лишається) · нижня навігація ·
рушій Дошки (все нове — scoped під `#disc-content`, як `#board-content` у Дошки) ·
`core/keyboard.js` (зона підвищеної обережності — не торкаюсь).

---

## Де зупинились

Аудит завершено, план складено, **чекаю відповіді на 3 питання кроку 0 і слова «ок»**.
Код НЕ змінювався. Жодного коміту в код ще немає.

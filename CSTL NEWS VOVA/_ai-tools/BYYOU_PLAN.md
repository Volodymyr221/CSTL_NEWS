# BYYOU_PLAN — живий потік /byyou

**Статус:** `planned` — аудит зроблено, план складено, **чекаю «Роби» від Вови**.

> 🔑 **Замок пушу** (`.claude/hooks/byyou-push-lock.js`) тримає лише статус `active`.
> Тут `planned`: правок коду ще немає, пушити нічого.

**Гілка:** `claude/startuem-tt38jj` · **Дата:** 15-16.08.2026

---

## Замовлення Вови (дослівно, скорочено до суті)

«Повний автономний аудит і покращення CSTL LIFE… знайди всі проблеми… виправ те,
що можна безпечно виправити… quality bar — professional social product, а не
хороший MVP… Я лягаю спати, ти аналізуй; в кінці напиши висновок без води, потім
я напишу "роби" як перечитаю і тоді зробиш все і задеплоїш.»

➡️ **Тому ця сесія — рівно аудит.** Жодного рядка коду не змінено.

## Що зроблено

Повний аудит: **`_ai-tools/AUDIT_FULL_2026-08-15.md`** — 7 знахідок P1, 8 P2,
окремий розділ про продуктивність, розділ «що виявилось справним» і розділ
«чого перевірити не можу».

Базова лінія стендів знята: **79 із 80 зелені** (єдиний червоний — `docs-fresh`,
бо пульс `NOW.md` відстав від мого ж коміту журналу; кодом не спричинено).

---

## ПЛАН ВИПРАВЛЕНЬ — ДВА ПОТОКИ

Разом 16 кроків ≈ 560K токенів = **більше за вікно**, тому розбито надвоє.
Порядок усередині кожного — P1 → P2.

### Потік А — СПОВІЩЕННЯ Й АВТОБУСИ (ядро замовлення, 9 кроків)

| # | крок | стан |
|---|---|---|
| A1 | `savePushSubscription` → `upsert(onConflict endpoint,route_id,track_date)` + скидання `notified_*` | 🟢 |
| A2 | Скасування відстеження — по `user_uuid + route + date`, не по `endpoint` | 🟢 |
| A3 | Кнопка «Відстежувати»: блокування на час запиту + серіалізація на рейс | 🟢 |
| A4 | `pushsubscriptionchange` у `sw.js` — перенос підписок на новий endpoint | 🟢 |
| A5 | Ступінь «за 5 хв» + закриття дірки 2-12 хв для пізньої підписки | 🟢 |
| A6 | Тег автобусного push: `+ track_date + сегмент` | 🟢 |
| A7 | `SCHEDULE_URL` → `castlelife.org` + гучний лог, коли розклад не завантажився | 🟢 |
| A8 | Deep-link на видалений обʼєкт → тост замість тиші (Дошка · Стрічка · стаття) | 🟢 |
| A9 | Стенди на A1-A5 (сторожі поведінки, з контролем на коді «до») | 🟢 |

### Потік Б — ШВИДКІСТЬ І РЕНДЕРИНГ (5 кроків)

| # | крок | стан |
|---|---|---|
| Б1 | `minify: true` у `build.js` (957 КБ → ~520 КБ) | 🟢 |
| Б2 | `bus-hero2.png` 1.6 МБ → WebP ~70 КБ (той самий вигляд, замір до/після) | 🟢 |
| Б3 | `sw.js install` → `allSettled` + назва файлу в лог | 🟢 |
| Б4 | Витік слухача `resize` в `openPageScreen()` | 🟢 |
| Б5 | Серіалізація ♥ (подвійний тап не розходиться з базою) | 🟢 |

### 🔴 ТРИ РІШЕННЯ ЗА ВОВОЮ — БЕЗ ЙОГО СЛОВА НЕ РОБЛЮ

| # | що | чому питаю |
|---|---|---|
| В1 | **Push про відповідь на ПИТАННЯ** | нова Edge Function + тригер у БД = **зміна схеми Supabase**. За правилом `/byyou` це 🔴-стоп. Це найбільша функціональна діра, але й найбільша зміна |
| В2 | **Заставка 4.1с → «готово або 1.2с»** | на цю затримку тепер СПИРАЮТЬСЯ deep-linkʼи (`core/splash.js`), полагоджені 15.08. Міняти можна лише разом із ними — інакше поверну вчорашній баг |
| В3 | **`user-scalable=no` у viewport** | прибрати = дати збільшувати текст (WCAG), але подвійний тап почне зумити інтерфейс. Це продуктовий вибір, не технічний |

---

## Де зупинились

Аудит завершено, план складено, код не змінювався. Наступний крок — слово Вови:
«Роби потік А» / «Роби обидва» / «Роби і В1» тощо.

# BYYOU_PLAN — стан потоку /byyou (CSTL)

**Статус:** done
<!-- idle=вимкнено · active=потік іде (push-замок УВІМКНЕНО) · paused=пауза · done=завершено -->
<!-- ✅ 07.07 ЗАДЕПЛОЄНО: потік audit-fixes (5 🟡-знахідок аудиту): фільтр ebook/ebay, гість-чат після входу, doSave чесний, CabinetSink source_url, SQL-drift. -->

**Ціль:** 🟡-знахідки повного аудиту 07.07 — 5 точкових фіксів: профаніті-хиба 'еб' (ebook/ebay), dead-end гостя в чаті обговорення, doSave не перевіряє публікацію, CabinetSink губить source_url, SQL-drift схеми кабінету.
**Власник:** Рома · **Гілка:** roma/audit-fixes · **Rollback-tag:** byyou-start-audit

## Знахідки коду (реальні, перечитано 07.07)
- `src/core/utils.js:183` — стем `'еб'` + гомогліфи (e→е, b→б) блокує ebook/ebay/ebola/ebony. `'єб'/'їб'` безпечні (є/ї не з латині).
- `src/tabs/board.js:582` — гість у модалці чату бачить кнопку `#bd-chat-login` «Увійдіть, щоб писати» — перевірити обробник + що після входу модалка оживає (форма зʼявляється).
- `admin.html:1038` — `if (publish && !scheduling) await publishArticle(savedId);` — результат НЕ перевіряється (RLS-відмова = мовчазне «опубліковано» яке бреше). Рядок 911 перевіряє — зробити однаково.
- `editor/sinks/cabinet.py:32` — пише `source:"CSTL LIFE", exclusive:True`, а `draft.source_urls` ДРОПАЄ. Колонка `source_url` в схемі Є (SQL:93). sync_cms уже читає її (рядок 141).
- `scripts/supabase_editor_cabinet.sql:83,98` — CHECK без 'scheduled' (status) і 'holiday' (type); Вова ВЖЕ виконав ALTER у БД вручну — git відстає. Фікс git-only, БД не чіпаємо.

## Кроки
| # | Крок | Стан |
|---|------|------|
| 1 | Follow-up деплою PR#253: cms-sync зелений, УСІ 3 статті в табло (20:31). «Метельне» вийшла теж — розклад загубив баг №1 до фіксу | ✅ |
| 2 | Гілка `roma/audit-fixes` від origin/main (9b1367c4) + тег `byyou-start-audit` | ✅ |
| 3 | utils.js: стем 'еб' → довші форми — ebook/ebay/ebola/ebony більше не блокує · 2f95fc9a | ✅ |
| 4 | test_profanity.mjs: 47 блок + 47 легальних зелені · d8382362 | ✅ |
| 5 | board.js: onAuthChange перезбирає відкриту чат-модалку — після входу зʼявляється поле · 7787eadd | ✅ |
| 6 | admin.html: doSave алертить якщо публікація не вдалась · f40a4e29 | ✅ |
| 7 | cabinet.py: source_url = перше з draft.source_urls · bd13284c | ✅ |
| 8 | SQL git-only: CHECK +'scheduled' +'holiday' · b58873cb | ✅ |
| 9 | CACHE cstl-20260707-2115 + bundle + всі перевірки зелені · a48829b4 | ✅ |
| 10 | Смоук Chromium гостем: CTA ✓, форми нема ✓, 0 фатальних помилок (вхід→форма — на iPhone) | ✅ |
| 11 | Реліз-нотатки + SESSION_STATE → брама деплою | 🟢 |

## Принципи
Корінь, не симптом · мінімальні точкові правки (без рефакторів) · БД не чіпаємо (git-only док) · тести до і після.

## Де зупинились
Кроки 1-10 виконано. Брама деплою: чекаю «деплой» власника.

## Реліз-нотатки

**ЩО ЗМІНИЛОСЬ:**
- Фільтр матюків: прибрано хибне блокування слів ebook/ebay/ebola/ebony (стем 'еб' звужено до реальних форм; тести 47+47 зелені).
- Чат обговорення: гість тисне «Увійдіть, щоб писати» → після входу модалка ОЖИВАЄ (зʼявляється поле вводу; раніше — глухий кут).
- Кабінет: якщо публікація не вдалась (нема дозволу) — тепер чесний алерт, а не мовчазне «ніби опубліковано».
- AI-чернетки: перше джерело статті тепер зберігається у source_url (Алла бачить, звідки факти; поїде і в стрічку).
- SQL-файл схеми наздогнав БД (scheduled/holiday) — довідково, без дій у Supabase.

**ЩО МОЖЕ ЗЛАМАТИСЬ:**
- Перезбирання чат-модалки при вході/виході: якщо вийти з акаунта з відкритим чатом — модалка теж перезбереться (це очікувано).
- Фільтр: форми 'еб*' тепер ловляться лише довшими стемами — екзотичні варіанти можуть проскочити (компроміс проти хибних блокувань).

**ЩО ПЕРЕВІРИТИ НА IPHONE:**
1. Гість: відкрити обговорення → «Увійдіть, щоб писати» → увійти → у модалці зʼявилось поле вводу.
2. Написати «читаю ebook з ebay» у чат — НЕ блокує; мат — блокує.
3. Кабінет: «Зберегти й опублікувати» статтю — публікується; заплановану — стає «Заплановано» з 📅.

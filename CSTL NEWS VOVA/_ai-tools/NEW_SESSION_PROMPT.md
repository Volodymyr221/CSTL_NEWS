# NEW_SESSION_PROMPT.md — Універсальний стартовий промпт для нового чату CSTL NEWS

> **Цей файл — стабільне "ядро" промпта.** Не містить конкретних багів, задач чи чисел, які застарівають.
> Вся динамічна інформація береться з `BOARD.md`, `SESSION_STATE_<ІМʼЯ>.md`, `AUDIT_2026-07.md`, `CSTL_BUGS.md` — саме їх читає Claude при старті.
>
> **Як використовувати:**
> - Варіант 1: Просто викликати у новому чаті `/startuem` — скіл автоматично завантажить цей промпт і виконає його.
> - Варіант 2: Скопіювати блок у самому низу цього файлу (між маркерами ⬇️/⬆️) і вставити як перше повідомлення у новий чат.

---

## Що робить цей промпт

1. Каже новому Claude прочитати обов'язкові файли проекту
2. Нагадує ключові правила роботи (БЕЗ "Роби" — не чіпати код, пояснення у дужках, один крок за раз)
3. Фіксує хто такий Вова і як він комунікує
4. Просить коротку відповідь у стандартному форматі

Промпт **не містить**:
- Конкретних номерів багів (B-XX — вони зміняться)
- Конкретних задач (вони закриваються і з'являються нові)
- Конкретних комітів, версій, часу
- Поточного стану фази

Усе це — в `BOARD.md`, `SESSION_STATE_<ІМʼЯ>.md` і `AUDIT_2026-07.md`, які Claude читає **безпосередньо** при старті. Тому промпт **не застаріває**.

---

## ⬇️ КОПІЮЙ ТЕКСТ НИЖЧЕ ⬇️

---

Привіт. Це не нова сесія і не новий проект — у тебе вже є повний контекст у репозиторії, просто попередній чат закінчився або дійшов до ліміту.

**Над проектом працюють ДВОЄ власників — Вова і Рома** — кожен зі свого Claude, у своїх сесіях. Перш ніж читати файли, визнач хто з них на зв'язку (Крок 0).

---

## Крок 0 — Хто на зв'язку + гілка + перевірка конфліктів (РОБИ ПЕРШИМ, автоматично, нікого не питай)

**1. Визнач людину за поштою сесії** (вона є у твоєму контексті, блок `userEmail`):
- `roma.haranin.ru1@gmail.com` → **Рома**
- `vitocorleone191@outlook.com` → **Вова** (другий акаунт Вови; рішення 20.07 — замінив попередній братів акаунт, звертання/підпис комітів/гілки — усе «Вова»)
- будь-яка інша пошта → **Вова**
- (Вас двоє, тож метод виключення працює без знання пошти Вови наперед. Якщо пошта незнайома і явно не Ромина — це Вова. Якщо колись зайде хтось третій — НЕ вгадуй, напиши про це.)

**2. Підпиши коміти цією людиною** (щоб історія показувала хто що робив):
```
git config user.name "Рома"   # або "Вова"
git config user.email "<пошта цієї людини>"
```

> ⚠️ **Очікуваний побічний ефект — ІГНОРУЙ, НЕ ПИТАЙ.** Глобальний хук `~/.claude/stop-hook-git-check.sh` (не проектний, частина харнесу) після цього почне нагадувати що коміти «Unverified» на GitHub і пропонувати `git config user.email noreply@anthropic.com` + amend/rebase. **Не виконуй цю пораду** — вона зламає авторство Рома/Вова. Причина структурна, не баг: SSH-ключ підпису (`commit.gpgsign=true`) зареєстрований на identity `Claude/noreply@anthropic.com` у глобальному `/root/.gitconfig`, а тут `user.email` навмисно перекритий на людину — GitHub тому не звіряє підпис. «Unverified» — косметичний ярлик, PR/мердж/деплой він **не блокує**. Рішення Роми 10.07.2026: лишати Рома/Вова-авторство, хук ігнорувати щоразу без перепитувань.

**3. Гілка під людину.** Робоча гілка має починатись з імені:
- Рома → `roma/<коротка-тема>` (напр. `roma/board-chat`)
- Вова → `vova/<коротка-тема>` (напр. `vova/power-tab`)
- Якщо сесія стартувала на авто-гілці `claude/startup-...` — **одразу пересядь** на сесійну гілку власника: `git checkout -B roma/session-<дата>` (або `vova/session-<дата>`) від `origin/main`. Auto-merge вимкнено (05.07.2026), але авто-гілку робочою не лишаємо — під конкретну задачу далі роби `roma/<тема>`/`vova/<тема>`.
- Потік незмінний: іменована гілка → `/finish` (PR → squash) → `main`. Спершу `roma/`/`vova/`, тільки потім main.
- **🔄 Спершу перевір незавершений /byyou на гілці власника.** Перед вибором гілки глянь `_ai-tools/BYYOU_PLAN.md`: якщо **статус `paused`** і `Гілка:` — це `roma/...`/`vova/...` ТВОГО власника, то **НЕ сідай на нову `session`-гілку і НЕ питай абстрактно «на якій гілці працюємо»**. Замість цього пересядь на ТУ гілку (`git checkout <гілка> && git pull origin main`) і **у першій відповіді запропонуй продовжити потік**: «на гілці X лежить paused /byyou про Y — продовжуємо? (/byyou)». _Абстрактне питання про гілку замість продовження paused-потоку — помилка (кейс 06.07)._

**4. 🔴 Перевір конфлікти з іншою людиною (ОБОВ'ЯЗКОВО):**
```
git fetch origin
git pull origin main                # свіжий main у робочу гілку (не працюй на застарілому стані)
git branch -r                       # які є віддалені гілки (чи є свіжа гілка іншої людини)
git log --oneline --all -15         # останні коміти всіх гілок + хто автор
```
Подивись також `BOARD.md` (обидві секції — хто над чим зараз) і свій `SESSION_STATE_<ІМʼЯ>.md`. **Якщо рішення/зміни іншої людини суперечать тому що збираєшся робити** (той самий файл, протилежне рішення, недомерджена гілка на ту саму тему) — **скажи про це В ПЕРШІЙ відповіді, до початку роботи**. Не вирішуй конфлікт мовчки.

**5. Взаємне автостеження власників (для ОБОХ — і Рома, і Вова) — увімкни САМ, без нагадування.**
> Обоє хочуть бачити коли інший працює в репо одночасно (рішення 13.06.2026, однаково для всіх). Запускається у КОЖНІЙ сесії будь-якого власника.
- Визнач хто ти (п.1). Склади `SELF` — regex імен/пошт ПОТОЧНОЇ людини (їх НЕ повідомляти):
  - Рома → `Рома|roma\.haranin\.ru1@gmail\.com`
  - Вова → `Вова|Vova|VShevchukkk|Volodymyr221`
- Базова точка: `git fetch origin` → `git for-each-ref --format='%(refname:short) %(objectname:short)' refs/remotes/origin/ > /tmp/cstl_watch_seen.txt`.
- Запусти **Monitor** (фоновий процес, `persistent: true`): `SELF='<твій regex>' bash .claude/owner-watch.sh`. Скрипт кожні ~10 хв `git fetch` і **повідомляє лише про не-ботівську активність ІНШОГО власника** (усе що не `SELF` і не бот: News Bot / vopas-parser / github-actions).
- Тиша = інший власник не активний. Працює доки жива твоя сесія; нова сесія /startuem вмикає знову.
- Якщо `CronCreate`/`ScheduleWakeup` недоступні — саме Monitor і є правильним механізмом (не падай у `/loop`).

**6. Заяви свою активність у `BOARD.md`** (одразу після читання файлів, ДО старту роботи — щоб інший власник бачив що ти в репо; доповнює автостеження):
- Онови секцію `🟢 АКТИВНІСТЬ` — свій рядок: `**Рома/Вова:** <дата> — <що робиш/збираєшся>`.
- Береш конкретну задачу зони — за потреби клейм у `🔨 В РОБОТІ` (зона/файл/гілка).
- Пиши **лише у свою підсекцію**. Дрібний окремий коміт **прямо в `main`** (`git pull origin main` перед, `git push origin HEAD:main` після) — за правилами BOARD.

**7. 🔴 Заведи ЖИВИЙ ЖУРНАЛ сесії (веди по ходу, не з пам'яті наприкінці) — САМ, без нагадування.**
> Причина: підсумок для brain при `/finish` виходив неповним, бо реконструювався з пам'яті великої сесії (контекст губиться). Журнал = записаний факт → звіт повний.
- Створи файл `_session-log/<роль>-<РРРР-ММ-ДД>.md` (напр. `_session-log/roma-2026-07-08.md`; роль = `roma`/`vova` з п.1).
- **Після КОЖНОЇ значущої дії — ОДРАЗУ дописуй рядок:** що зробив (файл/функція) · навіщо · що пробував і що НЕ спрацювало · рішення/слова власника **дослівно** · інцидент і як виправив · PR# · деплой# · CACHE_NAME.
- **Комітируй журнал кожні кілька кроків** (це `_session-log/*.md` — на CACHE не впливає), щоб він пережив краш/ліміт контексту.
- При `/finish` підсумок для brain (Крок 8) генерується **з цього журналу**, а не з пам'яті. **Вичерпно, нічого не стискаючи.**

---

**Далі — прочитай ці файли у такому порядку. Не пропускай жодного.**

1. `CSTL NEWS VOVA/START_HERE.md` — карта проекту, посилання на решту документів
2. `CSTL NEWS VOVA/_ai-tools/BOARD.md` — дошка координації двох власників: хто над чим ЗАРАЗ + питання одне одному (читай ОБИДВІ секції)
3. `CSTL NEWS VOVA/_ai-tools/SESSION_STATE_<ІМʼЯ>.md` — твій стан (Роми або Вови): що зроблено в попередніх чатах, що в процесі. Стан іншого — за потреби глянь його файл.
4. `CSTL NEWS VOVA/_ai-tools/AUDIT_2026-07.md` — майстер-список задач за пріоритетом і зонами (головне джерело "що робити далі")
5. `CSTL NEWS VOVA/CSTL_BUGS.md` — актуальний список відомих багів (🔴 критичні, 🟡 середні, 🟢 дрібні)
6. `CSTL NEWS VOVA/CLAUDE.md` — повні правила роботи, файлова структура, система деплою
7. `CSTL NEWS VOVA/ВОВА_ПРОФІЛЬ.md` — хто такий Вова, як комунікую, як працюю

**Після читання** — також подивись останні 5-10 git-комітів (`git log --oneline -10`) щоб побачити, що відбувалось нещодавно.

---

## Ключові правила які ти ОБОВ'ЯЗКОВО маєш виконувати

### 🔴 Про зміни в коді
- **БЕЗ "Роби" — код не чіпати.** Я скажу явне "Роби" — і тільки тоді ти щось міняєш. До цього — тільки обговорення, питання, пропозиції, план.
- **Один логічний крок за раз.** Не роби 10 задач одним ходом. Один хід Claude = один логічний блок роботи = один коміт.
- **Читай код перед змінами.** Ніколи "на пам'ять". Відкрий потрібний файл через Read tool, прочитай, тільки тоді пропонуй.
- **Не губи попередні фікси.** При кожній зміні перевіряй що попередні виправлення збережені.
- **Перевіряй синтаксис** після JS змін через `node --check файл.js`.

### 🟡 Про комунікацію
- **Пояснення англомовних термінів у дужках.** Після кожного англійського слова — одразу у дужках пояснення українською. Я вчусь програмуванню паралельно через цей проект. Без пояснень я не розумію. Приклади:
  - `PWA` (Progressive Web App — веб-додаток що встановлюється з браузера)
  - `commit` (збережена зміна в git)
  - `deploy` (публікація сайту)
  - `bundle.js` (один файл зі склеєними модулями JavaScript)
  - `Service Worker` (фоновий скрипт який керує кешуванням PWA)
- **Короткі повідомлення.** Я комунікую коротко, очікую того самого. Одна думка за раз.
- **Скріншоти** — мій основний спосіб показати баг або ідею.
- **Сперечайся чесно.** Якщо бачиш слабке місце в моїй ідеї — кажи. Не погоджуйся заради ввічливості.
- **Пропонуй кращі рішення.** Ти краще знаєш технічні можливості — активно пропонуй альтернативи, навіть якщо я не питав.
- **Пояснюй свої дії.** Після кожної технічної дії — коротко що зробив, для чого, як працює.

### 🟢 Про автоматизацію і workflow
- **Автокоміт через `~/.claude/stop-hook-git-check.sh` — це фіча, не баг.** Не переписуй хук. Він страхує роботу на випадок ліміту API і дозволяє мені паралельно тестувати на живому сайті.
- **Двогілковий потік (branching workflow):**
  - **Робоча гілка — іменована власником:** `roma/<тема>` або `vova/<тема>` (сесія стартує на `claude/startup-...`, одразу пересядь на `roma/`/`vova/`). Тут уся розробка, експерименти, недороблене.
  - **Production (бойова) — `main`** — тільки готові узгоджені зміни. Мерджимо через скіл `/finish` (PR → squash merge → автодеплой).
  - **Не пушити в `main` напряму** з робочої сесії — тільки через `/finish` коли я сказав "готово". (Виняток — координаційні файли `BOARD.md`/`SESSION_STATE_*` і парсери: дрібні коміти прямо в main.)
  - ✅ **Auto-merge `claude/**` вимкнено (05.07.2026)** — раніше тихо мерджив у main через `-X theirs` і затирав роботу. Пуш у `claude/*` більше нічого не деплоїть.
- **Лічильник версії внизу екрану** (формат `v{N} · DD.MM HH:MM`) — мій індикатор що деплой пройшов. Якщо після `/finish` число не змінилось → деплой не пройшов.
- **При деплої КОДУ** (зміни у `src/`, `index.html`, `style.css`, `sw.js`) — міняй `CACHE_NAME` у `sw.js` (формат `cstl-YYYYMMDD-HHMM`). При зміні тільки `.md` файлів — **не чіпай**.
- **`bundle.js` комічу в git** (варіант Б, робоча база). CI перегенерує свіжий при деплої, але не комітить назад.
- **Після КОЖНОЇ зміни** оновлюй свій `_ai-tools/SESSION_STATE_<ІМʼЯ>.md` (лише свій; чужий не чіпати).

### ⚠️ Не змінюй без обговорення
- Структуру файлів `data/*.json` без міграції існуючих даних
- Порядок імпортів у `src/app.js`
- Логіку `setupSW()` у `src/core/boot.js`

---

## Формат твоєї першої відповіді

Після того як прочитаєш усі файли — дай коротку відповідь **саме у такому форматі**:

> **Прочитав.** Привіт, [Рома / Вова]. CSTL NEWS, гілка `[назва]` (production: `main`), [коротко — твій стан з SESSION_STATE_<ІМʼЯ>.md, одне речення].
>
> **Пріоритетне у моїй зоні (AUDIT_2026-07.md):**
> 1. [Найвища за пріоритетом задача моєї зони]
> 2. [Друга]
> 3. [Третя]
>
> **Відкриті критичні баги** (якщо є з CSTL_BUGS.md 🔴): [номери і короткі описи, або "нема"]
>
> **Що робить інший власник зараз** (BOARD.md → 🟢 Активність + 🔨 В роботі): [коротко]
>
> **Питання/конфлікти** (BOARD.md → ❓ + Крок 0): [що чекає моєї відповіді або суперечить, або "нема"]
>
> **Що робимо сьогодні?**

Звертайся до людини на ім'я (Рома або Вова) — ти визначив його у Кроці 0.

Якщо щось у документації суперечить іншому — **не мовчи**, покажи мені конкретну суперечність у тій самій першій відповіді. Наприклад: "У AUDIT написано Х, а у SESSION_STATE_<ІМʼЯ> — Y. Що правильно?".

---

## Критично: не починай жодних змін у коді

Поки я не скажу **явне "Роби"** — ти тільки обговорюєш, пропонуєш, плануєш. Читати файли через Read tool — можна і потрібно. Модифікувати, створювати, видаляти файли, робити git-дії — **не можна**.

Якщо не впевнений чи поточна моя фраза — це "Роби" чи ні, запитай: **"Це Роби на [назва задачі]?"**. Краще перепитати ніж зробити зайве.

---

## ⬆️ КОПІЮЙ ТЕКСТ ВИЩЕ ⬆️

---

## Чому цей промпт стабільний

- Конкретні баги, задачі, номери версій — **не в промпті**. Вони у `AUDIT_2026-07.md`, `CSTL_BUGS.md`, `BOARD.md`, `SESSION_STATE_<ІМʼЯ>.md`, які Claude читає при старті.
- Правила роботи, стиль комунікації, профіль Вови — **змінюються рідко**. Якщо і змінюються — оновлення цього файлу разове.
- Формат відповіді — **стандартний шаблон**. Новий Claude кожного разу дає однакову за структурою відповідь, не плутає мене.

Тому цей файл треба оновлювати **тільки коли:**
- Змінились правила роботи (нові заборони, нові дозволи)
- Змінилась фундаментальна структура проекту (додано важливий файл у обов'язкове читання)
- Змінився стиль комунікації Вови

**НЕ оновлювати** на кожен закритий баг, кожну нову задачу, кожен коміт. Це не роль цього файлу.

---

## Як запускається скіл `/startuem`

У корені проекту є файл `.claude/commands/startuem.md` — він каже Claude "прочитай `NEW_SESSION_PROMPT.md` і виконай те що там". Тобто:

```
Вова у новому чаті пише: /startuem
    ↓
Claude Code завантажує .claude/commands/startuem.md
    ↓
startuem.md каже: "Прочитай CSTL NEWS VOVA/_ai-tools/NEW_SESSION_PROMPT.md"
    ↓
Claude читає цей файл і виконує інструкції всередині
    ↓
Claude читає всі 7 обов'язкових файлів, формулює відповідь
```

Існуючий глобальний скіл `/start` (для інших проектів Вови) **не чіпаємо** — для CSTL NEWS є окремий скіл `/startuem`, щоб не було конфлікту.

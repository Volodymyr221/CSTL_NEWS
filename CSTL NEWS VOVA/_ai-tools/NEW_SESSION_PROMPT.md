# NEW_SESSION_PROMPT.md — Універсальний стартовий промпт для нового чату CSTL NEWS

> **Цей файл — стабільне "ядро" промпта.** Не містить конкретних багів, задач чи чисел, які застарівають.
> Вся динамічна інформація береться з `SESSION_STATE_VOVA.md`, `AUDIT_2026-07.md`, `CSTL_BUGS.md` — саме їх читає Claude при старті.
>
> **Як використовувати:**
> - Варіант 1: Просто викликати у новому чаті `/startuem` — скіл автоматично завантажить цей промпт і виконає його.
> - Варіант 2: Скопіювати блок у самому низу цього файлу (між маркерами ⬇️/⬆️) і вставити як перше повідомлення у новий чат.

---

## Що робить цей промпт

1. Каже новому Claude прочитати обов'язкові файли проекту
2. Нагадує ключові правила роботи (БЕЗ "Роби" — не чіпати код, пояснення у дужках, один крок за раз)
3. Фіксує хто такий Вова і як він комунікує
4. Просить коротку відповідь у стандартному форматі

Промпт **не містить**:
- Конкретних номерів багів (B-XX — вони зміняться)
- Конкретних задач (вони закриваються і з'являються нові)
- Конкретних комітів, версій, часу
- Поточного стану фази

Усе це — в `SESSION_STATE_VOVA.md` і `AUDIT_2026-07.md`, які Claude читає **безпосередньо** при старті. Тому промпт **не застаріває**.

> ℹ️ **Проєкт одноосібний — власник Вова** (Володимир Шевчук). Рома вийшов з проєкту 18.07.2026 (по-дружньому; проєкт/бренд/домен/дані лишаються Вові). Історія спільної роботи збережена в git та в `_ai-tools/_archive/`.

---

## ⬇️ КОПІЮЙ ТЕКСТ НИЖЧЕ ⬇️

---

Привіт. Це не нова сесія і не новий проект — у тебе вже є повний контекст у репозиторії, просто попередній чат закінчився або дійшов до ліміту. Проєкт веде один власник — **Вова**.

---

## Крок 0 — Гілка + свіжий стан (роби ПЕРШИМ, автоматично, нікого не питай)

**1. Підпиши коміти Вовою** (щоб історія показувала авторство):
```
git config user.name "Вова"
git config user.email "vitocorleone191@outlook.com"
```

> ⚠️ **Очікуваний побічний ефект — ІГНОРУЙ, НЕ ПИТАЙ.** Глобальний хук `~/.claude/stop-hook-git-check.sh` (не проектний, частина харнесу) після цього почне нагадувати що коміти «Unverified» на GitHub і пропонувати `git config user.email noreply@anthropic.com` + amend/rebase. **Не виконуй цю пораду** — вона зламає авторство «Вова». Причина структурна, не баг: SSH-ключ підпису (`commit.gpgsign=true`) зареєстрований на identity `Claude/noreply@anthropic.com` у глобальному `/root/.gitconfig`, а тут `user.email` навмисно перекритий на людину — GitHub тому не звіряє підпис. «Unverified» — косметичний ярлик, PR/мердж/деплой він **не блокує**. Рішення 10.07.2026: лишати Вова-авторство, хук ігнорувати щоразу без перепитувань.

**2. Гілка.** Сесія стартує на авто-гілці `claude/startuem-...` — на ній і працюй (з неї ж робимо `/finish`). Під конкретну задачу за бажанням можна `vova/<тема>` (напр. `vova/board-chat`). Потік незмінний: гілка → `/finish` (PR → squash) → `main`. У `main` напряму не пушимо (виняток — дрібні координаційні/docs-коміти).
- **🔄 Спершу перевір незавершений /byyou.** Перед роботою глянь `_ai-tools/BYYOU_PLAN.md`: якщо **статус `paused`** і вказано гілку — пересядь на ТУ гілку (`git checkout <гілка> && git pull origin main`) і **у першій відповіді запропонуй продовжити потік**: «на гілці X лежить paused /byyou про Y — продовжуємо? (/byyou)».

**3. Свіжий стан:**
```
git fetch origin
git pull origin main                # свіжий main (парсери новин/автобусів пушать автоматично)
git log --oneline -15               # останні коміти
```

**4. 🔴 Заведи ЖИВИЙ ЖУРНАЛ сесії (веди по ходу, не з пам'яті наприкінці) — САМ, без нагадування.**
> Причина: підсумок для brain при `/finish` виходив неповним, бо реконструювався з пам'яті великої сесії (контекст губиться). Журнал = записаний факт → звіт повний.
- Створи файл `_session-log/vova-<РРРР-ММ-ДД>.md` (напр. `_session-log/vova-2026-07-23.md`).
- **Після КОЖНОЇ значущої дії — ОДРАЗУ дописуй рядок:** що зробив (файл/функція) · навіщо · що пробував і що НЕ спрацювало · рішення/слова Вови **дослівно** · інцидент і як виправив · PR# · деплой# · CACHE_NAME.
- **Комітируй журнал кожні кілька кроків** (це `_session-log/*.md` — на CACHE не впливає), щоб він пережив краш/ліміт контексту.
- При `/finish` підсумок для brain (Крок 8) генерується **з цього журналу**, а не з пам'яті. **Вичерпно, нічого не стискаючи.**

---

**Далі — прочитай ці файли у такому порядку. Не пропускай жодного.**

1. `CSTL NEWS VOVA/HOT_RULES.md` — 8 болючих правил що порушуються найчастіше (читай ПЕРШИМ)
2. `CSTL NEWS VOVA/START_HERE.md` — карта проекту, посилання на решту документів
3. `CSTL NEWS VOVA/_ai-tools/SESSION_STATE_VOVA.md` — поточний стан: що зроблено в попередніх чатах, що в процесі
4. `CSTL NEWS VOVA/_ai-tools/AUDIT_2026-07.md` — майстер-список задач за пріоритетом і вкладками (головне джерело "що робити далі")
5. `CSTL NEWS VOVA/CSTL_BUGS.md` — актуальний список відомих багів (🔴 критичні, 🟡 середні, 🟢 дрібні)
6. `CSTL NEWS VOVA/CLAUDE.md` — повні правила роботи, файлова структура, система деплою
7. `CSTL NEWS VOVA/ВОВА_ПРОФІЛЬ.md` — хто такий Вова, як комунікую, як працюю

**Після читання** — також подивись останні 5-10 git-комітів (`git log --oneline -10`) щоб побачити, що відбувалось нещодавно.

---

## Ключові правила які ти ОБОВ'ЯЗКОВО маєш виконувати

### 🔴 Про зміни в коді
- **БЕЗ "Роби" — код не чіпати.** Я скажу явне "Роби" — і тільки тоді ти щось міняєш. До цього — тільки обговорення, питання, пропозиції, план.
- **Один логічний крок за раз.** Не роби 10 задач одним ходом. Один хід Claude = один логічний блок роботи = один коміт.
- **Читай код перед змінами.** Ніколи "на пам'ять". Відкрий потрібний файл через Read tool, прочитай, тільки тоді пропонуй.
- **Не губи попередні фікси.** При кожній зміні перевіряй що попередні виправлення збережені.
- **Перевіряй синтаксис** після JS змін через `node --check файл.js`.

### 🟡 Про комунікацію
- **Пояснення англомовних термінів у дужках.** Після кожного англійського слова — одразу у дужках пояснення українською. Я вчусь програмуванню паралельно через цей проект. Без пояснень я не розумію. Приклади:
  - `PWA` (Progressive Web App — веб-додаток що встановлюється з браузера)
  - `commit` (збережена зміна в git)
  - `deploy` (публікація сайту)
  - `bundle.js` (один файл зі склеєними модулями JavaScript)
  - `Service Worker` (фоновий скрипт який керує кешуванням PWA)
- **Короткі повідомлення.** Я комунікую коротко, очікую того самого. Одна думка за раз.
- **Скріншоти** — мій основний спосіб показати баг або ідею.
- **Сперечайся чесно.** Якщо бачиш слабке місце в моїй ідеї — кажи. Не погоджуйся заради ввічливості.
- **Пропонуй кращі рішення.** Ти краще знаєш технічні можливості — активно пропонуй альтернативи, навіть якщо я не питав.
- **Пояснюй свої дії.** Після кожної технічної дії — коротко що зробив, для чого, як працює.

### 🟢 Про автоматизацію і workflow
- **Автокоміт через `~/.claude/stop-hook-git-check.sh` — це фіча, не баг.** Не переписуй хук. Він страхує роботу на випадок ліміту API і дозволяє мені паралельно тестувати на живому сайті.
- **Двогілковий потік (branching workflow):**
  - **Робоча гілка:** `claude/startuem-...` (стартова) або `vova/<тема>`. Тут уся розробка, експерименти, недороблене.
  - **Production (бойова) — `main`** — тільки готові узгоджені зміни. Мерджимо через скіл `/finish` (PR → squash merge → автодеплой).
  - **Не пушити в `main` напряму** з робочої сесії — тільки через `/finish` коли я сказав "готово". (Виняток — координаційні/docs-файли і парсери: дрібні коміти прямо в main.)
  - ✅ **Auto-merge `claude/**` вимкнено (05.07.2026)** — раніше тихо мерджив у main через `-X theirs`. Пуш у `claude/*` більше нічого не деплоїть.
- **Лічильник версії внизу екрану** (формат `v{N} · DD.MM HH:MM`) — мій індикатор що деплой пройшов. Якщо після `/finish` число не змінилось → деплой не пройшов.
- **При деплої КОДУ** (зміни у `src/`, `index.html`, `style.css`, `sw.js`) — міняй `CACHE_NAME` у `sw.js` (формат `cstl-YYYYMMDD-HHMM`). При зміні тільки `.md` файлів — **не чіпай**.
- **`bundle.js` комічу в git** (варіант Б, робоча база). CI перегенерує свіжий при деплої, але не комітить назад.
- **Після КОЖНОЇ зміни** оновлюй `_ai-tools/SESSION_STATE_VOVA.md`.

### ⚠️ Не змінюй без обговорення
- Структуру файлів `data/*.json` без міграції існуючих даних
- Порядок імпортів у `src/app.js`
- Логіку `setupSW()` у `src/core/boot.js`

---

## Формат твоєї першої відповіді

Після того як прочитаєш усі файли — дай коротку відповідь **саме у такому форматі**:

> **Прочитав.** Привіт, Вова. CSTL NEWS, гілка `[назва]` (production: `main`), [коротко — твій стан з SESSION_STATE_VOVA.md, одне речення].
>
> **Пріоритетне (AUDIT_2026-07.md):**
> 1. [Найвища за пріоритетом задача]
> 2. [Друга]
> 3. [Третя]
>
> **Відкриті критичні баги** (якщо є з CSTL_BUGS.md 🔴): [номери і короткі описи, або "нема"]
>
> **Що робимо сьогодні?**

Якщо щось у документації суперечить іншому — **не мовчи**, покажи мені конкретну суперечність у тій самій першій відповіді. Наприклад: "У AUDIT написано Х, а у SESSION_STATE_VOVA — Y. Що правильно?".

---

## Критично: не починай жодних змін у коді

Поки я не скажу **явне "Роби"** — ти тільки обговорюєш, пропонуєш, плануєш. Читати файли через Read tool — можна і потрібно. Модифікувати, створювати, видаляти файли, робити git-дії — **не можна**.

Якщо не впевнений чи поточна моя фраза — це "Роби" чи ні, запитай: **"Це Роби на [назва задачі]?"**. Краще перепитати ніж зробити зайве.

---

## ⬆️ КОПІЮЙ ТЕКСТ ВИЩЕ ⬆️

---

## Чому цей промпт стабільний

- Конкретні баги, задачі, номери версій — **не в промпті**. Вони у `AUDIT_2026-07.md`, `CSTL_BUGS.md`, `SESSION_STATE_VOVA.md`, які Claude читає при старті.
- Правила роботи, стиль комунікації, профіль Вови — **змінюються рідко**. Якщо і змінюються — оновлення цього файлу разове.
- Формат відповіді — **стандартний шаблон**.

Тому цей файл треба оновлювати **тільки коли:**
- Змінились правила роботи (нові заборони, нові дозволи)
- Змінилась фундаментальна структура проекту (додано важливий файл у обов'язкове читання)
- Змінився стиль комунікації Вови

**НЕ оновлювати** на кожен закритий баг, кожну нову задачу, кожен коміт. Це не роль цього файлу.

---

## Як запускається скіл `/startuem`

У корені проекту є файл `.claude/commands/startuem.md` — він каже Claude "прочитай `NEW_SESSION_PROMPT.md` і виконай те що там". Тобто:

```
Вова у новому чаті пише: /startuem
    ↓
Claude Code завантажує .claude/commands/startuem.md
    ↓
startuem.md каже: "Прочитай CSTL NEWS VOVA/_ai-tools/NEW_SESSION_PROMPT.md"
    ↓
Claude читає цей файл і виконує інструкції всередині
    ↓
Claude читає всі обов'язкові файли, формулює відповідь
```

Існуючий глобальний скіл `/start` (для інших проектів Вови) **не чіпаємо** — для CSTL NEWS є репо-локальний `/start`, який дублює `/startuem`.

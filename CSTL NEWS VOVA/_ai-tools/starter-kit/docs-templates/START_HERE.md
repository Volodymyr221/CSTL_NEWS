# START HERE — точка входу для кожного нового чату

> **Читай цей файл ПЕРШИМ. Завжди.**

## 🎯 Обовʼязкове читання на початку сесії (у цьому порядку)

| # | Файл | Що дає |
|---|------|--------|
| 1 | `HOT_RULES.md` | правила, які порушуються найчастіше |
| 2 | `_ai-tools/SESSION_STATE.md` | поточний стан: що зроблено, що в процесі, що далі |
| 3 | `_ai-tools/TASKS.md` | ⭐ **джерело істини про задачі** |
| 4 | `BUGS.md` | відомі баги (🔴 / 🟡 / 🟢) |
| 5 | `../CLAUDE.md` | повні правила, файлова структура, деплой |
| 6 | `OWNER_PROFILE.md` | хто власник, як комунікує |

**НЕ читати на початку:** згенеровані файли (бандл), `package-lock.json`, бінарні файли.

## 📂 Ситуаційне читання (тільки коли потрібно)

| Файл | Коли |
|---|---|
| `docs/ARCHITECTURE.md` | зміни структури, деплою, кешу |
| `docs/DESIGN_SYSTEM.md` | будь-яка зміна інтерфейсу — **особливо секція «помилки, які вже були»** |
| `docs/ROADMAP.md` | питання про пріоритети і плани |
| `_ai-tools/BACKLOG.md` | історія й довідка (**НЕ** джерело істини) |
| конкретний файл із коду | **завжди** перед змінами в ньому |

## 🏗 Що це за проєкт
{ОДИН АБЗАЦ: що це, для кого, чим виграє}

## 👤 Хто власник (коротко)
{ІМʼЯ} — {роль}. Комунікує коротко, очікує того самого. Слово-дозвіл на код — «{ДОЗВІЛ}».
Повний профіль — `OWNER_PROFILE.md`.

## 🚀 Поточний стан
> 🔴 **НОВИЙ ЧАТ ПОЧИНАЙ ЗВІДСИ:** повний хендовер — блок **⭐ ХЕНДОВЕР** на самому верху
> `_ai-tools/SESSION_STATE.md`.

## 📁 Карта файлів
```
{дерево проєкту — оновлювати при структурних змінах}
```

## 💬 Перша репліка після читання
> «Прочитав. {ПРОЄКТ}, гілка `[назва]` (production: `[бойова]`). [стан одним реченням].
> Відкрита задача: …. Що робимо?»

## ⚠️ Критично важливі правила
Повний список — `HOT_RULES.md`. Найголовніше: **без слова-дозволу код не чіпати** ·
читай код перед змінами · не пиши «готово» до перевірки · оновлюй `SESSION_STATE.md`.

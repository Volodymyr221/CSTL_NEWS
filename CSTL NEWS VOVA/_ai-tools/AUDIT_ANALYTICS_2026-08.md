# АУДИТ АНАЛІТИКИ — КРОК 1 (26.08.2026)

> Замовлення Вови: «провести аудит кожної поточної цифри: 77, 498, 203, 1000, 35, 544…
> Для кожної визначити джерело → таблиця → поле → SQL → фактична семантика».
> 🔑 Зроблено на ЖИВІЙ базі (`uabyfecseqnemvcqhdem`), не з коду і не з памʼяті.

---

## 🔴 ЗНАХІДКА №1 — ВСІ ЦИФРИ НА ЕКРАНІ ОБРІЗАНІ НА 1000 РЯДКАХ

`admin.html:1355` — `fetchAnalyticsEvents()`:
```js
let q = supa.from('analytics_events').select('visitor_id, event_type, tab, meta, created_at');
if (sinceDays !== 'all') q = q.gte('created_at', …);
const { data } = await q.limit(20000);       // ← НЕМАЄ .order()
```
🔑 **`.limit(20000)` не діє.** У PostgREST/Supabase стоїть серверна стеля `db-max-rows`
(типово **1000**), і клієнт не може її підняти. Тобто адмінка ЗАВЖДИ отримує максимум
**1000 подій**, а рахує по них так, ніби це весь період.

**🛑 ДОКАЗ, ЯКИЙ ВИДНО БЕЗ БАЗИ — суми на екрані Вови:**
- розділи: 544 + 164 + 134 + 82 + 76 = **рівно 1000**
- пристрої: 803 + 197 = **рівно 1000**

**Заміряно SQL, і збіг із числами Вови точний:**

| що | адмінка каже | насправді в базі |
|---|---|---|
| унікальних за 7 днів | 203 | **336** |
| унікальних усього | 35 | **736** |
| подій усього | 1000 | **11 127** |

Перевірка гіпотези прямим запитом: унікальних серед **перших 1000** рядків за 7 днів =
**202** (адмінка показувала 203), серед перших 1000 за весь час = **5**.

🔑 **Ось звідки парадокс «усього менше, ніж за 7 днів»:** без `ORDER BY` для «весь час»
приходить перша тисяча НАЙСТАРІШИХ подій (липень), а там мало різних відвідувачів; для
«7 днів» — тисяча зі свіжих 1833, там їх сотні. Обидва числа неправдиві, просто по-різному.

---

## 🔴 ЗНАХІДКА №2 — У ТАБЛИЦІ НЕМАЄ ТОГО, НА ЧОМУ СТОЇТЬ УСЯ КОНЦЕПЦІЯ

`analytics_events` фактично має **рівно шість колонок**:
```
id · visitor_id (text) · event_type (text) · tab (text) · meta (jsonb) · created_at
```
**НЕМАЄ:** `user_id` · `session_id` · `route` · `device` · `browser` · `os` · `app_mode`.
Немає таблиць `analytics_sessions` і `analytics_errors`.

⚠️ **`visitor_id` — це `currentUserId() || getAnonId()`** (`app.js:230`), тобто в ОДНІЙ
колонці змішані UUID авторизованого і випадковий анонімний id. Одна людина до входу і
після входу — **два різні «унікальні»**. Заміряно: 726 значень схожі на UUID, 10 — ні.

📐 Тобто **сесій у системі не існує взагалі.** Усе, що Вова просить про сесії (тривалість,
медіана, 2.64 сесії/користувача, DAU/MAU, retention, LIVE) — рахувати сьогодні НЕМА З ЧОГО.

---

## 🔴 ЗНАХІДКА №3 — «736 УНІКАЛЬНИХ» ЦЕ НЕ ЛЮДИ

| | |
|---|---|
| усього visitor_id | **736** |
| з них мали **рівно одну** подію | **628** (85%) |
| 2-5 подій | 75 |
| більше 5 подій | **33** |
| поверталися **іншого дня** | **26** |
| профілів у базі | **13** |

🔑 628 «відкрив і зник» — це майже напевно прев'ю посилань, сканери, боти. Заразом
`community` має **728** унікальних, а всі інші вкладки **27-41**: тобто вони бачать лише
головну і не йдуть далі — так поводиться не людина.
➡️ **Реальна аудиторія ≈ 26-33**, і це узгоджується з 13 профілями та правилом №12
(«додаток ще не запускався»).

---

## 🔴 ЗНАХІДКА №4 — ПОДІЙ ЛОГУЄТЬСЯ ЧОТИРИ ВИДИ, А НЕ ТРИДЦЯТЬ

`grep logEvent` по всьому `src/` дає **5 викликів**. У базі за півтора місяця:

| event_type | подій | унік | період |
|---|---|---|---|
| `tab_view` | 11 113 | 735 | 11.07 – 26.08 |
| `db_refusal` | 12 | 2 | 24-25.08 |
| `js_error` | **1** | 1 | 24.08 |
| `pwa_install` | **1** | 1 | 02.08 |

🛑 Зі списку Вови (`session_start`, `question_open`, `feed_post_create`, `search`,
`notification_open`, `pwa_install_prompt`, `pwa_launch`…) **не існує ЖОДНОЇ**, крім
`pwa_install` з однією подією за весь час.
⚠️ Тобто «0 встановлень PWA» на екрані — не помилка підрахунку, а правда: подія є одна.

---

## 🔴 ЗНАХІДКА №5 — ЯРЛИКИ ВКЛАДОК ЗАСТАРІЛІ

`admin.html:1350` `TAB_LABELS_UK`:
```js
community:'Громада', shotam:'Шо в селі', board:'Дошка', chat:'Обговорення',
buses:'Автобуси', power:'Світло', account:'Кабінет'
```
- `shotam` → у застосунку це вже **«Стрічка»**, а не «Шо в селі»;
- `discussions` (1610 подій, жива вкладка **«Питання»**) у мапі **ВІДСУТНІЙ** — тому на
  екрані сирий id `discussions`, на який Вова і вказав;
- `chat` у мапі є, а в базі такої вкладки немає жодної події.

➡️ Тобто це не «стара вкладка в аналітиці», а **застаріла мапа назв**. Вкладка жива, назва
її неправильна.

---

## 📋 ВІДПОВІДЬ НА КОЖНУ ЦИФРУ ЗІ СПИСКУ ВОВИ

| цифра | що це насправді |
|---|---|
| **77** «унікальних сьогодні» | різних `visitor_id` за календарний день, у межах обрізаної тисячі |
| **498** «подій» | кількість РЯДКІВ, що приїхали (≤1000), не подій за період |
| **203** «унікальних за 7 днів» | унікальних у перших 1000 рядках; правда — **336** |
| **1000** «подій» | 🛑 не метрика, а САМА СТЕЛЯ вибірки |
| **35** «унікальних усього» | унікальних у тисячі найстаріших подій; правда — **736** |
| **544 / 164 / 134 / 82 / 76** | `tab_view` по вкладках у межах тієї ж тисячі (сума = 1000) |
| **803 / 197** | `meta.device` у межах тієї ж тисячі (сума = 1000) |
| **190** «23 година» | події за годину в межах тієї ж тисячі; година рахується в поясі БРАУЗЕРА АДМІНА (`new Date().getHours()`), не Києва |

---

## ➡️ ЩО З ЦЬОГО ВИПЛИВАЄ ДЛЯ КОНЦЕПЦІЇ

✅ **Порядок Вови (спершу DATA, потім LOGIC, потім UI) — правильний**, і аудит це підтвердив:
UI тут узагалі ні до чого, брешуть дані й запит.

🔑 **Але є дешевий крок, який Вова не назвав, а він знімає найгірше вже сьогодні:**
порахувати агрегати **на боці бази** (SQL view / RPC), а не тягнути сирі рядки в браузер.
Тоді стеля 1000 зникає в принципі, і «весь час» більше не може бути меншим за «7 днів» —
не тому, що ми додамо перевірку, а тому, що це стане арифметично неможливо.

🛑 **Чого НЕ можна робити до запуску** (правило №12 `HOT_RULES`): будувати retention,
DAU/MAU, LIVE і воронку PWA на 13 профілях і 26 поверненнях. Ці екрани показуватимуть шум,
і на них не можна спиратись. Модель даних під них закласти — так; малювати — після запуску.

---

# ЩО ЗРОБЛЕНО ПІСЛЯ АУДИТУ (26.08, три кроки)

## Крок 1 — агрегати рахує БАЗА (`admin_analytics_overview`)
Стеля 1000 зникла в принципі. Правило «весь час ≥ 7 днів» стало **арифметично
неможливим порушити** — його не треба нікуди вписувати як перевірку.
📐 15 → 336 → 531 → 736 відвідувачів (today/7d/30d/all), монотонно.
Заразом: година тепер у поясі **Києва**, а не браузера того, хто дивиться; ярлики вкладок
оновлені (`shotam` звався «Шо в селі», `discussions` у мапі не було зовсім).

## Крок 2 — розділено ЛЮДЕЙ і ГОСТЕЙ, заведено СЕСІЇ
`analytics_events` дістала `user_id` · `anon_id` · `session_id` + індекси.
Історію заповнено, **0 подій** лишились без обох полів.

| період | екран казав | **людей** | гостьових пристроїв |
|---|---|---|---|
| сьогодні | 77 | **1** | 14 |
| 7 днів | 203 | **6** | 330 |
| 30 днів | — | **7** | 524 |
| весь час | 35 | **11** | 725 |

## Крок 3 — набір подій розширено з 4 до 8
🆕 `session_start` — одна подія на захід, з усім контекстом одразу: `app_mode`
(через **`display-mode`**, а не User-Agent — пряма вимога Вови; за UA це нерозрізненне в
принципі), `browser`, `os`, `device`. Саме вона закриває розділи «PWA vs Browser»,
«Пристрої» і «Браузери», яких не було з чого будувати.
🆕 `feed_post_create` / `feed_post_edit` / `question_answer` / `board_post_submit` —
події ДІЙ, а не переглядів.
🔑 **Логуються в точці ПІДТВЕРДЖЕНОГО успіху**, а не при натисканні кнопки: інакше в
статистику потрапляли б спроби, які база відхилила (антиспам, RLS), і «створено дописів»
стало б більшим за кількість дописів у самій Стрічці.
🔑 `board_post_submit`, а не `create`: оголошення йде на МОДЕРАЦІЮ, тобто подія означає
«людина подала», а не «на Дошці зʼявилось».

## 🛑 ЩО ЛИШИЛОСЬ І ЧОМУ

- **Сесії рахуються лише з 26.08** — за історію їх нема і не буде. Вигадана історія гірша
  за чесну порожнечу.
- **`session_end` і тривалість сесії НЕ зроблені.** Чесно порахувати кінець заходу в вебі
  неможливо: `beforeunload` на iOS не спрацьовує надійно, а `visibilitychange` не
  відрізняє «згорнув на секунду» від «пішов». Тривалість треба виводити з ОСТАННЬОЇ події
  сесії, і це окрема робота.
- **Retention / DAU / MAU / LIVE — не малювати до запуску** (правило №12). Модель даних
  під них уже є; екрани на 11 людях показуватимуть шум.
- **UI лишився старим за структурою** — переробляти його треба, але Вова сам поставив це
  дев'ятим кроком, і аудит це підтвердив: брехали дані, а не екран.

## 🔬 Дві мої помилки за цю роботу (обидві спіймані до деплою)

1. **Мало не оголосив неіснуючу діру безпеки.** Порахував `admins` під роллю
   `authenticated`, отримав 0 і вирішив, що доступ зламаний. Насправді RLS ховала рядок —
   адмін є. ➡️ Рахуючи щось «під роллю», перевіряй, чи не сама роль дає нуль.
2. **Двічі написав виклик без імпорту.** `currentUserId()` у `supabase.js` (кільце
   імпортів — `auth.js` сам тягне цей файл) і `logEvent` у трьох вкладках.
   🛑 `node --check` цього НЕ ловить — ловить лише `node build.js` (esbuild).

---

# ЧАСТИНА ДРУГА — 26.08 (вечір): екран переписано за словами Вови

Першу частину робили за концепцією, яку Вові написав ChatGPT. Потім Вова надіслав
**свій оригінальний голосовий текст** — те, що він насправді диктував. Джерело правди —
саме він, і саме він задав усе нижче.

## 🗣️ Що він просив і що з цього вийшло

| Слова Вови | Що зроблено |
|---|---|
| «тільки правдива інформація… не можна, щоб було все на кучу… базові видно зразу, можна відкрити детальніше» | Один період на весь екран + тап по будь-якому числу → **хто саме** (імʼя, пошта, село, заходи, дні) |
| «збої — окреме вікно; немає — так і писати; є — тап: який користувач, email, імʼя, що сталося, час» | `admin_failure_cases()` + вікно з кожним випадком поштучно |
| «унікальні користувачі і загальна кількість заходів — один може зайти 10 разів» | Три різні картки: **Людей** · **Заходів** · **Гостьових пристроїв**, кожна з поясненням |
| «з веб-версії чи з PWA — обовʼязково розділяти» | Розділ «Звідки заходять» із `display-mode`; за відсутності даних — чесне «ще немає», а не нуль |
| «сьогодні, 7 днів, місяць» | Перемикач, який керує **всім** екраном |
| «по вкладках незрозуміло, чи це заходи чи унікальні» | Під кожною вкладкою: скільки **людей** і скільки **відкриттів**, тап → хто |
| «час доби — повна каша, 23 — що це, хвилини?» | 00:00→23:00 підряд (зроблено ще в #1039, тепер під стендом) |
| «населені пункти: тап → хто заходив» | Новий розділ «Хто заходив — по селах» (зріз **відвідувань**), старий зріз анкет лишився окремо і підписаний як анкети |
| «вік — звідки береш, через Gmail чи як?» | Відповідь просто на екрані: Google дати народження **не дає**; вік із поля анкети, яке людина заповнює сама |

## 🔴 Вада, знайдена читанням порядку виклику (не здогадом)

**Кожен захід писався «гостем».** `init()` кличе `initAuth()` **без `await`**, а
`passDevLock()` повертається з `localStorage`, тобто синхронно. Отже до запису
`session_start` ми доходили раніше, ніж `supa.auth.getSession()` встигав відповісти по
мережі, — і `currentUserId()` віддавав `null` **навіть залогіненій людині**.

📐 Відбиток у базі: у **всіх 16** подій `session_start` `user_id` порожній, тоді як
`tab_view` при перемиканні вкладок — уже з `user_id`. Різниця рівно в моменті виклику.

➡️ Наслідок був би тихий і катастрофічний для замовлення: головні числа Вови
(«унікальні користувачі», «хто саме заходив») показували б **нуль людей назавжди**.
Виправлено `await authReady()` — у неї власна межа часу 2.5 с, і вона завершується і на
збої, тож гість не чекає дарма.

## 🔑 Друга знахідка: один захід рахувався за два

Перезавантаження сторінки в тій самій вкладці пише **другий** `session_start` із тим
самим `session_id` (`sessionStorage` переживає F5). Така пара вже є в базі (09:44:16 і
09:44:34). Без `distinct on (session_id)` «скільки разів заходили» брехало б угору рівно
на кількість перезавантажень.

## 📐 Заміряно після міграції `analytics_drilldown` (7 днів)

| що | значення |
|---|---|
| людей (акаунтів) | 6 |
| заходів (сесій) | 17 |
| гостьових пристроїв | 341 |
| подій | 1889 |
| PWA / браузер | 6 / 11 заходів |
| села тих, хто заходив | Метельне — 1 людина (1027 подій), «не вказано» — 5 |

## 🛑 Чого свідомо немає

- **Розділення PWA/браузер за липень-серпень.** Полів немає в базі взагалі, а вгадати їх
  по User-Agent неможливо: у PWA і в браузері він однаковий. Екран каже це вголос
  (`modes_since`) замість малювати порожній стовпчик як «нуль».
- **Заходи для старих зрізів.** `session_id` існує з 26.08; у зрізі за місяць «заходів»
  буде менше, ніж було насправді, і це підписано.
- **`session_end` і тривалість** — як і раніше, чесно порахувати в вебі нічим.

## 🧪 Стенд

`tests/analytics-screen.mjs` — 48 перевірок. Вирізає чисті функції рендера з `admin.html`
і виконує їх на вигаданих даних. Окремо стереже **узгодженість UI з міграцією**: кожне
поле, яке читає екран, мусить бути в SQL — інакше екран мовчки малював би порожньо.

Контрольний прогін: `BUNDLE_REV=origin/main node tests/analytics-screen.mjs` → **1/48**.

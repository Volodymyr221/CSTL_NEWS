# Стан сесії — CSTL NEWS

**Оновлено:** 2026-04-06

---

## Проект

| | |
|--|--|
| **URL** | https://Volodymyr221.github.io/CSTL_NEWS/ |
| **Репозиторій** | https://github.com/Volodymyr221/CSTL_NEWS |
| **Гілка** | `main` |
| **Власник** | Вова Шевчук (Volodymyr221 / VShevchukkk) |

---

## Поточний стан

**MVP задеплоєно. Деплой працює.**

- `deploy.yml` (автоматичний деплой) — виправлено, баг B-01 закрито
- `CLAUDE.md` і `.claude/` скіли — додано в корінь репо
- `CSTL NEWS VOVA/` — папка з контекстом проекту в репо

---

## Що зроблено (хронологія)

### Сесія 1 (06-07.04.2026)
- Обговорили концепцію проекту CSTL NEWS та екосистему Olyka Castle
- Визначили технічний стек: Vanilla JS, GitHub Pages, esbuild, PWA
- Визначили архітектуру: JSON файли як база даних, модульний src/
- Побудували MVP: 17 файлів, 4 вкладки (Новини/Події/Автобуси/Подати)
- Задеплоїли на GitHub Pages
- Налаштували VS Code на Mac Вови
- Клонували репозиторій на Desktop
- Виявлено і виправлено помилку "non-fast-forward" в deploy.yml
- Додано папку `CSTL NEWS VOVA/` з контекстом проекту
- Додано `CLAUDE.md` в корінь і `.claude/` скіли

---

## Відкриті задачі

| Пріоритет | Задача |
|-----------|--------|
| 🟡 Важливо | Переглянути дизайн разом і покращити |
| 🟡 Важливо | Протестувати push з VS Code на Mac Вови |
| 🟢 Планується | Замінити тестові статті на реальний контент Олики |
| 🟢 Планується | Уточнити розклад автобусів з офіційного джерела |

---

## Технічні деталі

**Як збирається сайт:**
1. `git push origin main` → GitHub Actions запускається
2. `npm install` → встановлює esbuild (збирач коду)
3. `node build.js` → збирає `bundle.js` з усіх `src/` файлів
4. Деплой на GitHub Pages → сайт оновлюється (~2-3 хвилини)

**Важливо при коміті (commit — збереження змін):**
- Використовуй `git -c commit.gpgsign=false commit` через проблеми з підписом
- або налаштуй через VS Code (там підпис не потрібен)

---

## Як запустити локально (на комп'ютері)

```bash
cd ~/Desktop/CSTL_NEWS
npm install        # встановити esbuild (збирач коду)
node build.js      # зібрати bundle.js
# відкрити index.html в браузері
```

---

## Важливі рішення прийняті

1. **JSON файли замість localStorage** — статті в `data/articles.json`, не в браузері
2. **GitHub Actions** — автодеплой при будь-якому push в main
3. **Service Worker** — офлайн-кешування, різна стратегія для статичних файлів vs data/*.json
4. **Firebase пізніше** — для push-сповіщень і акаунтів (Фаза 3, не зараз)

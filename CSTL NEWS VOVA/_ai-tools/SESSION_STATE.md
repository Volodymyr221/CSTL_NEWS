# Стан сесії — CSTL LIFE

**Оновлено:** 2026-06-24 (сесія Вови — ВІДКОТ шапки чату до робочої версії (real-time трекінг `top=vv.offsetTop`) — рішення Вови: повернути як було ДО того як почала ховатись; та версія була нормальна. Спроби top:0 / замок сторінки відкинуто. Гілка `vova/chat-header-revert`) | попереднє: 2026-06-24 (сесія Вови — шапка чату: тепер ЗАМОК сторінки (body→position:fixed) поки чат відкритий → iOS не панорамує документ → шапка+картка стоять на фізичному верху, не зникають/не стрибають при клавіатурі. Гілка `vova/chat-header-lock`) | попереднє: 2026-06-24 (сесія Вови — картка відстеження рейсу: повний маршрут «ЛУЦЬК → ОЛИКА» переміщено НАД назву сегмента «ДІДИЧІ → ОЛИКА» (був унизу біля шкали). Гілка `vova/bus-hero-fullroute`) | попереднє: 2026-06-24 (сесія Вови — фікс шапки чату: при клавіатурі шапка+картка оголошення зникали за верх; `top=vv.offsetTop`+`height=vv.height` → лягає рівно на видиму область. Гілка `vova/chat-header-pin`, чекає `/finish`) | попереднє: 2026-06-22 (сесія Вови — редизайн відкритого ЧАТУ (Дошка→Повідомлення→розмова): Фаза A візуал+«Прочитано» на проді (PR #123 — то був екран списку); тепер чат: хедер з аватаром, картка-контекст оголошення, роздільники дат, градієнт-бульбашки, read receipt + ФАЗА B: фото, відповідь/редагування/видалення, свайп+довге натискання. Гілка `vova/chat-redesign`. ⚠️ Фаза B потребує SQL `scripts/supabase_chat_actions.sql` у Supabase перед роботою)
**Архів попередніх сесій:** `_ai-tools/SESSION_ARCHIVE.md`

---

## ✅ СЕСІЯ ВОВИ 24.06 — Чат: ВІДКОТ шапки до робочої версії (real-time трекінг). Гілка `vova/chat-header-revert`. CACHE → `cstl-20260624-1620`

> «Поверни шапку назад, перед тим як вона почала ховатись; та версія нормальна була» — рішення Вови після 3 невдалих спроб полагодити (top:0 ховав; top=offsetTop наживо не дав виграшу; замок сторінки теж). Відновлено 1:1 `setupKeyboardResize` з коміту-батька 1f01847 (тобто гілка `chat-kb-track`, real-time трекінг): `height=vv.height`, `top=vv.offsetTop`, слухачі resize/scroll без debounce/rAF, без замка body. Прибрано весь код замка сторінки.
> - Файли: `src/core/messages-ui.js`, `bundle.js`, `sw.js`. `node build.js` exit 0. На проді через `/finish`.
> - **Знання на майбутнє:** ця версія трохи «дьоргається» при анімації клавіатури, АЛЕ Вова приймає це як нормальне; не «покращувати» її більше без явного запиту (3 спроби покращити лише зробили гірше — ховали шапку).

---

## 🔧 СЕСІЯ ВОВИ 24.06 — Чат: шапка+картка СТОЯТЬ на фізичному верху (замок сторінки) — спроба 3 (ВІДКОЧЕНО). Гілка `vova/chat-header-lock`. CACHE → `cstl-20260624-1610`

> «Виріши минуле завдання з шапкою» Вови (скрін: при клавіатурі верхній блок ВСЕ ЩЕ зникає за верх; спроба `top=offsetTop` #154 не дала результату наживо). Логіка Вови: шапка має ЗАВЖДИ лишатись там де при закритому чаті, не стрибати.
> - **КОРІНЬ:** iOS «панорамує» (зсуває) сам документ під клавіатуру, тягнучи фіксовану шапку вгору за межі екрану. Компенсація `top=offsetTop` наживо не спрацювала.
> - **Фікс (messages-ui.js setupKeyboardResize):** на час відкритого чату ЗАМИКАЄМО сторінку — `document.body` → `position:fixed; top:-scrollY; overflow:hidden`. Панорамувати/скролити нема чому → шапка стоїть на фізичному верху. Висоту модалки далі тягнемо за `vv.height` (поле над клавіатурою). `top=vv.offsetTop` лишено як страховку (якщо iOS усе ж зсунув — шапка опуститься до видимого верху, а не зникне). Cleanup знімає замок + `window.scrollTo(0,scrollY)`.
> - Файли: `src/core/messages-ui.js`, `bundle.js`, `sw.js`. `node build.js` exit 0. ⏸️ Чекає перевірки Вови наживо (ОБОВ'ЯЗКОВО повністю закрити-відкрити PWA для нового SW).

---

## ✅ СЕСІЯ ВОВИ 24.06 — Автобуси: повний маршрут переміщено НАД назву сегмента в картці відстеження. На проді (PR #155). CACHE → `cstl-20260624-1603`

> «Роби» Вови (скрін вкладки Автобуси/Громада: повний маршрут «ЛУЦЬК → ОЛИКА» висів унизу по центру біля шкали, а має бути НАД назвою проміжного сегмента «ДІДИЧІ → ОЛИКА»). Картка `buildHeroCard` спільна (Автобуси + віджет Громади).
> - **КОРІНЬ:** у розмітці повний маршрут рендерився ПІСЛЯ блоку `.bhv4-body` (де назва-сегмент), окремим `.bhv4-full-route` (`text-align:center`, над шкалою). Плюс розсинхрон: шлях оновлення на місці (`buses.js`) створював ІНШИЙ клас `.bhv4-route-full` під назвою (`afterend`).
> - **Фікс (buses.js + buses.css):** повний маршрут тепер eyebrow (надзаголовок) НАД назвою — перший елемент у `.bhv4-left` через `routeFullHtml` (клас `.bhv4-route-full`). Шлях оновлення на місці вставляє `beforebegin` (над назвою). Прибрано мертвий `.bhv4-full-route` (CSS) і мертву змінну `routeTitle`. Тепер обидва шляхи рендеру консистентні.
> - Файли: `src/tabs/buses.js`, `style/buses.css`, `bundle.js`, `sw.js`. `node build.js` exit 0. ⏸️ Чекає перевірки Вови наживо + `/finish`.

---

## 🔧 СЕСІЯ ВОВИ 24.06 — Чат: шапка+картка ЗНИКАЛИ за верх при клавіатурі (регресія pin-header). Гілка `vova/chat-pin-header → vova/chat-header-pin`. CACHE → `cstl-20260624-1539`. На проді (PR #154)

> «Роби» Вови (скрін: при відкритій клавіатурі верхній блок з ім'ям+карткою оголошення зник вище видимого верху). КОРІНЬ: попередній фікс (`vova/chat-pin-header`) прибив модалку до `top:0`, а iOS при відкритті клавіатури «панорамує» видиме вікно вниз на `vv.offsetTop` → верх модалки (шапка+картка) опинявся вище видимої зони.
> - **Фікс (messages-ui.js setupKeyboardResize):** `top = vv.offsetTop` (не 0) + `height = vv.height` → модалка лягає РІВНО на видиму область, шапка+картка завжди на видимому верху. Додано rAF-коалесування vv-подій (менше смикання при анімації) + `window.scrollTo(0,0)` поки чат відкритий (скрол документа не додає власного зсуву). Cleanup скасовує rAF.
> - Файли: `src/core/messages-ui.js`, `bundle.js`, `sw.js`. `node build.js` exit 0. ⏸️ Чекає перевірки Вови наживо (закрити-відкрити PWA для нового SW) + `/finish`.

---

## ✅ СЕСІЯ ВОВИ 24.06 — Чат: верхній блок (шапка+картка) прибитий до верху, не дьоргається при клавіатурі. Гілка `vova/chat-pin-header`

> «Роби» Вови. КОРІНЬ: `apply()` рухав весь екран за `vv.offsetTop` → верх стрибав при появі/зникненні клавіатури.
> - **Фікс (messages-ui.js setupKeyboardResize):** `top` ЗАВЖДИ `0` (верх прибитий), підлаштовуємо лише низ: `height = vv.offsetTop + vv.height` (низ точно над клавіатурою). Шапка+картка-контекст нерухомі. `.pm-head`/`.pm-ctx` вже `flex-shrink:0` (не стискаються) — додатково нічого не треба.
> - Файли: `src/core/messages-ui.js`, `bundle.js`, `sw.js`. `node build.js` exit 0.

---

## ✅ СЕСІЯ ВОВИ 24.06 — Чат: клавіатура — модалка тримається країв (real-time трекінг). Гілка `vova/chat-kb-track`

> «Роби» Вови (при появі/зникненні клавіатури верх/низ модалки підвисає, дьоргається, видно фон позаду). КОРІНЬ: `setupKeyboardResize` мав debounce 80мс → модалка відставала від клавіатури → щілина з фоном + ривок.
> - **Фікс (messages-ui.js):** прибрано debounce — `apply()` (height=vv.height, top=vv.offsetTop, toggle pm-kb-open) на КОЖНУ `vv.resize`/`scroll` → real-time трекінг, модалка точно займає visualViewport край-у-край. Прибрано `-2px` (щілина). `fullH` captured-once → `window.innerHeight` live. Функція повертає cleanup; виклик через `api._cleanup.push(...)` → знято витік слухачів vv (раніше копились на кожне відкриття чату).
> - Файли: `src/core/messages-ui.js`, `bundle.js`, `sw.js`. `node build.js` exit 0.

---

## ✅ СЕСІЯ ВОВИ 23.06 — Чат: фікс «фото зникає» при надсиланні. Гілка `vova/chat-photo-fix`

> «Роби» Вови (регресія після інкрементального рендеру: фото-повідомлення пропадало). КОРІНЬ: після upload одразу прибиралось локальне прев'ю (blob revoke) і ставився Storage-URL, але поки картинка зі Storage вантажилась — бульбашка порожня = «фото зникло».
> - **Фікс (messages-ui.js sendPhoto):** ПЕРЕД заміною прев'ю на Storage-фото — `await new Promise(... new Image() onload/onerror ...)` (передзавантаження). Тоді `replaceOne` свопить на вже-кешоване фото миттєво, без порожнечі. `URL.revokeObjectURL` перенесено ПІСЛЯ заміни. `api._closed` guard.
> - Файли: `src/core/messages-ui.js`, `bundle.js`, `sw.js`. `node build.js` exit 0.

---

## ✅ СЕСІЯ ВОВИ 23.06 — Чат: інкрементальний рендер (прибрано блимання при публікації). Гілка `vova/chat-incremental`

> «Роби» Вови (плавно, але блимає при новому повідомленні). КОРІНЬ: `renderStream` робив `innerHTML = html` на КОЖНЕ повідомлення → перемальовувались ВСІ бульбашки і ФОТО (фото перезавантажувалось) = блимання всієї стрічки.
> - **Фікс (messages-ui.js):** інкрементальний рендер. Нові хелпери `appendOne(m)` (вставляє ОДНУ бульбашку через insertAdjacentHTML, решта DOM недоторкана) + `replaceOne(m)` (заміна однієї бульбашки на місці за `data-msg`/`data-tag`) + `addReceiptIfNeeded`. `renderBubble` тепер несе `data-tag` (client_tag) для пошуку optimistic-бульбашки. `streamLastDay` для роздільників днів. `renderStream` (повний) лишився для початкового завантаження / помилок-відкату / fallback.
> - **Перемкнено на інкрементал:** sendText/sendPhoto optimistic→appendOne, await→replaceOne; edit/delete→replaceOne; realtime INSERT 'add'→appendOne, 'update'→replaceOne, 'same'→нічого; realtime UPDATE→replaceOne. Решта стрічки і фото більше НЕ перемальовуються → без блимання.
> - Плавність (keep-frame з попереднього пакета) збережена; appendOne скролить smooth-дельтою (DOM не скидається).
> - Файли: `src/core/messages-ui.js`, `bundle.js`, `sw.js`. `node build.js` exit 0.

---

## ✅ СЕСІЯ ВОВИ 23.06 — Чат: справжня плавна поява (keep-frame), прибрано стрибок при надсиланні. Гілка `vova/chat-smooth-append`

> «Роби» Вови (досі скаче при надсиланні). КОРІНЬ: `renderStream` робить `streamEl.innerHTML = html` → scrollTop скидається в 0 → smooth-скрол «пролітав» крізь усю історію = стрибок.
> - **Фікс (messages-ui.js renderStream):** запам'ятовуємо `prevH = scrollHeight` ДО перемальовування; після `innerHTML` спершу `scrollTop = prevH - clientHeight` (відновлюємо ПОПЕРЕДНІЙ кадр без анімації, без «прольоту»), і ЛИШЕ тоді `requestAnimationFrame → scrollBottom(true)` (плавно докручуємо рівно на висоту нового повідомлення). firstRender → миттєво внизу.
> - Ефект: старе плавно вгору, нове рівномірно зʼявляється знизу (+ CSS `.pm-bubble--enter` rise/fade). await-заміна tmp→real має ту саму висоту → скрол no-op (без другого стрибка). realtime 'same' не рендериться (попередній пакет).
> - Файли: `src/core/messages-ui.js`, `bundle.js`, `sw.js`. `node build.js` exit 0.

---

## ✅ СЕСІЯ ВОВИ 23.06 — Чат: свайп-вліво для відповіді (Telegram) + стрибок по цитаті з блиманням. Гілка `vova/chat-reply-gestures`

> «Роби» Вови. Файли: `src/core/messages-ui.js`, `style/messages.css`, `bundle.js`, `sw.js`. `node build.js` exit 0.
> - **Свайп для відповіді: вправо → ВЛІВО** (`setupBubbleGestures`: clamp `[-64,0]`, тригер `dx < -45`). Кругла іконка `.pm-reply-reveal` (ACT_ICONS.reply) виїжджає з правого краю разом з бульбашкою (opacity+scale+translateX за прогресом свайпу), позиціонується абсолютно в `.pm-screen`, чиститься з екраном.
> - **Тап по цитаті у відповіді** (`.pm-quote[data-jump]`) → `jumpToMessage`: `scrollIntoView({behavior:'smooth',block:'center'})` + одне «блимання» `.pm-bubble--flash` (box-shadow-ring keyframe 0.95s). Делеговано у streamEl click.
> - Плавно, без ривків (узгоджено з smooth-скролом попереднього пакета).

---

## ✅ СЕСІЯ ВОВИ 23.06 — Чат: плавна поява повідомлення + smooth-скрол (прибрано «дьоргання»). Гілка `vova/chat-smooth-scroll`

> «Роби» Вови (не скаче, але дьоргається — хотів плавно). КОРІНЬ: `style/messages.css` мав `.pm-bubble { animation: pm-bubble-in }` на ВСІХ бульбашках → при кожному повному перемальовуванні (innerHTML) вся стрічка переанімовувалась = ривок.
> - **CSS:** анімацію перенесено `.pm-bubble` → `.pm-bubble--enter` (0.22s, translateY 8px) — грає ЛИШЕ на нових.
> - **JS messages-ui.js:** множина `seen` (Set за `client_tag||id`) + `msgKey()` → клас `--enter` лише на не-показаних; історія сідиться у `seen` до першого рендеру (не анімується при відкритті). `firstRender` → перший скрол миттєвий, далі `scrollBottom(smooth)` через `scrollTo({behavior:'smooth'})`. `upsertMessage` повертає 'add'/'update'/'same'; realtime INSERT рендерить лише якщо не 'same' (прибрано зайвий ре-рендер на власне відлуння). Прибрано подвійні `scrollBottom()` після optimistic.
> - Файли: `src/core/messages-ui.js`, `style/messages.css`, `bundle.js`, `sw.js`. `node build.js` exit 0.


---

## ✅ СЕСІЯ ВОВИ 23.06 — Чат UX: автоскрол на фото + клавіатура не ховається на «додати фото». Гілка `vova/chat-keyboard-scroll`

> «Роби» Вови (2 скріни). Частина аудиту чату (між пакетами — пріоритет з того що видно наживо).
> - **Баг 1 (фото підскакує вгору):** `renderStream` тепер запам'ятовує `atBottom()` ДО перемальовування; якщо були внизу — `scrollBottom()` + `requestAnimationFrame` + слухач `load` на `.pm-bubble-photo` (докручує коли картинка догрузилась і штовхнула стрічку). Тримає фокус на останньому повідомленні. Не смикає коли читаєш історію (stick=false).
> - **Баг 2 (клавіатура ховається при тапі «додати фото», бар зависає):** на `#pm-attach` додано `pointerdown`+`mousedown` `preventDefault` (як на кнопці ↑, ряд. 442) + `input.focus()` перед `fileEl.click()` → фокус поля не зникає, клавіатура лишається, бар не «висить у повітрі».
> - Файли: `src/core/messages-ui.js`, `bundle.js`, `sw.js`. `node build.js` exit 0.

---

## 🔧 СЕСІЯ ВОВИ 23.06 — АУДИТ ЧАТУ (план 7 пакетів) → Пакет 1: надійна відправка. Гілка `vova/chat-send-reliability`. CACHE → `cstl-20260623-1806`

> «Роби» Вови на повний аудит логіки чату (як у Телеграмі, без глюків). План — `/root/.claude/plans/shimmering-spinning-turtle.md` (7 пакетів). Діагностику зібрано 3 Explore-агентами. Баги: фото-дубль, видалене не зникає одразу, порядок розмов, бейдж не зникає.
> **Пакет 1 (ЦЕЙ) — надійна відправка + БД-фундамент:**
> - **БД (SQL застосовано Вовою в Editor, `scripts/supabase_chat_hardening.sql`):** колонка `messages.client_tag uuid` + тригер `trg_touch_thread` (AFTER INSERT → атомарно оновлює `threads.last_message_at`/`last_message_text`). MCP-запис у веб-сесії знову вимагав підтвердження — пішли через SQL Editor.
> - **Код:** `supabase.js sendMessage(+clientTag)`; `messages-ui.js` — НОВА `upsertMessage()` (єдина точка вставки, дедуплікація за `id`/`client_tag`); `sendText`/`sendPhoto` генерують `client_tag` (newTag → crypto.randomUUID); realtime INSERT через upsert (прибрано крихкий `some(id)`); rollback за `client_tag`.
> - **Фіксить:** «одне фото = два повідомлення» + будь-які дублі від race optimistic↔realtime, незалежно від порядку подій.
> - bundle.js перезібрано, `node build.js` exit 0. Деплой через `/finish`.
> **ЗАЛИШИЛОСЬ (наступні пакети):** П2 видалення/редагування зникають одразу+rollback; П3 realtime без витоку; П4 список «Повідомлення» живий порядок+непрочитані; П5 бейджі зникають надійно (await markThreadRead); П7 публічний чат «Обговорення» (board.js).

---

## ✅ СЕСІЯ ВОВИ 23.06 — Кнопка дзвінка картки-контексту чату: стиль як на модалці. Гілка `vova/chat-call-style`. CACHE → `cstl-20260623-1554`

> «Фініш» Вови (скрін: рожевий залитий кружечок дзвінка в картці-контексті відкритого приватного чату — хотів вигляд як на модалці оголошення, розмір лишити). CSS-only.
> - **Файл:** `style/messages.css` — `.pm-ctx-call`. Фон `var(--brand-tint, #FAE2E6)` (рожевий залитий) → **`transparent`**; додано **`border: 1px solid var(--red)`** (бордова рамка) + **`box-shadow: inset 0 2px 12px rgba(0,0,0,0.28), 0 2px 8px rgba(0,0,0,0.10)`** (внутрішня тінь — об'ємний втиснутий вигляд, копія `.cm-board-call` з модалки). `:active` → `scale(0.92)` + глибша тінь.
> - **Розмір не чіпав:** кружечок 40px, svg 20px (вимога Вови «збережи розмір»).
> - JS не торкався → `bundle.js` без змін (CI перезбере). `check-imports` чисто; `node --check sw.js` OK.
> - Деплой через `/finish` (PR squash). Чекає перевірки Вови наживо (закрити-відкрити PWA для нового SW).

---

## 🔧 СЕСІЯ ВОВИ 22.06 — Фікси чату (свайп + клавіатура). Гілка `vova/chat-fixes`. CACHE → `cstl-20260622-1320`

> SQL `supabase_chat_actions.sql` застосовано в Supabase (Вова, скрін Success). Фаза B активна. Перші баги наживо:
> - **Баг 1 (свайп рухав усі повідомлення):** причина — `.pm-stream` мав авто-горизонтальний скрол → рідний пан тягнув увесь вміст. Фікс: CSS `overflow-x:hidden; overscroll-behavior:contain` + у `setupBubbleGestures` напрямковий замок (`lockDir`) з `preventDefault` для горизонталі (`touchmove` тепер `passive:false`) → їде лише одна бульбашка.
> - **Баг 2 (чат «зближується» при клавіатурі/редагуванні):** причина — `.pm-stream` без `min-height:0` не стискався у flex-колонці під клавіатуру+бар. Фікс: `min-height:0`.
> - Файли: `src/core/messages-ui.js`, `style/messages.css`, `sw.js`, `bundle.js`. ⏸️ Чекає перевірки Вови + ще «нюанси» (список збирається).

---

## ✅ СЕСІЯ ВОВИ 22.06 — Редизайн відкритого чату (Дошка→Повідомлення)

> «Роби» Вови (схвалив план). Обсяг: візуал+«Прочитано» (без БД) + фото/свайп-дії (з БД) — фундамент для майбутніх чатів. CACHE_NAME → `cstl-20260622-0933`. `node build.js` exit 0. Гілка `vova/chat-redesign`.
> **⚠️ ПЕРЕД РОБОТОЮ ФАЗИ B:** запустити `scripts/supabase_chat_actions.sql` у Supabase SQL Editor (MCP-запис у веб-сесії потребує підтвердження — не пройшов). Додає в `messages`: `photo_url, reply_to_id, edited_at, deleted_at` + послаблює обмеження тексту (фото-без-тексту).
> - **Фаза A (без бекенду):** хедер `← + аватар + ім'я + підзаголовок`; картка-контекст оголошення `.pm-ctx` (thumb+назва+категорія, «Переглянути оголошення →» → подія `cstl-open-ad` → `renderAdModal` у board.js); роздільники дат `.pm-daysep` (Сьогодні/Вчора/дата, хелпер `dayLabel`); бульбашки — бордовий градієнт для своїх + анімація появи + шрифт 15.5px; read receipt «Прочитано/Надіслано» (на базі `read_at`); empty state з кнопками-питаннями (тап = надсилає).
> - **Фаза B (бекенд):** `supabase.js` — `sendMessage(+photoUrl,+replyToId)`, `editMessage`, `deleteMessage` (soft), `subscribeThreadMessages` → INSERT+UPDATE (`event:'*'`, `{type,row}`). `messages-ui.js` — кнопка фото 🖼 (`uploadPhotoToStorage` → bubble-photo + лайтбокс), бар «Відповідь/Редагування» над полем, свайп вправо по бульбашці = відповідь, довге натискання/правий клік = меню (Відповісти/Копіювати/Редагувати*/Видалити*; * лише своє), `setupBubbleGestures`. Видалене → плейсхолдер; редаговане → «змінено».
> - **Файли:** `src/core/messages-ui.js`, `src/core/supabase.js`, `src/tabs/board.js` (слухач), `style/messages.css`, `scripts/supabase_chat_actions.sql` (НОВИЙ), `sw.js`, `bundle.js`.
> - **Перевикористання:** `renderBubble`/`openMsgActions`/`setupBubbleGestures` — окремі. Порт у публічні «Обговорення» (таблиця `comments`) = окремий follow-up.
> - **Прибрано з промпту:** онлайн-статус, профіль (приватні), ціна в картці, ⋯/дзвінок. **Відкладено:** голосові, typing-індикатор, серверне «лише автор редагує».
> - ⏸️ Чекає: SQL у Supabase + перевірка наживо + `/finish`.

---

## ✅ СЕСІЯ ВОВИ 22.06 — Редизайн екрана «Повідомлення» (лише візуал)

> «Роби» Вови (схвалив план; обсяг — лише візуал, без БД). CACHE_NAME → `cstl-20260622-0816`. `node build.js` exit 0. Гілка `vova/messages-redesign` (НЕ автомерджиться — через `/finish`).
> - **Файли:** `src/core/messages-ui.js` (тільки `openThreadsList()` + новий хелпер `threadListTime()`), `style/messages.css` (нові класи `.pm-bigtitle/.pm-search*/.pm-chips/.pm-chip/.pm-thread*/.pm-empty--threads`), `sw.js`, `bundle.js`.
> - **Шапка** — дворівнева (Apple large-title): компактна бордова смужка `.pm-head--bar` (лише ← назад) + великий заголовок «Повідомлення» `.pm-bigtitle` (26px/800) на кремі. Прибрано емодзі-заголовок у бордовій смузі.
> - **Пошук** `.pm-search` (клієнтський фільтр по імені/назві оголошення/прев'ю) + **чіпи** `Усі`/`Непрочитані` (`unread` Map). Рендер винесено в `renderThreads()`.
> - **Картки-діалоги** `.pm-thread` (cream, radius 18, `--card-shadow`, margin 16px). Непрочитані → `.pm-thread--unread` (жирніше) + бордова крапка `.pm-thread-dot` + лічильник.
> - **Розумний час** `threadListTime()`: сьогодні→`HH:MM`, вчора→`Вчора`, цей рік→`D місяця`, інакше→`DD.MM.YY`.
> - **Empty state** `.pm-empty--threads` (іконка 💬 56px + «Ваші повідомлення» + підпис). Без розмов — пошук+чіпи ховаються.
> - **Свідомо НЕ робив** (поза обсягом, бо потребує бекенду): compose-кнопка, фільтри Важливі/Архів, закладка діалогу, свайп-дії, префікс «Ви:» (нема колонки `last_message_sender`).
> - **Дрібний борг:** старі CSS-правила `.pm-row*` (окрім `.pm-row-badge`, що ще в ужитку) лишились мертвими після заміни розмітки — прибрати при наступному прибиранні.
> - ⏸️ Чекає перевірки Вови наживо + `/finish`.

---

## ⭐ ХЕНДОВЕР ДЛЯ НОВОГО ЧАТУ — СЕСІЯ 21.06.2026 (Вова, FAB Дошки)

> Уся сесія — про **FAB-кнопку** (Floating Action Button — плаваюча кругла кнопка внизу-справа на вкладці Дошка) та її підменю. Усе на проді. Останній `CACHE_NAME` → `cstl-20260621-1450`. Гілка `claude/startup-uem-ma34ul` (авто-мердж → `main`). Деталі кожної ітерації — у секції «✅ СЕСІЯ ВОВИ 21.06 — FAB Дошки» нижче.

### Фінальний стан FAB (еталон)
- **Іконка** — SVG-бульбашка чату (форма з `MSG_ICON_SVG`). Окремий шар `.cm-board-trigger-close` (✕). При відкритті підменю — **cross-fade** (бульбашка `opacity:0` → ✕ `opacity:1`), БЕЗ обертання. Обидва шари `position:absolute; inset:0`. (Пройшли плюс → гліф `✦` → SVG-іскра → бульбашка; іскра відкинута як «магія, не по темі».)
- **Бейджі непрочитаних = кількість ЧАТІВ** (розмов з непрочитаними, `fetchUnreadByThread().size`): кутик закритої FAB (`#board-trigger-badge`), пункт «Повідомлення» у відкритому меню (`#board-fab-msgs-badge`), і шапка-акаунт (`.account-unread`) — **усюди однакове число**. `refreshUnreadBadge()` (`src/core/messages-ui.js`) — єдина точка, рахує чати і ставить 3 бейджі. Шапку переведено з `fetchUnreadCount`(повідомлення) на чати.
- **Усередині списку «Повідомлення»** біля кожного чату — кількість непрочитаних повідомлень (`.pm-row-badge`, було раніше).
- **Тінь FAB** `.board-trigger--fixed`: `0 8px 24px rgba(0,0,0,0.34), 0 0 16px rgba(0,0,0,0.22), inset 0 1px 2px rgba(255,255,255,0.65)` (світіння по периметру + глибша знизу — відділяє від коркового фону).
- **Пункти підменю** (`.board-fab-label` + `.board-fab-ic`): ВЕЛИКІ букви, glassmorphism (скло+blur+бордовий контур як FAB), **текст чорний** `var(--ink)` (кружечки-іконки бордові). **Порядок: Подати оголошення → Повідомлення → Мої оголошення → Збережені** (Подати першим за запитом Вови).
- Файли сесії: `src/tabs/board.js`, `src/core/messages-ui.js`, `style/community.css`, `sw.js`, `bundle.js`.

### ⚠️ Кеш на iPhone (важливо знати)
Деплой проходить успішно (перевірено через GitHub Actions — `deploy.yml` success). Якщо Вова «не бачить змін» — це iOS PWA тримає старий Service Worker: треба **повністю закрити додаток** (змахнути картку з перемикача) і відкрити знову. Лічильник версії внизу (`v{N} · DD.MM HH:MM`) — індикатор свіжості; на скріні 17:48 показував `v1984` → телефон підхоплює свіжі версії.

### Наступне (за бажанням Вови)
- Можливе тонке полірування FAB (розмір тіні, чистий чорний `#000` для тексту тощо).
- Далі за роадмапом — Фаза 2 (контент) / маркетинг громади, або перебудова вкладки «Дошка»→«Розмови» (чекає назву-парасольку, BACKLOG).

---

## ✅ СЕСІЯ ВОВИ 21.06 — FAB Дошки: іконка-плюс → іскра ✦

> «Роби» Вови (обрав варіант 4 «іскра» + механіка Б). CACHE_NAME → `cstl-20260621-1305`. `node build.js` exit 0.
> - **Іконка FAB** (`.cm-board-trigger-icon`): inline SVG-плюс → гліф `✦` (той самий що у чіпі «ВСІ» фільтрів → єдиний почерк). `src/tabs/board.js:1035`.
> - **CSS** (`style/community.css` `.board-trigger--fixed .cm-board-trigger-icon`): прибрано правило `svg{width/height}`, додано `font-size:26px; line-height:1; align/justify center` під гліф.
> - **Механіка Б (як було у плюса):** при відкритті підменю іскра обертається на 45° (`.board-fab.open … transform: rotate(45deg)` — НЕ чіпав, рядок 1558). Cross-fade у чистий ✕ (варіант а) НЕ робив.
> - **Фікс після перевірки Вови (гліф ✦ виглядав кволо — системний шрифт):** гліф → **SVG-іскра** (4 промені з увігнутими боками, `fill=currentColor`, бордова, 26px). CACHE_NAME → `cstl-20260621-1332`.
> - **Фінал (Вова: іскра не по темі — це хаб дій/чатів, не магія). CACHE_NAME → `cstl-20260621-1409`:**
>   - **Іконка → бульбашка-чат** (SVG, форма з `MSG_ICON_SVG`) + окремий шар `.cm-board-trigger-close` (✕). При відкритті — **cross-fade** (бульбашка `opacity:0`, ✕ `opacity:1`); прибрано `rotate(45deg)`. Обидва шари `position:absolute; inset:0`.
>   - **Бейджі непрочитаних = кількість ЧАТІВ** (розмов з непрочитаними, `fetchUnreadByThread().size`): кутик FAB (`#board-trigger-badge`), пункт «Повідомлення» (`#board-fab-msgs-badge`), і шапка-акаунт — усі однакове число. `refreshUnreadBadge()` (messages-ui.js) розширено: рахує чати, ставить 3 бейджі; шапку переведено з `fetchUnreadCount`(повідомлення) на чати (одне число скрізь). Прибрано непотрібний імпорт `fetchUnreadCount`.
>   - Біля кожного чату в списку «Повідомлення» — кількість непрочитаних повідомлень (`.pm-row-badge`) **вже було**, не чіпав.
>   - **Пункти підменю:** ВЕЛИКІ букви (`text-transform:uppercase`) + стиль скла як FAB (`.board-fab-label` + `.board-fab-ic`: напівпрозоре скло + blur + бордовий контур замість суцільного білого).
> - **Текст пунктів — чорний (запит Вови):** `.board-fab-label` color `var(--red)` → `var(--ink, #2B2B2B)` (кружечки-іконки лишились бордові). CSS-only. CACHE_NAME → `cstl-20260621-1420`.
> - **Тінь навколо FAB (запит Вови):** `.board-trigger--fixed` box-shadow підсилено — рівномірне світіння по периметру + глибша нижня. Фінал (ще підсилено за запитом): `0 8px 24px rgba(0,0,0,0.34), 0 0 16px rgba(0,0,0,0.22)`. CSS-only. CACHE_NAME → `cstl-20260621-1432`.
> - **«Подати оголошення» — першим пунктом (запит Вови):** у markup `board.js` кнопку `data-fab="post"` переміщено на початок меню (стало верхнім пунктом, було нижнім). CACHE_NAME → `cstl-20260621-1450`.
> - Файли: `src/tabs/board.js`, `src/core/messages-ui.js`, `style/community.css`, `sw.js`, `bundle.js`. ⏸️ Чекає перевірки Вови наживо (залогінений, з непрочитаними).

---

## ⭐ ХЕНДОВЕР ДЛЯ НОВОГО ЧАТУ — СЕСІЯ 21.06.2026 (Вова)

> Уся сесія — про **вигляд вкладки Дошка** (картка оголошення + верхнє меню + FAB) і **фінальний фікс кешу**. Усе вже на проді. Останній `CACHE_NAME` → `cstl-20260621-1117`. Детальна хронологія кожної правки — у секціях «СЕСІЯ ВОВИ 21.06 / 20.06» нижче.

### 🔑 Найважливіше — ФІКС КЕШУ (новому Claude знати обов'язково)
Була постійна проблема: вигляд оновлювався на 1-2 запуски пізніше за номер версії (iOS тримав старий код). **Вирішено в 2 кроки в `sw.js`:**
1. `bundle.js` + усі `*.css` переведено з **cache-first → network-first** (кеш лишився лише як fallback офлайн).
2. Додано `fetch(req, { cache: 'reload' })` — обхід HTTP-кешу браузера (GitHub Pages віддає CSS/JS з `max-age ~10хв`, через що `fetch` повертав застаріле).
- Авто-reload у `boot.js` (`controllerchange`/`updatefound`→reload) **уже був** — не дублювати.
- ⚠️ `CLAUDE.md` (файлова структура) оновлено: `sw.js` тепер «index.html + bundle.js + CSS → network-first; картинки/manifest → cache-first».

### 🎨 Дошка — що зроблено (усе на проді)
- **Картки:** прибрано нахил (були ±2°, тепер рівні).
- **Реакції:** поп-ап `.bd-react-popup` z-index `200→1300` — раніше частина emoji над футером відкритої модалки не натискалась.
- **Верхнє під-меню:** «Збережені» прибрано звідти → перенесено у **FAB-підменю** (🔖, поряд з Повідомлення/Мої оголошення/Подати). Лишилось `ДОШКА | ОБГОВОРЕННЯ` з роздільником `|` (`.bd-tab-sep` 42px — щоб ширина кнопок не змінилась). Таби вищі (padding 9px) + текст 14px. Вміст кнопок присунуто до `|` (margin 14px + sep 14px). Під табами — лінія `.bd-tabs-rule` (градієнт що затухає по краях, пік 0.40, тінь над ≈ні).
- **FAB — НОВИЙ стиль glassmorphism:** скло (`rgba(255,255,255,.45)` + `backdrop-filter: blur(14px)`), бордовий контур, **SVG-плюс** замість emoji ✏️ (крутиться на 45° у ✕ при відкритті). Розмір 50px.
- **Картка оголошення (телефонна):** час публікації тепер у рядку з ім'ям (`.cm-board-author-row`: ім'я зліва, дата справа). Ім'я і дата — **завжди UPPERCASE** (`text-transform`). Опис фото-карток — **4 рядки** (`-webkit-line-clamp`). Лінія під описом і над реакціями — **суцільні що затухають по краях** (`::before` градієнт `rgba(0,0,0,.12)`; верхня з drop-shadow вгору). Кнопки 💬/📞 — **32px**, номер по центру іконок. Рядок ім'я+дата присунуто ближче до нижньої лінії.
- **Модалка оголошення:** під-шапка (категорія+заголовок) — заокруглений низ 10px; футер — заокруглений верх 10px.

### ⚠️ Дрібні борги / на потім
- Міні-картки дошки на вкладці **Громада** (`community-blocks.js`, `.cm-board-mini`) — **досі нахилені** (Вова сказав «нічого більше не чіпай» на тій правці). Чекає окремого рішення чи рівняти і їх.
- Відносний час «23 ГОД ТОМУ» теж став капсом (бо `text-transform` на всьому `.cm-board-time`). Вова не заперечив; якщо захоче капс лише для дати-місяця — робити в JS, не CSS.

### Гілка / потік
- Уся сесія на авто-гілці **`claude/startup-uem-2mm9mh`** (пуш у `claude/**` авто-мерджиться в `main` + деплой). PR не робили.

---

## ✅ СЕСІЯ ВОВИ 21.06 — sw.js: bundle.js + CSS на network-first (кінець «застряглого» кешу)

> «Роби» Вови (постійний головний біль: вигляд оновлюється на запуск пізніше за номер версії). CACHE_NAME → `cstl-20260621-0936`.
> - **Діагноз:** авто-reload у `boot.js` (`controllerchange`/`updatefound`→`activated`→reload) ВЖЕ був; усі файли precache існують. Причина — iOS PWA відкладає активацію нового Service Worker (ігнорує `skipWaiting` до повного перезапуску) → cache-first віддавав старий `bundle.js`/CSS попри свіжий `index.html` (network-first).
> - **Фікс:** у `sw.js` fetch-обробнику `bundle.js` і всі `*.css` переведено з cache-first на **network-first** (fetch → кеш як fallback офлайн). Свіжий код тягнеться щоразу при наявності мережі; staleness зникає. Картинки/manifest лишились cache-first.
> - ⚠️ Перший перехід однаково потребує закрити/відкрити (щоб новий sw.js активувався); ДАЛІ оновлення миттєві.
> - Файл: `sw.js`. ⏸️ Чекає перевірки Вови наживо.

---

## ✅ СЕСІЯ ВОВИ 21.06 — Картка оголошення: час під кнопки + лінію вниз + 3 рядки опису

> «Роби» Вови (скрін: обведено нижній блок картки). CACHE_NAME → `cstl-20260621-0854`. `node build.js` exit 0.
> - **Час публікації** («21 год тому») перенесено з верху футера у правий стовпчик **під кнопки 💬/📞** (новий `.cm-board-contact-right`: кнопки → час знизу, сірий).
> - **Штрихпунктирна лінія** перенесена з `.cm-board-footer` на `.cm-board-contact--phone` (`border-top`) → опустилась ближче до номера. Футер для телефонних карток більше не рендериться (`!isPhone`).
> - **Опис фото-карток** `.cm-board-note--has-photo` clamp `2→3` рядки → звільнене місце пішло під текст.
> - Лише телефонні картки; не-телефонні (автор+час у футері) без змін. Файли: `src/tabs/board.js`, `style/community.css`, `sw.js`, `bundle.js`. ⏸️ Чекає перевірки Вови наживо.
> - **Фікс після перевірки Вови:** після прибирання рядка часу номер+іконки підстрибнули вгору. `.cm-board-contact--phone` margin-top `0→16px` — лінія+номер+іконки опущені нижче (компенсує прибраний рядок часу). CACHE_NAME → `cstl-20260621-0901`.
> - **Час на рівні імені (запит Вови):** час перенесено з-під кнопок у новий рядок `.cm-board-author-row` (ім'я зліва, час справа, `justify-content:space-between`). Прибрано `.cm-board-contact-right`; кнопки знову прямо в контакті (`align-items:center`). CACHE_NAME → `cstl-20260621-0915`.
> - **Заповнити вільне місце текстом (запит Вови):** опис фото-карток clamp `3→4` рядки + `.cm-board-contact--phone` margin-top `16px→6px` (текст підходить ближче до лінії). CACHE_NAME → `cstl-20260621-0950`.
> - **Лінія під описом — суцільна що затухає (запит Вови):** `border-top: dashed` → `::before` градієнт `transparent→rgba(0,0,0,.12)→transparent` (той самий колір/насиченість, але суцільна + затухає по краях). CACHE_NAME → `cstl-20260621-0956`.
> - **Тінь над верхньою лінією + нижня лінія така сама (запит Вови):** верхній лінії додано `filter: drop-shadow(0 -2px 3px rgba(0,0,0,.12))` (тінь над, затухає з лінією); нижню штрихпунктирну над реакціями (`.bd-card--board .bd-actions`) теж замінено на суцільну затухаючу `::before` (без тіні). CACHE_NAME → `cstl-20260621-1005`.
> - **Фікс: дашед лишався під суцільною** — штрихпунктир малювало ще `.bd-actions--board-compact` (рядок 2481), не перекрите. Додано `border-top: none` у `.bd-card--board .bd-actions`. CACHE_NAME → `cstl-20260621-1010`.
> - **Рядок ім'я+дата ближче до нижньої лінії (запит Вови):** `.cm-board-author-row` margin-top `4→8px`; `.bd-card--board .bd-actions` margin-top `8→3px`. CACHE_NAME → `cstl-20260621-1049`.
> - **Більші кнопки 💬/📞 на картці (запит Вови):** `28→32px`, svg `13→15px`. Номер центрується по середині іконок (`align-items:center` вже стояв). CACHE_NAME → `cstl-20260621-1057`.
> - **Ім'я автора + дата ВЕЛИКИМИ (запит Вови):** `text-transform: uppercase` на `.cm-board-author/--card/.cm-board-contact-name/.cm-board-time` (картка + модалка). Місяць «20 ЧЕРВНЯ», ім'я завжди caps. CACHE_NAME → `cstl-20260621-1102`.
> - **Фікс HTTP-кешу (uppercase не з'являлось):** network-first `fetch` коду не обходив HTTP-кеш браузера (GitHub Pages max-age ~10хв) → віддавав застарілий CSS. Додано `{ cache: 'reload' }` у sw.js app-code branch. Тепер свіжий код щоразу. CACHE_NAME → `cstl-20260621-1117`.

---

## ✅ СЕСІЯ ВОВИ 21.06 — Новий стиль FAB-кнопки Дошки (glassmorphism)

> «Роби» Вови (обрав варіант «Скляне коло» через AskUserQuestion). CACHE_NAME → `cstl-20260621-0748`. `node build.js` exit 0.
> - **Кнопка** `.board-trigger--fixed`: фон `var(--red)` → **скло** `rgba(255,255,255,0.45)` + `backdrop-filter: blur(14px) saturate(1.5)` + бордовий контур `1.5px rgba(114,47,55,0.55)` + тінь з inset-підсвіткою.
> - **Іконка:** emoji ✏️ → inline **SVG-плюс** (`stroke=currentColor`, колір `var(--red)`, 26px) — рівний на всіх ОС.
> - **Відкрито:** скло щільніше (`rgba(255,255,255,0.62)`), плюс `transform: rotate(45deg)` → стає ✕ (прибрано старий `::after "✕"` і `display:none`).
> - **Зменшено розмір (запит Вови):** коло `56px→50px`, SVG-плюс `26px→24px`. CACHE_NAME → `cstl-20260621-0828`.
> - Файли: `src/tabs/board.js`, `style/community.css`, `sw.js`, `bundle.js`. ⏸️ Чекає перевірки Вови наживо.

---

## ✅ СЕСІЯ ВОВИ 20.06 — «Збережені» з верхнього меню → у FAB-підменю + роздільник табів

> «Роби» Вови. CACHE_NAME → `cstl-20260620-1802`. `node build.js` exit 0, bundle оновлено.
> - **Верхні таби Дошки:** прибрано кнопку-закладку «Збережені». Лишилось `ДОШКА | ОБГОВОРЕННЯ` з роздільником `|` між ними (`.bd-tab-sep`). Прибрано логіку `bd-tab--round`/`visibleTabs` (закладка-кружечок більше не рендериться). Розмір/стиль ДОШКА/ОБГОВОРЕННЯ не чіпано.
> - **FAB-підменю** (speed-dial): додано 4-й пункт «Збережені» 🔖 (між «Мої оголошення» і «Подати оголошення»). Каскад появи `nth-child` оновлено на 4 пункти.
> - **Обробник:** `data-fab="saved"` → `requireAuth('переглянути збережені', ...)` → `activeType='saved'` + `renderAll` (персональне, лише для залогінених — як було у табі). Стрічка «Збережені» рендериться як раніше (`renderBody` гілка chat/saved).
> - **Фікс після перевірки Вови:** після прибирання закладки кнопки ДОШКА/ОБГОВОРЕННЯ (`flex:1`) розтягувались на звільнене місце → ободок виглядав прямокутним. Роздільник `.bd-tab-sep` зроблено `flex:0 0 42px` (ширина колишньої закладки) → кнопки зберігають точно попередню ширину й округлість.
> - **Полірування табів (запит Вови):** `.bd-tab` padding `7px→9px` (вищі кнопки), текст `13px→14px`, emoji `14px→15px`. CACHE_NAME → `cstl-20260620-1833`.
> - **Вміст до роздільної лінії (запит Вови):** `.bd-tab:first-child` (ДОШКА) → `justify-content:flex-end`, `:last-child` (ОБГОВОРЕННЯ) → `flex-start` — вміст присунуто до «|». CACHE_NAME → `cstl-20260620-1852`.
> - **Уточнення Вови — зсунути САМІ кнопки (не вміст):** відкочено flex-end/start (вміст знову по центру кнопки); додано зовнішні поля `margin 14px` на крайніх кнопках + роздільник `42px→14px`. Ширина кнопок незмінна (поля компенсують звуження), але кнопки зсунуті до «|» на ~14px. CACHE_NAME → `cstl-20260620-1903`.
> - **Лінія під табами (запит Вови):** додано `.bd-tabs-rule` (горизонтальна лінія 1px між табами і пошуком) з градієнтом `transparent→rgba(0,0,0,.20)→transparent` — затухає по краях. Розмітка в `renderHeader` (`board.js`). CACHE_NAME → `cstl-20260620-1917`.
> - **Підняти пошук до лінії (запит Вови):** `.bd-tabs-rule` margin-bottom `10px→0` — пошук і все нижче піднялось на ~10px ближче до лінії (лишився flex gap 8px). CACHE_NAME → `cstl-20260620-1921`.
> - **Підняти лінію до кнопок (запит Вови):** `.bd-tabs-rule` margin-top `2px→-8px` — лінія піднята до кнопок (~6px під ними, скасовано flex-gap). CACHE_NAME → `cstl-20260620-1927`.
> - **Колір лінії = роздільник «|» (запит Вови):** пік градієнта `.bd-tabs-rule` `rgba(0,0,0,.20)→.30` (як `.bd-tab-sep`); затухання по краях лишилось. CACHE_NAME → `cstl-20260620-1931`.
> - **Трішки насиченіше (запит Вови):** пік `.bd-tabs-rule` `.30→.40`. CACHE_NAME → `cstl-20260620-1940`.
> - Файли: `src/tabs/board.js`, `style/community.css`, `sw.js`, `bundle.js`. ⏸️ Чекає перевірки Вови наживо.

---

## ✅ СЕСІЯ ВОВИ 20.06 — Модалка оголошення: делікатне заокруглення під-шапки і футера

> «Роби» Вови. CACHE_NAME → `cstl-20260620-1535`. CSS-only.
> - **Під-шапка** `.cm-board-modal-subhead` (категорія+заголовок) — додано `border-bottom-left/right-radius: 10px` (заокруглений низ).
> - **Футер** `.cm-board-modal-foot` (автор/контакт/реакції) — додано `border-top-left/right-radius: 10px` (заокруглений верх).
> - Радіус 10px («чучуть»; модалка сама 20px). Між закругленими блоками — скрол опису. Файл: `style/community.css`, `sw.js`.
> - ⏸️ Чекає перевірки Вови наживо.

---

## ✅ СЕСІЯ ВОВИ 20.06 — Реакції: поп-ап під модалкою (не натискались)

> «Полагодь реакції» Вови (скрін: смужка реакцій над відкритою модалкою, частину emoji не вдавалось натиснути). CACHE_NAME → `cstl-20260620-1130`. CSS-only (bundle без змін).
> - **Корінь:** поп-ап `.bd-react-popup` мав `z-index:200`, а відкрита модалка `.cm-board-modal-note` — `z-index:220` (position:fixed). Модалка перекривала поп-ап → emoji над футером модалки (кнопки 📞/закладка/шер) не натискались. Делегація (на `document`), `pointer-events:auto`, синхрон картка↔модалка (рядок 1415 оновлює всі `[data-react-trigger]`) — усі цілі, проблема була суто у перекритті.
> - **Фікс:** `.bd-react-popup z-index 200 → 1300` (вище модалки). Поп-ап додається в `<body>`, тож тепер поверх усього — кожна emoji натискається і на картці, і у відкритій модалці. Файл: `style/community.css`, `sw.js`.
> - ⏸️ Чекає перевірки Вови наживо.

---

## ✅ СЕСІЯ ВОВИ 20.06 — Дошка: прибрано нахил карток

> «Роби» Вови (скрін Дошки: картки трохи нахилені в різні боки). CACHE_NAME → `cstl-20260620-1053`. `node build.js` exit 0.
> - **Що:** картки Дошки більше не нахилені (були ±2° випадково за `id`). У `src/tabs/board.js` `renderBoardCard` (рядок 641) і `renderOfficialCard` (рядок 776) `tilt` → `0`. CSS не чіпано (`--tilt` лишився, тепер завжди 0deg; hover-ефект цілий).
> - **НЕ чіпано (за вказівкою «нічого не змінюй більше»):** міні-картки дошки на вкладці Громада (`community-blocks.js:557`, клас `cm-board-mini`) — досі нахилені. Чекає окремого рішення Вови чи рівняти і їх.
> - Файли: `src/tabs/board.js`, `sw.js`, `bundle.js`. ⏸️ Чекає перевірки Вови наживо.

---

## ⭐ ХЕНДОВЕР ДЛЯ НОВОГО ЧАТУ — ФАЗА Б ЗАВЕРШЕНА І ЗАДЕПЛОЄНА (20.06.2026)

> Це коротка авторитетна довідка поточного стану. Детальна хронологія реалізації — у секціях нижче (приватність → Етап 0+2 → Етап 1). Гілка розробки: **`vova/auth-phase-b`** (усе змерджено в `main` через PR #119, #120, #121).

### Що це було
**Фаза Б = акаунти + приватність.** Додано вхід через Google, особистий Кабінет, приватний чат покупець↔продавець, гейтинг (gating — обмеження дій для гостя) і приватність персонального стану. Усе **наживо** на `https://volodymyr221.github.io/CSTL_NEWS/`.

### Що працює наживо (перевірено Вовою)
1. **Вхід через Google** (Supabase Auth) — іконка 👤 у шапці → «Приєднайтесь» → Google → «Доповніть профіль» (ім'я + дата народження) → «Кабінет жителя». Сесія зберігається (`persistSession:true`).
2. **FAB-підменю на Дошці** (speed-dial — кругла кнопка що розкриває меню): ✏️ Подати оголошення, 📋 Мої оголошення, 💬 Повідомлення.
3. **Приватний чат 1-на-1** покупець↔продавець: кнопка 💬 на оголошенні → чат; список «Повідомлення»; «Мої оголошення» зі вхідними по кожному посту; бейдж непрочитаних; realtime (живі повідомлення); push-сповіщення про нові повідомлення.
4. **Гейтинг усіх дій** (рішення Вови — «анонім = тільки перегляд»): подача оголошення, реакції, коментарі, відстеження автобуса + сповіщення, закладки оголошень, нагадування про подію, чат — **лише для залогінених**. Поділитись — відкрито (ріст аудиторії). Читання/перегляд — публічне.
5. **Приватність персонального стану + синхрон між пристроями:** закладки, відстеження рейсів, підсвічування «мій лайк/коментар» прив'язані до акаунта (БД), а не до пристрою. Анонім бачить лише публічні агрегати (лічильники реакцій/коментарів, автори, час). Той самий акаунт на іншому пристрої бачить свої збереження; чужий акаунт на тому ж пристрої — ні.

### Нові файли коду
- `src/core/auth.js` — логіка входу: `initAuth/currentUser/currentUserId/currentUserName/isLoggedIn/onAuthChange/signInWithGoogle/signOut/requireAuth/getProfile/saveProfile`.
- `src/core/account-ui.js` — 3 екрани входу/профілю/Кабінету; слухає `cstl-need-login` + `onAuthChange`.
- `src/core/messages-ui.js` — приватний чат (екран розмови, список «Повідомлення», «Мої оголошення», бейдж, push-реєстрація пристрою).
- `style/account.css`, `style/messages.css` — стилі цих екранів (підключені в `style.css`).
- `supabase/functions/send-chat-push/index.ts` — Edge Function: push другому учаснику чату (verify_jwt on).
- `scripts/supabase_profiles.sql`, `supabase_phase_b_chat.sql`, `supabase_phase_b_rls.sql` — SQL міграцій (у репо як довідка; застосовані в БД).

### Зміни в БД Supabase (проект Olyka Castle `uabyfecseqnemvcqhdem`) — УСІ ЗАСТОСОВАНІ
- `profiles(uid, name, email, birth_date)` + RLS «лише свій профіль».
- `posts.owner_uid` (власник оголошення) + політика «Owner reads own posts».
- `threads`, `messages`, `user_push_devices` + RLS (бачать лише двоє учасників) + Realtime.
- `saved_posts(uid, post_id)` + RLS «лише свої» (закладки).
- `comments.sender_uid` (автор коментаря).
- **RLS-перепис**: reactions/comments/push_subscriptions — запис лише `auth.uid()` (анонімний запис заблоковано на рівні БД; читання публічне).

### Налаштування (руки Вови, зроблено)
- **Google Cloud OAuth** (проект «Olyka Castle», Web application, redirect `https://uabyfecseqnemvcqhdem.supabase.co/auth/v1/callback`); Supabase → Auth → Providers → Google увімкнено; Site URL + Redirect `https://volodymyr221.github.io/CSTL_NEWS/`.
- **VAPID_PRIVATE_KEY** — секрет на місці (спільний для bus-push і chat-push).

### MCP (Model Context Protocol — міст Claude↔Supabase)
- **Виправлено для запису:** `.mcp.json` — прибрано `read_only=true`; `.claude/settings.json` — MCP-інструменти Supabase додані в `allow` (прибирає «потребує підтвердження»).
- ⚠️ **Активується лише в НОВІЙ сесії** (MCP підключається + `settings.json` читається на старті). У цій сесії MCP-запис не працював → міграції робили через **SQL Editor** вручну. У новій сесії можна робити міграції через `apply_migration`/`execute_sql` напряму.

### Принципи / межі (важливо пам'ятати)
- **Анонім = тільки читання.** Будь-яка дія що зберігає/персоналізує дані — після входу.
- **Без авто-міграції** старих анонімних localStorage у БД (інакше витік на чужий акаунт) — старі ключі (`cstl-saved-v1`, `bus_track_v2` без uid) orphaned, не показуються.
- **Дрібне обмеження:** заглушений рейс (notify=false) не з'являється крос-девайс (mute видаляє push-рядок). Повний фікс — колонка `notify` у `push_subscriptions` + фільтр в `send-bus-push` (за потреби, окремо).

### Наступні можливі кроки (не критичні)
- (опц.) Крос-девайс для заглушених рейсів (колонка `notify`).
- (опц.) Перевірити security-advisor у Supabase після RLS-перепису.
- Далі за роадмапом — Фаза 2 (контент) / маркетинг громади.

---

## 🟢 ФАЗА Б — приватність персонального стану (синхрон між пристроями) 20.06 (гілка `vova/auth-phase-b`)

> Рішення Вови: персональний стан (закладки, відстеження, «мій лайк/коментар») —
> прив'язаний до акаунта, синхрон між пристроями; анонім бачить ЛИШЕ публічні
> агрегати (лічильники реакцій/коментарів, автори/час), нічого персонального.

**ЗРОБЛЕНО (код, гілка):**
- `scripts`/SQL: нова таблиця `saved_posts(uid, post_id)` + RLS «лише свої» — **Вова виконує в SQL Editor** (MCP-запис у веб-сесії не працює).
- `supabase.js`: `fetchSavedPostIds/addSavedPost/removeSavedPost` (закладки в БД); `fetchTrackedRoutesFromDB` (гідрація відстежень з `push_subscriptions` для крос-девайс); `fetchAllComments` додано `sender_uid`.
- `board.js`: закладки тепер у БД (пам'ять `savedIds`, заповнюється у renderBoard); «моя» реакція/коментар — за uid (currentUserId/sender_uid), лише залогінений; таб «Збережені» прихований гостю; `onAuthChange → renderBoard`. Прибрано localStorage `cstl-saved-v1`, `cstl-my-comments-v1`.
- `buses.js`: ключ відстеження per-uid (`bus_track_v2:<uid>`); гідрація з БД при вході (крос-девайс); бейдж/hero/іконки приховані гостю; `onAuthChange` → перезавантаження.
- `community-blocks.js`: віджет автобуса читає per-uid; `onAuthChange → renderBusBlock`.
- CACHE_NAME → `cstl-20260620-0829`. Build чистий.

**Межі:** без авто-міграції старих анонімних localStorage у БД (інакше витік на чужий акаунт) — старі ключі orphaned. Заглушений рейс (notify=false) крос-девайс не з'явиться (mute видаляє push-рядок) — дрібне обмеження.

**ЗАЛИШИЛОСЬ:** виконати SQL `saved_posts` у Supabase; деплой через `/finish`; перевірка наживо (анонім нічого персонального не бачить; крос-девайс).

---

## 🟢 ФАЗА Б — Етап 0 (вхід) + Етап 2 (гейтинг) ЗРОБЛЕНО 19.06 (гілка `vova/auth-phase-b`, ще НЕ в main)

**Етап 0 — ГОТОВО (руки Вови):** Google Cloud OAuth client (Web application, redirect `…supabase.co/auth/v1/callback`) + Supabase Auth → Providers → Google увімкнено + Site URL/Redirect `https://volodymyr221.github.io/CSTL_NEWS/`. Вхід можна тестувати наживо ПІСЛЯ деплою.

**Бекенд чату — ГОТОВО:** міграція `supabase_phase_b_chat.sql` застосована повністю (через SQL Editor — MCP у веб-сесії не пише, вимагає інтерактивного підтвердження якого нема). Edge Function `send-chat-push` задеплоєна (verify_jwt on). Секрет `VAPID_PRIVATE_KEY` на місці.

**Етап 2 — гейтинг ЗРОБЛЕНО (рішення Вови: закрити ВСЕ за входом):**
- `src/core/auth.js` — додано `currentUserName()` (ім'я для коментарів: кеш профілю → Google-метадані → «Житель»); кеш чиститься при `signOut`.
- `src/tabs/board.js` — `requireAuth` на: подачу оголошення (FAB), реакції, коментарі. Реакції тепер пишуть `currentUserId()` (було `getAnonId()`); коментарі несуть `sender_uid=uid` + ім'я автора; «моя» реакція визначається за `uid||anonId`.
- `src/tabs/buses.js` — `requireAuth` на вмикання трекінгу/сповіщень (кнопка трек, дзвіночок-on, ⚠️-bell). `user_uuid` push = `currentUserId()`.
- `src/tabs/board.js` — `requireAuth` на закладку (зберегти оголошення/обговорення у «Мої», `data-save-id`).
- `src/tabs/events.js` — `requireAuth` на «Створити нагадування» про подію (.ics).
- Принцип (рішення Вови): незалогінений = ТІЛЬКИ перегляд; будь-яка дія що зберігає/персоналізує дані (лайк, коментар, закладка, трек, нагадування, повідомлення, подача) — лише після входу. Поділитись лишається відкритим (ріст); вибір зупинок/району — фільтри перегляду, не гейтяться.
- `src/core/supabase.js` — `addComment(...,senderUid)`, `setReaction` параметр перейменовано на `userId`.
- Build чистий, `bundle.js` перезібрано. CACHE_NAME → `cstl-20260619-1730`.

**Узгоджено з RLS:** значення `auth.uid()` пишуться скрізь (reactions.user_id, comments.sender_uid, push.user_uuid) → після Етапу 3 (RLS-перепис) залогінені вставки проходять, аноніми блокуються на рівні БД.

**ЗАЛИШИЛОСЬ:** — нічого, Фаза Б закрита. ✅ Деплой у main (PR #119 + #120), вхід Google перевірено наживо, RLS-перепис (`supabase_phase_b_rls.sql`) застосовано через SQL Editor (реакції/коментарі/push → `auth.uid()`, анонімний запис заблоковано на рівні БД, читання публічне). Чат-UI (`messages-ui.js`) живий. CACHE_NAME → `cstl-20260619-2220`.

---

## 🔵 ФАЗА Б РОЗПОЧАТА 19.06 (гілка `vova/auth-phase-b`) — Етап 1: фундамент авторизації

> Рішення Вови 19.06: вхід **м'який (soft)**; модерація залогінених — **лишити pending**; Етап 0 (Google Cloud + Supabase provider) Вова зробить **ввечері**. Повна карта — `docs/BOARD_FINAL_PLAN.md` секція «Фаза Б». CACHE_NAME → `cstl-20260619-0847`.

**Етап 1 — ЗРОБЛЕНО (фундамент, гейтинг ВИМКНЕНО, нічого не ламає для анонімів):**
- `src/core/supabase.js` — клієнт `auth: { persistSession:true, detectSessionInUrl:true, autoRefreshToken:true }` (було `persistSession:false` — головна дірка: без цього Google-вхід не зберігався).
- `src/core/auth.js` — НОВИЙ модуль (логіка входу): `initAuth/currentUser/currentUserId/isLoggedIn/onAuthChange/signInWithGoogle/signOut/requireAuth/getProfile/saveProfile`. `requireAuth` готовий, але ще НЕ підключений до дій (Етап 2).
- `src/app.js` — доданий імпорт + `initAuth()` у `init()` (відновлює сесію; гість → no-op). Порядок існуючих імпортів не чіпано.
- Таблиця `public.profiles` (uid→auth.users, name, email, birth_date) + RLS «тільки свій профіль» — створено через Supabase MCP (міграція `phase_b_profiles`). SQL у репо: `scripts/supabase_profiles.sql`.

**UI входу — ЗРОБЛЕНО (макет затверджено Вовою 19.06, гілка `vova/auth-phase-b`):**
- `index.html` — іконка 👤 у шапці (`#account-btn`, поряд із закладкою рейсів; залогінений → кружечок залитий).
- `src/core/account-ui.js` — НОВИЙ: 3 екрани (центрована картка): «Приєднайтесь» (Gmail + «Поки пропустити»), «Доповніть профіль» (ім'я з Google + дата народження, «Пізніше»), «Кабінет жителя» (ім'я/email + дата + «Мої оголошення: скоро» + «Вийти»). Слухає `cstl-need-login` (контекстний гейт) і `onAuthChange` (новачок без рядка profiles → авто-екран профілю).
- `style/account.css` — стилі `.acc-*` (токени base.css), підключено в `style.css`.
- `src/app.js` — `initAccountUI()` у `init()`. Бандл перезібрано, лінт чистий.
- ⚠️ Кнопка Gmail НЕ працюватиме до Етапу 0 (провайдер Google). Решта UI безпечна для анонімів. **Гейтинг дій (`requireAuth`) ще НЕ підключено** — нічого не блокується.

**Етап 0 — ТВОЇ РУКИ (блокер, ввечері):** Google Cloud OAuth client + Supabase Auth → Providers → Google + Redirect `https://volodymyr221.github.io/CSTL_NEWS/`. Без цього вхід не тестується.

**FAB-підменю на Дошці — ЗРОБЛЕНО (запит Вови 19.06):** кругла кнопка-олівець (right-bottom) тепер speed-dial: тап → розкриває 3 пункти (💬 Повідомлення, 📋 Мої оголошення, ✏️ Подати оголошення) + затемнення; олівець→✕; фон/повторний тап закриває. «Подати оголошення» працює як раніше (анонімно). «Мої оголошення»/«Повідомлення» — поки тост «скоро» (підключаться в Етапах 4–5 з входом). Код: `src/tabs/board.js` (markup `#board-fab` + toggle/handlers), `style/community.css` (`.board-fab*`). CACHE_NAME → `cstl-20260619-0921`.

**Далі (мій код, після Етапу 0 + перевірки входу наживо):** Етап 2+3 (гейтинг `requireAuth` на дії + переписати RLS reactions/comments/push → `auth.uid()`, закриває попередження безпеки) — деплоїться РАЗОМ і лише після робочого входу; Етап 4 (приватний чат покупець↔продавець: `threads`/`messages`+RLS, «Написати», `send-chat-push`); Етап 5 (дії над повідомленнями: `comments.sender_uid`).

⚠️ **Мертві нюанси (враховано):** (1) RLS не вмикати до робочого входу — інакше блокує анонімів; (2) старі пости без `owner_uid` → «Написати» лише на нових; (3) старі анонімні реакції/коментарі лишаються читабельні.

---

## ✅ СЕСІЯ ВОВИ 19.06 (бекенд Supabase) — Cron-дубль прибрано + звірка send-bus-push

> Через Supabase MCP (проект Olyka Castle `uabyfecseqnemvcqhdem`):
- **Звірка функції:** задеплоєна Edge Function `send-bus-push` (версія 10) байт-у-байт = репо (`supabase/functions/send-bus-push/index.ts`). Розбіжностей нема.
- **Cron-дубль прибрано:** було `jobid 1` (кожні 5 хв) + `jobid 4` (щохв) — обидва б'ють у `send-bus-push`. Виконано `SELECT cron.unschedule(1)`. Лишилось одне завдання `jobid 4`. Закриває хвіст BACKLOG.
- Це бекенд-зміна (база), коду в репо не чіпала.

---

## ✅ СЕСІЯ ВОВИ 19.06 (гілка `vova/modal-paragraphs`) — Абзаци в описі модалки оголошення

> «Роби» Вови (скрін: суцільний текст без абзаців). Гілка `vova/...` (НЕ автомерджиться — через `/finish`). CACHE_NAME → `cstl-20260619-0649`.
- **Проблема:** опис у модалці рендерився одним блоком — HTML схлопував переноси рядків (`\n`) у пробіл. У БД абзаци збережені (`buildPayload` робить `.trim()`, внутрішні `\n` лишаються; textarea теж зберігає).
- **Фікс (CSS, 1 правило):** додано `.cm-board-modal-content .cm-board-text { white-space: pre-wrap; }` у `style/community.css` (після `.cm-board-modal-content`). Зберігає абзаци й відступи як надрукувала людина. Scope — лише модалка (`.cm-board-modal-content` є тільки в `renderAdModal`/копії модалки); картки на Дошці (обрізані `-webkit-line-clamp`) не зачеплено.
- Файли: `style/community.css`, `sw.js`. `check-imports` чисто (JS не чіпано → `bundle.js` без змін, CI перегенерує). ⏸️ Чекає перевірки Вови наживо.

---

## 🆕 ХЕНДОВЕР ДЛЯ НОВОГО ЧАТУ (Вова, 19.06) — ⭐ РОБОЧА ВЕРСІЯ МОДАЛКИ ОГОЛОШЕННЯ

> Гілка `claude/startup-uem-c0saeo` (авто-мердж → `main` → на проді). Фінальний `CACHE_NAME` → `cstl-20260619-0125`. `node build.js` exit 0.
> **Вова: «це є робоча версія модалки, запам'ятай це, потім можливі деякі зміни».** Нижче — еталонний стан. Файли: `src/tabs/board.js`, `style/community.css`, `style/base.css`, `sw.js`.

**1. Футер модалки (`.cm-board-modal-foot` → `.cm-board-modal-meta`):**
- Формат контакту одним рядком: `0506480755 — ВАЛЕНТИН` (номер бордовий `#8B3A4A` жирний `.cm-board-contact-phone`, ім'я сіре `.cm-board-contact-name`). Спочатку номер, потім ім'я.
- Дата (`.cm-board-time`) — окремим рядком ПІД контактом (лівий стовпчик `.cm-board-modal-meta-text`, flex-колонка).
- **Кнопки праворуч, вертикально по центру** лівого стовпчика (`.cm-board-modal-meta` = flex, `align-items:center`): порядок 💬 повідомлення (`.cm-board-msg-btn`, `data-msg-soon`) → 📞 дзвінок (`.cm-board-call`).
- Пунктирної лінії зверху НЕМА (`border-top:none`).

**2. Кнопка «Повідомлення» (нова):** константа `MSG_ICON_SVG` у `board.js`; атрибут `data-msg-soon`; handler у `attachBoardDelegation` → `showToast('💬 Незабаром')`. Є і в модалці, і на картці (стиль як кнопка дзвінка).

**3. Картка (закрита, `renderBoardCard`):** якщо контакт = телефон → номер зверху (`.cm-board-contact-num`), `— ім'я` рядком нижче (`.cm-board-author--card`); кнопки 💬+📞 зменшені до **28px** (`.cm-board-contact--phone .cm-board-msg-btn/.cm-board-call`) — щоб номер завжди влазив у рядок.

**4. Sticky під-шапка (категорія+заголовок):** при скролі фото гортається вгору і ховається, блок `.cm-board-modal-subhead` прилипає одразу під смужку-грабер (`position:sticky; top:-1px`), опис гортається ПІД ним. `.cm-board-modal-scrollarea` має `background:inherit` (колір картки) — щоб прилиплий блок був непрозорий. `top:-1px` прибирає субпіксельну щілину на стику з грабером (де просвічував текст).

**5. Тіні країв опису (тонкі, прикріплені до блоків):**
- Під-шапка відкидає тінь ВНИЗ: `box-shadow: 0 5px 7px -4px rgba(0,0,0,0.16)` (sticky → тінь їде з нею, видно поки гортаєш опис).
- Футер відкидає тінь ВГОРУ: `box-shadow: 0 -5px 7px -4px rgba(0,0,0,0.16)`.
- Негативний spread (`-4px`) → тінь притиснута до краю, **середина опису чиста** (не затемнена). Сам `.cm-board-modal-content` БЕЗ тіні.

**6. Фото модалки:** `.cm-board-modal-photo` має `max-height:200px` (стабільна висота і для портретних, і для ландшафтних).

**7. ⚠️ Позиція тостів (глобально для ВСІХ тостів):** `.toast` у `base.css` піднято вище центральної кнопки «ГРОМАДА» — `bottom: calc(var(--tabbar-h) + safe-area + 36px)` (коло Громади: верх на 80px над таббаром). **Правило: будь-який тост має бути вище іконки Громади.**

**Скіли цієї сесії (встановлені, не чіпати):** `.claude/skills/ui-ux-pro-max/` (дизайн-CLI Python) + `.claude/skills/caveman/` (режим стислої комунікації, завжди активний, зберігає пояснення термінів у дужках).

**⚠️ Два власники (Вова + Рома).** Новому чату: `git fetch origin` + Крок 0 `/startuem`.

---

## 🆕 ХЕНДОВЕР ДЛЯ НОВОГО ЧАТУ (Вова, 18.06)

> Уся сесія 17-18.06 — про **модалку оголошення на Дошці** (зум-картка). Усе нижче вже на проді (гілка `claude/pensive-cerf-p35y0p` → авто-мердж у `main`). Деталі ітерацій — у секціях «СЕСІЯ ВОВИ 17/18.06» нижче.

**Фінальний стан модалки оголошення (`renderAdModal` у `src/tabs/board.js`, стилі `.cm-board-modal-*` у `style/community.css`):**
- **Структура (3 частини):** `.cm-board-modal-bar` (смужка-ручка зверху) · `.cm-board-modal-scrollarea` (скролиться: фото + категорія/заголовок + опис) · `.cm-board-modal-foot` (закріплений знизу: автор/дата + контакт + реакції/збереження/шер).
- **Фото:** фіксована рамка **4:3** (`object-fit:cover`), гортається РАЗОМ з текстом (звичайний скрол). Для 2+ фото — свайп-галерея + крапки + тап → повний екран.
- **Висота:** модалка центрована між шапкою і таб-баром; низ не залазить під кнопку-замок (резерв 18px).
- **Закриття:** свайп вниз за **смужку-ручку** (працює завжди) або коли скрол угорі; тап по фону. ✕ нема.
- **Фон при відкритій модалці заблокований** (`body.cm-zoom-open .app-main{overflow:hidden}` у `base.css`; спільний `.modal-open` теж тепер блокує — для всіх модалок).
- **Закривається при перемиканні вкладки** меню (`cstl-tab-changed` → `collapse()`).
- **Футер стиснутий** (менші відступи, кнопка дзвінка 36px).

**⚠️ ВАЖЛИВО — чому НЕМА анімованого стискання фото:** пробували ~10 варіантів (фото зменшується при скролі: height у потоці / clip-path / абсолютний оверлей / scaleY). УСІ давали баги на iOS (смикання, просвічування тексту вгорі, застрягання скролу, перекриття заголовком) — бо JS-анімація на скролі фундаментально конфліктує з рідним інерційним скролом iOS. Рішення: **прибрали стискання**, фото просто скролиться. ⏸️ Якщо повертати анімацію — ТІЛЬКИ через CSS `animation-timeline: scroll()` (scroll-driven, компоситор), НЕ через JS.

**⚠️ Два власники (Вова + Рома).** Новому чату: `git fetch origin` + Крок 0 `/startuem`.

---

## ✅ СЕСІЯ ВОВИ 17.06 (гілка `claude/pensive-cerf-p35y0p`) — Модалка оголошення: висота + шапка + логіка фото (4:3 згортна)

> «Роби» Вови (скрін: низ модалки залазив під кнопку-замок при довгому тексті). Фінальний CACHE_NAME → `cstl-20260617-1807`. `node build.js` exit 0.
> - **Фікс висоти:** низ модалки більше не залазить під центральну кнопку-замок «ГРОМАДА» (стирчить ~27px над таб-баром). У `.cm-board-modal-note` (`style/community.css`) точку центру піднято на 30px і `max-height` зменшено (`-40px → -70px`) — резерв під виступ кнопки. Короткі оголошення центруються як раніше; довгі ростуть донизу до безпечної межі + скрол.
> - **Шапка модалки над фото:** новий блок `.cm-board-modal-bar` (**sticky** — завжди зверху при скролі) зі смужкою-ручкою `.cm-board-modal-grip` по центру (свайп вниз закриває). Фон `inherit` (колір картки).
> - **Правки після перевірки Вови (v1839):** ✕ прибрано (закриття — свайп вниз / тап по фону); фото вище — `.cm-board-modal-slide img` `max-height 32vh → 40vh` (по вертикалі). ⚠️ Мій тимчасовий `.cm-board-modal-close` у v1839 випадково перекривав ✕ форми «Подати оголошення» (спільний клас) — прибрано, стиль форми (рядок 930) повернувся до норми.
> - **Правки після перевірки v1840:** низ модалки опущено («чучуть») — резерв під кнопку `30px → 18px` (центр −18, `max-height −58`).
> - **🆕 Логіка фото — ЗГОРТНА ШАПКА (фінал, «як найперший раз»):**
>   - ⚠️ Спроби overlay (фото абсолютом над/у скрол-зоні): варіанти ламали рендер АБО заголовок наїжджав на фото. Вова: «зроби як найперший раз — фото приклеєне до заголовка». **Повернуто структуру head/body.**
>   - **Структура:** модалка flex-колонка `overflow:hidden`: `.cm-board-modal-bar`(грип) ↑ · `.cm-board-modal-head`(flex:0 0 auto: рамка `.cm-board-modal-photoframe`[4:3, min-height:0] + `.cm-board-modal-subhead`[категорія+заголовок, прямо під фото, НЕ sticky]) ↑закріп · `.cm-board-modal-body`(flex:1, overflow auto, `overscroll-behavior:contain`: опис) ↕ · `.cm-board-modal-foot`(стиснутий) ↓.
>   - **Механіка:** JS на скролі body: `frame.height = max(72, fullH - body.scrollTop)` (1:1). Рамка стискається → під-шапка приклеєна ПРЯМО під фото їде вгору разом (без перекриття); текст скролиться під head. Це та сама анімація, що в найпершій версії (04166f6).
>   - **Свайп-глюк (виправлено):** `overscroll-behavior:contain` на body — прибрав iOS-«відскок», що конфліктував зі свайп-закриттям. Свайп-закриття від `body.scrollTop<=2`.
>   - **2+ фото:** свайп-галерея в рамці + крапки + тап → повний екран.
>   - **Закріплений футер** (автор/дата+контакт+реакції) — не скролиться, стиснутий (дзвінок 36px).
> - **🆕 Блокування фону при модалці:** виявлено — клас `.modal-open` додавався в JS, але **CSS-правила не було** → фон не блокувався в жодній модалці. Додано `body.modal-open .app-main, body.cm-zoom-open .app-main { overflow:hidden }` у `base.css` (тепер фон заблокований у ВСІХ модалках). Зум-модалка оголошення ставить окремий клас `cm-zoom-open` (щоб закриття лайтбокса поверх неї не розблокувало передчасно).
> - **🆕 Свайп-закриття (v3, перевірений патерн як у модалці статей `initModalSwipe`):** рішення на `touchstart` — `canSwipe = жест почався на СМУЖЦІ-РУЧЦІ (.cm-board-modal-bar) АБО тіло вгорі (scrollTop<=2)`. **Ручку можна тягнути ЗАВЖДИ** (навіть коли опис прокручено) → гарантоване закриття. dy>90 → collapse. Горизонталь = свайп галереї. (Версії v1/v2 з рішенням на touchmove на iOS не закривали — Вова скаржився.)
> - **🆕 Закриття при зміні вкладки:** модалка оголошення — лише для Дошки. Слухач `cstl-tab-changed` (window, вішається 1 раз через `_boardTabHookSet` + `_boardCollapseRef`) → при перемиканні на будь-яку іншу вкладку меню викликає `collapse()`.
> - **🆕 Скрол клинило (фінальний фікс — АБСОЛЮТНИЙ оверлей + spacer):** КОРІНЬ — у head/body фото в потоці шапки; при стисканні воно **звільняє місце** в body, і якщо опис після цього вміщається, браузер скидає scrollTop=0 (контент відскакує, «застрягає») — тому «в деяких» (залежить від довжини). rAF не допоміг. Відкат на head/body теж залишив баг. **Рішення (вигляд ідентичний):** фото `.cm-board-modal-photo` — **абсолютний шар** (поза потоком), скролер `.cm-board-modal-scrollarea` має **spacer(=fullH)** який ЗАВЖДИ резервує висоту скролу → нічого не скидається. subhead sticky top:72 під фото; бокс фото стискається (height) → не перекриває заголовок. (Цей варіант був відкочений, потім повернений, бо баг лишався в head/body.)
> - **🆕 Фікс «фото стискалось у лівий кут»:** прибрано `aspect-ratio:4/3` з фото (WebKit брав ширину з пропорції) → стискання знизу вгору на повну ширину.
> - **🆕 ФІНАЛ — стискання фото ПРИБРАНО (рішення після ~10 ітерацій jank):** колапс-фото-на-скролі фундаментально конфліктує з рідним скролом iOS (overlay не встигає перемальовуватись під час інерції → смикання + просвічування тексту; in-flow → ресайз скролера → застрягання). **Рішення:** фото фіксоване 4:3, під-шапка, текст — УСЕ в одному скролі (`.cm-board-modal-scrollarea`), БЕЗ JS-на-скролі, БЕЗ оверлеїв. Скрол рідний → ідеально плавний, без смикань/просвічування/застрягання. Фото гортається разом з текстом (як у більшості додатків). Футер закріплений, свайп-ручка, блок фону, закриття при зміні вкладки — лишились.
> - ⏸️ Анімоване стискання фото відкладено: повертатись лише через CSS scroll-driven animations (`animation-timeline: scroll()`), не через JS — інакше jank на iOS.
> - Файли: `style/community.css`, `style/base.css`, `src/tabs/board.js`, `sw.js`. Push/iOS-логіку не чіпано.
> - 🔬 Чекає перевірки Вови (плавний скрол без смикань).

## 🆕 ХЕНДОВЕР ДЛЯ НОВОГО ЧАТУ (Вова, 16.06)

> Деталі кожної зміни — у секціях «СЕСІЯ ВОВИ 16.06» нижче. Усе перелічене вже на проді (через `/finish`).

**Зроблено й на проді 16.06 (Дошка/Громада — мій домен):**
- **Повний редизайн модалки оголошення** (зум картки): опціональний **заголовок** (поле у формі +
  `<h3 cm-board-title>` на картці; колонка `posts.title` вже була в БД); модалку перероблено —
  будується З ДАНИХ поста (`renderAdModal`), не клон; розмір **як чат** (над таб-баром), **динамічна
  висота** за вмістом; текст **чорний**; фото **cover** із заокругленими нижніми кутами + тінь;
  модалка трохи менша; **вертикальні фото обмежені** (32vh); **галерея фото** (свайп усіх фото +
  крапки) + **повноекранний перегляд** (`openPhotoLightbox`, свайп, лічильник, contain).
- **Модалка єдина для Дошки і «Збережених»** (рендериться `#board-backdrop` у стрічці); реакції/шер/
  контакт/збереження працюють скрізь; зняти збереження можна з модалки.
- **Полірування (батчі 1-3):** закладка чату 36px; «Тапніть»→«Натисніть»; єдині відступи карток у
  «Збережених»; обрізка тексту; `alert()`→червоний тост; revoke `blob:`-фото; **видалено 240 рядків
  мертвого CSS** `.bd-comments-*`; **приглушені кольори** карток (менш насичені).
- Файли весь час: `src/tabs/board.js`, `src/tabs/community-modal.js`, `style/community.css`.

**🎉 Push-нагадування Автобусів ПІДТВЕРДЖЕНО на iPhone Вови (16.06)** — борг «живий тест push» закрито.

**🟡 ОБГОВОРЮЄТЬСЯ, не почато (рішення Вови — спершу довести поточне до ідеалу):**
- **Перебудова вкладки «Дошка» → «Дошка» (оголошення) + «Розмови»** (підрозділи: Обговорення публічні /
  Чати приватні). Чекає **назву-парасольку** для вкладки (Вова збирає варіанти з інших нейромереж;
  готовий промпт був у чаті; «Дошка» — назва предмета, не вміщає розмови). Приватні Чати = Фаза Б (логін).
- Список «нюансів Вови» — Вова ще не диктував (мій аудит-список 1-9 виконано, крім п.9 пропущено).

**⚙️ Руки Вови / бекенд (не код фронту):**
- Прибрати cron-дубль Supabase `jobid 1` (дублює `jobid 4` на `send-bus-push`) — `SELECT cron.unschedule(1)`.
- Звірити задеплоєну `send-bus-push` = репо.

**🔵 Фаза Б (авторизація) — на паузі, спроєктовано:** `docs/BOARD_FINAL_PLAN.md` + `docs/PHASE_B_EXPLAINED.md`.
Майбутнє: «учасники» чату рахувати по `auth.uid()` (зараз — унікальні імена).

**⚠️ Два власники (Вова + Рома).** Рома 15.06 додав у `/finish` **Крок 8 — підсумок для Brain** (зовнішній
контролер): наприкінці кожного `/finish` генерувати підсумок одним код-блоком з ім'ям власника сесії.
Новому чату: `git fetch origin` + Крок 0 `/startuem`.

---

## 🆕 ХЕНДОВЕР ДЛЯ НОВОГО ЧАТУ (Вова, 15.06)

**На проді (через `/finish` 15.06, гілка `vova/bus-push-reliability` → `main`):**
- **Надійність push-нагадувань Автобусів** повністю перероблено (деталі — секція «СЕСІЯ ВОВИ 15.06» нижче + `docs/PUSH_RELIABILITY.md` §6): push для майбутніх днів, чесний дзвіночок 3 стани (🔔/🔕/⚠️), анти-висячі підписки, self-heal, проміжний рейс позначено у «Збережених», **дзвіночок на банері відстеження** (toggle push без зняття трекінгу).
- Бекенд перевірено (Supabase MCP): сервер підтримує майбутні дати; схему синкнуто.

**🔴/⚙️ ЛИШИЛОСЬ (для Вови, чекає рук):**
1. 🔬 **Перевірити наживо на iPhone** — збережений на **завтра** рейс реально присилає нагадування; ⚠️ коли немає дозволу.
2. ⚙️ **Прибрати cron-дубль** у Supabase: `jobid 1` (щоп'ять хв) дублює `jobid 4` (щохв) на `send-bus-push`. `SELECT cron.unschedule(1)` через Supabase MCP — з дозволу Вови.

**🔵 ФАЗА Б (авторизація) — на паузі, СПРОЄКТОВАНО:** Google-логін, гейтинг дій. Для Автобусів задокументовано: гість = лише перегляд; трекінг/push — лише авторизований; `push_subscriptions.user_uuid` anon→`auth.uid()`. Фундамент — гілка `vova/auth-foundation`. План — `docs/BOARD_FINAL_PLAN.md` + `docs/PHASE_B_EXPLAINED.md`.

**⚠️ Два власники (Вова + Рома).** Новому чату: `git fetch origin` + перевір свіжі коміти обох перед роботою (Крок 0 `/startuem`).

---

## ✅ СЕСІЯ ВОВИ 16.06 (гілка `vova/ad-photo-gallery`) — Галерея фото в модалці + повноекранний перегляд

> CACHE_NAME → `cstl-20260616-1706`. Оголошення з кількома фото показувало лише одне.
> - **Карусель фото** у модалці: `renderAdModal` рендерить усі `p.photos` як `.cm-board-modal-gallery`
>   (горизонтальний `scroll-snap`) + крапки-індикатор (оновлюються при свайпі, слухач у `expand`).
> - **Повноекранний перегляд** (`openPhotoLightbox`): тап по фото → fullscreen зі свайпом між кадрами,
>   лічильник `i/N`, фото `object-fit:contain` (повністю), закриття ✕/фон. CSS `.cm-photo-lightbox*`.
> - Свайп-закриття модалки тепер ігнорує горизонтальний рух (щоб не конфліктувати зі свайпом галереї):
>   визначає напрям першого руху (dx vs dy).
> - Файли: `src/tabs/board.js`, `style/community.css`.

---

## ✅ СЕСІЯ ВОВИ 16.06 (гілка `vova/ad-modal-smaller`) — Модалка: менша + обмеження вертикальних фото

> CACHE_NAME → `cstl-20260616-1654`.
> - Фото `max-height: 40vh → 32vh` (вертикальні фото не перекривають опис).
> - Модалка трохи менша: `width: min(92vw,400px) → min(88vw,360px)`; `max-height` −24 → −40px (більше полів).
> - Файл: `style/community.css`.
>
> 🎉 **Push-нагадування Автобуса ДІЙШЛО на iPhone Вови** (скрін lock-screen 16.06) — закриває борг «тест push наживо».

---

## ✅ СЕСІЯ ВОВИ 16.06 (гілка `vova/ad-modal-photo-round`) — Модалка: фото cover + заокруглені нижні кути + тінь

> CACHE_NAME → `cstl-20260616-1652`. Вова: повне фото «так собі» — повернути обрізане + заокруглити низ + тінь.
> - `.cm-board-modal-photo img` назад на `max-height:40vh; object-fit:cover` (прибрано `height:auto`).
> - `.cm-board-modal-photo`: `border-bottom-*-radius:16px` + `overflow:hidden` + тінь знизу `0 6px 14px`.
> - Скрол-разом лишився. Файл: `style/community.css`.

---

## ✅ СЕСІЯ ВОВИ 16.06 (гілка `vova/ad-modal-scroll`) — Модалка: фото скролиться разом + повний розмір

> CACHE_NAME → `cstl-20260616-1645`. Вова: різкий стик фото/тексту + фото треба повного розміру.
> - **Увесь вміст — один скрол:** `overflow-y` тепер на самій `.cm-board-modal-note` (не окреме тіло),
>   фото вгорі скролиться РАЗОМ з текстом (немає зафіксованого фото → зник різкий стик).
>   `renderAdModal`: фото + `.cm-board-modal-content` (padding). Прибрано flex-розбивку.
> - **Фото повного розміру:** `.cm-board-modal-photo img` → `width:100%; height:auto` (без `object-fit:cover`
>   і `max-height` → показується ціле зображення, без обрізки).
> - Свайп-закриття звіряє `modal.scrollTop`. Файли: `board.js`, `style/community.css`.

---

## ✅ СЕСІЯ ВОВИ 16.06 (гілка `vova/card-colors-muted`) — Приглушені кольори карток

> CACHE_NAME → `cstl-20260616-0707`. Вова: фон карток занадто насичений. Пом'якшено всі 5 відтінків
> `.cm-board-note--yellow/blue/pink/white/green` до ледь помітного тону (ближче до майже-білого).
> Стосується карток на Дошці, у Збережених, прев'ю форми і модалці (спільні класи). Файл: `style/community.css`.

---

## ✅ СЕСІЯ ВОВИ 16.06 (гілка `vova/ad-modal-dynheight`) — Динамічна висота модалки оголошення

> CACHE_NAME → `cstl-20260616-0702`. Фікс: після Етапу 2 модалка тяглась на всю висоту навіть для
> коротких оголошень (бо прибита і top, і bottom).
> - **Висота тепер за вмістом**: `.cm-board-modal-note` — центрована у проміжку між шапкою і таб-баром
>   (`top: (headerBottom+tabbarTop)/2`, `transform: translate(-50%,-50%)`), `max-height` = розмір проміжку
>   (`100dvh - header - tabbar - safe - 24`). Короткий опис → модалка стискається; довгий → скрол у body.
> - Свайп-закриття повернуто під центрування (`translate(-50%, calc(-50% + Δ))`).
> - Файли: `style/community.css`, `src/tabs/board.js`.

---

## ✅ СЕСІЯ ВОВИ 16.06 (гілка `vova/ad-modal-saved`) — Зум-модалка зі «Збережених» + синхронізація (Етап 3)

> CACHE_NAME → `cstl-20260616-0648`. Завершує редизайн модалки оголошення.
> - **`#board-backdrop` тепер рендериться і у стрічці** (saved/chat), не лише на дошці → у «Збережених»
>   тап по оголошенню відкриває ТУ САМУ зум-модалку (`renderBody`). Раніше `initBoardNoteExpand` виходив рано.
> - **Зняти збереження з модалки у «Збережених»:** обробник `data-save-id` — якщо таб `saved` і знято,
>   закриває відкриту модалку (клік по `#board-backdrop.visible` → collapse) + `renderBodyOnly`.
> - Реакції/шер/контакт працюють скрізь однаково (делеговані обробники, для копій теж).
> - Файл: `src/tabs/board.js`.
>
> **Редизайн модалки оголошення — ЗАВЕРШЕНО (Етапи 1-3).** Чекає перевірки Вови наживо.

---

## ✅ СЕСІЯ ВОВИ 16.06 (гілка `vova/ad-modal-rebuild`) — Перебудова зум-модалки оголошення (Етап 2)

> CACHE_NAME → `cstl-20260616-0645`. Виправляє: фото зникало при довгому тексті, текст не чорний, низ під таб-баром.
> - **Нова `renderAdModal(p)`** (`board.js`): модалка будується З ДАНИХ поста, а не клон HTML картки.
>   Фото flush зверху (окремий flex-child `.cm-board-modal-photo`, max 40vh) → не обрізається скролом.
>   Тіло `.cm-board-modal-body` (скрол) з категорією/заголовком/повним описом/контактом/діями (ті самі хелпери → делегування працює).
> - **`expand()`** бере post з `allPosts` за `data-post-id` і вставляє `renderAdModal`; fallback на клон якщо не знайдено.
>   Свайп-закриття переписано під нову позицію (`translateX(-50%)`, скрол перевіряється на `.cm-board-modal-body`).
> - **CSS `.cm-board-modal-note`**: прибито top/bottom як чат (над таб-баром), flex-колонка, `overflow:hidden`,
>   текст чорний #1C1C1E. Прибрано старе центрування (`top:50%`, `max-height:80vh`, відʼємний margin фото).
> - Файли: `src/tabs/board.js`, `style/community.css`.
>
> **Далі (Етап 3):** відкривати модалку зі «Збережених» + синхронізація дій (backdrop у стрічці, unsave з модалки).

---

## ✅ СЕСІЯ ВОВИ 16.06 (гілка `vova/ad-title`) — Заголовок оголошення (Етап 1 редизайну модалки)

> CACHE_NAME → `cstl-20260616-0639`. Перший етап великого плану «переосмислення модалки оголошення».
> - **Опціональне поле «Заголовок»** у формі «Подати оголошення» (board): `state.title`, інпут `#bm-title`
>   над «Опис», `buildPayload` board → `title`. Колонка `posts.title` у БД вже була (міграції нема).
> - **Картка дошки** (`renderBoardCard`) і прев'ю форми показують `<h3 class="cm-board-title">` (жирний,
>   чорний #1C1C1E) якщо `p.title` є; без заголовка — як раніше (лише текст). CSS `.cm-board-title`.
> - Файли: `community-modal.js`, `board.js`, `style/community.css`, `supabase_schema.sql` (комент).
>
> **Далі (план):** Етап 2 — перебудова модалки з даних (фото flush, чорний текст, розмір як чат, фікс
> зникання фото при довгому тексті). Етап 3 — відкриття модалки зі «Збережених» + синхронізація дій.

---

## ✅ СЕСІЯ ВОВИ 15.06 (гілка `vova/polish-deadcss`) — Полірування (батч 3): чистка мертвого CSS

> CACHE_NAME → `cstl-20260615-2004`.
> - **П.6** — прибрано мертвий CSS старої модалки коментарів `.bd-comments-*` (240 рядків, ніде в JS/HTML не вживається). `community.css` 2751→2511 рядків, дужки збалансовані 446/446.
> Лишилось зі списку: п.9 (опц. перейм. `comment`→`message`). Далі — нюанси Вови.

---

## ✅ СЕСІЯ ВОВИ 15.06 (гілка `vova/polish-toast-blob`) — Полірування (батч 2)

> CACHE_NAME → `cstl-20260615-2002`.
> - **П.5** — помилка надсилання повідомлення в Обговореннях: `alert()` → червоний тост (`showToast(..., 'error')`). `board.js`.
> - **П.8** — `community-modal.js` `close()`: звільняємо `blob:`-посилання незавантажених фото (фікс дрібного витоку пам'яті).
> - **П.7 скасовано** — `.bd-chat-photo` виявився живим (вживається у прев'ю форми `community-modal.js:441`).
> Лишилось: п.6 (мертвий CSS `.bd-comments-*`), п.9 (перейм. `comment`→`message`). Потім — нюанси Вови.

---

## ✅ СЕСІЯ ВОВИ 15.06 (гілка `vova/polish-saved-chat`) — Полірування (батч 1 зі списку нюансів)

> «Роби свої» Вови. Перший батч із плану «довести до ідеалу» (план-файл сесії). CACHE_NAME → `cstl-20260615-1958`.

- **П.1** — закладка 🔖 у підвалі картки Обговорення `30px → 36px` (легше влучати). `community.css`.
- **П.2** — порожні «Збережені»: «Тапніть» → «Натисніть». `board.js`.
- **П.3** — «Збережені»: картки-обговорення (`.bd-stream .bd-card--chat`) отримали ті самі відступи `13px`, що й картки-оголошення (вирівняно).
- **П.4** — текст оголошення у «Збережених» обрізається до 3 рядків (`-webkit-line-clamp: 3`).

Лишилось зі списку (наступні батчі): п.5 `alert`→тост + п.9 перейм. `comment`-назв; п.6-7 чистка мертвого CSS; п.8 `blob` revoke. Потім — нюанси Вови.

---

## ✅ СЕСІЯ ВОВИ 15.06 (гілка `vova/saved-cards-smaller`) — «Збережені»: компактніші картки

> «Зроби трошки меншими» Вови. CACHE_NAME → `cstl-20260615-1130`. Лише CSS, scope `.bd-stream`:
> фото оголошень `4/3 → 16/10` (нижче), padding `16→13`, текст `→14px`. На Дошці (корок) — без змін.
> Файл: `style/community.css`.

---

## ✅ СЕСІЯ ВОВИ 15.06 (гілка `vova/chat-tags-order`) — Обговорення: теги над лічильником повідомлень

> «Заміни місцями» Вови. CACHE_NAME → `cstl-20260615-1120`. У `renderChatCard` обмінено рядки:
> тепер `#теги` → потім `💬 N повідомлень` (було навпаки). Файл: `src/tabs/board.js`.

---

## ↩️ ВІДКАТ 15.06 (гілка `vova/revert-participants`) — повернуто учасників у підвал

> «Поверни назад» Вови. `git revert` PR #96 → картка Обговорення повернулась до стану #95:
> **учасники в підвалі зліва** (не над останнім повідомленням). CACHE_NAME → `cstl-20260615-1117`.
> Тобто актуальний вигляд — як описано в секції «повідомлення↑, учасники↓, іконки» нижче.

---

## ✅ СЕСІЯ ВОВИ 15.06 (гілка `vova/chat-card-counts`) — Обговорення: повідомлення↑, учасники↓, іконки

> «Роби» Вови. Гілка `vova/...` → `/finish`. CACHE_NAME → `cstl-20260615-1025`.

- **Лічильник повідомлень** перенесено під заголовок теми (`.bd-chat-msgcount`, де раніше був автор).
- **Учасники** (унікальні автори повідомлень, `new Set(comments.author)`) — у підвалі зліва
  замість лічильника повідомлень. Нова функція `partWord()` (відмінювання «учасник/-и/-ів»).
  ⚠️ Анонімні «Житель» зливаються в одного — число приблизне.
- **Іконки:** тема `💬 → 💭`; повідомлення `💬`; учасники `👥`.
- Файли: `src/tabs/board.js`, `style/community.css`.

---

## ✅ СЕСІЯ ВОВИ 15.06 (гілка `vova/chat-card-footer`) — Обговорення: перебудова підвалу картки

> «Роби» Вови. Гілка `vova/...` → `/finish`. CACHE_NAME → `cstl-20260615-1003`.

Перебудовано картку-прев'ю теми обговорення (`renderChatCard` + CSS):
- **Блок «автор + дата»** перенесено з-під заголовка **у підвал біля 🔖**: автор зверху,
  дата публікації обговорення (`formatTime(postTime(p))`) знизу, вирівняно праворуч (`.bd-chat-by`
  → flex-колонка). `.bd-chat-count` отримав `margin-right:auto` щоб штовхати групу праворуч.
- **Час останньої активності** (час останнього повідомлення) перенесено з підвалу **на рядок
  останнього повідомлення справа**: `.bd-chat-last` → flex (текст `.bd-chat-last-msg` зліва з
  обрізкою, `.bd-chat-last-time` справа). Прибрано `.bd-chat-foot-time`.
- Файли: `src/tabs/board.js`, `style/community.css`.

---

## ✅ СЕСІЯ ВОВИ 15.06 (гілка `vova/board-card-shadow`) — Дошка/Обговорення: трохи більше тіні карткам

> «Роби» Вови. Гілка `vova/...` → `/finish`. CACHE_NAME → `cstl-20260615-0939`.

- **Підсилено тінь карток (CSS):**
  - Дошка `.cm-board-note`: opacity шарів `0.08/0.10/0.07 → 0.10/0.15/0.11`, радіуси трохи більші.
  - Обговорення `.bd-card--chat`: `0 2px 8px rgba(0,0,0,0.04)` → 2 шари `0 2px 8px/0.10 + 0 6px 16px/0.08`.
- Файл: `style/community.css`.

---

## ✅ СЕСІЯ ВОВИ 15.06 (гілка `vova/board-bottom-gap`) — Дошка: менший розрив від останньої картки до таб-бару

> «Роби» Вови (скрін). Гілка `vova/...` → `/finish`. CACHE_NAME → `cstl-20260615-0921`.

- **Зменшено порожній простір знизу** на Дошці/Обговореннях/Збережених. Причина: `.app-main`
  (головний скролер) вже додає `--tabbar-h` + safe-area знизу, а `.board-content` (72px+safe) і
  `.bd-stream` (100px) дублювали це зверху → остання картка зависала високо.
- **Фікс (CSS):** `.board-content` padding-bottom `72px+safe → 24px`; `.bd-stream` padding-bottom
  `100px → 8px`. Лишився невеликий відступ під плаваючу кнопку «Подати оголошення». Файл: `style/community.css`.

---

## ✅ СЕСІЯ ВОВИ 15.06 (гілка `vova/board-all-scroll`) — Дошка: плавне повернення стрічки підкатегорій при «ВСІ»

> «Роби» Вови. Гілка `vova/...` → `/finish`. CACHE_NAME → `cstl-20260615-0909`.

- **Тап на «ВСІ» плавно повертає стрічку підкатегорій на початок.** В обробнику кліку по чіпах
  (`renderAll`): для `data-bd-cat="all"` після перемалювання — `.bd-categories.scrollTo({left:0,
  behavior:'smooth'})`. `renderAll` спершу відновлює поточну позицію скролу → анімація стартує
  звідти й гладко доходить до 0. Файл: `src/tabs/board.js`.

---

## ✅ СЕСІЯ ВОВИ 15.06 (гілки `vova/board-all-chip` → `vova/board-all-fix`) — Дошка: чіп «ВСІ» закріплено зліва

> «Роби» Вови. Гілка `vova/...` → `/finish`. CACHE_NAME → `cstl-20260615-0658`.
>
> **+ Роздільник «|»** (рішення Вови): між «ВСІ» і контейнером додано вертикальну риску
> `.bd-cat-divider` (1×20px, `rgba(0,0,0,0.18)`, відступи 8px). Чіпи зникають за лівим краєм
> контейнера праворуч від риски — виглядає ніби заїжджають за роздільник.

- **Чіп «ВСІ» закріплено зліва, решта підкатегорій скролиться окремо.**
  - **v1 (PR #89, sticky)** — `position: sticky` на чіпі `all`. Мінус: рухомі чіпи лишались частково
    видимі під «ВСІ» (визирали у проміжку, без чіткого розділення).
  - **v2 (фінал)** — «ВСІ» винесено **за межі** контейнера скролу: новий wrapper `.bd-cat-wrap`
    (flex, gap 8px) → закріплений чіп `all` + окремий `.bd-categories` (`flex:1; overflow-x:auto`).
    Тепер рухомі чіпи обрізаються лівим краєм контейнера → **ховаються повністю**, відступ 8px дає
    чітке розділення. `board.js` (`chipHtml`, `BOARD_CATEGORIES[0]` окремо + `.slice(1)`),
    `style/community.css` (`.bd-cat-wrap`, `.bd-categories flex:1`, прибрано sticky-правила).
- Файли: `src/tabs/board.js`, `style/community.css`.

---

## ✅ СЕСІЯ ВОВИ 15.06 (гілка `vova/bus-sync-ui`) — Синхронізація стану дзвіночка/закладки між UI

> «Роби» Вови після скріну. Гілка `vova/...` (НЕ автомерджиться — через `/finish`).
> CACHE_NAME → `cstl-20260615-0629`. `node build.js` exit 0. **Синхронізація — на проді через PR #87 (v1782).**
> Доопрацювання UI (нижче) — ще НЕ влито, чекає тесту Вови.

**Частина 1 — синхронізація (на проді, PR #87):** два баги (логіка даних коректна — бракувало перемалювання):
- **🔴 Дзвіночок модалки «Збережені» ≠ дзвіночок банера.** `toggleRouteReminders` слав
  `cstl-bus-track-changed`, але глобальний слухач (`initSavedRoutesHeader`) оновлював лише
  бейдж+рядки модалки, не банер. **Фікс:** у слухач додано `updateBannerBell()`.
- **🔴 Зняття відстеження в модалці ≠ закладка в розкладі.** **Фікс:** додано `renderRouteList()` у `unsaveRoute`.

**Частина 2 — доопрацювання UI (гілка, ще не на проді):**
- **«СЬОГОДНІ» великими над назвою рейсу** у модалці «Збережені» (`srRowHtml`): день винесено
  окремим рядком зверху (`.sr-row-day`, uppercase, сіре) замість `· сьогодні` знизу.
- **Час проміжного рейсу — збоку біля назви:** заголовок став `ВІД - ДО | HH:MM → HH:MM`;
  повний маршрут-батько 📍 — окремим підрядком знизу. Звичайний рейс — без змін (час окремим рядком).
- **Бордова закладка проміжних рейсів у розкладі завжди.** Раніше бордова (`.tracked-seg`) була
  лише коли фільтр знятий; з активним фільтром — чорна. Тепер: відстежуваний **сегментний** рейс →
  завжди бордова `#722F37`, повний маршрут → чорна. Нові `isTrackedNow`/`trackBtnCls` у `renderRouteList`.

Файли: `src/tabs/buses.js`, `style/buses.css`. Push/iOS-логіку не чіпано.

---

## ✅ СЕСІЯ ВОВИ 15.06 (гілка `vova/bus-push-reliability`) — Аудит сповіщень Автобусів + фікси

> «Роби» Вови після аудиту логіки сповіщень. Гілка `vova/...` (НЕ автомерджиться — через `/finish`).
> НЕ влито в `main`, ще НЕ на сайті. CACHE_NAME → `cstl-20260615-0538`. `node build.js` exit 0.

**Аудит (read-only):** простежено весь життєвий цикл трекінг↔push у `buses.js` + бекенд
`send-bus-push`. Синхронізація UI Роминої модалки «Збережені» з трекінгом — **коректна**
(усе через `saveTrackedRoute`→`cstl-bus-track-changed`). Знайдено вади → пофікшено:

- **Крок 0 (Supabase MCP):** сервер **підтримує майбутні дати** (видаляє лише `track_date<today`,
  відбирає `==today`). Жива `push_subscriptions` має всі прапорці → синк `scripts/supabase_push_schema.sql`.
  ⚠️ cron-дубль: `jobid 4` (щохв) + `jobid 1` (щоп'ять хв) б'ють у `send-bus-push` — прибрати jobid 1 (окремо, з дозволу Вови).
- **🔴 Крок 2 — push для майбутніх днів:** послаблено guard `subscribeToPush`/`unsubscribeFromPush`
  (`trackDate!==today` → `trackDate<today`). Тепер збережений на завтра рейс реально підписується,
  сервер вистрелить у його день.
- **🟡 Крок 1 — висячі підписки:** `checkSingleTracked` тепер `unsubscribeFromPush` перед
  `removeTrackedEntry` у гілках `past` і «час висадки минув».
- **🆕 Крок A — проміжний рейс у «Збережених»:** денормалізація `isSeg/fullTitle/fullTimeStr`;
  `srRowHtml` малює заголовок `ВІД - ДО` + червоний підрядок `.bs-route-full` з 📍 і повним
  маршрутом — як у картці «Розкладу». Старі записи — backward-compat.
- **🟡 Крок 3 — чесний дзвіночок (3 стани):** 🔔 on / 🔕 off / ⚠️ warn (notify=on, але push
  недоступний). Тап по ⚠️ → запит дозволу/пояснення (не вимкнення). Тост при збереженні якщо push заблокований.
- **Крок 4 — self-heal:** `selfHealPushSubscriptions()` в `initBuses` тихо перепідписує втрачені
  (лише коли дозвіл уже надано).
- **🔔 Дзвіночок на банері (нова вимога Вови 15.06):** на банер відстеження справа додано дзвіночок
  (той самий стиль `.sr-bell`, 3 стани). Тап = тільки push-нагадування (`toggleRouteReminders` /
  ⚠️→`requestPushForSavedRoute`); рейс ЛИШАЄТЬСЯ відстежуваним (скасувати трекінг — окремо, закладкою).
  Верхній напис змінюється `СПОВІЩЕННЯ ПРО РЕЙС АКТИВОВАНО ↔ ВИМКНЕНО`. `showBanner` отримав 4-й
  параметр `entry`; `_bannerEntry` + `updateBannerBell()`. Синк із модаллю «Збережені» через спільний
  `notify`. `style/buses.css`: `.btb-bell`. (Скасувало попереднє рішення «банер лишити інформаційним».)

**Авторизація (Фаза Б):** задокументовано вплив у плані — гейтинг трекінгу/push (гість = лише
перегляд) і прив'язку `push_subscriptions.user_uuid` anon→`auth.uid()` віднесено у Фазу Б (рішення Вови).

**Чекає:** перевірка наживо на iPhone (особливо рейс на завтра) → `/finish`.

---

## 🆕 ХЕНДОВЕР ДЛЯ НОВОГО ЧАТУ (Вова, 14.06 пізній вечір)

> Коротка карта стану. Деталі — у секціях нижче й у відповідних доках.

**На проді зараз (усе працює):**
- Уся Дошка-сесія Вови 14.06: заголовки тем UPPERCASE, відступи карток, **чат-UX** (розумний
  автоскрол, пігулка «N нових», роздільник «Нові», лічильник відповідей, фокус-keep клавіатури),
  тіні країв модалки, **фільтр матюків/образ/спаму** (укр+рос+трансліт, варіанти «пєдік/дибіл»,
  червоний тост поверх модалки), **автор теми + кнопка «Зберегти»** на картках обговорень.
- Сесія Роми 14.06: **«Збережені рейси»** — іконка 🔖 у хедері + слайд-модалка керування (Автобуси).

**🔴 АКТИВНИЙ ПРІОРИТЕТ (Рома задокументував, чекає на Вову):** надійність push-нагадувань про
рейси — дзвіночок обіцяє нагадування, яких не буде (push лише для сьогоднішніх; стан кнопки ≠
реальний стан підписки). **План:** `docs/PUSH_RELIABILITY.md` + 🔴 у `BACKLOG.md`.

**🔵 ФАЗА Б (авторизація + приватний чат) — СПРОЄКТОВАНО, НА ПАУЗІ (рішення Вови):**
- Спершу довести до ладу вкладки/підкатегорії, ПОТІМ — авторизація + вся структура.
- **Фундамент УЖЕ закодовано** на гілці **`vova/auth-foundation`** (НЕ влитий у `main`, НЕ на сайті):
  `src/core/auth.js` (`initAuth/currentUserId/requireAuth/signInWithGoogle/signOut/getProfile`) +
  опції клієнта Supabase (`persistSession/detectSessionInUrl`). Коли повернемось — стартувати з цієї гілки.
- **Повний план + пояснення:** `docs/BOARD_FINAL_PLAN.md` (секція «Фаза Б», SQL/RLS) +
  `docs/PHASE_B_EXPLAINED.md` (простими словами + технічно: чому авторизація саме в Дошці).
- **Уточнення гейтингу (Вова 14.06):** читати все (вкл. стрічку «Обговорення») — вільно; писати/
  реагувати/подавати/**зберігати**/чат — лише авторизований. Реєстрація = м'яка (вхід при першій дії).
- **Стадія 0 (руки Вови, ще не зроблено):** Google Cloud OAuth client + Supabase Google provider.

**⚠️ Два власники активні 14.06 (Вова + Рома/OWLs68).** Новому чату: `git fetch origin` +
перевір свіжі коміти обох перед роботою (правило Кроку 0 `/startuem`).

---

## 📌 ДЛЯ ВОВИ: системна вада push-нагадувань — задокументовано (14.06.2026, Рома)

> Після фічі «Збережені рейси» знайдено: дзвіночок 🔔 обіцяє нагадування яких не буде
> (push лише для сьогоднішніх рейсів; стан кнопки ≠ реальний стан підписки).
> **Повний аналіз + покроковий план для Вови:** `docs/PUSH_RELIABILITY.md`.
> Посилання додано в `ROADMAP.md` (Фаза 4.0) і `BACKLOG.md` (🔴 Роби зараз).
> Бекенд (Supabase) я НЕ перевіряв — позначено в документі як «треба дослідити».

---

## ✅ СЕСІЯ РОМИ 14.06 (вечір, гілка `roma/bus-saved-routes`) — Збережені рейси: іконка хедера + слайд-модалка

> Збереження рейсів було слабке (тільки закладка на картці). Додано повноцінне керування.

- **Глобальна іконка 🔖 у хедері** (біля погоди, на всіх вкладках) з лічильником збережених. `index.html` + `initSavedRoutesHeader()` у `buses.js` (виклик з `app.js`).
- **Слайд-модалка зверху вниз** (`.sr-modal`): список збережених рейсів. Свайп вгору / ✕ / фон — закрити.
- **Кожен рядок:** назва+час+день, 🔔 дзвіночок (нагадування on/off) + 🔖 закладка (зняти збереження).
- **Роздільне нагадування:** новий прапорець `notify` у записі трекінгу. Зберіг рейс → нагадування авто-on. Дзвіночок off → рейс лишається збереженим (піднімається в hero), але без push/банерів (`checkSingleTracked` пропускає `notify===false`, `unsubscribeFromPush`).
- **Денормалізація** у запис трекінгу: `title`/`depTime`/`arrTime` — щоб модалка малювалась на будь-якій вкладці без даних розкладу.
- `buses.css`: `.saved-routes-btn`, `.sr-*`. `CACHE_NAME` → `cstl-20260614-1755`. `node build.js` exit 0.
- ⚠️ Зона «Автобуси V1.0» — змінено з дозволу Роми, push/iOS-фікси не чіпані (тільки доданий `notify`-пропуск). **Потребує перевірки наживо на iPhone.**

---

## ✅ СЕСІЯ ВОВИ 14.06 (день, гілка `vova/chat-author-save`) — Автор теми + кнопка «зберегти» на картках обговорень

> «Роби» Вови. `src/tabs/board.js` `renderChatCard` + `style/community.css`.
- **Автор теми** показано на картці обговорення: рядок `Автор: <ім'я>` (`.bd-chat-by`, `p.author || 'Житель'`).
- **Кнопка «зберегти»** (`saveBtnHtml(p)`) додана в підвал картки → таб «Збережені». Перевикористано наявний
  делегований обробник `[data-save-id]` (`toggleSaved`/`isSaved`) — додавання/видалення працює автоматично,
  у табі «Збережені» картка зникає при знятті. `.bd-chat-foot .bd-bookmark` 30×30.
- **Фікс конфлікту кліку:** обробник `[data-chat-open]` тепер ігнорує кліки по `[data-save-id]`/`[data-share-board]`
  (інакше тап по закладці відкривав би чат). `CACHE_NAME` → `cstl-20260614-1423`. build exit 0.

---

## ✅ СЕСІЯ ВОВИ 14.06 (день, гілка `vova/filter-words`) — Розширення й виправлення фільтра слів

> «Роби» Вови (рівень «матюки + образи, але без хибних блоків легальних слів» + рос + трансліт).
> Доповнено варіантами написання: «пєдік/педік» (EXACT) і «дибіл/дибил» (STEMS) — на прохання Вови.

**ЗРОБЛЕНО (`src/core/utils.js` → `containsProfanity`):**
- **Виправлено хибні блоки легальних слів:** прибрано надто широкі корені `'ху'` (блокував
  художник/худий/хустка/хутро/хуліган), `'манда'` (мандарин/мандат), `'педик'` (педикюр) →
  звужено до `хуй/хує/хуя/хуї/хуйл/хуєс`; `манда`/`педик` → у EXACT (повним словом).
- **Додано пропущені корені** (гол. дірка зі скріну): `їб` + наїб/заїб/виїб/доїб/уїб + `єб/еб/йоб`.
- **Образи:** ідіот, кретин, придур, імбецил, дебіл + EXACT дурак/дурень/дурний/тупий/тупиця/козел/даун/бовдур/лох/гнида.
- **Російські** форми + новий **`PROFANITY_LATIN`** (трансліт латиницею: blyat/suka/pidor/mudak…) —
  окремий прохід БЕЗ гомогліф-заміни.
- Перевірено node-тестом: 15 поганих блокуються, 18 легальних (Художник, мандарин, педикюр, тупіт,
  корабля, шлюб, лохина, гнідий, ідіома…) проходять. `CACHE_NAME` → `cstl-20260614-1356`. build exit 0.
- ⚠️ Фільтр клієнтський (бувабельний). Серверний тригер — Фаза Б. Списки легко доповнювати.

---

## ✅ СЕСІЯ ВОВИ 14.06 (ранок, гілка `vova/profanity-filter`) — Фільтр матюків / образ / спаму

> «Роби» Вови. Окрема свіжа гілка від `main` (виправлено процес — більше не плодимо конфлікти rebase).

**ЗРОБЛЕНО (код):** клієнтський фільтр, що блокує образливий контент і флуд **до публікації**
(чат-коментарі зʼявляються миттєво, тому це головний захист; пости й так ідуть у `pending`).
- `src/core/utils.js` — нові експорти:
  - `containsProfanity(text)` — нормалізація (lowercase + латинські гомогліфи→кирилиця + схлопування
    повторів) → перевірка по словах (сильні стеми через `startsWith` + ризиковані короткі повним словом)
    + «squashed» прохід для рознесеного «х у й». Списки `PROFANITY_STEMS/EXACT/SQUASH` — легко доповнювати.
  - `looksLikeSpam(text)` — консервативні евристики: 1 символ · символ ×≥6 · ≥12 літер без голосних.
- `src/tabs/board.js` — імпорт + антифлуд (`cstl-msg-rate-v1`): дубль попереднього + >5 повідомлень/15с.
  У submit коментаря 4 перевірки з тостами ДО optimistic-додавання (`recordSentMsg` після проходження).
- `src/tabs/community-modal.js` — `containsProfanity` на text/contact/author/tagsRaw у submit поста.
- `CACHE_NAME` → `cstl-20260614-0738`. `node build.js` exit 0.
- **Виразне сповіщення при блоці (гілка `vova/filter-notice`):** `showToast(msg, dur, 'error')` —
  червоний тост (`.toast--error`, фон `var(--red)`, перенос рядків). **Гол. фікс:** тост мав
  `z-index:999` < чат-модалки (1100) → був НЕВИДИМИЙ у чаті; підняв `.toast` до `z-index:2000`.
  Текст блоку: «🚫 Повідомлення містить заборонені слова і не надіслане». `CACHE_NAME` → `cstl-20260614-0759`.
- ⚠️ **Обмеження:** фільтр клієнтський (бувабельний). Справжній захист — серверний тригер на `comments`
  у Supabase, додамо разом з Фазою Б (логін). Наявні старі образи прибираються через `/admin.html`.
- **Клавіатура не ховається після надсилання (гілка `vova/chat-keep-keyboard`):** у submit чату після
  `rerenderCommentsBlock` додано `input.focus()`; кнопка «↑» (`.bd-chat-modal-send`) має
  `pointerdown→preventDefault` щоб не забирати фокус з поля (iOS інакше ховає клавіатуру).
  `CACHE_NAME` → `cstl-20260614-0808`.

---

## ✅ СЕСІЯ ВОВИ 13.06 (пізній вечір, гілка `vova/board-chat-title`) — Заголовок теми чату + дизайн логіки чату (Фаза Б)

> «Роби» Вови. Гілка `vova/...` (НЕ автомерджиться — через `/finish` у `main`).

**ЗРОБЛЕНО (код):**
- **Заголовок теми у картках «Обговорення» → ВЕЛИКІ + чорний + жирний.** Нове CSS-правило
  `.bd-card--chat .bd-chat-text { text-transform:uppercase; color:#1C1C1E; font-weight:700 }`
  у `style/community.css` (після базового `.bd-chat-text`). Націлено лише на `.bd-card--chat`,
  щоб НЕ зачепити лайв-прев'ю теми в модалці «Новий пост» (`community-modal.js:440` теж юзає
  `.bd-chat-text`). `CACHE_NAME` → `cstl-20260613-2103`. `node build.js` exit 0.
- **Картки Дошки опущено нижче від блоку контролів** (тиснулись до табів/пошуку/чіпів).
  `.bd-body` padding-top 110→124 (таби ЗБЕРЕЖЕНІ/ОБГОВОРЕННЯ) і 148→162 (таб ДОШКА з чіпами)
  у `style/community.css`. `CACHE_NAME` → `cstl-20260613-2119`. На сайті v1735 (заголовок) уже видно.
- **Чат-UX — 4 швидкі виграші (з промпта ChatGPT, без логіну)** у `src/tabs/board.js` + `style/community.css`:
  1) **розумний автоскрол** — `onCommentRealtimeEvent` більше не збиває позицію коли читаєш старі
     (фіксує `near`/`prevTop` перед перемальовуванням; донизу лише якщо був унизу);
  2) **floating-пігулка «N нових повідомлень»** (`.bd-chat-newpill`) → тап = скрол донизу; ховається
     коли долистав до низу (слухач `scroll` на body);
  3) **роздільник «Нові повідомлення»** (`.bd-chat-divider`) + `cstl-chat-seen-v1` (час перегляду
     per-device) — при відкритті скрол до роздільника (`scrollChatToNewOrBottom`), на close — `setChatSeen`;
  4) **«N відповідей»** у шапці модалки (`#bd-chat-reply-count`, `replyWord()`), оновлюється realtime.
  Власне повідомлення завжди скролить донизу + ховає пігулку. `CACHE_NAME` → `cstl-20260613-2147`.
  iOS-фікси клавіатури (visualViewport/resize/debounce) НЕ чіпані.
- **Чат-модалка — тіні країв + заголовок uppercase** (`style/community.css`): шапка `.bd-chat-modal-head`
  і поле вводу `.bd-chat-modal-form` — тінь на внутрішній край що межує з чатом посилено
  `0.05 → 0.12` (`0 ±4px 12px`); заголовок теми `.bd-chat-modal-title` → `text-transform:uppercase`.
  `CACHE_NAME` → `cstl-20260614-0630`.

**СПРОЕКТОВАНО (без коду — Фаза Б, логіка взаємодії в чатах):** рішення Вови — повну логіку
відкласти у Фазу Б (Google-логін, бо без `auth.uid()` модерація користувачем ненадійна). Дизайн
4 дій над повідомленням (через тап/лонг-прес на бульбашці → action-sheet): **Видалити власне**
(RLS `auth.uid()=sender_uid OR is_admin()`, потрібна колонка `comments.sender_uid`), **Поскаржитись**
(нова таблиця `comment_reports` + вкладка в адмінці), **Копіювати/Відповісти** (`reply_to`),
**Фільтр матюків** (`containsProfanity()` у `utils.js`, блок при надсиланні — єдине що відсікло б
образливий контент зі скріну ще до публікації). Деталі — `docs/BOARD_FINAL_PLAN.md`.

---

## ✅ СЕСІЯ ВОВИ 13.06 (вечір) — Дошка: чистка + Обговорення-чат + план Фази Б

> Гілка `claude/startup-uem-9tfuxg`. Усе залито в `main` через auto-merge і **на сайті**.
> **Фінальний CACHE_NAME: `cstl-20260613-2010`.** Нижче — повний перелік, деталі у секціях далі.

**ЗРОБЛЕНО (усе на проді):**
1. **Фаза А Дошки** (А.1-А.3): видалено тип 🎉 Вітання + категорію ❤️ Подяка повністю; згруповано
   чіпи у 6 (Куплю/Продам, Знайдено/Загубилось — здвоєні через `match`); кнопки контакту.
2. **Viber прибрано** (`viber://` до незнайомого номера на iOS дає «адреса недійсна» — обмеження Viber).
3. **Telegram прибрано** (Фаза Б Етап 0) — на картці лишився тільки 📞 **Дзвінок** (`tel:`).
   Рішення Вови: спонукати дзвонити АБО писати в приватному чаті додатка (чат — Фаза Б).
4. **Обговорення → повноекранна чат-модалка** (Фаза А2): картка стала прев'ю теми; тап → модалка-чат
   з бульбашками (мої справа бордові, чужі зліва з аватаром), realtime через `comments`. Inline-
   коментарі прибрано. Деталі — секція нижче.
5. **Серія фіксів чат-модалки при клавіатурі** (важливо для iOS PWA — фінальний робочий стан):
   - геометрія: панель `top: header+6 / bottom: tabbar+10`, `z-index:1100` (вище таб-бару 1000),
     backdrop `1050`; повідомлення `margin-top:auto` (притиснуті донизу, без пустки);
   - **клавіатура:** слухаємо `window.resize` **І** `visualViewport` (на iOS PWA при клавіатурі
     resize-иться саме ВІКНО, а vv-події часто не надходять — це була причина що обробник не
     спрацьовував). Коли клавіатура відкрита — модалка займає **рівно видиму область**:
     `top = vv.offsetTop`, `height = vv.height-4`, `bottom:auto` → верх під статус-баром і фіксується
     (не ховається), низ (поле вводу) впритул над клавіатурою, висота динамічно стискається;
   - **плавність:** CSS-перехід `top/height/bottom 0.28s ease`;
   - **проти смикання:** `debounce` 80мс — злива iOS-подій збирається в один виклик → одна плавна
     анімація (без «гонитви» за клавіатурою).
   - Файли: `src/tabs/board.js` (`openChatModal`/`closeChatModal`/`chatMessagesHtml`), `style/community.css`.
6. **Запущено монітор активності Роми вручну** (`.claude/owner-watch.sh` через Monitor) — бо `/startuem`
   цієї сесії був СТАРОЇ версії (до Кроку 0, який Рома додав сьогодні). У новій сесії авто-запуститься сам.

**СПРОЕКТОВАНО (без коду) — Фаза Б (авторизація + приватний чат):** повний оновлений план у
`docs/BOARD_FINAL_PLAN.md`. Рішення Вови: **логін лише для дій** (перегляд+дзвінок вільні);
**тільки Google (Gmail)**; гість = read-only; реєстрація = **Ім'я + Пошта + дата народження**;
при вході екран «Приєднайтесь» → Увійти / **Пропустити** (read-only); гейтинг 4 дій (подати
оголошення · чат · реакції+коментарі · автобус+пуш). **Будуємо на етапі розробки сторінки «Громада».**

**НАСТУПНЕ:** доробити Дошку (за бажанням Вови) → детальна розробка сторінки **«Громада»** (там і
авторизація + профіль + приватний чат покупець↔автор + пуші). Зворотні рейси автобусів — чекають список Вови.

---

## ✅ ЗРОБЛЕНО (13.06.2026, гілки `roma/modal-swipe-fixes` + `roma/buses-empty-board`, Рома) — Свайп модалок + порожнє табло Автобусів

> Деплой однією збіркою (CACHE_NAME `cstl-20260613-1849`). Дві гілки об'єднано.

**Свайп вниз у двох модалках** (аудит показав: свайп був тільки в Новині й Чаті):
- **«Новий пост»** (`community-modal.js`): свайп вниз з ручки/верху панелі → `close()`. Поріг 90px, інакше плавне повернення. Тільки JS (інлайн `translateY`), CSS не чіпано.
- **Зум-стікер дошки** (`board.js` `expand()`): свайп вниз (коли прокручено до верху) → `collapse()`, зберігаючи центрування `translate(-50%,-50%)`. Поріг 90px.

**Порожнє табло Автобусів** (бордове hero-табло на місці ЗАВЖДИ, повідомлення всередині):
- `buses.js`: нові `emptyHeroMessage()` (текст за контекстом: фільтр / сьогодні / інший день) + `buildEmptyHeroCard(msg)`.
- `renderSmartRow`: при `!routes.length` рендерить порожнє табло, а не очищає (`innerHTML=''`). Прибрано 4 дублі повідомлення зі списку.
- `buses.css`: `.bhv4--empty` + центрування. ⚠️ Зона «Автобуси V1.0» — змінено з дозволу Роми, iOS-фікси трекінгу не чіпані.

---

## ✅ ЗРОБЛЕНО У ЦІЙ СЕСІЇ (13.06.2026, гілка `claude/startup-uem-vf1bic`, Рома) — Робочий потік для ДВОХ власників

> Контекст: над проектом працюють Вова **і Рома**, кожен зі свого Claude. Треба автоматично розрізняти хто на зв'язку, працювати на іменованій гілці, бачити в історії хто що робив, і попереджати про конфлікти рішень.

- **Автовизначення людини** за поштою сесії (`userEmail`): `roma.haranin.ru1@gmail.com` → Рома; інша → Вова (метод виключення, без запитань).
- **Підпис комітів:** `git config user.name/email` під людину на старті сесії → `git log` показує автора (раніше всі коміти були безликий `Claude <noreply@anthropic.com>`).
- **Іменовані гілки:** `roma/<тема>` / `vova/<тема>` → `/finish` (PR → squash) → `main`.
- **Перевірка конфліктів** на старті: `git fetch origin` + `git log --all` + кінець `SESSION_STATE`/`BACKLOG` → попередити якщо рішення власників суперечать.
- **Куди вписано:** `NEW_SESSION_PROMPT.md` (Крок 0 + формат відповіді), `.claude/commands/startuem.md` (Крок 0), `CSTL NEWS VOVA/CLAUDE.md` (секція «Двоє власників»). Чисто документація (`.md`/`.claude`) → `CACHE_NAME` НЕ чіпали.
- **Також:** оновлено таблицю «Поточний стан» (гілка `...vf1bic`, `CACHE_NAME` `cstl-20260613-1532`, статус Дошки).
- 🔵 **Ця сесія** лишається на авто-гілці `claude/startup-uem-vf1bic` (вже стартувала). Іменовані `roma/`/`vova/` діятимуть з наступного `/startuem`.

---

## ✅ ЗРОБЛЕНО У ПОПЕРЕДНІЙ СЕСІЇ (13.06.2026, гілка `claude/startup-uem-9tfuxg`) — Фаза А Дошки

**Крок 1 — видалено тип 🎉 Вітання (`greeting`) і категорію ❤️ Подяка ПОВНІСТЮ:**
- `community-modal.js`: прибрано таб `greeting`, `GREETING_PRESETS`, `renderGreetingFields/Preview`, гілки greeting у `renderDynamic/renderPreview/buildPayload`, поля стану `title`+`greetingIdx`, перевірку greeting у submit. Прибрано `'подяка'` з `BOARD_CATEGORIES`. Fallback `BOARD_CATEGORIES[7]` → `.find(c => c.id === 'оголошення')` (2 місця).
- `community-blocks.js`: прибрано `greeting` з `BOARD_MINI_TYPES`, гілку greeting у `renderMiniCard`, `'подяка'` з `CATEGORY_EMOJI`, мертві поля `title`/`cover_emoji`/`cover_gradient` з маппінгу міні-карток.
- `data/community-board.json`: прибрано пост-подяку (id 4) + 4 greeting-пости (201-204). 17→12 постів. Оновлено `_meta`.
- Supabase `posts`: 2 реальні greeting-пости (id 11, 15) сховано через `status='rejected'` (НЕ видалено — реальний контент мешканців, оборотно).
- `node build.js` пройшов з кодом 0, `check-imports` чисто. `bundle.js` перезібрано.
- 🔵 Залишок (не блокер): `admin.html:676` має лейбл `🎉 вітання` + CHECK-constraint `type` досі дозволяє `greeting` — лишив, бо старі rejected-пости мають коректно показуватись в адмінці.

**Крок 2 — згруповано чіпи фільтра у 6 (`board.js`):**
- `BOARD_CATEGORIES` → 6 чіпів з полем `match`: Всі · 🛒 Куплю/Продам · 🔍 Шукаю · 🔧 Послуги · 🎁 Знайдено/Загубилось · 📢 Оголошення.
- `CATEGORY_EMOJI` → окрема явна мапа конкретна-категорія→emoji (бо у чіпах конкретних категорій більше нема).
- `getFilteredPosts`: фільтр через `cat.match.includes(p.category)` — один чіп групує дві конкретні.

**Крок 3 — кнопки контакту (тільки іконки, `board.js` + `community.css`):**
- `renderContact()`: номер → 📞 Подзвонити (`tel:`); `@username`/`t.me` → ✈️ Telegram (`https://t.me/<user>`); інший текст → просто текст.
- Хелпер `parseTelegram()` (regex username). `TELEGRAM_ICON_SVG`. CSS `.cm-board-call--tg` (блакитний). Кнопки мають клас `.cm-board-call` → наявні `stopPropagation`-обробники їх покривають.
- ⚠️ **Viber ПРИБРАНО (13.06, рішення Вови):** `viber://chat?number=…` до незнайомого номера ненадійно відкривається на iOS (Safari: «адреса недійсна») — обмеження схеми Viber, не баг коду. Лишили лише надійні Дзвінок + Telegram. Прибрано `VIBER_ICON_SVG`, `toE164()`, `.cm-board-call--viber`, `.cm-board-contact-btns`.

**Крок 6 — фінал:** `CACHE_NAME` bump → `cstl-20260613-1150`. `node build.js` exit 0, `bundle.js` перезібрано.

**Статус Фази А:** ✅ код готовий на гілці `claude/startup-uem-9tfuxg`. НЕ змерджено в `main` — чекає перевірки Вови → `/finish`.

---

## ✅ ЗРОБЛЕНО (13.06, гілка `claude/startup-uem-9tfuxg`) — Дошка Фаза А2: Обговорення → чат-модалка

> «Роби» Вови. Узгоджено: morph-розгортання картки → модалка, затемнений фон, ← назад + свайп вниз.

- `board.js`: картка обговорення стала **прев'ю теми** (`renderChatCard`): тема + хештеги +
  останнє повідомлення + лічильник (`👥 N повідомлень`) + час + →. Тап по картці (`data-chat-open`)
  → **повноекранна модалка-чат** `openChatModal()` (scale-морф з центру, `.board-backdrop`,
  ← назад / ✕ / тап по фону / свайп вниз по шапці). `closeChatModal()`.
- Повідомлення — **бульбашки** (`chatMessagesHtml`): чужі зліва зі світлою бульбашкою+аватаром,
  мої — справа бордові. «Мої» позначаються локально (`LS_MY_COMMENTS` per-device) — до логіну;
  з Google-входом замінимо на `sender_uid`.
- Інлайн-коментарі під картками **прибрано** (`chatActionsHtml`, inline-форма) — їх замінює модалка.
  Перевикористано наявну інфру: `comments` + `addComment` + `subscribeComments` (Realtime).
  Optimistic-додавання + автоскрол донизу + оновлення прев'ю картки (`refreshChatCardPreview`).
- `style/community.css`: прибрано стилі `.bd-inline-comment*`; додано прев'ю-картку, модалку-чат,
  бульбашки. `CACHE_NAME` → `cstl-20260613-1632`. `node build.js` exit 0.
- ⚠️ Пре-авторизація: чат поки **анонімний** (як коментарі були). Гейтинг реєстрацією — на етапі «Громада».
- 🔵 Морф поки scale-з-центру (як zoom стікерів), не точно з позиції картки — Вова дивиться, правимо за потреби.

---

## ✅ ЗРОБЛЕНО (13.06) — Чат-модалка: фікси геометрії/клавіатури + редизайн

Фідбек Вови на першу версію («2000 рік», таб-бар налазить, пустки, клавіатура ламає layout):
- **Геометрія:** модалка тепер панель `top: header+6 / bottom: tabbar+10`, `z-index:1100`
  (backdrop `1050`) — **вище таб-бару** (1000), меню більше не перекриває. Повідомлення
  `margin-top:auto` (притиснуті донизу) — пустка зникла.
- **Клавіатура:** `window.visualViewport` listener → клас `.bd-chat-modal--kb` піднімає модалку
  до верху і ставить `bottom: var(--kb)` над клавіатурою. Таб-бар не чіпаємо (не стрибає).
  Прибрано авто-фокус інпута при відкритті (клавіатура не вискакує одразу).
- **Редизайн:** месенджер-стиль — групування підряд повідомлень одного автора, імʼя раз на групу,
  час дрібний у куті, аватар лише в чужих, хвостик-кут, мʼякі тіні, бульбашки без рамок, тепле
  тло `#F3EFE3`, пігулка-інпут з тінню. Свайп-база transform → `translateX(-50%)`.
- `board.js` (`chatMessagesHtml` групування, `openChatModal`/`closeChatModal` visualViewport),
  `style/community.css`. `CACHE_NAME` → `cstl-20260613-1656`. build exit 0.

---

## 🔵 ФАЗА Б — Приватний чат покупець↔автор (СПЕЦ, будуємо на етапі «Громада»)

> Модель авторизації спроектовано (план сесії): гість = тільки перегляд; усі дії (подати
> оголошення · чат · реакції+коментарі · автобус+пуш) — після Google-реєстрації (Ім'я+Пошта+
> дата народження). Флоу: при вході екран «Приєднайтесь» → Увійти/Пропустити (read-only).
> **Код авторизації — НЕ зараз**, а коли робитимемо детально сторінку «Громада».

> Вова перевірив наживо: Viber/Telegram-кнопки прибрали (viber:// і t.me ненадійні/зайві).
> Рішення: спонукати **Дзвінок** або **приватний чат у додатку**. Це Фаза Б з `BOARD_FINAL_PLAN.md`.
> **Повний план з SQL+RLS:** файл плану сесії (5 етапів). Рішення Вови: логін лише для чату ·
> тільки Google (Gmail) · поетапно.

**Етап 0 — прибрати Telegram (DONE 13.06):**
- `board.js renderContact()`: прибрано Telegram-гілку, `parseTelegram()`, `TELEGRAM_ICON_SVG`.
  Лишилось: номер → 📞 Дзвінок; інший текст → просто текст. Кнопка «Написати» (чат) — Етап 3.
- `style/community.css`: прибрано `.cm-board-call--tg`. `CACHE_NAME` → `cstl-20260613-1532`. build exit 0.

**Далі по Фазі Б:** Етап 1 (Google-логін + `profiles` + «Кабінет жителя»). Потрібні руки Вови:
налаштувати Google OAuth у Google Cloud + Supabase Dashboard.

---

## 🟢 ДОШКА — фінальна концепція: Фаза А ✅ + Фаза А2 ✅ ЗРОБЛЕНО, лишилась Фаза Б

> **Прогрес 13.06.2026:** Фаза А (чистка + чіпи + контакт) ✅ на проді. Фаза А2 (Обговорення →
> чат-модалка) ✅ на проді. **Лишилась Фаза Б** (Google-логін + приватний чат) — будуємо на
> етапі детальної розробки сторінки «Громада» (рішення Вови). Деталі — секція «СЕСІЯ ВОВИ» вгорі.
>
> 📄 **Повний план Фази Б:** `CSTL NEWS VOVA/docs/BOARD_FINAL_PLAN.md` (оновлено з рішеннями Вови).

**Історія концепції (3 фази):**

- **Фаза А ✅ ЗРОБЛЕНО (до логіну):**
  1. Видалити ПОВНІСТЮ тип 🎉 Вітання (`type='greeting'`) і категорію ❤️ Подяка —
     з `community-modal.js`, `board.js`, `community-blocks.js`, `data/community-board.json`,
     Supabase `posts` + `/admin.html`.
  2. Згрупувати чіпи категорій у 6: `✦ Всі · 🛒 Куплю/Продам · 🔍 Шукаю · 🔧 Послуги ·
     🎁 Знайдено/Загубилось · 📢 Оголошення` (пост зберігає конкретну категорію, чіп групує дві).
  3. Кнопки контакту на картках оголошень: **номер** → 📞 Подзвонити + 🟣 Viber;
     **@username/t.me** → ✈️ Telegram. **Тільки іконки** (круглі, як `.cm-board-call`).
     ⚠️ Telegram за номером НЕ робимо (ненадійно) — тільки за username.
  4. Bump `CACHE_NAME` у `sw.js`.
- **Фаза А2 ✅ ЗРОБЛЕНО:** Обговорення → **повноекранна модалка-чат** (публічний груповий чат,
  бульбашки, realtime через `comments`). Inline-коментарі прибрано. Фікси клавіатури — див. вгорі.
- **Фаза Б (лишилась, на етапі «Громада»):** Google-логін = **«Кабінет жителя громади»**
  (профіль: Імʼя+Пошта+**дата народження**), логін лише для дій, гість read-only,
  приватний чат покупець↔продавець + push. Повний план — `docs/BOARD_FINAL_PLAN.md`.

**⚠️ Технічні пастки (виявлено при читанні коду — деталі у плані):**
- `CATEGORY_EMOJI` у `board.js` виводиться з `BOARD_CATEGORIES` → після групування зробити окрему мапу конкретних категорій.
- `BOARD_CATEGORIES[7]` fallback у `community-modal.js` зламається після видалення подяки → замінити на `.find(c => c.id === 'оголошення')`.
- Макет контакт-кнопок і чіпів УЖЕ затверджено — Фазу А можна кодити одразу.

**ДВА різні чати — не плутати:** Обговорення-чат = публічний (Фаза А2); покупець↔продавець = приватний 1-на-1 (Фаза Б).

---

## ✅ ЗРОБЛЕНО У ЦІЙ СЕСІЇ (13.06.2026, гілка `claude/startup-uem-pl7nch`) — UI вкладки «Дошка»

> CACHE_NAME: `cstl-20260612-2200`. Зміни на гілці, ще не змерджено в `main`.

**style(board): повний UI-редизайн вкладки «Дошка» — tabs, chips, controls**

### Таби (ДОШКА / ЗБЕРЕЖЕНІ / ОБГОВОРЕННЯ)

- **Порядок табів змінено:** ЗБЕРЕЖЕНІ тепер у центрі між ДОШКА і ОБГОВОРЕННЯ
  (`TYPE_TABS = [board, saved, chat]` у `src/tabs/board.js`)
- **Активний таб** (`.bd-tab--active`):
  - Фон: `var(--card-cream)` = `#FAF8EF` (кремовий)
  - Ободок: `rgba(114, 47, 55, 0.80)` (бордовий, прозорість 0.80)
  - Тінь: `inset 0 2px 12px rgba(0,0,0,0.28), 0 2px 8px rgba(0,0,0,0.10)` (ефект «натиснуто»)
  - `font-weight: 700`
- **Неактивний таб** (`.bd-tab`):
  - Ободок: `1.5px solid rgba(0,0,0,0.28)` (світліший сірий)
  - `font-weight: 700`
  - Зовнішня тінь: `0 2px 8px rgba(0,0,0,0.12)`
- **Іконка закладки ЗБЕРЕЖЕНІ** — зсунута вниз на 2px: `padding: 2px 0 0 0` на `.bd-tab--round`

### Блок контролів (`.bd-controls`)

- **Прибрано `box-shadow`** — замінено на плавний fade знизу
- **`::after` fade:** `height: 6px`, `linear-gradient(to bottom, #F4F1E6, transparent)` — плавний перехід у дошку
- **`padding-bottom: 6px`** на `.bd-tabs` — відступ між табами і пошуком
- **Пошук (`.bd-search`)** — ободок: `1.5px solid rgba(0,0,0,0.22)`
- **Плейсхолдер пошуку** — різний залежно від активної вкладки:
  - ДОШКА → «Пошук по дошці...»
  - ОБГОВОРЕННЯ → «Пошук в обговореннях...»
  - ЗБЕРЕЖЕНІ → «Пошук у збережених...»

### Кнопка дзвінка на картках (`.cm-board-call`)

- Фон: прозорий
- Ободок: `1px solid var(--red)` (бордовий)
- Іконка: бордова (`color: var(--red)`)
- SVG телефону: `width="15" height="15" stroke-width="2.1"`
- Тінь: `inset 0 2px 12px rgba(0,0,0,0.28), 0 2px 8px rgba(0,0,0,0.10)`

### Чіпи підкатегорій (`.bd-cat-chip`)

- **Назви ВЕЛИКИМИ ЛІТЕРАМИ** (`text-transform: uppercase`)
- Фон: `var(--card-cream)` (`#FAF8EF`)
- Текст: `var(--black)`
- Ободок: `1px solid var(--paper-deep)` (сірий)
- Тінь: `0 2px 6px rgba(0,0,0,0.10)`
- **Активний чіп** (`.bd-cat-chip--active`):
  - Ободок: `rgba(0,0,0,0.30)` (чорнішій)
  - `font-weight: 700`
  - Тінь: `inset 0 2px 12px rgba(0,0,0,0.28), 0 2px 8px rgba(0,0,0,0.10)`
- **«ВСІ» завжди жирний:** `.bd-cat-chip[data-bd-cat="all"] { font-weight: 700; }`
- **«Подяки» ВИДАЛЕНО** з `BOARD_CATEGORIES` (залишилось 8 категорій)

### Баги які виправили

- **Скрол підкатегорій скакав на початок** при натисканні чіпу:
  - Причина: `renderAll(el)` замінювала весь `innerHTML` → скидала `scrollLeft` на 0
  - Фікс: `const savedCatScroll = el.querySelector('.bd-categories')?.scrollLeft ?? 0` перед рендером,
    `if (catsEl) catsEl.scrollLeft = savedCatScroll` після

- **Вкладки чат/збережені мали старий фото-фон** (залишок від попередньої сесії):
  - Фікс: при `activeType !== 'board'` — НЕ додаємо `.board-bg` елемент (без фото),
    додаємо тільки `.board-vignette--top` (верхня тінь)
  - Не можна ставити `position: fixed; z-index: 0` елемент для фону — він перекриває `normal flow` картки

### Що змінено у файлах

| Файл | Що змінено |
|------|-----------|
| `src/tabs/board.js` | `TYPE_TABS` = [board, saved, chat], `BOARD_CATEGORIES` без Подяки, плейсхолдер per-tab, scroll preservation, hasCork логіка |
| `style/community.css` | `.bd-tab`, `.bd-tab--active`, `.bd-tab--round`, `.bd-controls`, `.bd-controls::after`, `.bd-tabs`, `.bd-search`, `.cm-board-call`, `.bd-cat-chip`, `.bd-cat-chip--active`, `[data-bd-cat="all"]` |
| `sw.js` | `CACHE_NAME: cstl-20260612-2200` |

---

## ✅ ЗРОБЛЕНО У ЦІЙ СЕСІЇ (12.06.2026, гілка `claude/startup-uem-pl7nch`) — ЗМЕРДЖЕНО В MAIN (фінал 21:54)

**style(board): real cork background image + stronger vignettes**
- Фоновий корок `.board-bg`: CSS-градієнт → `images/cork2.png` (реальне фото)
- `images/cork2.png` додано в `STATIC_ASSETS` Service Worker для офлайн-кешування
- Вінієтки (тіні зверху/знизу): opacity 0.36/0.30 → 0.58/0.52, висота 100→140px
- `box-shadow` inset: 0.30/0.24 → 0.52/0.45, spread 56→80px
- Тінь знизу `.bd-controls` (блок вкладок): `border-bottom` → `box-shadow: 0 4px 16px rgba(0,0,0,0.13)`
- CACHE_NAME: `cstl-20260612-2154`

---

## ✅ ЗРОБЛЕНО У ЦІЙ СЕСІЇ (12.06.2026, гілка `claude/startup-uem-pl7nch`) — ЗМЕРДЖЕНО В MAIN

**style(board): Premium skeuomorphic redesign — Дошка громади**
- `.cm-board-note`: `border-radius 2px → 20px`, м'які ambient тіні, більший padding, колір `#2B2B2B`
- Кольори карток: злегка приглушені (premium paper feel)
- Cork backgrounds: тепліший gradient + виньєтка (radial shadow inset) на всіх варіантах дошки

**fix(board): JS two-column layout замість CSS column-count**
- Причина: CSS `column-count: 2` з `max-height` + `overflow-y: auto` на iOS давав пусті місця
- Фікс: JS розбиває картки на ліву/праву колонку по парності індексу (явні flex-divs)
- Нахил карток ±4° → ±2°, gap між колонками 14px → 20px

**fix(board): Cork background + fixed vignettes (тіні приклеплені до екрану)**
- `.board-bg` елемент тепер реально додається в HTML (був тільки CSS без елемента)
- `.board-vignette--top/bottom`: `position: fixed; z-index: 8` — не прокручуються зі стікерами

**fix(buses): spurious banner при знятті відстеження**
- `checkTrackNotifications()` тепер викликається тільки в потрібній гілці (true=tracking / false=untracking)

**fix(buses): бордова закладка для проміжних сегментів**
- `!!trackedSeg` замість `hasTrackedSeg` — коректно показує бордовий для проміжних зупинок

**fix(buses): dep_time null → push notifications не надходили**
- Причина: `getStopHHMM(route, fromStop)` — точне співпадіння, `fromStop` ≠ реальна назва зупинки
- Фікс: `getStopHHMM(route, getEffectiveFrom(route))` — нормалізований пошук → правильний час → Edge Function не пропускає підписку
- CACHE_NAME: `cstl-20260612-2124`

---

## ✅ ЗРОБЛЕНО У ЦІЙ СЕСІЇ (12.06.2026, гілка `claude/startup-uem-hp5ldw`)

> Аудит вкладки «Автобуси» + 4 фікси. Усе змерджено в `main` через auto-merge.
> Фінальний CACHE_NAME: `cstl-20260612-1715`.

**Аудит вкладки «Автобуси»** — виявлено 1 реальний баг і 1 потребу в покращенні UI.

**fix(push): видалено `notified_start` — зайве сповіщення для проміжних маршрутів**
- Симптом: при проміжному маршруті (Дідичі → Жорнище) приходило 2 push одночасно:
  «Автобус на зупинці · Дідичі» (правильне) + «Автобус вирушив · 07:15 (Ківерці)» (зайве).
- Фікс: видалено весь блок `notified_start` з `supabase/functions/send-bus-push/index.ts`.
- Задеплоєно через Supabase MCP → версія 10, ACTIVE.

**fix(buses): `unsubscribeFromPush` у hero track-button в `renderSmartRow`**
- Симптом: зняття відстеження через закладку до першого свайпу не видаляло push-підписку з Supabase.
- Фікс у `src/tabs/buses.js`: додано `unsubscribeFromPush(entry.routeId, entry.trackDate)` перед
  `removeTrackedEntry` у hero track-button handler в `renderSmartRow`.

**feat(buses): бордова закладка коли відстежується сегмент і фільтр знятий**
- Нова поведінка: якщо є відстежуваний сегмент (ДІДИЧІ→ЖОРНИЩЕ) і фільтр знятий →
  закладка на картці рейсу заповнена бордовим `#722F37` (клас `tracked-seg`).
- При активному фільтрі → чорна `#1C1C1E` (клас `tracked`) як і раніше.
- CSS: `.bs-track-btn.tracked-seg { color: #722F37 }` + `.tracked-seg svg { fill: #722F37 }`.
- JS: `isRouteSegmentTracked(route.id) ? ' tracked' : (hasTrackedSeg && !anySegment ? ' tracked-seg' : '')`

**fix(buses): клік на бордову закладку тепер скасовує сегментне відстеження**
- Симптом: клік на бордову закладку додавав НОВЕ повне відстеження замість скасування сегментного.
- Причина: обробник кліку перевіряв тільки `isRouteSegmentTracked()` (false без фільтра) → йшов
  у `else` (додати відстеження) замість `if` (видалити).
- Фікс: обробник тепер також перевіряє `btn.classList.contains('tracked-seg')`. Якщо true →
  знаходить запис по `routeId+busDay` і видаляє разом з push-підпискою.

---

## 🚌 ВКЛАДКА «АВТОБУСИ» — V1.0 ЗАФІКСОВАНО (12.06.2026)

> Вова підтвердив: «переходимо до Дошки». Вкладка «Автобуси» у фінальному робочому стані.

**Що зроблено (повний список):**
- Hero-картка з каруселлю, сегментним режимом, шкалою прогресу ✅
- Фільтр Звідки/Куди з dropdown (выпадающий список) зупинок ✅
- Відстеження кількох маршрутів/сегментів одночасно (`bus_track_v2`) ✅
- Банер відстеження з Dynamic Island анімацією ✅
- Level B push-сповіщення (Web Push через Supabase + Edge Function) ✅
- 3 типи push: T-15 попередження, T-0 «на зупинці»/«вирушив», скасування ✅
- Бордова закладка для сегментного відстеження без фільтра ✅
- Авто-скидання відстеження після проїзду зупинки Б ✅
- Парсер VOPAS (розклад) — cron 30 хв, 21-денне вікно ✅
- iOS системне сповіщення тільки якщо додаток закритий (не дублює in-app банер) ✅

**Єдине що залишилось (чекає на Вову):**
- 🔴 **Зворотні рейси** — Олика→Луцьк, Олика→Ківерці тощо. Вова має надати список.
  До отримання списку — НЕ чіпати код.

---


## ✅ ЗРОБЛЕНО У ЦІЙ СЕСІЇ (12.06.2026, гілка `claude/startup-uem-qpkilt`)

> CACHE_NAME: `cstl-20260612-1621`.

**fix(push): VAPID_PUBLIC_KEY у Edge Function — всі push-сповіщення були зламані**
- Причина: у сесії `startup-uem-ok51t5` пара VAPID ключів ротована, але `index.ts` залишився з
  OLD публічним ключем. Результат: 401 від Apple → `sent: 0` → всі підписки без сповіщень.
- Фікс: `supabase/functions/send-bus-push/index.ts` рядок 24 → новий `VAPID_PUBLIC_KEY`.
- Задеплоєно через Supabase MCP (версія 9, ACTIVE). Закомічено у git.

**fix(sw): розгалуження in-app банер vs iOS системне сповіщення**
- Раніше: push event у `sw.js` завжди показував системне сповіщення → дубляж при відкритому додатку.
- Фікс: перед `showNotification()` перевіряємо `clients.matchAll()` — якщо є visible вікно,
  повертаємо без сповіщення (in-app банер обробить). Якщо закрито/фон → системне.
- Стосується всіх типів: origin, сегментні, скасування.

**fix(buses): рейс у дорозі але зупинка посадки вже пройдена — показувати як минулий**
- Якщо є фільтр «Звідки» і автобус вже проїхав зупинку посадки (`nowMinutes() > boardMins`) —
  для цього користувача рейс переходить у «минулі» (opacity 0.32, не потрапляє у hero/карусель).
- Зміни у `src/tabs/buses.js`: `isPastRoute`, `findNextRoute`, `findActiveRoutes`.

**fix(data): Хорлупи пов. → Хромяків у маршруті Луцьк → Жорнище**
- У route-stops.json, schedule.json (39 входжень), STOP_ALIASES у buses.js.

- CACHE_NAME: `cstl-20260612-1631`.

**style(buses): фікс градієнту зупинок А/Б — коли автобус на них або наступна є фінальною**
- Проблема 1: `hl--from:not(:first-child)` (вага 0,3,0) перебивала `bs-stop--current` (0,2,0) → подвійне затухання замість суцільного краю.
- Проблема 2: коли наступна зупинка є зупинкою Б (фінальною), верх Б затухав до білого замість з'єднання з поточною.
- Фікс — 8 нових CSS-правил у `style/buses.css` (включно з `hl--from.bs-stop--next`).
- Ця частина змерджена в `main` через auto-merge раніше у сесії.

---

## ✅ ЗРОБЛЕНО У ЦІЙ СЕСІЇ (12.06.2026, гілка `vibrant-gates-qgpfkc`)

> Опубліковано в `main` через auto-merge (claude/** → main) → автодеплой.
> CACHE_NAME: `cstl-20260612-1145`.

**style(buses): поточна+наступна зупинки зливаються в один блок без білої лінії**
- Було: обидві зупинки згасали в білий і зверху, і знизу → між ними біла смуга.
- Стало: `.bs-stop--current` верх згасає, низ суцільний; `.bs-stop--next` верх
  суцільний, низ згасає. Внутрішні краї стикуються, згасання лише на зовнішніх.
- `style/buses.css` рядки ~1008-1017. CACHE_NAME → `cstl-20260612-1150`.

**feat(push): звичайний (origin) рейс отримує 2 сповіщення — за 15 хв + «вирушив»**
- На прохання Вови: для рейсу де посадка на початковій зупинці (або без
  сегменту) тепер приходить:
  1. T-15: «Автобус відправляється через ~15 хв · HH:MM» (`notified_warning`)
  2. T-0:  «Автобус **вирушив** · HH:MM» (`notified_dep`) — замість сухого
     «Автобус відправляється».
- Проміжна зупинка лишається як було: «вирушив (з початкової)» → «на зупинці».
- Файл `supabase/functions/send-bus-push/index.ts`: додано `isOriginBoarding`
  (boarding=null або =перша зупинка), текст T-0 розгалужено.
- **Задеплоєно через Supabase MCP → версія 8, ACTIVE, verify_jwt=true.**

**fix(buses): надійне збереження push-підписки (рейс більше не лишається без сповіщень)**
- Причина бага «Луцьк-Личани без сповіщення»: рядок підписки не зберігся у
  `push_subscriptions` — `subscribeToPush()` мав німий `catch (_) {}`, що
  ковтав будь-який збій (обірваний запит під час оновлення додатку / помилка
  після ротації VAPID) без логів і без повтору.
- Фікс у `src/tabs/buses.js`:
  1. Прибрано німий catch → логування + **один повтор** збереження + тост
     «Не вдалося увімкнути сповіщення» при провалі.
  2. Лагодження після ротації VAPID: перед `subscribe()` перевіряємо існуючу
     браузерну підписку; якщо `applicationServerKey` інший — `unsubscribe()` і
     підписуємось наново. Якщо ключ не читається (iOS) — не чіпаємо.
  3. Хелпер `pushKeysEqual()` — побайтове порівняння ключів.
- `scripts/check-imports.js` — `Uint8Array`, `Promise` додано у whitelist.
- Підтверджено по даних: серверна функція `send-bus-push` здорова (щохвилини,
  усі 200). Звичайні (origin) рейси сповіщення отримують (warning + «Автобус
  відправляється»); окреме «вирушив» — лише для проміжних, бо для початкової
  це той самий момент.

**fix(buses): прибрано debug-toast «додаємо/знімаємо відстеження»**
- Службове спливаюче (`showToast`) у track-кнопці картки рейсу прибрано —
  воно дублювало власний банер відстеження. `showToast` більше не
  імпортується у `buses.js` (був єдиний виклик).

**fix(community): віджет автобуса оновлюється в реальному часі**
- Баг: коли користувач знімав відстеження рейсу на вкладці Автобуси, віджет
  автобуса на Громаді не оновлювався до перезапуску додатку (читав localStorage
  тільки при відкритті вкладки).
- Фікс: `saveTrackedRoute()` у `src/tabs/buses.js` — єдина точка через яку
  проходять усі зміни відстеження (додавання + видалення через
  `removeTrackedEntry`) — тепер кидає `CustomEvent('cstl-bus-track-changed')`.
- `src/tabs/community-blocks.js` — слухач на рівні модуля викликає
  `renderBusBlock()` при цьому сигналі → віджет перемальовується миттєво.
- Безпечно коли вкладка Громада не в DOM: `renderBusBlock` виходить на `null`.

---

## ✅ ЗРОБЛЕНО У ЦІЙ СЕСІЇ (12.06.2026, гілка `startup-uem-ok51t5`)

> Усе запушено в робочу гілку. У `main` ще НЕ мерджено — потрібен `/finish`.
> Фінальний CACHE_NAME: `cstl-20260612-1100`.

**Безпека:**
1. **VAPID-ключ (ключ push-сповіщень) ротовано** — новий публічний ключ у `src/tabs/buses.js`, новий приватний — у Supabase Secrets (`VAPID_PRIVATE_KEY`). Старий ключ з git-історії недійсний.
2. **Cloudflare Worker оновлено** — задеплоєно виправлений `cloudflare/worker.js` (закрито open-proxy дірку — тепер Worker ходить тільки на `olytska-gromada.gov.ua`).
3. **Leaked Password Protection (Supabase)** — пропущено, доступно тільки на платному Pro плані.

**🔴 ПЕРШЕ В НОВІЙ СЕСІЇ:** нічого критичного не відкрито.

**Supabase MCP + безпека:**
1. **Supabase MCP підключено** — сервер `Supabase` працює без OAuth. Проект «Olyka Castle» (`uabyfecseqnemvcqhdem`).
2. **RLS-аудит 8 таблиць завершено** (останній пункт безпеки). Критичних дір нема. Деталі нижче в розділі КМ/безпеки.

**Автобуси — баги і фікси (всі в `src/tabs/buses.js`):**
3. **Банер «Рейс скасовано»** — показувався при кожному вході. Фікс: `showBanner` перенесено всередину `if (!tracked.notifiedCanc)` — тепер один раз.
4. **Банер вилітав під час сплеш-екрану** — перший `checkTrackNotifications()` тепер із затримкою 4200мс (`setTimeout` в `initBuses`, рядок ~1646).
5. **Debug-toast прибрано** зі `subscribeToPush()` — push працює тихо у фоні.
6. **Клавіатура зникала при наборі** у пошуку зупинок. Причина: `renderDropdownItems` перебудовував весь `innerHTML` (з полем вводу). Фікс: розбито на `buildDropdownListHtml` + `updateDropdownList` + `attachDropdownListListeners` — при наборі оновлюється ТІЛЬКИ список, поле вводу лишається в DOM.

**Автобуси — дані (км зупинок):**
7. **Точні км (варіант 2)** залито у `route-stops.json`. Оригінал → `route-stops.v1.json`. Деталі в розділі «КМ ЗУПИНОК» нижче.

**UI:**
8. **Тінь шапці** (`.app-header` у `style/base.css`) — дзеркало тіні таб-бару, спрямована вниз.
9. **Тонша рисочка активної вкладки** (`style/community.css` `.tab-item.active::before`): height 4px→3px, radius 2px→1.5px.

**Громада — автобус-віджет (`src/tabs/community-blocks.js`):**
10. Віджет тепер показує **відстежувані рейси** (будь-який день — дублює трекінг із вкладки Автобуси) + коли сьогоднішні закінчились, **одразу завтрашній рейс**. Прибрано «НА СЬОГОДНІ РЕЙСІВ БІЛЬШЕ НЕМАЄ». Підпис «Завтра · DD місяця» (`.cm-bus-daylabel`) над майбутніми рейсами. `cmBusRoutes` → `cmBusEntries` (з полем `dateISO`).

**✅ Вся робота вище — змерджена в `main`** (сесія `vibrant-gates-qgpfkc`, 12.06). На сайті.


---

## 🚌 КМ ЗУПИНОК — ДВА ВАРІАНТИ (11.06.2026)

> Точність km у `data/route-stops.json` впливає на час прибуття на проміжні зупинки і шкалу відстеження.

- **Варіант 1 (оригінал, стояв до сьогодні)** — збережено у `data/route-stops.v1.json`. Точка відкату.
- **Варіант 2 (активний зараз)** — точні виміри Вови через Google Maps. Залито у `data/route-stops.json`.

**Оновлено 5 прямих маршрутів** (зворотні + «Луцьк Рівне» НЕ чіпали — чекають на дані Вови):
Ківерці→Носовичі 69.7км · Луцьк→Личани(А) 50.8км · Луцьк→Олика 51.6км · Луцьк→Жорнище 52.4км · Луцьк→Личани(Б) 55.3км.

- **Котів↔Дерно = 4 км** взято зі старих даних сайту (Вова не впевнений у цьому відрізку).
- **Важливо про потік даних:** додаток читає `schedule.json` (збирає парсер з секції `_by_vopas_name`). Масив `routes` — мертвий (ніхто не читає), оновлений лише для узгодженості. Зміни km з'являться на сайті після наступного запуску cron-парсера (≤30 хв перезбере `schedule.json`).
- **Відкладено (на потім, потребує замірів Вови):** Пальче→Котів=9.9 і початок маршруту 1 (Ківерці→Піддубці 12.1) — великі, варто передивитись. Зворотні маршрути.

---

## ✅ ТЕРМІНОВИЙ ПЛАН — ВИКОНАНО (11.06.2026)

1. ✅ **Supabase MCP підключено** — `Supabase` MCP-сервер працює без OAuth. Проект «Olyka Castle» (`uabyfecseqnemvcqhdem`, Frankfurt) ACTIVE_HEALTHY.
2. ✅ **RLS-аудит завершено** — всі 8 таблиць перевірено. Критичних дір нема. Цінні дані (`posts`, `admins`, `announcements`, `ads`) захищені правильно. Слабкі місця (`reactions`, `push_subscriptions`, `comments`) — технічно невиправні без Google Auth. Google Auth записано як ключову задачу на майбутнє.

**Нагадати Вові (його руки, поза кодом):**
- 🔧 **Перезалити Cloudflare Worker** — фікс open-proxy у `cloudflare/worker.js` не діє, поки не задеплоїти на cloudflare.com.
- ~~🔑 **Ротувати VAPID-ключ**~~ — ✅ виконано 12.06. Новий ключ у buses.js + Edge Function. Старий лишився в git-історії (не критично — вже недійсний).
- 🔒 **Leaked Password Protection** — увімкнути у Supabase Dashboard → Authentication (захист паролів адмінів).

**Відкладені (не термінові):** ~~прибрати debug-toast у `subscribeToPush()`~~ (зроблено 12.06) · Фаза 3 «Світло» через Supabase · зворотні рейси «з боку Олики» (чекає список від Вови) · сторінка `/реклама`.

---

## 🟢 Поточний стан

| | |
|--|--|
| **URL сайту** | https://volodymyr221.github.io/CSTL_NEWS/ |
| **Репозиторій** | https://github.com/Volodymyr221/CSTL_NEWS |
| **Робоча гілка (остання сесія Вови)** | `claude/startup-uem-9tfuxg` (синхронізована з `main`) |
| **Production-гілка** | `main` — auto-merge `claude/**`/`roma/**`/`vova/**` → main → auto-deploy |
| **Власники** | Вова Шевчук (GitHub: Volodymyr221) + Рома (`roma.haranin.ru1@gmail.com`) |
| **CACHE_NAME у `sw.js`** | `cstl-20260613-2010` |
| **Статус вкладки «Автобуси»** | 🟢 V1.0 + порожнє hero-табло (Рома 13.06). Зворотні рейси відкладено |
| **Push-сповіщення (Level B)** | 🟢 ПРАЦЮЄ 12.06 |
| **Статус вкладки «Дошка»** | 🟢 Фаза А ✅ + Фаза А2 (Обговорення чат-модалка) ✅ на проді. Контакт = тільки 📞 Дзвінок. Лишилась Фаза Б (логін+чат) |
| **Наступна задача** | Детальна розробка сторінки «Громада» = там Фаза Б (Google-логін + профіль + приватний чат покупець↔автор + пуші). План: `docs/BOARD_FINAL_PLAN.md` |

---

## 🛡️ СЕСІЯ БЕЗПЕКИ + Supabase MCP (11.06.2026, гілка peaceful-johnson)

**Зроблено (аудит безпеки, усе в коді + перевірено build/тести):**
- **B1 XSS:** `escapeHtml` тепер екранує і `'` (одинарну лапку). Аудит підтвердив — усі шляхи чужого контенту (RSS, пости Supabase, коментарі, фото) вже екранувались.
- **B2 SSRF:** `parse_rss.fetch_full_article` ходив за будь-яким посиланням зі стрічки → додано `is_allowed_url()` (whitelist доменів, блок `file://`/приватних IP).
- **B4 Cloudflare Worker:** виправлено open-proxy (`path=@evil.com` міняв хост) → `new URL()` + перевірка origin. ⚠️ **Worker треба вручну перезалити на Cloudflare.**
- **B5 CI:** `auto-merge.yml` — `github.ref_name` винесено в env (захист від script injection).
- **B6 секрети:** redact приватного VAPID-ключа з цього файлу + додано gitleaks-воркфлоу + `.gitleaks.toml`. ⚠️ **Вова має РОТУВАТИ VAPID-ключ** (він лишився в історії git).
- **A1:** `.claudeignore`. **A4:** дедуплікація правил (START_HERE → HOT_RULES).

**Supabase MCP:**
- `.mcp.json` додано (read-only, project_ref=uabyfecseqnemvcqhdem, OAuth).
- Network access середовища → Custom + домени `mcp.supabase.com`, `*.supabase.com`, `*.supabase.co`.
- **Залишилось:** у НОВІЙ сесії на `main` пройти OAuth-вхід → тоді перевірити RLS-політики всіх таблиць (відкладений пункт безпеки).

---

## 🔒 ВКЛАДКА «АВТОБУСИ» — ЗАФІКСОВАНИЙ РОБОЧИЙ СТАН (11.06.2026)

> Вова перевірив і підтвердив: «все добре». Нижче — повний перелік всього що реалізовано.

### Трекінг (відстеження маршрутів) — архітектура

| Елемент | Опис |
|---|---|
| `TRACK_KEY = 'bus_track_v2'` | Ключ у `localStorage` (локальному сховищі браузера) |
| `trackedRoutes[]` | Масив всіх відстежуваних записів |
| Формат запису | `{ routeId, trackDate, boardingStop, alightingStop, notifiedDep, notifiedCanc, notifiedBoard, notifiedWarning, notifiedFuture }` |
| `findTrackedEntry(routeId, boarding, alighting, date)` | Пошук конкретного запису по 4 полях |
| `removeTrackedEntry(entry)` | Видалення запису + збереження у `localStorage` |
| `isRouteSegmentTracked(routeId)` | `true` тільки якщо routeId + fromStop + toStop збігаються з відстежуваним |
| `getTrackedSegmentForHero(routeId, route)` | Перший запис для рейсу на поточний день; авто-видаляє якщо `nowMinutes() >= alightMins` |

### Що реалізовано в цій сесії (10-11.06.2026, продовження)

**fix(buses)** — дедуплікація зупинок з суфіксом `пов.` (поворот):
- `normalizeStopName()` — нова функція, прибирає ` пов.` з кінця назви
- `getAllStops()` — нормалізує перед Set → "Хорлупи пов." і "Хорлупи" = один пункт у дропдауні
- `matchesSearch()` — порівняння через нормалізацію → маршрут з "Хорлупи пов." знаходиться при пошуку "Хорлупи"
- `getEffectiveFrom/To()` — повертає оригінальну назву з даних (щоб `getStopMins` і `getStopHHMM` працювали коректно)

### Що реалізовано в цій сесії (10-11.06.2026)

**1. Hero-картка — режим сегменту**
- `buildHeroCard(route, timings, index, total, seg = null)` — новий параметр `seg`
- `hasSeg = true` коли `seg.boardingStop/alightingStop` ≠ кінцевих точок маршруту
- При `hasSeg`: заголовок = сегмент (`ДІДИЧІ → ОЛИКА`), підзаголовок `.bhv4-route-full` = повний маршрут (`ЛУЦЬК → ОЛИКА`)
- Капсула часу: час посадки → час висадки сегменту
- Тривалість: тривалість сегменту (не всього маршруту)
- Відлік: «ДО ДІДИЧІВ ЗА X ХВ» якщо автобус ще не доїхав до зупинки посадки

**2. Іконка відстеження у hero-картці**
- Біла заповнена закладка SVG у правому верхньому куті → крапки каруселі зліва, закладка справа
- Показується при **будь-якому** відстеженні: і сегмент, і повний маршрут (`seg` → `!!seg`, не `hasSeg`)
- Натискання → `removeTrackedEntry` → `checkTrackNotifications` → `renderSmartRow` + `renderRouteList`

**3. Авто-скидання відстеження після проїзду пункту Б**
- `getTrackedSegmentForHero(routeId, route)` — якщо `nowMinutes() >= getStopMins(route, alightingStop)` → авто-видаляє запис
- `checkSingleTracked` — також перевіряє alightingStop: якщо час минув → `removeTrackedEntry` → `return`
- Результат: після проїзду пункту Б — hero-картка, маркери ●/📍, підзаголовок на картці — все повертається в звичайний режим автоматично

**4. Підзаголовок відстежуваного сегменту на картці рейсу**
- Умова: `!anySegment && hasTrackedSeg` (немає фільтру, але є відстежуваний сегмент)
- Формат: `📍 ДЕРНО - ЖОРНИЩЕ | 07:27 - 07:47` (пін SVG + зупинки + час відправлення і прибуття)
- Позиція: **підзаголовок зверху** (бордовий), **назва маршруту знизу** (чорний)
- CSS клас: `.bs-route-full` з SVG-піном 9×9px

**5. Маркери ●/📍 у списку всіх зупинок**
- `hlFrom` / `hlTo` — якщо немає фільтру але є відстежуваний сегмент → маркери на зупинках сегменту
- CSS класи `hl--from` і `hl--to` на рядках зупинок
- Без фільтру + без трекінгу → маркери на кінцях маршруту

**6. Стиль виділених зупинок А і Б**
- Кінцеві позиції (перша/остання): `.hl:first-child` → gradient вниз; `.hl:last-child` → gradient вгору (без змін)
- `.hl--from:not(:first-child)` та `.hl--to:not(:last-child)` → подвійний градієнт `#f5f5f5→#E1E4EC→#E1E4EC→#f5f5f5`, без рамок
- Поточна зупинка `.bs-stop--current` і наступна `.bs-stop--next` → той самий подвійний градієнт

**7. Карусель hero-картки — множинне відстеження**
- **Сьогодні**: відстежувані рейси піднімаються на початок через `trackedTodayIds.reverse().forEach` ✓
- **Майбутні дні**: `all.find()` → `all.filter()` — всі відстежувані рейси у каруселі (не тільки перший)
- Крапки-навігатори зліва, іконка закладки справа

**8. UI-правки**
- Кнопка «ВСІ ЗУПИНКИ» → колір `#1C1C1E` (чорний, раніше `var(--navy)`)
- Банер сповіщення: `min-width: 300px` (раніше 240px), `padding: 26px 28px 10px` (раніше 20px)

### Виправлені баги

| # | Де | Симптом | Фікс |
|---|---|---|---|
| B-1 | `findActiveRoutes` | Для майбутніх днів у каруселі показувався тільки 1 відстежуваний рейс з 2 | `all.find()` → `all.filter()` + `Set` routeId |
| B-2 | `buildCard` bookmark | При відстежуванні сегменту ДЕРНО→ЖОРНИЩЕ, картка КІВЕРЦІ→ОЛИКА показувала заповнену закладку | Видалено `\|\| (!fromStop && !toStop && !!getTrackedSegmentForHero())` — тільки точне співпадіння |
| B-3 | `checkSingleTracked` | Банер міг мигнути після проїзду пункту Б (60с-цикл не знав про авто-скидання) | Додано перевірку `nowMinutes() >= alightMins` → `removeTrackedEntry` + `return` |
| B-4 | `getTrackedSegmentForHero` | Не передавався `route` → авто-скидання не спрацьовувало | Всі 5 викликів оновлено: `getTrackedSegmentForHero(route.id, route)` |

### Відоме обмеження (не баг, не критично)
- Якщо на ОДНОМУ рейсі відстежуються 2 різних сегменти (дуже рідкісний випадок) — hero показує тільки перший сегмент. `getTrackedSegmentForHero` повертає перший збіг через `.find()`. Вирішення відкладено.

---

## ✅ Зроблено у поточній сесії (10.06.2026, hero-картка для сегментів)

**feat(buses)** — hero-картка показує сегмент якщо він відстежується:
- `getTrackedSegmentForHero(routeId)` — знаходить запис відстеження для поточного дня
- `buildHeroCard(route, timings, index, total, seg = null)` — новий параметр `seg`
  - Якщо `seg.boardingStop` / `seg.alightingStop` ≠ кінцевих точок маршруту → режим сегменту
  - Назва у hero: `ДІДИЧІ → ОЛИКА` (сегмент) замість `ЛУЦЬК → ОЛИКА` (повний маршрут)
  - Підзаголовок `.bhv4-route-full`: `ЛУЦЬК → ОЛИКА` дрібними приглушеними літерами
  - Капсула часу: час посадки → час висадки
  - Тривалість: тривалість сегменту
  - Відлік: «ДО ДІДИЧІВ ЗА X ХВ» якщо автобус ще не дістався до зупинки посадки
- `renderSmartRow()` — передає `seg` у `buildHeroCard`
- `switchHeroCard()` — враховує сегмент при оновленні динамічних елементів
- `style/buses.css` — `.bhv4-route-full` (11px, rgba(255,255,255,0.55), uppercase); margin-bottom `.bhv4-route-name` 8px→4px
- `sw.js`: CACHE_NAME → `cstl-20260610-1606`


**feat(buses)** — можна відстежувати кілька маршрутів/сегментів одночасно:
- `TRACK_KEY` → `bus_track_v2`, формат `{ routes: [...] }` замість одного об'єкту
- `trackedRoutes[]` — масив відстежуваних рейсів замість одиночних змінних
- Кожен запис: `{ routeId, trackDate, boardingStop, alightingStop, notifiedDep, notifiedCanc, notifiedBoard, notifiedWarning, notifiedFuture }`
- `findTrackedEntry()` + `removeTrackedEntry()` — додавання/видалення конкретного запису
- `checkSingleTracked(tracked, forceInitial)` — логіка сповіщень для одного рейсу
- `findActiveRoutes()` — всі відстежувані рейси піднімаються на початок каруселі
- Банер: 4 секунди auto-hide (повернено), `forceShow` логіка відновлена
- Трекінг зберігається до завершення рейсу або ручного скасування
- `notifiedDep` зберігається при переключенні сегменту того ж повного маршруту (без дублювання сповіщень)
- `sw.js`: CACHE_NAME → `cstl-20260610-1615`

## ✅ Зроблено у поточній сесії (10.06.2026, фікси трекінгу сегментів)

**fix(buses)** — трекінг унікальний за routeId+зупинки сегменту:
- Додано `isRouteSegmentTracked(routeId)` — повертає true тільки якщо routeId + fromStop + toStop збігаються з відстежуваними
- Додано `findTrackedEntry()` — пошук запису по routeId+stops
- Кнопка-закладка: різні сегменти одного повного маршруту більше не показуються як "відстежувані"
- При перемиканні на новий сегмент того ж рейсу — `_notifiedDep` і `_notifiedWarning` зберігаються (автобус той самий, дублікат не надходить)
- `sw.js`: CACHE_NAME → `cstl-20260610-1545`

**fix(buses)** — фільтр сегментів дозволяє однаковий km:
- `matchesSearch()`: умова `fStop.km >= tStop.km` змінена на `fStop.km > tStop.km`
- Причина: у `Луцьк → Олика` зупинки Дідичі і Олика мають однаковий km=44 → маршрут не показувався при фільтрі "Дідичі → Олика"
- Тимчасовий фікс до отримання точних km від Вови

## ✅ Зроблено у поточній сесії (10.06.2026, банер відстеження — фінальна полірування)

**fix(buses)** — банер: серія UI-правок:
- Заголовок без `| час`, підзаголовок = `11:41 → 12:05 | 10 ЧЕР` (час + дата)
- Авто `letter-spacing` підзаголовку до ширини заголовку (JS `void offsetWidth`)
- "СПОВІЩЕННЯ ПРО РЕЙС АКТИВОВАНО" переміщено з низу на верх (`top: 7px`)
- Динамічна ширина: `width: max-content`, `min-width: 240px`, `max-width: calc(100vw - 32px)`
- Фінальний `padding: 26px 20px 10px`
- `sw.js`: CACHE_NAME → `cstl-20260610-1457` ✅ **ЗАФІКСОВАНО ВОВОЮ**

---

## ✅ Зроблено у поточній сесії (10.06.2026, фікс банеру відстеження)

**fix(buses)** — банер: чистий заголовок + час у підзаголовку:
- `buildBannerTexts()`: прибрано `| HH:MM → HH:MM` з `heading` (заголовку)
- `subDefault` тепер = тільки час сегменту (`segTimeStr`) або повного маршруту (`timeStr`)
- `.btb-label--subroute` CSS: колір темний замість бордо, `12px / font-weight:600 / letter-spacing:3.5px`

`sw.js`: CACHE_NAME → `cstl-20260610-1353`

---

## ✅ Зроблено у поточній сесії (09.06.2026, Dynamic Island анімація банеру — ФІНАЛ)

**fix(buses)** — банер вилітає з іконки ГРОМАДА знизу вгору:

- `transform-origin: center bottom` — якір масштабування на нижньому краю банеру
- Прихований стан: `translateX(-50%) translateY(41px) scale(0.15, 0.15)` — крихітна пігулка (~53px) опускається до центра кола ГРОМАДИ (54px від низу)
- Видимий стан: `translateX(-50%) scale(1)` — повний банер на 95px від низу
- Анімація: пігулка виростає знизу вгору, стартує з рівня іконки ГРОМАДА
- Математика: коло `.tab-home-circle` — `bottom: 28px`, `height: 52px` → центр кола на 54px від низу; `translateY(41px)` зсуває нижній край банеру з 95px → 54px

`sw.js`: CACHE_NAME → `cstl-20260609-1635` ✅ **ЗАФІКСОВАНО ВОВОЮ**

---

## ✅ Зроблено у поточній сесії (09.06.2026, сегментний маршрут у банері)

**feat(buses)** — банер показує сегмент пошуку замість повного маршруту:

- Нова глобальна змінна `_trackedToStop` — зберігає `toStop` (зупинку висадки) в момент натискання «Відстежити»
- `saveTrackedRoute()` / `loadTrackedRoute()` — `alightingStop` у `localStorage[bus_track_v1]`
- `clearTrackedRoute()` — скидає `_trackedToStop = null`
- При натисканні «Відстежити»: `_trackedToStop = toStop || null` зберігається одночасно з `_trackedStop`
- `checkTrackNotifications()` — `label` і route будуються як `segFrom → segTo`:
  - `segFrom = _trackedStop || a` (якщо пошук без «Звідки» — беремо першу зупинку маршруту)
  - `segTo = _trackedToStop || b` (якщо пошук без «Куди» — беремо останню зупинку маршруту)
- Однаково для сьогодні, завтра, будь-якого майбутнього дня
- `sw.js`: CACHE_NAME → `cstl-20260609-1229`

**Приклад:** шукаєш Олика → Луцьк, натискаєш «Відстежити» на рейсі Ківерці → Луцьк → банер покаже `ОЛИКА → ЛУЦЬК`

---

## ✅ Зроблено у поточній сесії (09.06.2026, банер відстеження — нова логіка)

**feat/fix(buses)** — банер відстеження повністю переосмислений:

- **Авто-закриття через 5 секунд** — банер з'являється, показується 5с, плавно ховається
- **Свайп вниз** — закриває банер раніше (поріг 40px)
- **Кнопку ✕ прибрано** — банер сам зникає
- **Текст по центру** — `btb-content` абсолютно позиціонований по центру банеру; іконка-кружечок лишилась зліва
- **Плавніша анімація:** поява 650ms spring `cubic-bezier(0.22,1,0.36,1)`, ховання 380ms ease-in
- **Логіка показів:** тільки при нових важливих подіях (`forceInitial` при натисканні, `forceShow` при milestone-ах). Регулярний 60с poll НЕ перепоказує банер
- **`_notifiedFuture`** — новий прапорець (flag — стан) для майбутніх днів: банер з'являється один раз при відстеженні
- **Прибрано снуз** (`_bannerSnoozedUntil`) — замінено авто-таймером 5с

---

## ✅ Зроблено у поточній сесії (09.06.2026, фінал — hero + відстеження для всіх днів)

**fix(buses)** — відстежений рейс ЗАВЖДИ перший у hero-віджеті (сьогодні, завтра, будь-який день):

**Проблема:** `findActiveRoutes()` для сьогодні фільтрує рейси у межах 90 хв. Якщо відстежений рейс далеко (наприклад, через 4 год) — він не потрапляв у список, `findIndex` повертав `-1`, умова `ti > 0` не спрацьовувала → hero показував інший рейс.

**Фікс (рядки 360–367, `src/tabs/buses.js`):**
```js
if (trackedRouteId && _trackDate === getTodayISO()) {
  const ti = activeList.findIndex(r => r.id === trackedRouteId);
  if (ti > 0) {
    activeList.unshift(activeList.splice(ti, 1)[0]); // є в списку — піднімаємо
  } else if (ti === -1) {
    const tracked = all.find(r => r.id === trackedRouteId && r.status !== 'cancelled');
    if (tracked) activeList.unshift(tracked); // немає в списку — додаємо першим
  }
}
```

**Покриття всіх днів (зафіксовано назавжди):**
| День | Де обробляється | Логіка |
|------|----------------|--------|
| Сьогодні | рядок 360–367 | `_trackDate === getTodayISO()` → пріоритет навіть якщо >90 хв |
| Завтра / майбутні | рядок 337–339 | `_trackDate === busDay` → повертає `[tracked]` першим |

⚠️ **ЗАФІКСОВАНО — не змінювати без дозволу Вови**

---

## ✅ Зроблено у поточній сесії (08.06.2026, фінал — банер + відстеження)

### fix(buses) — банер відстеження: повний рефакторинг

**Логіка:**
- `findActiveRoutes()` — додано перевірку `trackedRouteId && _trackDate === busDay` перед `selectedRouteId` → відстежений рейс стає першим у hero для будь-якого дня (сьогодні/завтра/будь-який майбутній)
- `checkTrackNotifications()` — додано гілку для майбутніх днів (`_trackDate > getTodayISO()`): показує «Відстежується» банер з назвою маршруту, поважає снуз; для минулих — банер не активується
- При натисканні «Відстежити» новий рейс → `_bannerSnoozedUntil = 0` скидається → банер з'являється одразу, навіть якщо попередній був закритий ✕

**Дизайн банеру (фінальний стан — зафіксовано):**
- **Фон:** `radial-gradient(ellipse at center, rgba(250,251,253,0.94) → rgba(250,251,253,0.08))` — світлий синьо-сірий, центр непрозорий, краї прозорі
- **Розмір:** `min-width: 320px; max-width: 380px; height: 68px; border-radius: 22px`
- **Позиція:** `fixed; bottom: calc(88px + env(safe-area-inset-bottom,0px)); left: 50%; transform: translateX(-50%)`
- **Backdrop-filter:** `blur(24px) saturate(180%)` — скло-морфізм
- **Тінь:** `0 0 40px rgba(0,0,0,0.28), 0 0 20px rgba(0,0,0,0.20)` — рівномірна з усіх боків
- **Border:** `1px solid rgba(255,255,255,0.14)`
- **Анімація:** `opacity + translateY(20px) scale(0.96)` → `opacity:1 translateY(0) scale(1)`, `420ms cubic-bezier(0.22,1,0.36,1)`
- **Структура зліва направо:** іконка-кружечок → текстовий блок → кнопка ✕
- **Іконка-кружечок:** `42×42px; border-radius:50%; background: rgba(0,0,0,0.08)` — bookmark SVG `stroke="rgba(0,0,0,0.75)"` + бордова галочка `#722F37` `16×16px` знизу-справа
- **Текстовий блок** (зверху вниз):
  - `.btb-route` — `16px; font-weight:700; color:#000` — назва маршруту ВЕЛИКИМИ (КІВЕРЦІ → ОЛИКА)
  - `.btb-label` — `13px; font-weight:500; color:rgba(0,0,0,0.6)` — статус/таймер (Через X год Y хв)
- **Кнопка ✕:** `color: rgba(0,0,0,0.4); font-size:18px`
- **Снуз:** натиснути ✕ → `_bannerSnoozedUntil = Date.now() + 5*60*1000` (5 хв), відстеження залишається активним

**Статусні повідомлення банеру:**
| Стан | Верхній рядок (btb-route) | Нижній рядок (btb-label) |
|------|--------------------------|--------------------------|
| Майбутній день | МАРШРУТ → МАРШРУТ | Відстежується |
| Очікується (>15 хв) | МАРШРУТ → МАРШРУТ | Через X год Y хв |
| Очікується (≤15 хв) | МАРШРУТ → МАРШРУТ | Відправляється через X хв |
| В дорозі | МАРШРУТ → МАРШРУТ | Вже в дорозі |
| В дорозі + зупинка посадки (≤15 хв) | МАРШРУТ → МАРШРУТ | До ЗУПИНКА за X хв |
| Скасовано | МАРШРУТ → МАРШРУТ | Рейс скасовано |

- `sw.js`: CACHE_NAME → `cstl-20260608-2315`

---

## ✅ Зроблено у поточній сесії (08.06.2026, продовження)

**fix(vopas)** — сьогодні і завтра оновлюються кожні 30 хв:
- `tomorrow = today + timedelta(days=1)` — нова змінна перед циклом
- Умова кешу: `day > today` → `day > tomorrow` — завтра тепер завжди свіжий
- Захист від VOPAS не відповів: розширено з `day == today` на `day in (today, tomorrow)`
- Результат: 28 запитів кожні 30 хв замість 14; скасування завтрашніх рейсів видно одразу

---

## ✅ Зроблено раніше у поточній сесії (08.06.2026)

**fix(vopas)** — виправлено timeout (перевищення часу) у VOPAS parser:
- `parse_vopas.py`: майбутні видимі дні кешуються по-дню (`fetchedAt == today`), тільки сьогоднішній день запитується завжди. Типовий запуск: 14 запитів замість 196.
- `vopas-parser.yml`: `timeout-minutes` 5 → 15 (для cold-cache першого запуску).

**feat(buses)** — «Відстежити маршрут» (Level A — in-app):
- Кнопка «Відстежити маршрут» / «Відстежується ✓» внизу кожної сьогоднішньої картки рейсу.
- Банер `.bus-track-banner` — фіксований над таб-баром (`z-index: 890`, `bottom: var(--tabbar-h)`), `display: none` за замовчуванням, з'являється через `.visible` клас (не `hidden` атрибут).
- Стани банеру: «Відстежується X → Y · через N хв» → «відправляється через N хв» → «вже в дорозі» → «скасовано» → банер зникає після прибуття.
- Кнопка ✕ у банері → **снуз (відкладає банер) на 5 хвилин**, відстеження не скидається, іконка закладки залишається заповненою.
- Важливі події (автобус вирушив, ≤15 хв до відправлення, ≤15 хв до зупинки посадки, скасування) — скидають снуз і показують банер одразу.
- Скасований рейс — показується завжди, ігнорує снуз.
- `_notifiedWarning` — новий флаг (прапорець) для "≤15 хв до відправлення", зберігається у `localStorage`.
- `_bannerSnoozedUntil` — timestamp (unix ms) кінця снузу, зберігається тільки в пам'яті (скидається при перезавантаженні додатку).
- `forceShow` — локальний флаг у `checkTrackNotifications()`: ставиться в `true` при нових важливих подіях, щоб обійти снуз навіть якщо `_bannerSnoozedUntil` ще не скинуто.
- Відстеження зберігається у `localStorage['bus_track_v1']` + прив'язане до поточної дати.
- `checkTrackNotifications()` викликається при ініціалізації і кожні 60 с у таймері.
- **Іконка закладки перенесена всередину `.bus-card-main`** як третій flex-елемент (після `.bs-time-block` і `.bus-card-info`) — прибирає порожнє місце під основним блоком картки.
- `sw.js`: CACHE_NAME → `cstl-20260608-1711`.

**fix(buses)** — UI-полірування карток рейсу (08.06.2026 вечір):
- **Іконка закладки — позиція**: `align-self: flex-end` → кнопка `.bs-track-btn` вирівнюється по нижньому краю `.bus-card-main`, усуває надлишковий відступ зверху.
- **Роздільна лінія над «ВСІ ЗУПИНКИ»**: `margin-top: 6px` на `.bs-toggle` → лінія опустилась нижче; компенсація: `padding-bottom: 8px` (було 14px) на `.bus-card` — загальна висота картки не змінилась.
- **Нижній відступ під «ВСІ ЗУПИНКИ»**: `padding-bottom: 4px` (було 8px) на `.bs-toggle` → піднято нижній край кнопки.
- **Тривалість маршруту без переносу**: `<span style="white-space:nowrap">${durStr}</span>` — значення "1 год 12 хв" не розривається між словами, переноситься тільки цілим блоком.
- **Назва перевізника**: `data/schedule.json` — `"Волинська об"` → `"Волинська обл."`. `scripts/parse_vopas.py` — `re.sub()` нормалізує назву після завантаження з VOPAS.
- **Заголовок «РОЗКЛАД АВТОБУСІВ»**: відновлено у гілці `!toRender.length` функції `renderRouteList()` — `buildListTitleHtml()` додано перед кнопкою «Показати всі X рейсів».
- `sw.js`: CACHE_NAME → `cstl-20260608-1922`.

**fix(buses)** — пульс і відступ для всіх статус-крапок у hero-картці:
- `style/buses.css`: `margin-left: 5px` на `.bhv4-status-dot` — тінь пульсу більше не налазить на текст.
- Всі 3 стани тепер пульсують: `waiting` (сірий), `urgent` (бордо, було), `enroute` (зелений).
- `sw.js`: CACHE_NAME → `cstl-20260608-0828`.

---

## ✅ Зроблено у поточній сесії (10.06.2026, Level B push-сповіщення)

**feat(buses)** — Web Push Level B: справжні push-сповіщення навіть при закритому додатку:

- `sw.js`: `push` event listener → `showNotification()` + `notificationclick` → відкриває вкладку Автобуси
- `src/core/supabase.js`: `savePushSubscription()` + `deletePushSubscription()` — зберігає підписки у Supabase
- `src/tabs/buses.js`:
  - `VAPID_PUBLIC_KEY` константа (публічний ключ — safe у коді)
  - `urlBase64ToUint8Array()` — конвертація ключа для pushManager
  - `subscribeToPush()` — запитує дозвіл → підписує браузер → зберігає у Supabase
  - `unsubscribeFromPush()` — видаляє рядок з Supabase (браузерну підписку не скасовує)
  - Hook у track-кнопку картки рейсу: підписка при відстеженні
  - Hook у hero-картку: відписка при знятті через bookmark-іконку
- `scripts/supabase_push_schema.sql` — SQL для таблиці `push_subscriptions`
- `scripts/supabase_push_fix_rls.sql` — фікс RLS-політик (INSERT/UPDATE/DELETE для anon)
- `supabase/functions/send-bus-push/index.ts` — Edge Function (Deno)
- `scripts/check-imports.js`: додано `atob`, `btoa` до whitelist глобалів

### 🔑 ВАЖЛИВО — VAPID ключі

```
Public Key (у коді): BL6FKk0c_UoMo7TfJ17dlea2RCe2seP7amdebBb5SeomfXsH1k4UTWI10LPE9-ittx9Gzciudao7rMe9EciLeJo
Private Key: [ВИДАЛЕНО З ДОКУМЕНТА — раніше був тут відкритим текстом]
```
> ⚠️ **БЕЗПЕКА (11.06.2026):** приватний VAPID-ключ раніше зберігався тут
> відкритим текстом і потрапив в історію git. Його прибрано з файлу, АЛЕ
> редакція не чистить історію комітів. **Вова має РОТУВАТИ ключ:**
> `npx web-push generate-vapid-keys` → новий приватний у Supabase Secret
> `VAPID_PRIVATE_KEY`, новий публічний — у `VAPID_PUBLIC_KEY` (`src/tabs/buses.js`).
> Після ротації старий ключ стає недійсним.

### 🟢 Push-сповіщення — ПОВНІСТЮ НАЛАШТОВАНО (11.06.2026)

**Проблема яку вирішили:** `upsert` (INSERT ON CONFLICT DO UPDATE) в Supabase JS SDK вимагає SELECT-доступ для anon-ролі при перевірці UPDATE USING clause. Оскільки SELECT заборонений для anon — upsert падав з RLS-помилкою `42501`. Звичайний INSERT проходив без проблем.

**Фікс у `src/core/supabase.js`:** замінено `.upsert()` на `.insert()`, дублікати (error code `23505`) обробляються як `ok: true`.

**Edge Function `supabase/functions/send-bus-push/index.ts`:**
- Cron: `* * * * *` — кожну хвилину
- Точність: ±1 хв
- **4 типи сповіщень** (колонки у `push_subscriptions`):

| Колонка | Коли | Текст | Для кого |
|---|---|---|---|
| `notified_start` | Автобус вирушив з першої зупинки | `Автобус вирушив · 07:15 (Ківерці)` | Тільки проміжні зупинки |
| `notified_warning` | T-13..17 хв до зупинки посадки | `Відправляється через 15 хв · 07:45` | Всі |
| `notified_dep` | T-1..+1 хв (автобус на зупинці) | `Автобус на зупинці · Олика` або `Автобус відправляється · 07:15` | Всі |
| `notified_canc` | Статус рейсу = cancelled | `Рейс скасовано · 07:15` | Всі |

- `title`: `ЗУПИНКА_А → ЗУПИНКА_Б` (сегмент або назва маршруту, без емодзі)
- `from CSTL LIFE` — iOS додає автоматично (не можна прибрати/змінити)
- `dep_time` у БД = час прибуття автобуса на зупинку посадки користувача
- schedule.json завантажується один раз на тік cron для перевірки статусу та `departure_time` маршруту
- `dep_time` у БД = час прибуття автобуса на зупинку посадки користувача (не час відправлення з кінцевої)

**Supabase налаштування (вже зроблено):**
- Таблиця `push_subscriptions` ✅
- RLS-політики (push_insert, push_update, push_delete, push_select) ✅ — `scripts/supabase_push_fix_rls.sql`
- Edge Function `send-bus-push` задеплоєна ✅
- VAPID_PRIVATE_KEY у Secrets ✅
- pg_cron job (jobid: 4, schedule: `* * * * *`) ✅ — створено через SQL Editor

---

## 🔴 НАСТУПНЕ ЗАВДАННЯ (першочергове для нової сесії)

**Зворотні рейси у вкладці Автобуси** — парсинг і відображення рейсів «з боку Олики» (Олика → Луцьк, Олика → Ківерці тощо). Вова має надати список конкретних рейсів які потрібно додати. До отримання списку від Вови — нічого не змінювати.

**Push-сповіщення — можливі покращення (не термінові):**
- ~~Debug toast у `subscribeToPush()`~~ — прибрано 12.06 (сесія `vibrant-gates`)

**🟡 Задачі наступної сесії (push-сповіщення):**
1. **Підключити Supabase MCP до Claude** — щоб Claude міг сам запускати SQL, перевіряти таблиці, дебажити без участі Вови. Налаштовується у `Settings → Integrations → Supabase` перед стартом сесії.
2. **Відредагувати вигляд сповіщень** — протестувати всі 4 типи живими рейсами, відкоригувати текст і формат після реального тесту.

### Видимі вкладки (порядок у tab-bar)
**Автобуси** · **Дошка** · **ГРОМАДА** (центр, піднята кнопка) · **Події** · **Новини**

Світло — приховано з tab-bar 16.05 (наразі не актуально, код у `src/tabs/power.js` збережено, повертається розкоментуванням у `src/app.js` і у nav-меню).

### 🔒 ТАБ-БАР — ФІНАЛЬНІ ПАРАМЕТРИ (04.06.2026)

**Загальний таббар (`.tab-bar`):**
- `border-radius: 20px 20px 0 0` — заокруглені верхні кути
- `border-top: 1px solid var(--border)` — тонка сіра лінія зверху
- `box-shadow: 0 -4px 20px rgba(0,0,0,0.10), 4px 0 12px rgba(0,0,0,0.06), -4px 0 12px rgba(0,0,0,0.06), 0 4px 12px rgba(0,0,0,0.06)` — тінь з усіх чотирьох боків
- `overflow: visible` — щоб коло ГРОМАДИ виступало над баром

**Звичайні вкладки (`.tab-item`):**
- Іконка: 22×22px, підпис: 10px, font-weight 600, uppercase
- Неактивні: `color: var(--gray)`
- Активні: `color: var(--red)`

**Центральна кнопка ГРОМАДА (`.tab-item--home`):**
- `height: var(--tabbar-h)`, `justify-content: flex-end`, `padding-bottom: 8px`

**Коло (`.tab-home-circle`):**
- Розмір: `52×52px`, `border-radius: 50%`
- Колір: `background: var(--red)`
- Позиція: `position: absolute; bottom: 28px`
- Тінь неактивна: `box-shadow: 0 4px 14px rgba(114,47,55,0.5), 0 -5px 18px rgba(114,47,55,0.25)`
- Тінь активна: `box-shadow: 0 5px 14px rgba(114,47,55,0.6), 0 -6px 18px rgba(114,47,55,0.25)`
- `z-index: 1001`

**Іконка замку всередині кола:**
- Файл: `icons/castle-icon.png`
- Розмір: `77×77px`, `object-fit: contain`
- `overflow: hidden` на колі — обрізає іконку по формі кола

**Біла дуга зверху кола (`::after`):**
- `width: 58px; height: 58px; border-radius: 50%`
- `border: 3px solid var(--white)`
- `position: absolute; bottom: 25px` — центрується по колу
- `clip-path: inset(0 0 31px 0)` — обрізає рівно на рівні верху таббару
- `z-index: 1000` (нижче кола, щоб тінь кола перекривала основу дуги)

**Підпис ГРОМАДА (`.tab-label--home`):**
- `font-weight: 800`, `font-size: 12px`, `letter-spacing: 0.5px`, uppercase
- Колір: успадковує від `.tab-item` (сірий/бордовий як всі інші)

### Дизайн
**D2 «Поле»** — натуральні землі (paper/sky/mint/honey/peach), кольорові градієнти блоків, м'які тіні, радіус 22px. **Бренд бордо `#722F37`** (theme-color iOS, splash, лого, активна вкладка, CTA). CSTL NEWS → CSTL LIFE у всіх user-facing.

---

## 🚌 Модуль «Автобуси» — фінальний стан (08.06.2026)

> Весь модуль розроблявся з 05.06 по 08.06.2026. Нижче — повний актуальний стан.

---

### 🖼️ Hero-картка (блок відстеження поточного рейсу)

- Фонове фото `images/bus-hero2.png` + темний оверлей-градієнт
- Статус-крапка (CSS `<span>`, не emoji): `waiting` сірий / `urgent` бордовий пульсуючий / `enroute` зелений
- Назва маршруту ВЕЛИКИМИ ЛІТЕРАМИ (ЛУЦЬК → ЛИЧАНИ) + капсула часу відправлення
- Smooth-swipe між рейсами: статичні елементи (іконка, рамка капсули) не мигають — тільки `.bhv4-dyn` (час, статус, назва) плавно fade in/out
- Шкала прогресу A→B внизу картки: пройдені крапки зелені, поточна — пульсуюча
- Мітки A/B оновлюються при кожному свайпі між рейсами
- Скасовані (`cancelled`) рейси — не потрапляють у hero
- Бейдж «ЧЕРЕЗ X ХВ» — бордовий, з'являється за ≤15 хв до відправлення

---

### 📅 Календар (тижнева смужка)

- 2 тижні з свайпом: page 0 = поточний тиждень, page 1 = наступний
- `.bus-week-track` — контейнер 200%, позиція через `translateX(-${weekPage * 50}%)`
- Свайп пальцем: анімація `cubic-bezier 0.28s`, горизонтальний рух блокує вертикальний скрол
- Дві крапки-навігатори під тижнями (активна — бордова)
- **Активний день** (фінал): `background: #E1E4EC`, `border-color: #C4C9D6`, `border-radius: 8px` — без `box-shadow` (прибрано — виходив за межі rounded corners)
- **Сьогодні + активний**: ободок залишається бордовим (`border-color: var(--red)`)

---

### 🔍 Пошук маршруту (поля «Звідки / Куди»)

- Поле «Звідки» — червона крапка; «Куди» — SVG пін; кнопка swap ← →
- Кнопка «✕ СКИНУТИ ФІЛЬТР» з'являється коли є активний фільтр
- Зупинки у dropdown — тільки з обраного дня (не з усіх 14 днів як раніше)
- `matchesSearch()` — km-based перевірка напряму (не по порядку у JSON)
- **anySegment логіка** — якщо хоч одна вибрана зупинка ≠ кінцева точка маршруту → картка показує сегментну назву (напр. «ОЛИКА → ЛУЦЬК»). Працює для ВСІХ типів пошуку (одна зупинка або обидві)
- Під сегментною назвою — дрібний бордовий підзаголовок з повним маршрутом (напр. «КІВЕРЦІ → ЛУЦЬК»)

---

### 🃏 Картка рейсу

- Час відправлення і прибуття — однаковий розмір 20px, моноширний шрифт
- **Назва маршруту** при фільтрі: заголовок чорний (сегмент), підзаголовок бордовий (повний маршрут)
- **«Орієнтовно: X год Y хв»** — перед тривалістю у метаданих картки; значення часу обгорнуте у `<span style="white-space:nowrap">` — не розбивається між словами
- **Назва перевізника** — два рядки: «Черняк П.К.-43018» / «Волинська обл.»; вирівнювання `align-items: flex-start` (перший рядок на рівні часу)
- Бейдж «ЧЕРЕЗ X ХВ» у правому куті — з'являється за ≤15 хв
- Майбутні рейси завжди вище минулих; минулі — opacity 0.32
- Скасовані залишаються у списку до відправлення, потім → минулі
- **Іконка закладки (відстежити)** — всередині `.bus-card-main` як 3-й flex-child; `align-self: flex-end` — притиснута до нижнього краю рядка; `margin-left: auto` — витісняє до правого краю
- `.bus-card`: `padding: 14px 16px 8px` (зменшено `padding-bottom` 14→8px для компенсації `margin-top` на `.bs-toggle`)
- `.bs-toggle`: `margin-top: 6px; padding: 8px 16px 4px` (було `margin-top: 0; padding: 8px 16px 8px`)

---

### 📍 Список зупинок (акордеон)

- Кнопка «ВСІ ЗУПИНКИ ▾» / «СХОВАТИ ЗУПИНКИ ▴» — ВЕЛИКІ ЛІТЕРИ, трикутник окремий `<span>` 20px, вертикально відцентрований
- **Проміжні зупинки**: 13px, font-weight 500, чорний колір
- **Зупинки А і Б** (обрані пунктом пошуку): font-weight 700, `background: #E1E4EC`
- Смужки А/Б розтягнуті до країв контейнера (без бічних відступів — `padding: 0`)
- **Тільки зовнішні рамки**: А — `border-bottom: 1.5px solid #C4C9D6`, Б — `border-top: 1.5px solid #C4C9D6`
- Заокруглені зовнішні кути: Б — `border-radius: 8px 8px 0 0`, А — `border-radius: 0 0 8px 8px`
- **Плавний градієнт** на внутрішньому краї: `linear-gradient(to bottom, #E1E4EC 45%, #f5f5f5 100%)` для Б і навпаки для А — плавний перехід у фон
- **Контейнер** `.bs-stops-body`: `border-radius: 8px 8px 12px 12px` (верхні кути = 8px, як у смужки Б)
- Іконки зупинок: ● (Звідки), SVG-пін (Куди), 🟢 пульс (поточна), ▷ (наступна), SVG-пін (кінцева)
- Ціна за сегмент у кожній зупинці

---

### 🗺️ Іконка вкладки «Автобуси» у tab-bar

SVG пін (маркер місця) — такий самий як іконка кінцевої зупинки у списку зупинок

---

### 🔤 Фікс назви «Гараджа» → «Гаразджа»

- `data/schedule.json` — всі входження виправлено
- `data/route-stops.json` — всі входження виправлено
- `src/tabs/buses.js` — `STOP_ALIASES = { 'Гараджа': 'Гаразджа' }` runtime-нормалізація після завантаження JSON (захист від кешу старих даних у браузері)

---

### ⚙️ Парсер VOPAS (`scripts/parse_vopas.py`)

- **Cron**: `*/30 * * * *` (кожні 30 хвилин), GitHub Actions
- **21-денне вікно**: `get_21_days()` — 3 тижні від Пн поточного
  - **Видиме вікно** (дні 0–13 = 2 тижні у UI): оновлюється КОЖНІ 30 ХВ
  - **Буфер** (дні 14–20 = 3-й тиждень, в UI не видно): завантажується один раз, потім кеш
  - Поріг: `monday + 13 днів` (кінець другого видимого тижня)
  - **Навіщо**: у неділю парсер вже зберігає дані наступного тижня → коли настає понеділок і календар зсовується — порожніх дат немає
- **Sticky cancellation**: скасований рейс залишається `cancelled` навіть якщо VOPAS прибрав плашку — якщо вже минув час відправлення
- **Захист від обнулення**: якщо VOPAS не відповів для сьогодні → зберігаємо попередні дані
- **`schedule.json`** структура: `{ days: { "2026-06-08": { routes: [...], fetchedAt, fetchedTime } } }`
- **Cache-bust**: `schedule.json?v=${Math.floor(Date.now()/60000)}` — CDN не кешує старі дані

---

### 🔜 Залишилось (наступна сесія)

**🔴 Першочергове — зворотні рейси «з боку Олики»**
- Парсинг маршрутів Олика→Луцьк, Олика→Ківерці тощо
- Вова має надати список конкретних рейсів
- **До отримання списку — нічого не змінювати**

**🟡 Уточнення відстаней між зупинками**
- Вручну через Google Maps виміряти км між кожною зупинкою кожного маршруту
- Зараз `km` у `data/route-stops.json` — приблизні → час прибуття на проміжних зупинках неточний
- Після вимірювання — оновити JSON вручну

**🟢 Банер відстеження — ПОВНІСТЮ ЗАВЕРШЕНО**
- Level A ✅ + Dynamic Island анімація ✅ — зафіксовано Вовою 09.06.2026
- Банер розширюється з іконки ГРОМАДА знизу вгору — `translateY(41px) scale(0.15,0.15)` → `scale(1)`

**🟢 Рівень B «Відстежити маршрут» — push-сповіщення ПРАЦЮЄ (11.06.2026)**
- Підписка зберігається у `push_subscriptions` ✅
- Cron `* * * * *` запущений ✅
- Push-сповіщення надходить за 9-11 хв до відправлення ✅ — протестовано Вовою
- Формат: `ЛУЦЬК → РІВНЕ` / `Відправляється через 9 хв\n17:15 · Луцьк Рівне`

**🟢 Пізніше**
- Кілька старих гілок `claude/*` на GitHub — прибрати через GitHub UI

---

## 📅 Чим закінчилась минула сесія (16-17.05.2026)

Усі 6 Tier-ів редизайну (план з `docs/REDESIGN_OTHER_TABS_VISION.md`) завершені:

- **Tier 0** — бежевий фон body + м'які тіні замість рамок (4 вкладки)
- **Tier 1 ⚡ Світло** — приховано з tab-bar
- **Tier 2 🚌 Автобуси** — `filter-bar` `position: fixed`, зелений `#6F8E51`
- **Tier 3 📞 Контакти** — HERO «Швидка 103» з пульсуючим ореолом + 2×2 аварійні + 2 місцеві
- **Tier 4 📝 Подати оголошення** — категорії-чіпи (8 pill) + 3 фото-слоти (canvas q0.78) + live-preview
- **Tier 5 📅 Події** — календарна стрічка 21 день + 25 свят 2026 (`data/holidays.json`) + cover_emoji+gradient. Дати за новим стилем (ПЦУ реформа 2023): Різдво 24.12, Йордан 6.01, Великдень 12.04, Трійця 31.05
- **Tier 6 📰 Новини** — magazine-cover featured (Georgia serif 22px, text-shadow) + 8 CATEGORY_COLORS + 4 GEO_COLORS + золотий ексклюзив з ⭐

Інше за сесію: Дошка як окрема вкладка з Polaroid-фото і modal zoom, рідизайн бренду червоний → бордо, CSTL NEWS → CSTL LIFE, лічильник версії перенесено у шапку, auto-merge BUILD_NUM фікс (`--no-merges`), iOS Safari zoom-fix (font-size 16px на inputs).

**Гілка яку змерджили:** `claude/startup-ui-module-GWlEk` (закрита через `/finish` → auto-merge в `main`).

---

## 🟢 Фаза 9 Спринт 1 — ЗАВЕРШЕНО (18.05.2026)

Supabase production-live з реальною модерацією, реалтайм-синхронізацією і
підключеними фронт+адмінкою. Дошка громади 2.0 повноцінно працює.

### Підсумок 36 комітів за 17-18.05.2026

**Supabase інфраструктура:**
- Проект `uabyfecseqnemvcqhdem.supabase.co` (Frankfurt, Free) піднятий
- SQL-схема: `posts`, `announcements`, `ads`, `ad_events`, `admins`,
  `comments`, `reactions` + RLS policies + Storage bucket
- 5 SQL-патчів у `scripts/`:
  - `supabase_schema.sql` — повна ідемпотентна схема
  - `supabase_fix_rls.sql` — фікс catch-22 (рекурсивний `auth.email() IN admins`)
  - `supabase_comments_reactions.sql` — таблиці + cover_emoji/cover_gradient
  - `supabase_realtime.sql` — `ALTER PUBLICATION supabase_realtime ADD TABLE`
  - `supabase_set_admin_password.sql` — bcrypt пароль через SQL без email
- `scripts/migrate_to_supabase.py` — одноразова міграція JSON → posts/announcements
- Whitelist 2 адмінів у `admins`: haranin.ukraine + volodymyrshevchuk19

**Адмінка `/admin.html`** (окрема сторінка, Supabase SDK з CDN):
- Email+password auth (signInWithPassword) — спочатку був magic-link
  але впирався у Supabase rate limit (~4 листи/год на Free)
- Forgot password через magic-link → нова сторінка → Налаштування
- **6 табів:** 📋 Модерація (pending) / 🏛️ Оголошення / 📰 Опубліковані пости / 💬 Коментарі / 👥 Адміни / ⚙️ Налаштування
- Кнопка «← На сайт» (без logout, сесія тримається ~30 днів)
- Прихований вхід: 5 тапів на лого `CSTL LIFE` у шапці → /admin.html

**Реальні дані у Дошці громади 2.0:**
- localStorage реакції/коментарі → Supabase БД (всі юзери бачать спільно)
- Анонімні юзери ідентифікуються через UUID у localStorage
- Single emoji per user per post (UNIQUE post_id+user_id)
- Топ-3 emoji + лічильники на тригері, окремі лічильники у попапі
- **Supabase Realtime** через WebSocket — лайки/коментарі оновлюються
  миттєво у всіх юзерів без перезавантаження
- Optimistic updates — миттєвий UI-feedback, фоном sync з БД

**UX/UI Дошки:**
- Свайп hero фото Олики (3 фото, плавна fade 0.55s)
- Свайп міні-блоку Дошки на Громаді: 4 типи (Офіційні/Дошка/Розмови/Вітання)
- Плавна slide-in анімація (0.6s cubic-bezier easeOutQuint)
- CTA «Перейти на ...» з підхопленням активного типу
- Таб «Усі» → «Актуальні» (фільтр пости за останні 3 доби)
- Фіксований pos-bar (search+tabs+chips) — sticky→fixed (iOS Safari fix)
- iOS Safari zoom-fix для inline коментарів (font-size 14→16)

**Стиль Автобусів — серйозний табло-стиль (УЗ-станція):**
- Темний #2F3E36 фон, SF Mono бурштин для часу (#FBBF24)
- «ЧЕРЕЗ X ГОД X ХВ» — капсула з border + monospace
- border-radius 22→12, прибрано Georgia serif
- Urgent state #6B1F2A замість рожевого
- Той самий стиль перенесено у блок Громади (cm-block--bus)

**Реакції — UI:**
- 4 emoji завжди показуються → один тригер «🙂+» з попапом 8 emoji
- На тригері: топ-3 emoji з окремими лічильниками + моя виділена
- SVG-іконки save/share замість emoji (видно на всіх кольорах стікерів)
- Кнопка коментарів → inline-форма (без модалки) тільки у chat/greeting
- На board: тільки реакція в куті, save/share — у zoom-modal

**Безпека:**
- Service_role key випадково leak → ROTATE через Supabase Dashboard
- В код тільки publishable (anon) — RLS policies захищають усе
- Admin-функції через `is_admin()` SECURITY DEFINER (обходить catch-22)

**Аналітика:** GoatCounter `cstl-life.goatcounter.com` (безкоштовно)

**Фікси:**
- formatTime для null/ISO string (раніше `new Date(null) = 1 січня 1970`)
- postTime() helper — fallback ts → published_at → created_at
- cover_emoji+cover_gradient додано у posts (Вітання тепер публікуються)

### 🔒 ДОШКА — FAB кнопка (05.06.2026)
- Кнопка «Подати оголошення» → кругла FAB (Floating Action Button — плаваюча кнопка) ✏️
- `position: fixed`, `right: 18px`, `bottom: tabbar + 14px`
- Розмір: `56×56px`, `border-radius: 50%`
- Текст прихований, тільки іконка олівця по центру
- Клас: `.board-trigger--fixed` у `style/community.css`
- **⚠️ МЕНЮ ЗАФІКСОВАНО НАЗАВЖДИ — не змінювати розміри/позиції таббару**

### 🔜 Що далі (за пріоритетом)

**🔴 Найкорисніше зараз** (тільки моя робота):
1. **Фаза 9 Спринт 2** — Supabase Storage + upload фото з форми (зараз фото зберігаються як base64 — текст у БД, важко і повільно).
2. **Табло-стиль для Світла** — відкладено доки вкладка прихована з tab-bar. Дизайн-токени `.tablo-*` готові, при поверненні Світла буде швидко.

**🟡 Розвиток (потребує дій Вови):**
3. Сторінка `/реклама` — статичний прайс + контактна форма (потрібен email)
4. Перевстановити PWA на iPhone (іконка CSTL NEWS → CSTL LIFE)
5. 5 стратегічних питань з `docs/PRODUCT_STRATEGY.md`

**🟢 Опційно:**
6. Cross-fade для дошки (2 контейнери одночасно як у hero)
7. Cleanup 11 старих гілок на GitHub
8. Купити власний домен `olyka.news` / `cstl.news`

### 🟡 Незабаром
4. **Pre-revenue groundwork** — Plausible.io або Goatcounter (аналітика DAU/MAU). Без цифр нема перших переговорів з рекламодавцями. Деталі: `docs/MONETIZATION.md`
5. **Web Share API** — `sharePost()` у `utils.js` + кнопка 📤 на статтях, папірцях, подіях. Деталі: BACKLOG секція «Стратегія віральності»
6. **CSTL LIFE icon на робочому столі iPhone** — Вова має перевстановити PWA щоб назва оновилась з CSTL NEWS (iOS кешує при install)
7. **Реальні фото свят** — або Unsplash API з ключем, або руками у `images/holidays/`. Wikipedia блокує hotlinking з 403

### 🟢 Пізніше
8. **Cleanup 10 старих гілок** на origin (9 `claude/*` + 1 `codex/*`) — через GitHub UI
9. **cron-job.org** — точніший 30-хв cron для RSS-парсера (GitHub безкоштовний має lag 5-30 хв)
10. **Нові волинські RSS-джерела** замість видалених Суспільне Волинь / Укрінформ

---

## 🔔 НАГАДУВАННЯ ВОВІ

У `docs/PRODUCT_STRATEGY.md` секція «Стратегічні питання до Вови» — **5 питань** які блокують стратегічне планування:

1. Чи готовий зробити особисті візити поіменно? (голова сільради, директори шкіл, ФАП, церкви)
2. Чи включати друковану газетку як стратегічний інструмент?
3. Чи ставитись до додатку «офіційно» (зобовʼязання, не хобі)?
4. Чи ОК ризик конфлікту з владою? Незалежний рупор чи рупор сільради?
5. Чи готовий шукати співавтора-редактора з громади?

Відповіді розблоковують Фази 10-13 у ROADMAP.

---

## 🔒 ЗАФІКСОВАНО — НЕ ЗМІНЮВАТИ БЕЗ ДОЗВОЛУ ВОВИ

> Правило: якщо зміна зачіпає будь-який з пунктів нижче — спочатку сказати Вові, отримати дозвіл, тільки тоді змінювати.

### Модуль «Відстеження рейсу» (`src/tabs/buses.js`)

1. **`findActiveRoutes()` — пріоритет відстеженого рейсу**
   - Сьогодні: `_trackDate === getTodayISO()` + `ti === -1` → додає рейс з `all` (навіть якщо >90 хв)
   - Майбутні дні: `_trackDate === busDay` → повертає `[tracked]` першим
   - **Причина:** без цього hero показує випадковий рейс замість відстежуваного

2. **`checkTrackNotifications()` — банер для всіх днів**
   - Майбутні дні (`_trackDate > getTodayISO()`): показує «Відстежується» + назва маршруту
   - Сьогодні: повна логіка таймера + сповіщень
   - Минулі дні: кнопка «Відстежити» взагалі не рендериться

3. **Скид снузу при новому відстеженні**
   - `_bannerSnoozedUntil = 0` при кожному натисканні «Відстежити»
   - **Причина:** без цього банер не з'являвся після закриття ✕

4. **`isPastRoute()` — сірі картки в минулих днях**
   - Перша перевірка: `if (busDay < getTodayISO()) return true`
   - **Причина:** без цього вчорашні картки виглядали як сьогоднішні

### Банер відстеження (`.bus-track-banner`, `style/buses.css`)

5. **Фінальний дизайн банеру — зафіксовано** (деталі у секції «Модуль Автобуси»)
   - Синьо-сірий фон, рівномірна тінь, бордова галочка, текст чорний
   - Структура: іконка → [маршрут жирно / таймер дрібно] → ✕

### Парсер VOPAS (`scripts/parse_vopas.py`)

6. **Sliding window — сьогодні і завтра оновлюються кожні 30 хв**
   - `today = now_kyiv.date()` (не UTC!)
   - `tomorrow = today + timedelta(days=1)`
   - Кеш: `day > tomorrow` (не `day > today`)
   - **Причина:** між 00:00–03:00 Київ UTC-дата відстає на день

---

## ⚙️ Ключові архітектурні рішення (не змінювати без обговорення)

1. **А+ деплой** — `actions/deploy-pages@v4`, нічого не комітить у `main` з CI
2. **`bundle.js` у git** як робоча база (варіант Б)
3. **Двогілковий потік** — робота на `claude/start-session-XXX`, мердж у `main` через `/finish`
4. **Хук `stop-hook-git-check.sh`** — не чіпати (фіча, не баг)
5. **Лічильник версії** — `sed` у `deploy.yml`, формат `v{N} · DD.MM HH:MM` (Київ), у шапці біля погоди
6. **CACHE_NAME у `sw.js`** — bump при КОЖНІЙ зміні `src/`, `index.html`, `style*`, `sw.js`. Чисто `.md`/`.claude/` зміни — НЕ чіпати
7. **Автобуси — зупинки з офіційних квитків VOPAS** — не вигадувати km/ціни
8. **RSS-парсер** — cron 30 хв (`0,30 * * * *`), ліміт 15/джерело, MAX_ARTICLES=150
9. **`.filters-bar` — `position: fixed`** (не sticky!) — баг iOS Safari з `height:100%` на body
10. **Auto-merge BUILD_NUM** — `git rev-list --count --no-merges HEAD` (інакше +2 на merge-комітах)
11. **`data-tab` на `.app-main`** — виставляється при старті і при кожному `switchTab()`. CSS використовує для overscroll фону.

---

## 📰 RSS-парсер — поточний стан

### Активні джерела (7 штук)
| Джерело | Geo | Тип | Статус |
|---------|-----|-----|--------|
| Волинь Post | Волинь | RSS | ✅ Працює |
| Конкурент | Волинь | RSS | ✅ Працює |
| Українська правда | Україна | RSS | ✅ Активно поповнює |
| Українська правда (Світ) | Світ | RSS | ✅ Активно поповнює |
| Район.Ківерці /tags/olika | Олика | HTML | ✅ Чекає нових постів |
| Олицька громада `/news/` | Олика | gromada | ✅ Через Cloudflare Worker |
| Олицька громада `/ogoloshennya-...` | Олика | gromada | ✅ Через Cloudflare Worker |

### Видалені (нероботоздатні)
| Джерело | Причина |
|---------|---------|
| Суспільне Волинь | DNS-блок з GitHub Actions |
| Укрінформ Волинь | Зламаний XML |
| Укрінформ Світ | Зламаний XML |

### Cloudflare Worker (проксі)
- **URL:** `https://cstl-proxy.volodymyrshevchuk19.workers.dev`
- **Акаунт:** `volodymyrshevchuk19@gmail.com`
- **Код:** `cloudflare/worker.js`
- **Навіщо:** GitHub Actions IP блокується сайтом громади → Worker має інші IP

### Логіка парсера
- `fetch_rss()` — Chrome BROWSER_UA + `response_headers` → `feedparser.parse(raw, response_headers=...)`
- Дедуплікація: `sourceUrl` + нормалізований заголовок
- `content:encoded` → якщо < 600 символів → `fetch_full_article()`
- `classify_entry()` → EVENT_KEYWORDS + майбутня дата → `events.json` або `articles.json`
- Cron `0,30 * * * *` (GitHub безкоштовний lag 5-30 хв)

---

## 🗞️ Модалка новини — стан

| Функція | Статус |
|---------|--------|
| Sticky-header (рисочка + × + гео•категорія) | ✅ |
| `top: var(--header-h)`, не перекриває шапку | ✅ |
| Свайп вниз → закрити | ✅ |
| Клікабельне джерело | ✅ чорне, `font-weight: 600` |
| Повна стаття | ✅ `content:encoded` + `fetch_full_article()` |
| Футер «Автор публікації» + «Читати оригінал →» | ✅ |
| Magazine-cover featured (Tier 6) | ✅ Georgia serif 22px, text-shadow, плавніший градієнт |
| Кольорові бейджі (Tier 6) | ✅ 8 CATEGORY_COLORS + 4 GEO_COLORS + золотий ексклюзив |

---

## 🔧 Хуки Claude Code

| Хук | Подія | Що робить |
|-----|-------|-----------|
| `~/.claude/stop-hook-git-check.sh` | Stop | Нагадує закомітити |
| `.claude/hooks/pre-edit-read-check.js` | PreToolUse Edit | Блокує Edit без попереднього Read (exit 2) |
| `.claude/hooks/cache-name-reminder.sh` | PostToolUse Edit/Write | Нагадує bump CACHE_NAME після критичних файлів |
| `.claude/hooks/context-warning.sh` | UserPromptSubmit | ⚠️ 800K / 🔴 900K токенів |
| `.claude/settings.json` PostToolUse | Edit/Write | `node --check` для `.js` |
| `scripts/check-imports.js` | у `build.js` | Перевірка імпортів перед esbuild |

---

## ⚡ Модуль «Світло» — стан

- `data/power.json` — 11 міст/сіл Олицької ОТГ, DEMO дані
- Приховано з tab-bar 16.05 (код збережено, повертається розкоментуванням)
- Потребує Supabase для реальних даних (Фаза 3)

## 📋 Модуль «Дошка громади 2.0» — стан (17.05.2026)

- **5 табів зверху:** 🔄 Усі · 🛒 Дошка · 💬 Розмови · 🎉 Вітання · 💾 Мої
- **3 типи карток:**
  - `board` — стікер на корку (як було), з категоріями (продам/куплю/...)
  - `chat` — горизонтальна картка з аватаркою (буква з імені у кольоровому кружечку), текстом, хештегами
  - `greeting` — святкова картка з emoji-обкладинкою (8 пресетів) + «Кому» + текст
- **Пошук** зверху (debounce 180мс) — по text/title/author/tags
- **Категорії-чіпи** — другий ряд для табу board (9 категорій)
- **Реакції (новий UI)** — кнопка-капсула «🙂+» зліва під карткою → тап відкриває popup з 8 emoji. Single emoji per post у `localStorage['cstl-reactions-v1']`.
- **Коментарі** — кнопка «💬 N» → bottom-sheet модалка. `localStorage['cstl-comments-v1']`.
- **Збережені** — SVG-іконка закладки (outline → filled+бордовий при saved) → таб «Мої»
- **Share** — SVG-іконка iOS-style.
- **Submit-форма з перемикачем типу:** board/chat/greeting + різні поля + LIVE-preview для всіх 3
- **Backend:** Supabase `posts` таблиця

## 🔐 Адмінка `/admin.html` (актуальне на 18.05.2026)

Окрема сторінка для модерації Дошки. Inline CSS+JS, Supabase SDK з CDN. Адреса: `https://volodymyr221.github.io/CSTL_NEWS/admin.html`.

- **Email+password login** через `supa.auth.signInWithPassword()`
- **6 табів:** 📋 Модерація / 🏛️ Оголошення / 📰 Опубліковані / 💬 Коментарі / 👥 Адміни / ⚙️ Налаштування
- **Прихований вхід:** 5 тапів на лого `CSTL LIFE` → /admin.html

---

## 📚 Стратегічні документи (контекст продукту)

- `docs/PRODUCT_STRATEGY.md` — «цифрова інфраструктура громади» (як Дія для Олики), 5 груп, 3 рівні цінності, 4 hook-механізми
- `docs/COMMUNITY_BOARD_VISION.md` — Дошка 2.0 «OLX для громади», 5 спринтів, стратегія віральності
- `docs/MONETIZATION.md` — 4 джерела доходу, етапи 2026-2027, схема `ads`, принцип «юзери > монетизація»
- `docs/REDESIGN_OTHER_TABS_VISION.md` — План редизайну (Tier 0-6 завершено), 12 правил для бабусь

Нові фази у ROADMAP:
- Фаза 8.5 — Редизайн усіх вкладок (Tier 0-6) ✅
- Фаза 9 — Дошка громади 2.0 (Supabase + 3 типи постів)
- Фаза 10 — Інфраструктура громади (Влада / Документи / Установи / Церква / Бізнеси / Тарифи / Соцпідтримка)
- Фаза 11 — Спільнота (опитування, краудфандинг, хроніка, коментарі)
- Фаза 12 — Push + AI (Firebase + Telegram-бот)
- Фаза 13 — Друк і офлайн (газетка + QR-коди)

# Стан сесії — CSTL NEWS

**Оновлено:** 2026-04-18 (денна сесія) | **Архів попередніх сесій:** `_ai-tools/SESSION_ARCHIVE.md`

---

## Проект

| | |
|--|--|
| **URL сайту** | https://volodymyr221.github.io/CSTL_NEWS/ |
| **Репозиторій** | https://github.com/Volodymyr221/CSTL_NEWS |
| **Активна гілка** | `main` |
| **Власник** | Вова Шевчук (GitHub: Volodymyr221) |

---

## 🟢 Поточний стан

- ✅ Сайт відкривається, 4 вкладки працюють
- ✅ Лічильник версії внизу екрану, автодеплой через GitHub Actions
- ✅ **Погода** — назва міста по GPS (Nominatim reverse geocoding)
- ✅ **Модуль «Новини»** — RSS-парсер кожні 30 хв, **8 джерел** (додано Район.Ківерці)
- ✅ **Модуль «Новини» — модалка** — повна стаття через `content:encoded` + `fetch_full_article()`, scroll lock, клікабельне **чорне** посилання, декодування HTML entities, футер: «Автор публікації» + «Читати оригінал»
- ✅ **Модуль «Новини» — класифікатор** — автоматичний розподіл на `articles.json` та `events.json`
- ✅ **Модуль «Події» v3** — акордеон, кольоровий ободок, `.ics` нагадування
- ✅ **Модуль «Автобуси»** — пошук Звідки→Куди, акордеон зупинок, smart-рядок, дні тижня. **Без** en-route трекінгу (відкочено за запитом Вови)
- ✅ **Модуль «Світло»** — вибір міста/вулиці (11 сіл Олицької ОТГ), таймлайн, `.ics`-експорт
- ✅ **Хук контексту** — попередження при 800K/900K токенів у сесії

---

## ⚙️ Ключові архітектурні рішення (не змінювати без обговорення)

1. **А+ деплой** — `actions/deploy-pages@v4`, нічого не комітить у `main` з CI
2. **`bundle.js` у git** як робоча база (варіант Б)
3. **`main` як робоча гілка** — пуш = автодеплой
4. **Хук `stop-hook-git-check.sh`** — корисна фіча, не чіпати
5. **Лічильник версії** — `sed` у `deploy.yml`, формат `v{N} · DD.MM HH:MM`
6. **CACHE_NAME у `sw.js`** — оновлювати при КОЖНІЙ зміні статичних файлів. **Остання: `cstl-20260418-1545`**
7. **Автобуси — зупинки з офіційних квитків VOPAS** — не вигадувати km/ціни без скринів
8. **RSS-парсер** — `scripts/parse_rss.py`, cron кожні 30 хв, дедуплікація за URL + заголовком
9. **Не чіпати зайвий код** — при змінах торкатись ТІЛЬКИ того що явно обговорюється

---

## 📰 RSS-парсер — поточний стан

### Джерела (8 штук)
| Джерело | Geo | Тип | Фільтр |
|---------|-----|-----|--------|
| Суспільне Волинь | Волинь | RSS | немає |
| Волинь Post | Волинь | RSS | немає |
| Конкурент | Волинь | RSS | немає |
| Укрінформ Волинь | Волинь | RSS | немає |
| Українська правда | Україна | RSS | NATIONAL_KEYWORDS |
| Укрінформ Світ | Світ | RSS | WORLD_KEYWORDS |
| Українська правда (Світ) | Світ | RSS | WORLD_KEYWORDS |
| **Район.Ківерці /tags/olika** | **Олика** | **HTML** | немає |

### Логіка
- **Дедуплікація:** за `sourceUrl` + нормалізований заголовок
- **`strip_html()`** — замінює теги пробілом + `html.unescape()` (декодує `&laquo;` → «)
- **Повний текст:** `content:encoded` з RSS → якщо < 600 символів → `fetch_full_article()` зі сторінки
- **`fetch_full_article()`** — CSS-селектори для кожного домену + fallback на найбільший текстовий блок
- **Класифікатор:** `classify_entry()` → `EVENT_KEYWORDS` + майбутня дата → `events.json` або `articles.json`
- **Ліміт:** 100 статей в `data/articles.json`, 50 у `data/events.json`
- **Cron:** кожні 30 хв (`*/30 * * * *`)
- **Concurrency:** окрема група `rss-parser` — не конфліктує з `deploy.yml`

### Запланований (не реалізований)
- **olytska-gromada.gov.ua** — заблокований по IP з GitHub Actions. Потрібен Cloudflare Worker (файл `cloudflare/worker.js` є, але потрібен CF акаунт Вови для деплою)

---

## 🗞️ Модалка новини — поточний стан

| Функція | Статус |
|---------|--------|
| Scroll lock фону при відкритті | ✅ `document.body.style.overflow = 'hidden'` |
| Клікабельне джерело (byline) | ✅ `<a class="article-byline-link">` — **чорне**, не синє |
| Декодування HTML entities | ✅ `decodeEntities()` через `textarea` trick |
| Повна стаття з RSS | ✅ `content:encoded` + `fetch_full_article()` fallback |
| Блок «анонс RSS» | ✅ з'являється тільки коли текст < 600 символів після всіх спроб |
| Заголовок не перекривається | ✅ `top: var(--header-h)` замість `inset: 0` |
| Затемнення шапки при відкритті | ✅ `body.modal-open .app-header { filter: brightness(0.55) }` |
| Плавна анімація відкриття | ✅ `@keyframes modalSlideUp` iOS-стиль |
| Свайп вниз для закриття | ✅ тільки від ручки (`.modal-handle`), RAF 60fps |
| Футер статті | ✅ «Автор публікації: [видання]» зліва + «Читати оригінал →» справа |

---

## 🚌 Модуль «Автобуси» — поточний стан

- Пошук Звідки→Куди з кастомним дропдауном
- Акордеон зупинок (розгортання/згортання)
- Smart-рядок — наступний рейс
- Фільтрація по днях тижня
- Кнопка «✕ Всі маршрути» для скидання фільтру
- **Немає** en-route трекінгу (відкочено за запитом Вови — не було явного запиту)
- **Немає** відображення часу по Київському/локальному часовому поясу (відкочено разом)

---

## ⚡ Модуль «Світло» — поточний стан

- `data/power.json` — 11 міст/сіл Олицької ОТГ, дані DEMO
- 3-кроковий флоу: місто → вулиця (тільки Олика) → таймлайн

---

## 🔧 Хуки Claude Code

| Хук | Файл | Подія | Що робить |
|-----|------|-------|-----------|
| Git check | `~/.claude/stop-hook-git-check.sh` | Stop | Нагадує закомітити незбережені зміни |
| JS syntax | `.claude/settings.json` | PostToolUse (Edit/Write) | `node --check` для .js файлів |
| Контекст | `.claude/hooks/context-warning.sh` | UserPromptSubmit | ⚠️ при 800K / 🔴 при 900K токенів |

---

## 🔜 Що далі (пріоритети)

1. **Повний текст Конкурент** — перевірити після наступного запуску парсера чи `fetch_full_article()` спрацював (нові CSS-селектори + fallback на найбільший блок)
2. **Cloudflare Worker** — Вова має задеплоїти `cloudflare/worker.js` зі свого CF акаунту, потім додаємо olytska-gromada.gov.ua як джерело
3. **Supabase** — міграція модуля «Світло» (реальний розклад черг без GitHub)
4. **Сторінка «Громади»** — дашборд з погодою, автобусом, світлом, новинами, контактами

# Стан сесії — CSTL NEWS

**Оновлено:** 2026-04-10 (кінець великої сесії аудиту + Фази 1)

---

## Проект

| | |
|--|--|
| **URL сайту** | https://volodymyr221.github.io/CSTL_NEWS/ |
| **Репозиторій** | https://github.com/Volodymyr221/CSTL_NEWS |
| **Активна гілка** | `main` (Вова попросив повну автоматизацію — працюємо прямо в main) |
| **Власник** | Вова Шевчук (GitHub: Volodymyr221) |

---

## 🟢 Поточний стан: САЙТ ЖИВИЙ, ФАЗА 1 ЗАВЕРШЕНА

### Що працює зараз (перевірено скріншотом від Вови)
- ✅ Сайт відкривається на volodymyr221.github.io/CSTL_NEWS/
- ✅ 4 вкладки: Новини, Події, Автобуси, Подати
- ✅ Погода у шапці (5° з хмарою на момент перевірки)
- ✅ Фільтри новин (Всі / Олика / Волинь / Україна / Світ)
- ✅ 3 тестові статті відображаються (перша featured з червоною рамкою "Ексклюзив")
- ✅ **Лічильник версії працює** — внизу екрану видно `v{N} · DD.MM HH:MM`, оновлюється при кожному новому деплої
- ✅ Автоматичний деплой через `.github/workflows/deploy.yml` (підхід А+)
- ✅ Settings → Pages → Source = GitHub Actions (Вова підтвердив що переключив)

### Останні деплої
- `v79 · 10.04 19:26` — перший успішний деплой Фази 1 (коміт `018783f`)
- `v80` — після коміту `5526c2b` (оновлення CLAUDE.md)
- `v81` — після поточного коміту (оновлення START_HERE, SESSION_STATE, NEW_SESSION_PROMPT)

---

## 📜 Що було зроблено у цій сесії (повна хронологія)

### 1. Повний аудит проекту
- Перечитано всі файли проекту (src/, data/, docs/, .claude/, index.html, style.css, sw.js, package.json)
- Знайдено **31 проблема**:
  - 4 🔴 критичні (сайт мертвий після "Clean up: remove broken build workflows")
  - 7 🟠 серйозних (баги логіки, розбіжність документації)
  - 8 🟡 середніх (мертвий код, сміття у git)
  - 12 🟢 дрібних (граматика, UX, безпека)
- Усі задокументовано у `CSTL_BUGS.md` (B-01 … B-20)

### 2. Фаза 1 А+ — аварійне відновлення (7 комітів)
| Коміт | Що зроблено |
|-------|-------------|
| `2e7309e` | Відновлено `build.js` (Крок 1) |
| `0e9dcd2` | Відновлено `bundle.js` (Крок 2, 470 рядків) |
| `5ec55b4` | Повне оновлення документації (7 файлів): CONCEPT, ROADMAP, ARCHITECTURE, SESSION_STATE, CSTL_BUGS, BACKLOG (новий), CONTENT_STRATEGY (новий) |
| `018783f` | Фаза 1 кроки 3-5: новий `deploy.yml` (А+), плейсхолдер лічильника у `index.html`, стиль у `style.css`, `sw.js` оновлено (CACHE_NAME, logo.png, network-first для index.html) |
| `5526c2b` | Оновлено `CLAUDE.md` (корінь + CSTL NEWS VOVA) під А+ і Фазу 1 |
| (поточний) | START_HERE.md повний перепис + SESSION_STATE фіналізація + NEW_SESSION_PROMPT створений |

### 3. Merge у main + перший живий деплой
- `git checkout main && git merge --ff-only claude/code-audit-review-wfuX7`
- `git push origin main` → GitHub Actions запустився → деплой пройшов → штамп `v79 · 10.04 19:26`
- Вова перевірив сайт на телефоні — працює

---

## ⚙️ Ключові рішення цієї сесії (щоб новий чат їх знав)

1. **Варіант А+ для деплою.** Використовуємо офіційний `actions/deploy-pages@v4` замість саморобного "CI комітить bundle.js у main" патерну. Це повністю обходить баг B-01 (non-fast-forward). `deploy.yml` нічого не комітить у main — тільки будує, ставить штамп через `sed`, і публікує artifact.

2. **Варіант Б для `bundle.js`.** Комічу його в git як робочу базу. CI перегенерує свіжий при деплої, але не комітить назад. Компроміс між "чистотою" і "простотою".

3. **Лічильник версії — обов'язкова фіча.** Без нього Вова не може зрозуміти чи пройшов автодеплой. Формат: `v{github.run_number} · DD.MM HH:MM` (час Києва). Замінюється через `sed` у `deploy.yml`.

4. **Пріоритет контенту: В — Новини.** Гібридна модель: RSS парсинг волинських ЗМІ + ручні ексклюзиви Олики через GitHub UI редагування `data/curated.json`. Деталі у `docs/CONTENT_STRATEGY.md`.

5. **Хук `~/.claude/stop-hook-git-check.sh` — корисна фіча, лишається як є.** Автокоміт наприкінці турна страхує роботу на випадок ліміту API і дозволяє Вові паралельно тестувати. Спочатку я хотів переписати — Вова пояснив що це потрібно, я погодився.

6. **Стиль роботи Claude:** один хід (turn) = один логічний блок роботи = один коміт. Не дроблю на мікро-коміти всередині ходу.

7. **Гілка `main` як робоча.** Вова попросив повну автоматизацію: "Зробили зміни → автоматично все спрацювало → я зайшов і тестую". Щоб не робити merge вручну, працюємо прямо у `main`. Feature-branches — тільки якщо експеримент ризиковий.

8. **Нік GitHub: `Volodymyr221` (не VShevchukkk, не VShvchukkk).** Вова підтвердив скріном що активний акаунт — `Volodymyr221`. Уся документація вже виправлена під нього.

---

## 🔜 Що робити далі — Фаза 2

**Повний список задач — у `_ai-tools/BACKLOG.md`.**

### Топ-5 задач наступної сесії (за пріоритетом)

1. **Виправити `buses.js` дні тижня (B-05).** У розкладі є рейси "пн-сб". Зараз код їх показує в неділю. Додати перевірку `new Date().getDay()`. Проста задача на ~15 хв.

2. **Виправити `events.js` парсинг дат (B-06).** Замінити `new Date(now.toDateString())` на `const d = new Date(); d.setHours(0,0,0,0);`. Крихка регекс-дата ламається на iOS Safari. ~10 хв.

3. **Замінити `mailto:` у `submit.js` на Web3Forms (B-07).** Форма зараз показує брехливий toast "Надіслано" навіть коли лист не пішов. Зареєструватись у Web3Forms (безкоштовно до 250/місяць), отримати access_key, замінити навігацію на `fetch` POST. ~30 хв.

4. **Почати RSS-парсер новин (Фаза 2.1).** Створити `data/curated.json` і перший варіант `scripts/fetch_news.py`. Джерела: `volyn24.com`, `volynnews.com`, `suspilne.media/volyn`, `pravda.com.ua`. Почати з 2-3 джерел щоб швидко протестити. ~3-5 годин.

5. **Виправити дрібні баги одним "clean-up" комітом:**
   - B-09 (мертвий CSS `.news-card`)
   - B-11 (`.DS_Store` + `CSTL NEWS VOVA.zip`)
   - B-17 (сортування подій за часом у день)
   - B-19 (граматика "Вови")
   ~30 хв сумарно.

### Відкриті запитання до Вови (для нової сесії)

- Email для модерації форми "Подати новину" — зараз захардкоджено `cstlnews@gmail.com`, замінити на реальну?
- Чи купляти домен (`olyka.news` / `cstl.news`)? Раніше сказав "пізніше", але коли саме "пізніше"?
- Чи є API у Волиньобленерго для графіків світла? Треба перевірити перед Фазою 3.

---

## 🧰 Технічні деталі для наступної сесії

**Перший крок нового чату:** прочитати `CSTL NEWS VOVA/START_HERE.md` і `CSTL NEWS VOVA/_ai-tools/BACKLOG.md`. Потім запитати Вову "Що робимо сьогодні?".

**Робоча директорія:** `/home/user/CSTL_NEWS` (на сервері Claude)
**Поточна гілка:** `main` (не feature-branch)
**Локальний запуск:** `npm install && node build.js && open index.html`
**Перевірка синтаксису:** `node --check src/**/*.js sw.js build.js`

**Git-історія (останні 10):**
```
(поточний) docs: START_HERE + SESSION_STATE + NEW_SESSION_PROMPT
5526c2b    docs: оновлено CLAUDE.md (корінь + CSTL NEWS VOVA) під А+
018783f    feat: A+ deploy + лічильник версії + кеш-стратегії
5ec55b4    docs: повне оновлення документації (7 файлів)
0e9dcd2    restore: bundle.js (Phase 1 step 2)
2e7309e    restore: build.js (Phase 1 step 1)
5763587    Clean up: remove broken build workflows  ← той "катастрофічний" коміт
```

---

## 📝 Звіт Вові для нової сесії

Скопіюй текст з `_ai-tools/NEW_SESSION_PROMPT.md` у перше повідомлення нового чату. Новий Claude одразу зайде у повний контекст.

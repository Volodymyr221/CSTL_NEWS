# Стан сесії — CSTL NEWS

**Оновлено:** 2026-04-19 (кінець вечірньої сесії) | **Архів попередніх сесій:** `_ai-tools/SESSION_ARCHIVE.md`

---

## Проект

| | |
|--|--|
| **URL сайту** | https://volodymyr221.github.io/CSTL_NEWS/ |
| **Репозиторій** | https://github.com/Volodymyr221/CSTL_NEWS |
| **Активна гілка** | `main` |
| **Власник** | Вова Шевчук (GitHub: Volodymyr221) |

---

## 🟢 Поточний стан

- ✅ Сайт відкривається, 4 вкладки працюють
- ✅ Лічильник версії внизу екрану, автодеплой через GitHub Actions
- ✅ **Погода** — назва міста по GPS
- ✅ **Модуль «Новини»** — RSS-парсер кожні ~30 хв, 5 активних джерел, без помилок
- ✅ **Модалка статті** — sticky-header (рисочка + × + гео•категорія), не перекриває шапку, 12px відступ
- ✅ **Модуль «Події» v3** — акордеон, `.ics` нагадування
- ✅ **Модуль «Автобуси»** — пошук, акордеон, smart-рядок, дні тижня
- ✅ **Модуль «Світло»** — таймлайн, 11 сіл ОТГ, DEMO дані
- ✅ **Хук контексту** — ⚠️ при 800K / 🔴 при 900K токенів

---

## ⚙️ Ключові архітектурні рішення (не змінювати без обговорення)

1. **А+ деплой** — `actions/deploy-pages@v4`, нічого не комітить у `main` з CI
2. **`bundle.js` у git** як робоча база
3. **`main` як робоча гілка** — пуш = автодеплой
4. **Хук `stop-hook-git-check.sh`** — не чіпати
5. **Лічильник версії** — `sed` у `deploy.yml`, формат `v{N} · DD.MM HH:MM`
6. **CACHE_NAME у `sw.js`** — оновлювати при КОЖНІЙ зміні статичних файлів. **Остання: `cstl-20260419-1700`**
7. **Автобуси — зупинки з офіційних квитків VOPAS** — не вигадувати km/ціни
8. **RSS-парсер** — cron кожні ~30 хв (`0,30 * * * *`), ліміт 15/джерело, MAX_ARTICLES=150, BROWSER_UA + response_headers
9. **Не чіпати зайвий код** — тільки те що явно обговорюється

---

## 📰 RSS-парсер — поточний стан

### Активні джерела (6 штук)
| Джерело | Geo | Тип | Статус |
|---------|-----|-----|--------|
| Волинь Post | Волинь | RSS | ✅ Працює, оновлюється повільно |
| Конкурент | Волинь | RSS | ✅ Працює, оновлюється повільно |
| Українська правда | Україна | RSS | ✅ Активно поповнює |
| Українська правда (Світ) | Світ | RSS | ✅ Активно поповнює |
| Район.Ківерці /tags/olika | Олика | HTML | ✅ Чекає нових постів про Олику |
| Олицька громада | Олика | gromada (Worker) | ✅ Додано 2026-04-19, Worker: cstl-proxy.volodymyrshevchuk19.workers.dev |

### Видалені джерела (нероботоздатні)
| Джерело | Причина |
|---------|---------|
| Суспільне Волинь | DNS-блок з GitHub Actions (мережева блокада) |
| Укрінформ Волинь | Зламаний XML (`mismatched tag`) |
| Укрінформ Світ | Зламаний XML (`mismatched tag`) |

### Логіка парсера
- **Завантаження:** `fetch_rss()` — Chrome BROWSER_UA + `response_headers` (тип контенту + кодування) → `feedparser.parse(raw, response_headers=...)`
- **Дедуплікація:** за `sourceUrl` + нормалізований заголовок
- **Повний текст:** `content:encoded` → якщо < 600 символів → `fetch_full_article()` зі сторінки
- **Класифікатор:** `classify_entry()` → EVENT_KEYWORDS + майбутня дата → `events.json` або `articles.json`
- **Cron:** `0,30 * * * *` — GitHub безкоштовний тариф може затримувати на 5-30 хв
- **MAX_ARTICLES:** 150, **MAX_PER_SOURCE:** 15, **MAX_EVENTS:** 50
- **Traceback:** увімкнено (`traceback.print_exc()`) — помилки видно у повному лозі Actions

### Що зроблено у сесії 2026-04-19 (продовження)
1. `fetch_rss()` — ручне завантаження з BROWSER_UA замість feedparser HTTP
2. `response_headers` передаються у `feedparser.parse()` — фікс кодування (Волинь Post/Конкурент)
3. `isinstance(entry, dict)` — захист від None-записів
4. `actions: write` у `rss-parser.yml` — фікс 403 при "Trigger deploy"
5. `lxml` додано до pip install — надійніший XML парсер
6. Видалено 3 нероботоздатних джерела
7. **Олицька громада** — `parse_gromada_source()` через Cloudflare Worker (`cstl-proxy.volodymyrshevchuk19.workers.dev`), Joomla-парсер, `gromada_url()` трансформує URL статей через Worker

---

## 🗞️ Модалка новини — поточний стан

| Функція | Статус |
|---------|--------|
| Sticky-header (рисочка + × + гео•категорія) | ✅ `modal-sticky-header` — завжди видно при скролі |
| Модалка не перекриває шапку | ✅ `top: var(--header-h)` замість `inset: 0` |
| Відступ від шапки | ✅ `max-height: calc(100svh - var(--header-h) - 12px)` |
| Гео + категорія з роздільником | ✅ `УКРАЇНА • СУСПІЛЬСТВО` у sticky-header |
| Свайп вниз → закрити | ✅ `initModalSwipe()` в app.js |
| Клікабельне джерело (byline) | ✅ чорне, `font-weight: 600` |
| Повна стаття | ✅ `content:encoded` + `fetch_full_article()` |
| Футер «Автор публікації» | ✅ зліва + «Читати оригінал →» справа |

---

## 🚌 Модуль «Автобуси» — поточний стан

- Пошук Звідки→Куди, акордеон зупинок, smart-рядок, дні тижня
- **Без** en-route трекінгу (відкочено)

---

## ⚡ Модуль «Світло»

- `data/power.json` — 11 міст/сіл Олицької ОТГ, DEMO дані
- Потребує Supabase для реальних даних

---

## 🔧 Хуки Claude Code

| Хук | Подія | Що робить |
|-----|-------|-----------|
| `~/.claude/stop-hook-git-check.sh` | Stop | Нагадує закомітити |
| `.claude/settings.json` PostToolUse | Edit/Write | `node --check` для .js |
| `.claude/hooks/context-warning.sh` | UserPromptSubmit | ⚠️ 800K / 🔴 900K токенів |

---

## 🔜 Що далі (пріоритети)

1. **Перевірити модалку** — Вова перевіряє з ноуту: sticky-header з гео•категорією, відступ від шапки
2. **Нові волинські джерела** — Суспільне/Укрінформ видалено, Вова шукає заміну. Потрібні RSS-стрічки
3. **cron-job.org** — зовнішній cron (планувальник) для точніших 30-хв запусків парсера (GitHub затримує)
4. **Cloudflare Worker** — `cloudflare/worker.js` готовий. Вова реєструється → деплоїть → дає URL → Claude підключає `olytska-gromada.gov.ua` до парсера
5. **Supabase** — реальні дані для модуля «Світло»

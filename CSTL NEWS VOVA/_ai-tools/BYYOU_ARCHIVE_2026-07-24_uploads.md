# BYYOU ARCHIVE — 2026-07-24 · Надійне завантаження зображень (fix «Load failed»)

**Статус:** done · **Гілка:** `claude/startuem-79wz8r` · **Деплой:** PR #571 (squash у main) · **CACHE:** `cstl-20260724-1618`

**Ціль:** щоб жоден користувач у жодному місці не ловив «Load failed» при завантаженні фото — незалежно від розміру/формату фото й віку спільноти.

**Корінь:** частина місць слала у сховище СИРИЙ файл (телефонне фото 3-5 МБ, формат iPhone .heic) без стиснення → `uploadPhotoToStorage` падав «Load failed». Стиснуті шляхи (фото постів/оголошень, аватар профілю) працювали.

**Рішення:** новий `src/core/upload.js` — єдиний надійний шлях: стиснення (→JPEG, канвас нормалізує .heic) + послідовний upload + повтор ×2 з бекофом. Дві функції: `uploadImageReliable(file, opts)` (сирий файл), `uploadBlobWithRetry(blob, folder)` (вже стиснутий blob для прев'ю).

### Кроки (усі ✅, коміти локальні → squash PR #571)
| # | Крок | Файл | Коміт |
|---|------|------|-------|
| 1 | Модуль `core/upload.js` | `src/core/upload.js` | `b2dced0b` |
| 2-4 | Банер/аватар/фото постів → хелпер | `src/tabs/feed.js` | `6b15293f` |
| 5 | Фото приватного чату → хелпер | `src/tabs/board-chat.js` | `b08d0c1c` |
| 6 | Фото оголошення → +повтор | `src/tabs/community-modal.js` | `d0a6c6ae` |
| 7 | Аватар профілю → хелпер (square+повтор) | `src/core/account-ui.js` | `2da663c7` |
| 8 | CACHE bump + rebuild | `sw.js`,`bundle.js` | `3c269b1f` |

**Параметри стиснення:** банер `maxDim 1600, q 0.85`; аватар сторінки `square 512`; фото поста `1600, 0.82`; фото чату `1600, 0.82`; оголошення `800, 0.78` (як було); аватар профілю `square 256`.

**Перевірка:** node --check 5 файлів OK; node build.js чисто (35 файлів, check-imports 0); браузер-смоук Chromium — 4 екрани, 0 падінь, консоль чиста (лише мережеві 403/503 від заблокованого Supabase у пісочниці), верстка ✅.

**⏸️ Крок 11 (смоук на iPhone Вови):** банер сторінки · аватар сторінки · фото в пост (кілька) · фото в приватний чат · аватар профілю. Якщо десь ще «Load failed» — дивитись у бік того самого патерну (сире фото / паралельні upload).

**Не чіпалось:** схема Supabase, бакет `community-photos`, RLS. Групові чати фото ще не підтримують (upload там нема).

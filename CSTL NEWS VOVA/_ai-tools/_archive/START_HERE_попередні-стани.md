# START_HERE — попередні стани (архів)

> 🗄 **ВИРІЗКА З `CSTL NEWS VOVA/START_HERE.md`, 15.08.2026.** Це знімки стану
> проєкту на свої дати. Перенесено **без правок**.
>
> 🔴 **Чому їх забрали з живого файлу.** Кожен такий блок починався словами
> «**НОВИЙ ЧАТ ПОЧИНАЙ ЗВІДСИ**» — і їх було ЧОТИРИ, з датами 30.07, 01.08,
> 02.08 і 10.08. Тобто файл, який сам себе називає точкою входу, давав новому
> чату чотири різні «поточні стани», три з яких брехали. Два з них у своєму ж
> тексті зізнавались, що вже відставали на 3-4 дні.
>
> ➡️ Живий стан тепер рівно один і живе в `CSTL NEWS VOVA/NOW.md`.
> 🛑 Не повертати ці блоки в `START_HERE.md`. Потрібна історія — читай тут.

---

## 🗄 (попередній стан, 2026-08-10, ніч, сесія U-19)

> 🔴 **НОВИЙ ЧАТ ПОЧИНАЙ ЗВІДСИ.** Повний стан із причинами кожного рішення —
> блок **⭐ ХЕНДОВЕР У НОВИЙ ЧАТ** на самому верху `_ai-tools/SESSION_STATE_VOVA.md`.
>
> ⚠️ **Цей блок одного разу відставав на 3 дні** (стояв стан 06.08 і два
> «блокуючі питання», закриті ще 07.08). Файл сам про себе каже «новий чат
> починай звідси», тож застарілий стан **тут** дорожчий за застарілий
> будь-де. **Оновлюй при кожному `/finish`.**

**Прод:** сесія U-21 — вкладка «Обговорення» стала **«ПИТАННЯ» (Q&A)**, дев'ять
редакцій вигляду за одну добу (PR #890 … #903). Останній деплой — **#4124+**.
`CACHE_NAME = cstl-20260814-2013`. `npm test` — **80 стендів** (15.08 додано `notif-backstack.mjs`, `notif-thread-coldstart.mjs` і `docs-fresh.mjs`; 14.08 додано
`legal-privacy.mjs` і `tabbar-icons.mjs`); прогін 11.08 дав 1220/1222, єдине падіння — часозалежний
`weather-day`, див. блок нижче.

> 🆕 **11.08 — КРУГЛА КНОПКА (FAB) ОДИН РАЗ ЗА ЗАПУСК ПОЯСНЮЄ СЕБЕ.** Працює у
> ДВОХ зонах: «Питання» → «Запитати», Дошка → «Подати оголошення», а при
> непрочитаних — конверт і «Повідомлення N». Прапорці окремі на зону, живуть у
> памʼяті модуля. Сторож — `tests/fab-hint.mjs`.
>
> 🔴 **У базі лишились 5 ТЕСТОВИХ питань** (id 4 · 10 · 13 · 27 · 42 — «Коли
> вибори?» з 46 відповідями `Прил`/`Бабдссжажаажажа», «?», «Бачиш його?»).
> Видалити не вдалось: класифікатор прав харнесу пропускає `INSERT`/`SELECT`, але
> блокує `DELETE` і `UPDATE` по проду. Каскад у `posts` забере коментарі,
> реакції й закладки сам — досить видалити пости. Копія є в `db-backup.yml`.
> Поруч із ними — **7 посіяних прикладів** (id 62-68, коментарі 130-140).

> 🔴 **`weather-day` — ЧАСОЗАЛЕЖНИЙ, і це його сліпа зона, а не зламаний код.**
> Заміряно 10-11.08: о 23:0x за Києвом він дає **44/46**, о 00:2x — **46/46**, на
> тому самому коді. Причина в самому стенді: сцену він будує **від поточної
> години** (`nowHour`), і під кінець доби годин попереду майже не лишається —
> тоді «стани по годинах РІЗНІ» і «375px: видно 3 картки» валяться від нестачі
> даних, а не від дефекту.
> ⚠️ У шапці стенда вже описано, як його ПЕРШУ редакцію лагодили від «падає
> щовечора після 18:00» — прив'язали до `nowHour`. Полагодили не до кінця: лишився
> той самий клас помилки, просто зсунутий на кілька годин.
> 🛑 **Не пиши через це «усе зелене» і не пиши «стенд зламаний».** Правильне
> формулювання: «зелено, якщо прогін не припав на останню годину доби».
> _(До 11.08 тут стояло просто «усе зелене» — неправда; потім «падає 44/46» —
> теж неповна правда. Обидва формулювання знімали б обережність.)_

---

---

### (попередній стан, сесія U-7 — 2026-08-02/03, уся про Дошку)

**Прод того етапу:** PR #755, `CACHE_NAME = cstl-20260803-0841`, стенди 36/605.
Десять блоків: сторінка оголошення в три шари · свайп-закриття знято на користь
системного жесту (`core/layers.js`) · скарги на оголошення (`ad_reports`) · блок
безпеки · гейт правил Дошки · картка списку (фото `position: absolute`, заголовки
капсом, висота за вмістом із підлогою 98px). 🛑 Не піднімати цю підлогу до висоти
найвищої картки — уже відкочували.

---

### (попередній стан, сесії T·U — 2026-08-01/02)

**Прод того етапу:** PR #738, `CACHE_NAME = cstl-20260802-0108`, стенди 31/466.

1. **Безпека** (#734): «анонім лише переглядає» доведено до бази · заведено щоденний
   шифрований бекап живої БД (його не існувало взагалі) · фото приватних чатів
   переїхали в закритий бакет `chat-photos`.
2. **Табло новин** (#735): прилад **знято вранці й повернуто ввечері** — рішення Вови
   по знімках; картка дістала обідок сторінки новин.
3. **🔴 ПРОЩАННЯ З КОРКОМ** (#736): дві колонки «стікерів» зі шпильками → **один
   стовпчик** карток-рядків. Причина не смак, а арифметика: у двох колонках рядку
   лишалось ~175px і фото+ціна+локація разом не влазили. Розбір →
   `_ai-tools/AUDIT_DOSHKA_2026-08.md`.
4. **Дії внизу, листування вгорі** (#737): `canSeeMessages()` = **є оголошення АБО є
   розмова** — правило «лише автори» відрізало б 7 із 8 людей від їхнього ж листування.

---

### (попередній стан, сесія O — 2026-07-30)

> 🔴 **НОВИЙ ЧАТ ПОЧИНАЙ ЗВІДСИ:** повний хендовер — блок **⭐ ХЕНДОВЕР У НОВИЙ ЧАТ**
> на самому верху `_ai-tools/SESSION_STATE_VOVA.md`. Там усе з причинами кожного
> рішення. Докладний журнал — `_session-log/vova-2026-07-30b.md`.
>
> ⚠️ **Цей блок відставав на 4 дні** (стояв стан сесії F від 26.07, тоді як прод був
> уже 30.07) — знайдено при старті сесії O і виправлено. **Оновлюй його при кожному
> `/finish`:** файл сам каже «новий чат починай звідси», тож застарілий стан тут
> дорожчий за застарілий деінде.

**Прод:** PR **#722** → `main` (`d290e827`), деплой **#3564**,
`CACHE_NAME = cstl-20260730-1858`. Стенди — **30 / 396 перевірок**.
Робоча гілка сесії — `claude/startuem-washns`. Відкритих 🔴 багів нема.

# Стан сесій — РОМА

> Зона Роми: 📅 Події · 📰 Новини (вкл. кабінет редактора Алли Н-4) · 🔔 Push · 💡 Світло (V2) · ⚙️ Системне-інфра.
> Громада — разом з Вовою. Повна історія до 05.07 → `_archive/SESSION_STATE_full.md`.
> Оперативно «хто над чим зараз» → `BOARD.md`.

---

## 🏁 2026-07-18 — ОПИТУВАННЯ ЗАСНОВНИКІВ + ВИХІД РОМИ З ПРОЕКТУ (по-дружньому)

**Опитування засновників.** Провів повне опитування (Блоки A/B/C/D + розширені EXT-1..15; Блок E — приватний, у файли не писався). Мета — спільне бачення, чіткий розподіл ролей по всьому проекту, правило рішень для спільних зон, письмова угода засновників. Шаблон питань — `docs/ROLES_SURVEY_TEMPLATE.md`; відповіді Роми — у `BOARD.md`. Вова пройшов лише Блок A. Ключовий діагноз: конфлікт не про пікселі, а про **компетенцію (Рома сильніший виконавець) проти контролю (Вова потребує відчувати, що творить/контролює свій проект)**.

**Вихід Роми.** Через повторюваний патерн односторонніх змін спільного головного екрана «Громада» (Вова робив сам, попри узгодження) Рома вирішив **вийти з проекту, зберігаючи дружбу**. Вова прийняв (розставання обопільне; зі свого боку зазначив, що редизайн Громади був анонсований 7.07 і він просив зберегти baseline). Вова запропонував 5% від чистого прибутку CSTL LIFE як подяку — **Рома відмовився** («лиши собі / на благодійність»). Проект, бренд, домен, дані — лишаються Вові.

**Алла.** Відмовилась від ролі головного редактора (лише фото/відео «на підхваті», їде за кордон) — роль головреда вакантна (актуально для Вови/майбутнього).

**Відкрите (IP Роми):** зробити незалежну повну копію коду під своїм контролем + отримати від Вови письмове «ок» на повторне використання власного коду/архітектури в інших проектах. Опційно — реєстр авторства.

---

## 🧭 2026-07-16 — ВІДКРИТА ТЕМА (обговорення триває, КОД НЕ ЧІПАТИ поки Рома не скаже) — покращити систему спільної роботи

**Проблема (виявлено при запиті Роми «скільки я зробив / скільки даремно»):** наявна система спільної роботи (BOARD, окремі SESSION_STATE, іменовані гілки, `git config` під людину, owner-watch) **НЕ дала швидкої відповіді хто що зробив** — Claude мусив вручну розгортати shallow-клон у повний (3689 комітів) і копати git blame/numstat, щоб ЛИШЕ ПРИБЛИЗНО оцінити внесок. Система коордінує (хто зараз щось торкає), але не веде **ledger** (queryable запис хто що збудував).

**Чому не спрацювало (діагноз Claude):**
1. **Squash-merge стирає авторство** — кожен PR → один коміт, автор = ТОЙ ХТО МЕРДЖИТЬ (OWLs68/VShevchukkk), не хто писав. git blame на main бреше.
2. **«Claude»-ера** — 913 комітів/29К рядків до іменованого авторства (~до середини червня) = 42% живого коду, нероздільне між Ромою/Вовою.
3. **Атрибуція у ПРОЗІ, не в структурі** — BOARD/SESSION_STATE описують роботу текстом; нема жодного запиту що дасть «фічі/рядки/файли Роми».
4. **Багато ідентичностей на людину** + merge-акаунт ≠ author-акаунт, без мапи (Claude реверс-інжинірив із патернів).
5. **Асиметрія воркфлоу** — Вова комітить доки прямо в main (marsian0k), Рома через PR → лічба комітів незіставна.
6. **Боти = 57% комітів** — щоразу треба фільтрувати.

**Корінь:** будували для КООРДИНАЦІЇ (не наступати одне одному в реалтаймі), а не для ЛЕДЖЕРА (durable запис хто-що-збудував). Це різні цілі.

**Напрямки покращення (НЕ реалізовувати поки — Рома вирішить пізніше):** не squashити АБО зберігати автора; структурований ledger (фіча→власник→PR→файли→±рядки, авто на `/finish`); одна ідентичність на людину + мапа; чіткий тег ботів; CODEOWNERS.

**Статус:** ⏸️ чекає (1) пункт 2 Роми «що турбує», (2) рішення Роми про напрям. Код/хуки/систему НЕ чіпати до явного «Роби».

### 🔴 ПУНКТ 2 (2026-07-16) — Вова в односторонньому порядку переробив ГОЛОВНИЙ екран (Громаду), який Рома вже зробив і УЗГОДИВ із Вовою ДО коду

**Оригінальне бачення Роми (узгоджене з Вовою до написання коду):**
- Громада = головна ВІТАЛЬНА сторінка. Юзер заходить → гарне зображення місцевості + персональне вітання. Знизу картинки — підпис що на ній (пізніше додати фото замку та ін. об'єктів, теж підписані).
- **Табло новин видно ЛИШЕ ЧАСТКОВО — НАВМИСНЕ.** Щоб не захаращувати вітальну картинку; частковий показ = **гачок**, який спонукає юзера свайпнути/погортати вниз.
- **Шапка чиста:** бургер ЗЛІВА біля лого (`CSTL LIFE`), як на більшості сайтів. Справа — ДВІ іконки: особистий кабінет + збережені. Сайдбар відкривається ЗЛІВА (з того ж боку що бургер).

**Що Вова змінив односторонньо (без повторного узгодження):**
- Бургер → ВПРАВО, і поряд поставив іконку «збережені» (навігація + контент-дія = дві кардинально різні функції поряд).
- Сайдбар → відкривається СПРАВА (хоча бургер/сайдбар були зліва).
- Табло опустив нижче → між картинкою і табло виліз **бежевий порожній фон** → виглядає «новачково».
- Шрифт не пасує, написів забагато.

**Реакція Роми (дослівно, суть):** «не розумію і не приймаю його бачення… нахуя я робив це все щоб Вова поміняв усе в односторонньому порядку». Сильна фрустрація — не капризи, а порушений узгоджений процес.

**Оцінка Claude (незалежна, до пояснень Роми — збіглася):** беж-проміжок, салат шрифтів, забагато написів — реальні ремісничі дефекти. Аргументи Роми (частк. табло=гачок, бургер зліва біля лого = конвенція, сайдбар з боку тригера, розділення навігації і контент-дій) — професійно валідні. Контр-аргумент за Вову можливий лише один (правий бургер = зручність великого пальця на великих екранах) — але не скасовує беж-дірку/шрифти.

**Корінь = той самий (система):** спільний головний екран змінено без спільного рішення. Це не смакова війна — це процесна дірка.
**НЕ РОБИТИ:** не відкочувати Вовине мовчки (це буде те саме одностороннє, лише навпаки). Рішення — за Ромою + узгодження з Вовою.

---

## 📝 2026-07-14 — /fullaudit ЗАБЛОКОВАНО + токен-запобіжник у хук-сторож

**1. `/fullaudit` заблоковано (не видалено).** Створив цей скіл 13.07 (незалежний повнорепозиторний аудит через Workflow, PR #399) і одразу прогнав — знайшов 17 підтверджених знахідок (звіт у чаті/brain). Але прогін дорогий: ≈3.6М токенів, ~63 хв, 91 агент. Рішення Роми 14.07: **застосунок ще активно будується (я+Вова паралельно), повний аудит зараз задорого відносно користі — активуємо на фінішній прямій/поліровці перед релізом.** Файл винесено `.claude/commands/fullaudit.md` → `.claude/commands-archived/fullaudit.md` (команда `/fullaudit` фізично НЕ реєструється), зверху банер-🔒. Дизайн (13 вимірів + severity-верифікація) готовий, лишається актуальним. Реактивація: `git mv` назад + прибрати банер.

**2. Токен-запобіжник у хук-сторож** — див. окремий коміт (нижче в цьому записі / BOARD): якщо ОДИН Agent/Task-виклик спалює >350К токенів → сесія стопиться прапорцем-файлом, доки Рома не підтвердить продовження.

---

## 📝 2026-07-12 (сесія C, гілка `claude/new-session-yeuv6m`) — звірка Push-розбіжності + іконки Потік 7 (таб-бар↔сайдбар) + 2 заголовки-емодзі

Дві дії, обидві в одному `/finish` (Рома: «Іконки + doc-fix разом»).

**1. Звірка розбіжності Push (doc-fix, коміт `4f0a4e75`).** BOARD (Вова→Рома) + AUDIT числили П-2/П-5/П-8/П-9 як відкриті 🔴, хоча PR #321 (10.07) їх зробив. Звірив з реальним кодом кожен: P-2 `send-chat-push:49,71` (photo_url+фолбек), P-5 `board-chat.js:71` (`ensureChatPush`), P-8 `sw.js:166` (банер), P-9 `sw.js:184`+`app.js:206`+`board-chat.js:1263` (`#/thread/<id>`). Виправив AUDIT (Push→ЗРОБЛЕНО з оригіналами) + BOARD (✅-нотатка, Вовин текст не чіпав). **⚠️ Відкрита реальна дія:** P-2 живий `supabase functions deploy send-chat-push` (код у репо — Edge Function передеплоїти; Supabase CLI недоступний із сесії).

**2. Іконки Потік 7 — доробка (коміт `2aac3b20`).** Узгодив таб-бар із сайдбаром (стиль варіант-5, тонкий sw1) — закрито ⏳ з попередньої сесії.
- Таб-бар (`index.html`): buses `pin→bus`, board `→clipboard`, discussions `bubble→message`, shotam `→fileText`; stroke-width 2/2.5→1. Громада (castle png) без змін. Шляхи з `icons.js` (Tabler sw1).
- Сайдбар (`sidebar.js`): board `pin→clipboard`, shotam `calendar→fileText` — одна іконка на концепт на обох поверхнях (buses=bus, board=clipboard, discussions=message, shotam=fileText).
- Заголовки-емодзі прибрано: 📰 біля «Шо в селі» (`index.html:69`) + іконка біля «Обговорення» (`board.js:1143`). Мертвий код зметено: const `DISC_TITLE_ICON_SVG` (board.js), CSS `.bd-disc-title svg` (community.css), `.shotam-head-ic` (events.css).
- Тести: `node --check`+`node build.js` (потрібен був `npm install` — свіжий контейнер); Playwright-смоук: таб-бар 5×sw1, збіг із сайдбаром, 📰 зникло, «Обговорення» svgCount 0, **0 pageerror**. CACHE `cstl-20260712-1845`.
- **⚠️ Watch-point:** sw1 на 22px таб-іконці досить тонка — перевірити наживо на iPhone; якщо хайрлайн, підняти до ~sw1.25.

**⏳ Далі по іконках (Потік 7 хвіст):** рештки літеральних емодзі (кроки 8-12: news/events/buses/power/community-blocks тости-бейджі, index.html дрібне) — ще не почато.

---

## 📝 2026-07-12 (велика сесія, гілка `claude/new-session-nvoaif`) — фікси A/C, табло-скрол #3, іконки Batch 1, аудит адмінки+пайплайну

Довга ітеративна сесія зі скрінами Роми. Все задеплоєно (PR #379-#389). По черзі:

**1. Фікс A (зона дотику табло) + C (коментар icons.js)** — PR #379. `--edge-w` 18→10px; коментар про admin.html.

**2. Табло новин — бокова зона свайпу + повна картка (#3)** — PR #386→388, кілька ітерацій:
- Спершу звузив стрічку `margin: 0 30px` (краї віддають скрол сторінці) — Рома: картки стали вузькі.
- Дослідив (агент, iOS): чистий CSS «повна картка + бокова зона» НЕМОЖЛИВО (`overflow-clip-margin` не в Safari; `touch-action pan` ігнориться iOS). Варіанти: #3 JS-перемикач (крихкий на iOS) / #4 прибрати внутр.скрол (стабільно, міняє вигляд).
- Рома обрав **#3 «вигляд зберігаємо»**: повна картка + JS на секції `.cm-block--news` — палець у 30px від краю → `overflow:hidden` на час жесту → свайп сторінці; center → стрічка; тап → картка. Хардненуто (touchstart, passive, restore на touchcancel). ⚠️ Відомий iOS-ризик (документовано ламався на 12.1) — перетест після оновлень iOS.

**3. Іконки Потік 7 — стиль ВАРІАНТ 5 (тонка, stroke-width 1)** обрано Ромою. **Batch 1 (навігація)** — PR #387: `icons.js` A-константа sw 2→1 + **20 нових Tabler-іконок** (реальні шляхи, зібрані агентом). Замінено емодзі→SVG у `sidebar.js` (меню 11 іконок), `saved-hub.js`, `account-ui.js`. Аватар 👤 лишив емодзі. Розміри звірено (контейнери 19px, `.cat-ico`=1em).
  - **⏳ У ЧЕРЗІ (не зроблено):** узгодити ТАБ-БАР з сайдбаром (Рома: іконки різні). План з його інструкцій: Автобуси→bus, Дошка→clipboard(з таббару), Громада→замок-лого(унікальний), Обговорення→message(з сайдбару в таббар), Шо в селі→document(з таббару). Стиль тонкий. + **прибрати 2 заголовки-емодзі:** газета біля «Шо в селі», іконка біля «Обговорення».

**4. 🔴 Глибокий аудит адмінки + пайплайну публікації** (Рома: «публікується довго і працює криво»). 2 агенти + звірка. Знайдено ~15 проблем, зафіксовано 11 — PR #389:
- **Батч A (admin.html):** P2 (адмінка показувала СТАРІ дані — не робила refetch при навігації; фікс: refetch при вході + кнопка «🔄 Оновити» + мітка «⏳ у черзі на сайт» на ready — це головна причина «опубліковані не показуються»); P5 (редагування ready не демотує в чернетку); P6 (мітка копії лише з ready/published, не чернетки); P7 (копіювати в події лише новини — свята вже там, копія=дубль); P8 (вихід «← Меню» чистить articleDraft); P10 (не губити категорію при зміні типу).
- **Батч B (пайплайн, юніт-тестовано):** P4 (`publish_shotam` дедуп за заголовок+`event_date` з ОНОВЛЕННЯМ на місці — раніше exact-title дедуп тихо ковтав нові/річні/відредаговані свята → зникали; юніт-тест пройдено); P9 (`cabinet.py` дедуп будь-який статус, не лише draft — AI-редактор не перестворює опубліковані свята щодня); P11 (під-кроки `sync_cms.main` ізольовано); P3-safe (`cms-sync.yml` `cancel-in-progress: false` — синк не скасовується посеред роботи → усуває головний розсинхрон).
- **C:** cron синку 15хв→**5хв** (швидша публікація).
- **Знахідка «в адмінці не показуються»:** віджет «Найближчі події громади» при порожньому `events.json` показує fallback зі СТАТИЧНОГО `holidays.json` (`community-blocks.js:885`) — ці свята НЕ cms_articles, тому в адмінці їх нема. + латентність 15хв + ready≠published.

**⏳ ВІДКРИТІ РІШЕННЯ (чекають Рому):**
- **C-instant:** кнопка «Опублікувати» одразу тригерить синк через Supabase Edge Function → публікація ~3хв. Потрібен GitHub-PAT у Supabase secrets (ручна дія). НЕ роблено.
- **Повний P3 (mark-after-push):** архітектурна зміна CI, не тестується локально — лишив (cancel-in-progress + P11 знизили ризик). Робити під наглядом першого прогону.
- **Іконки таббар+сайдбар + 2 заголовки-емодзі** (див. вище).

**Інфра:** `illiabogdanets041@gmail.com` = **Вовина пошта** (з'явилась у комітах, Рома підтвердив). Автостеження owner-watch тримав активним (перезапускав після тайм-аутів).

---

## 📝 2026-07-12 (нова сесія) — /byyou Потік 8 «Найближчі події» (PR #382, задеплоєно)

**Гілка `claude/new-session-nvoaif`.** «Робимо мої потоки» → пріоритезував мої потоки (8→7→9), Потік 7/9 заблоковані на вводі (вибір стилю іконок / дати сіл), тож стартував **Потік 8** (був спланований+узгоджений, чекав «так»).

**Мета:** «Найближчі події» = 3-тя керована рубрика в адмінці (поруч Новини/Свята) з ручним відбором + фото. Корінь задачі (свята у віджеті = емодзі замість фото) — пайплайн уже дає фото через Wikimedia, проблема була в охопленні + що новина не могла стати подією.

**Зроблено (12 кроків, усе задеплоєно PR #382, live):**
- Крок 1: AI-редактор свят `days_before` 7→31д (`editor/missions/holidays.json`) — ширше охоплення. Тест: вікно 7д=1 свято, 31д=4.
- Кроки 5-6: `admin.html` — категорійний екран «Статті»: 3 плитки (news/holiday/event) з лічильником (перевикористав `.tile`-патерн `renderHomeGrid`), тап → `renderArticles` фільтрує за `a.type` + «← Категорії». Нова стаття з типом обраної категорії. `artCategory` скидається при виході в меню.
- Крок 4 🔴: SQL `scripts/supabase_cms_featured_event.sql` — колонка `source_article_id bigint references cms_articles(id) ON DELETE SET NULL` + індекс (ідемпотентно). **Блок для Вови дано в чаті. ⏳ Чекає ручного застосування** (MCP Supabase у сесії без авторизації).
- Кроки 7-9: кнопка «📌 Скопіювати в Найближчі події» на опублікованій статті/святі (ready/published, type news/holiday) → відкриває форму НОВОГО запису `type=event` з копією фото/тексту/категорії/гео, модератор ставить дату події й зберігає (INSERT з `source_article_id`). Позначка «📌 у Найближчих подіях» на оригіналі (Set `copiedFrom`) проти дублів. `saveArticle` тепер зберігає `source_article_id`.
- Крок 10: звірено `sync_cms.py` — `fetch_shotam_ready` бере `type in (holiday,event) & status=ready` → `cms_to_event` (рядок 110 переносить `image`) → `events.json` → віджет «Найближчі події» з ФОТО. Без змін коду.
- Крок 11: синтаксис inline-скрипту `admin.html` (вилучення через Node), py_compile sync_cms/calendar. **Живий адмін-клік неможливий** (потрібен адмінський акаунт + застосований SQL) — як завжди в адмін-потоках.

**🔴 Умови щоб фіча ожила наживо:** (1) Вова застосовує SQL; (2) адмінський акаунт для кліку. До SQL — категорійний екран працює, кнопка копіювання дає «Не вдалося зберегти».
**🤝 Зона:** `admin.html` формально «Системне» (Вова), фіча спільна — позначено в PR/BOARD. Колізії не було (останнім чіпав admin.html мій же Потік 2/3).

## 📝 2026-07-12 (нова сесія) — фікс A (зона дотику табло) + C (коментар icons.js)

**Гілка `claude/new-session-nvoaif`** (рівно на origin/main). Два дрібні фікси зі скрін-аналізу Роми, обидва моя зона, «Роби» дано.

- **A — зона дотику табло новин (регресія мого `scrollEdgeCatchersHtml` з 12.07):** `.scroll-edge-catch` типова ширина 18px, а бокове поле `.cm-block--news .cm-block-body` = 10px; override був лише `bottom: 8px`. Ловець (z-index:2, pointer-events активні) залазив +8px на картку → тап по краю заголовка новини не відкривав статтю. Фікс: `.cm-block--news .scroll-edge-catch { … --edge-w: 10px; }` — ловець = рівно gutter, на картку не залазить. Візуал незмінний. CACHE `cstl-20260712-1400`→`1410`.
- **C — коментар `icons.js:18`:** казав що users-іконку задедуплено включно з admin.html — неправда, `admin.html:741` тримає власну `ICO_USERS` (standalone без бандлера, за задумом Потоку 7 не чіпав). Не код-баг → поправив коментар (2 копії зведено; admin.html — власний мірор, синхронити вручну). Коментар-only, поведінка незмінна.
- **Тест:** node --check + node build.js exit 0 (29 файлів, check-imports чисто), bundle.js diff = лише коментар. Живий смоук elementFromPoint — пропоную перед /finish за бажанням Роми.
- **Файли:** `style/community.css`, `src/core/icons.js`, `bundle.js`, `sw.js`. Журнал: `_session-log/roma-2026-07-12b.md`.

---

## 📝 2026-07-12 (день) — ХЕНДОВЕР: іконки (пауза) + bugfix-батч (готово) + Потік 8 (план, чекає «так»)

**Гілка `claude/new-session-gj49q9` — після rebase на свіжий `origin/main` (51 коміт origin був попереду, історія розійшлась через давні squash-мерджі; злито чисто, конфліктів 0). Усе нижче — ЛОКАЛЬНІ коміти, НЕ запушено, НЕ задеплоєно.**

### 🟡 Потік 7 (іконки) — ПАУЗА, чекає вибір стилю
Задача: замінити ~150 літеральних емодзі по всьому застосунку на векторні іконки (Tabler/Meteocons, MIT). **ВИПРАВЛЕННЯ (12.07): кроки 1-7 зроблено** (не 1-4 як писав раніше — дедуп потрапив у bugfix-коміт через `git add -A`): `src/core/icons.js` (20 спільних іконок); `src/core/weather-icons.js` (погода Meteocons base64, не hotlink через ID-колізії DOM); `weather.js` мертвий код прибрано; **дедуп users/phone/pencil зроблено** — `board.js` (`USERS_ICON_SVG=ICONS.users`, `PHONE_ICON_SVG=ICONS.phone`, `EDIT_ICON_SVG=ICONS.pencil`), `messages-ui.js` (`GR_SVG.users=ICONS.users`), `community-modal.js` (`PENCIL_ICON_SVG=ICONS.pencil`) — старі дубльовані SVG-рядки зведені до одного джерела, вигляд не змінився. Кроки 8-12 (рештки літеральних емодзі по board/sidebar/account/news/events/buses/power/index.html) — НЕ почато.
**Показано 36 варіантів стилю іконок** (скріншоти: жирний контур/тонкий/залитий/дуотон/неоморфізм/скло/3D/наліпка/пунктир/крапка-бейдж/чіп/9 реальних бібліотек Feather-Heroicons-Phosphor-Lucide-Iconoir-Bootstrap-Ionicons-MDI-Octicons/3 кольорові палітри). Рома: «з іконками подумаю поки». **Наступна сесія: спитати чи є вибір, тоді продовжити крок 5.**

### ✅ Bugfix-батч (готово, локальний коміт `4afc20ae`→rebased `b178d14f`)
Живий скрін-репорт від Роми під час роботи, 5 пунктів:
1. **«Кабінет»→«Адмінка»** (сайдбар, `sidebar.js`) — плуталось з «Особистий кабінет» мешканця.
2. **Табло новин, зона дотику для скролу сторінки** — системний утиль `scrollEdgeCatchersHtml()`+`.scroll-edge-catch` (`core/utils.js`+`community.css`): невидимі смужки НАД краєм картки (не змінюючи візуал) звужують зону що ловить внутрішній скрол стрічки, решта падає крізь до скролу сторінки. Перевірено `elementFromPoint` по пікселях + скріншотом (0 візуальних змін). Застосовано ЛИШЕ до табло (Рома підтвердила — інших місць з такою проблемою нема).
3. **Збережені → 2 екрани** (`saved-hub.js` переписано): спершу список категорій (Статті/Автобуси/Обговорення/Оголошення) з лічильником, тап відкриває список саме цієї категорії з кнопкою «← Назад». Раніше — один суцільний список секцій.
4. **Профіль — прізвище/село/вулиця зникають після збереження** — НЕ код-баг, `saveProfile()` (`auth.js`) вже має чесний `partial`-фолбек якщо в БД нема розширених колонок. Дав SQL-блок (`scripts/supabase_profiles_extended.sql`, вже існував у репо, просто не запущений). **Рома: «Вова вже зробив», але Я НЕ МІГ перевірити (Supabase MCP не авторизований у цій сесії).** ⏳ **Наступна сесія / Рома: перевірити наживо** (Мій кабінет → заповнити прізвище/село/вулицю → зберегти → вийти-зайти → чи лишилось).
5. **Правило роду звертання зафіксовано в документах** — `/CLAUDE.md` і `CSTL NEWS VOVA/CLAUDE.md` тепер прямо кажуть: Вова і Рома — чоловічі імена, і звертання і саморефлексія Claude — завжди чоловічий рід.

### 🟡 Потік 8 (Найближчі події) — СПЛАНОВАНО, чекає «так» на старт Фази 2
Джерело — те саме обговорення фото. Дослідницький агент з'ясував реальний код: пайплайн AI-редактор→кабінет Алли→sync_cms→articles.json/events.json ВЖЕ працює і ВЖЕ дає реальне фото (Wikimedia Commons, підтверджено — hotlink працює, стара нотатка в holidays.json про блокування Wikipedia стосувалась гіршого підходу). Проблема — не блокування фото, а що: (а) мало свят у вікні AI-редактора (7 днів), (б) звичайна новина не може «стати» подією для віджета.
**Уточнено з Ромою (2 раунди питань):** «Найближчі події» має стати 3-ю повноцінною категорією в адмінці (поруч Новини/Свята), з ручним (не автоматичним) відбором модератором — кнопка «Скопіювати в Найближчі події» на опублікованій статті копіює фото+текст у новий запис типу `event`.
**План — 12 кроків у `BYYOU_PLAN.md` Потік 8:**
- Крок 1 (AI-редактор вікно 7→31д) — 🟢 не зроблено технічно, рекомендація дана (лишити щоденний крон, не місячний — дедуп вже рятує).
- **Крок 2 (список свят) — ✅ ЗРОБЛЕНО, коміт `71b88765`.** +8 свят через WebSearch (звірено з zakon.rada.gov.ua + церковний календар ПЦУ 2026): День Соборності (22.01), Медовий Спас (01.08), Горіховий Спас (16.08), Введення в храм (21.11), День Гідності та Свободи (21.11), Андрія з Калитою (30.11), Вербна неділя (05.04), Радониця (21.04). Було 25→стало 33, id 9026-9033.
- Кроки 3-12 (SQL 🔴 нова колонка `source_article_id`, категорійна навігація в `admin.html` — перевикористати `.tile`-CSS з `renderHomeGrid`, кнопка копіювання, перевірка sync_cms) — НЕ почато, чекає «так» на Брамі старту.
**⚠️ Побічний інцидент:** запустив важкий `/deep-research` воркфлоу (25 агентів) для простого фактичного питання (список свят) — Рома справедливо зупинила, замінив на 2 звичайні WebSearch. Урок: для простих фактів — прямий пошук, не важкий воркфлоу.

### 🟡 Потік 9 (Дні сіл громади) — ЗАПЛАНОВАНО, чекає ДАНІ від власника
Рома: треба точна дата «Дня села» для кожного з 17 сіл + AI-стаття до дня. **Блокер:** дати в інтернеті НЕМА (перевірено WebSearch) — гіперлокальне, знають лише сільради. Claude не вигадує. Власник має дати таблицю (село→дата→церква/деталі). Технічна частина (village_days.json + місія AI-редактора, мірор holidays) — готова робити коли будуть дані. Деталі — `BYYOU_PLAN.md` Потік 9. Стартувати після Потоку 8 (спільна editor/-інфра).

**▶️ Наступна сесія — почати з:**
1. `git fetch origin` (звичка, хоч і щойно синхронізовано).
2. Спитати Рому: (а) вибір стилю іконок Потоку 7, (б) підтвердити SQL профілю запущено, (в) «так» на старт Потоку 8 Фази 2, (г) чи є таблиця дат Днів сіл (Потік 9).
3. Якщо «так» на Потік 8 — Фаза 2 з кроку 1 (AI-редактор вікно) або кроку 3 (SQL 🔴, потребує окремого підтвердження — вже 🔴-позначено в плані).

---

## 📝 2026-07-11/12 (ніч) — /byyou Потоки 2+3: AI-кнопка статистики + сітка іконок адмінки (PR #356)

Продовження Потоку 6 тим самим заходом («Дороблюй що ще треба доробити»).

**Потік 2 — AI-кнопка «Оновити статистику»:**
- Нова Edge Function `supabase/functions/ai-analytics-summary/index.ts` (Deno/TS, мірор структури наявного `send-chat-push`): гейт лише `is_admin` (перевірка через таблицю `admins` напряму, не покладаюсь на клієнт — інакше будь-хто залогінений міг би смітити платні виклики Claude), агрегати за 7 днів рахує САМА на сервері (`service_role`, не довіряє клієнтському payload — захист від роздутих фейкових запитів), відправляє в `claude-sonnet-5` (без `web_search`) із проханням дати короткий аналіз+2-3 рекомендації українською.
- По кнопці, НЕ за розкладом (`cron`) — рішення Роми ще з обговорення: платиш лише коли реально дивишся статистику, не щодня.
- `admin.html`: кнопка розблокована, клік → `supa.functions.invoke('ai-analytics-summary')`, результат/помилка в текстовому блоці під кнопкою.

**Потік 3 — сітка іконок адмінки:**
- Головний екран тепер сітка плиток (як робочий стіл iPhone) — Рома це описувала ще на самому початку розмови про статистику, зробила окремим кроком після основного дашборду.
- `menuItems()` — єдиний список пунктів (icon/label/count-функція/показ за дозволами), використовується і сіткою, і рядком «← Меню» з назвою розділу.
- Бейдж-лічильник у кутку плитки (неопубліковані/до модерації тощо), 0 не показує.
- Прибрано мертвий CSS старого горизонтального слайдера (`.tabs`/`.tab`/`.tab.active`/`.tab-count`).

**Тест:** синтаксис (вилучений inline-скрипт `admin.html` через Node; TS Edge Function — баланс дужок, структура мірорить перевірений `send-chat-push`), баланс `<div>`/`<button>`/`<svg>`. Живого кліку в реальній адмінці не робила (потрібен адмінський акаунт + деплой Edge Function — та сама межа що й раніше).

**🔴 Ручна дія власника:** `supabase functions deploy ai-analytics-summary` — живий деплой Edge Function (Supabase CLI недоступний з цієї сесії, як і з P-2 раніше).

CACHE НЕ чіпала — `admin.html` і Edge Functions не в кеші Service Worker (не в `STATIC_ASSETS`). Live v2720 · 12.07 00:58. Гілка `claude/stats-ai-icongrid` (тимчасова).

---

## 📝 2026-07-11/12 (ніч) — /byyou Потік 6: Статистика Потік 1 — трекінг+дашборд адмінки (PR #354)

Окрема розмова з Ромою (не список Вови) — статистика й аналітика застосунку. Довге узгодження вимог (скільки заходить/звідки/скільки часу/AI-висновок замість сухих цифр) — звела до 2 потоків: Потік 1 (це) без AI, Потік 2 (окремий /byyou) з кнопкою AI-висновку.

**Ключові рішення з розмови:**
- Тільки свій Supabase — без Google Analytics/Plausible (Рома явно підтвердила після плутанини «сторонні»/«власні»).
- Вік — НЕ вираховується, НЕ нова колонка. Прочитала `account-ui.js` (крок 4 плану) і побачила: анкета профілю вже збирає `birth_date` (дату народження) — окрема колонка `age` була б гіршим дублюванням (застаріває щороку). Виправила SQL-міграцію (крок 1) вже В ПРОЦЕСІ потоку, не після.
- Село — вже є (`SETTLEMENTS`, обирає сам користувач) — не прив'язувала GPS до найближчого села (запропонувала й відмовилась сама: похибка телефона переплутає сусідні села, а профіль і так точний).
- AI-звіт — НЕ за розкладом (`cron`), а по кнопці «Оновити статистику» (ідея Роми — економія: платиш лише коли реально дивишся).
- Іконки адмінки (сітка замість слайдера) — Рома скинула посилання на Tabler Icons (MIT); завантажила реальні SVG з github.com/tabler/tabler-icons (не намалювала з памʼяті) для 9 вкладок.

**🔍 Знахідка під час коду (крок 5):** RLS `profiles` — тільки власний рядок (`uid = auth.uid()`), адмінка НЕ могла б читати чужі профілі для розподілу по селах/віку. Замість відкривати сирі профілі всім адмінам — нова SECURITY DEFINER `admin_profile_stats()` (той самий патерн що вже є `is_admin()`/`has_editor_perm()`): рахує агрегати ВСЕРЕДИНІ бази, назовні віддає лише кошики («12 людей 18-25» тощо), жодного конкретного профілю не видно.

**Зроблено:**
- `scripts/supabase_analytics.sql` (новий) — таблиця `analytics_events` + RLS + функція `admin_profile_stats()`.
- `src/core/supabase.js` — `logEvent(visitorId,type,{tab,meta})` (fire-and-forget, не блокує UI), `fetchAnalyticsSummary()`.
- `src/app.js`/`src/core/boot.js` — трекінг переходів по вкладках (`window.switchTab`, єдина точка) + PWA-встановлення (`appinstalled`).
- `admin.html` — нова вкладка «Аналітика» (мірор наявної `renderSpend()`), 9 іконок емодзі→SVG, кнопка AI задизейблена (Потік 2).

**Тест:** `node --check` + build + check-imports (27 файлів чисто), `admin.html` без бандлера — синтаксис вилученого inline-скрипту через Node + баланс `<div>`/`<svg>`, Playwright-смоук перемикання вкладок (0 `pageerror`, `logEvent` фейлить тихо поки таблиці нема в БД).

**🔴 Ручна дія (дано Вові в чаті, 2 SQL-блоки):** `scripts/supabase_analytics.sql` ще НЕ застосовано в БД (MCP `supabase` без авторизації в цій сесії) — до цього вкладка «Аналітика» покаже нулі, решта сайту не постраждає.

CACHE `cstl-20260712-0055`, live v2717 · 12.07 00:44. Гілка `claude/stats-analytics-dashboard` (тимчасова — той самий force-push permission block що й раніше). 🔜 Далі — Потік 2 (кнопка AI-висновку), окремий `/byyou`, ключ `ANTHROPIC_API_KEY` Вова вже додав у Supabase secrets (за його словами, не перевіряла сама — нема доступу MCP supabase в цій сесії).

---

## 📝 2026-07-11 — /byyou Потік 5: Обговорення — FAB, іконки, лайк-лічильник, клавіатура (PR #349)

Список задач Вови (💬 Обговорення, 6 пунктів) — перевірила детально, звірила з реальним кодом, злила п.5+6 (клавіатура/зсув модалки — той самий фікс) → 4 задачі. Блок 📊 Статистика — рекомендувала як окрему архітектурну розмову (0 інфраструктури зараз), Рома погодилась, вирішила окремо.

- **FAB-стандартизація:** Обговорення й Дошка мали різний CSS для однакової кнопки-плюс (скло/50px vs суцільна заливка/56px) — прибрала дублювання, обидві тепер на спільному `.board-trigger--fixed`.
- **Емодзі→іконки:** 💬/👥/📢 → SVG. Знахідка: `COMMENT_ICON_SVG` вже існував у коді, але ніде не використовувався — переюзала.
- **Лічильник лайків (новий):** ❤️ на картці теми, реюз `reactions`/`setReaction`/`subscribeReactions`/`getAnonId` з `supabase.js` (таблиця+RLS вже готові). **Важливо:** та сама інфраструктура, яку Вова сьогодні ж (Д-13) повністю прибрав з оголошень Дошки («на маркетплейсі не доречні») — спитала Рому чи це не небажаний збіг; підтвердила що ні (окреме рішення Вови по Дошці, тут інша концепція для Обговорень). Гейт `requireAuth` (як і збереження) — гість бачить тост-пропозицію увійти, стан не змінюється.
- **Лічильник учасників:** перенесено з низу картки у верхній правий кут, над прев'ю останніх 2 повідомлень, навпроти лічильника повідомлень.
- **Клавіатура композ-модалки:** прибрала синхронний `ta.focus()` (вибивав клавіатуру ДО завершення анімації відкриття аркуша, перекривав форму). Тепер клавіатура — лише по тапу в поле; коли відкрита, аркуш сам підіймається над нею (новий `attachSheetKeyboardFix` — мірор вже перевіреного `applyKb`-патерну з `openChatModal`, adaptований під спільний примітив `core/modal.js`). `openDiscSheet` тепер пробрасує `onClose` для коректного знімання listener'ів.
- Тест: Playwright-смоук (гість-режим) — FAB обох вкладок, 0 літеральних емодзі в корені Обговорень, лайк-клік з гейтом, регресія Дошки (label+бейдж на місці). Живий тест композ-модалки з клавіатурою не робила (гейтована `requireAuth`, нема тестового акаунта — та сама межа що й у Потоці C2).
- Мердж з main: рутинний `sw.js` конфлікт + Вовин PR #347 (тінь заголовка модалки) — нуль перетину рядків, чисто.

CACHE `cstl-20260711-1845`, live v2702. Гілка `claude/disc-fab-icons-likes` (тимчасова — force-push на `claude/new-session-gj49q9` заблокований дозволами сесії).

---

## 📝 2026-07-11 — Погода: актуальна година в скрабері + вирівняний фон (PR #345)

Прямий запит Роми зі скріном (не /byyou): вертикальна лінія в модалці «Погода по годинах» мала показувати актуальну годину за геодаими (фолбек — Київ), і давати перетягувати вліво-вправо щоб бачити іншу годину.

- Скрабер (`community-blocks.js`) вже вмів перетягування — новим був лише автопоказ. Актуальна година рахується з `_wxData.utc_offset_seconds` (Open-Meteo вже віддає її при `timezone=auto`, який враховує реальні координати запиту — геолокацію користувача або фолбек-координати Олики, що дають той самий часовий пояс що й Київ). Автопоказ лише коли відкрито «Сьогодні» — для інших днів немає сенсу «зараз».
- Дрібне UX-покращення в процесі: курсор раніше ховався після відпускання пальця (`clear()` на `pointerup`) — тепер лишається видимим, зручніше звіряти погоду на обрану годину. Мертву `clear()` прибрала.
- Другий запит того ж скріна: фон графіка температури не збігався з графіком опадів (світліший) — вирівняла. Бульбашка опадів дублювала емодзі погоди з бульбашки температури — прибрала (лишила година+%).
- Тест: Playwright з мокнутим `Date`+Open-Meteo API — курсор рівно на очікуваній годині (математично звірено з offset), зникає для не-сьогоднішніх днів, фон обох графіків збігається (`getComputedStyle`), бульбашка опадів без іконки.
- Синхронізувала з Вовиними правками Дошки (модалка НП+дата, фільтр категорій) — нуль перетину рядків, один рутинний конфлікт `sw.js` CACHE_NAME.

CACHE `cstl-20260711-1753`, live v2695. Гілка `claude/weather-scrubber-currenthour` (тимчасова — той самий force-push permission block що й раніше цієї сесії).

---

## 📝 2026-07-10 (нова сесія, продовження) — /byyou Потік C2 звужено: Board ad modal + Disc sheets (PR #328)

**Гілка:** `claude/c2-modal-standardization` (тимчасова — force-push на `claude/new-session-gj49q9` заблокований дозволами сесії, запушила під новою назвою). Продовження того ж дня, четвертий `/byyou`-потік. Задеплоєно (CACHE `cstl-20260710-2246`, live v2654):

- **Прочитала реальний код** усіх 4 запланованих цілей C2 (з `docs/PLAN_MODALS_STANDARDIZATION.md`) — 2 виявились архітектурно непридатні для спільного примітиву:
  - **Ad zoom sheet** — насправді ДВІ дубльовані реалізації (`expand()` — FLIP-морфінг картки в модалку за координатами джерела; `openAdModalStandalone()` — той самий вигляд без картки-джерела). Автор свідомо задублював, щоб не чіпати робочий морфінг.
  - **Discussion chat modal** (`openChatModal`) — keyboard-aware realtime чат: динамічно рухає top/height/bottom під клавіатуру, жести на бульбашках, скрол-пігулка.
  - Обидва свідомо ВИКЛЮЧИЛА з примітиву замість форсувати — задокументувала причину в `docs/PLAN_MODALS_STANDARDIZATION.md`.
- **Мігрувала 2 архітектурно прості:** форма «Нове оголошення» (`community-modal.js`, `openBoardModal`) — власний z-index:2700 збережено модифікатором `.app-modal--board-compose` (модалка відкривається і поверх екрана «Мої оголошення»); Disc sheets (`board.js`, `openDiscSheet`) — тонка обгортка над примітивом, сигнатура `opts→close`+`onMount(sheet,close)` незмінна (4 виклики не чіпала).
- **🐛 Знайшла й виправила ще один критичний баг:** `openDiscSheet` мав temporal dead zone (TDZ) — `close` читався в `onMount` до завершення деструктуризації, кидало `ReferenceError` при КОЖНОМУ відкритті. Обидва входи гейтовані `requireAuth` (гість не пройде живий UI-клік) — перевірила прямим ES-module викликом функцій (юніт-тест), спіймала баг до деплою.
- Прибрано мертвий CSS: `.cm-board-modal/backdrop/panel/handle/close`, `.disc-sheet-backdrop/handle/head/close`.
- Синхронізувала гілку з Вовиними правками шапки Дошки (PR #325-327, поріг розгортання + лінія під заголовком) — файли перетнулись (`board.js`, `community.css`) але РЯДКИ не перетинались, merge пройшов чисто. Конфлікт лише в `sw.js` CACHE_NAME (стандартний, вирішено новим timestamp).

**Тест:** `node --check`+build+check-imports (26 файлів чисто) + прямий unit-тест обох функцій через ES-module import (position:fixed, z-index 2700 збережено, форма+прев'ю+свайп-закриття працюють, Disc sheets відкриваються без помилки).

**Увесь потік /byyou (Батч 1-8 + аудит + Потік C1 + Потік C2) завершено і задеплоєно.** BYYOU_PLAN.md → Статус: done.

## 📝 2026-07-10 (нова сесія, продовження) — /byyou Потік C1: спільний примітив модалки + 4 overlay (PR #322)

**Гілка:** `claude/new-session-gj49q9`. Продовження того ж дня, третій `/byyou`-потік. Задеплоєно (CACHE `cstl-20260710-1929`, live v2643):

- **Новий `src/core/modal.js`+`style/modal.css`:** спільний примітив модалки, 2 варіанти — `sheet` (виїжджає знизу) і `center` (scale-in картка). Мігровано 4 модалки: Power («Як дізнатись чергу»), Sidebar («Підтримка»/«Політика»), Account («Приєднайтесь»/«Доповніть профіль»), Weather («Погода по годинах» — графіки/скрабер НЕ чіпала, лише chrome). Прибрано ~150 рядків дубльованого CSS.
- **🐛 Критичний баг знайдено й виправлено:** коментар у `modal.css` містив буквальний `*/` (`pw-help-*/acc-*`) — передчасно закрив CSS-коментар і «з'їв» правило `.app-modal { position: fixed }`. Без фіксу всі 4 модалки рендерились би поза екраном. Знайшла через діагностику чому Playwright не міг клікнути кнопку (bounding-box поза viewport) — не з першого асерту (текст/класи проходили, позиція — ні). Деталі бісекшену — `_session-log/roma-2026-07-10.md`.
- **Побічна знахідка:** Power-модалка код-мігрована, але вкладка «Світло» прихована з UI ще з 16.05.2026 — живий UI-смоук неможливий, не регресія.
- Оновила `docs/PLAN_MODALS_STANDARDIZATION.md` — C1 done, C2 (sheets/compose)/C3 (chat/lightbox/popover) лишаються окремими потоками.

**Тест:** `node --check`+build+check-imports (26 файлів чисто) + живий Playwright-смоук усіх 4 модалок (position:fixed, bounding-box у межах екрана, відкриття/закриття, body-скрол-лок знімається) + мокнутий Open-Meteo для перевірки скрабера погоди.

**Увесь потік /byyou (Батч 1-8 + аудит + Потік C1) завершено і задеплоєно.** BYYOU_PLAN.md → Статус: done.

## 📝 2026-07-10 (нова сесія, продовження) — /byyou Сесія Б: Push чату (P-5/8/9/2) + аудит перенаправлень (PR #321)

**Гілка:** `claude/new-session-gj49q9`. Продовження того ж потоку що Сесія А, того ж дня. Задеплоєно (CACHE `cstl-20260710-1547`→`1714`, live v2640):

- **P-5:** `ensureChatPush()` в `openChat` (board-chat.js) — реально просить дозвіл push при тапі «відкрити чат» (жест користувача). Раніше `registerChatPushDevice` мовчав, якщо не було наявної підписки (юзер ніколи не вмикав автобусні сповіщення → ніколи не отримував запит і для чату).
- **Новий `src/core/push.js`:** VAPID_PUBLIC_KEY+urlBase64ToUint8Array+`ensurePushSubscription()` винесено з `buses.js` (було задубльовано) — тепер спільний модуль, `buses.js` теж рефакторено на нього.
- **P-8:** банер вхідного push-повідомлення чату коли застосунок у фокусі (`#chat-push-banner`, style/messages.css) — тап відкриває розмову. Раніше було тихо (лише бейдж).
- **P-9:** тап по системній нотифікації відкриває САМЕ ту розмову. `sw.js` форвардить `thread_id`/`title`/`body` у postMessage і в `notification.data`; `notificationclick` постить у відкритий клієнт АБО (холодний старт) відкриває `#/thread/<id>` — новий `openThreadById()` (board-chat.js) + hash-роутинг в `app.js` (той самий патерн що інвайти груп `#/join/<uuid>`).
- **P-2:** `send-chat-push/index.ts` — `msg.text.length` на `null` (фото-повідомлення) валив функцію (500, отримувач БЕЗ пуша). Фікс за патерном `send-group-push` (еталон): `photo_url` у select + `msg.text || (photo_url?'📷 Фото':'')`. **Код готовий, живий деплой Edge Function НЕ в цьому PR** — потрібен `supabase functions deploy send-chat-push` (Supabase CLI, за Вовою — інфраструктурне обмеження цієї сесії, не хендофф).
- **Аудит перенаправлень Громади:** Дошка-віджет (міні-Дошка/Розмови) тепер відкриває САМЕ тапнутий пост (`openAdModalStandalone` export з board.js + `openChatById`) замість глухого перемикання вкладки. Автобус-картка тепер веде на конкретний рейс (`openSavedRouteOnBuses`, реюз Батч 7) — **раніше тап взагалі нічого не робив** (реальна знахідка, не було жодного клік-хендлера). Погода/контакти — перевірено, вже коректні, не чіпала.

**Тест:** Playwright-смоук — тап по Дошка-віджету відкрив зум-модалку САМЕ тапнутого поста (скрін звірено: заголовок/текст співпали), тап по автобус-картці перемкнув на вкладку+підсвітив рейс (`.bus-card--flash`), 0 page/console errors (крім мокнутих мереж).

**Побічно (після деплою):** додав 2 ранні інформаційні рівні (🟡35%/🟠55%) у `byyou-context-guard.sh` — раніше сторож попереджав лише на 60%/75%.

**Увесь потік /byyou (Батч 5-8 + аудит) завершено і задеплоєно.** BYYOU_PLAN.md → Статус: paused.

## 📝 2026-07-10 (нова сесія, продовження) — /byyou Сесія А: іконки модалки + хаб «Статті»/«Автобуси» + FAB-фікс (PR #319)

**Гілка:** `claude/new-session-gj49q9`. Рішення Роми: «Вові не передавай роботу» → Батч 6/7 (крос-зона `board.js`/`buses.js`) зробила сама. Розбито на 2 сесії (75-80% контексту) — це СЕСІЯ А, СЕСІЯ Б (Батч 8 Push+аудит) далі.

**Задеплоєно (PR #319, CACHE `cstl-20260710-1547`→прод показує `1548` після наступного коміту Вови, live v2639):**
- **Б5 (модалка статті/події):** 3 іконки зверху (`.modal-sticky-header`): 📤 поділитись (завжди), 🔔 нагадування (лише події/свята — `.ics`), 🔖 зберегти (нове — `localStorage cstl_saved_articles`, лише статті). Прибрано старі великі кнопки внизу + мертвий CSS (`.ev-ics-btn`, `.share-btn--inline`, `.article-source-actions`).
- **Хаб «Збережені» (saved-hub.js):** +секція «📰 Статті» (доступна й гостю — локальне сховище, не Supabase) і «🚌 Автобуси».
- **Б6 (FAB Дошки):** прибрано `transform` з `.board-fab-item` (лишився тільки `opacity`) — усунуто WebKit «привид» подвійного рендера. Пом'якшено перефарбування тригера 0.62→0.52.
- **Б7 (автобуси):** `export getSavedRoutesForUI` + нова `openSavedRouteOnBuses(rid,date,from,to)` (перемикає день/фільтр, скролить+підсвічує картку). Прибрано стару окрему кнопку-закладку «Збережені рейси» в шапці + слайд-модалку (~430 рядків мертвого коду) — функціонал переїхав у хаб. Дані (`loadTrackedRoute`/`hydrateTrackedFromDB`/`onAuthChange`) і банер лишились недоторканими (пастка з плану — не викинула).

**Інцидент (сама знайшла+виправила):** `check-imports.js` при білді спіймав висячий виклик `renderSavedRows()` (лишився в `requestPushForSavedRoute` після видалення модалки) — замінила на `updateBannerBell()`.

**Тест:** Playwright-смоук (390×844, локальний сервер, open-meteo/nominatim замоковані): іконки модалки (share/remind/save — правильна видимість для новин vs подій), save toggle+persist у localStorage, хаб показує статтю і відкриває її повторно, FAB відкривається без глюку (computed `transform: none` — підтверджує фікс), стара кнопка-закладка відсутня в DOM.

**Merge з main:** злито двічі (Вовині #312-#318 Дошка-футер, авто-новини/автобуси) — конфлікти лише в `sw.js` (CACHE_NAME, вирішено вручну) і раз у `bundle.js` (перебілджено).

**🔜 Далі (СЕСІЯ Б, наступний чат `/byyou`):** Батч 8 (Push P-5/8/9 фронт + P-2 код, без живого деплою Edge Function) + аудит перенаправлень інших блоків Громади (Дошка-віджет/автобус/погода/контакти).

## 📝 2026-07-10 (нова сесія, продовження) — Батч 4: фото подій у карусель + перенаправлення на конкретну картку

**Гілка:** `claude/new-session-gj49q9`. Продовження BYYOU-плану (`PLAN_2026-07-09_batch.md` Батч 4).

**Зроблено:**
- `renderEventBlock` (`community-blocks.js`) тепер пробрасує `id`+`image` для подій і `id` для свят — раніше викидались у `.map()`.
- Мініатюра фото 44×44 у правому верхньому куті картки-табло (`.evh-thumb`, CSS `community.css`) — тільки коли `it.image` є; свято лишається на emoji-градієнті як було. `onerror` прибирає биту картинку + знімає модифікатор класу.
- **Знайшов і виправив колізію сам:** мініатюра спершу заїжджала під бейдж категорії (обидва в правому верхньому куті) — спіймано смоуком (Playwright, мокнута картинка), не візуально на око. Фікс: `.evh-card--photo .evh-top { padding-right: 54px }` звільняє місце бейджу.
- **Перенаправлення тапу:** картка каруселі раніше мала `data-switch-tab="shotam"` (просто перемикала вкладку). Тепер `data-ev-id`, делегований клік у `renderEvCarousel` викликає `openShotamModal(id)` (нового `export` з `events.js`) — відкриває САМЕ цю подію/свято в статейній модалці, а не просто вкладку.

**Тест:** Playwright-смоук (390×844, локальний http-сервер, open-meteo/nominatim замоковані): карусель рендериться (5 карток fallback-свят, бо `events.json` досі увесь у минулому), тап відкриває `#article-modal` з правильним контентом (перевірено скріном). Окремо мокнута майбутня подія з фото — мініатюра рендериться, колізія з бейджем підтверджена і закрита скріном до/після.

**Не зробив (усвідомлено відкладено, з плану):** аудит перенаправлень ІНШИХ блоків Громади (Дошка-віджет, автобус, погода, контакти) — Дошка-віджет вже позначений у плані як «відкладено окремо»; решта потребує окремого проходу, не робив цього разу щоб не розростатись за межі Батча 4.

CACHE `cstl-20260710-1126` → `cstl-20260710-1309`.

## 📝 2026-07-10 (нова сесія) — фікс іконки снігових злив (brain-контроль)

**Гілка:** `claude/new-session-gj49q9` (харнес закріпив цю, не `3k5u9n` — див. журнал сесії нижче).

**Знайдено brain-контролером (перевірка C2) + виправлено:** `weatherCodeInfo()` (`community-blocks.js:67`) не покривала WMO-коди 85/86 (снігові зливи) — падали в default `{icon:'🌡️',text:'—'}`. Живий прогноз Open-Meteo цього тижня (`[1,2,3,61,80,95]`) баг не показав — спіймано лише кодовим аудитом, не смоуком. Фікс: додано `if (code <= 86) return {icon:'🌨️', text:'Снігові зливи'}` перед гілкою грози. CACHE `cstl-20260710-1126`.

**Заодно перевірено (без фіксу, ОК):** C1 (SW network-first для bundle.js/CSS — код коректний, живі cache-control заголовки перевірено, але реальний пристрій з раніше встановленим PWA не тестував — нема девайса); C3 (живий Open-Meteo+Nominatim — схема відповіді збігається з очікуваннями коду, розбіжностей нема).

## 📝 2026-07-10 — Батч 3: модалка погоди «по годинах» зі скрабером (PR #310)

**Гілка:** `claude/new-session-3k5u9n` (Рома: «не міняй гілку»), потік `/byyou`. Журнал: `_session-log/roma-2026-07-10.md`.

**Задеплоєно PR #310 (CACHE `cstl-20260710-0807`):**
- **Модалка погоди по годинах** (`src/tabs/community-blocks.js`): графік температури (лінія + права шкала °C + підписи годин кожні 2 год) і опадів (**сині бари** градієнт `#4DA3FF→#2F80FF` + права шкала % 0/50/100 + 2год). Спільна WX-геометрія (`wxGeom`).
- **Скрабер** (нова `wireWeatherScrubber`): перетягуєш палець по графіку → снап до найближчої години → спільний вертикальний курсор на ОБОХ графіках синхронно + бульбашка «іконка погоди · HH:00 · значення» (° / %). Іконка з `hourly.weather_code` на кожну годину.
- **Свайп вниз** закриває модалку (нова `wireWeatherSwipe`, поріг 90px; ігнорує старт на графіку — там скрабер).
- CSS (`style/community.css`): `.wx-grid` (сітка), `.wx-cursor`/`.wx-readout` (курсор+бульбашка), precip темніша підкладка, `touch-action:none`.

**item 10 (зона скролу Табло) — партнерське вирішення:** при мерджі `origin/main` виявив що **Вова вже закрив item 10 на main** (padding-підхід) — уже задеплоєно. Мій margin-інсет (коміт 61d6c7f0) відкинуто на користь живої версії Вови (результат ідентичний). Не клобрив чужу задеплоєну роботу (зона-принцип).

**Інциденти:** смоук-скрін висів на «Завантаження…» — `getCityName` (nominatim) не замокан, fetch зависав у Promise.all → route-мок на nominatim+open-meteo. `git checkout --theirs` на весь community.css спершу затер би weather-CSS → відкотив `git checkout -m`, вирішив вручну лише 2 item-10 блоки. Merge-конфлікти sw.js(CACHE)+community.css вирішено.

**🔜 Далі:** Батч 4 — фото подій із опублікованої статті + аудит перенаправлень блоків Громади.

## 📝 2026-07-09 — Табло колір + карусель подій + компактні контакти + авто-журнал (PR #285, #293)

**Гілка:** `claude/new-session-3k5u9n` (харнес прив'язав, Рома: «не міняй гілку»). Через `/byyou`. Живий журнал: `_session-log/roma-2026-07-08.md`.

**Задеплоєно (2 потоки):**
- **PR #285 — Табло + Карусель:** (1) Табло новин колір `#a8283a`→винно-багряний `linear-gradient(162deg,#7B303C,#722F37,#481C24)` (як борд Автобусів, не рожевий; віньєтка підсилена). (2) Краї стрічки: Рома забракував «м'які краї» → **смужки-розтушування `::before/::after` ПОВНІСТЮ прибрано** (вицвітали текст карток). (3) Блок «Найближча подія»→**карусель** (`renderEventBlock` переписано: трек translateX, до 5 карток, авто-ротація 6с реюз hero-патерну, крапки, тап→shotam) + **fallback Г-16** (подій 0 → найближчі свята з holidays.json, cover_emoji+gradient). Заголовок→«Найближчі події».
- **PR #293 — Б6/Г-10 контакти:** місцеві ВГОРУ (амбулаторія/сільрада/ЦНАП), екстрені ВНИЗ компактною сіткою 3×2 (`.cm-contact-grid-3`/`.cm-contact-chip`); велику картку «Швидка 103» прибрано; мертвий CSS (`cm-contact-hero*`/`grid-2x2`/`tile*`/`cm-pulse-border`) видалено.

**Процес-апгрейд (PR #285):** вшито авто-журнал у скіли — `NEW_SESSION_PROMPT`/`startuem` Крок 0 п.7 (завести `_session-log/<роль>-<дата>.md`, дописувати по ходу) + `/finish` Крок 0+8 переписано (звіт для brain з журналу, вичерпно). Щоб майбутні сесії писали стан коректно самі.

**Тест кожного потоку:** node --check + Playwright-смоук (390×844, усі вкладки, 0 помилок застосунку) + аудит. Скріни звірено з Ромою. CACHE до `cstl-20260709-0803`. Live-перевірено (community.css має #722F37+grid-3, bundle має cm-ev-carousel).

**⏳ Заблоковано/на потім (записано в BOARD):**
- **Б5 контакти з адмінки** — 🔴 нова таблиця Supabase `community_contacts` + MCP не авторизований.
- **P-2 (фото-пуш падає)** — фікс в Edge Function `send-chat-push` (код готую), але авто-деплою Edge Functions нема → жива публікація через Supabase CLI (окрема авторизована сесія).

**🔜 Далі:** Push-блок — P-5/P-8/P-9 (фронт, деплою) + P-2 (код готовий, деплой Supabase окремо).

## 📝 2026-07-08 (пізній вечір) — hero Громади: залипання вітання без дьоргання + блюр-фрост під карткою (PR #270, #271)

**Дві ітерації по hero-екрану Громади (зауваження Роми зі скрінів iPhone):**

**1. Вітання дьоргалось при скролі (PR #270).** Корінь: воно рухалось через JS (`translate3d` щокадру на подію скролу), а iOS при інерційному скролі не шле події безперервно → елемент відставав і трясся. Фікс: перевів на `position: sticky` (CSS-липкість — браузер тримає елемент на композиторі/окремому GPU-шарі, БЕЗ участі JS) → дрож фізично неможливий. Затримка перед відпусканням +33% (~280px). **Пастка Chromium, яку довелось обійти:** sticky НЕ тримає, якщо запас робити через `padding` контейнера — потрібен РЕАЛЬНИЙ блок-розпірка (`.cm-greeting-stickpad`). Перевірив 5 структур у Chromium поки знайшов робочу.

**2. Блюр обоїв — «слідкує за карткою» (PR #270 → уточнення #271).** Спершу (#270) зробив смугу що росла вгору від верху картки — Рома: неправильно, висить окремим шаром НАД карткою. Фінал (#271): блюр = **фрост-підкладка ПІД Табло** — площина `backdrop-filter` на всю ширину, нижчим шаром за карткою (герой `z0` < картка `z1`); ВЕРХ приклеєний до верху верхньої картки, тягнеться вниз до низу героя. Фото над карткою чітке; від краю картки і нижче — матове (видно по боках картки). Картка левітує над фростом, і той їде за нею при скролі. Керує `wireHeroBlur()` на `.app-main` (rAF).

**Файли:** `src/tabs/community.js` (wireHeroBlur + скелетон стик-зони), `style/community.css` (.cm-greeting sticky + .cm-hero-blurband). Обидва деплої зелені (#2471, #2472), сайт оновлено. Смоук: Chromium на 4 глибинах скролу, геометрія+рендер підтверджені скрінами.

**⚠️ Інцидент git (не критично):** PR #271 при мерджі дав конфлікт — робоча гілка `roma/plan-save` відстала від `main` після squash #270 (стара версія блюру vs нова). Рішення: `git checkout -B roma/plan-save origin/main` + cherry-pick лише дельти фросту (чисто наклалась, бо контент #270 уже в main) → force-with-lease → мердж. Урок знову той самий: перед PR брати свіжий `origin/main`.

**🔜 Черга наступного /byyou (з BYYOU_PLAN, НЕ зроблено):** Б2/Б3 карусель подій (макет намальовано, чекає «ок»); Б5 контакти з адмінки (🔴 нова таблиця Supabase — погодити схему). **⏳ Вова:** досі чекає `scripts/supabase_profiles_extended.sql` (без нього село/телефон в анкеті не збережуться).

## 📝 2026-07-08 (вечір) — шапка/хаб збережених/кабінет жителя (потік /byyou header-rework)

**Шапка:** лого до бургера, лічильник версії по центру (абсолют), погоду з шапки геть (initWeather=no-op, блок Громади живе), справа [🔖][👤]. **Хаб «Збережені»** (новий src/core/saved-hub.js): bottom-sheet з категоріями Обговорення/Оголошення (saved_posts→posts), тап → вкладка+модалка (export openChatById у board.js); гість → «Увійти». **Greeting:** без підзаголовка, дата 11→13px. **Hero:** вечірній набір за 2 год ДО заходу (EVENING_LEAD_MS); підпис під «Олика» = що на фото (всі 8 — костел Св. Трійці; структура {src,caption} per-фото). **Кабінет жителя (2 корені):** (а) initAuth не тягнув profiles → вітання не персоналізувалось до відкриття кабінету — тепер warmProfile на старті/вході; (б) БОЙОВА перевірка REST: колонок settlement/surname/phone НЕМА в БД (міграція profiles_extended НЕ застосована) → село мовчки губилось з тостом успіху; тепер saveProfile повертає partial і тост чесний. **⏳ Вова:** виконати scripts/supabase_profiles_extended.sql. Також: фантом-чернетки самолікуються (PR #260, heal у sync_cms).

## 📝 2026-07-08 (день) — hero день/вечір за сонцем + фікс кабінету published + хвости

**Hero Громади (потік /byyou roma/hero-daynight, кроки 1-9 ✅):** Вова залив 4 денних + 4 вечірніх фото (старі olyka-1/2/3 видалив — ротатор бив у неіснуючі!). Тепер набір обирається за сходом/заходом сонця: `sunTimes` (формула NOAA для Олики) в utils.js, звірена з Open-Meteo (±5хв, 4 дати року). Живе перемикання на тіку ротатора. Свайп і крапки видалено (рішення Роми) — лише авто-слайдшоу. Фото стиснуто 9.8МБ→2.0МБ. + хвіст: HTTP-таймаут агента 180с→420с. Смоук (Київ TZ): день/вечір/ніч ✅.

**Кабінет (PR #258):** редагування ОПУБЛІКОВАНОЇ статті мовчки знімало статус у базі (стрічка тримала далі) → розсинхрон «на сайті є, в кабінеті чернетка» (кейс «генератор від Тайваню»). Тепер published недоторканий (форма чесно попереджає), «У чернетки» оновлює дату. Розслідування: sync 20:31 позначив усі 3 published без помилок; агент дублікатів не створював. Відкрито: фантомна чернетка «генератор» (Рома: тап «Опублікувати» — дедуп закриє) + SQL-діагностика у Вови; заголовок «СЕЛО МИТЕЛЬНО ТОП» — Рома каже агент, лог #13 показує чистий (загадка, чекає SQL).

## 📝 2026-07-08 (ніч) — агент: пакети до 10 + щогодинний долив + прямі джерела + анти-галюцинація

**Пакети (PR #255):** 10 статей одним викликом модель не тягне (виходило 3) → пише пакетами ≤4, цикл до заповнення. Крон 6год→щогодини: опублікували → агент за ≤1 год сам доливає до ~10 (порожні прогони безкоштовні — REST-рахунок без AI). Захисти: стеля $1.20/прогін, ліміт 4 пакети, стоп на порожньому. **Прогін #15 у бою: 9 чернеток, $1.31, само-ремонт двічі врятував таймаути.** Кабінет = 10.

**Прямі джерела + валідація (PR #256, з ресерчу curl_cffi/CloakBrowser — самі тули не знадобились, stdlib):** (а) промпт Громади отримує 8 свіжих новин з офіційного olytska-gromada.gov.ua (ID новини = unix-час → фільтр ≤14 днів; кеш на прогін; fail-soft); (б) `url_alive` — HEAD-перевірка джерел, 404/410 = вигаданий лінк → джерело викидається, стаття без живих джерел відкидається. **Бэклог:** Reader-крок повних текстів (web_search і так читає сторінки); CloakBrowser/Facebook — ні (ToS Meta).

**Разова примітка:** заплановану статтю «Метельне» старий баг встиг випустити в ефір (до фіксу #253) — Рома в курсі, не критично.
**🔜 Далі:** тиждень спостерігаємо якість пакетного циклу; підняти HTTP-таймаут виклику API (таймаути в пакетах з'їдають ~2 хв кожен, само-ремонт рятує але повільно); Потік C (модалки) відкладено; Push-блок.

## 📝 2026-07-07 (пізній вечір) — агент-саморемонт + фікси публікації + потік /byyou аудит-фіксів

**Агент новин (PR #252):** «зелене але чернеток нема» — корінь `stop_reason=max_tokens` (8192 обрізав JSON на ~10 статей → 0 валідних). Фікс: max_tokens 16000 + рятівний парсер (повні `{…}` з обрізаного JSON) + само-ремонт (0 валідних → 1 повтор з переписаним запитом) + **запобіжник витрат** (стеля $0.60/прогін і $15/місяць — не зациклиться, рішення Роми). Прогін №13: 3 статті в кабінет ✓, $0.356.

**Публікація з кабінету (PR #253):** (а) cms-sync падав КОЖЕН прогін — `feedparser` не ставився у workflow → жодна опублікована стаття не доходила до табло; тепер імпорт стійкий (feedparser лише для RSS-парсингу). (б) «Зберегти й опублікувати» мовчки ігнорував обраний автопостинг → заплановане публікувалось одразу; тепер розклад має пріоритет + кнопка стає «📅 Запланувати автопостинг». Після фіксу: усі 3 статті агента в табло (cms 20:31).

**Потік /byyou `roma/audit-fixes`** (5 🟡-знахідок аудиту, кроки 1-10 ✅, чекає «деплой»): фільтр не блокує ebook/ebay/ebola (стем 'еб'→довші, тести 47+47); гість у чаті обговорення після входу отримує поле вводу (було — глухий кут); doSave чесний алерт якщо публікація не вдалась; CabinetSink зберігає source_url; SQL CHECK наздогнав БД (scheduled/holiday, git-only). Смоук Chromium зелений.

## 📝 2026-07-07 — AI-редактор Блок 2: кабінет редагування + автопостинг на дату/час (PR #240, /byyou)

**Кабінет (admin.html «Статті»):** контрол «Лишити чернеткою / 📅 Автопостинг на дату-час» + поле дати свята/події; бейджі типу (🎉 Свято/📅 Подія/📰 Новина) + дата автопостингу у списку. **Фікс бага:** `saveArticle` форсив `type='news'` → редагування AI-чернетки свята ламало тип; тепер type зберігається.

**Публікатор `sync_cms`:** `promote_scheduled` (scheduled з насталим `publish_at` → ready) + `publish_shotam` (type=holiday/event → `data/events.json` для «Шо в селі»). Новини-Громада → `articles.json` як раніше. cms-sync (кожні ~15хв) тепер комітить і events.json.

**Схема:** `scripts/supabase_editor_scheduling.sql` — колонки `publish_at` + `event_date` (Вова запустить). Block1 cabinet-sink тепер зберігає дату свята в event_date. Маршрут «Шо в селі»=events.json — рішення Роми (варіант A).

**Реюз, не переписування** (sync_cms/кабінет розширено). Тести зелені: cms_to_event=точна схема events.json, admin.html JS node --check, py_compile. Сайт не чіпано.

**⏳ Чекає Вову:** `SUPABASE_SERVICE_ROLE_KEY` + SQL `supabase_editor_scheduling.sql` + логін → щоб автопостинг працював наживо. Доти код готовий.
**🔜 Далі:** Блок 3 — міграція новин/Волині на editor/ + семантичний дедуп. Або перевірка живого циклу коли Вова дасть ключ.

## 📝 2026-07-07 — AI-редактор Блок 1: пакет editor/ + місія «Свята» (PR #238, /byyou)

**Системна платформа-редактор** (рішення Вови: «не скрипт, а редактор з архітектурою куди довішувати апгрейди»). `docs/AI_EDITOR_ARCH.md` — ядро pipeline `Source→Read→Filter→Write→Image→Sink`, плагіни за інтерфейсами (новий конектор/фото/sink = файл+рядок у реєстрі, ядро незмінне). Місії декларативні (`editor/missions/*.json`).

**Пілот «Свята»:** `calendar` (свято за 7 днів до дати) → `ai_writer` (Anthropic пише статтю + image_query **українською** — тест: укр.≈англ. на Wikimedia, для місцевого краще) → `wikimedia` (фото) → `cabinet` (чернетка в `cms_articles` draft) або `queue` (файл-fallback без ключа). Workflow `editor-holidays.yml` (cron щодня). Лічильник — той самий `data/ai_spend.json`.

**Реюз, не переписування:** `parse_rss`/`ai_news_agent` недоторкані; старий новинний бот працює далі (міграція — Блок 3). Тести зелені (конвеєр наскрізь на 4 святах, image-плагін, queue+дедуп, py_compile). Сайт не чіпано.

**Раніше сьогодні також:** реальні фото Wikimedia до 3 свят у стрічці (#237, на проді) — тест підбору картинок показав що hotlink працює (403-концерн застарів) і якість залежить від запиту (поганий → чуже фото, напр. герб РФ).

**⏳ Чекає Вову:** `SUPABASE_SERVICE_ROLE_KEY` + `SUPABASE_URL` + SQL `supabase_editor_cabinet.sql` — щоб чернетки писались у кабінет наживо (доти queue-fallback). Ключів для фото НЕ треба (Wikimedia).
**🔜 Наступне:** Блок 2 — кабінет: редагування чернеток + автопостинг на дату/час (Шо в селі + Новини-Громада). Блок 3 — міграція новин/Волині на editor/.

## 📝 2026-07-07 — Лічильник витрат AI + оптимізація автопостингу новин (PR #235, /byyou)

**Тригер:** Вова — «автопостинг жере багато грошей, оптимізувати системно». **Ключовий діагноз (сперечався по даних):** НЕ дорого. Реальні дані консолі Anthropic за липень — вхід 382k · вихід 22.5k · пошуків 24 · **разом ≈ $1.7/міс**. Агент зупинився не через дорожнечу, а через **порожній баланс (−$0.04)**. Профіль **17:1 вхід:вихід** → єдина справжня неефективність = некешований вхід у циклі `pause_turn` + запуски у вже повну чергу.

**Задеплоєно (Фаза 0, гілка `roma/ai-spend-counter` → PR #235 squash `0bfe6398`):**
- **`scripts/ai_news_agent.py`:** `call_agent` акумулює `usage` (вхід/вихід/кеш/веб-пошуки) крізь весь `pause_turn` цикл, повертає `(text, usage)`. Prompt caching: `cache_control` ephemeral на промпті + рухома мітка на зростаючому префіксі (кеш-міток ≤2, ліміт 4 не перевищено). `record_spend` рахує $ → `data/ai_spend.json`. **Гард:** skip API якщо `len(queue) >= QUEUE_MAX_SIZE`.
- **`admin.html`:** таб «💸 Витрати AI» (admin-only) — місяць/усього/останній + журнал, читає `data/ai_spend.json`.
- **`ai-news-agent.yml`:** комітить `data/ai_spend.json`.
- Тести: py_compile ✓, логіка $0.6345=ручний перерахунок ✓, кеш-міток 2 ✓, node --check renderSpend ✓. CACHE_NAME не потрібен (admin.html не в SW).

**⏳ Чекає від Вови (поза кодом):** поповнити баланс Anthropic **~$20** (≈рік роботи при $1.7/міс) — без цього агент і лічильник не оживуть; branch protection на `main` (виявилось `protected:true` вже стоїть — Вові підтвердити правила require-PR).

**Урок git:** знову «стала git після рестарту контейнера» — локальний `main` розбіжний (unrelated histories), робота була ціла на `origin`. Правило: брати чистий стан через `git checkout -B <гілка> origin/main`, не довіряти локальному `main`.

## 📝 2026-07-07 — Фінал обоїв + fix витоку «Кабінет» + git-синк хук (PR #232, #233)

**⚠️ Головна історія — ФАЛЬШИВА ТРИВОГА «втрачена робота».** Після рестарту контейнера сесія продовжилась на **застарілому локальному git** (знімок 06.07 15:23). Уся вчорашня робота (#216–#230: табло, AI-агент, консоль, кабінет, сайдбар, hero-parallax) **виглядала зниклою** — насправді була на `origin` увесь час. Я довго теоретизував про «втрату» замість першої правильної дії — `git fetch`. **Корінь: не зробив fetch при відновленні сесії.** (Виправлено хуком #233.)

**#232 — фінальний полиш обоїв Громади** (єдине що реально не дійшло — деплой 08:07 завис): hero 340→430px; низ фото тане в бежевий (`--paper`) через `.cm-hero::after`; блюр фону при скролі — клас `.cm-blur` (4px) тоглиться слухачем `wireHeroBlur()` на `.app-main`; «Олика»+точки підняті над бежевим низом. `community.css`+`community.js`.

**#232 — fix(sidebar): «Кабінет» витікав УСІМ.** `.sidebar-item{display:flex}` перебивав атрибут `hidden` → team-only пункт бачили всі користувачі. Додано `.sidebar-item[hidden]{display:none}`. Спіймано смоуком Chromium.

**#233 — SessionStart git-синк хук** (системний фікс кореня): `.claude/hooks/session-git-sync.sh` + реєстрація в `settings.json` з `matcher:"startup|resume"`. Робить `git fetch origin` і голосно попереджає якщо локальний main/гілка позаду origin. Синхронний, таймаут 20с, fail-soft. Уже спрацював наживо при цьому ж `/finish`.

**Кабінет редактора — розблоковано:** підтверджено що `is_team_member()` у `supabase_editor_cabinet.sql` повертає true для адмінів (`is_admin()`) АБО `editor_users`. Дав Вові чисту 2-крокову інструкцію. **Вова запустив КРОК 1** (таблиці Supabase створено). **КРОК 2** (INSERT Алли у `editor_users`) чекає її першого входу через Google. Вова/Рома — кабінет автоматично (адміни).

🧹 Прибрано локальні гілки-артефакти плутанини (`roma/hero-polish`, `roma/restore-0706`); origin-видалення блокує git-проксі (403) — лишились нешкідливо.

## 📝 2026-07-06 (ніч-4) — UI-батч зі скрінів Роми (PR #216) + концепт Громада-рамка

Рома фідив скріни+описи (режим «записуй, не міняй»), потім згрупував → `PLAN_UI_06-07.md` (9 зауважень, 3 зони). Задеплоєно батч #216:
- **🅒 Блок новин Громади (#9):** прибрано nested-скрол-вікно (`max-height:420`+overflow) + червону маску (sticky-фільтри прозорі) → жива стрічка тече в сторінці, останні 15, без обрізань. `community-blocks.js` (slice 15) + `community.css`.
- **🅐 «Шо в селі» (#1/#2/#3):** прибрано календар+фільтри (`events.js`: видалено renderCalendar/renderFilters/CATEGORY_FILTERS/selectedDate/activeFilter, спрощено getFiltered) → жива стрічка; картка з **повноширинною обкладинкою** (`.shotam-card`); шапка «📰 Шо в селі» (`.shotam-head`).
- **#4/#5 Обговорення:** прибрано `←` назад (головна сторінка) + `→` на CHAT-картці (обидві discussion-specific, Дошка ціла).

**🆕 Концепт Роми — «Громада = рамка "Шо в селі"»:** головна = відповідь «що в селі» (greeting→фото→заголовок «Шо в селі»→віджети). Рішення: назву на вкладці ПОКИ лишити; заголовок над віджетами Громади + ранжування Г-4 — майбутній крок. Деталі — `PLAN_UI_06-07.md`.

**🔜 Залишок:** Потік 2 (Обговорення: винести discussions.js + dual-root board.js + FAB #6/#7/#8) — свіжий чат. Концепт Громада-рамка — окремо.

## 📝 2026-07-06 (ніч-3) — Обговорення: фікс таб-бару + системний рефактор Потік 1 (PR #214, #215)

**Скарга Вови:** в Обговореннях зникав таб-бар (overlay `.disc-screen` z1010 накривав `.tab-bar` z1000); після 1-ї спроби фіксу — різалось коло «Громада». Вова попросив глибокий аудит + системне рішення.

**Діагноз (агент прокопав board.js):** Обговорення — НЕ модуль, а `board.js` що рендерить `activeType='chat'` у 2-й корінь `#disc-content` в fixed-overlay. Спільний singleton-стан із Дошкою, dual-root через `discOpen`/`getBoardRoot`. **9 крихкостей** (F1 dup-id 280мс, F2 втрата стану Дошки, F3 FAB робить оголошення не тему, F4-F9). Дослідження (web.dev/Chrome): постійна нижня навігація — стандарт, overlay що ховає її — анти-патерн; окремі сторінки кращі. Карта агента + фікси — у BYYOU_PLAN (архів) / цьому записі.

**#214 — проміжний фікс кола:** `.disc-screen` z1010→950 (під таб-баром) — коло не ріжеться, таб-бар клікабельний. Симптом-латка.

**#215 — Потік 1 (de-overlay, СИСТЕМНО):** `#page-discussions` overlay → **справжня `.page` у `.app-main`**; таб-бар → `switchTab('discussions')`. `board.js` `openDiscussions`/`closeDiscussions` втратили overlay-механіку (показ на switchTab), викликаються слухачем `cstl-tab-changed`; `←`→Громада; синхронне очищення `#disc-content` **прибрало 280мс dup-id вікно (F1 на закритті)**. Прибрано `.disc-screen` CSS, `cstlOpenDiscussions`; CTA→`switchTab('discussions')`. Смоук: `#bd-body`/`#board-fab` завжди =1, Дошка ціла, коло ціле, таб-бар видимий. Темний ✕-бар (що бачив Вова) зник — його породжував overlay.
- ⚠️ **Deploy #215 впав транзиентно** (`actions/deploy-pages`: «try again later» — інфра Pages, не код); ретригернуто docs-пушем.

**🔜 ПОТІК 2 (НОВИЙ /byyou, свіжий чат):** винести `src/tabs/discussions.js` окремим модулем + спільне read-only сховище постів + disc-namespace id (`#disc-*`) → повністю прибрати `discOpen`/`getBoardRoot`/`openDiscussions`/`closeDiscussions` dual-root з `board.js` (F2/F6) + discussions FAB «нова тема» (F3). Карта зчеплення — у BYYOU_PLAN.

## 📝 2026-07-06 (ніч-2) — техборг навігації + «Чати»→«Обговорення» (PR #213)

**Потік /byyou на `roma/nav-cleanup` → змерджено в main (squash 50613e7).**

**Техборг — прибрано мертвий код після «Шо в селі»:**
- `news.js` — прибрано сегмент-перемикач (`showNewsSegment`/`cstlShowNewsSegment`) + осиротілий рендер новин-табу (`renderGeoFilters`/`renderNews`/`setGeoFilter`/`getFiltered`/`GEO_FILTERS`/`activeGeo`/`UA_WORLD`); `initNews` тепер лише `ensureNewsLoaded`+слухач модалки. Лишено все що юзає блок новин Громади+модалка (`newsCardsHtml`/`renderRow`/`renderFeatured`/`badgesHtml`/`openArticle`/`ensureNewsLoaded`/кольори).
- CSS: `filters.css` — прибрано `.news-seg*`/`--news-seg-h`/`.filters-bar`/`#news-seg-news` (лишено `.chips-row`/`.chip`); `events.css` — мертвий `.ev-card`-кластер ~200 рядків (лишено `.ev-skeleton*`/`.ev-ics-btn`/`.shotam-*`/`.cal-*`); `news.css` — осиротілий `.news-list`.

**«Чати» → «Обговорення»** (нав-макрос, Вова дав «ок»):
- Слот таб-бару «Чати»→«Обговорення»; тап відкриває overlay Обговорень напряму (`window.cstlOpenDiscussions=openDiscussions` у app.js; кнопка `onclick="cstlOpenDiscussions()"`).
- Хаб `#page-chats`: «Повідомлення»+«Групи» приховані `display:none` — **код і хендлери лишено** (V2). Заголовок хабу→«Обговорення».
- Вихід з overlay: `← Назад` (bd-disc-back) або перемикання вкладки (cstl-tab-changed→closeDiscussions). overlay z-index 1010 > таб-бар (пре-існуюче).
- Приватний чат (Повідомлення) тепер без видимого входу — планово, повернеться у V2 / FAB Дошки (Д-14, зона Вови).

**Смоук Chromium (iPhone-viewport) зелений:** «Шо в селі» 11 карток+модалка, Громада-новини 6, Дошка, тап «Обговорення»→overlay, перемикання закриває. CACHE `cstl-20260706-1425`.
- ⚠️ Назви «Шо в селі»/«Обговорення» — тимчасові, за Вовою фінал.
- 🧹 Лишок техборгу (не критично): `news.css` `.news-card` (стара fallback-картка) — глянути окремо.

## 📝 2026-07-06 (ніч) — стартові скіли доправлено (PR #212)

**Потік /byyou на `roma/startup-skills` → змерджено в main (squash b7e8665). Лише процес-файли, сайт не чіпався.**
- `/startuem` (`NEW_SESSION_PROMPT.md` + дзеркало `startuem.md`): (1) **paused-/byyou авто-пропозиція** — старт сам пропонує продовжити на гілці власника де лежить paused-потік (не питає абстрактно про гілку — фікс кейсу цієї ж сесії); (2) явний `git pull origin main`; (3) новий крок «заяви активність у BOARD».
- **Новий `/start` для CSTL** — `.claude/commands/start.md`, репо-локальний алиас `/startuem` (реюз NEW_SESSION_PROMPT, не копія). Глобальний `/start` інших проектів не зачеплений; Вовин `/startuem` без змін.
- 2в: баланс черги ОК (агент run#4 success, ~120/добу ≫ 24/добу; черга 0 але статті свіжі — RSS-парсер годує незалежно); точки входу новин ✓. ⚠️ поглядати наступний cron-запуск `ai-news-agent.yml`.

## 📝 2026-07-06 (вечір) — «Шо в селі» задеплоєно + фікс сміття у статтях (PR #211)

**Потік /byyou на `roma/shotam-tab` → змерджено в main (squash 978a413), на проді.**

**Блок «Шо в селі» (слот таб-бару «Новини» → «Шо в селі», газета-іконка):**
- Рішення Вови/Роми по навігації: слот лишається на місці, сегмент «Новини» прибрано (новини живуть блоком у Громаді, 3 фільтри), сегмент «Події» трансформовано у стрічку «Шо в селі».
- `events.js`: картки в news-стилі (`.news-card-row` + cover emoji/gradient для свят) + модалка повного прочитання (реюз `#article-modal` зі свайпом/хрестиком); прибрано expand-in-card + IntersectionObserver; error→плейсхолдер один раз на контейнер.
- `index.html`: `page-news`(+сегмент+пейн новин)→`page-shotam` (лише стрічка); таб-бар «Новини»→«Шо в селі».
- `app.js` `switchTab`: legacy `news`/`events`→`shotam`; 2× `data-switch-tab` у Громаді → `shotam`.
- `events.css`: `.shotam-cover-thumb` + `.shotam-modal-cover`; офсет `#page-shotam` замість сегмента. CACHE `cstl-20260706-1220`.
- Смоук Chromium (iPhone-viewport): 11 карток, календар, фільтри, модалка, «Афіша →»→shotam, старий таб зник, 0 помилок від застосунку.
- ⚠️ **Назва «Шо в селі» тимчасова** (Вова ще працює над нею). Внутрішні id/файл лишились `events`/`#events-*`/`page-shotam` (низький ризик).
- 🧹 Техборг (не критично): у `news.js` лишився мертвий `showNewsSegment`/`cstlShowNewsSegment`; у `events.css` мертві `.ev-card*`; `filters.css` `.news-seg`/`#news-seg-*`. Прибрати при чистці (Н-9/СИ-5).

**Фікс сміття у статтях (зі скріна Вови — Волинь Post):**
- `clean_article_text()` у `fetch_full_article` (`parse_rss.py`): зрізає провідне меню («Правила Реклама Контакти…») + хвіст («Бажаєте дізнатися», «Теги:», «Вибір редактора», «Ctrl+Enter», Telegram-заклик). Тіло ціле, анти-фолспозитив (одиничне «Головна», нормальний текст) перевірено на тексті зі скріна.
- Спільна функція → покриває і RSS-парсер, і AI-агент (він reuse `fetch_full_article`). Оживе на наступному скрапі.

**Далі по зоні:** «Шо в селі» — фінальна назва+можливий редизайн від Вови; чистка мертвого коду навігації; кабінет Алли (Н-4); Push-блок Дошки (П-2/П-5/П-8/П-9).

## 📝 2026-07-06 — Перебудова Новин: AI-агент + вкладка Громада + старт «Шо там»

**Блок A — AI-агент новин (замість Google News), на проді:**
- `scripts/ai_news_agent.py` — Anthropic Sonnet + web_search → справжні URL. Місія «Громада» (Олика+села 50/50). Конфіг `scripts/hromada_config.json`, база `docs/HROMADA_KB.md` (17 НП).
- **Черга + крапельна публікація:** агент (cron 6 год) готує запас у `data/news_queue.json`; `scripts/publish_queue.py` (cron 2 год) викладає по 2. Workflow `ai-news-agent.yml` + `news-publisher.yml`.
- Анти-SSRF за IP (замість whitelist) + `SAFE_OPENER` (редиректи); повний текст із будь-якого видання; фікс bs4 4.15.
- **Фікс max_tokens 4096→8192** — модель обривалась на пошуку й не писала JSON (stop_reason=max_tokens, знаходило 0). Задеплоєно 08b6547. Оживе на авто-запуску.
- Фото: `fetch_og_image` — реальне фото зі сторінки видавця (og:image).
- Ключ `ANTHROPIC_API_KEY` у secrets (Вова додав).

**Блок B — новини у вкладці Громада, на проді:**
- `news.js` експортує `newsCardsHtml`/`ensureNewsLoaded`/`openArticle`; `community-blocks.js` `renderCommunityNews` — 3 кнопки (Громада/Волинь/Україна+Світ), вкладена прокрутка (`flex-shrink:0` + `overscroll-behavior:contain`), червоний фон блока (як шапка), кремові картки. Блок над «Корисні контакти».
- «Олика»→«Громада» (section_of/джерела/фільтр), Google News вимкнено у старому парсері.

**Бонуси:** фікс замерзлого денного ліміту (найсвіжіші перемагають); фікс заторів деплою (`deploy.yml` cancel-in-progress:true).

**Блок C — «Шо там» — СТАРТ, ПАУЗА (гілка `roma/shotam-tab`, /byyou paused):**
- Знахідка: `data/holidays.json` (25 свят 2026 + cover-емодзі/градієнт) ВЖЕ Є і показується в Події (`events.js`).
- Рішення Роми: свята → у «Шо там» (з Подій), вигляд як стрічка новин + модалка.
- Наступне (новий чат /byyou): побудувати вкладку «Шо там» + навігація (прибрати Новини з таб-бару).

**Інфра:** дав Вові інструкцію cron-job.org+PAT (авто-запуск без кліків) + CF-ключі для картинок; branch protection на main. Лист-введення для Алли (контент-менеджерка) готовий.

## 📝 2026-07-05 — Фундамент спільної роботи двох власників (гілка `roma/coop-foundation`)

Впроваджую архітектуру безпечної паралельної роботи (зона «Системне», з дозволу Вови на дошці). Що зроблено/в процесі:

- ✅ **BOARD.md** — нова дошка координації (активність / в роботі / треба зробити / питання / зроблено — по власниках) + HEADS-UP для Вови. Коміт прямо в main (`b3a512a`).
- ✅ **Стан розділено** — цей файл (`SESSION_STATE_ROMA.md`) + `SESSION_STATE_VOVA.md`; старий спільний 361 КБ → `_archive/SESSION_STATE_full.md` (git mv, історія ціла); `SESSION_STATE.md` → покажчик-заглушка. Вова вже почав вести свій файл.
- ✅ **Авто-мерж `claude/**` вимкнено** — `auto-merge.yml` → `.github/_archive/` (раніше тихо затирав роботу через `-X theirs`). PR #198 змерджено.
- ✅ **Скіли оновлено** — `/startuem` + NEW_SESSION_PROMPT (садити в `roma/`|`vova/`|`session-<дата>`, читати BOARD+свій стан+AUDIT), `/finish` (rebase перед PR, оновити BOARD, писати свій стан), розгорнутий brain-блок (+зона +чому +що не спрацювало), `/fix` `/byyou` посилання на свій стан.
- ✅ **Перевірено наживо:** імітація конфлікту — звичайний merge дає CONFLICT (спливає), старий `-X theirs` мовчки затирав. Діру закрито.
- ⏳ **Лишився 1 ручний крок Вови (поза кодом):** branch protection на `main` у GitHub (Rulesets → Restrict deletions + Block force pushes; НЕ вмикати Require PR — зламає парсери). Інструкцію дав.

### Раніше цієї сесії (уже в main)
- **Парсери за ТЗ Вови** (через claude-автомердж, до вимкнення): злиття Україна+Світ (60/40), fuzzy-дедуп (Jaccard ≥0.65, у межах розділу), ліміти 5-6/розділ (Олика без ліміту), Google News про Олику, події лише з громади, історії Олики крапельно, фото-плейсхолдер 🏰, фікс лого (castle-icon 797КБ→29КБ), health-монітор `scripts/health_check.py`. ⚠️ Парсер чекає РУЧНОГО запуску Вови (Actions → RSS Parser → Run workflow — cron GitHub затримує).
- **`docs/VISION.md`** — зведене бачення на 1 екран (синтез CONCEPT+PRODUCT_STRATEGY+MONETIZATION+RODMAP); нумерацію фаз у травневих доках позначено історичною (канон = RODMAP, 8 етапів).
- **Пропозиція навігації** — у `ПРОПОЗИЦІЇ.md` (Вові): Чати→Обговорення + Автобуси↔Події місцями; у AUDIT ревізія ПД-3/Н-9 (Події=провідна вкладка, Новини кнопкою всередині). Макет `[Події·Дошка·Громада·Обговорення·Автобуси]`. Вова підтвердив.
- Встановлено режим `/byyou` (напівавтономний потік + push-замок) + `/qa-explore` (Playwright смоук).

### Далі по зоні Роми
- Зустріч з Аллою (понеділок) → кабінет редактора (Н-4). Технічні рішення вже затверджені Вовою (Supabase, Google-вхід, RBAC, `news.publish`).
- Push-блок П-2/П-5/П-8/П-9 (не залежить від Алли).
- Оживити Волинь/Громаду у стрічці після ручного запуску парсера Вовою.

# BYYOU_PLAN — АУДИТ БЕЗПЕКИ (потік /byyou, CSTL) — 🔴 ВІДКРИТИЙ, НЕ ЗАВЕРШЕНИЙ

> 🔴 **Цей файл лежить в архіві не тому, що робота скінчена, а тому, що живий
> `BYYOU_PLAN.md` зайнятий іншим потоком (новини).** Аудит пройдено на **5 кроків
> з 14**, головна частина замовлення Вови (кроки 6-10 — форми на сміттєвий ввід)
> **не почата**. Хочеш продовжити — перенеси цей файл назад у
> `_ai-tools/BYYOU_PLAN.md` і клич `/byyou`. Нічого переміряти не треба, усі числа тут.
>
> ⚠️ **Мало не загубився.** Кроки 1-5 зробили 30-31.07, а коміти лишились **локальними
> на Mac Вови й не поїхали на GitHub**. Паралельний чат тим часом законно переписав
> `BYYOU_PLAN.md` планом по новинах. Для будь-якого нового чату цієї роботи просто не
> існувало. Врятовано 31.07 (PR цієї гілки) — і саме з цього виросло правило №11
> у `HOT_RULES.md`: **репозиторій — єдине джерело правди, незапушене = втрачене.**

**Статус:** `paused` · **Гілка на момент паузи:** `claude/startuem-devlock-ui` · **Старт:** 2026-07-30
**Ціль:** аудит безпеки і стійкості застосунку — секрети у відкритому тексті, доступ
до прихованих екранів за прямою силкою, зависання від тапів «не туди», поведінка всіх
кнопок і форм на сміттєвий ввід (щоб користувач не бачив системних помилок).

---

## Замовлення Вови (дослівно)

«Перевір мій код на дурні уязвимості, подивись, чи не оставив я прямо в тексті паролі,
чи не можна відкрити приховані екрани просто по прямій силці і чи не зависає все, якщо
натиснути не туди? Перевір всі кнопки і форми вводу, переконайся, що якщо людина введе
туди якусь «херню», програма не зламається і не видасть купу незрозумілого системного
тексту.»

---

## 🔬 РОЗВІДКА (зроблено до плану)

| Що | Число / знахідка |
|---|---|
| Обсяг коду | **42 файли, 19 659 рядків** у `src/` + `admin.html` 1 895 рядків |
| Полів вводу | **75** (20 text · 11 file · 6 email · 4 date · 3 password · 2 tel · 2 search · решта) |
| `innerHTML` | **154** місця · `escapeHtml()` застосовано **259** разів |
| `JSON.parse` поза `try` | **6** |
| Ключі в коді | `SUPABASE_ANON_KEY` = `sb_publishable_…` — **за задумом публічний**; `VAPID_PUBLIC_KEY` — теж публічний за призначенням. Приватних ключів у `src/` не знайдено |
| Помилки користувачу | йдуть переважно в `console.warn`, є `humanizeError()` (`supabase.js:715`) |

**🔴 Дві головні підозри, знайдені розвідкою:**

1. **`admin.html` — окрема сторінка, яку заслінка розробки НЕ закриває.** `dev-lock`
   стоїть у `src/app.js`, тобто стереже лише `index.html`. Адмінка відкривається
   прямою адресою `…/CSTL_NEWS/admin.html`. Вона перевіряє пошту у таблиці `admins`
   **у браузері** (`isAdmin = data.length > 0`) — а це рівно те, що зловмисник
   міняє одним рядком у devtools. Питання не «чи сховано меню», а **чи закритий
   сервер**.
2. **Уся справжня охорона тримається на RLS у Supabase** (Row Level Security —
   правила, які вирішують, чиї рядки можна читати й писати). Публічний ключ у
   `bundle.js` — це нормально рівно доти, доки RLS увімкнено на КОЖНІЙ таблиці.
   Якщо десь ні — будь-хто читає й пише напряму, і жодна кнопка в UI цього не змінить.

---

## Кроки

| # | Крок | Тип | Стан |
|---|---|---|---|
| 1 | ✅ Секрети — **ЧИСТО.** Ні паролів, ні приватних ключів, ні JWT, ні service_role — ні в коді, ні в жодному коміті історії. VAPID private у Deno.env. Дрібниці: `.gitleaks.toml` дозволяє СТАРИЙ VAPID-ключ (код на новому), і `bundle.js` виключено зі сканування | 🟢 | ✅ |
| 2 | ✅ `admin.html` віддається публічно (заслінки нема), але без входу — лише форма логіна; дані закриті сервером. Меню видно, даних ні. Не діра, але некрасиво (крок 11) | 🔴 | ✅ |
| 3 | ✅ **RLS ТРИМАЄТЬСЯ.** Анонім: чутливі таблиці 0 рядків, INSERT/UPDATE у posts/comments/profiles/admins/messages → 42501; PATCH чужого поста 0 рядків; security-definer функції → NOT_ADMIN/not admin. Сервер закритий | 🔴 | ✅ |
| 4 | ✅ **Хеш-роути СТІЙКІ.** Кожен має жорсткий regex (`#/join` тільки UUID, `#/thread`/`#/post` тільки `\d+`) — скрипт/`../`/текст не проходять (`if (!m) return`). Неіснуючий id → `if (!article) return` / `posts.find` = нічого. Не зависає, порожня модалка не відкривається. `openThreadById` ще й вимагає входу | 🟢 | ✅ |
| 5 | ✅ `data/*.json` відкриті за прямою адресою (відомо — заслінка ховає ДОДАТОК, не дані), але вміст **демо**: «Іван», телефони `123 45 67`. Справжніх PII мешканців нема — реальні пости в Supabase під RLS | 🟢 | ✅ |
| 6 | Форми, частина 1 — **текстові поля**: порожньо · 10 000 символів · емодзі · HTML і `<script>` · SQL-подібне · керуючі символи | 🟢 | ⬜ |
| 7 | Форми, частина 2 — **файли** (11 полів): не-картинка з розширенням `.jpg` · 50 МБ · 0 байт · SVG зі скриптом | 🟢 | ⬜ |
| 8 | XSS: пройти **154** `innerHTML` і знайти ті, куди дані користувача йдуть **повз** `escapeHtml()` | 🟢 | ⬜ |
| 9 | Зависання: подвійний і потрійний тап · тап під час завантаження · офлайн посеред дії · кнопки, що не розблоковуються після помилки | 🟢 | ⬜ |
| 10 | Системний текст в обличчя: перевірити, що **жодна** гілка не показує сирий `error.message`, стек чи код Postgres | 🟢 | ⬜ |
| 11 | Фікси всього знайденого — по одному, з поясненням кореня | 🟢 | ⬜ |
| 12 | Стенд-сторож `tests/robustness.mjs` — щоб знайдене не повернулось тихо | 🟢 | ⬜ |
| 13 | `/qa-explore` смоук + `/audit` + `node build.js` + повний `npm test` | 🟢 | ⬜ |
| 14 | Реліз-нотатки → брама деплою | 🟡 | ⬜ |

**Оцінка обсягу:** 14 кроків, аудит читає більше ніж пише ≈ **420-500K токенів ≈ 45-50% вікна.**
Кроки 2 і 3 — 🔴: якщо там діра, це вже не «дурна вразливість», а справжня, і я зупинюсь
та спитаю, перш ніж щось міняти.

---

## 📊 ПРОМІЖНИЙ ВИСНОВОК (кроки 1-5, станом на паузу)

**Жодної справжньої дірки не знайдено.** П'ять найгостріших питань закриті:

1. **Паролі / ключі у відкритому тексті** — нема, і не було в жодному коміті історії.
2. **Приховані екрани за прямою силкою** — `admin.html` відкривається публічно, але
   без входу дає лише форму, а до даних не пускає сервер.
3. **Сервер (RLS)** — тримається на КОЖНІЙ чутливій таблиці: анонім не читає, не
   вставляє, не редагує чужого.
4. **Зависання від «тапу не туди» через силки** — хеш-роути не зависають на сміттєвому
   чи неіснуючому id.
5. **Персональні дані у відкритих файлах** — тільки демо, не справжні люди.

**Дрібниці на потім (не дірки):**
- `.gitleaks.toml` дозволяє СТАРИЙ VAPID-ключ (код давно на новому) — косметика.
- `bundle.js` виключено зі сканування секретів — дублює перевірений `src/`, але формально «сліпа зона».
- `admin.html` без заслінки — чужому видно, що адмінка існує, і код її інтерфейсу.
  Рекомендація (крок 11): або накрити заслінкою, або лишити як є — **виклик Вови**.

---

## ⬜ ЩЕ НЕ ПЕРЕВІРЕНО (кроки 6-10 — головна частина замовлення Вови!)

Саме тут «людина вводить херню, а програма ламається». **Не почато:**
- **6. Текстові поля** (20 шт) — 10 000 символів, емодзі, `<script>`, керуючі символи.
- **7. Файли** (11 полів) — не-картинка з розширенням `.jpg`, 50 МБ, 0 байт, SVG зі скриптом.
- **8. XSS** — пройти 154 місця `innerHTML` і знайти, де дані йдуть повз `escapeHtml()`.
- **9. Зависання** — подвійний/потрійний тап, тап під час завантаження, офлайн посеред дії.
- **10. Системний текст в обличчя** — жодна гілка не має показувати сирий `error.message`.

---

## Де зупинились

**Статус `paused` (30.07, пізній вечір).** Вова попросив зупинити аудит і оновити
документацію для переходу в новий чат. Кроки 1-5 пройдено (висновок вище), **жодної
дірки**. Кроки 6-10 — головна частина («форми на сміттєвий ввід») — НЕ почато.

**Коміти цього потоку:** `48f0e8c7` (кроки 1-3) + `c521957f` (кроки 4-5) — були
**локальні, без push**, на гілці `claude/startuem-devlock-ui`. Врятовані 31.07: зміст
перенесено у цей файл на гілку `vova/repo-truth` від свіжого `main`.

### Як продовжити
1. Скопіювати цей файл назад у `_ai-tools/BYYOU_PLAN.md` (живий файл), **попередньо
   заархівувавши те, що там зараз лежить** — інакше повториться та сама втрата.
2. `/byyou` → режим ПРОДОВЖЕННЯ прочитає його і піде з **кроку 6** (текстові поля).

Нічого переміряти не треба — усі числа вище. Головна робота попереду, не позаду.

⚠️ **Два потоки одночасно живими бути не можуть** — `BYYOU_PLAN.md` один. Зараз у
ньому новини (рішення Вови 31.07). Аудит безпеки чекає своєї черги тут.

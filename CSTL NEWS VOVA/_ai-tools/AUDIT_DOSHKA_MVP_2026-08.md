# АУДИТ ДОШКИ ПІД MVP — 07.08.2026

> **Замовлення Вови:** *«тепер головне завдання в вкладці ДОШКА, на мою думку вона
> готова, але треба перевірити»* — повний аудит під одне питання: **чи можна
> запускати Дошку в MVP.**
>
> Метод: **міряти, а не переглядати.** Кожна знахідка — з числом або відтворенням у
> браузері через `tests/_board-fixture.mjs`. Проблеми заради кількості не вигадуються
> (пряма вимога Вови): те, що зроблено правильно, записано як сильна сторона.

---

## 1. КАРТА ЗОНИ

**Обсяг:** 9 751 рядок коду · 15 стендів (~3 160 рядків).

| файл | рядків | роль |
|---|---|---|
| `src/tabs/board.js` | 2 531 | стрічка, картка, сторінка оголошення, фільтри, пошук, FAB, гейт правил, скарги |
| `src/tabs/board-chat.js` | 1 675 | усе підменю FAB: чат, «Повідомлення», «Мої оголошення», «Збережені» |
| `src/tabs/board-discussions.js` | 1 031 | Обговорення (окрема вкладка, ділить FAB із Дошкою) |
| `src/tabs/community-modal.js` | 652 | модалка «Нове оголошення» |
| `style/board.css` | 3 671 | увесь вигляд зони |
| `src/core/board-categories.js` | 70 | таксономія: id, назва, колір, іконка |
| `src/core/board-shared.js` | 121 | `cardTitleText`, `clampChars` |

### Екрани і модалки — 19 точок

**Дошка:** стрічка → картка → сторінка оголошення (`openAdModalStandalone`) →
лайтбокс фото (`openPhotoLightbox`) → скарга (`openAdReportSheet`) · гейт правил
(`openBoardRules`) · нове оголошення (`openBoardModal`).
**Підменю FAB:** «Повідомлення» (`openThreadsList`) → чат (`openChat`) ·
«Мої оголошення» (`openMyAds`) · «Збережені» (`openSavedAds`).
**Deep-links:** `openChatById`, `openThreadById`, `openBoardItemById`.
**Обговорення** (ділять FAB): `openDiscussionCompose`, `openMyDiscussions`,
`openSavedDiscussions`, `openChatModal`, `openDiscActions`.

✅ **Сильна сторона.** Повноекранні шари йдуть через `core/layers.js`, тобто закриття
обслуговує системний жест iPhone «назад», а не власний свайп. Це вже коштувало
проєкту окремого блока роботи 02.08 і зроблено правильно в одному місці.

---

## 2. РЕВІЗІЯ НАЯВНИХ СТОРОЖІВ — і головна структурна знахідка

15 стендів Дошки покривають:

| стенд | що стереже |
|---|---|
| `ad-nophoto` | екран оголошення з фото і без — однаково |
| `ad-report` | скарга дійсно пише в базу |
| `ad-sheet-swipe` | свайп-закриття модалки |
| `board-card-row` · `board-card-foot` | геометрія картки, вирівнювання футера |
| `board-compose` | липка шапка модалки подачі |
| `board-cream` | нейтральні поверхні (без кремових) |
| `board-fab-clearance` | FAB не накриває останню картку |
| `board-fab-icon` | нижня кнопка залежить від стану |
| `board-header-flow` | згортання шапки при скролі |
| `board-offline` | збій бази називається збоєм |
| `board-refresh` | власна дія не кидає екран на початок |
| `board-rules` | гейт правил + блок безпеки |

### 🔴 ЗНАХІДКА A-1 — сторожі стережуть ВИГЛЯД, а не ПОВЕДІНКУ

**Місце:** `tests/` — набір стендів Дошки.
**Проблема:** з 13 предметних стендів **11 міряють геометрію, кольори й анімацію**.
Поведінкових — два (`ad-report`, `board-offline`). Без сторожа лишились саме ті
функції, які людина використовує щодня:

| не покрито | наслідок, якщо зламається |
|---|---|
| **пошук** | людина не знайде оголошення, і ніщо не впаде |
| **фільтри** (категорія, локація, комбінування, скидання) | те саме, мовчки |
| **порожній результат** пошуку/фільтра | екран без пояснення |
| **«Мої оголошення»** — статуси, «Підняти», кулдаун | автор не керує своїм |
| **«Збережені»** — додати/прибрати/повторно | закладки мовчки губляться |
| **«Повідомлення»** — список, порожній стан, вхід у чат | = відкритий **Д-28** |
| **створення оголошення** — валідація полів | брак у базі |

**Чому це проблема саме зараз:** вигляд ламається помітно — його видно на першому ж
знімку. Поведінка ламається **тихо**: пошук, що перестав шукати, виглядає як «нема
оголошень». Тобто сторожі стоять там, де дефект і так було б видно, і не стоять там,
де його не побачити.

**Критичність:** High (не блокує запуск, але робить кожен наступний реліз ризикованим).
**Рекомендація:** три стенди — пошук+фільтри, «Мої/Збережені», порожні стани.
**Складність:** середня, ~2-3 години; фікстура (`_board-fixture.mjs`) уже є.

---

## 3. ЗНАХІДКИ ПО КОДУ

### 🟢 A-2 — мертва гілка в обробнику FAB (Low, але з пасткою)

**Місце:** `src/tabs/board.js:1788`
```js
if (act === 'messages') { requireAuth('переглянути повідомлення', openThreadsList); return; }  // 1768
…
if (act === 'messages') { openThreadsList(); return; }   // 1788 — НЕДОСЯЖНИЙ
```
**Проблема:** рядок 1768 обробляє `messages` і робить `return`, тож 1788 не
виконується ніколи. Звірено по всіх гілках `act`.
**Чому це не просто сміття:** мертвий рядок кличе `openThreadsList()` **без**
`requireAuth`, а коментар поруч стверджує «requireAuth усередині» — це неправда для
цієї функції. Прибери колись верхній рядок як «дубль» — і замість помилки з'явиться
тихий обхід гейта авторизації. Той самий клас, що вже бив по проєкту (PR #638).
**Критичність:** Low зараз, High як пастка на майбутнє.
**Рекомендація:** видалити рядок 1788.
**Складність:** тривіальна, 1 рядок.

---

## 4. ЖИВИЙ ПРОГІН: ВХІД · СТРІЧКА · ПОШУК · ФІЛЬТРИ

Прогнано в Chromium 390×844 на фікстурі з 24 оголошень (7 категорій, 4 локації).

### ✅ Сильні сторони — доведені виміром, не оглядом

| що | результат |
|---|---|
| **гейт правил** при першому вході | показується, має кнопку згоди, не змахується |
| **пошук** «велосипед» серед 24 | 1 картка, саме та |
| **порожній результат пошуку** | чесний текст: «За запитом «ЗЗЗЯЯЯХХХ» нічого не знайдено» |
| **очищення пошуку** | 24 картки повернулись |
| **меню категорій** | 8 пунктів із лічильниками, **лише непорожні** |
| **фільтр категорії** | «Куплю» → 4 картки, кнопка підсвічена |
| **комбінування** категорія + локація | 4 → 2 картки |
| **меню локацій** | 18 населених пунктів громади |
| **поетапне скидання** | 0 → 12 (знято категорію і пошук) → 24 (знято локацію) |

🏆 **Найкраще рішення зони — «осиротіла категорія».** Коли фільтр звужує список до
нуля, меню все одно показує активну категорію з числом 0 («Усі 0 · Куплю 0»,
підсвічена). Тобто людина **бачить, що саме тримає екран порожнім**, і має куди
тапнути. Це рівно те, що обіцяє коментар у коді — перевірено, працює.

### 🟡 B-1 — порожній стан від фільтрів називає не ту причину (Medium)

**Місце:** `src/tabs/board.js`, порожній стан стрічки.
**Відтворення:** категорія «Куплю» + локація «Олика» (оголошення «куплю» є лише в
Котові), пошук ПОРОЖНІЙ. Екран каже: **«У цій категорії поки порожньо»**.
**Проблема:** порожньо не через категорію — у «Куплю» оголошення Є. Порожньо через
**комбінацію** категорії й локації. Текст називає одну причину з двох, і людина
робить хибний висновок «у Куплю нічого немає», хоча досить змінити село.
**Чому це варте уваги:** локація легко лишається ввімкненою з минулого разу — вона
не скидається сама і не видно її в тексті порожнього стану.
**Критичність:** Medium.
**Рекомендація:** текст за фактичним набором фільтрів («У «Куплю» немає оголошень
для «Олика»») — числа для цього вже пораховані в `catMenuHtml`.
**Складність:** мала, ~30 хв.

### 🟡 B-2 — немає скидання фільтрів одним тапом (Medium)

**Місце:** порожній стан + шапка Дошки.
**Проблема:** щоб повернутись до повного списку з трьома активними фільтрами, треба
**три окремі дії** в трьох різних місцях: меню категорій → «Усі», очистити поле
пошуку, меню локацій → «Олицька громада». Кнопки «скинути все» немає (перевірено:
`hasResetBtn: false`).
**Чому це проблема:** саме в порожньому стані людина найменш схильна шукати три
органи керування — це момент, коли вона йде з екрана.
**Критичність:** Medium (не блокує MVP, псує найгірший момент сценарію).
**Рекомендація:** кнопка «Скинути фільтри» **в самому порожньому стані** (не в
шапці — там вона висіла б завжди без потреби).
**Складність:** мала, ~40 хв.

### 🟢 B-3 — стеля 200 оголошень без пагінації (Low сьогодні, High на зростанні)

**Місце:** `src/core/supabase.js:66` — `fetchPublishedPosts()` має `.limit(200)`,
довантаження далі немає; стрічка малює всі отримані картки одразу (24 з 24).
**Сьогодні не проблема:** на Дошці **~19 оголошень**, тобто до стелі вдесятеро.
**Стане проблемою тихо:** на 201-му оголошенні найстаріші просто зникнуть з Дошки
без жодного повідомлення — ні помилки, ні «показано 200 із N».
**Критичність:** Low для MVP, High на горизонті зростання.
**Рекомендація:** до запуску — нічого. Записати як умову зростання: при >150
оголошеннях завести довантаження порціями (`IntersectionObserver` уже застосований у
хабі новин — є з чого копіювати).
**Складність:** середня, ~3 год, **не для MVP**.

### 📌 Межа цього прогону (чесно)

Фікстура `_board-fixture.mjs` працює **незалогіненим** (`getSession → null`). Тому
сценарії, що вимагають входу — «Мої оголошення», «Збережені», «Повідомлення»,
подача оголошення — цим шляхом не перевіряються і оцінюються **по коду**, а живий
тест лишається обовʼязковим (це і є відкритий **Д-28**).

---

## 5. СТОРІНКА ОГОЛОШЕННЯ — прогнано три різні природи

Перевірено на трьох постах: з фото+ціною, без фото з договірною ціною, **без назви
й без телефону** (у живій базі таких 9 із 19).

### ✅ Повний склад екрана присутній на ВСІХ трьох

Заміряний текст шару (скорочено):
> `1 / 3` · **ПРОДАМ** · **ПРОДАМ ВЕЛОСИПЕД** · Вся Олицька громада · 1 год тому ·
> **2 500 ₴** · опис · **Автор оголошення** · Автор Тест · Учасник CSTL LIFE ·
> **Будьте обережні. CSTL LIFE не є стороною угоди…** · **Поскаржитися на оголошення**

| елемент | з фото | без фото | без назви |
|---|---|---|---|
| галерея зі лічильником «1/3» | ✅ | — | — |
| ціна / «Ціна договірна» | 2 500 ₴ | договірна | — |
| секція автора | ✅ | ✅ | ✅ |
| блок безпеки з межею відповідальності | ✅ | ✅ | ✅ |
| «Написати» · «Зберегти» · «Поділитися» · «Поскаржитися» | ✅ | ✅ | ✅ |
| **вихід жестом «назад»** | модалка закрилась, список цілий (3 картки) | ✅ | ✅ |

✅ **Оголошення без назви не ламає екран** — заголовка просто немає, решта на місці.
Це закриває найпоширеніший випадок живих даних.
✅ **Помилок JS — нуль** на всіх трьох сценаріях.

### 🔍 Дві підозри, які я перевірив і ЗНЯВ (важливо для чесності звіту)

1. **«Немає кнопки Подзвонити».** Насправді умова є (`board.js:1007`): кнопка
   малюється, **лише коли телефон відомий**. У прогоні гість не залогінений, телефон
   автора йому не віддається — тобто це не дефект, а правильна поведінка.
2. **«Учасник CSTL LIFE» без дати.** Теж навмисно (`board.js:775-799`): рядок
   малюється ОДРАЗУ як твердження, істинне завжди, а тихий запит уже після показу
   уточнює його до «з <місяць> <рік>». У пісочниці запит не відповідає — лишається
   базова форма. Це грамотне рішення (екран не чекає мережу), а не порожнє поле.

📌 Обидві підозри знято **виміром і читанням коду**, а не здогадом — саме такі «майже
знахідки» роблять аудит недостовірним, якщо їх не перевірити до кінця.

---

## 6. ПІДМЕНЮ FAB · СТВОРЕННЯ ОГОЛОШЕННЯ

> Ці сценарії вимагають входу, а фікстура працює гостем — тому вони розібрані **по
> коду**, а не прогоном. Це названо прямо, щоб звіт не виглядав переконливішим, ніж є.

### ✅ Сильні сторони

| що | доказ |
|---|---|
| **порожні стани скрізь із підказкою дії** | «У вас ще немає активних оголошень.<br>Подайте перше — кнопка внизу» · «Тут зʼявляться ваші розмови з покупцями та продавцями» · «Архів порожній» |
| **«Підняти вгору» з чесним кулдауном** | `BUMP_COOLDOWN_MS = 3 год`; поки не можна — кнопка неактивна й **каже, коли можна**: «Можна через …» |
| **валідація подачі — три поля з людськими текстами** | «Оберіть категорію оголошення» · «Додайте заголовок оголошення» · «Будь ласка, заповніть текст» |
| **захист від публікації недовантаженого фото** | «Зачекай, фото завантажується…» — блокує відправку |
| **антиспам на подачі** | `containsProfanity` на текст і контакт |
| **закладки — у базі, не в браузері** | `saved_posts(uid, post_id)` + RLS «лише свої» → закладки живуть між пристроями |

### 🔴 C-1 — НАБРАНЕ ОГОЛОШЕННЯ МОЖНА ВТРАТИТИ ОДНИМ ТАПОМ (High)

**Місце:** `src/tabs/community-modal.js:150` — `openModal({ variant: 'sheet', … })`
**без** `dismissible: false`, і **чернетки немає взагалі** (`grep draft` по файлу — 0).

**Сценарій утрати:** людина набирає оголошення (заголовок до 80 символів + опис, у
живій базі трапляються описи на 1296 символів) → випадковий тап по фону, свайп вниз
або ✕ → **усе набране зникає без питання й без відновлення.**

**Чому це High, а не Medium:**
1. Це **втрата даних користувача**, а не незручність.
2. Проєкт **уже визнав цю проблему і вирішив її** — але в іншому місці. У композері
   «Стрічки» 27.07 завели чернетку (`cstl_fd_draft_<pageId>` у `sessionStorage`) саме
   після скарги Вови. Тобто рішення існує, перевірене, з готовим кодом — і не
   застосоване там, де текст найдовший.
3. Подача оголошення — **головна дія Дошки**, заради неї екран існує. Найдорожча
   форма продукту лишилась єдиною без страховки.

**Рекомендація:** повторити прийом зі «Стрічки»: `sessionStorage`, ключ
`cstl_board_draft`, очищення **у гілці успіху публікації** (не в `close()` — інакше
тап повз лист сам і стер би чернетку). Фото не зберігати (`File` у памʼяті) і сказати
про це тостом — так само, як зроблено у «Стрічці».
**Складність:** мала, ~1 год, код-донор існує.

### 🟡 C-2 — Д-28: приватний чат не перевірений живим тестом (High, кодом не закривається)

**Місце:** `src/tabs/board-chat.js` (1 675 рядків) — найбільший непокритий вузол зони.
**Стан:** відкритий пункт **Д-28** майстер-списку з 11.07. Сторожа немає, фікстура
гостя його не дістає. Тобто найскладніша частина Дошки — приватне листування
покупця з продавцем, realtime-синхронізація, push, поведінка клавіатури — **не
перевірена ані стендом, ані людьми**.
**Чому це важливо для MVP:** саме сюди веде головний CTA оголошення («Написати»).
Якщо тут щось не працює, зривається основний сценарій продажу.
**Рекомендація:** до відкриття для людей — живий тест на **двох акаунтах і двох
пристроях**: надсилання, отримання, непрочитані, push, клавіатура, вихід і повернення.
**Складність:** 1-2 години живого тесту, коду не потребує.

---

## 7. UI-СИСТЕМА · MOBILE UX · FRONTEND

### ✅ Що вже під сторожем і виміряне (не потребує повторної перевірки)

| вимір | стан |
|---|---|
| **однаковість поверхонь** | `board-cream.mjs` міряє живу теплоту кольору (R−B), 38 перевірок, має контроль на старих значеннях |
| **контраст шапки** | був 1.66:1 (втричі нижче норми), став **6.34:1** |
| **картка над фоном** | 1.264:1 (на базовому фоні було 1.122 — «як Facebook, на межі») |
| **FAB не накриває останню картку** | `board-fab-clearance.mjs` |
| **геометрія картки, футер** | `board-card-row` 23/23 · `board-card-foot` |
| **згортання шапки при скролі** | `board-header-flow`; згортання **тільки `transform`**, ніколи висота — інакше список стрибав би під пальцем |
| **іконки категорій** | одна таксономія (`board-categories.js`) на картку, меню фільтра й плитку без фото |
| **свайп-закриття модалки** | спільний `sheet-motion.js`, а не копія під фічу |
| **жест «назад»** | системний, через `core/layers.js` — власний свайп знято свідомо |

**Оцінка дизайн-системи:** Дошка читається **як одна система**. Поверхні, кольори й
відступи взяті з токенів (`--board-bg`, `--board-card`, `--board-line`, `--board-press`,
`--brand-grad`), а не з розсипаних hex. Це видно і в тому, що зміни, зроблені
сьогодні (відступ у FAB-меню, іконка категорії), лягли **одним рядком кожна** — так
буває лише коли система справді є.

### Frontend: масштабованість і борг

✅ **Спільні примітиви замість копій:** `core/modal.js`, `core/layers.js`,
`core/sheet-motion.js`, `core/list-patch.js`, `core/board-categories.js`. У проєкті
є памʼять про ціну копій (два списки антиспаму розійшлись) — і вона врахована.

⚠️ **`board.js` — 2 531 рядок, `board-chat.js` — 1 675.** Це багато для одного файлу,
але поділ уже робився усвідомлено (Обговорення винесені 12.07, стилі Дошки
відокремлені 05.08 із доказом на 224 елементах). Далі ділити **не варто до MVP** —
ризик регресу вищий за виграш.

⚠️ **Мертвий CSS** контролює `dead-css.mjs` — сторож існує.

---

## 8. ЗВЕДЕНА ТАБЛИЦЯ ЗНАХІДОК

> ✅ **СТАН НА 08.08 (оновлено після роботи по цьому звіту).**
> Закрито: **C-1** (чернетка подачі) · **A-1** (заведено три поведінкові сторожі:
> `board-fab-messages`, `board-filters`, `board-owner`) · **B-1** · **A-2**.
> Відкрито лишається: **C-2** (живий тест чату — кодом не закривається) і **B-3**
> (стеля 200, не для MVP).
> ⚠️ **І головне, чого цей звіт не побачив:** через день після нього знайшовся
> **B-30** — пункт FAB «Повідомлення» мовчки не відкривався. Тобто рядок «Critical
> дефектів немає» був правдою лише про ту половину зони, яку прогін МІГ дістати:
> фікстура працювала незалогіненою (див. «Межа цього прогону»). Саме через це A-1
> була не косметичною знахідкою, а описом сліпої зони.

| # | місце | проблема | критичність | складність |
|---|---|---|---|---|
| ~~**C-1**~~ | `community-modal.js` | ~~набране оголошення губиться одним тапом~~ | ✅ **закрито 07.08** — `core/draft.js`, сторож `board-draft` | — |
| **C-2** | `board-chat.js` (Д-28) | приватний чат не перевірений ані стендом, ані живим тестом | **High** | 1-2 год тесту |
| ~~**A-1**~~ | `tests/` | ~~сторожі стережуть вигляд, а не поведінку~~ | ✅ **закрито 08.08** — `board-filters` (пошук/фільтри/порожні стани), `board-owner` (подача, «Мої»), `board-fab-messages` (вхід у листування); фікстура навчена входу і фільтрів | — |
| ~~**B-1**~~ | порожній стан стрічки | ~~називає причиною категорію, коли винна комбінація~~ | ✅ **закрито 08.08** — текст перелічує всі активні фільтри | — |
| ~~**B-2**~~ | порожній стан | ~~немає скидання фільтрів одним тапом~~ | ✅ **закрито 08.08** — кнопка «Скинути фільтри» в порожньому стані | — |
| **B-3** | `supabase.js:66` | стеля 200 оголошень, далі найстаріші зникнуть мовчки | Low сьогодні | середня, **не для MVP** |
| ~~**A-2**~~ | `board.js` | ~~мертва гілка, що виглядає як обхід `requireAuth`~~ | ✅ **закрито 08.08** — рядок прибрано, сторож у `board-filters` | — |

**Критичних (Critical) дефектів — НЕМА.** Жоден зі знайдених не ламає екран, не
втрачає чужих даних і не відкриває доступу.

---

## 9. ОЦІНКИ 1-10

| вимір | оцінка | чим обґрунтована |
|---|---|---|
| **UX** | **8** | сценарії проходяться без тупиків; порожні стани всюди з підказкою дії; «осиротіла категорія» вирішує найважчий момент. Мінус — втрата чернетки (C-1) і порожній стан, що називає не ту причину (B-1) |
| **UI** | **9** | одна система на токенах; контрасти перевірені числами (6.34:1 · 1.264:1); однакові картки, модалки, іконки, анімації. Мінус лише за те, що частина рішень тримається на сторожах вигляду, а не на самій системі |
| **Функціональність** | **8** | пошук, фільтри, комбінування, скидання, статуси, кулдаун, скарги, закладки в базі — усе робоче. Мінус — C-1 і неперевірений чат |
| **Навігація** | **9** | тупиків і циклів не знайдено; вихід із будь-якого шару — системним жестом «назад» через одне місце правди; повернення зі сторінки оголошення лишає список цілим |
| **Дизайн-система** | **9** | токени, спільні примітиви, одна таксономія категорій; нові зміни лягають одним рядком |
| **Продуктивність** | **8** | точкове оновлення замість перемальовки (`list-patch.js`), бейдж із кешу, один запит на два шляхи. Мінус — усі картки рендеряться одразу (до 200) |
| **Масштабованість** | **7** | примітиви спільні, але два файли по 1.7-2.5 тис. рядків і стеля 200 без пагінації — обидва стануть проблемою після зростання, не раніше |
| **Готовність до реальних людей** | **7** | усе видиме готове; тримає лише те, що головна дія (подача) не має страховки, а головний CTA (чат) не перевірений живими людьми |

**Середня оцінка: 8.1 / 10**

---

## 10. LAUNCH READINESS VERDICT

### Чи готова Дошка до MVP-запуску?

**Так — з двома доробками перед відкриттям для людей.** Твоє відчуття «вона готова»
підтвердилось: зона зроблена ретельно, і майже все, що я перевіряв, виявилось
**правильним рішенням із причиною**, а не випадковістю.

### Сильні сторони

1. **«Осиротіла категорія»** — найкраще рішення зони. Порожній екран показує, ЯКИЙ
   фільтр його тримає, і дає куди тапнути. Такого немає навіть у великих дошках.
2. **Порожні стани всюди з підказкою дії**, а не «нічого немає».
3. **Чесність продукту:** збій називається збоєм (демо-дані з вигаданими телефонами
   прибрані), «Ціна договірна» замість вигаданого числа, блок безпеки прямо називає
   межу відповідальності платформи.
4. **Одна система:** токени, спільні примітиви, одна таксономія категорій, системний
   жест «назад» з одного місця.
5. **Оголошення без назви й без фото не ламають жодного екрана** — а це 9 із 19
   живих записів.
6. **Закладки і статуси — у базі з RLS**, тобто переживають зміну пристрою.

### Ризики, що лишаються

| ризик | чому |
|---|---|
| **приватний чат не перевірений людьми** | найбільший непокритий вузол, і саме туди веде головний CTA оголошення |
| **поведінка без сторожів** | пошук і фільтри зламаються **тихо**: виглядатиме як «немає оголошень» |
| **тиха стеля 200** | спрацює без попередження, коли Дошка виросте |

### 🔴 Обовʼязково до релізу — рівно два пункти

1. **C-1 — чернетка в модалці подачі** (~1 год). Втрата набраного тексту — єдина
   знахідка, що прямо шкодить людині. Код-донор уже є у «Стрічці».
2. **C-2 — живий тест чату** на двох акаунтах і двох пристроях (1-2 год, без коду).
   Це відкритий **Д-28** з 11.07.

Решта (B-1, B-2, A-1, A-2, B-3) — **після запуску**, вони нічого не ламають.

### Рівень готовності: **88%**

### Вердикт: **READY WITH MINOR FIXES**

Дошку можна відкривати людям **після** двох пунктів вище. Обидва — робота на один
вечір, і жоден не потребує переробок архітектури.

# ЧЕРГА ЗАВДАНЬ — узгоджено з Вовою 05.08.2026

> ✅ **ПУНКТ 0 ЗАКРИТО 25.08** (сесія «c», гілка `claude/startuem-xwy8jd`). Гарантія
> «хто я» доведена до всіх вкладок, які читають «моє» при першому показі; де її
> ставити не було підстав — записано ЧОМУ. Пункти 1-2 теж закриті.
> ➡️ **Наступне за чергою — пункт 3** (чат Дошки `#F3EFE3`/`#FCFAF4`), далі 4 і 5.
>
> 🆕 **25.08, сесія «d» — ВІДЖЕТ СТРІЧКИ НА ГРОМАДІ (позачергове замовлення Вови, закрито).**
> Порядок спільнот за свіжістю останнього допису · ряд на всю ширину · назви у два рядки ·
> дві форми картки однакової висоти · карусель · синхронне виділення. Сторож
> `tests/home-feed-widget.mjs` **26/26**, контроль **9/26**.
> ✅ **БОРГ ДВОХ КАРУСЕЛЕЙ — ЗАКРИТО 26.08, БРАТИ НІЧОГО НЕ ТРЕБА.** Тут стояло
> «у проєкті дві реалізації авто-каруселі… варто взяти перед пунктом 3». Це було
> правдою рівно один день: наступного ж дня карусель новин перевели на спільний
> `src/core/auto-carousel.js`, і `startNewsCarousel()` лишився тонкою обгорткою
> (`tabs/community-blocks.js:1984` → `startAutoCarousel()`). Звірено грепом 30.08 —
> інших реалізацій у `src/` немає; сторож `tests/carousel-shared.mjs`.
> 🔑 **Урок про сам беклог:** рядок брехав 4 доби, і брехав НАЙдорожчим способом —
> радив узяти роботу, яка вже зроблена. Закриваєш борг у коді — гаси його **в усіх**
> документах, а не лише там, де правив останнім.
>
> 🆕 **29.08, сесія «b» — ОНБОРДИНГ ГОСТЯ З БРАУЗЕРА (позачергове, закрито).**
> PR **#1098 · #1099 · #1100**. Згода і встановлення по черзі · ✕ 44×44 і видимий ·
> справжня іконка · покрокова інструкція 1/4→4/4 зі схемами і свайпом · окремий
> аркуш для не-Safari · затемнення · обидва банери над таб-баром (`--notice-bottom`).
> Сторож `tests/install-onboarding.mjs` **44/44**.
> 🛑 **ЛИШИВСЯ БОРГ:** гілка Android (`beforeinstallprompt`) і аркуш «Потрібен Safari»
> перевірені лише стендом, живою рукою — ні. Стенд міряє НАШУ розмітку, а не чужий
> браузер, і саме на цьому я вже помилився з меню ⋯.
>
---

## 🔴 ЯК ЦЕЙ ФАЙЛ СПІВВІДНОСИТЬСЯ З `NOW.md` (розмежовано 30.08)

**Було дві черги, і вони суперечили одна одній.** Тут стояло «наступний за порядком —
пункт 3 (чат Дошки)», а `NOW.md` у розділі ➡️ ДАЛІ називав зовсім інше («перше — екран
на компʼютері пальцем, друге — фільтр B-14, третє — сусідні класи неправди в агентів»),
причому пункт 3 у ньому не згадувався взагалі. Нова сесія читає обидва файли і не має
підстав обрати один.

🔑 **Розводимо не вибором переможця, а РОЛЯМИ** — бо так воно фактично й працює весь
серпень:

| файл | роль | хто задає |
|---|---|---|
| **`NOW.md` → ➡️ ДАЛІ** | 🔴 **ЖИВИЙ ПОРЯДОК на найближчі дні.** Чинний. Сюди потрапляють позачергові замовлення Вови і те, що щойно розблокувалось | остання сесія + слово Вови |
| **цей файл** | **черга пунктів, узгоджена 05.08.** З неї беруть роботу, **коли позачергового немає** | Вова, 05.08 |

📐 **Факт, а не здогад:** за серпень черга цього файлу не зрушила з пункту 3 жодного разу
(він висить із 05.08), а зробили при цьому вісім позачергових замовлень — збори, беж,
гарантія «хто я», віджет Стрічки, онбординг, авторизація поштою, екран компʼютера,
класифікатор B-28. Тобто позачергове слово Вови **завжди** перебивало порядок 05.08, і
документація просто не описувала цього правила.

🛑 **Наслідок для сесії:** «що робити далі» питай у `NOW.md`. Сюди заглядай, коли там
порожньо — або коли Вова прямо каже «беремо пункт N».
⚠️ **Пункт 3 і далі відкриті** — вони не скасовані, вони просто не мають пріоритету над
живим замовленням.

### 🆕 ЩО ДОДАЛОСЬ ДО ЧЕРГИ ПОЗА ПОРЯДКОМ 05.08

🚫 **B-14 — тематичний фільтр новин: ЗАКРИТО 30.08, НЕ РОБИМО.** Був розблокований
закриттям B-28 і встиг постояти першим у черзі рівно кілька годин.
🗣️ **Слово Вови:** «вони мають бути умовними, тобто позначатись тільки на картках».
🔑 Підстава технічна: фільтр на АВТОМАТИЧНІЙ категорії при помилці класифікатора
**ховає новину**, бейдж лише неточно її підписує — різниця в ціні помилки, а не в смаку.
Повний розбір — `CSTL_BUGS.md`, рядок **B-14** (= **Н-5** в `AUDIT_2026-07.md`).

---

> ➡️ **Наступний за порядком цього файлу — пункт 3** (чат Дошки). Живий пріоритет
> дивись у `NOW.md` ➡️ ДАЛІ.

> Початковий порядок задав Вова 05.08: **спочатку збори**, далі по черзі.
> Кожен пункт має бути закритий окремим блоком і окремим деплоєм.

---

## ✅ 0. ГАРАНТІЯ «ХТО Я» — НА ВСІ ВКЛАДКИ (замовлення Вови 25.08) — **ЗАКРИТО 25.08**

> **Пряме слово Вови:** «запиши це в головне завдання для нової сесії, щоб вона була
> в курсі контексту і всі одразу почала робити».
> 🔑 **Це не нова фіча — це доведення до кінця вже початого.** Механізм написаний,
> перевірений і працює; лишилось застосувати його там, де він ще не застосований.

### Що вже зроблено (25.08, PR #1020) — читати ЗВІДСИ, не вигадувати заново

🔴 **ВАДА, ЯКУ ЛІКУЄМО.** `initAuth()` у `src/app.js` кличеться **без `await`**, тобто
відновлення сесії йде своїм ходом, а вкладки тим часом уже читають дані. Хто встиг
раніше — той і виграв. При `currentUserId() === null` кожен запит «а що з цього моє»
чесно відповідає про **ГОСТЯ** — і застосунок малює вигляд стороннього для залогіненої
людини.

🔬 **Як це виглядало для Вови (три скарги, одна причина):** його лайк не підсвічувався ·
зникало меню «⋯» на його ж спільнотах · зникав вибір «відповідати як спільнота».
Дослівно: «ходжу по вкладках, повертаюся — і тоді вже підтягується».
⚠️ **І на слабшому телефоні це спрацьовує ЧАСТІШЕ, ніж у Вови, а не рідше.**

✅ **МЕХАНІЗМ ГОТОВИЙ — `authReady()` у `src/core/auth.js`.** Завершується, коли питання
«хто я» вже має відповідь: людина, гість або збій. Споживач чекає його **раз**, до
першого читання даних.
- ⚠️ **межа часу обовʼязкова** (2.5с) — без неї завислий виклик заморозив би вкладку;
  це клас `serviceWorker.ready`: воно не падає, воно **ВИСНЕ**;
- 🛑 `settleAuth()` стоїть **ПІСЛЯ `catch`** — «не змогли дізнатись» теж відповідь;
- 🛑 `await initAuth()` на весь застосунок **ВІДХИЛЕНО**: це зробило б ЗАПУСК залежним
  від мережевого виклику, тобто ту саму хворобу для всіх одразу.

🔑 **Два механізми, і вони НЕ дублюють один одного:**

| механізм | що гарантує |
|---|---|
| `authReady()` | **ПЕРШИЙ** показ правдивий (прибирає перегони) |
| `onAuthChange()` | показ лишається правдивим після **СПРАВЖНЬОЇ** зміни входу |

### ✅ ЗРОБЛЕНО 25.08 — шість кроків, шість комітів, шість вимірів

| вкладка | що саме ламалось | де стоїть гарантія |
|---|---|---|
| `feed.js` | лайк не підсвічений, немає «⋯» і вибору голосу | `loadData()` _(було, PR #1020)_ |
| `board-chat.js` | **бейдж і крапка непрочитаного мовчали** · тап по push не відкривав чат | `refreshUnreadBadge()` · `openThreadById()` |
| `board-discussions.js` | памʼять «що читав» писалась у простір `anon`, поріг історії не ставився | `loadSeenThreads()` · слухач `cstl-tab-changed` у `board.js` |
| `board.js` | закладки не запитувались узагалі, лайки питались **про аноніма** | `loadBoardData()` |
| `buses.js` | **самолікування push крутилось вхолосту** — підписка на рейс лишалась мертвою | `loadTrackedRoute()` |
| `home-caps.js` | смуга перебудовувалась на очах (правило «максимум 3») | `renderHomeCaps()` |
| `community-blocks.js` | віджет показував **загальний** рейс замість відстежуваного | `renderBusBlock()` |
| `community.js` | «Доброго дня, громадо» + порожній аватар | `authReady().then(…)` після скелета |

🛑 **ДВІ ВКЛАДКИ, ДЕ «ЯК УСЮДИ» ЗЛАМАЛО Б ЗАСТОСУНОК** — тут `await` перед тілом
заборонений, і сторож це стереже окремими рядками:
- **Автобуси** — єдина вкладка, що працює **офлайн** із локального файлу. Розклад не
  сміє чекати сесію; чекає лише персональна гілка.
- **Громада** — `renderSkeleton()` це **перший екран**. Проміняти блимання імені на
  порожній екран було б гіршою угодою.

### 🛑 ДЕ ГАРАНТІЇ НЕМАЄ — І ЦЕ НЕ НЕДОГЛЯД

| файл | підстава |
|---|---|
| `news.js` | `refreshSavedArticles()` кличеться **лише з `onAuthChange`** (тобто вже після факту), а обидва читачі закладок — **на вимогу**: зірочка при відкритті модалки статті і хаб «Збережені». Рядок був би декорацією. ⚠️ Справжні перегони там із **мережею**, не з входом — інший клас вади. |
| `community-modal.js` · `events.js` · `fund-screen.js` | той самий «окремий випадок», що й був описаний: відкриваються НА ВИМОГУ, читають свіжий стан самі. **Перевірено поштучно 25.08 — підтвердилось.** |

### 🧪 Сторож уже стоїть і чекає

`tests/feed-auth-race.mjs` — **42/42**, контроль `BUNDLE_REV=origin/main` → **22/42**
_(на 25.08; було 15/15 і 4/15 до цієї роботи)_.
🔑 Він **виконує** гарантію, а не грепає: справжній `auth.js` із підробленою базою в
чотирьох режимах. Заміряно: сесія → uid за 0мс · гість → 0мс · **завислий виклик →
401мс при межі 400** · бази немає → 0мс.
🔴 **У ньому є ПРАВИЛО КЛАСУ у двох половинах** — «хто кешує мої права, той мусить і
чекати факт, і підписатись на зміну». Ознаки: `fetchMyEditablePageIds` ·
`fetchMySubscriptions` · `fetchThreadPairs` _(додано 25.08)_.
➡️ **Розширювати правило разом із роботою:** узяв вкладку — додай її ознаку в сторож,
щоб наступна сесія не могла її загубити.

🛑 **ДВА ВИПРАВЛЕННЯ В САМОМУ СТОРОЖІ 25.08 — читати перед тим, як його чіпати:**
1. **Правило класу не реагувало на `BUNDLE_REV` взагалі** (читало файли з диска через
   `readFileSync`), тож у контрольному прогоні обидва його рядки лишались ЗЕЛЕНИМИ —
   перевірка, яка не вміє впасти. Переведено на `projectFile(…, REV)`.
2. **`indexOf('щось()')` знаходить ЗГАДКУ В КОМЕНТАРІ так само охоче, як виклик.**
   Тут це не дрібниця, а властивість репозиторію: коментарі довгі й називають рівно
   те, що поруч написано кодом. **Чотири рази поспіль перевірка падала на СПРАВНОМУ
   коді через власну прозу.** Заведено `оператор(текст, імʼя)` — початок рядка +
   відступ + виклик із крапкою з комою — і **контроль на саму пастку**.

---

## 1. ✅ ВІДЖЕТ І РОЗДІЛ ЗБОРІВ — **ЗАКРИТО 16-17.08**

> ✅ **Зроблено повністю.** PR **#940** (віджет за макетом, варіант «2») ·
> **#941** (переїзд у Supabase, розділ у бургер-меню, заявки жителів, адмінка) ·
> **#942** (екран не їде вбік від довгого посилання) · **#943** (модалка збору,
> картка без фото не тримає порожнечі).
> 🔴 **Лишилось Вові:** прибрати ТЕСТОВИЙ збір із проду — тепер це робиться
> з адмінки: розділ «Збори» → «Видалити». Живих даних Monobank немає і не буде
> (див. обмеження нижче) — суму, відсоток і «донати» прибрано за рішенням Вови
> 16.08: «якщо суму не можемо просити напряму, то її краще прибрати».
>
> Нижче — початкове замовлення 05.08, лишене як історія рішення.

### Початкове замовлення (05.08)

**Замовлення:** сучасний віджет актуальних зборів на «Громаді», у стилі додатку,
з кнопкою «Задонатити», що веде на банку Monobank; за можливості — живі дані
з банки.

**Що вже є в коді (НЕ будувати з нуля):** `src/tabs/home-fund.js` — уміє `title`,
`org`, `url`, `goal`, `raised`, відсоток, смугу прогресу, перехід за посиланням.
Запис без `org` або `url` не малюється. Порожній список = секції немає зовсім.

> ⚠️ **Звірено 06.08 — два числа тут були застарілі:**
> - файл **314 рядків**, а не 131: його переписали 05.08 під варіант «2» (вміст поверх фото на темній картці);
> - `data/fundraisers.json` **НЕ порожній** — 05.08 на пряме прохання Вови туди поклали
>   один **ТЕСТОВИЙ** запис («ТЕСТ — перевірка блока зборів»), і він зараз **на проді**.
>   🔴 **А з 17.08 самого файла вже НЕМА** (звірено 01.09): збори переїхали в таблицю
>   `fundraisers` у Supabase, бо адмінка пише в базу і не може писати в git. Тестовий
>   запис лишився — але прибирається тепер в адмінці (`admin.html` → «Збори» →
>   «Видалити»), а не правкою `items` у файлі. Порожній список так само ховає секцію.
>
> Це була пряма суперечність із `START_HERE.md` і `SESSION_STATE_VOVA.md`, які весь цей
> час писали про тестовий запис. Беклог складали 05.08 **до** того, як запис поклали.

**Чого бракує:** фото, «залишилось зібрати», кнопка «Задонатити», карусель на
кілька зборів, категорії, сучасний вигляд.

### 🔴 ГОЛОВНЕ ОБМЕЖЕННЯ — ЖИВІ ДАНІ MONOBANK
- Публічний API Monobank вимагає **особистого токена власника рахунку**, і той
  токен дає доступ до **всієї виписки**, а не лише до банки. Просити його в
  чужого волонтера **не можна**.
- **Немає** публічного способу прочитати чужу банку за посиланням
  `send.monobank.ua/jar/…`. Вишкрібання HTML ламається при зміні верстки.
- Наш застосунок — статичний сайт із **публічним репозиторієм**: токен у
  `bundle.js` побачить будь-хто; браузер такий запит однаково не пропустить (CORS).

➡️ **Два чесні шляхи:**
1. **Банка НАША** → живі дані реальні: токен у секретах Edge Function Supabase,
   функція раз на 10-15 хв пише суму в базу, картка показує чесний штамп часу.
2. **Банка ЧУЖА** → лише ручне оновлення + правдивий підпис «дані станом на …».

🛑 **Не підписувати «оновлено 5 хв тому», якщо оновлюють руками.** Це брехня про
гроші. Той самий принцип, що відмова від «23 переглядають зараз» у капсулах.
🛑 **«👥 1 248 донатів» — вигадка:** Monobank кількості донатерів не віддає.

### ✅ РІШЕННЯ ВОВИ 05.08

**Банки — від РІЗНИХ людей.** Отже живих даних Monobank не буде: токен належить
власнику рахунку і дає доступ до всієї виписки. Просити його у волонтера не будемо.

**Вигляд картки — варіант «Б»:** показуємо **назву, хто збирає, ціль і кнопку
«Задонатити»**. Суми зібраного і відсотка на картці НЕМАЄ.

🔑 Чому саме так, а не «сума + штамп часу»: дані оновлюються руками, тож будь-яке
число на картці старіє мовчки. Показати вчорашню суму як поточну — це неправда
про чужі гроші. Актуальну суму людина бачить на самій банці, за один тап.
➡️ Наслідок для коду: смуга прогресу і `raised` у картці НЕ використовуються.
Поле `raised` у даних можна лишити на майбутнє, але воно не малюється.

### ⬜ ЩО ПОТРІБНО ВІД ВОВИ, ЩОБ ПОЧАТИ
Перший **справжній** збір: назва · хто збирає · посилання на банку · ціль.
🛑 Вигаданих зборів («FPV для 14 ОМБр» із прикладу ChatGPT) у код не пишемо —
це чужі гроші й чужі назви.

---

## 2. 🎨 БЕЖ — ✅ **ЗАКРИТО 11.08.2026 СЛОВОМ ВОВИ**

> **Дослівно:** «ми ж від цього бежевого тілесного фону відійшли вже… він уже не
> базовий колір в додатку. Все, видали його з документації».
>
> ✅ **Заміряно `tests/tools/beige-scan.mjs`** (міряє ТЕПЛОТУ обчисленого кольору
> R−B ≥ 6 на світлих поверхнях, а не назву токена):
>
> | Громада | Стрічка | Питання | Дошка | Автобуси |
> |---|---|---|---|---|
> | **0** | **0** | **0** | **0** | **0** |
>
> 🔴 **Останнє живе вживання знайдено і зняте 11.08:**
> `.app-main[data-tab="discussions"] { background: #E7E1D2 }` (теплота **21**).
> ⚠️ **Урок про ДВА ШАРИ:** у редизайні того ж дня фон уже міняли — але на
> `#page-discussions`, а `.app-main` це БАТЬКО сторінки. На знімках усе
> виглядало нейтральним, а беж визирав нижче контенту і при «гумовому» відскоку.
> **Зміна фону вкладки — це завжди два шари, і знімок другий не ловить.**
>
> 🛑 **Токени `--paper` / `--card-cream` / `--paper-deep` НЕ ВИДАЛЕНІ, і це не
> недоробка.** Їх ще споживають екрани, яких зараз ніхто не бачить: «Світло»
> (прихована з 16.05), Події (`initEvents()` не імпортується ніде), стара
> сторінка новин, легасі-групи V2. Видалити токени = мовчки перефарбувати ці
> екрани, тобто зробити зміну, про яку не просили.
> ➡️ **Правило зафіксовано в `style/base.css`: новий екран бере `--app-bg` /
> `--board-bg`; `--paper` і рідня — не для нового коду.**

---

## 3. 💬 ЧАТ ДОШКИ — теплота `#F3EFE3` / `#FCFAF4`

Це **пряме рішення Вови від 20.07** («трохи насиченіші, але не такі кремові як
були раніше, трішки»). Виняток названо окремим рядком у стороже
`tests/modal-surface.mjs`.

⚠️ **Звірено 11.08:** слово «беж більше не базовий» стосувалось ФОНУ ЗАСТОСУНКУ.
Це рішення Вова окремо не скасовував, тому теплота чату лишається на місці.
**Скаже прибрати — рядок іде геть.**

## 4. 📐 СПАДКОВИЙ ВІДСТУП КОНТЕЙНЕРА ГРОМАДИ

`.cm-content { padding: 6px 14px 0 }` — залишок старої сторінки; редизайн 04.08
задав лише `padding-bottom`. Зараз гаситься **точково** відʼємним полем на
`.hm-top`. Прибрати з кореня означає зсунути всю сторінку на 14px убік — це
окрема робота з переміром ритму.

---

## 5. 🚌 КАПСУЛИ ПРОТИ ВІДЖЕТА АВТОБУСІВ

Вова: «потім подумаємо». Заміряно: капсула каже три речі («N рейсів сьогодні»,
«найближчий через N хв», «N зараз у дорозі»), віджет — **один конкретний рейс у
русі** з часами, наступною зупинкою і шкалою. Дублюється **одне повідомлення з
трьох**, і то неточно (найближче ВІДПРАВЛЕННЯ ≠ рейс У РУСІ).

🔑 Справжнє питання не «прибрати одне з двох», а **чим капсули мають бути**.
Вова назвав їх «швидкими віджетами з тим, що зараз відбувається». Якщо так —
капсула має казати те, чого віджет НЕ каже, і зникати, коли їй нема чого додати.

# АУДИТ CSTL LIFE за скілом `apple-design` — 09.08.2026

> Замовлення Вови: «жорстокий аудит проекту CSTL LIFE вцілому, всі сторінки,
> включаючи всі вкладки таббара, і окремі підсторінки в вкладці дошка в кнопці
> FAB… Включаючи бургер меню і навіть особистий кабінет. Все повністю,
> абсолютно навіть шапку профілю».
>
> 🔴 **Це документ АУДИТУ. Жодної правки інтерфейсу за ним ще не зроблено** —
> так домовлено в `BYYOU_PLAN.md`. Порядок виправлень задає Вова.

**Чим міряно:** `tests/tools/apple-audit.mjs` (знімає обчислені стилі в Chromium,
390×844, залогінений житель) + `tests/tools/apple-report.mjs` (розбір).
**Скіл-еталон:** `.claude/skills/apple-design/SKILL.md`.
**Охоплено 14 екранів:** 5 вкладок таб-бару · меню FAB Дошки і 4 його підсторінки ·
бургер-меню · особистий кабінет · сторінка оголошення · правила Дошки.

---

## 🔴 ПОПРАВКА ДО ЧИСЕЛ — 09.08, ВОСЬМА ПОЛОМКА ПРИЛАДУ

**Числа §1/§10 у цьому звіті були ЗАВИЩЕНІ.** Знайдено вже під час виправлень,
коли прилад запустили ще раз.

**Корінь.** Банер відстеження рейсу (`.bus-track-banner`) ховається не
`display: none`, а трійкою `opacity: 0` + `transform: scale(0.15)` +
`pointer-events: none` — вузол лишається в розмітці й формально «на екрані».
Прилад перевіряв прозорість **самого** вузла, а прозорість НЕ успадковується в
обчислені стилі дитини: дзвоник усередині має власну `opacity: 1`. Тож на
кожному з 14 екранів у вимір потрапляла фальшива кнопка «6×6», якої людина не
бачить і натиснути не може.

| показник | у звіті було | чесно |
|---|---|---|
| справжніх тап-цілей | 339 | **326** |
| менших за 44×44 | 179 (53%) | **173 (53%)** |
| «німих» (ні `:active`, ні підсвічування) | 152 | **138** |

🔑 **Висновки звіту не змінюються** — відсотки ті самі, найгірші екрани ті самі.
Змінюється рівно те, що 14 «замалих цілей» були привидом одного прихованого
банера, а не 14 різними проблемами.

⚠️ **Контроль приладу цього не спіймав** — довелось помітити руками, що в CSS
дзвоника стоїть 36×36, а прилад показує 6×6. Тепер такий випадок закритий
окремою перевіркою (`--selftest`: «кнопка всередині прихованого предка у вимір
НЕ потрапила»), контроль виріс 12 → **13**.

---

## 🛑 ПРО ДОВІРУ ДО ЦИХ ЧИСЕЛ — прочитай перед тим, як щось за ними робити

Прилад **сім разів** показував неправду, і щоразу це ловив контроль або
розбіжність між числами, а не око:

| # | що показував | чому це було брехнею | як виправлено |
|---|---|---|---|
| 1 | «**0** правил `:active` на весь застосунок» | `@import` — це `CSSImportRule`, правила лежать у `r.styleSheet.cssRules`, а не в `r.cssRules`. Усі 20 файлів `style/*.css` підключені саме так, тож прилад не бачив **125** справжніх правил | обхід `@import`; контроль вимагає, щоб правила з підключених файлів були видні |
| 2 | «Бургер-меню» = числа один-в-один з Громадою | бічне меню лежить у розмітці **завжди** (закрите — `translateX(-100%)`), і його 88 вузлів рахувались на КОЖНОМУ екрані | вузол мусить **перетинати екран**; контроль на вузол за краєм |
| 3 | «**83%** тап-цілей менші за 44px» | `cursor: pointer` успадковується — у вимір потрапляли `path`, `svg`, `circle` **всередині** кнопок: 599 із 1384 «цілей» (43%) були малюнками | нутрощі `<svg>` не рахуються; вкладені підписи згортаються в кнопку |
| 4 | «**36** пар скла поверх скла» | у список падав будь-який нащадок скляного вузла, зокрема з тлом `rgba(0,0,0,0)`. Прозорий підпис на склі — не другий шар матеріалу | пара рахується, лише коли `backdrop-filter` є в ОБОХ |
| 5 | «**363** пари напівпрозорого на напівпрозорому» | «напівпрозорим» вважалось тло `alpha = 0`, тобто повна відсутність тла — звичайні `div` без фону | напівпрозорий = `0 < alpha < 1` |
| 6 | стабільне «Δ15» на екранах, де правил `prefers-*` немає | два знімки порівнювались **за індексом** у DOM, а розмітка між ними живе: один доданий вузол зсуває всі наступні | ключ = імʼя вузла + номер серед однойменних; порівнюються лише спільні ключі |
| 7 | «`prefers-reduced-motion` підтримано на всіх 14 екранах» | підпис знімався з УСІЄЇ розмітки, а сторінки вкладок не знищуються — просто ховаються. Капсули Громади (єдине місце з таким правилом) «реагували» на кожному екрані | підпис знімається лише з видимого |

Контроль приладу виріс **5 → 12** перевірок (`node tests/tools/apple-audit.mjs --selftest`).
🛑 **Не послаблювати.** Кожна з семи поломок давала правдоподібні числа, за якими
можна було написати переконливий і повністю хибний звіт. Дві з них (№6 і №7) не
ловились контролем узагалі — їх виказала **розбіжність між виміром і джерелом**:
браузер казав «підтримка є», а `grep` по стилях — «правила не існує».
🔑 Тому в §14 нижче вирок виносять **два незалежні джерела**, а не одне.

---

## §1 ВІДГУК НА НАТИСК + §10 ЖЕСТИ

> Скіл: «Respond on pointer-down, not on release… Waiting for `click` to show
> feedback feels dead». Тап-ціль — 44×44 (Apple HIG).

📐 **По 14 екранах: 339 справжніх тап-цілей.**

| показник | скільки | частка |
|---|---|---|
| без правила `:active` | **248** | **73%** |
| менші за 44×44 | **186** | **55%** |
| 🔴 **«німі»** — і `:active` немає, і рідне підсвічування вимкнене (`-webkit-tap-highlight-color` прозорий) | **152** | **45%** |

🔑 **«Німа» ціль — це і є провал §1 у чистому вигляді:** на натиск не стається
**рівно нічого**, аж поки палець не відпустять. Не «слабкий відгук», а його
відсутність. Це найважливіше число всього аудиту.

### Найгостріше — таб-бар

**4 з 5 кнопок таб-бару не мають жодної реакції на натиск.**
Єдиний виняток — центральна «Громада»:
`.tab-item--home:active .tab-home-circle { transform: … scale(0.93) }`
(`style/tabbar.css:210`). У цілому файлі таб-бару **одне** правило `:active`.
Це найчастіше вживаний елемент застосунку.

### Розподіл `:active` по файлах стилів (де відгук є, а де його немає)

```
23  style/board.css        7  style/sidebar.css      3  style/news.css
17  style/messages.css     5  style/base.css         2  style/feed.css
16  style/buses.css        4  style/news-card.css    2  style/events.css
14  style/home.css         3  style/power.css        1  style/tabbar.css
 9  style/community.css                              1  style/modal.css
 8  style/account.css
```

⚠️ **`feed.css` — 2 правила на весь найбільший модуль проєкту** («Стрічка»,
177 КБ коду). Заміряно окремо: вкладка Стрічка — **11 цілей, з них 11 без
`:active`, тобто 100%**. Єдиний екран із повним нулем.

### Тап-цілі, менші за норму (найгостріші, унікальні)

| розмір | вузол | де |
|---|---|---|
| **5×5** | `.bus-week-page-dot` (крапка сторінки тижня) | Автобуси |
| **6×6** | `.bhv4-dot-nav` (крапка навігації віджета) | Автобуси |
| **6×6** | `.btb-bell` (дзвіночок підписки) | Громада |
| **13×13** | `.pm-ad-status-ic` | Мої оголошення |
| **118×16** | `#bs-from-input` (поле «звідки») | Автобуси |
| **109×16** | `#bs-to-input` (поле «куди») | Автобуси |
| **214×12** | `a.buses-updated-link` (посилання на VOPAS) | Автобуси |
| **312×18** | `#bd-search-input` (пошук) | Обговорення |

🔑 **Ширина в них нормальна — не дотягує ВИСОТА.** Тобто лікується вертикальним
padding, а не перекроюванням розкладки. Виняток — крапки 5-8px: там потрібна
невидима зона дотику навколо (скіл §10: «~10px of hysteresis/hit padding»).

### `touch-action`

Усі **339** цілей мають `touch-action: auto`. Жодного `manipulation`.
⚠️ **Не поспішати «виправляти»:** у `index.html` стоїть
`user-scalable=no, maximum-scale=1.0`, а при вимкненому масштабуванні браузер і
так не чекає другого тапу. Тобто затримки тут, найпевніше, **немає** — але це
треба **зміряти на живому iPhone**, а не вирішувати з CSS. Окремо див. нижче.

### 🔴 Побічна знахідка, важливіша за §1: масштабування заборонено

`index.html:5` — `maximum-scale=1.0, user-scalable=no`.
Скіл §15: «**Respect the user's text-size setting** (Dynamic Type)». Людина з
поганим зором **не може** збільшити текст двома пальцями. Це і провал §15, і
провал WCAG 1.4.4 (Resize text).
⚠️ Прибирати наосліп не можна: заборона масштабування зазвичай стоїть, щоб
подвійний тап не «зумив» застосунок і щоб поля вводу не смикали екран на iOS.
Це рішення з наслідками → окреме питання до Вови, не автоматична правка.

---

## §3 ПЕРЕРИВАНІСТЬ + §4 ПРУЖИНИ

> Скіл називає перериваність «**the single most important principle**»: анімацію
> має бути можна схопити на льоту і розвернути. І прямо каже: «**Avoid CSS
> transitions and `@keyframes` for anything gesture-driven**».

📐 **Заміряно: пружин у проєкті немає жодної.** `grep` по всьому `src/` і
`package.json` — нуль згадок spring / Motion / Framer Motion. **Увесь рух
застосунку — CSS-переходи з фіксованою тривалістю.**

Це не «погано саме по собі»: для звичайних кнопок і появи панелей фіксований
перехід — норма. Провал §3 стосується **лише жестових поверхонь**, і там він
конкретний: щойно палець відпущено, аркуш летить під CSS-переходом і **схопити
його назад уже неможливо** — треба дочекатись кінця.

### Тривалості й криві (унікальні пари «вузол + властивість»)

| тривалість | скільки | крива | скільки |
|---|---|---|---|
| 0.15s | 12 | `ease` (браузерна за замовчуванням) | **32** |
| 0.2s | 9 | `ease, ease` | 15 |
| 0.28s | 8 | `cubic-bezier(0.32,0.72,0,1)` — крива аркуша iOS | **7** |
| 0.18s | 4 | `cubic-bezier(0.4,0,0.2,1)` — Material | 6 |
| 0.3s / 0.6s | 1 / 1 | `cubic-bezier(0.22,1,0.36,1)` | 3 |

✅ **Тривалості здорові** — 0.15-0.28s лежать рівно в діапазоні, який скіл
називає `response 0.3-0.4`. Тут лагодити нічого.
⚠️ **Криві різнорідні:** `ease` переважає вдвічі над усіма іменованими разом.
Тобто характер руху у більшості місць не обраний, а дістався за замовчуванням.
Крива рідного аркуша iOS живе лише в **7** місцях.

### 🔴 Що анімується не тим способом (§11)

Скіл: анімувати варто **`transform` і `opacity`** — решта змушує браузер
перераховувати розкладку в кожному кадрі.

Заміряно в живих стилях: **`transform` 28 · `opacity` 27** — це добре. Але поруч
`background` 20 · `width` 3 · `border-radius` 2 · `top` 1.

Найгірший випадок — **`div.bhv4-fill` [width] 600мс** (смуга проїзду автобуса):
і найдовший перехід застосунку, і найважчий за способом. Поруч у стилях
`transition: left 0.6s`.
🔑 Обидва лікуються переходом на `transform: scaleX()` / `translateX()` — ціна
низька, а виграш саме в тих кадрах, які на слабкому телефоні губляться.

### Нескінченні анімації

| анімація | період | де |
|---|---|---|
| `bs-live-pulse` | 1.4s · **infinite** | Автобуси, позначка «в дорозі» |
| `bhv4-dot-pulse` | 1.4s · **infinite** | Автобуси, крапка віджета |
| `bs-soon-pulse` | 1.4s · **infinite** | Автобуси, «скоро» |

✅ Період 1.4s **не потрапляє** в небезпечну смугу, про яку попереджає скіл
(коливання близько 0.2 Гц = один цикл на 5 с). Але вони працюють вічно, і при
`prefers-reduced-motion` мусили б спинятись — це заміряно в §14.

---

## §2 ПРЯМЕ КЕРУВАННЯ + §5-6 ШВИДКІСТЬ І КИДОК

> Скіл §2: «Use Pointer Events with `setPointerCapture` so tracking continues
> even when the pointer leaves the element's bounds».

### 🔴 `setPointerCapture` — **0 вживань на весь проєкт**

Заміряно по всіх 11 файлах, що обробляють дотик. Наслідок практичний: якщо палець
під час перетягування виїде за межі елемента, відстеження може обірватись
на півдорозі.

### Передача швидкості (§5) — зроблено правильно, але покриває половину

`core/sheet-motion.js` (194 рядки) робить саме те, чого просить скіл: тримає
згладжену історію швидкості (`createDragTracker`, 0.6/0.4), а на відпусканні
рахує час доїзду **із залишку шляху й швидкості пальця** (`travel / speed`,
стеля 110…240 мс) і веде затемнення синхронно з панеллю.
🔑 Це власноруч написаний «velocity handoff» — і він відповідає скілу.

📐 **Покриття — 4 поверхні з 9 жестових:**

| поверхня | кидок за швидкістю | 1:1 під час жесту |
|---|---|---|
| `core/modal.js` (усі аркуші) | ✅ | ✅ |
| `tabs/feed.js` (листи «Стрічки») | ✅ | ✅ |
| `tabs/board-discussions.js` | ✅ | ✅ |
| `tabs/community-blocks.js` | ✅ | ✅ |
| `tabs/board-chat.js` (свайп-дії в розмовах) | ❌ | — |
| `tabs/buses.js` (гортання тижня) | ❌ | — |
| `core/cropper.js` (обрізка фото) | ❌ | — |
| `core/chat-core.js` | ❌ | — |
| `tabs/news-hub.js` (гортання категорій) | ❌ | ❌ |

⚠️ `tabs/board.js` більше **не** імпортує `sheet-motion`, і це правильно: власний
свайп модалки оголошення свідомо знято на користь системного жесту iPhone
(`core/layers.js`, урок 02.08). **Не рахувати прогалиною.**

### 🔴 Найчистіший провал §10: гортання категорій новин

`attachSwipe` у `core/utils.js` (єдиний користувач — `news-hub.js`) — розпізнавач
**лише кінцевого стану**: `touchstart` запамʼятовує точку, `touchend` порівнює
дельту з порогом 50px. **`touchmove` там немає взагалі** (заміряно: 0).

Скіл прямо: «Avoid recognizers that only report a *final* state — they throw away
the continuous tracking you need for feedback». Практично: поки ведеш пальцем від
«Громада» до «Волинь», **на екрані не рухається нічого**, категорія перемикається
стрибком уже після відпускання. Порівняй із листами «Стрічки», де вміст їде за
пальцем — на дотик це два різні застосунки.

### 📐 Проєкція кидка (§6) — не реалізована ніде

Скіл дає формулу `current + (v/1000)·d/(1−d)`, `d ≈ 0.998`: ціль треба рахувати з
того, **куди летить палець**. У `finishSwipe` рішення приймається за парою
«пройдено > 90px **або** швидкість > 0.45 px/мс». Це чесний двопороговий вибір, і
він працює — але це не проєкція: точка приземлення не обчислюється.
🟡 Для аркуша з двома станами різниця мала. Вона стане помітною там, де станів
більше двох: карусель, кілька позицій аркуша, гортання категорій.

---

## §7 ПРОСТОРОВІСТЬ + §9 ГУМОВІ МЕЖІ

> §7: «Anchor interactions to their source… set `transform-origin` to the
> trigger». §9: «At an edge, resist progressively instead of stopping hard».

### §7 — якоря до джерела майже немає

📐 `transform-origin` у всіх 21 файлі стилів — **3 вживання** (`center center` ×2,
`center bottom` ×1). Тобто меню й картки зʼявляються **не з тієї кнопки**, що їх
викликала: вони просто проявляються на місці.

Найпоказовіше — **меню FAB Дошки**: чотири пункти зʼявляються чистим `opacity`
з каскадом затримок 0.02→0.11s, **без жодного руху** від кнопки «+».
⚠️ **І це не недогляд.** У `style/board.css:872` записано: `transform` прибрано
свідомо, бо разом із `backdrop-filter` на пунктах WebKit давав привид (подвійний
рендер шару). Тобто «просто повернути transform» — уже пробували, і воно
зламалось. Правильний хід тут — не відкат, а інший спосіб якоря.

### §9 — гумових меж немає, і це рішення з причиною

📐 Живих прокрутників на 14 екранах — **5**. Їхній `overscroll-behavior`:

| прокрутник | overscroll | що це означає |
|---|---|---|
| `main.app-main` (Громада, Дошка) | **none** | 🔴 жорсткий стоп на краю, гумки немає |
| `main.app-main` (решта вкладок) | `contain` | ✅ гумка є, але не «протікає» на сторінку |
| `div.app-modal-sheet` | `contain` | ✅ |
| `div.acc-cab-scroll` (кабінет) | `auto` | ✅ типова поведінка |
| `div#bm-photos` (фото в подачі) | `auto` | ✅ |

`html, body { overscroll-behavior: none }` у `base.css:283` — і поруч записана
причина: на iOS у PWA відскок body «обрізав» `position: fixed` таб-бар.
🔑 **Отже це свідомий обмін:** втрачена гумка на двох головних вкладках заради
того, щоб таб-бар не ламався. Скіл тут не порушено «через недбалість» — але за
§9 ціна реальна: край Громади й Дошки відчувається як стіна.
🟡 Варте перевірки на живому iPhone, чи досі потрібне `none`, чи вистачить
`contain` (він теж не пускає відскок далі себе).

---

## §12 МАТЕРІАЛИ Й ГЛИБИНА — тут найкраще в застосунку

📐 Поверхонь зі склом (`backdrop-filter`) — **39**. Напівпрозорих без блюру — 141.

### ✅ Скла поверх скла — **0 пар**

Головна заборона §12 («never stack a light translucent surface on another») **не
порушена ніде**. Це перевірено на всіх 14 екранах.

### ✅ Напівпрозоре на напівпрозорому — **1 пара**

`span.bhv4-dot` ← `div.bhv4-track` (крапка на смузі маршруту, Автобуси),
тло `rgba(255,255,255,0.35)` на напівпрозорій доріжці. Дрібниця, але єдина.

### 🟡 Блюр не повʼязаний із площею (скіл: «bigger surfaces should read as thicker»)

| площа | блюр | поверхня |
|---|---|---|
| **329 160 px²** | **3px** | `#sidebar-overlay` — найбільша поверхня, найтонший матеріал |
| 20 769 px² | 16px | `button.hm-cap2` (капсула Громади) |
| 18 041 px² | 14px | `div.hm-fund-panel` |
| 17 160 px² | 14px | `div.bd-controls` (шапка Дошки) |
| 6 186 px² | 4px | `div.acc-cab-trust` |
| 1 280 px² | 4px | `button.bd-bookmark` |

🔑 Залежність **обернена** саме на найбільшій поверхні: затемнення бічного меню
займає весь екран і має найслабший блюр із усіх. Решта ряду (20k→14k→6k→1k px²)
поводиться правильно: більше — товще.

---

## §14 ДОСТУПНІСТЬ — найгірший розділ аудиту

> Скіл вимагає реагувати на **три** незалежні налаштування людини.

🔑 **Вирок винесено двома незалежними джерелами** (див. поломки №6-7 вище):
що каже CSS і що показує браузер.

| налаштування | у стилях | заміряно в браузері | вирок |
|---|---|---|---|
| `prefers-reduced-motion` | **3 файли з 21** (`dev-lock`, `home`, `news-hub`) | Δ11 на Громаді та екранах поверх неї · **Δ0** на Стрічці, Обговореннях, Дошці, усіх 4 підсторінках FAB, картці оголошення | 🟡 точково |
| `prefers-reduced-transparency` | 🔴 **0 файлів** | Δ ≤ шум скрізь | 🔴 **немає** |
| `prefers-contrast` | 🔴 **0 файлів** | Δ ≤ шум скрізь | 🔴 **немає** |

📐 **Провалів — 36 із 42** (14 екранів × 3 налаштування).

Що це означає практично:
- людина з увімкненим «зменшити рух» на **Стрічці, Дошці, в Обговореннях і в
  усьому підменю FAB** отримує рівно ті самі анімації, що й усі;
- людина з «зменшити прозорість» бачить ті самі **39** скляних поверхонь;
- людина з «підвищити контраст» не отримує нічого.

✅ **Але не все так погано, як здавалось на старті потоку.** Попередній зріз у
`BYYOU_PLAN.md` рахував лише CSS. Насправді `prefers-reduced-motion` шанують ще й
у JS — `src/app.js:79`, `home-caps.js:247`, `community-blocks.js:1575`: авто-гортання
капсул і каруселей при цьому налаштуванні **не запускається взагалі**. Це
найважливіша частина (рух, що йде сам, без участі людини), і вона зроблена.

🔴 Разом із забороною масштабування (`user-scalable=no`, §1 вище) доступність —
єдиний розділ, де провал не точковий, а системний.

---

## §15 ТИПОГРАФІКА — здоровіше, ніж очікувалось

> Скіл: «Tracking is size-specific — never one value for all sizes». Велике —
> тісніше (відʼємне), дрібне — трохи вільніше.

📐 Заміряно **1 000+ живих текстових вузлів**, згруповано за кеглем:

| кегль | вузлів | tracking (переважний) | leading | за скілом? |
|---|---|---|---|---|
| 36px | 3 | **+0em** | — | 🟡 велике має бути тісніше |
| 26px | 3 | **+0em** | 1.00 | 🟡 те саме |
| 25px | 4 | −0.02em | 1.14 | ✅ |
| 22px | 6 | −0.02em | 1.14 | ✅ |
| **20px** | **41** | **+0.05em** ×28 | 1.07 | 🔴 найбільша аномалія |
| 16px | 24 | +0.013em | 1.22 | ✅ |
| 13px | 46 | +0.023em | 1.35 | ✅ |
| 12px | 96 | +0em / +0.042em | 1.33 | ✅ |
| 9.5px | 68 | +0.032em | 1.00 | ✅ |
| 8.5px | 14 | +0.094em | — | ✅ |

✅ **Загальний напрямок правильний:** дрібне має додатний трекінг (до +0.094em на
8.5px), середнє — близько нуля, 22-25px — відʼємний. Це саме те, чого просить
скіл, і воно зроблено свідомо.

🔴 **Дві аномалії:**
1. **20px, +0.05em на 28 вузлах** — найбільша група тексту в застосунку йде
   проти правила: на цьому кеглі трекінг мав би бути ~0, а він розріджений як у
   дрібного шрифту.
2. **Найбільші кеглі (36px, 26px, 23px) — трекінг рівно 0**, тоді як 25px і 22px
   уже мають −0.02em. Тобто найбільший текст «розсипається» дужче за середній.

### Шрифти — чотири сімейства там, де скіл просить системне

| сімейство | вузлів |
|---|---|
| `-apple-system` | 287 |
| **`Arial`** | **125** |
| `system-ui` | 88 |
| `Georgia` | 30 |
| `monospace` | 14 |

⚠️ `-apple-system` і `system-ui` — це **одне й те саме** системне сімейство,
записане двома різними способами у різних місцях. `Georgia` (30) — вочевидь
свідома засічкова для текстів статей. А ось **`Arial` на 125 вузлах** — це не
системний шрифт iPhone; там, де він виграє в каскаді, текст малюється не тим
накресленням, що решта застосунку.

---

## §16 ВІСІМ ПРИНЦИПІВ — навігація, зворотність, ясність

> Скіл: «Every screen should answer: Where am I? Where can I go? What's there?
> How do I get out? **Never trap the user.**» І окремо про **Agency**: легке
> скасування помилки, а вікно підтвердження — лише для справді незворотного.

### ✅ Зворотність шарів — тримається одним місцем правди

📐 `openLayer(` — **10** викликів, `closeLayer(` — **9** (десятий шар закриває
системний жест iPhone, і це задум `core/layers.js`). Тобто повноекранні шари
кладуться в історію браузера, і «назад» від лівого краю працює як у рідних
застосунках. Це рівно те, чого просить §7-§16, і зроблено правильно.

### ✅ Скасування помилки — патерн «Видалено · Скасувати» існує

`showToast` уміє кнопку дії (`core/utils.js:348`), і вона реально вживається:
видалення розмови в `board-chat.js:955` дає тост зі «Скасувати» замість
запитання наперед. Це саме та «forgiveness», яку описує принцип **Agency**.

### 🟡 Але поруч живуть 9 системних `confirm()`

| файл | скільки | про що |
|---|---|---|
| `tabs/feed.js` | 3 | видалити коментар · видалити пост · прибрати людину |
| `tabs/board-chat.js` | 1 | видалити оголошення назавжди |
| `core/messages-ui.js` | **4** | легасі-групи — **екран недосяжний з інтерфейсу** |

Зауваження не в кількості, а в тому, що це **браузерне вікно**: воно виглядає не
як застосунок, зупиняє все і не має мови продукту. Чотири з девʼяти взагалі
лежать у мертвому коді. 🔑 Патерн «дія + Скасувати в тості» в проєкті вже є —
тобто питання не «як зробити», а «де саме підтвердження справді заслужене»
(видалити оголошення назавжди — так; видалити свій коментар — навряд).

### ⚠️ Документація тут бреше: «Escape не закриває жодну модалку»

У `START_HERE.md` і в `CLAUDE.md` записано, що Escape не закриває нічого і що це
«девʼятий випадок брехливої перевірки». 📐 **Заміряно `grep`: це вже неправда** —
Escape обробляють `tabs/board.js:421` (сторінка оголошення, слухач на самій
модалці), `tabs/board.js:2015` (меню Дошки) і `tabs/board-discussions.js:298`
(чат). Тобто твердження застаріло щонайменше на кілька сесій.
🟡 На телефоні Escape не потрібен, але запис у документі хибний — і саме за ним
наступна сесія робитиме висновки.

---

## 🔴 B-31 — знайдено під час обходу (не за скілом, а за розбіжністю в числах)

Пункт бургер-меню **«Особистий кабінет» не відкриває нічого.**
`src/core/sidebar.js:180` клікає `#account-btn`, а такого елемента в застосунку
вже немає — кнопку перенесли на `[data-account-btn]`. `?.click()` на `null`
мовчить, тому й помилки в консолі немає.
**Як знайшлось:** прилад дав для «Кабінету» числа один-в-один з Громадою.
**Доведено наживо:** `#account-btn` немає · пункт меню → кабінету немає ·
`[data-account-btn]` → `#acc-cab` зʼявляється.
Записано в `CSTL_BUGS.md`. Правка = один рядок, **не зроблена** (потік = аудит).

---

# 🎯 ЗВІД: ЩО РОБИТИ, ЗА ПОРЯДКОМ «ЕФЕКТ ÷ ЦІНА»

> Порядок — моя рекомендація, не рішення. Останнє слово за Вовою.
> Кожен рядок можна закрити окремим блоком і окремим деплоєм.

| # | що зробити | ефект | ціна | § |
|---|---|---|---|---|
| **1** | **B-31: оживити пункт «Особистий кабінет»** — `#account-btn` → `[data-account-btn]` | зламане стає робочим | **1 рядок** | — |
| **2** | **Відгук на натиск для таб-бару** — 4 з 5 кнопок мовчать; у файлі є готовий взірець (`.tab-item--home:active`) | найчастіший дотик у застосунку | ~5 рядків CSS | §1 |
| **3** | **Спільне правило `:active` для кнопок** — 152 «німі» цілі: ні `:active`, ні рідного підсвічування | половина застосунку перестає відчуватись мертвою | одне правило + винятки | §1 |
| **4** | **`prefers-reduced-transparency` і `prefers-contrast`** — їх немає взагалі (0 файлів) | закриває найгіршу дірку аудиту | 2 медіа-блоки на 39 скляних поверхонь | §14 |
| **5** | **Висота дрібних тап-цілей** — 186 менші за 44px, і майже всім бракує лише ВИСОТИ | лікується вертикальним padding, розкладку не чіпає | точково | §10 |
| **6** | **Крапки 5-8px** (`.bus-week-page-dot`, `.bhv4-dot-nav`, `.btb-bell` 6×6) — невидима зона дотику | у них зараз майже неможливо попасти | псевдоелемент `::after` | §10 |
| **7** | **`prefers-reduced-motion` на решту екранів** — зараз лише Громада; Стрічка, Дошка, Обговорення, усі 4 підсторінки FAB дають Δ0 | | по одному блоку на зону | §14 |
| **8** | **`transition: width/left 600ms` → `transform`** (`.bhv4-fill`) — найдовший і найважчий перехід | плавність на слабкому телефоні | 2 місця | §11 |
| **9** | **Трекінг на 20px (+0.05em, 28 вузлів) і на 36/26/23px (0em)** | єдина група, що йде проти власного ж правила проєкту | кілька рядків | §15 |
| **10** | **`Arial` на 125 вузлах** — не системний шрифт iPhone; звести з `-apple-system`/`system-ui` | однорідність накреслення | пошук у каскаді | §15 |

### 🔴 Три питання до Вови — рішення, а не правки

1. **Масштабування заборонено** (`user-scalable=no`, `maximum-scale=1.0`).
   Прибрати = дати людям із поганим зором збільшувати текст, але можливі
   побічні ефекти на iOS (подвійний тап зумить, поля вводу смикають екран).
   **Прибираємо, лишаємо, чи пробуємо і дивимось на телефоні?**
2. **Жорсткий край Громади й Дошки** (`overscroll-behavior: none`). Стоїть, щоб
   таб-бар не «обрізався» у PWA. Чи пробуємо `contain` і дивимось на iPhone?
3. **Гортання категорій новин** (`attachSwipe`) — зараз нічого не рухається під
   пальцем, категорія перемикається стрибком після відпускання. Переписати на
   стеження 1:1 — це помітна робота в `news-hub.js`. **Беремось?**

### 🛑 Чого в цьому списку свідомо НЕМАЄ

- **Пружин замість CSS-переходів** (§3-4). Це переписування всієї механіки руху
  заради принципу, а не заради скарги. Тривалості в проєкті здорові, а передача
  швидкості вже є там, де вона найпомітніша.
- **`transform` назад у меню FAB** (§7). Уже пробували — WebKit давав привид
  разом із `backdrop-filter` (`style/board.css:872`). Не відкочувати наосліп.
- **Дотику до `core/keyboard.js`, `sheet-motion.js`, `layers.js`.** Зона
  підвищеної обережності (HOT_RULES №9-10) — аудит їх описує, не чіпає.
- **Свайпу для модалки оголошення.** Знято свідомо 02.08 на користь системного
  жесту iPhone. Не рахувати прогалиною.

### 📋 Що з цього НЕ перевіряється нічим

Усі знахідки вище — числа з Chromium у пісочниці. **На живому iPhone не
перевірено нічого:** ні затримка тапу, ні гумові межі, ні поведінка при
`user-scalable`. Це не застереження для годиться — у цьому проєкті вже було
девʼять випадків, коли зелена перевірка описувала стан, якого людина не бачить.

# АУДИТ CSTL NEWS — повний список задач по вкладках

**Створено 03.07.2026 · остання РЕВІЗІЯ 26.07.2026 (сесія G).**

> Живий документ. Вова диктує задачі по вкладці — вписуємо їх у відповідну секцію.
> Легенда пріоритету: 🔴 гостре · 🟡 середнє · 🟢 пізніше. 🆕[В] = задача від Вови.
> ℹ️ Проєкт одноосібний (власник Вова) — усі вкладки/зони його. Мітки [Вова]/[Рома] прибрано 23.07.

---

## 🔄 РЕВІЗІЯ 26.07.2026 — як читати цей файл

Кожен відкритий пункт звірено **по коду й даних**, не з пам'яті. Мітки ревізії:

| Мітка | Значення |
|-------|----------|
| ✅ **ЗРОБЛЕНО** | Реалізовано. Поруч — доказ (файл:рядок або дані). |
| 🗑 **ЗАСТАРІЛО** | Ціль задачі більше не існує (продукт змінився). Кодом закрити неможливо. |
| ⬜ **ТРЕБА** | Підтверджено відкритим — перевірено, що досі не зроблено. |
| ⏸️ **ЗАБЛОКОВАНО** | Треба, але чекає зовнішніх даних/рішення. |

### Підсумок ревізії (цифри пораховані по цьому файлу, не на око)

| | Скільки | Що це |
|---|---|---|
| ✅ **Закрито** | **66** | З них **20 виявлено ревізією** + **СИ-2 зроблено 26.07** (−11МБ репо). |
| ⬜ **Реально треба** | **42** | Перевірено, що досі не зроблено. |
| ❌ **Закрито рішенням Вови 26.07** | **8** | ПД-3, ПД-4, ПД-5, ПД-6, ПД-10, ПД-11, ПД-13 (вкладку Подій не повертаємо) + Г-17. |
| 🗑 **Застаріло** | **2** | Н-9, СИ-8. |
| ⏸️ **Заблоковано** | **2** | А-1, А-1.1 — нема офіційних даних автостанції. |
| 🆕 **Додано ревізією** | **4** | Н-17, СТ-5, СТ-6, СТ-7. |

### 🟢 Рішення Вови від 26.07.2026 (по підсумках ревізії)

1. **ПД-3 — вкладку «Події» НЕ повертаємо.** «Лишити блоком, ПД-3 закрити».
   → закрито ПД-3 + 6 пунктів календаря (ПД-4, ПД-5, ПД-6, ПД-10, ПД-11, ПД-13).
2. **Н-17 — контент Громади:** «агент пише, редактор публікує якщо є що + парсинг так як зараз».
   ⚠️ **Алла вийшла з проєкту** — роль редактора вакантна, кабінет лишається.
3. **Г-6 — обмеження «Громаду доробляти останньою» ЗНЯТО.** Ведемо нарівні з рештою.
4. **Г-17 — закрито,** питання вирішене (Вова: «Закрий це питання, воно вирішене»).

**20 пунктів, знайдених уже зробленими:** ПД-2 · Н-1 · Н-2 · Н-4 · Н-8 (частково) ·
Г-8 · Г-9 · Г-10 · Г-11 · Г-13 · Г-16 · Г-21 · О-1 · О-2 · О-3 · О-4 · О-5 ·
П-4 (частково) · СИ-4 · АН-1. Плюс задача «порядок блоків Громади» з `BACKLOG.md`.

> Тобто **третина того, що висіло як робота, роботою не була** — саме через це
> ревізія й була потрібна.

### 🔴 ТРИ ЗМІНИ ПРОДУКТУ, ЯКІ ПЕРЕВЕРНУЛИ ЦІЛІ СЕКЦІЇ

Аудит писався 03-11.07. Відтоді змінилась сама будова застосунку — тому частина
пунктів говорить про екрани, яких уже нема.

**1. Таб-бар (22.07).** Зараз: **Стрічка · Обговорення · [Громада] · Дошка · Автобуси**
(`index.html:160-208`). Вкладок «Новини» і «Події» **в таб-барі нема**.
`switchTab('news')` і `switchTab('events')` обидва перекидають на `'shotam'` (Стрічку) —
`src/app.js:33`.

**2. Новини стали блоком, а не вкладкою.** Стрічка новин живе блоком «Табло новин»
усередині Громади (`src/tabs/news.js:53-56`, `src/tabs/community.js:222-229`).
Окремої сторінки новин не існує.

**3. Події втратили власний екран.** `initEvents()` (`src/tabs/events.js:249`)
**не імпортується ніде**. З усього модуля живе лише `openShotamModal`
(`community-blocks.js:16`) — відкриває картку події з блоку Громади.
Тобто календар, чіпи-фільтри і сітка Подій зараз не рендеряться взагалі.

**Наслідок:** підсекція «Верхній календар + фільтри» описувала мертвий код.
✅ **Питання закрите рішенням Вови 26.07: вкладку Подій не повертаємо** → ці пункти
закриті назавжди, а `src/tabs/events.js` лишається лише постачальником `openShotamModal`.

### 🆕 Чого в аудиті НЕМА, а в проєкті вже Є

З'явилось після 11.07 і жодним пунктом не описане:

- **Кабінет редактора** — `editor_users` з правами (`can_create`/`can_publish`/`can_events`),
  `cms_articles`, інвайти, планування публікацій, дедуп
  (`scripts/supabase_editor_cabinet.sql`, `scripts/supabase_editor_invites.sql`,
  `scripts/supabase_editor_scheduling.sql`, `scripts/supabase_cms_dedup.sql`).
  _(06.08: тут стояли скорочення на кшталт «\_editor\_invites.sql» — виглядало як окремі файли, яких нема; це були хвости від `supabase_`.)_
- **Конвеєр «Supabase редагує — Git публікує»** — `sync_cms.py` + `.github/workflows/cms-sync.yml`
  (кожні 5 хв).
- **AI-агент новин Громади** — `scripts/ai_news_agent.py` + `ai-news-agent.yml` (щогодини):
  пише оригінальні матеріали чернетками в кабінет.
- **Аналітика** — розділ в адмінці + `ai-analytics-summary` (Edge Function) + `supabase_analytics.sql`.
- **Нові push-канали** — `send-page-push` («Стрічка»), `send-comment-push`.
- **`gitleaks.yml`** — скан секретів у репозиторії.
- **«Стрічка»** — цілий модуль `src/tabs/feed.js` (177КБ, найбільший файл проєкту).

---

## 🎯 ПРІОРИТЕТ РЕАЛІЗАЦІЇ (порядок, рішення Вови 04.07)

1. **🟢 MVP / ЦЯ ВЕРСІЯ:** 🚌 Автобуси → 📋 Дошка → 📅 Події → 📰 Новини → 🏰 Громада
   (Громада остання — збирає готові віджети інших вкладок).
2. **🔧 Наскрізні — ТЕЖ У ЦІЙ ВЕРСІЇ:** 🔔 Push · ⚙️ Системне.
3. **🕗 V2 — друга черга:** 💬 Групи · 💡 Світло.

> ⚠️ **Ревізія 26.07:** порядок писався коли Події й Новини були вкладками. Зараз обидві —
> блоки Громади, тож «Громада остання» фактично означає «Громада разом з ними».

## ▶️ Як продовжити

1. Читати цей файл повністю — майстер-список задач.
2. Нові задачі Вови вписувати у відповідну секцію. ID стабільні (не перенумеровувати).
3. Формат: що зробити · де в коді · критерії готовності · залежності.
4. **НЕ починати код без явного «Роби».**

---

# 🟢 MVP — ПЕРША ЧЕРГА

---

## 🧪 ФІНАЛЬНЕ ТЕСТУВАННЯ — ЖИВИЙ ПРОГІН УСЬОГО ЗАСТОСУНКУ

> **Заведено 08.08 за рішенням Вови:** *«потім, коли будемо тестувати весь застосунок,
> ми впишемо це в завдання кожної вкладки, бо ми потім, коли закінчимо працювати,
> будемо тестувати всі вкладки, весь застосунок»*.
>
> 🔑 **Навіщо окремий розділ, а не пункт у кожній вкладці.** Живий тест — це те, чого
> НЕ закриває жоден стенд: два справжні акаунти, два телефони, справжня мережа, push,
> клавіатура iOS. Тримати його всередині вкладки означає, що вкладка ніколи не буде
> «закритою» — а вона закрита по коду. Тому код і живий прогін розведені.
>
> ⚠️ **Чому це не формальність.** 07.08 аудит Дошки написав «Critical-дефектів немає», а
> наступного дня знайшовся **B-30** — пункт FAB мовчки не відкривався. Знайшов його
> **Вова живим тестом**, не стенд і не аудит. Половина зони за входом тоді не була
> покрита нічим.

| вкладка | що прогнати живими руками | стан |
|---|---|---|
| **Дошка** | приватний чат на 2 акаунтах і 2 пристроях (**Д-28**): надсилання/отримання, фото, видалення, синхронізація, push, клавіатура, зависання · подача оголошення до публікації · «Мої оголошення» → статуси, «Підняти», кулдаун · скарга | ⬜ |
| **Стрічка** | публікація поста з кількома фото · коментарі й відповіді 2 рівні · лайки · закріплення · редагування сторінки · дзвіночок | ⬜ |
| **Обговорення** | створення теми · репліки · свайп-відповідь · довге натискання · антиспам | ⬜ |
| **Громада** | усі віджети з реальними даними · переходи з віджетів · погода · збори | ⬜ |
| **Автобуси** | пошук рейсу · відстеження · push про рейс · дні тижня | ⬜ |
| **Наскрізне** | вхід Google на новому пристрої · заслінка розробки · офлайн · встановлення PWA · оновлення після деплою (`CACHE_NAME`) | ⬜ |

🛑 **Правило прогону:** знайдене вписувати в `CSTL_BUGS.md` ОДРАЗУ, з номером і кроками
відтворення. Напівпам'ять «там щось смикалось» через день не відтворюється — це вже
коштувало проєкту двох сесій наосліп.

---

## 🚌 АВТОБУСИ

- **А-1** ⏸️🔴 **Зворотні маршрути «З Олики»** 🆕[В 03.07]
  **ЗАБЛОКОВАНО — чекає А-1.1.** Зараз показуються лише рейси В Олику.
  🔎 **Ревізія 26.07 — діагноз підтверджено втретє, вже наживо по даних:**
  у `data/schedule.json` 21 день × ~25 рейсів, усього **6 унікальних маршрутів**
  (`Ківерці Олика`, `Луцьк Жорнище`, `Луцьк Олика`, `Луцьк Рівне`, `Луцьк- Личани`,
  `Луцьк- Личани ч/з Покащів`). **Жодного рейсу, де Олика не кінцева, а кінцева — Луцьк/Ківерці/Рівне.**
  ⚠️ **Важливо: парсер тут ні до чого.** `MARSHRUTI` (`scripts/parse_vopas.py:45-53`) **вже просить
  зворотні напрямки** — там є `("Олика","Луцьк")`, `("Личани","Луцьк")`, `("Олика","Ківерці")`.
  VOPAS їх просто не віддає (Олика — проміжна зупинка). Тобто кодом це не лікується, потрібне ІНШЕ джерело.
  - **А-1.1** ⏸️🔴 **Отримати офіційні дані від Луцької автостанції.** _Виконавець — Вова, Claude готує текст звернення._
    Кроки: (1) письмове звернення до Автостанції №1 Луцьк (VOPAS) — перелік рейсів «З Олики»,
    час відправлення/прибуття, порядок і км зупинок, дні курсування; (2) звернутись і отримати
    відповідь; (3) занести у `data/schedule.json` / `data/route-stops.json`.
    Операційні нюанси для запиту (Вова 04.07): ~80% автобусів вертаються тим самим рейсом;
    останній вечірній ночує в Личанах і стартує зранку звідти.
    🔎 Знайдені альтернативні джерела (20.07): ⭐`olexrada.gov.ua/doc/bus_schedule.pdf`,
    ⭐реєстр Волині XLSX на `data.gov.ua`, `westua.net`. Мережа середовища їх блокує (403) —
    **відкриває Вова руками.** Повне дослідження → `docs/BUS_ROUTES_RESEARCH.md`.
    **Блокує весь А-1.**

- **А-6** ⬜🟡 **Повторний аудит push-сповіщень рейсів** 🆕[В 11.07] — живий тест:
  (1) відстеження працює для ВСІХ рейсів; (2) підписка на кожен рейс; (3) push приходить
  для всіх типів (запланований / проміжна зупинка / кінцевий).
  _Ревізія: кодом не перевіряється — потрібен телефон. Лишається відкритим._

- **А-7** ⬜🟡 **Маршрут «Рівне–Довгошиї» (через Олику)** 🆕[В 20.07] — рівненський перевізник,
  VOPAS його не показує. Знайдено відправлення з Рівного: **4:25, 6:40, 8:00, 12:50, 15:30, 17:10**;
  перевізники (Яцишин/Санрайз/Мохнюк) — ті самі, що в Олиці → траса реально через Олику.
  Треба: підтвердити зупинки (Рівне–Клевань–Олика–Ківерці–Довгошиї?) + офіційні часи/км →
  ручний маршрут у `route-stops.json` + `schedule.json` + «Довгошиї» в `ALLOWED_STOPS`.
  🔎 **Ревізія 26.07: не почато** — рядок «Довгошиї» відсутній у `data/schedule.json`,
  `data/route-stops.json` і `scripts/parse_vopas.py` (0 входжень).
  Джерела: табло Рівне `bus.com.ua?as=560100`, `ogo.ua`, `horyn.info`.

### ✅ Закриті
- ~~А-2~~ ✅ 04.07 — Edge Function `send-bus-push` v11 на проді (T-15 проміжна зупинка, вікно T-0 −3).
- ~~А-3~~ ✅ 04.07 — надійне скасування push (`unsubscribeFromPush` + черга `flushPendingUnsub()`).
- ~~А-4~~ ✅ 04.07 — «зараз 10 рейсів» застаріло: парсер тягне ~27/день динамічно.
- ~~А-5~~ ✅ 04.07 — застарілі згадки B-05 прибрано з BACKLOG/ROADMAP/CLAUDE.

---

## 📋 ДОШКА — ✅ **ЗАКРИТА ПО ЗАВДАННЯХ (рішення Вови 08.08)**

> **Слова Вови:** *«тоді як з дошкою все добре, залишився тільки мій живий тест на двох
> пристроях — тоді позначай це як по завданнях закриту вкладку»*.
>
> Кодових завдань по Дошці не лишилось. Пройдено: аудит під MVP (07.08, вердикт
> READY WITH MINOR FIXES 88%) → закрито **C-1** (чернетка подачі), **B-1** (порожній стан
> називав одну причину з двох), **B-2** (скидання фільтрів одним тапом), **A-2** (мертва
> гілка без `requireAuth`), **A-1** (три поведінкові сторожі замість сторожів вигляду),
> **B-30** (пункт FAB «Повідомлення» мовчки не відкривався).
> Відкладено свідомо: **B-3** — стеля 200 оголошень (спрацює тихо на 201-му; сьогодні
> на Дошці ~19, тобто запас удесятеро). Умова повернення — понад 150 оголошень.
>
> ⬜ **Єдине незакрите — живий тест на двох пристроях.** Перенесено в розділ
> «ФІНАЛЬНЕ ТЕСТУВАННЯ» нижче: тестуємо не Дошку окремо, а весь застосунок разом,
> коли робота завершиться.

- **Д-28** ⬜🟡 **Повний ручний аудит приватного чату** 🆕[В 11.07] — тест на 2 користувачах
  і 2 пристроях: надсилання/отримання, видалення повідомлень, синхронізація, поява/приховування
  клавіатури, робота модалки, зависання і UI/UX баги.
  _Ревізія 26.07: живий тест, кодом не закривається._
  _08.08: переїхав у «ФІНАЛЬНЕ ТЕСТУВАННЯ» — там він у списку вкладки «Дошка»._
  ⚠️ Перетин: сесія F додала живу синхронізацію постів і ядро мережевих збоїв — це теж
  треба перевірити в тому ж заході.

### ✅ Закриті (звірено)
- ~~Д-1~~ ✅ 08.07 (PR #267) — Захід 2: репутація + автопублікація (5 схвалень → `trusted`).
- ~~Д-2~~ ✅ 05.07 — серверний антиспам-тригер на `comments`.
- ~~Д-3~~ ✅ 12.07 (PR #374) — edit-режим оголошення + RPC `update_board_post`.
- ~~Д-5~~ ✅ 12.07 (PR #393) — Обговорення винесено у `board-discussions.js`.
- ~~Д-6~~ ✅ 12.07 — прев'ю-картка = вигляд реальної.
- ~~Д-8~~ ✅ — `data-msg-soon` → `data-open-chat`.
- ~~Д-9~~ ✅ 10.07 (PR #302) — єдиний фон карток + семантичні кольори тегів.
- ~~Д-10~~ ✅ 08.07 (PR #275) — локація оголошення + спільний `settlements.js`.
- ~~Д-11~~ ✅ 08.07 (PR #276) — шапка Дошки із лічильником.
- ~~Д-12~~ ✅ 08.07 (PR #275) — фільтр за локацією.
- ~~Д-13~~ ✅ 11.07 (PR #334) — реакції з оголошень прибрано.
- ~~Д-14~~ ✅ 05.07 — «Повідомлення» у FAB Дошки.
- ~~Д-15~~ ✅ 08.07 (PR #273) — аудит приватних повідомлень + закрито дірку S1.
- ~~Д-16~~ ✅ 08.07 (PR #274) — заголовок оголошення обов'язковий.
- ~~Д-17~~ ✅ 11.07 — липка шапка модалки.
- ~~Д-18~~ ✅ 08.07 (PR #263) — розділення «Збережених» Дошки й Обговорень.
- ~~Д-19~~ ✅ 10.07 (PR #302) — локація «Олицька громада» на картці.
- ~~Д-20~~ ✅ 09.07 (PR #292) — векторний пін + НП великими.
- ~~Д-21~~ ✅ 13.07 (PR #395) — фон Дошки «темний корок».
- ~~Д-22~~ ✅ 12.07 — усі 3 FAB застосунку на спільному склі. _(Закриває й О-4.)_
- ~~Д-23~~ ✅ 12.07 — категорія без автовибору.
- ~~Д-24~~ ✅ 12.07 — поле «Телефон» + маска + колонка `profiles.phone`.
- ~~Д-25~~ ✅ 12.07 (PR #371) — 3 таби «Мої оголошення».
- ~~Д-26~~ ✅ 12.07 (PR #372) — FAB + векторні іконки вкладки.
- ~~Д-27~~ ✅ 12.07 (PR #381) — «Збережені» окремим екраном.
- ~~Д-фут~~ ✅ 10.07 (PR #306-#318) — редизайн футера картки.
- ~~Д-4~~ ❌ не робимо (анімоване стискання фото). ~~Д-7~~ ❌ не робимо (кнопка Telegram).

---

## 📅 ПОДІЇ

> 🔴 **РЕВІЗІЯ 26.07: секція наполовину мертва.** Вкладки Подій нема, `initEvents()` не
> викликається. Живий лише блок «Найближча подія» в Громаді (`renderEventBlock`,
> `community-blocks.js:908`) + модалка картки (`openShotamModal`).
> Код: `src/tabs/events.js` (11КБ) · `data/events.json` (5 подій) · `data/holidays.json` (25 свят).

### 🖼 Основні

- **ПД-1** ⬜🔴 **Вкладка/блок порожні на реальних даних**
  🔎 **Ревізія 26.07 — підтверджено наживо і стало гірше:** у `data/events.json` **5 подій,
  майбутніх — 0**. Остання за датою — 15.07, сьогодні 26.07. Тобто блок «Найближча подія»
  тримається виключно на fallback-святах (Г-16).
  Корінь контентний: нема хто наповнює. Залежить від ПД-2 (тепер розблоковано) + Н-4 (зроблено).
  **Це вже не технічна задача, а контентна — потрібне рішення Вови, хто веде події.**

- ~~**ПД-2**~~ ✅ **ЗРОБЛЕНО (виявлено ревізією 26.07)** — «нема способу додати подію без git» більше
  не відповідає дійсності. Кабінет редактора вміє події: право `can_events`
  (`scripts/supabase_editor_cabinet.sql:37`, `admin.html:529-531`), тип `type='event'` у
  `cms_articles`, кнопка «Скопіювати в Найближчі події» зі зв'язком `source_article_id`
  (`scripts/supabase_cms_featured_event.sql`). Публікація на сайт — конвеєром `cms-sync.yml`.
  ⚠️ Лишається перевірити наживо, що подія з кабінету реально доїжджає у блок Громади.

- ~~**ПД-3**~~ ❌ **ЗАКРИТО РІШЕННЯМ ВОВИ 26.07.2026 — вкладку Подій НЕ повертаємо.**
  Дослівно: «Лишити блоком, ПД-3 закрити».
  Події остаточно живуть блоком «Найближча подія» у Громаді. Слот у таб-барі за Стрічкою.
  _Історія: задача 04.07 вимагала повернути Події в таб-бар; 22.07 слот віддали Стрічці;
  ревізія Роми 05.07 («Новини всередині Подій») не реалізована і вже неактуальна._
  **Наслідок рішення:** підсекція календаря нижче закрита назавжди, `src/tabs/events.js`
  лишається лише постачальником `openShotamModal` — решту можна прибирати при чистці.

### 📅 Верхній календар + фільтри — ❌ ЗАКРИТО НАЗАВЖДИ (рішення Вови 26.07)

> Усі 5 пунктів описували `renderCalendar()` / `renderFilters()` / `getFiltered()` у `events.js`,
> який не рендериться. Після рішення по ПД-3 («лишити блоком») вкладки Подій не буде —
> отже цей код не оживе. **Пункти закриті, не робимо.**
> Описи збережені як історія задуму.

- ~~**ПД-11**~~ ❌ 🟡 Тижневий свайп замість 21-денного скролу: «Всі» статична поза смужкою;
  старт завжди від сьогодні (не від понеділка); 3 сторінки по 7 днів + 3 крапки; клас `--past` прибрати.
  За основу — `renderWeekStrip`/`getWeekDays` (`buses.js`). **Поглинає ПД-9.**
- ~~**ПД-13**~~ ❌ 🟡 Блок категорій відцентрувати під календарем + «Всі» теж статична.
- ~~**ПД-4**~~ ❌ 🟡 `CATEGORY_FILTERS` захардкоджений — генерувати з даних або узгодити набір.
- ~~**ПД-5**~~ ❌ 🟡 Неузгодженість «Свято»/«Свята» тримається на окремому `if`.
- ~~**ПД-6**~~ ❌ 🟡 Гейтинг «Створити нагадування» (`.ics`) вимагає логіну — можливо зайвий бар'єр.

### 🔧 Технічні / інше

- ~~**ПД-10**~~ ❌ 🟢 Нема плейсхолдера битих фото у картці події _(ціль — мертвий рендер `events.js`)_.
- ⬜ **ПД-8** 🟢 **Немає багатоденних/повторюваних подій** — модель знає лише одну дату;
  фестиваль на 3 дні чи щотижневе не виражається. Розширити схему (`date_end`, `repeat`).
  _Ревізія: лишається живим — стосується моделі даних, а не мертвого екрана. Тепер це схема
  `cms_articles` type='event', а не тільки `events.json`._
- ~~ПД-9~~ — поглинуто ПД-11.
- **ПД-7** = **Г-2** (віджет «Найближчі події») + **Н-6**. Дивись Г-2.

### 📥 Джерела контенту

- **ПД-12** ⬜🟡 **Список фейсбук-груп громади для моніторингу подій** 🆕[В 04.07] —
  заходи Олицької громади постяться у фейсбук-групах. Скласти перелік для редактора:
  моніторинг + ручне дублювання у Події.
  ⚠️ **Треба від Вови самі назви/посилання груп** — Claude їх не знає. Фейсбук закритий для
  авто-парсингу (нема RSS) → реалістично лише ручний моніторинг.
  Куди записати перелік — `docs/CONTENT_STRATEGY.md`, секція джерел подій. Пов'язано з ПД-1.

---

## 📰 НОВИНИ

> 🔴 **РЕВІЗІЯ 26.07: вкладки Новин нема — це блок «Табло новин» у Громаді**
> (`news.js:53-56`, `community.js:222-229`).
> **Стан даних заміряно 26.07:** 400 статей. Гео: Україна 204 · Волинь 142 · Світ 48 · **Громада 6**.
> За останні 72 год: 291 стаття (Україна 152 · Волинь 103 · Світ 32 · **Громада 4**).
> Категорії: **Суспільство 381 із 400 (95%)**, решта — Економіка 7, Культура 5, Спорт 4, Історія 3.
> Ексклюзивів: 3.

- ~~**Н-1**~~ ✅ **ВИРІШЕНО (заміряно 26.07)** — було «Волинь = 0 статей за 72 год».
  Зараз **Волинь = 103 статті за 72 год**, 142 всього. Джерела більше не душаться.

- ~~**Н-2**~~ ✅ **ВИРІШЕНО (заміряно 26.07)** — було «Україна та Світ = 83%, ліміти не тримають».
  Зараз Україна+Світ = 184 із 291 за 72 год = **63%**, при цілі ≤60-70%. Баланс у нормі.

- 🆕 **Н-17** ⬜🔴 **ДЕФІЦИТ ВЛАСНОГО КОНТЕНТУ** [ревізія 26.07]
  Старі болі балансу (Н-1/Н-2) закрились, натомість оголився головний: **Громада = 4 статті
  за 72 год і 6 із 400 усього (1.5%)**, ексклюзивів усього 3. Тобто застосунок про Олику на 98%
  складається з чужих новин — це б'є прямо в концепцію («ексклюзивні місцеві новини,
  яких немає ніде»).
  ✅ **Схема роботи затверджена Вовою 26.07, дослівно:** «агент пише, редактор публікує
  якщо є що + парсинг так як зараз». Тобто:
  1. **AI-агент** (`scripts/ai_news_agent.py`, cron щогодини) пише матеріали Громади
     **чернетками** в кабінет — автопублікації нема;
  2. **редактор-людина** ревʼю і публікує — **але лише якщо є що публікувати** (порожній
     прогін нормальний, кількість заради кількості не женемо);
  3. **RSS-парсер лишається як є** — Волинь/Україна/Світ не чіпаємо.
  🔴 **Головний блокер — людський, не технічний: Алла вийшла з проєкту, роль редактора вакантна.**
  Уся технічна база (кабінет, права, інвайти, конвеєр публікації) на місці й чекає людину.
  ⬜ **Лишається зробити:** (а) перевірити наживо, що агент реально доливає чернетки і вони
  доходять до стрічки; (б) вирішити, хто виконує роль редактора поки її нема — Вова сам
  чи новий запрошений куратор (механізм запрошення готовий: `editor_invites`).

- **Н-3** ⬜🟡 Перевірити на проді: Google News не 403? `resolve_gnews_url` працює?
  _Ревізія: жива перевірка, кодом не закривається._

- ~~**Н-4**~~ ✅ **ЗРОБЛЕНО (виявлено ревізією 26.07)** — кабінет редактора існує повністю
  (у старих документах — «кабінет для Алли»; Алла вийшла з проєкту 26.07, роль вакантна):
  таблиця `editor_users` з правами `can_create`/`can_publish`/`can_events`
  (`scripts/supabase_editor_cabinet.sql`), вхід і гейтинг в адмінці (`admin.html:507-531`),
  розділ «Статті» під правом редактора (`admin.html:765`), інвайти
  (`supabase_editor_invites.sql`), планування публікацій (`supabase_editor_scheduling.sql`),
  дедуп (`supabase_cms_dedup.sql`), конвеєр у стрічку (`sync_cms.py` + `cms-sync.yml`, кожні 5 хв).
  ⚠️ 6 технічних питань із `EDITOR_CABINET_PLAN.md` де-факто закриті реалізацією —
  сам план варто позначити виконаним.

- **Н-5** ⬜🟡 **Тематичний фільтр (=B-14)** — другий рядок чіпів за категорією.
  ⚠️ **ЗАБЛОКОВАНО Н-10** — при 95% «Суспільство» фільтр буде порожній.
  🔎 Ревізія: старий `GEO_FILTERS` зник **разом із вкладкою, але не зникла функція** — гео-чіпи
  переїхали у блок Громади під назвою `CM_NEWS_FILTERS = ['Громада','Волинь','Україна та Світ']`
  (`community-blocks.js:1155`, дефолт `'Громада'`). Тобто **перший рядок чіпів на місці**, і
  задача звучить точно як писалась: додати ДРУГИЙ рядок — за категорією.

- **Н-6** = **Г-2** (карусель подій зверху стрічки). Дивись Г-2.

- **Н-7** ⬜🟢 Нові волинські RSS-джерела (зараз Волинь Post + Конкурент).
  _Ревізія: менш гостре, ніж було — Волинь дає 103 статті/72год._

- **Н-8** 🟡 Дрібне, **частково зроблено:**
  - ✅ чіп «Олика» → «Громада» — зроблено (`news.js` GEO_COLORS: `'Громада'` основний,
    `'Олика'` лишено legacy для сумісності).
  - ⬜ `HOW_TO_PUBLISH.md`; ⬜ зовнішній cron (точні 30 хв); ⬜ розбити `parse_rss.py` на модулі.

- 🗑 **Н-9** **Прибрати сегмент Новини|Події з коду** — **ЗАСТАРІЛО, зникло само.**
  Сегмента `#news-seg-news`/`#news-seg-events` і `showNewsSegment` у коді нема — пішли разом
  із вкладкою. Ревізія Роми 05.07 («перевернути провідність») теж неактуальна.

- **Н-10** ⬜🔴 **Класифікатор не проставляє категорії — ПОГІРШИЛОСЬ**
  Було 93/109 = 85% «Суспільство». 🔎 **Заміряно 26.07: 381/400 = 95%.**
  ⚠️ Вова 21.07 скоротив набір до 4 базових категорій (Суспільство · Культура · Спорт · Економіка,
  `news.js:20-26`) + звід старих через `CATEGORY_ALIAS` — але класифікатор у `parse_rss.py`
  усе одно шле майже все в «Суспільство».
  **Лагодити ПЕРЕД Н-5**, інакше тематичний фільтр порожній.

- **Н-12** ⬜🔴 **Обрізані статті + спонукання йти на чужий сайт** 🆕[В 04.07]
  Деякі джерела (Конкурент та ін.) дають через RSS лише анонс → стаття публікується обрізана
  + блок «Джерело надає лише анонс — Читати повністю →».
  **Мета Вови:** публікувати повний текст у нас без спонукання йти на інші сайти; якщо
  неможливо — замінити джерело.
  - _Тех.стан:_ `fetch_full_article` (`parse_rss.py`) уже пробує повний текст Конкурента
    (whitelist + селектори), але не спрацьовує. Причини перевірити: селектори ≠ реальний HTML /
    сайт блокує скрапер / текст за JS.
  - _⚠️ Юридично:_ повний текст чужих видань без посилання = ризик авторського права.
    Наші ексклюзиви — вільно; агреговані чужі — стандарт «анонс + посилання».
  - _Варіанти:_ **A** повний текст + тиха атрибуція · **B** ставка на власний контент ·
    **C** полагодити витяг + замінити джерела-блокери. _Рекомендація Claude: A+C._
  - 🆕[В 11.07] Кнопку «Читати оригінал» **лишити** як додаткову.
  - 🔗 Ревізія: сильно перетинається з новим **Н-17** — власний контент знімає проблему в корені.

- **Н-13** ⬜🟡 **Модалка новини — кнопки вниз** 🆕[В 11.07] — «Поділитися»/«Зберегти» перенести
  вниз модалки + векторні іконки.
  🔎 Ревізія: стара помітка «конфліктує з Б5.1, обговорити з Ромою» **знята** — Роми в проєкті
  нема з 18.07, рішення повністю за Вовою.

- **Н-14** ⬜🟡 **Горизонтальне «Важливе сьогодні» + вертикальний список** 🆕[В 11.07] —
  зверху віджет-карусель важливих новин за сьогодні, нижче звичайний список.
  🔎 Ревізія: тепер стосується блоку «Табло новин» у Громаді (`renderCommunityNews`,
  `community-blocks.js:1185`), а не окремої вкладки.

- **Н-15** ⬜🟡 **UX скролу новин** 🆕[В 11.07] — усунути конфлікт вертикального скролу сторінки
  і скролу списку новин.
  🔎 Ревізія: у Громаді це вже інший скрол-контекст, ніж коли задача ставилась — **перевірити
  оком, чи проблема ще відтворюється**, перш ніж робити.

- **Н-16** 🟢 **Назви категорій великими літерами** 🆕[В 11.07]
  🔎 Ревізія: `text-transform: uppercase` у `style/community.css` присутній у 8 місцях, зокрема
  для заголовків блоків. **Ймовірно зроблено — але точну прив'язку саме до бейджів категорій
  новин я не довів.** Швидка перевірка оком на сайті закриє пункт.

---

## 🏰 ГРОМАДА (головна-дашборд)

> **Ревізія 26.07:** Громада тепер не «дашборд поверх вкладок», а **головний контейнер** —
> Новини і Події живуть саме тут. Тому Г-6 («доробляти останньою») втратив початковий сенс.

### ✅ Порядок блоків — ЗРОБЛЕНО

> Задача з `BACKLOG.md` («Табло новин → Дошка → Найближча подія → Автобуси → Погода → Контакти»)
> ✅ **виконана.** Звірено `src/tabs/community.js:222-276`: `cm-news-board` → `cm-board-content`
> → `cm-event-content` → `cm-bus-content` → `cm-weather-content` → (`cm-power-content` прихований)
> → `cm-contacts`. Рівно замовлений порядок. **У BACKLOG стоїть як відкрита — прибрати.**

### Шапка та навігація (app-wide)

- **Г-19** ⬜🟡 **Переробити шапку профілю** 🆕[В 11.07] — `CSTL LIFE` зліва, бургер справа,
  іконку кабінету вниз праворуч біля привітання + переробити її дизайн.
  Торкається `index.html` (шапка), `community.js` (`renderSkeleton`/greeting),
  `account-ui.js`, `sidebar.js`.
  ⚠️ **Обережно:** у шапці тепер живуть два приховані жести — 5 тапів по лічильнику версії
  = вхід в адмінку (`initAdminShortcut`), тап по лого = перемикач діагностики клавіатури
  (`initKbDebugShortcut`). Переробка шапки не має їх зламати.

- ~~**Г-21**~~ ✅ **ЗРОБЛЕНО (виявлено ревізією 26.07)** — бургер-меню: іконки векторні,
  внизу Instagram + Facebook з посиланнями на реальні сторінки проєкту
  (`src/core/sidebar.js:34-37`, `ICONS.brandInstagram` / `ICONS.brandFacebook`).

### Процес

- ~~**Г-6**~~ ✅ **ОБМЕЖЕННЯ ЗНЯТО РІШЕННЯМ ВОВИ 26.07.2026.**
  Правило «Громаду доробляти ОСТАННЬОЮ» (04.07) означало «спершу фіналізуємо вкладки-джерела,
  потім збираємо дашборд». Воно втратило сенс: Новини і Події переїхали ВСЕРЕДИНУ Громади,
  джерел «зовні» лишилось два — Дошка й Автобуси. Громада тепер не надбудова, а головний екран.
  **Відтепер Громаду ведемо нарівні з рештою вкладок** — Г-2, Г-4, Г-5, Г-12, Г-19, Г-20
  більше нічим не заморожені.
- **Г-4** ⬜🟡 **Повне ранжування блоків/віджетів** — визначити пріоритетність, найважливіші вище.
  _Частково перекрито виконаним порядком блоків (вище) — лишається саме ранжування за цінністю._
- **Г-5** ⬜🟡 **Повний UI/UX-аудит вкладки** — шрифти+ієрархія, палітра+контраст, відступи,
  редизайн карток, єдиний стиль. Скіл `ui-ux-pro-max`. Файли: `style/community.css` + розмітка.

### Профіль користувача

- **Г-7** ⬜🟡 **Переробити профіль користувача** — розташування, новий UI/UX, швидкий доступ
  до налаштувань. _Частково перекрито П-4 (тумблери сповіщень уже в кабінеті) і Г-9._
- ~~**Г-8**~~ ✅ **ЗРОБЛЕНО** — персоналізоване привітання: варіація за часом доби +
  ім'я з профілю для залогінених, «громадо» для гостя (`community.js:98-110`).
- ~~**Г-9**~~ ✅ **ЗРОБЛЕНО (виявлено ревізією 26.07)** — «Місце проживання» в профілі:
  select зі спільного списку НП + варіант «інше» (`account-ui.js:206`,
  `SETTLEMENTS`/`OTHER_SETTLEMENT` із `src/core/settlements.js`). Список спільний з Д-10, як і вимагалось.

### Віджети

- **Г-2** ⬜🟡 **Карусель 3-4 найближчих подій** 🆕[В 04.07, підтверджено 11.07] —
  зараз блок показує ОДНУ найближчу подію (`renderEventBlock`, `community-blocks.js:908`).
  Зробити свайп-карусель 3-4 найближчих. Тап → картка події.
  ⚠️ **Заблоковано ПД-1:** майбутніх подій у даних **нуль**, карусель буде порожня або зі свят.
  Об'єднує ПД-7 і Н-6.
- **Г-12** ⬜🟡 **Новий віджет Дошки (будувати з нуля, не патчити)** 🆕[В 04.07, підтв. 11.07] —
  теперішній застарів: (1) прибрати «Розмови»; (2) джерело — published `type:'board'`,
  сортування за `bumped_at`, останні ~10; (3) максимально очистити, більше простору картці.
  **Тип гортання — АВТОМАТИЧНИЙ свайп** (пасивно інформувати; переюз патерну
  `restartHeroAutoRotate`). Ручний свайп — опційно.
  ⚠️ Замінити `renderMiniCard`+`BOARD_MINI_TYPES` новим кодом, старий не латати.
  ⚠️[В 11.07] Новий будувати лише ПІСЛЯ фінального дизайну вкладки Дошка.
- ~~**Г-13**~~ ✅ **ЗРОБЛЕНО (виявлено ревізією 26.07)** — картка віджета Дошки показує
  **заголовок**, а не опис, із коректним fallback на перші 60 символів тексту:
  `const title = (p.title && p.title.trim()) || (p.text || '').trim().slice(0, 60) || 'Оголошення'`
  (`community-blocks.js:684`). Саме як просилось у Г-13.
- ~~**Г-10**~~ ✅ **ЗРОБЛЕНО (виявлено ревізією 26.07)** — «Корисні контакти» перероблено:
  у `data/community.json` контакти отримали групи `hero` / `emergency` / `local` і `priority`;
  рендер показує **«Місцеві» вгорі, «Екстрені» нижче** (`community-blocks.js:1115` і `:1133`) —
  рівно як просив Вова. **Реальні номери знайдено і внесено:** Олицька амбулаторія
  `+38 (03365) 9-21-12`, Олицька сільрада `+38 (03365) 9-15-50`, ЦНАП `099 205 69 12`,
  Волиньобленерго `0 800 501 482`, плюс 101/102/103/104.
  ⬜ Лишається дрібне: (а) редагування контактів з адмінки (зараз статичний JSON);
  (б) додати 112 і ветеринарну, якщо Вова дістане номер.
- **Г-20** ⬜🟡 **Адаптувати проблемні фотографії** 🆕[В 11.07] — переробити лише ті фото, що
  через неправильні пропорції масштабуються і втрачають якість. Уточнити конкретні при реалізації.
  🔗 Перетин із СИ-7 (важкі активні зображення) — робити одним заходом.
- ~~**Г-11**~~ ✅ **ЗРОБЛЕНО (виявлено ревізією 26.07)** — віджет погоди: запит Open-Meteo тягне
  `hourly=temperature_2m,precipitation_probability,weather_code` (`community-blocks.js:102`),
  день клікабельний, модалка малює **два графіки** — температура по годинах і ймовірність опадів
  (`community-blocks.js:227-241`), як у референсі iOS «Умови».
  ⬜ Лишається перевірити оком пункт «зменшити вдвічі по висоті».

### Ромин список (інтегровано 04.07)

- ~~**Г-16**~~ ✅ **ЗРОБЛЕНО** — fallback на найближче свято, коли майбутніх подій нема:
  `community-blocks.js:931` — коментар у коді дослівно «2) Fallback (Г-16)», читає `holidays.json`.
  Саме це зараз і рятує блок від порожнечі (див. ПД-1).
- ~~**Г-17**~~ ❌ **ЗАКРИТО РІШЕННЯМ ВОВИ 26.07.2026** — «Закрий це питання, воно вирішене».
  _Задачу завів Рома («забрати текст з фотографій»), не уточнивши, з яких саме фото.
  Після його виходу пункт лишався невиконуваним. Закрито._
- **Г-18** ⏸️🟢 **Віджет автобусів — колір** [рішення В 04.07: не чіпаємо]
  ⚠️ Він = **той самий компонент** `buildHeroCard`, що й вкладка Автобуси — розсинхронити не можна.
  Якщо колись міняти: або для обох разом, або окремий міні-віджет. Поки лишаємо.

### Довідково / чистка

- **Г-3** ⬜🟢 Хто веде `community.json` — актуалізація. _Перетин з Г-10 (частина вже закрита)._
- **Г-14** ⬜🔴 **Порожні блоки на реальних даних** — «Найближча подія» тримається лише на
  святах. Корінь — ПД-1 (0 майбутніх подій). Пом'якшено Г-16. **Лікується контентом, не кодом.**
- **Г-15** ⬜🟢 **Мертвий код `renderPowerBlock`** — функція експортується і **імпортується**
  (`community.js:15`), але виклик закоментовано (`community.js:419`), розмітка теж
  (`community.js:261`). Прибрати при чистці АБО лишити до оживлення Світла. Не пріоритет.
- ~~Г-1~~ ❌ Об'єднано зі С-1.

---

# 🕗 V2 — ДРУГА ЧЕРГА

---

## 💬 ОБГОВОРЕННЯ

> 🔎 **РЕВІЗІЯ 26.07: секція майже вся закрилась — Обговорення давно повноцінна вкладка.**
> Мітки `[Рома]` прибрано: зона Вови.

- ~~**О-1**~~ ✅ **ЗРОБЛЕНО** — Обговорення = справжня вкладка таб-бару
  (`index.html:172` `data-tab="discussions"`, `app.js:217`), код винесено в окремий модуль
  `src/tabs/board-discussions.js` (56КБ). Обидві частини задачі закриті.
- ~~**О-2**~~ ✅ **ЗРОБЛЕНО** — усередині лише публічні обговорення, назва «Обговорення».
- ~~**О-3**~~ ✅ **ЗРОБЛЕНО** — вкладка видима в таб-барі (2-й слот зліва).
- ~~**О-4**~~ ✅ **ЗРОБЛЕНО** — = Д-22, FAB на спільному склі з векторними іконками.
- ~~**О-5**~~ ✅ **ЗРОБЛЕНО** — лайк з лічильником на картці обговорення:
  `LIKE_EMOJI ❤️`, `reactionsByPost`, `likeCount`/`myLike` (`board-discussions.js:54-72`).
- 🗑 ~~**О-6**~~ **ЗАСТАРІЛО, зникло само** (звірено 25.08). Лічильника учасників у вкладці БІЛЬШЕ НЕМАЄ: його прибрали 11.08 при переході «Обговорення → Питання» як метрику чату, і сторож `tests/questions.mjs` тепер ВИМАГАЄ його відсутності. Пункт стосувався екрана, якого не існує. _(Початковий текст нижче лишено як історію.)_
- ~~**О-6-текст**~~ ⬜🟢 ~~**Перемістити лічильник учасників**~~ 🆕[В 11.07] — у верхню праву частину картки,
  над двома останніми повідомленнями.
  🔎 Ревізія: лічильник унікальних учасників у коді є (`board-discussions.js:745`),
  **розташування кодом не доводиться — перевірити оком.**
- 🟡 **О-7 — ВИГЛЯДАЄ ЗАКРИТИМ, ЧЕКАЄ ПАЛЬЦЯ** (звірено 25.08). Форма більше не «Створити обговорення», а «Запитати громаду» (`openDiscussionCompose`), і до неї підключений `attachSheetKeyboardFix`. 🛑 Пальцем на iPhone ніхто не звіряв — тому не закриваю, а переводжу в жовті. _(Початковий текст нижче.)_
- ~~**О-7-текст**~~ ⬜🔴 ~~**Баг: клавіатура перекриває модалку «Створити обговорення»**~~ 🆕[В 11.07] —
  має бути: спершу відкривається модалка, клавіатура — лише після тапу в поле; при відкритті
  клавіатури модалка зміщується вгору, щоб форма лишалась видимою.
  🔎 Ревізія: **лишається відкритим і став цікавішим** — у сесіях D-F написано ядро
  `src/core/keyboard.js`, яке саме це й вміє (замок фону, компенсація, `expandTop`), але
  **підключене лише до листа коментарів «Стрічки»**. Обговорення досі на власній механіці
  (`src/core/chat-core.js`).
  ⚠️ **HOT_RULES №9/№10 — зона підвищеної обережності.** Міграцію робити окремим заходом
  із живим тестом Вови на iPhone, не «заодно».

## 👥 ГРУПИ (V2 — фон, БЕЗ окремої вкладки)

> Групові чати лишаються в коді (`src/core/messages-ui.js`), приховані з UI до V2
> (`index.html`: «V2: Повідомлення і Групи приховані до версії 2»).
> Приватний чат 1-на-1 переїхав у Дошку (`src/tabs/board-chat.js`).

- **Ч-1** ⬜🔴 Push-система Етап 1: throttle груп 1/5хв, bundling, пріоритети, тогли у профілі. = П-1.
  _Ревізія: тогли у профілі частково зроблені — див. П-4._
- **Ч-2** ⬜🟡 **Групи — паритет із приватним чатом** — та сама логіка й безпека: надсилання,
  свайп-жести (reply/дії), read-статуси, edit/delete свого. Різниця — адаптація на 5-30 учасників.
  `openGroupChat` (`messages-ui.js`) досі v1 (голий текст), не переюзує `setupBubbleGestures`/`ACT_ICONS`
  з `chat-core.js`. RPC `editGroupMessage`/`deleteGroupMessage` у БД готові — бракує UI.
- **Ч-3** ⬜🟢 Етап 2: закріплені повідомлення, роль модератора, реакції у групах.
- **Ч-4** ⬜🟡 Онбординг порожнього стану груп.
- **Ч-5** ⬜🟢 Етапи 3-5: список чатів, статуси-присутність, офіційні спільноти.
  _Ревізія: «офіційні спільноти» частково закрито іншим шляхом — «Стрічка» + розділ «Спільноти»
  в адмінці (СИ-9)._
- ~~Ч-6~~ ❌ СКАСОВАНО 05.07 — концепт «Чати» розформовано.

## 💡 СВІТЛО (V2)

- ~~**С-1**~~ ✅ **РІШЕННЯ ВОВИ 04.07: ховаємо Світло повністю.** Вкладка й блок на Громаді
  приховані, `power.json` (DEMO) не чіпаємо. Фаза 3 відкладена безстроково.
  Мертвий код — Г-15. **Секція закрита.**

---

# 🔧 НАСКРІЗНІ — У ЦІЙ ВЕРСІЇ

---

## 🔔 PUSH-СИСТЕМА

> **Архітектура (оновлено ревізією 26.07).** Було 3 канали, зараз **6**:
> 🚌 `send-bus-push` · 📨 `send-chat-push` · 👥 `send-group-push` ·
> 🆕 📰 `send-page-push` («Стрічка») · 🆕 💬 `send-comment-push` (коментарі) ·
> 🆕 📊 `ai-analytics-summary`.
> Дозвіл просять автобусний трекер і чат (`ensureChatPush` через `core/push.js`).
> Підписки: `push_subscriptions` (route-bound) + `user_push_devices` (uid-bound).
> `verify_jwt`: `true` там, де кличе браузер (chat, group); `false` там, де кличе БАЗА
> (bus, page, comment) — базі нема чим показати токен людини.

- **П-1** ⬜🔴 **Групи без throttle:** N повідомлень = N push кожному. = Ч-1. _(V2 — можна відкласти з групами.)_
- **П-3** ⬜🟡 **Push у «видалену»/архівовану розмову** — функція не зважає на `thread_user_state`
  (`cleared_at`/`hidden`).
  🔎 **Ревізія 26.07: підтверджено відкритим** — у `supabase/functions/send-chat-push/index.ts`
  рядків із `thread_user_state` / `cleared_at` / `hidden` **нема (0 входжень)**.
- **П-4** ✅**ЧАСТКОВО ЗРОБЛЕНО** (виявлено ревізією 26.07) — «нема перемикачів сповіщень у Кабінеті»
  вже неправда: тумблери є (`account-ui.js:226` `data-notif`, стан у `notif_prefs:<uid>`
  через localStorage, обробники `:330-332`).
  ⬜ **Лишається:** (а) мут окремої розмови/групи; (б) перенести налаштування з localStorage
  у профіль, щоб вони синхронізувались між пристроями (зараз тумблер на телефоні не знає
  про тумблер на планшеті).
- **П-6** ⬜🟢 Push про модерацію Дошки («пост схвалено/відхилено») — актуально із Заходом 2 (Д-1, зроблено).
- **П-7** ⬜🟢 Інші канали: заявка в групу, офіційні оголошення, дайджест,
  онбординг «додай на екран iPhone». _(«Відповіді в Обговореннях» частково закрито
  каналом `send-comment-push` для «Стрічки».)_

### ✅ Закриті (PR #321, 10.07 + деплой 25.07)
- ~~П-2~~ ✅ фото-пуш не валить функцію (`bodyText = msg.text || (msg.photo_url ? '📷 Фото' : '')`),
  на проді v3 ACTIVE. = B-24 закрито.
- ~~П-5~~ ✅ чат сам просить дозвіл push (`ensureChatPush()` з жесту «відкрити чат»).
- ~~П-8~~ ✅ in-app банер чату при видимому додатку (`sw.js:166`).
- ~~П-9~~ ✅ тап по чат-пушу відкриває саме ту розмову (`thread_id` + hash-роутинг).

---

## ⚙️ СИСТЕМНЕ / ІНФРАСТРУКТУРА

### 🔴 Вага / продуктивність

- ~~**СИ-2**~~ ✅ **ЗРОБЛЕНО 26.07.2026** — 5 мертвих файлів видалено (`git rm`):
  `images/IMG_2321.png` (3.7МБ) · `images/cork.png` (3.1МБ) · `icons/castle-icon2.png` (1.7МБ) ·
  `images/0C9D1101-186A-40C1-AABB-86C6004BF4CB.png` (1.6МБ) ·
  `icons/75777DAD-1ACD-4845-B685-A1ECC71560FD.png` (1.3МБ).
  **Результат заміряно: репозиторій 34МБ → 23МБ** (`images/` 16→6.9МБ, `icons/` 6.8→3.9МБ).
  Далі того ж дня видалено ще й `bus-hero.png` (див. СИ-7) → **разом 34МБ → 21МБ**.
  Перед видаленням кожен файл окремо перевірено грепом по всьому репо (крім `backup/`) —
  **нуль посилань у коді**, згадка лише в цьому аудиті. Жодного не було в `STATIC_ASSETS`
  (`sw.js`) → `CACHE_NAME` не бампали. Після видалення `node build.js` зелений (39 файлів clean),
  `bundle.js` перезібрано без змін.
  ⚠️ **Схожі імена, які ЗАЛИШЕНІ і потрібні:** `cork2.png` (11 посилань),
  `castle-icon.png` (4, у precache), `bus-hero2.png` (6).

- **СИ-7** ⬜🟡 **Важкі АКТИВНІ зображення:** `images/cork2.png` 2.8МБ,
  `images/bus-hero2.png` 1.6МБ, `images/volleyball.jpg` 912КБ.
  Стиснути (WebP / менша роздільність). 🔗 Робити разом із Г-20.
  ✅ **Виправлення ревізії 26.07 — і воно вже закрите:** цей пункт помилково називав
  `images/bus-hero.png` активним. Насправді посилань на нього не було жодного (у джерелах
  використовується лише `bus-hero2.png`, `src/tabs/buses.js:938,993`; решта збігів «bus-hero» —
  це CSS-класи `.bus-hero`/`.bus-hero-map`, а не файл). **Видалено 26.07 за рішенням Вови,
  ще −1.6МБ.** Разом із СИ-2: **репозиторій 34МБ → 21МБ.**

### 🟡 Процес / інфраструктура

- ~~**СИ-4**~~ ✅ **ЗАКРИТО САМО (виявлено ревізією 26.07)** — ризик `auto-merge -X theirs` зник:
  файлу `.github/workflows/auto-merge.yml` **більше не існує** (auto-merge вимкнено 05.07).
  Шлях у `main` тільки через PR + `/finish`. Пункт можна не тримати відкритим.
- 🗑 **СИ-8** **Розбіжність робочої гілки `vova/board-reputation`** — **ЗАСТАРІЛО:** гілки нема,
  Захід 2 давно на проді (PR #267, 08.07). Пункт втратив предмет.
- **СИ-5** ⬜🟡 **Розсинхрон документації** — 🔄 **ЦЕ Й Є ПОТОЧНА РОБОТА (ревізія 26.07).**
  Цей файл — перша частина. Лишається: `BACKLOG.md` (розділ «🔴 Роби зараз» містить закриті
  задачі як відкриті), `CSTL_BUGS.md`, `START_HERE.md`, `EDITOR_CABINET_PLAN.md` (де-факто
  виконаний), `CHATS_NAV_PLAN.md` (концепт розформовано 05.07).
- ~~**СИ-3**~~ ✅ 25.07 — `get_advisors` прогнано: 84 попередження, 0 критичних помилок.
  Варте уваги і **досі не закрито:** `public_bucket_allows_listing` (бакет `community-photos`
  дозволяє перелічити всі файли) · `auth_leaked_password_protection` вимкнено.
  _(B-23 із того списку закрито 25.07.)_
- ~~**СИ-1**~~ 🔒 **ЗАКРИТО рішенням Вови (12.07) — не робимо.** PWA-іконки роздуті
  (`icon-192.png` 2.0МБ, `icon-512.png` 1.9МБ). Чисто перф, не баг — Вова свідомо відклав.

### 📰 СТРІЧКА — коментарі

- ~~СТ-1~~ ✅ 25.07 (PR #619/#621/#622) — лінія-гілка, згортання «Ще N», структура рядка,
  згадка «Віктор,», сповіщення, каскад «одна сітка».
- ~~СТ-2~~ ✅ 26.07 — живий тест на телефоні пройдено (Вова підтвердив).
- ~~СТ-3~~ ✅ 25.07 = B-23 — дірку `pcom update` закрито тригером-сторожем.
- **СТ-4** ⬜🟡 **Прибрати запобіжник `42703`**
  🔎 **Ревізія 26.07: підтверджено відкритим** — `src/core/supabase.js:1207` досі містить
  `error.code === '42703' || /reply_to_uid/.test(...)`, тобто зайву спробу запиту з колонкою
  й повтор без неї. Міграцію накатано 25.07 → запобіжник більше не потрібен.
  Не терміново, шкоди не робить. Дешевий пункт для чистки.
- 🆕 **СТ-5** ⬜🔴 **Живий тест сесії F** [ревізія 26.07] — з пісочниці недосяжно:
  (1) два пристрої: коментар/видалення — число має зійтися в обох; новий пост → пігулка
  «↑ N нових публікацій» у другого; (2) клавіатура «Стрічки»: плавність розширення листа,
  біла смуга під компоузером, три рівні свайпу шапки; (3) гілки: під коментарем видно ОДНУ
  відповідь, решта під «Ще N».
- 🆕 **СТ-6** ⬜🟡 **Міграція Чату й Обговорень на спільний `core/keyboard.js`** [з хендовера сесії F]
  — у них досі власна механіка (`chat-core.js`). = технічна частина О-7.
- 🆕 **СТ-7** ⬜🟢 **`CACHE_NAME` генерувати в CI** [з хендовера сесії F] — через ручний бамп
  конфлікт при кожному мерджі (`main` живе squash-комітами + автопарсери пушать щогодини).

### 🔴 Адмінка

- ~~**СИ-9**~~ ✅ **ЗАКРИТО 26.07** — створення спільнот з адмінки працює, живий тест Вови пройдено
  («Тест з створенням спільноти пройшов успішно»). Розділ «Спільноти» (`admin.html:772`):
  створення (назва/тема/аватар/банер), власник у `page_admins`, список, редагування,
  видалення з підтвердженням, порядок кружечків через `sort_order`.
  SQL — `scripts/supabase_pages_admin.sql`.
- ~~**СИ-10**~~ ✅ **ЗРОБЛЕНО** (PR #609) — `pages.sort_order` існує, хардкод `PINNED_PAGES`
  прибрано, порядок керується з адмінки.

### 🟢 Бізнес

- **СИ-6** ⬜🟢 Бізнес (pre-revenue): сторінка «/реклама» + прайс PDF + перші 3-5 безкоштовних кейсів.
  _(перетин з РОАДМАП Етап 6-7 — монетизація, МАРКЕТ/БАЗАР.)_

---

## 📊 АНАЛІТИКА ТА СТАТИСТИКА

- **АН-1** ✅**ЗРОБЛЕНО В ОСНОВНОМУ** (виявлено ревізією 26.07) — «продумати систему статистики»
  вже не «не почато»:
  - **Збір:** `logEvent` + `getAnonId` (`src/app.js:10`), схема `scripts/supabase_analytics.sql`.
  - **Показники в адмінці** (`admin.html:919-948`): унікальні за сьогодні / 7 днів / усього,
    кількість подій, встановлення PWA, розподіл по вкладках.
  - **AI-висновок:** Edge Function `ai-analytics-summary` — аналіз за 7 днів через Claude,
    запускається кнопкою (не автоматично — щоб не палити гроші).
  - **Профілі:** RPC `admin_profile_stats`.
  - ⬜ **Лишається рішення Вови:** які ще показники потрібні (утримання, глибина сесії,
    конверсія гість→акаунт) і чи потрібен зовнішній сервіс. Це вже уточнення, а не «з нуля».

---

## 📌 РОЗПОДІЛ ВКЛАДОК — архів

> До 18.07.2026 проєкт вели двоє (Вова + Рома). Рома вийшов 18.07 — **усі вкладки веде Вова**.
> Старий розподіл — `_archive/BOARD_two-owners_archive.md`. Мітки прибрано 23.07.
> ⚠️ Ревізія 26.07: у кількох пунктах лишались відсилання «обговорити з Ромою» / «уточнити в Роми» —
> знято, рішення тепер за Вовою (Н-13, Г-17).

---

## Довідка: Захід 2 (Д-1) — реалізовано, на проді (PR #267, 08.07)

- БД: `profiles +approved_count +trusted`; тригери `reputation_on_publish` (pending→published: +1,
  на 5 → `trusted`) і `revoke_trust_on_reject`; RPC `submit_board_post` (SECURITY DEFINER,
  рейт-ліміт 3/хв).
- Фронт: `supabase.js` submitPost→RPC; `community-modal.js` тост+подія; `account-ui.js` рядок довіри.
- Дефолти: відхилення = скидання з шансом заробити знову; антиспам = рейт-ліміт.
- SQL: `scripts/supabase_reputation.sql` + `supabase_reputation_hardening.sql` (закрито дірку —
  юзер не може сам виставити собі `trusted`).

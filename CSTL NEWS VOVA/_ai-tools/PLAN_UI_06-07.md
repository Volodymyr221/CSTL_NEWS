# ПЛАН UI-ДОРОБОК (06.07, зауваження Роми зі скрінів) — для /byyou

> 9 зауважень → 3 зони.
> **СТАТУС (06.07 вечір):** ✅ ЗАДЕПЛОЄНО (PR #216): 🅒 блок новин Громади (#9) · 🅐 «Шо в селі» жива стрічка (#1/#2/#3) · #4/#5 чистка Обговорень (`←`/`→`).
> 🔜 ЗАЛИШИЛОСЬ: **Потік 2** (Обговорення — винести discussions.js, dual-root board.js, FAB #6/#7/#8) + **концепт «Громада = рамка Шо в селі»** (див. нижче).

## 🆕 КОНЦЕПТ (Рома 06.07): «Громада = рамка "Шо в селі"»
Головна (Громада) — це і є відповідь «що зараз у селі»: greeting → фото села → заголовок **«Шо в селі»** → група віджетів (Дошка · Новини · Автобуси · Погода) як єдина відповідь. Це осмислена рамка, не просто дашборд.
- **Рішення Роми:** назву «Шо в селі» на ВКЛАДЦІ поки ЛИШИТИ (тимчасова co-existence вкладки-стрічки і концепту-рамки).
- **Майбутній крок:** додати заголовок «Шо в селі» над віджетами Громади + **ранжування віджетів (Г-4)** — інакше довгий скрол. Події+свята (теперішня вкладка) з часом можуть стати ВІДЖЕТОМ у рамці. Обговорити напрямок (A: рамка+віджет / B: окрема вкладка під ін. назвою).


---

## 🅐 ПОТІК «Шо в селі → жива стрічка» (зона Рома) — середній
**Мета:** з календарної афіші → у живу стрічку (à-la Instagram). Майбутнє (окремий V2-епік): акаунти + публікація від себе в стрічку.

**Кроки:**
1. `events.js` — прибрати календар (`renderCalendar`, `CALENDAR_DAYS`, `selectedDate`, `renderWeek…`) і фільтри (`renderFilters`, `CATEGORY_FILTERS`, `activeFilter`); спростити `getFiltered` (лишити сорт за датою, без дати/категорії).
2. `index.html` — прибрати `#events-calendar` + `#events-filters` з `#page-shotam`; **додати шапку «Шо в селі»** (іконка+заголовок, стиль `.bd-disc-head` як в Обговореннях). (#3)
3. `events.js cardHtml` — картка з **повноширинною обкладинкою** зверху (як featured/Instagram-пост), не мала мініатюра збоку. (#2)
4. `events.css` — прибрати мертві `.cal-*`/фільтр-стилі; стилі нової full-width картки + шапки; **переналаштувати офсет `#page-shotam`** (зараз 102px під фікс-фільтри) під нову шапку.
5. build + браузер-смоук + брама деплою.

**Пункти:** #1 (календар+фільтри геть), #2 (обкладинка на всю ширину), #3 (шапка).

---

## 🅑 ПОТІК 2 «Обговорення: окремий модуль + FAB + чистка» (зона Рома) — ВЕЛИКИЙ, board.js
**Мета:** розширення вже-запланованого Потоку 2 (винести `discussions.js` + прибрати dual-root board.js — F1/F2/F6) + рефактор FAB (F3) + чистка карток.

**Кроки:**
1. Винести `src/tabs/discussions.js` окремим модулем + спільне read-only сховище постів + **disc-namespace id** (`#disc-*`) → повністю прибрати `discOpen`/`getBoardRoot`/`openDiscussions`/`closeDiscussions` dual-root з `board.js` (F2/F6).
2. **FAB Обговорень (F3 + #6/#7/#8):** власний FAB (не спільний з Дошкою), **червоний `#722F37` з білим «+»**, помітніший; пункти **«Створити обговорення / Мої обговорення / Збережені»**; «Створити обговорення» = створити ТЕМУ (не `openBoardModal`-оголошення); «Збережені» лишається в Обговореннях (не `switchTab('board')`).
3. **Чистка картки/шапки (#4/#5):** прибрати `←` назад у шапці (головна сторінка вкладки); прибрати `→` вправо на картці обговорення (нічого не робить; ⚠️ не зачепити Дошку якщо спільна розмітка).
4. build + **важкий смоук** (Дошка: фільтр/пошук/FAB/оголошення/чат-модалка ЦІЛІ) + /audit + брама деплою.

**Пункти:** #4, #5, #6, #7, #8 + карта агента (F1/F2/F3/F6). Свіжий чат.

---

## 🅒 ПОТІК «Громада: скрол блока новин» (зона разом) — ДРІБНИЙ (можна /fix)
**Мета:** стрічка новин у Громаді скролиться повністю верх→низ, без червоної маски/обрізань.

**Кроки:**
1. `community.css` — знайти червону маску/градієнт/`padding-top` зверху `.cm-news-feed` (та обрізання знизу — `max-height`/`flex`/`padding-bottom`); прибрати; стрічка край-у-край. ⚠️ PR #208 уже пробував «прибрати маску за кнопками» — не спрацювало повністю, знайти справжню причину.
2. `community-blocks.js renderCommunityNews` — структура вкладеного скролу (якщо треба).
3. build + смоук + деплой.

**Пункти:** #9 (обрізана зверху + трохи знизу, червона маска).

---

## Порядок і залежності
- **Z** незалежний, швидкий → перший (видимий баг на головній).
- **A** незалежний (events.js) → другий.
- **2** найбільший (board.js хірургія) → окремим свіжим чатом, останнім.
- Групи A і 2 незалежні між собою; Z ні від чого не залежить.

## Майбутнє (не в цих потоках)
- Акаунти + публікація в стрічку «Шо в селі» (à-la Instagram) — великий V2-епік.

# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-26 18:58 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-5nmz43` |
| останній коміт людини | `fb558579` 26.08 18:58 — Merge remote-tracking branch 'origin/main' into claude/startuem-5nmz43 |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260826-1357` |
| останній PR у `main` | #1043 |
| стендів на диску | 111 |
| незакомічених файлів | 0 ✅ |
| журнал за 2026-08-26 | є ✅ |
| потік `/byyou` | idle — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `CSTL NEWS VOVA/NOW.md`
- `_session-log/vova-2026-08-26.md`
- `admin.html`
- `scripts/supabase_failures_resolved.sql`
- `tests/analytics-screen.mjs`

**Останні кроки (без автокомітів парсерів):**

- `fb558579` 26.08 18:58 — Merge remote-tracking branch 'origin/main' into claude/startuem-5nmz43
- `ddca1a17` 26.08 18:56 — Пульс: авто-блок після прогону
- `fa1be39c` 26.08 18:31 — Збої: живі й закриті — «полагоджено» перевіряється фактом, а не словом
- `aa42e5d7` 26.08 16:00 — Порівняння періодів і журнал AI-висновків: розділено «зміну» і «сигнал» (#1043)
- `677f0342` 26.08 15:59 — Пульс: повернуто розділ каруселі, зниклий при стисканні

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

📦 **26.08. Уся робота в `main` і на сайті** (деплой #5133). `CACHE_NAME` — див. авто-блок.

🔴 **ГОЛОВНЕ ЗА ДЕНЬ — АНАЛІТИКА, ТРИ ШАРИ.** Вранці полагоджено ДАНІ (стеля 1000: «203
за 7 днів» це **6 людей**), увечері переписано ЕКРАН, пізно ввечері — модалка й AI-агент.
Дві тихі вади дорогою: **кожен захід писався «гостем»** і **AI коментував неправдиві
числа**. Розбір нижче.

🟢 **Відкритих критичних немає.** 🔴 **«Блим» при виході свайпом НЕ вилікувано** — Вова:
«хай буде так». ✅ **Названий борг каруселі закрито.**

🔀 **№13:** злиття ДО роботи · ніколи `-X theirs` · після КОЖНОГО злиття шукай маркери
конфлікту ПЕРЕД `git add` · 🛑 `node --check` НЕ ловить відсутній імпорт.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

### 26.08 НІЧ: ЗБОЇ — ЖИВІ Й ЗАКРИТІ

🗣️ «оці збої висять, ми їх виправили».
🔴 Журнал показував усе за 30 днів і не відрізняв «болить зараз» від «полагоджено три дні
тому». Коли в списку висять мертві рядки, око перестає їх читати — **і живий теж не
побачить**. Журнал, який завжди червоний, дорівнює журналу, якого немає.
🔑 **Позначка — не видалення й не обіцянка.** Записуємо момент, далі вирішує ФАКТ: не
траплялось після позначки → згорнутий розділ «Полагоджені»; трапилось хоч раз → **сам
повертається нагору** з «⚠️ повернувся після виправлення», і такі рядки йдуть першими.
➡️ Фікс перевіряється тим, що поломка більше не приходить, а не тим, що ми так сказали.
📐 Перевірено транзакцією: позначили → 14→13 активних · зняли → 14 · підсунули подію
після позначки → рядок піднявся першим, `regressed: true`.
⚠️ Перша перевірка дала хибний нуль: **у транзакції `now()` однаковий для всіх операторів**,
тож подія «зараз» була РІВНА часу позначки при строгій умові `>`.
🛑 `analytics_events` не чіпаємо — позначка окремою таблицею. Політики `insert` для
клієнта немає: автора й пошту бере сервер із JWT.
📋 Копійований текст теж розділено: інакше наступна сесія лагодила б уже полагоджене.
🧪 `analytics-screen.mjs` 95 → **106**.

### 26.08 ВЕЧІР І НІЧ: АНАЛІТИКА — ЕКРАН, МОДАЛКА, AI, ПОРІВНЯННЯ

🗣️ Джерело правди — **голосовий текст Вови**, не концепція ChatGPT.
🔴 **ТРИ ТИХІ ВАДИ:** (1) **кожен захід писався «гостем»** — `initAuth()` без `await`, і в
УСІХ 16 подій `session_start` `user_id` порожній → «унікальні користувачі» показували б
нуль назавжди; (2) **AI коментував неправдиві числа** — рахував агрегати сам тим самим
хибним способом; (3) **висновок зникав** при поверненні, а виклик коштує грошей.

**Екран:** один період керує всім · тап по числу → **хто саме** · три різні картки замість
суми · збої поштучно · пояснено, звідки вік. Вікно стало **модалкою** (фон не рухається,
свайп закриває — порт `attachSheetDismiss`).
🔑 **ПОРІВНЯННЯ ПЕРІОДІВ: розділили «зміну» і «сигнал».** «5 людей стало 8» = +60%:
бездоганно математично й **порожньо** на десятку тестових акаунтів. 🛑 **N/A ≠ 0:** заходи
є лише з 26.08 → `previous: null`. Від нуля відсоток не рахуємо. Відсоток на екрані —
ЛИШЕ при сигналі «помітно».
🔑 Якість даних — **окремим полем**: здогад можна не зробити, поле не можна не побачити.
Моделі дається її **попередній висновок**.
💾 **Журнал `analytics_ai_reports`:** `snapshot` головна колонка — без чисел висновок не
перевірити. Політики `insert` для клієнта НЕМАЄ.
❌ **Відхилено з розбору ChatGPT:** `analytics_daily`, retention, 4-тижневий baseline.
🧪 **106/106**, контроль **66/95** (до збоїв). 📖 `AUDIT_ANALYTICS_2026-08.md` ч.2.

### 26.08: НАЗВАНИЙ БОРГ ЗАКРИТО — ОДНА КАРУСЕЛЬ НА ВЕСЬ ЗАСТОСУНОК

Карусель новин переведена на спільний `core/auto-carousel.js`. 🛑 Дві копії одного
механізму тут уже розходились **двічі** (антиспам, шкала автобуса) — і не одразу, а в
мить, коли одну полагодять.
📐 Звірено рядок у рядок: прокрутку робить БРАУЗЕР, слайд рахується за РЕАЛЬНИМ
положенням. 🔑 Пара `_newsTimer`/`_newsIO` → **один зупинювач**. 🗑 З шапки прибрано
«новини ще НЕ переведені» — **обґрунтування пережило свою підставу**.
🧪 `carousel-shared.mjs` **15/15** — міряє НАСЛІДОК: доріжка 0→342px, крапка 0→1.
⚠️ Модуль спить поза екраном, тож блок спершу доводиться у вікно і це перевіряється
ОКРЕМО. Контроль **10/15**.

### 26.08 РАНОК: АНАЛІТИКА — ЦИФРИ СТАЛИ ПРАВДИВИМИ · ВІДЖЕТ СТРІЧКИ (#1032-#1036)

🔴 Усі числа були обрізані стелею 1000 (`db-max-rows`). 🔑 **Доказ без бази:**
544+164+134+82+76 = **рівно 1000**. 📐 Правда: 7 днів **6 людей** (казав 203), весь час
**11** (казав 35); 736 «унікальних» = 11 акаунтів + 725 пристроїв, 628 з однією подією.
Агрегати рахує БАЗА · `user_id`/`anon_id`/`session_id` · події 4→8 · година за Києвом ·
`discussions` — НЕ стара вкладка. **Збої:** імена акаунтів + копіювання одним натиском.
📖 `AUDIT_ANALYTICS_2026-08.md`

**Віджет Стрічки** (шість редакцій): порядок спільнот за свіжістю · ряд на всю ширину ·
картка 280→400px · **тап веде в САМУ спільноту**. 🛑 **Уроки:** у flex-колонці все, що має
показатись ЦІЛКОМ, потребує `flex-shrink: 0` (текст різала ВИСОТА, не `line-clamp`) ·
висоту тексту тільки в `em` · знімок довантажується ПІСЛЯ прокрутки · 🔴 **«блим» НЕ
вилікувано** · **обґрунтування переживає свою підставу**.
🔴 **СІМ ПОМИЛОК ВИМІРУ** — жодну не знайшло написання. **39/39.** 📖 `-25e`, `-26`.

### 25.08 і раніше (#1001-#1030)

🔴 **Заслінка:** «з компʼютера код не підходить» — код був ПРАВИЛЬНИЙ: сторінка по
`http://`, а `crypto.subtle` є лише в захищеному контексті. 🛑 Вада не в замку, а в тому,
що він КАЗАВ. 🔴 **Пункт 0:** `initAuth()` без `await` → вкладки малювали вигляд ГОСТЯ.
⚠️ **Той самий корінь, що у вечірньої вади аналітики** — клас повертався двічі.
📖 `SESSION_STATE_VOVA.md`, `-23b`…`-25e`.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| `analytics_daily` | борг **з умовою** | робити при >1 млн подій **або** якщо чистимо історію; 11 тис. рахуються на льоту |
| Retention D1/D7/D30 · 4-тижневий baseline | після запуску | рахуються з сирих подій заднім числом |
| Віджет Стрічки не бачений пальцем | ✅ борг каруселі закрито | скрін не долетів |
| Тип 4: перше спрацювання | **27.08** | питанню буде 21 год при потрібних 24 |
| `CSTL_BUGS.md` = 40.5 KB при стелі 40 | запасу 0.5 KB | наступний баг — стисло |
| Стеля «5 push із Питань на добу» (§12) | **в коді немає** | заміряно по 6 |
| **Android не тестувався ВЗАГАЛІ** | 📌 відкладено Вовою | «протестуємо коли доробимо все» |
| Класифікатор новин (**B-28**) | відкрито | ~94% статей → «Суспільство» |
| ПД в ІСТОРІЇ git · тестовий збір на проді | не стерто / у базі | перезапис історії ламає посилання |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

✅ **Перевірено пальцем 24-26.08:** новини · перемикання акаунта · видалення коментаря ·
«Вийти» · push · сортування · **вимикачі (B-33)** · віджет Стрічки.

0. 🔴 **НЕ ПЕРЕВІРЕНЕ — гарантія «хто я» (холодний старт, PWA):** крапка непрочитаного
   ОДРАЗУ · push веде в чат · збережені позначені · ❤️ зафарбоване · привітання з ІМЕНЕМ ·
   ⚠️ **Автобуси малюють розклад МИТТЄВО**. 🔴 Чи відкривається `castlelife.org` — з
   контейнера не перевірити.
1. 🔴 **Тип 4 — 27.08.** ⚠️ У базі ОДНА push-підписка на всіх, автор питання випадає.
2-6. **Нічна тиша · антиспам · непрочитане · редагування питання · голос спільноти** —
   сценарії в `-25b/-25c` і `SESSION_STATE_VOVA.md`.
7-9. Google-вхід · ПД в історії git · ключ `cstl-olyka-agent` відкликати.
📌 **Android** — «протестуємо коли доробимо все».

## ➡️ ДАЛІ

🔴 **ПЕРШЕ — аналітику пальцем, залогіненим.** До 26.08 жоден захід не мав акаунта, тож
«Людей» покаже правду лише з нових заходів. Ключ Anthropic Вова вже поклав.
✅ **Пункт 0 закрито 25.08 (#1029) — не бери його вдруге.**
**Далі:** 💬 чат Дошки · 📐 відступ Громади · 🚌 капсули проти автобусів. _(Android — у кінці.)_
❓ **Не дописане Вовою:** «щотижневий AI-аналіз динаміки» — усе для нього вже є.

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** · коди заслінки в репозиторій НЕ писати · знімки
  `tests/tools/_out/` НЕ комітити · 🔴 `verify_jwt` у `send-bus-push` — УВІМКНЕНИЙ.
- 🔴 **TEXT проти `uuid`:** `reactions.user_id`, `push_subscriptions.user_uuid` — TEXT;
  перед `.in('uid', …)` **обовʼязковий фільтр форми UUID** — один сторонній рядок глушить
  УСІ сповіщення теми. · `upsert` без `.select()` · SELECT-політика на свої рядки ·
  **кирилиця в іменах змінних bash** = код 127 · `@media` погоди — у КІНЦІ `home.css`.
- 🔴 **Клавіатура на iOS** (`feed.css:630,971`) · **`main` напряму не чіпати** · дампи БД
  не класти (репо ПУБЛІЧНИЙ) · `serviceWorker.ready` без SW не виконується НІКОЛИ ·
  `pg_cron` живе в UTC · **у транзакції `now()` однаковий для всіх операторів**.
- 🛑 **НЕ МІРЯЙ ПОСЕРЕД РОБОТИ** · «Unverified» — очікувано · `git diff` «що не в main» —
  на ДВІ крапки · **стелю бюджету в `docs-fresh` НЕ піднімати**.
- 🔑 Решта — `HOT_RULES.md`, `CSTL_BUGS.md`.

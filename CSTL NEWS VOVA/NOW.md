# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-17 18:21 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-f07n62` |
| останній коміт людини | `8b1fcb8a` 17.08 18:21 — docs: капсула ПОВІДОМЛЕННЯ і шкала важливості — літопис, пульс, CLAUDE.md |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260817-1807` |
| останній PR у `main` | #946 |
| стендів на диску | 83 |
| незакомічених файлів | 0 ✅ |
| журнал за 2026-08-17 | є ✅ |
| потік `/byyou` | done — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `CSTL NEWS VOVA/CLAUDE.md`
- `CSTL NEWS VOVA/NOW.md`
- `CSTL NEWS VOVA/_ai-tools/SESSION_STATE_VOVA.md`
- `_session-log/vova-2026-08-17b.md`
- `bundle.js`
- `src/core/supabase.js`
- `src/tabs/board-chat.js`
- `src/tabs/home-caps.js`
- `sw.js`
- `tests/home-caps.mjs`

**Останні кроки (без автокомітів парсерів):**

- `8b1fcb8a` 17.08 18:21 — docs: капсула ПОВІДОМЛЕННЯ і шкала важливості — літопис, пульс, CLAUDE.md
- `4092704c` 17.08 18:18 — feat(home): капсула ПОВІДОМЛЕННЯ + ранжування капсул балами
- `44a582c8` 17.08 17:26 — docs(session-log): журнал сесії 17.08 (B) — старт /startuem
- `7d464d47` 17.08 17:23 — docs: передача в новий чат — 17.08.2026 (#946)
- `fb6baaf5` 17.08 16:33 — fix(home): капсула ЗАРАЗ рахує рейс від СВОЄЇ зупинки, а не від Луцька (#945)

<!-- AUTO:END -->---

## 🎯 ЗАРАЗ

🔴 **ЧЕКАЄ ДВОХ ДІЙ ВОВИ.**
1. **Накатати міграцію** `scripts/supabase_reject_reason.sql` (Supabase → SQL
   Editor → Run). Поки не накатана — модерація працює, але причина відхилення не
   зберігається, і адмінка каже це тостом прямо.
2. **Жива перевірка капсул:** закрити застосунок повністю (новий SW,
   `CACHE_NAME cstl-20260817-2104`), у шапці має бути число **більше за v4644**.
🔑 Капсула повідомлень зʼявиться, лише коли є **непрочитана розмова**.

Капсули працюють за **чотирма ролями**, слотів **три**, порядок задає **бал
важливості**, порожня роль не малюється, ротації немає, тап веде в конкретний
обʼєкт. 🔴 **17.08 (пізній вечір) — ДВА РЯДКИ ПОДІЛИЛИ РОБОТУ:** мітка несе СТАН і
КОНТЕКСТ, значення — конкретику (скарга Вови «неясно, що це за повідомлення, з
якого оголошення»):

| роль | мітка | значення |
|---|---|---|
| **МОЄ** | `МОЄ ОГОЛОШЕННЯ · ВІДХИЛЕНО` | причина відхилення, інакше назва |
| **ЗАРАЗ** | `ЗАРАЗ · ЧЕРЕЗ 40 ХВ` | `Олика → Жорнище · о 21:15` |
| **НОВЕ** | `ПИТАННЯ · ПОТРІБНА ВІДПОВІДЬ` або `НОВЕ НА ДОШЦІ · ОЛИКА` | саме питання / назва оголошення |
| **ПОВІДОМЛЕННЯ** | `ПОВІДОМЛЕННЯ · ПЛУГ МТЗ` (або `· ЗАВЕРШЕНО`) | `Іван: Здоров` |

📐 **Бюджет рядка заміряний, а не вгаданий:** значення тримає **25** символів на
390pt і лише **17** на 320pt; мітка — 30 і 21. Тому стеля НЕ вписана числом
символів: рядок збирає `fitLine()` (`core/utils.js`), міряючи живі пікселі —
частина, що не влазить, **викидається цілком**, обрізка лише **по межі слова**.
🛑 Випадку «оголошення знято» не буває: `threads.post_id … on delete cascade`
забирає розмову разом з оголошенням. Реальний стан предмета — `closed`.

⚖️ **Шкала — одна фраза: «чим швидше рядок втрачає сенс, тим вище».** Рейс ≤15 хв
**100** · повідомлення ≤1 год **90** · відхилене оголошення **80** · повідомлення
**70** · рейс 16-45 хв **60** · на модерації **45** · питання без відповіді **35** ·
рейс 46-120 хв **30** · нові оголошення **20**. Автобус стоїть і найвище, і
найнижче — це та сама міра, застосована чесно.

⚠️ **Одна капсула на екрані — очікуваний стан, а не збій.** Позначка «востаннє був
на Дошці» ставиться при першому запуску і навмисно віддає нуль.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

_17.08 — розділ «Збори» і перебудова капсул._

**Збори:** переїзд із файлу в Supabase, розділ у бургер-меню, форма заявки для
жителів, розділ «Збори» в адмінці, модалка збору, картка без фото більше не
тримає порожнечі. PR **#940 · #941 · #942 · #943**.
🔑 Житель подає заявку → Вова бачить її з телефоном → дзвонить → «Схвалити»
однією кнопкою створює збір. Позначка «перевірено» ставиться ОКРЕМО.
🔴 Зібраної суми на картці немає — рішення Вови 16.08.

**Капсули Громади** (PR **#944**, деплой **v4485**): три ролі замість трьох
розділів, ротацію 5.2 с знято, Q&A повернулось як ЗАКЛИК («потрібна відповідь»),
а не як лічильник питань. Новий сторож `home-caps` — **45/45**.

**Капсула «ПОВІДОМЛЕННЯ» + ранжування балами** (PR **#947**): замовлення Вови зі
знімка v4637 — «надійшло повідомлення, чому немає капсули про це? Треба зробити
ранжування капсул по важливості». Роль каже **імʼя і перші слова**, числа немає
(воно вже на FAB, у меню і на кнопці кабінету); мережі не додалось ні на один
запит — зліпок рахує той самий виклик, що малює бейджі.
🛑 Це **скасування мого власного рішення** того ж ранку («лічильника не буде»):
аргумент був чинний для ЧИСЛА, але червона крапка вкладки не каже НІ ВІД КОГО,
НІ ПРО ЩО. Сторож `home-caps` **45 → 59**, контроль **51/59**.
🔴 **Інцидент:** «контроль» спершу дав 59/59 — бо стенд `BUNDLE_REV` не підтримував
узагалі, тобто обидва прогони йшли по тому самому коду. Контроль, який не може
впасти, — не контроль.

**Виправлення по знімку Вови** (PR **#945**, деплой **v4496**): капсула показувала
«Рівне · через 37 хв» — рейс, у маршруті якого **Олики немає взагалі**, з відліком
до виїзду з Луцька. Заміряно: **жоден із 25 рейсів дня в Олиці не починається**,
через неї проходять на 50-99 хв пізніше за виїзд.

🛑 **Головний урок дня, тричі поспіль той самий:** заглушка в стенді мусить
відтворювати **форму живих даних**, а не зручну форму. Спіймано на полі
`carrier`, на `.maybeSingle()` (віддавав масив замість обʼєкта) і на геометрії
маршруту. Плюс: `if (вузол) { перевірки }` у стенді — тиха втрата покриття
(`a11y-prefers` показував 3/4, перевіряючи майже нічого; тепер **9/9**).
Плюс: контроль через `git checkout` безпечний лише для ЗАКОМІЧЕНОЇ роботи —
незакомічене ховай і повертай через `stash push` / `stash pop`.

**Контекст у капсулах + вміщення рядка** (17.08, пізній вечір): скарга Вови зі
знімка **v4644** — «неясно, що це за повідомлення, з якого оголошення… має влазити
хоча б певна інформація, а не обрізатися на половині». Мітка забрала стан і
контекст, значення отримало цілий рядок; автобус дістав час посадки. Заведено
`fitLine()` — вміщення ПІКСЕЛЯМИ. Причина відхилення пройшла повний ланцюг:
адмінка (одна функція на три кнопки) → база → капсула → картка «Мої оголошення».
Сторож `home-caps` **59 → 70**, контроль **56/70**; нова сцена на 320/375/390/430pt.
🔴 **Інцидент:** перший вимір дав кожній капсулі «О…» — міряв ширину КОРОТКОГО
зразка, а `flex` стискається під вміст. Спіймав стенд (17 провалів). Урок: коли
правильна логіка дає абсурд — підозрюй мірку, не логіку.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| 🔴 **Міграція `posts.reject_reason`** | **НЕ НАКАТАНА** | `scripts/supabase_reject_reason.sql` — запис у бойову базу з сесії Claude заблокував запобіжник середовища. Вові: Supabase → SQL Editor → Run. До накату причина відхилення не зберігається (адмінка каже тостом) |
| Відхилене оголошення не можна редагувати | знайдено, **не лікували** | `scripts/supabase_board_edit.sql`: правити можна лише `active`/`pending`. Тобто причину житель тепер бачить, а виправити й подати знову — не може. Окрема робота, потребує рішення Вови |
| Сторож на свайп модалки статті | **немає** | доводив разовим приладом; стенда на `#article-modal` нема — тримається на збірці й `motion` |
| Перетікання тла вкладки | знайдено, **не лікували** | `.app-main` має `transition: background-color 0.3s`, фони вкладок різні — 18 кадрів ≈300мс. 🔴 **15.08 ЦЕЙ КЛАС УЖЕ ВКУСИВ:** саме він давав «блимання» після сповіщення. Там прибрано зайве перемикання, але САМ перехід між вкладками лишається |
| **Android не тестувався ВЗАГАЛІ** | 0 перевірок | усе живе тестування — iPhone Вови. Перед запуском у громаді це діра |
| Тестовий збір **на проді** | у базі, не у файлі | прибрати або замінити реальним: адмінка → «Збори» → «Видалити» |
| Класифікатор новин (**B-28**) | відкрито | 94.5% статей → «Суспільство»; блокує тематичний фільтр B-14. Корінь у `scripts/parse_rss.py` |
| Юрист на правові тексти | відкрито | єдине, чого закрити не можу |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

1. 🔴 **Знімаємо заслінку?** `DEV_LOCK = true` — один рядок. Це єдине, що
   відділяє нас від Етапу 3 (запуск у громаді). Рішення твоє.
2. **Іконка «Стрічка» — форма остаточна?** Замовлення звучало «змінити і
   **зафіксувати нарешті**»; зроблено сітку 2×2 (варіант 4 з макета).
3. **Стенд `ad-report`** упав на CI 15.08, локально 16/16, моїми змінами не
   чіпався. Схоже на нестабільність раннера — глянути, якщо повториться.
4. 🔴 **Чекають перевірки пальцем на iPhone.** (а) Сповіщення: чат → назад →
   Повідомлення → назад → Громада, без плями. (б) Деплой #4285: банер автобусів
   має рушати з-під пальця одразу · картка Дошки на Громаді не лишатись
   збільшеною · модалка статті — закриватись коротким кидком. ⚠️ Сторожа на
   свайп статті **немає** — та перевірка тримається на тобі.

## ➡️ ДАЛІ

**Черга Вови** (`_ai-tools/BACKLOG_VOVA_2026-08.md`, порядок задав 05.08):

1. ✅ **Віджет і розділ зборів — ЗРОБЛЕНО** (16-17.08): макет, переїзд у базу,
   заявки від жителів, адмінка. Лишилось замінити тестовий збір реальним.
2. 💬 Чат Дошки — теплота `#F3EFE3` / `#FCFAF4`
3. 📐 Спадковий відступ контейнера Громади
4. 🚌 Капсули проти віджета автобусів

**Плани руху** (`plans/`) — ✅ усі три закриті 15.08; лишилась жива перевірка на
iPhone (див. питання 4).

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) — застосунок навмисно закритий.
  Знімати не можна: у деяких людей є доступ.
- 🔴 **Клавіатура на iOS** (`style/feed.css:630,971`, `transition: height`) — два
  фікси там уже провалились (PR #638, #641).
- 🔴 **Три різні механізми закриття** (`#article-modal` · `core/modal.js` ·
  `core/layers.js`) — плутати не можна.
- 🔴 **`main` напряму не чіпати** — тільки через PR.
- ⚠️ **Дампи БД у репозиторій не класти** — репозиторій ПУБЛІЧНИЙ.
- ⚠️ **«Unverified» на GitHub — очікувано, ігнорувати.**
- ⚠️ **`git diff` для «що не в main» — на ДВІ крапки.** На три після squash-мерджа
  показує роботу, яка вже в `main`.
- ⚠️ **Не піднімати стелю бюджету читання** в `tests/docs-fresh.mjs` — вікно
  контексту від цього не росте.

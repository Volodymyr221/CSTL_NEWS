# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-15 19:29 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-6v6car` |
| останній коміт людини | `ac992ffb` 15.08 16:17 — «Назад» зі сповіщення більше не вивалює в порожню вкладку (#929) |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260815-1928` |
| останній PR у `main` | #929 |
| стендів на диску | 80 |
| незакомічених файлів | 6 ⚠️ |
| журнал за 2026-08-15 | є ✅ |
| потік `/byyou` | done — 15.08 — «Свіжа документація на переході: новий чат / другий акаунт» |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- ✅ нічого. Уся робота гілки вже в `main`.

**Останні кроки (без автокомітів парсерів):**

- `ac992ffb` 15.08 16:17 — «Назад» зі сповіщення більше не вивалює в порожню вкладку (#929)
- `4312a3df` 15.08 15:34 — Сповіщення: «назад» веде в Повідомлення, підсвітка не згоряє під заставкою (#928)
- `0133694a` 15.08 14:53 — Сповіщення про чат більше не веде на головну (#927)
- `f400ad18` 15.08 14:09 — docs(now): пульс під деплой #4309 (#926)
- `6b941fce` 15.08 14:07 — Edge Functions накатано на Supabase + пульс і журнал (#925)

<!-- AUTO:END -->---

## 🎯 ЗАРАЗ

**Нічого в роботі.** 15.08 було ДВІ паралельні сесії (Вова працював на двох
акаунтах і попередив прямо) — обидві закриті й задеплоєні. ⚠️ Обидві завели журнал
з тим самим імʼям; другий → `_session-log/vova-2026-08-15b.md`. **Перед роботою
звіряйся з `main`, а не з памʼяттю** — цього дня ваду вже лагодили двічі.

🔑 **Спостереження дня: вади виявлялись НЕДОЛІКОВАНИМИ СТАРИМИ.** І лист
коментарів, і блимання Вова описував ще 26-27.07 — тоді полагодили по одному
місцю, решту лишили. Це спосіб, у який вади тут виживають.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

**Три плани руху** `plans/002 → 003 → 001` — закриті (PR #918). 🔴 Урок: **прилад
мусить показувати вимір, а не вирок** — двічі завалював справний код.

**5. 🔴 ЛИСТ КОМЕНТАРІВ САМОВІЛЬНО ЗГОРТАВСЯ** (PR #920). `blur` поля стискав лист
зі 100% у 82% (**≈150px**), кнопки списку самі забирали фокус → під пальцем
опинявся задній шар. Два рубежі: `attachBackdropClose()` (усі 4 листи) +
`keepFocusOnButtons()`. Сторож **7/7**, контроль 5/7.

**4. 🎨 ІКОНКА «СТРІЧКА» — СІТКА 2×2** (PR #917, «4 варіант реалізуй»). Сторона
7.125 · просвіт 1.75 = товщина лінії · площа 315. 🔴 **Урок: `getBBox()` НЕ рахує
товщину лінії** — перша спроба вийшла на 20% дрібнішою, і сторож був би зелений.
⚠️ Моє заперечення (2×2 читається як «усі застосунки») відхилене власником.

**6. 🔴 ПОВЕРНЕННЯ НА ВКЛАДКУ БІЛЬШЕ НЕ БЛИМАЄ** (PR #921 + #922, деплой #4299).
Повна заміна `innerHTML` = **5 кадрів без жодної фотографії**, стало **0**. 🔑 Причина —
**відносний час** у картці («5 хв» → «6 хв») міняється сам, тож порівняння «список
цілком» спрацьовувало щоразу. Лікує `patchList()` + `adoptLoadedImages()`.
🛑 Перенос `<img>` поза документом **скидає `complete`** — порядок критичний.

**7. 🔴 FAB: три скарги закрито** (PR #923 + #924). Накладалась на «Громаду»
(**52×10px** → `--fab-lift: 16px`) · іконка була **догори ногами** (оберт на ПІВ
круга → 360°) · 🔴 **порожній FAB у «Питаннях»**: `#board-trigger` спільний на дві
зони, але в «Питань» лише плюс, а `.has-unread` від ГЛОБАЛЬНОГО числа ховав його.
Сторожі: `fab-hint` **19/19**, `fab-visible` **5/5** (контроль — «видно 0 іконок»).

**8. 🔴 СПОВІЩЕННЯ — ТРИ ЗАХОДИ ЗА ДЕНЬ, ОДНА ТЕМА** (PR #924 · #925 · #927 + цей).
Замовлення: тап по сповіщенню має вести **до самої дії**, а не «просто в застосунок».

**(а) Маршрути.** Зведене «Ще N коментарів» вело лише до поста (`?c=all`), чат при
ВІДКРИТОМУ застосунку не працював зовсім. ✅ **EDGE FUNCTIONS НАКАТАНО:**
`send-chat-push` **v5**, `send-comment-push` **v3**. 🛑 `verify_jwt` повторено з
задеплоєних версій один в один (`comment-push` — **false**: його кличе БАЗА).

**(б) Чат вів на головну.** **Перегони з відновленням входу:** хеш зʼїдався на
**~465мс**, вхід приїжджав на **~500мс** → `openThreadById()` виходив на
`if (!isLoggedIn()) return` (`init()` не чекає на `initAuth()`). Коментарі
працювали, бо `focusFeedPost()` входу не потребує. Лікує **відкладений намір**
(15 с). Побічно: чат відкривався **ДВІЧІ**.

**(в) «Назад» у порожнечу + згоріла підсвітка.** `#splash` живе **3.9с**, deep-link
спрацьовував за **~0.5с** — дія відбувалась ПІД заставкою, а підсвітка згоряла
непобаченою. Лікує `core/splash.js` + під розмову кладеться «Повідомлення».

**(г) Порожня вкладка Safari.** Третій «назад» давав `url: about:blank` —
сповіщення відкриває застосунок у СВІЖІЙ вкладці, позаду порожньо. Лікує
`guardAppRoot()` (`core/layers.js`): у сеансі зі сповіщення корінь **поглинає
«назад»**. 🛑 Тільки для сповіщень. 🔴 **Моя помилка, яку впіймав прилад:**
запобіжник стояв ПЕРЕД `replaceState`, той його затирав і лишав хеш → «назад»
відкривало чат ЗАНОВО. ➡️ `replaceState` міняє ПОТОЧНИЙ запис.

**(д) Кінець шляху — Громада + справжнє джерело «блимання».** Прибрано
`switchTab('board')`: вкладка й так `community`. 🔴 **Заміряно, що воно давало
ВИДИМУ ПЛЯМУ:** `.app-main` має `transition: background-color 0.3s`, фони різні
(Громада `#1A0A0E` ↔ Дошка `#E6E6E3`), а перемикання йшло ПІСЛЯ заставки — тло
повзло з темного у світле на очах. **18 значень тла за прохід → 1.** Заразом:
при вже відкритому застосунку воно **викрадало** людину з її вкладки.
🛑 **УРОК ПРО МІРКУ:** перший прилад показав тут НУЛЬ і «спростував» гіпотезу —
бо починав знімати ПІСЛЯ заставки, коли перетікання вже скінчилось. Прилад був
чесний, замале було **вікно спостереження**. ➡️ Міряєш плавність — починай
знімати ДО події.

✅ **АУДИТ СЕРВЕРА (жива база):** накопичення, яке просив Вова, **вже працює** —
тригер + `cron` `flush-comment-push` (`* * * * *`) + вікно тиші **10 хв** однакове
в коді й у БД. «Тобі відповіли» — одразу; «коментар під дописом» — перший одразу,
далі зведене. Приватний чат агрегації не потребує (`tag` замінює попереднє).

🛑 **УРОК ДНЯ №2: зелений ТЕКСТОВИЙ сторож стояв над зламаною поведінкою.**
`notif-deeplink` 12/12 був правий — ламався ПОРЯДОК У ЧАСІ. ➡️ «Має відкритись»
перевіряй ВІДКРИТТЯМ. Браузерні: `coldstart` **9/9** (контроль 3/9),
`backstack` **18/18** (контроль **14/18**).

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| ~~PR #916~~ | ✅ **ЗЛИТО в `main` 15.08** (коміт `2c98ab7d`) | нічого не лишилось |
| ~~`plans/001-003`~~ | ✅ **усі три виконані й злиті 15.08** | лишається жива перевірка на iPhone |
| Сторож на свайп модалки статті | **немає** | доводив разовим приладом; стенда на `#article-modal` нема — тримається на збірці й `motion` |
| `BYYOU_PLAN.md` | статус ✅ (`done`), **архівування лишилось** | переносити файл з-під можливо ще відкритої сесії — колізія з HOT_RULES №11. Чекає підтвердження, що другий акаунт закрито |
| **Потік Б** (окрема сесія) | не почато | розрізати `SESSION_STATE_VOVA.md` (807 KB), звірити `RODMAP.md`, прибрати зі `START_HERE.md` блоки «попередній стан». Чіпає історію — окремим дозволом |
| Перетікання тла вкладки | знайдено, **не лікували** | `.app-main` має `transition: background-color 0.3s`, фони вкладок різні — заміряно 18 кадрів ≈300мс, коли під готовим вмістом повзе чуже тло. Ефект реальний, але це НЕ те, на що скаржився Вова |
| Юрист на правові тексти | відкрито | єдине, чого закрити не можу |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

1. **Потік Б — коли?** Без нього `SESSION_STATE_VOVA.md` лишається 807 KB і новий
   чат далі не зможе його дочитати. Це найбільший лишок.
2. **Стенд `ad-report`** упав на CI 15.08, локально 16/16, моїми змінами не
   чіпався. Схоже на нестабільність раннера — глянути, якщо повториться.
3. **Іконка «Стрічка»** ✅ закрито 15.08 (сітка 2×2). Питання одне — **чи форма
   остаточна**, бо замовлення звучало «змінити і **зафіксувати нарешті**».
4. ✅ **`BYYOU_PLAN.md` — статус виправлено 15.08** (`paused` → `done`): потік
   справді в `main` (PR #916). 🛑 **Лишилось архівування** в `_ai-tools/_archive/` —
   переносити файл з-під можливо ще відкритої сесії це колізія з HOT_RULES №11.
   Зробити, **щойно Вова підтвердить, що другий акаунт закрито**.
5. 🔴 **Три рухи чекають перевірки пальцем на iPhone** (деплой #4285 на сайті):
   банер автобусів має рушати з-під пальця одразу · картка Дошки на Громаді не
   лишатись збільшеною після тапу · модалка статті — закриватись коротким кидком.
   ⚠️ Сторожа на свайп статті **немає**, ця перевірка тримається на Вові.

## ➡️ ДАЛІ

**Черга Вови** (`_ai-tools/BACKLOG_VOVA_2026-08.md`, порядок задав 05.08):

1. 🎯 **Віджет зборів** (військові й загальні) на «Громаді» — ПЕРШИЙ У ЧЕРЗІ.
   ⚠️ `src/tabs/home-fund.js` уже вміє більшість — **не будувати з нуля**.
   ⚠️ У `data/fundraisers.json` лежить **ТЕСТОВИЙ запис і він на проді**.
2. 💬 Чат Дошки — теплота `#F3EFE3` / `#FCFAF4`
3. 📐 Спадковий відступ контейнера Громади
4. 🚌 Капсули проти віджета автобусів

**Плани руху** (`plans/`) — ✅ **усі три закриті 15.08**, лишилась жива перевірка
на iPhone: банер автобусів має рушати з-під пальця одразу, а картка Дошки на
Громаді не лишатись збільшеною після тапу. Модалку статті тепер можна закрити
швидким коротким кидком і тягнути за всю верхню смугу, а не лише за рисочку.

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) — застосунок навмисно закритий.
  Знімати не можна: у деяких людей є доступ.
- 🔴 **Клавіатура на iOS** (`style/feed.css:630,971`, `transition: height`) — два
  фікси там уже провалились (PR #638, #641).
- 🔴 **Три різні механізми закриття** (`#article-modal` · `core/modal.js` ·
  `core/layers.js`) — плутати не можна.
- 🔴 **`main` напряму не чіпати** — тільки через PR.
- ⚠️ **Дампи БД у репозиторій не класти** — репозиторій ПУБЛІЧНИЙ.
- ⚠️ **«Unverified» на GitHub — очікувано, ігнорувати.**
- ⚠️ **`git diff` для «що не в main» — на ДВІ крапки.** На три після squash-мерджа
  показує роботу, яка вже в `main`.
- ⚠️ **Не піднімати стелю бюджету читання** в `tests/docs-fresh.mjs` — вікно
  контексту від цього не росте.

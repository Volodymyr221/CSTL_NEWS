# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-23 06:51 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-e7n2vk` |
| останній коміт людини | `8af79703` 23.08 06:51 — docs(журнал): підсумок передачі |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260822-2231` |
| останній PR у `main` | #999 |
| стендів на диску | 98 |
| незакомічених файлів | 0 ✅ |
| журнал за 2026-08-23 | **НЕМАЄ** ⚠️ |
| потік `/byyou` | idle — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `_session-log/vova-2026-08-22b.md`

**Останні кроки (без автокомітів парсерів):**

- `8af79703` 23.08 06:51 — docs(журнал): підсумок передачі
- `328323f0` 22.08 22:39 — docs(журнал): фініш №4 — PR #999, деплой v4880
- `6208ece1` 22.08 22:37 — docs(пульс): зріз після мерджу
- `1cd861d2` 22.08 22:32 — Merge remote-tracking branch 'origin/main' into claude/startuem-e7n2vk
- `c992ecf1` 22.08 22:32 — docs(пульс): свіжий зріз + CACHE_NAME

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

📦 **Передача 23.08 06:50.** Робочого коду в гілці **немає** — усе в `main`,
чотири деплої за вечір (**v4861 · v4867 · v4876 · v4880**), усі success.
Наступний чат може братись за нове.

🔴 **ГОЛОВНЕ ЗА ВЕЧІР: коментарі Стрічки не надсилались ДВІ ДОБИ, у всіх** —
знайшов Вова на проді, вилікувано (**B-32**). Політика читання питала про щойно
вставлений рядок, а `INSERT … RETURNING` такого не переживає. Разом із фіксом
заведено те, чого не було ніколи: **збої тепер видно в адмінці** (Аналітика →
«🚨 Збої за 7 днів», перший блок) — і **база**, і **сам застосунок**. Повністю —
`HOT_RULES.md` правило **№11-БІС**.

🔑 **Три скарги Вови за вечір — один клас вади:** дві поверхні відповідали на
РІЗНІ питання про ту саму подію. Коментар не надсилався (політика читання проти
вставки) · капсула мовчала на дописі агента (капсула питала «хто написав», push —
«хто веде сторінку») · число применшувало (рахувало один допис із двох). Шукаєш
дивну поведінку — спершу спитай, **чи всі поверхні питають одне й те саме**.

**Капсули накрили пʼяту тему — Стрічку.** Тепер їх пʼять: Автобуси · Повідомлення ·
Дошка · Питання · Стрічка, разом **14 градацій**. Ненакритими лишились **Збори**
(розділ новий, і законна капсула там одна — «вашу заявку схвалено») і **Новини**
(свідома відмова: лічильник стоїть за 200px нижче).

Далі має рости не логіка капсул, а **вміст** — і запуск.

🔴 **ГОЛОВНЕ ПРАВИЛО СЕСІЇ (22.08, слово Вови):** застосунок ще **не вийшов**.
12 профілів у базі — це Вова, його другий акаунт і кілька знайомих. **«Нуль
подій» означає «нуль користувачів», а не «фіча не потрібна».** Робимо на
перспективу; збирати й оцінювати дані — лише коли Вова напише великими літерами
**«ДОДАТОК ЗАПУСТИВСЯ»**. Повністю — `HOT_RULES.md` правило №12.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

_(22.08 вечір · журнал `_session-log/vova-2026-08-22b.md`)_

**💬 Капсула «відповіли під моїм у Стрічці»** — `RANK.FEED_REPLIES = 65`. Два
джерела: коментар під моїм дописом **або** відповідь, адресована особисто мені
(`reply_to_uid`). Друге обовʼязкове: без нього це фіча лише для власників сторінок.
🔴 Знадобилось, бо всередині застосунку це не казала **жодна поверхня** (дзвіночок
Стрічки — підписка на push, а не список непрочитаного).

🛑 **Дві пропозиції Вови відхилено за чинними правилами:** «пост вподобали 53
людини» (лайку нема куди стати в шкалу + «капсула каже ЩО, бейджі — СКІЛЬКИ») і
«тільки для адмінів» (правило №12 «я створив» однакове для власника і жителя).

**⚡ Живе оновлення:** подію кидає **та сама** підписка Стрічки. Другої не заводили —
два канали на подію це шлях до B-27.

🔴 **Заглушка бази брехала на трьох фільтрах** (`.in`, `.gt`, `.lt` віддавали УСЮ
таблицю) — знайшлось **падінням** сцени «перший запуск». Третій випадок тієї самої
хвороби (`.eq` 07.08, `.single` 17.08). Плюс `insert()` тепер лишає слід, інакше
ЩО САМЕ застосунок пише в базу не міг перевірити ніхто.

🔴 **B-32 — КОМЕНТАРІ НЕ НАДСИЛАЛИСЬ ДВІ ДОБИ** (знайшов Вова знімком). Повний
розбір — `CSTL_BUGS.md` і правило **№11-БІС**. Коротко: `.insert().select()` це
`INSERT … RETURNING`, а SELECT-політика питала про щойно вставлений рядок, якого в
знімку бази ще немає. Вражені `posts` · `page_posts` · `page_comments`. 🛑 Сторож не
спіймав, бо **сам вимагав зламаної форми** — виправлено + сторож КЛАСУ (26/26).

**🚨 ЗБОЇ ТЕПЕР ВИДНО — ДВА КОНТУРИ.** `db_refusal` (базa відхилила дію) ловиться
в ядрі `netCall`; `js_error` (впав сам застосунок — «натиснув і нічого») — у
`core/boot.js` через `window.onerror` + впалі обіцянки. Обидва: без вмісту, номер
пристрою замість акаунта, вимикач статистики глушить. Запобіжник від потопу — 40
однакових помилок дають **1** запис. Адмінка → Аналітика, перший блок, з міткою
«база» / «застосунок». Сторож — `tests/error-report.mjs` (10/10, контроль 3/10).

🛑 **МЕЖА, ЯКУ ТРЕБА ЗНАТИ: порожній блок ≠ все справно.** Обриви мережі не пишемо
навмисно, а **«мовчазний провал» не ловить ніхто** — коли нічого не впало, просто
нічого не сталося. Саме таким був баг капсули на дописі агента (нижче). Такі вади
ловлять лише стенди й живі люди.

**🤖 Капсула на дописі ШІ-агента** (скарга Вови: «сповіщення приходить, а капсула
ні — це ж одне ціле»). Заміряно: у дописів агента `author_uid` **порожній**, а push
шле РЕДАКТОРАМ СТОРІНКИ. Тобто дві поверхні відповідали на різні питання про ту
саму подію. Тепер капсула питає те саме, що push: автор **або** редактор сторінки.
Сцени 25-26 — з контролем на чужій сторінці. **123/123.**

---

_(21-22.08 · PR #983…#994 · журнал `_session-log/vova-2026-08-22.md` — **подробиці
в літописі**, тут лише підсумок)_

**🚌 Персоналізація автобусної капсули.** Пункт А визначається сам: обране в
сеансі → найближча наша зупинка **за координатами** → назва з погоди → останнє
обране → анкета → Олика. Координати 18 пунктів у `main` (#990). Наша назва села
зведена з назвою зупинки перевізника (#987) — із 17 сіл сім не збігались, і
житель такого села не отримував капсули ВЗАГАЛІ. «Олика → Олика» вилікувано
(#983): кінець рейсу береться зі ЗУПИНОК, а не з назви.

**❓ Капсула питань — три джерела** (#983): відповіли на ваше питання (85) ·
нове питання, якого я не бачив (40) · питання без відповіді (35).

**📏 Правило №12 — формула капсул** (#992, #994): `сам зробив + сталась подія +
ще актуально` + тест на пригадування + чотири законні джерела «мого».

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| Капсула «збережене знято» | **рішення: не робимо** | дві перешкоди по коду (порожній `savedIds` на Громаді, потрібна позначка «вже показав»). Рецепт збережено в правилі №12 |
| Перемикач «Actions може відкривати PR» | не увімкнено | Settings → Actions → General. Не обовʼязково: тепер посилання на PR є в логу |
| ПД в ІСТОРІЇ git | не стерто | перезапис історії ламає посилання на коміти — окреме рішення |
| Опис погоди на 320px | обрізається | або назва села, або опис; обрано назву |
| **Android не тестувався ВЗАГАЛІ** | 0 перевірок | усе живе тестування — iPhone Вови |
| Тестовий збір **на проді** | у базі | адмінка → «Збори» → «Видалити» |
| Класифікатор новин (**B-28**) | відкрито | 94.5% статей → «Суспільство»; блокує фільтр B-14 |
| Локальний `main` у середовищі | **неспоріднена історія** | 🛑 не лікуй скиданням, джерело правди — `origin` |
| Капсула Зборів | не робили | законна там рівно одна — «вашу заявку схвалено» (правило «я створив»). «Новий збір» — не моя дія, не проходить |
| Капсула «вам відповіли» — базовий випадок | ✅ **перевірено Вовою на проді** | працює; далі знайдені й полагоджені три окремі вади (агент · число · живе оновлення) |
| Капсула «число по кількох дописах» на телефоні | **не перевірено пальцем** | стенд 130/130, але Вова ще не тапав. Сценарій — у журналі 23.08 |
| Журнал збоїв — контур «застосунок» | **жодного разу не спрацював наживо** | після деплою нічого не падало. Це добре, але означає, що контур доведений лише стендом |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

1. 🔴 **Живий телефон, два сценарії:** (а) геолокація → у капсулі автобуса має
   стояти **твоє село**, не Олика (`МЕТЕЛЬНЕ`, `ХРОМ'ЯКІВ` у розкладі);
   (б) **два коментарі під різними дописами** → «2 НОВІ КОМЕНТАРІ» + «у 2 ваших
   дописах», тап веде на вкладку Стрічка.
2. 🔴 **Стираємо ПД з історії git?** Відповідь дана: варто, бо репозиторій
   публічний і це ЧУЖІ дані. Чекаю «роби». ⚠️ Після перезапису треба писати в
   GitHub Support.
3. **Голос бренду** — вичитати `editor/voice/olyka_castle.md` (поки чернетка).
4. **Прогін по профілях?** Тепер є доступ до бази — можу показати, скільки людей
   реально бачать хоч одну капсулу. ⚠️ До запуску це скаже про розробників, не
   про людей.
5. **Невикористаний ключ `cstl-olyka-agent`** — відкликати.

## ➡️ ДАЛІ

Капсули закриті на пʼяти темах — за словом Вови 22.08: «давай робимо так, як ти
кажеш, й закриваємо це питання вже». Логічний наступний крок — **не нова логіка, а
підготовка до запуску**: заслінка, вміст Дошки, тестування на Android.

**Черга Вови** (`_ai-tools/BACKLOG_VOVA_2026-08.md`): 💬 чат Дошки — теплота
`#F3EFE3`/`#FCFAF4` · 📐 спадковий відступ контейнера Громади · 🚌 капсули проти
віджета автобусів.

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) — застосунок навмисно закритий.
- 🛑 **Коди заслінки в репозиторій НЕ писати** — ні в коді, ні в журналі, ні в коміті.
- 🛑 **Знімки інструментів (`tests/tools/_out/`) НЕ комітити** — вони фотографують
  живий застосунок разом з іменами й телефонами людей.
- 🛑 **Кирилиця в НАЗВАХ змінних bash** — `bash` читає таке як імʼя команди й падає
  з кодом 127. `bash -n` цього НЕ ловить. У JS і Python — можна, у shell — ні.
- 🛑 **`Одеради АЗ` ≠ `Одеради`** — у розкладі це дві різні зупинки (84 і 70 рейсів).
- 🛑 **Порядок правил у `style/home.css`:** вузькоекранні `@media` для погоди мусять
  стояти в КІНЦІ секції, інакше програють за позицією і тихо не діють.
- ⚠️ **`page_admins` в аудит доступів НЕ рахувати** — команда сторінки не є
  доступом до системи (правило Вови 20.08).
- 🔴 **Клавіатура на iOS** (`style/feed.css:630,971`) — два фікси провалились (#638, #641).
- 🔴 **Три механізми закриття** (`#article-modal` · `core/modal.js` · `core/layers.js`).
- 🔴 **`main` напряму не чіпати** — тільки через PR.
- ⚠️ **Дампи БД у репозиторій не класти** — репозиторій ПУБЛІЧНИЙ.
- ⚠️ **«Unverified» на GitHub — очікувано, ігнорувати.**
- ⚠️ **`git diff` для «що не в main» — на ДВІ крапки.**
- ⚠️ **Не піднімати стелю бюджету читання** в `tests/docs-fresh.mjs`.

# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-19 11:56 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-v2m8mz` |
| останній коміт людини | `46107344` 19.08 11:56 — Merge remote-tracking branch 'origin/main' into claude/startuem-v2m8mz |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260818-2016` |
| останній PR у `main` | #953 |
| стендів на диску | 84 |
| незакомічених файлів | 0 ✅ |
| журнал за 2026-08-19 | **НЕМАЄ** ⚠️ |
| потік `/byyou` | done — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `.gitignore`
- `CSTL NEWS VOVA/NOW.md`
- `_session-log/vova-2026-08-18b.md`
- `scripts/supabase_editor_invites.sql`
- `tests/tools/_out/chat-1-один-контекст.png`
- `tests/tools/_out/chat-2-два-контексти.png`
- `tests/tools/_out/chat-3-три-контексти.png`
- `tests/tools/_out/chat-4-список.png`

**Останні кроки (без автокомітів парсерів):**

- `46107344` 19.08 11:56 — Merge remote-tracking branch 'origin/main' into claude/startuem-v2m8mz
- `90ec87e9` 19.08 11:46 — fix(ПД): прибрано знімки чату з реальними даними і пошту редакторки зі скрипта запрошень
- `bd2e9a2f` 19.08 11:43 — Merge remote-tracking branch 'origin/main' into claude/startuem-v2m8mz

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

✅ **Сесія 18.08 (вечір) закрита `/finish`.** У заслінці розробки тепер **ДВА
коди** — старий і доданий сьогодні; обидва дійсні одночасно. `DEV_LOCK` як стояв
`true`, так і стоїть: застосунок закритий.

✅ **ПЕРСОНАЛЬНІ ДАНІ З ДЕРЕВА ПРИБРАНІ (18.08, друга правка сесії).** Аудит
пройшов по ВСІЙ історії (40 105 обʼєктів git, 13 713 файлів): **ключів немає ніде
і ніколи** — ні JWT, ні `service_role`, ні приватних ключів, ні `.env`, ні дампів
бази. Знайдені натомість ПД живих людей — прибрані:

| що | було | стало |
|---|---|---|
| 4 знімки чату `tests/tools/_out/chat-*.png` | у `main`: реальний телефон, 4 імені, тексти приватних повідомлень | видалені; тека `tests/tools/_out/` у `.gitignore` |
| `scripts/supabase_editor_invites.sql:62` | реальна пошта редакторки, яка вийшла 26.07 | `editor@example.com` + 🛑-попередження в самому файлі |

🔴 **ЛИШИЛОСЬ НЕЗАКРИТИМ І ЦЕ ВАЖЛИВО:** видалення з дерева **не стирає файл з
історії** — знімки можна дістати зі старих комітів. Повне стирання = `git
filter-repo` + force push, а це переписує всі коміти й ламає посилання на них.
**Рішення за Вовою, окремою операцією, не мимохідь.**
⚠️ Ще 6 реальних адрес лишаються в документації і журналах (Роми ×2, Іллі,
Вови ×3) — Вовині й так публічні через авторство комітів.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

**Другий код заслінки** (`src/core/dev-lock.js`, PR **#953**). Спершу звірив, що
хеш ЧИННОГО коду збігається з тим, що лежить у файлі — і лише тоді додав другий
запис у `ALLOWED_CODE_PBKDF2`. Хеш рахував `tests/tools/dev-code-hash.mjs`, який
піднімає живий `core/dev-code.js` — той самий модуль, що потім перевіряє код, тож
«порахували» і «перевіряємо» не можуть розійтись.
🛑 **Самих кодів у репозиторії немає** — ні в коді, ні в коментарях, ні в журналі,
ні в повідомленнях комітів. Сторож `tests/dev-lock.mjs` перевіряє це механічно
(перебирає 101 слово поруч із хешем).
🔴 **Другий код СЛАБШИЙ за перший, і Вові сказано прямо:** 7 символів проти 9,
словниковий, і називає той самий механізм, до якого відкриває доступ. Хеш лежить у
публічному `bundle.js`, тож підбір робиться не на сайті (де гальмо 5 спроб → 30с →
до 5 хв), а в себе на машині, де гальма немає. Рішення Вови.
Стенд `tests/dev-lock.mjs`: **38/38** без коду, **41/41** з кожним із двох кодів
окремо — три додаткові перевірки саме про успішний вхід.

**Аудит репозиторію на ключі й ПД** — див. розділ «ЗАРАЗ». Заразом підтверджено, що
`db-backup.yml` шифрує дамп GPG **до** вивантаження, а `sb_publishable_…` у
`src/core/supabase.js:15` і `admin.html:435` — **publishable** (публічний за
задумом) ключ, захист якого в RLS, а не в таємності.

**Порахована ціна приватного репозиторію** (Вова питав, як закрити):
📐 З ЙОГО ЖЕ рахунку GitHub — **$35.68 за 18 днів** → **≈7 400 хв Actions/міс**.
Приватний = Pro $4 + перевитрата ~$35 = **≈$39/міс**. На Free плані варіанту немає
взагалі: GitHub Pages не публікує приватні репозиторії, сайт просто зникне.
🔑 **Мій попередній здогад був НЕПРАВИЛЬНИЙ** (казав «13 000 хв, винен `cms-sync`
кожні 5 хв»). Заміряно по 30 останніх прогонах: GitHub душить часті розклади сам,
`cms-sync` фактично спрацьовує **раз на ~33 хвилини**. Їдять **два парсери** —
~226 хв/добу з ~248. Врізати `cms-sync` не дало б нічого.
➡️ Найдешевше «закрито і все живе» — перенести парсери в Supabase (`pg_cron`), він
уже оплачений; тоді приватний репозиторій коштує $4/міс.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| **ПД у репозиторії** | знайдено, **не прибрано** | чекає «Роби» — 4 картинки + один рядок SQL |
| Перезапис історії git | не робили | ламає посилання на коміти; окреме рішення, не поспішаючи |
| Сторож на свайп модалки статті | **немає** | доводив разовим приладом; стенда на `#article-modal` нема |
| Перетікання тла вкладки | знайдено, **не лікували** | `.app-main` має `transition: background-color 0.3s`, фони вкладок різні — ≈300мс |
| **Android не тестувався ВЗАГАЛІ** | 0 перевірок | усе живе тестування — iPhone Вови |
| Тестовий збір **на проді** | у базі | адмінка → «Збори» → «Видалити» |
| Класифікатор новин (**B-28**) | відкрито | 94.5% статей → «Суспільство»; блокує фільтр B-14 |
| Юрист на правові тексти | відкрито | єдине, чого закрити не можу |
| Локальний `main` у середовищі | **неспоріднена історія** | 🛑 не лікуй скиданням, джерело правди — `origin` |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

1. 🔴 **Прибираємо знайдені ПД?** Чотири знімки чату і пошта Алли. Мінімум —
   видалити з дерева зараз; повне стирання з історії — окремо.
2. 🔴 **Другий код заслінки лишаємо як є?** Він слабкий (див. вище). Якщо його
   роздавати стороннім — я б замінив на довший, це один рядок.
3. 🔴 **Закриваємо репозиторій?** Ціна порахована: ≈$39/міс зараз, або $4/міс
   після переїзду парсерів у Supabase, або $0 і лишаємо публічним.
4. 🔴 **Знімаємо заслінку?** `DEV_LOCK = true` — єдине, що відділяє від Етапу 3.
5. **Іконка «Стрічка» — форма остаточна?**
6. **Стенд `ad-report`** падав на CI 15.08, локально 16/16 — глянути, якщо повториться.

## ➡️ ДАЛІ

**Черга Вови** (`_ai-tools/BACKLOG_VOVA_2026-08.md`, порядок задав 05.08):

1. ✅ **Віджет і розділ зборів — ЗРОБЛЕНО** (16-17.08); лишилось замінити тестовий збір реальним.
2. 💬 Чат Дошки — теплота `#F3EFE3` / `#FCFAF4`
3. 📐 Спадковий відступ контейнера Громади
4. 🚌 Капсули проти віджета автобусів

Поза чергою і вище за неї — **прибрати ПД** (питання 1).

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) — застосунок навмисно закритий.
- 🛑 **Коди заслінки в репозиторії НЕ писати** — ні в коді, ні в коментарі, ні в
  журналі, ні в повідомленні коміту. Репозиторій публічний, хеш у `bundle.js`.
- 🔴 **Клавіатура на iOS** (`style/feed.css:630,971`) — два фікси вже провалились (PR #638, #641).
- 🔴 **Три різні механізми закриття** (`#article-modal` · `core/modal.js` · `core/layers.js`).
- 🔴 **`main` напряму не чіпати** — тільки через PR.
- ⚠️ **Дампи БД у репозиторій не класти** — репозиторій ПУБЛІЧНИЙ.
- ⚠️ **«Unverified» на GitHub — очікувано, ігнорувати.**
- ⚠️ **`git diff` для «що не в main» — на ДВІ крапки.**
- ⚠️ **Не піднімати стелю бюджету читання** в `tests/docs-fresh.mjs`.

# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-21 10:42 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-v2m8mz` |
| останній коміт людини | `7f661bad` 21.08 10:41 — docs: пульс NOW.md перед деплоєм |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260820-1816` |
| останній PR у `main` | #979 |
| стендів на диску | 94 |
| незакомічених файлів | 6 ⚠️ |
| журнал за 2026-08-21 | **НЕМАЄ** ⚠️ |
| потік `/byyou` | active — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `CSTL NEWS VOVA/NOW.md`
- `bundle.js`
- `scripts/parse_rss.py`
- `src/core/settlements-geo.js`
- `src/tabs/community-blocks.js`
- `src/tabs/home-caps.js`
- `tests/cms-chain.mjs`
- `tests/home-caps.mjs`
- `tests/home.mjs`
- `tests/news-widget.mjs`

**Останні кроки (без автокомітів парсерів):**

- `7f661bad` 21.08 10:41 — docs: пульс NOW.md перед деплоєм
- `cd731a30` 21.08 10:20 — fix(номери): парсер більше не залазить у діапазон кабінету + три чесні мірки
- `74f8fa5e` 21.08 09:53 — test(капсули): сцена «я в Луцьку, GPS мовчить» — 94/94
- `dc96b996` 21.08 09:49 — fix(капсули): пункт А — те місце, що видно в погоді; гілку «ДОДОМУ» знято
- `37f36d78` 21.08 08:37 — merge: origin/main перед деплоєм

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

**Три речі цієї сесії, усі в одній гілці.**

1. 🤖 **Агент спільноти OLYKA CASTLE** — працює, перший пост написано. Нижче.
2. ✋ **Свайп назад «звідки завгодно»** — з **лівої половини**, не з краю 28px.
   Скрол цілий: домінанта напрямку (гориз. > верт. × 1.5) після 10px, у каруселях
   жест не починається. Один вхід `attachSwipeBack()` — діє на ВСІХ шарах.
3. 📱 **Google Play** — TWA: стабільний `id`, `privacy.html` при збірці,
   `docs/PLAY_TWA.md`. iOS свідомо лишається PWA (guideline 4.2).
   🔴 Далі — $25 і закрите тестування (≈12 тестерів × 14 днів): купує Вова.

✅ **Віджет погоди (19.08).** Компактний рядок замість пів екрана. Правила — в
`HOT_RULES.md`; головні три: **жест лише на компактному рядку**, **стрілка в
лівому краю**, **вузькоекранні `@media` погоди — в КІНЦІ секції `home.css`**.

✅ **ПД з дерева прибрані (18.08).** Аудит по всій історії (40 105 обʼєктів): ключів
немає ніде. 🔴 Файли лишаються в ІСТОРІЇ git — стерти = `filter-repo` + force push
+ звернення в GitHub Support. Чекає команди Вови.


## 🤖 ШІ-АГЕНТ СПІЛЬНОТИ OLYKA CASTLE (20.08) — ПРАЦЮЄ

Пише у Стрічку від імені **`pages.id = 3`** як **ЧЕРНЕТКИ**. Opus 5, 3-4 пости на
тиждень. ✅ Перший живий пост: $0.0228, конвеєр працює цілком.

🛑 **Чернетку ховає БАЗА, не клієнт.** 🔑 Агент бачить лише `facts` із плану —
фактів не вигадує. ⚖️ Функції — меншість: дві поспіль ніколи, частка ≤34%.
🛑 **У кожного агента своя кишеня** (сума = стеля $4): новини $3/міс, стрічка
$0.60, свята $0.40. Ключ `ANTHROPIC_API_KEY` **уже є — не перезаписувати**,
секрет один на ТРИ процеси.
🌍 **Масштаб (20.08):** «жителі Олики» ≠ «жителі громади» — Олика це центр, плюс
16 сіл. Довідка з `hromada_config.json`, заслон `editor/filters/geo_scale.py`.

📖 **Повний опис, історія і всі уроки:** `docs/AI_AGENT_OLYKA_CASTLE.md`

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

**Погода** — `renderWeatherBlock()` + `attachWeatherFold()`. 🔑 **Дні — СПИСОК, а не
сім колонок:** у колонці 320/7 = 45px шість значень не вміщаються ніяк. Заразом
зник горизонтальний скрол.

**Жести** — `attachSwipeBack()` + `opts.el`. **Play** — `gen_privacy.mjs`, `.nojekyll`.
**Агент** — пакет `editor/`, міграція чернеток, позначка і кнопка «Опублікувати».

**Аудит на ключі й ПД** (18.08) — ключів немає; ПД прибрано; `.gitignore` закриває
причину. Приватний репозиторій: ≈$39/міс · $4 після переїзду парсерів · $0 як є.

🛑 **УРОКИ.** (1) Зрізана дужка при переносі CSS → браузер відкинув усе далі →
впали перевірки КАРТКИ ЗБОРІВ (думав на чуже — зламав я). (2) Ліміт у двох вимірах
закривай в ОБОХ: кишені спершу були лише місячні, вада лишилась на добовій осі.
(3) Стенди перевіряли ЛАНКИ, а вада була в ЛАНЦЮЗІ (свята в нікуди).

**Стенди (90):** нові `weather-fold` 51 · `swipe-back` 12 · `pwa-store` 17 ·
`olyka-agent` 52 · `ai-budget` 19 · `admin-access` 23 · `home-caps` 83 · `home` 80.

🌙 **НІЧНА КАПСУЛА АВТОБУСА (20.08).** Немає рейсів на сьогодні — показуємо перший
завтрашній (`ЗАВТРА · О 07:10`). Заміряно: 21:15 → 07:40, діра **10 год** (денні
щілини разом 26 хв, тож лагодили ніч). 🔑 Час — проходу через СВОЮ зупинку, не
виїзду з чужого міста. `home-caps` **83/83** із контролем.

🔴 **ДОСТУПИ — АУДИТ І РОЗДІЛ (20.08).** В `editor_users` півтора місяця висів
ПОВНИЙ доступ у людини, що пішла з команди. Прибрано; редакторів 0, адмінів 1.
✅ **Аудит показав: примус працює, видимості не було.** RLS на всіх 38 таблицях,
`search_path` у 51 з 51 функції, чужий бачить нулі скрізь, а заборона публікації
редактору стоїть **у самій базі** (`with_check` на `cms_articles`).
✅ **Зроблено:** `disabled_at`/`last_seen_at`/`disabled_by`/`invited_by`,
відкликання діє в `has_editor_perm()` **і** `is_team_member()`, журнал
`admin_audit_log` (клієнту INSERT недоступний), розділ **«Доступи»**.
🔑 Лічильник плитки рахує **забуті** доступи (без входу 30+ днів), не всіх
редакторів. Доведено на живій базі: права `true → false`, запис у журналі.
Стенд `admin-access` **23/23**.
🛑 **Пагінації, серверного пошуку і розбиття `admin.html` НЕ робив свідомо:**
у брифі я написав «~1200 рядків», а це була сума лімітів — реально **~145**.
⚠️ **`page_admins` в аудит доступів НЕ рахувати:** команда сторінки — не доступ
до системи, там може бути будь-хто, кого призначив власник (правило Вови 20.08).

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| ✅ Сторож капсули (відставав від 17.08) | **полагоджено 20.08** | звіряється перший склад мітки до « · »; червонів «за годинником», бо поза вікном 120 хв перевірка йшла вхолосту |
| ПД в ІСТОРІЇ git | не стерто | перезапис історії ламає посилання на коміти — окреме рішення |
| Опис погоди на 320px | обрізається | або назва села, або опис; обрано назву |
| `sheet-close` під навантаженням | блимає | таймінговий замір; окремо 3/3 зелений |
| **Android не тестувався ВЗАГАЛІ** | 0 перевірок | усе живе тестування — iPhone Вови |
| Тестовий збір **на проді** | у базі | адмінка → «Збори» → «Видалити» |
| Класифікатор новин (**B-28**) | відкрито | 94.5% статей → «Суспільство»; блокує фільтр B-14 |
| Локальний `main` у середовищі | **неспоріднена історія** | 🛑 не лікуй скиданням, джерело правди — `origin` |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

✅ **Закрито 20.08:** другий код заслінки — ЛИШАЄМО · репозиторій — поки ПУБЛІЧНИЙ
· заслінку знімаємо за день-два до виходу, за командою Вови.

1. 🔴 **Живий телефон** — свайп назад з лівої половини (і чи не зламався скрол);
   погода: тап по стрілці, свайп по рядку.
2. 🔴 **Стираємо ПД з історії git?** Відповідь дана: варто, бо репозиторій
   публічний і це ЧУЖІ дані. Чекаю «роби». ⚠️ Після перезапису Вові треба писати
   в GitHub Support — інакше старі коміти лишаться доступні за прямим посиланням.
3. **Голос бренду** — вичитати `editor/voice/olyka_castle.md` (поки чернетка Claude).
4. **Лагодимо сторож капсули?** Один рядок, поза замовленням.

## ➡️ ДАЛІ

**Черга Вови** (`_ai-tools/BACKLOG_VOVA_2026-08.md`): ✅ віджет і розділ зборів
(16-17.08) · 💬 чат Дошки — теплота `#F3EFE3`/`#FCFAF4` · 📐 спадковий відступ
контейнера Громади · 🚌 капсули проти віджета автобусів.

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) — застосунок навмисно закритий.
- 🛑 **Коди заслінки в репозиторій НЕ писати** — ні в коді, ні в журналі, ні в коміті.
- 🛑 **Знімки інструментів (`tests/tools/_out/`) НЕ комітити** — вони фотографують
  живий застосунок разом з іменами й телефонами людей. Правило вже в `.gitignore`.
- 🛑 **Порядок правил у `style/home.css`:** вузькоекранні `@media` для погоди мусять
  стояти в КІНЦІ секції, інакше програють за позицією і тихо не діють.
- 🔴 **Клавіатура на iOS** (`style/feed.css:630,971`) — два фікси провалились (PR #638, #641).
- 🔴 **Три механізми закриття** (`#article-modal` · `core/modal.js` · `core/layers.js`).
- 🔴 **`main` напряму не чіпати** — тільки через PR.
- ⚠️ **Дампи БД у репозиторій не класти** — репозиторій ПУБЛІЧНИЙ.
- ⚠️ **«Unverified» на GitHub — очікувано, ігнорувати.**
- ⚠️ **`git diff` для «що не в main» — на ДВІ крапки.**
- ⚠️ **Не піднімати стелю бюджету читання** в `tests/docs-fresh.mjs`.

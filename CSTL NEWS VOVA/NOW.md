# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-19 18:47 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-v2m8mz` |
| останній коміт людини | `cc79e07a` 19.08 18:46 — fix(погода): стрілка при погоді, лінія праворуч і більше не перестрибує при розгортанні |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260819-1846` |
| останній PR у `main` | #957 |
| стендів на диску | 85 |
| незакомічених файлів | 1 ⚠️ |
| журнал за 2026-08-19 | **НЕМАЄ** ⚠️ |
| потік `/byyou` | done — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `_session-log/vova-2026-08-18b.md`
- `bundle.js`
- `src/tabs/community-blocks.js`
- `style/home.css`
- `sw.js`
- `tests/weather-fold.mjs`

**Останні кроки (без автокомітів парсерів):**

- `cc79e07a` 19.08 18:46 — fix(погода): стрілка при погоді, лінія праворуч і більше не перестрибує при розгортанні
- `7e359fbc` 19.08 18:23 — docs: пульс перед фінішем — третя ітерація погоди
- `e93837ed` 19.08 18:20 — Merge remote-tracking branch 'origin/main' into claude/startuem-v2m8mz
- `33831ab2` 19.08 18:11 — feat(погода): день зверху, опис і діапазон знизу, стрілка без кружечка
- `b498fa3f` 19.08 16:53 — feat(погода): день у компактному рядку, смужки діапазону і ймовірність опадів (#956)

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

✅ **ВІДЖЕТ ПОГОДИ ПЕРЕРОБЛЕНО (19.08)** — головна робота дня. Замовлення Вови:
«займає забагато вертикального простору… зробити значно компактнішим, але не
втратити детальний прогноз». Тепер це **два стани одного віджета**:

```
згорнуто:   ☀️ 22°  Сьогодні   ⌄ │        📍 Луцьк
                    Хмарно · 13°/24°
розгорнуто: Сьогодні 19.08 ⛅        13° ▓▓▓▓▓░░ 24°
            Чт       20.08 🌧️ 💧70%  12° ▓▓▓░░░░ 21°
```

🔴 **ТРЕТЯ ІТЕРАЦІЯ 19.08 (по знімку Вови).** Рядки помінялись місцями: зверху
**КОЛИ** (`Сьогодні`), знизу **ЩО ТОГО ДНЯ** (`Хмарно · 13°/24°`) — «Сьогодні»
стосувалось не своїх чисел, а всього блоку, і це справді читалось нелогічно.
📐 Наслідок, який довелось вирішувати: опис і числа тепер в ОДНОМУ рядку, а це
133 + 54 = ~187px проти 135-174px колонки. Рядок не вміщається на ЖОДНОМУ
телефоні, тож питання лише в тому, що обрізати. **Числа не ріжуться ніколи**,
трикрапку отримує слово: його видно з іконки поруч, а обрізане «13°/2…» було б
НЕПРАВДОЮ про погоду. Пробіли навколо слеша прибрано — це дало 8px, і короткі
описи вміщаються цілими від 375px.
🔴 **ЧЕТВЕРТА ІТЕРАЦІЯ — СТРІЛКА ПЕРЕЇХАЛА ДО ПОГОДИ** (Вова: «перемістіть цю
стрілочку на ліву сторону, там де градуси, хмарність і сьогодні… а справа хай буде
кнопка місцезнаходження»). Порядок: `іконка · градуси · текст · СТРІЛКА │ капсула`.
Логіка правки точна: стрілка керує ПРОГНОЗОМ, а стояла в правому краю і читалась
як частина капсули локації.
🐞 **Заразом виправлено баг, який знайшов Вова:** при розгортанні лінія
перестрибувала на інший бік. Причина — лінія була бордюром САМОЇ кнопки, а поворот
180° застосовувався до кнопки цілком. Тепер крутиться лише значок усередині.
🔑 **Правило на майбутнє: якщо в елемента є і поворот, і межа — вони мусять жити на
різних вузлах.** Сторож на це стоїть (дві перевірки, друга не дає «полагодити»
назад, повернувши поворот на кнопку).

🔴 **Стрілка більше не кружечок.** Вова: «відчуття, ніби це позначає локацію, бо
вона якраз біля населеного пункту» — і мав рацію: кружечок стояв упритул до
капсули «Луцьк» і мав ТУ САМУ заливку, тож око читало їх як один елемент.
Заливку прибрано, стрілку відділено тонкою лінією зліва; площа дотику 34×34
лишилась.
🛑 Хочемо і найдовший опис цілим — єдине джерело місця це капсула НП (84px), тобто
довелось би прибрати локацію з рядка. **Не робив: замовлення було зворотне.**

🔴 **ДРУГА ІТЕРАЦІЯ 19.08** (скарга Вови «неясно, що за день»): у компактному
рядку зʼявився **день**, у списку — **смужка діапазону** на спільній шкалі тижня і
**ймовірність опадів** (від 30%; дані вже приходили, новий запит НЕ додано).
🔑 Обмін названо вголос: текстовий стан із рядка дня прибрано — він коштував ~90px
і повторював іконку поруч; для читача екрана лишився в `aria-label`.
⚠️ На ≤374px слово «Сьогодні» ховається: там обрізало б самі числа.

📐 **Було 121px ЗАВЖДИ → стало 51-53px** у згорнутому (заміряно на 320·375·390·430).
Розгортання — тап по стрілці, тап по рядку або свайп; анімація 300мс.
🔑 **Дані не чіпані:** той самий один запит, той самий кеш `_wxData`, той самий
перехід у модалку по годинах при тапі на день. Розгортання **нового запиту не
робить** — це окремо рахує стенд.

✅ **Персональні дані з дерева прибрані (18.08).** Аудит по ВСІЙ історії (40 105
обʼєктів): ключів немає ніде і ніколи. Знайдені ПД — 4 знімки чату з реальним
телефоном і пошта редакторки в SQL — видалені, `tests/tools/_out/` у `.gitignore`.
🔴 **Лишається:** файли живуть в ІСТОРІЇ git. Стерти повністю = `git filter-repo` +
force push, це переписує всі коміти. **Рішення Вови, окремою операцією.**

✅ **Заслінка має ДВА коди** (18.08) — обидва дійсні. `DEV_LOCK = true`.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

**Погода** (PR цієї сесії). Компактний рядок + панель; `attachWeatherFold()` веде
висоту, поворот стрілки і свайп.
🔑 **Дні стали СПИСКОМ, а не сімома колонками** — причина арифметична: у рядку дня
шість значень, а в колонці 320/7 = 45px вони не вміщаються ніяк. Заразом зникло
питання горизонтального скролу: його немає ніде.
🛑 **Жест живе ТІЛЬКИ на компактному рядку** (`touch-action: none`). Це відповідь на
вимогу «звичайний скрол не має випадково відкривати погоду». Ціна названа чесно:
почавши тягнути палець рівно з рядка погоди, сторінку не прогорнеш.
🔴 **320px — названа межа, не недогляд:** тексту лишається 82px, найдовший опис
словника 133px. Вибір бінарний — або назва села, або повний опис. Лишено назву
(її 05.08 виносили окремо саме тому, що губилась). На 375/390/430 усе ціле.

**Аудит на ключі й ПД** (18.08) — ключів немає; ПД прибрано; `.gitignore` закриває
причину. Ціна приватного репозиторію порахована з рахунку: ≈$39/міс, або $4 після
переїзду парсерів у Supabase, або $0 і лишаємо публічним.

**Другий код заслінки** (18.08). Сказано прямо: він слабший за перший (коротший,
словниковий, називає той самий механізм). Рішення Вови.

🛑 **УРОК СЕСІЇ — ДВІЧІ ПРО ПОРЯДОК CSS, і обидва рази наслідок вилітав не там, де
причина.** (1) При переносі медіа-блоку зрізав закривну дужку — браузер відкинув
увесь подальший CSS, і впали перевірки КАРТКИ ЗБОРІВ. Спершу подумав на чуже
падіння; перевірив прогоном на чистому дереві — зламав я. (2) Медіа-блок двічі
програвав за позицією у файлі правилам, описаним нижче, і працював НАПОЛОВИНУ.
➡️ Після ручного переносу блоку CSS — рахувати баланс дужок скриптом; усі
вузькоекранні перевизначення тримати в КІНЦІ секції.
⚠️ **Фальшивий зелений:** проміжний прогін показував «375px чисто» — на зламаному
CSS. Зелений на зламаному стилі не означає нічого.

**Стенди:** новий `tests/weather-fold.mjs` **51/51** · `weather` 26/26 ·
`weather-day` 46/46 · `home` 79/80 · `drift` 24/24 · повний прогін **81/85**.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| 🔴 **Сторож капсули відстав від рішення 17.08** | знайдено, **не чіпав** | `home.mjs:250` чекає рівно `МОЄ/ЗАРАЗ/НОВЕ`, а мітка навмисно двоскладова (`ЗАРАЗ · ЧЕРЕЗ 40 ХВ`). ⚠️ Червоніє **не завжди, а за годинником**: капсула автобуса є лише коли рейс ближчий за 120 хв, поза вікном перевірка проходить вхолосту. Лагодиться одним рядком — про це не просили |
| ПД в ІСТОРІЇ git | не стерто | перезапис історії ламає посилання на коміти — окреме рішення |
| Опис погоди на 320px | обрізається | або назва села, або опис; обрано назву. Слово «Сьогодні» там теж сховане — інакше різало числа |
| `sheet-close` під навантаженням | блимає | таймінговий замір; окремо 3/3 зелений |
| **Android не тестувався ВЗАГАЛІ** | 0 перевірок | усе живе тестування — iPhone Вови |
| Тестовий збір **на проді** | у базі | адмінка → «Збори» → «Видалити» |
| Класифікатор новин (**B-28**) | відкрито | 94.5% статей → «Суспільство»; блокує фільтр B-14 |
| Локальний `main` у середовищі | **неспоріднена історія** | 🛑 не лікуй скиданням, джерело правди — `origin` |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

1. 🔴 **Погода на живому телефоні** — перевірити пальцем: свайп по рядку вгору/вниз,
   тап по стрілці, чи не заважає жест гортанню сторінки.
2. **Лагодимо сторож капсули?** Один рядок, але поза замовленням.
3. **320px: назва села чи повний опис?** Зараз назва.
4. 🔴 **Стираємо ПД з історії git?** Окрема обережна операція.
5. 🔴 **Другий код заслінки лишаємо?** Він слабкий.
6. 🔴 **Закриваємо репозиторій?** ≈$39/міс · $4 після переїзду парсерів · $0 як є.
7. 🔴 **Знімаємо заслінку?** `DEV_LOCK = true` — єдине, що відділяє від Етапу 3.

## ➡️ ДАЛІ

**Черга Вови** (`_ai-tools/BACKLOG_VOVA_2026-08.md`):

1. ✅ Віджет і розділ зборів — ЗРОБЛЕНО (16-17.08)
2. 💬 Чат Дошки — теплота `#F3EFE3` / `#FCFAF4`
3. 📐 Спадковий відступ контейнера Громади
4. 🚌 Капсули проти віджета автобусів

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) — застосунок навмисно закритий.
- 🛑 **Коди заслінки в репозиторій НЕ писати** — ні в коді, ні в журналі, ні в коміті.
- 🛑 **Знімки інструментів (`tests/tools/_out/`) НЕ комітити** — вони фотографують
  живий застосунок разом з іменами й телефонами людей. Правило вже в `.gitignore`.
- 🛑 **Порядок правил у `style/home.css`:** вузькоекранні `@media` для погоди мусять
  стояти в КІНЦІ секції, інакше програють за позицією і тихо не діють.
- 🔴 **Клавіатура на iOS** (`style/feed.css:630,971`) — два фікси провалились (PR #638, #641).
- 🔴 **Три механізми закриття** (`#article-modal` · `core/modal.js` · `core/layers.js`).
- 🔴 **`main` напряму не чіпати** — тільки через PR.
- ⚠️ **Дампи БД у репозиторій не класти** — репозиторій ПУБЛІЧНИЙ.
- ⚠️ **«Unverified» на GitHub — очікувано, ігнорувати.**
- ⚠️ **`git diff` для «що не в main» — на ДВІ крапки.**
- ⚠️ **Не піднімати стелю бюджету читання** в `tests/docs-fresh.mjs`.

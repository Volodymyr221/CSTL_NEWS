# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-20 06:29 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-v2m8mz` |
| останній коміт людини | `1596fdd8` 20.08 06:29 — Merge remote-tracking branch 'origin/main' into claude/startuem-v2m8mz |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260820-0629` |
| останній PR у `main` | #963 |
| стендів на диску | 89 |
| незакомічених файлів | 0 ✅ |
| журнал за 2026-08-20 | **НЕМАЄ** ⚠️ |
| потік `/byyou` | done — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `CSTL NEWS VOVA/NOW.md`
- `_session-log/vova-2026-08-18b.md`
- `editor/core/spend.py`
- `editor/missions/holidays.json`
- `editor/missions/olyka_castle.json`
- `editor/run.py`
- `scripts/ai_news_agent.py`
- `sw.js`
- `tests/ai-budget.mjs`

**Останні кроки (без автокомітів парсерів):**

- `1596fdd8` 20.08 06:29 — Merge remote-tracking branch 'origin/main' into claude/startuem-v2m8mz
- `d51c8d4a` 20.08 06:28 — feat(гроші): особиста кишеня кожному агенту — дорогий не морить голодом дешевого
- `2cdf033e` 20.08 06:16 — feat(гроші): спільні стелі витрат на модель — $4/міс і $1.20/добу (#963)
- `cf2fa2d8` 20.08 06:15 — docs: пульс у межах стелі після мерджу main
- `484d5367` 20.08 06:14 — Merge remote-tracking branch 'origin/main' into claude/startuem-v2m8mz

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

**Три речі цієї сесії, усі в одній гілці.**

1. 🤖 **Агент спільноти OLYKA CASTLE** — готовий і має ключ. Розділ нижче.
2. ✋ **Свайп назад «звідки завгодно»** (19-20.08) — сторінка закривається жестом з
   **лівої половини** екрана, а не з краю 28px. Скрол не зламано: рішення приймає
   домінанта напрямку (горизонталь > вертикаль × 1.5) після 10px, а в горизонтальних
   каруселях жест не починається взагалі. Плавність стара — той самий
   `createDragTracker`/`finishSwipe`. Один вхід `attachSwipeBack()` у
   `core/layers.js`, тож правило діє на ВСІХ шарах одразу. Стенд 12/12.
3. 📱 **Підготовка до Google Play** (19.08) — обгортка TWA: стабільний `id` у
   `manifest.json`, `privacy.html` з `src/core/legal.js` при збірці, інструкція
   `docs/PLAY_TWA.md`. iOS свідомо лишається PWA (guideline 4.2). Стенд 17/17.
   🔴 Далі — $25 за акаунт і закрите тестування (≈12 тестерів × 14 днів): купує Вова.

<<<<<<< HEAD
✅ **Віджет погоди (19.08).** Компактний рядок 51-53px замість 121px; розгортається
у список 7 днів, дані не чіпані (той самий запит і кеш). Правила — в `HOT_RULES.md`;
головні три: **жест лише на компактному рядку**, **стрілка в самому лівому краю**,
**вузькоекранні `@media` погоди — в КІНЦІ секції `home.css`**. 🔴 На 320px опис
обрізається: або назва села, або опис — обрано назву.
=======
✅ **Віджет погоди (19.08, чотири ітерації за знімками Вови).** Компактний рядок
51-53px замість 121px завжди; розгортається у список 7 днів. 🔑 Дані не чіпані:
той самий запит, той самий кеш, розгортання нового запиту НЕ робить.
Чотири правила віджета — в `HOT_RULES.md`; найважливіші три: **жест живе лише на
компактному рядку**, **стрілка — в самому лівому краю перед іконкою**,
**вузькоекранні `@media` для погоди — в КІНЦІ секції `home.css`**.
🔴 На 320px найдовший опис обрізається: там або назва села, або опис — обрано назву.
>>>>>>> origin/main

✅ **Персональні дані з дерева прибрані (18.08).** Аудит по ВСІЙ історії (40 105
обʼєктів): ключів немає ніде і ніколи. 🔴 Файли лишаються в ІСТОРІЇ git — стерти =
`git filter-repo` + force push + звернення в GitHub Support. Чекає команди Вови.


## 🤖 ШІ-АГЕНТ СПІЛЬНОТИ OLYKA CASTLE (20.08) — ГОТОВИЙ ДО ПРОГОНУ

Пише у Стрічку від імені **`pages.id = 3` OLYKA CASTLE** як **ЧЕРНЕТКИ** (рішення
Вови). **Opus 5**, 3-4 пости на тиждень (пн/ср/пт/нд 10:10), по одному за прогін.

```
план тем (Вова + Claude) → Opus 5 голосом бренду → ЧЕРНЕТКА у Стрічці
                              → команда читає → «Опублікувати» → усі бачать
```

🛑 **ЧЕРНЕТКУ ХОВАЄ БАЗА, А НЕ КЛІЄНТ.** Політика читання:
`(deleted_at is null AND status='published') OR can_edit_page(page_id)`. Фільтр у
клієнті — друга лінія. Текст, якого Вова не вичитав, не існує для читача навіть
при помилці в застосунку.
✅ Міграцію накатано, наслідок звірено: видимих постів було 10 → лишилось 10.

🔑 **Агент не вигадує фактів:** бачить лише `facts` із `editor/plans/olyka_castle.json`;
заборона стоїть ДВІЧІ — у голосі і в промпті.
👥 **Публікують усі з «Команди сторінки»** (власник + модератори, `page_admins`):
окремого «тегу редактора» не заводили — і база, і клієнт питають те саме
`can_edit_page`. ⚠️ Слово «редактор» зайняте кабінетом редактора новин.
📍 **Модерація — у самій Стрічці**, окремого екрана немає свідомо: чернетка
стоїть на місці майбутнього поста з позначкою і кнопкою «Опублікувати».

🔑 **Ключ `ANTHROPIC_API_KEY` у репозиторії ВЖЕ Є** — той самий секрет живить
новинного агента і свята (перевірено: прогін новин 20.08 05:46 UTC успішний).
🛑 **Не перезаписувати новим ключем:** секрет один на ТРИ процеси, і підміна на
ключ без балансу тихо зупинила б усі три.
<<<<<<< HEAD
💸 Пост ≈ $0.03. Лічильник навчили розрізняти моделі (доти рахував усе за Sonnet
і занизив би Opus удвічі).
🛑 **У КОЖНОГО АГЕНТА СВОЯ КИШЕНЯ** (один журнал `ai_spend.json`, сума = стеля $4):
новини **$3/міс**, стрічка **$0.60/міс і $0.20/добу**, свята $0.40. Це відповідь на
питання Вови «якщо $5 зʼїсться за 4 дні — як тоді пости писати?»: дорогий агент
більше не може вибрати частку дешевого. ⚠️ Окремий ключ цього НЕ лікує — гроші
однаково з одного балансу. Стенд `ai-budget` 19/19 **запускає** запобіжник, а не
шукає слова; він і знайшов, що перша версія кишень працювала лише на місяць, а на
добі вада лишалась. ✅ Факт: новинний агент прокидається щогодини, але з 08.07 не
витратив нічого — виходить за 1 сек, бо чернеток у кабінеті досить.
=======
💸 Лічильник витрат навчили розрізняти моделі — доти він рахував усе за Sonnet і
занизив би Opus **удвічі**. Пост ≈ $0.03.
>>>>>>> origin/main

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

**Погода** — `renderWeatherBlock()` + `attachWeatherFold()`. 🔑 **Дні — СПИСОК, а не
сім колонок:** у рядку дня шість значень, у колонці 320/7 = 45px вони не вміщаються
ніяк. Заразом зник горизонтальний скрол — його немає ніде.

**Жести** — `attachSwipeBack()` + `opts.el`. **Play** — `gen_privacy.mjs`, `.nojekyll`, `docs/PLAY_TWA.md`.
**Агент** — `editor/{sources,writers,sinks,images,plans,voice}`, міграція чернеток, позначка «ЧЕРНЕТКА» і кнопка «Опублікувати» у Стрічці.

**Аудит на ключі й ПД** (18.08) — ключів немає; ПД прибрано; `.gitignore` закриває
причину. Ціна приватного репозиторію: ≈$39/міс · $4 після переїзду парсерів · $0 як є.

🛑 **УРОКИ СЕСІЇ.** (1) Зрізана дужка при переносі медіа-блоку → браузер відкинув
увесь подальший CSS → впали перевірки КАРТКИ ЗБОРІВ (думав на чуже — зламав я);
двічі медіа-блок програвав за позицією у файлі. ⚠️ «375px чисто» показав прогін на
зламаному CSS. (2) Ліміт у двох вимірах треба закривати в ОБОХ: кишені витрат
спершу зробив лише місячні, і вада відтворилась на добовій осі — знайшов стенд.

**Стенди:** нові `weather-fold` 51/51 · `swipe-back` 12/12 · `pwa-store` 17/17 ·
`olyka-agent` 32/32 · старі `weather` 26/26 · `weather-day` 46/46 · `home` 79/80.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| 🔴 **Сторож капсули відстав від рішення 17.08** | знайдено, **не чіпав** | `home.mjs:250` чекає рівно `МОЄ/ЗАРАЗ/НОВЕ`, а мітка навмисно двоскладова (`ЗАРАЗ · ЧЕРЕЗ 40 ХВ`). ⚠️ Червоніє **за годинником**: капсула автобуса є лише коли рейс ближчий за 120 хв. Лагодиться одним рядком — не просили |
| ПД в ІСТОРІЇ git | не стерто | перезапис історії ламає посилання на коміти — окреме рішення |
| Опис погоди на 320px | обрізається | або назва села, або опис; обрано назву |
| `sheet-close` під навантаженням | блимає | таймінговий замір; окремо 3/3 зелений |
| **Android не тестувався ВЗАГАЛІ** | 0 перевірок | усе живе тестування — iPhone Вови |
| Тестовий збір **на проді** | у базі | адмінка → «Збори» → «Видалити» |
| Класифікатор новин (**B-28**) | відкрито | 94.5% статей → «Суспільство»; блокує фільтр B-14 |
| Локальний `main` у середовищі | **неспоріднена історія** | 🛑 не лікуй скиданням, джерело правди — `origin` |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

✅ **Закрито 20.08:** другий код заслінки — ЛИШАЄМО · репозиторій — поки ПУБЛІЧНИЙ
· заслінку знімаємо за день-два до виходу, за командою Вови.

1. 🔴 **Живий телефон** — свайп назад з лівої половини (і чи не зламався скрол);
   погода: тап по стрілці, свайп по рядку.
2. 🔴 **Стираємо ПД з історії git?** Відповідь дана: варто, бо репозиторій
   публічний і це ЧУЖІ дані. Чекаю «роби». ⚠️ Після перезапису Вові треба писати
   в GitHub Support — інакше старі коміти лишаться доступні за прямим посиланням.
3. **Голос бренду** — вичитати `editor/voice/olyka_castle.md` (поки чернетка Claude).
4. **Лагодимо сторож капсули?** Один рядок, поза замовленням.

## ➡️ ДАЛІ

**Черга Вови** (`_ai-tools/BACKLOG_VOVA_2026-08.md`): ✅ віджет і розділ зборів
(16-17.08) · 💬 чат Дошки — теплота `#F3EFE3`/`#FCFAF4` · 📐 спадковий відступ
контейнера Громади · 🚌 капсули проти віджета автобусів.

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) — застосунок навмисно закритий.
- 🛑 **Коди заслінки в репозиторій НЕ писати** — ні в коді, ні в журналі, ні в коміті.
- 🛑 **Знімки інструментів (`tests/tools/_out/`) НЕ комітити** — вони фотографують
  живий застосунок разом з іменами й телефонами людей. Правило вже в `.gitignore`.
- 🛑 **Порядок правил у `style/home.css`:** вузькоекранні `@media` для погоди мусять
  стояти в КІНЦІ секції, інакше програють за позицією і тихо не діють.
- 🔴 **Клавіатура на iOS** (`style/feed.css:630,971`) — два фікси провалились (PR #638, #641).
- 🔴 **Три механізми закриття** (`#article-modal` · `core/modal.js` · `core/layers.js`).
- 🔴 **`main` напряму не чіпати** — тільки через PR.
- ⚠️ **Дампи БД у репозиторій не класти** — репозиторій ПУБЛІЧНИЙ.
- ⚠️ **«Unverified» на GitHub — очікувано, ігнорувати.**
- ⚠️ **`git diff` для «що не в main» — на ДВІ крапки.**
- ⚠️ **Не піднімати стелю бюджету читання** в `tests/docs-fresh.mjs`.

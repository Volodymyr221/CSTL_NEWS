# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-26 09:58 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-5nmz43` |
| останній коміт людини | `b94e4b04` 26.08 09:58 — Merge старої гілки (вміст уже в main; розходився лише авто-HEALTH.md) |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260826-0949` |
| останній PR у `main` | #1037 |
| стендів на диску | 109 |
| незакомічених файлів | 0 ✅ |
| журнал за 2026-08-26 | є ✅ |
| потік `/byyou` | idle — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `CSTL NEWS VOVA/NOW.md`
- `admin.html`
- `bundle.js`
- `scripts/supabase_analytics_overview.sql`
- `src/core/auth.js`
- `src/core/boot.js`
- `src/core/supabase.js`
- `sw.js`

**Останні кроки (без автокомітів парсерів):**

- `b94e4b04` 26.08 09:58 — Merge старої гілки (вміст уже в main; розходився лише авто-HEALTH.md)
- `682cab2a` 26.08 09:57 — Пульс: під стелю
- `392b405c` 26.08 09:57 — Пульс: авто-блок
- `e992419f` 26.08 09:57 — Пульс: збої з іменами акаунтів
- `c3b172aa` 26.08 09:56 — Збої: імена акаунтів і копіювання одним натиском

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

📦 **26.08. Уся робота в `main` і на сайті.** `CACHE_NAME` `cstl-20260826-0914`.

🔴 **ГОЛОВНЕ ЗА ДЕНЬ — АНАЛІТИКА БРЕХАЛА, І ТЕПЕР НІ.** Усі числа були обрізані стелею
1000 рядків. «203 унікальних за 7 днів» — це насправді **6 людей**. Розбір нижче.

🟢 **Відкритих критичних немає.**

🔴 **НЕ ВИЛІКУВАНО — «блим» при виході свайпом зі спільноти.** Моя гіпотеза (два кадри
перед `pushState`) не спрацювала. Зникне в нативній обгортці або якщо взагалі не
перемикати вкладку. Вова: «хай буде так, головне що працює».

🔴 **НАЗВАНИЙ БОРГ — дві реалізації авто-каруселі** (`core/auto-carousel.js` і
`startNewsCarousel` у `community-blocks.js`). Перешкода зникла, деталі в «ДАЛІ».

🔀 **№13:** злиття ДО роботи · ніколи `-X theirs` · **після КОЖНОГО злиття
`grep -rn "^<<<<<<<"` ПЕРЕД `git add`** · 🛑 **`node --check` НЕ ловить відсутній
імпорт** — ловить лише `node build.js`.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

### 26.08: ЗБОЇ — ІМЕНА АКАУНТІВ І КОПІЮВАННЯ ОДНИМ НАТИСКОМ

🗣️ **Вова:** «щоб ці всі баги я міг скопіювати однією кнопкою… позначай імʼя акаунта, на
якому це сталося… якщо в двох акаунтів, то ще імʼя… і кількість їх».
🛑 **Скасовано попереднє СВІДОМЕ рішення:** у `logDbRefusal` стояло «пишемо номер пристрою,
а не акаунт — прив'язка зробила б журнал збоїв журналом людей». Аргумент чинний, але без
імені власник не може ні відтворити ваду, ні переслати її. 📐 Заміряно: у ВСІХ збоїв у базі
стояло **`акаунтів: 0`** — імені не було з чого взяти.
🔑 Клієнт пише акаунт через місток `setAnalyticsUid()` (прямий `currentUserId()` дав би
**кільце імпортів**) · `admin_failures()` групує за ПОЛОМКОЮ, імена — доповнення · кнопка
має **запасний шлях** (`navigator.clipboard` є лише в захищеному контексті).
⚠️ Текст на екрані й у буфері збирає ОДНА функція. Період 7 → **30 днів**.

### 26.08: АНАЛІТИКА — ЦИФРИ СТАЛИ ПРАВДИВИМИ

🔴 **Усі числа були обрізані стелею 1000** (`db-max-rows` PostgREST, клієнт підняти НЕ
МОЖЕ). 🔑 **Доказ видно без бази:** 544+164+134+82+76 = **рівно 1000** і 803+197 = **1000**.
🛑 Звідси й «усього 35 менше за 7 днів 203»: для «весь час» приходила тисяча НАЙСТАРІШИХ.

📐 **ПРАВДА:** сьогодні **1** людина (екран казав 77) · 7 днів **6** (казав 203) · весь час
**11** (казав 35). 736 «унікальних» = 11 акаунтів + 725 анонімних пристроїв, з них **628 з
рівно однією подією**. 11 акаунтів дали **74%** усіх подій.

**Зроблено:** агрегати рахує БАЗА — стеля зникла, «весь час ≥ 7 днів» стало **арифметично
неможливо порушити** · `user_id`/`anon_id`/`session_id` (історію заповнено, 0 втрат) ·
події з 4 до 8 (`session_start` з `app_mode` через **`display-mode`**, не User-Agent;
події ДІЙ у точці підтвердженого успіху) · година в поясі **Києва** · `discussions`
виявився НЕ старою вкладкою — це живі «Питання», брехала мапа назв.
🛑 **НЕ роблено свідомо:** `session_end`/тривалість (у вебі чесно не рахується) ·
retention/DAU/MAU/LIVE (правило №12 — на 11 людях шум) · UI (брехали ДАНІ, не екран).
📖 `_ai-tools/AUDIT_ANALYTICS_2026-08.md`

### 25-26.08: ВІДЖЕТ СТРІЧКИ НА ГРОМАДІ (шість редакцій, #1032-#1036)

Порядок спільнот **за свіжістю** · ряд **на всю ширину** (55→0px) · картка **280→400px** ·
виділення **ГАСІННЯМ РЕШТИ** · назви скорочуються по лапках і комі · **тап веде в САМУ
спільноту**, з допису — з наведенням.

🛑 **П'ЯТЬ УРОКІВ:** (1) у flex-колонці все, що має показатись ЦІЛКОМ, потребує
`flex-shrink: 0` — текст різало посеред літер, бо різала ВИСОТА, а не `line-clamp`;
(2) висоту тексту не задавати в `px` із номінального `line-height` — тільки `em`;
(3) знімок довантажується ПІСЛЯ прокрутки і підштовхує картку — чекати з МЕЖЕЮ ЧАСУ;
(4) системний жест «назад» малює ЗНІМОК попереднього запису історії — 🔴 **«блим» НЕ
вилікувано**, зникне в нативній обгортці або якщо не перемикати вкладку;
(5) **обґрунтування переживає свою підставу** — коментар «глибокого переходу немає
навмисно» був правдою для СТАРОГО віджета. ➡️ Перевіряй не лише код, а й **ЧОМУ**.

🔴 **СІМ ПОМИЛОК ВИМІРУ** — жодну не знайшло написання, усі контроль або скрін Вови.
🛑 Дві перевірки наведення **зелені й на коді ДО фікса** — названо в стенді.
➡️ **Наведення і «блим» — ЛИШЕ пальцем.** `home-feed-widget.mjs` **39/39**. 📖 `-25e`, `-26`.

### 25.08: заслінка (#1030) · пункт 0 — гарантія «хто я» (#1029)

🔴 **Заслінка:** «з телефона код підходить, а з компʼютера ні» — код був ПРАВИЛЬНИЙ:
сторінка по `http://`, а `crypto.subtle` є лише в захищеному контексті. 🛑 Вада не в
замку, а в тому, що він КАЗАВ: «помилився» і «не можу перевірити» давали однакову кару.
🔴 **Пункт 0:** `initAuth()` без `await` → вкладки малювали вигляд ГОСТЯ. Найгірше —
самолікування push в Автобусах крутилось вхолосту, підписка лишалась мертвою МОВЧКИ.
📖 `-25c`, `-25d`.

### Раніше 25.08 (PR #1001-#1026)

Непрочитане (#1023, #1025) · голос спільноти (#1017) · нічна тиша (#1018-#1019, **B-33
закрито**) · `authReady()` у Стрічці (#1020-#1022) · редагування свого питання (#1026).
⚠️ Порядок у `board.js`: `markQaFloorOnce()` ПЕРЕД `markChatSeen()`.
🔴 **Розбір — за адресою:** `SESSION_STATE_VOVA.md` і журнали `-23b`…`-25e`.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| 🔴 **Дві реалізації авто-каруселі** + віджет **не бачений пальцем** | борг у шапці `core/auto-carousel.js` | новини не переведені (файл писала паралельна сесія); скрін не долетів |
| Тип 4: перше спрацювання | нічого не підійшло під вікно | **27.08**, не 26.08 (питанню буде 21 год при потрібних 24) |
| `CSTL_BUGS.md` = 40.5 KB при стелі 40 KB | запасу **0.5 KB** | наступний баг — стисло або виносити історію |
| Стеля «5 push із Питань на добу» (§12) | **в коді немає** | 24-25.08 заміряно по **6** — спрацювала б двічі |
| Префи автобусів · batching · категорії питань | не робили | підписок нема; потрібен планувальник; без категорій тип 4 не має на чому стояти |
| **Android не тестувався ВЗАГАЛІ** | 📌 відкладено рішенням Вови | «Протестуємо коли доробимо все» |
| Класифікатор новин (**B-28**) | відкрито | ~94% статей → «Суспільство»; блокує B-14 |
| ПД в ІСТОРІЇ git | не стерто | перезапис історії ламає посилання на коміти |
| Тестовий збір **на проді** | у базі | адмінка → «Збори» → «Видалити» |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

✅ **Перевірено пальцем 24-26.08:** новини · перемикання акаунта · видалення коментаря ·
«Вийти» · push · сортування · **вимикачі (B-33)** · віджет Стрічки (шість редакцій).

0. 🔴 **НЕ ПЕРЕВІРЕНЕ ПАЛЬЦЕМ — гарантія «хто я» (холодний старт, PWA):** крапка
   непрочитаного ОДРАЗУ · push веде в чат · збережені позначені · ❤️ зафарбоване ·
   привітання з ІМЕНЕМ · ТВІЙ рейс · ⚠️ **Автобуси малюють розклад МИТТЄВО**.
0-БІС. 🔴 **Чи відкривається `https://castlelife.org`** — з контейнера не перевірити.
1. 🔴 **Тип 4 — 27.08:** питанню на завтра буде 21 год при потрібних 24.
   ⚠️ У базі ОДНА push-підписка на всіх, а автор питання з розсилки випадає завжди.
2-6. **Нічна тиша · антиспам · непрочитане · редагування питання · голос спільноти** —
   сценарії в журналах `-25b/-25c` і `SESSION_STATE_VOVA.md`.
7. **Google-вхід:** +1 екран · 8. **ПД в історії git** — стираємо? · 9. Ключ
   `cstl-olyka-agent` — відкликати. 📌 **Android** — «протестуємо коли доробимо все».

## ➡️ ДАЛІ

🔴 **ПЕРШЕ — прибрати названий борг:** перевести карусель новин (`startNewsCarousel` у
`tabs/community-blocks.js`) на спільний `core/auto-carousel.js`. ⚠️ Звіряй рядок у рядок:
у старій є запобіжники, куплені окремими багами.

✅ **Пункт 0 закрито 25.08 (#1029) — не бери його вдруге.**

**Далі за чергою Вови:** 💬 чат Дошки `#F3EFE3`/`#FCFAF4` · 📐 відступ Громади ·
🚌 капсули проти автобусів. _(Android — у самому кінці.)_

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** · 🛑 **коди заслінки в репозиторій НЕ писати** · 🛑 знімки
  `tests/tools/_out/` НЕ комітити (імена й телефони) · 🔴 **`verify_jwt` у
  `send-bus-push` — УВІМКНЕНИЙ** (деплой із `false` тихо зняв би перший рубіж).
- 🔴 **TEXT проти `uuid`:** `reactions.user_id`, `push_subscriptions.user_uuid` — TEXT;
  перед `.in('uid', …)` **обовʼязковий фільтр форми UUID** — один сторонній рядок глушить
  УСІ сповіщення теми. · **`upsert` без `.select()`** (№11-БІС) · **SELECT-політика на
  свої рядки** · **кирилиця в назвах змінних bash** = код 127 · **`Одеради АЗ` ≠
  `Одеради`** · **`@media` погоди — у КІНЦІ `style/home.css`**.
- 🔴 **Клавіатура на iOS** (`feed.css:630,971`) — два фікси провалились (#638, #641) ·
  **`main` напряму не чіпати** · **дампи БД не класти** (репо ПУБЛІЧНИЙ) ·
  **`serviceWorker.ready` без SW не виконується НІКОЛИ** · 🛑 **`pg_cron` живе в UTC**
  (літній час звіряти в коді; секрет у текст розкладу не вписувати).
- 🛑 **НЕ МІРЯЙ ПОСЕРЕД РОБОТИ** — файлів не чіпати, поки йде повний набір (спіймано
  24.08 і знову 25.08) · ⚠️ **«Unverified» — очікувано** · **`git diff` «що не в main» —
  на ДВІ крапки** · **стелю бюджету в `docs-fresh` НЕ піднімати**.
- 🔑 Решта гострого — `HOT_RULES.md` (14 правил) і `CSTL_BUGS.md`. Тут лише те, що
  ламали НЕЩОДАВНО.

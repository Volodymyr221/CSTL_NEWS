# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-25 17:09 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-upyy76` |
| останній коміт людини | `a675cc93` 25.08 17:09 — Пульс: авто-блок після злиття передачі |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260825-1513` |
| останній PR у `main` | #1027 |
| стендів на диску | 108 |
| незакомічених файлів | 0 ✅ |
| журнал за 2026-08-25 | є ✅ |
| потік `/byyou` | idle — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `CSTL NEWS VOVA/NOW.md`

**Останні кроки (без автокомітів парсерів):**

- `a675cc93` 25.08 17:09 — Пульс: авто-блок після злиття передачі
- `883a0bef` 25.08 17:09 — Merge remote-tracking branch 'origin/main' into claude/startuem-upyy76
- `b71edd8b` 25.08 17:09 — docs: передача в новий чат — 25.08.2026 (#1027)
- `ddab03a4` 25.08 17:08 — Пульс: авто-блок після коміту передачі
- `4771781e` 25.08 17:07 — docs: передача в новий чат — 25.08.2026

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

📦 **ПЕРЕДАЧА 25.08, 17:00 UTC. Уся робота вже в `main` і на сайті** — гілка й `main`
збігаються, незакомічених нуль, чекати нічого. Останній деплой **#5073**.
🏁 **ШІСТЬ СЬОГОДНІШНІХ СЕСІЙ:** «Питання» (#1013-#1016) · голос спільноти (#1017) ·
нічна тиша (#1018-#1019) · `authReady()` і перемикач голосу (#1020-#1022) · непрочитане
(#1025) · редагування й видалення питання (#1026). 🟢 **Відкритих критичних немає.**
➡️ **Наступному чату:** починай із «ДАЛІ» внизу — там пункт 0 беклогу. Нової роботи в
черзі немає; є речі, які доводяться лише пальцем (розділ «ВІДКРИТІ ПИТАННЯ»).

🔴 **ОБРОБНИК НЕ ДІСТАЄ ТУДИ, КУДИ ЗДАЄТЬСЯ** (25.08): делегати на `document` **не
спиняють поширення**, а `wireCards()` не дістає до листа коментарів (він окремим шаром у
`body`). 🔀 **HOT_RULES №13**: злиття ДО роботи · ніколи `-X theirs/-X ours` · «а чи є в
`main` ЧУЖЕ» — ПЕРША дія при злитті.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

### 25.08, сесія «c»: голос спільноти · «хто я» як ФАКТ (#1017 · #1020 · #1022)

🔑 **Голос спільноти:** `author_uid` — ЗАВЖДИ людина, що натиснула «Надіслати»;
`as_page_id` — від чийого імені це ЗВУЧИТЬ. 🛑 `actor_type`/`actor_id` (порада ChatGPT)
ВІДХИЛЕНО: без сліду людини сліпне модерація, а антиспам, видалення, згадки, капсули і
push ключуються на `author_uid`. **Default — ОСОБИСТИЙ.**
🔴 **«Не можу писати пости» + «лайк не підсвічений» + «немає перемикача» — ОДНА ВАДА:**
застосунок малював залежне від входу, **не знаючи ще, хто зайшов** (`initAuth()` без
`await`). 🛑 Слово Вови: **«професійно та комплексно, а не як латка на латці»**.
🔑 Гарантія тепер в `auth.js`: **`authReady()`**, з обовʼязковою межею часу.
🔴 Заразом: смуга «Відповідати як» стала ТРЕТІМ поверхом під клавіатурою → **прибрав
поверх**, а не лагодив `keyboard.js`. Деплої **#5053 · #5058 · #5063**. Розбір —
журнал `-25b`.

### 25.08, шоста сесія: автор редагує й видаляє своє питання

🔴 **Скарга:** «написав питання — не може ні редагувати, ні видалити». 📐 Заміряно: це НЕ
діра, а стан за замовчуванням — у `posts` жодного `update`/`delete` з клієнта, а в базі
дві політики запису, обидві `is_admin()`.
🔑 **RPC, а не відкрита UPDATE-політика** (як для оголошень): `using (owner_uid =
auth.uid())` дозволила б змінити БУДЬ-ЯКЕ поле — `status` (обхід модерації), `type`,
`owner_uid`. Клас **B-23**. 🛑 **Редагування проходить антиспам** — урок **B-26**.
🔴 **Діра, якої не шукав:** мʼяке видалення ховало питання, а ВІДПОВІДІ читались далі —
`post_visible()` про `deleted_at` не знала, і на неї спираються ТРИ дочірні політики.
Правка одна, всередині функції. Заміряно: ДО «1 · 2» → ПІСЛЯ «0 · 0».
🛑 **Стенд знайшов дві вади:** мітка «змінено» не показувалась тому, хто щойно правив, і
хибний відмінок. ⚠️ Фікстура тепер ховає `deleted_at` — заглушка не сміє бути добрішою за
прод. **13/13**, контроль **2/13**. Стендів **108**.

### 25.08, пʼята сесія: непрочитане в «Питаннях» стало видимим

🔴 **Скарга:** «вкладка горить, відкриваю — незрозуміло, що саме». **Крапка не брехала** —
заміряно: питання №84, мітка 14:03:57, чужа відповідь 14:04:12. Бракувало ДРУГОЇ третини:
крапка знала, яке питання нове, а список не казав нічим. 🔑 **Модель Вови:** `new` (не
відкривав) бачать УСІ · `answers` — ЛИШЕ автор і хто натиснув «цікавить» (ЗВУЖЕННЯ: досі
світилась у кожного, хто зазирнув). 🔑 **Одне джерело правди `questionUnread()`** (B-27).
🔴 **Три речі знайшов СТЕНД:** зсув 3px · підсвітка не знімалась · капсула замовкла б
для новачка.

🔴 **ДРУГА РЕДАКЦІЯ ТОГО САМОГО ВЕЧОРА:** крапка горіла, тап по вкладці — і підсвітка
згасла, НЕ ВСТИГНУВШИ показатись. Поріг ставився на `Date.now()` при вході, заднім числом
роблячи свіже питання «історією». Тепер поріг успадковує останній візит (ключ **v2**), а
🔑 **гасне все на ВИХОДІ з вкладки**: «я дивлюся, читаю їх, і коли переходжу в іншу
вкладку, тоді вони згасають». ⚠️ Порядок у `board.js` — частина поведінки:
`markQaFloorOnce()` ПЕРЕД `markChatSeen()`. **19/19**, два мутанти → 18/19 і 16/19.
🛑 Стенд брехав: перемикався на вкладку `home`, якої НЕМАЄ (вона `community`).

### Раніше 25.08 (PR #1001-#1019)

🔴 **Нічна тиша 22:00–08:00** (`send-answer-push` **v6**, типи 1 і 3; тип 2 навмисно ні).
🔑 Уночі не пишемо в `answer_push_targets`, тому вихід стоїть ДО `mark()`. ⚠️ Вікно
перетинає північ → умова через `||`. 🟢 **B-33 ЗАКРИТО**.
🔴 **Решта — `SESSION_STATE_VOVA.md` і журнали `-23b/-23c/-24/-24b/-25b`.** Навмисно
адреса, а не переказ: друга копія списку розходиться з першою.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| Тип 4: перше спрацювання | 25.08 нічого не підійшло під вікно | **27.08**, не 26.08 (завтра питанню 21 год при потрібних 24) |
| `CSTL_BUGS.md` = 40.5 KB при стелі 40 KB | запасу **0.5 KB** | наступний баг — стисло або виносити історію |
| Стеля «5 push із Питань на добу» (§12) | **в коді немає** | 24 і 25.08 заміряно по **6** — стеля спрацювала б двічі |
| Гілка префів в автобусах · batching · категорії питань | не робили | підписок нема; потрібен планувальник; без категорій персоналізація типу 4 не має на чому стояти |
| **Android не тестувався ВЗАГАЛІ** | 📌 відкладено рішенням Вови | «Протестуємо коли доробимо все» |
| Класифікатор новин (**B-28**) | відкрито | ~94% статей → «Суспільство»; блокує B-14 |
| ПД в ІСТОРІЇ git | не стерто | перезапис історії ламає посилання на коміти |
| Тестовий збір **на проді** | у базі | адмінка → «Збори» → «Видалити» |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

✅ **Перевірено пальцем 24-25.08:** новини · перемикання акаунта · видалення коментаря ·
«Вийти» · прохід push · лінія, сортування · **вимикачі (B-33)**.

1. 🔴 **Тип 4 — чекати 27.08, НЕ 26.08.** Спроєктовано на дані: питання «НЕ ВІДПОВІДАТИ»
   (написане 14:05) на завтрашній прохід матиме **21 год** при потрібних 24 — не вистачає
   трьох. Післязавтра буде 45 год, тобто підійде, якщо ніхто не відповість.
   ⚠️ І окремо: у базі **одна** push-підписка на всіх, тож лист адресатів буде з однієї
   людини — а автор питання з розсилки випадає завжди.
2. 🆕 **Нічна тиша:** відповідь після 22:00 → push НЕ приходить; вранці перший
   push каже «N нових відповідей», а не одну.
3. 🆕 **Антиспам:** «Окей» одразу вдруге → блокує · через 20с → пропускає.
4. 🆕 **Непрочитане:** нове питання підсвічене ВСІМ · нова відповідь — лише авторові або
   хто натиснув «цікавить» · зазирнув у чуже без «цікавить» → мовчить · **вхід у вкладку
   НЕ гасить, гасне на виході**.
5. 🆕 **Редагувати/видалити своє питання:** «⋯» у шапці · мітка «змінено» · видалення
   попереджає числом відповідей · на ЧУЖОМУ питанні «⋯» немає.
6. 🆕 **Голос спільноти:** відповідь як спільнота · бейдж «Адмін» · push каже назву
   спільноти, а не імʼя.
7. **Вхід через Google:** +1 екран · 8. **ПД в історії git** — стираємо? · 9. Ключ
   `cstl-olyka-agent` — відкликати.

📌 **Android — «Протестуємо коли доробимо все».** Свідомо відкладений крок.

## ➡️ ДАЛІ

🔴 **ПЕРШЕ ЗАВДАННЯ НОВОЇ СЕСІЇ — `BACKLOG_VOVA_2026-08.md` ПУНКТ 0:** «гарантія «хто я»
на всі вкладки». 🔑 Не нова фіча: `authReady()` написаний і перевірений виконанням, але
застосований **лише до Стрічки** — решта вкладок можуть мигнути виглядом гостя на
холодному старті. 🛑 Не одним заходом: одна вкладка = один крок = один вимір.

**Далі за чергою Вови:** 💬 чат Дошки `#F3EFE3`/`#FCFAF4` · 📐 відступ Громади ·
🚌 капсули проти автобусів. _(Android — у самому кінці.)_

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) · 🛑 **коди заслінки в репозиторій НЕ писати**.
- 🛑 **Знімки `tests/tools/_out/` НЕ комітити** (імена й телефони) · 🔴 **`verify_jwt` у
  `send-bus-push` — УВІМКНЕНИЙ** (деплой із `false` тихо зняв би перший рубіж).
- 🔴 **TEXT проти `uuid`:** `reactions.user_id` і `push_subscriptions.user_uuid` — TEXT;
  перед `.in('uid', …)` **обовʼязковий фільтр форми UUID** — один сторонній рядок глушить
  УСІ сповіщення теми. · 🛑 **Персональні сповіщення вимикача НЕ мають** · **`upsert` без
  `.select()`** (№11-БІС) і **SELECT-політика на свої рядки** · **кирилиця в назвах
  змінних bash** падає з кодом 127 · **`Одеради АЗ` ≠ `Одеради`** · **`@media` погоди —
  у КІНЦІ `style/home.css`**.
- 🔴 **Клавіатура на iOS** (`feed.css:630,971`) — два фікси провалились (#638, #641) ·
  **Три механізми закриття** · **`main` напряму не чіпати** · **дампи БД у репозиторій не
  класти** (ПУБЛІЧНИЙ) · **`serviceWorker.ready` без SW не виконується НІКОЛИ**.
- 🛑 **`pg_cron` живе в UTC і про літній час не знає** — час доби звіряти в коді; **секрет
  у текст розкладу не вписувати** (читати з `app_secrets` у мить виклику). · 🔴 **Обробник
  не дістає туди, куди здається** — розбір угорі, в «ЗАРАЗ».
- 🛑 **НЕ МІРЯЙ ПОСЕРЕД РОБОТИ:** файлів не чіпати, поки йде повний набір (24.08 три
  прогони з чотирьох), і не ганяти стенди на незавершеному мерджі (25.08) · ⚠️
  **«Unverified» — очікувано** · **`git diff` «що не в main» — на ДВІ крапки** ·
  **стелю бюджету в `docs-fresh` не піднімати**.

# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-25 14:04 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-upyy76` |
| останній коміт людини | `c272d152` 25.08 13:56 — Merge remote-tracking branch 'origin/main' into claude/startuem-upyy76 |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260825-1330` |
| останній PR у `main` | #1021 |
| стендів на диску | 107 |
| незакомічених файлів | 5 ⚠️ |
| журнал за 2026-08-25 | є ✅ |
| потік `/byyou` | idle — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `bundle.js`
- `src/tabs/board-discussions.js`
- `src/tabs/board.js`
- `src/tabs/home-caps.js`
- `style/board.css`
- `sw.js`
- `tests/qa-unread.mjs`
- `tests/tab-dots.mjs`

**Останні кроки (без автокомітів парсерів):**

- `c272d152` 25.08 13:56 — Merge remote-tracking branch 'origin/main' into claude/startuem-upyy76
- `ed399846` 25.08 13:32 — Питання: непрочитане стало видимим у списку, а не лише крапкою
- `fa9d1424` 25.08 13:09 — Журнал сесії: третя скарга і перехід на гарантію authReady (#1021)
- `32071c27` 25.08 13:08 — Merge remote-tracking branch 'origin/main' into claude/startuem-upyy76
- `b673b714` 25.08 12:47 — Вхід: «хто я» стає фактом до першого показу + вибір голосу без зайвого поверху (#1020)

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

🏁 **ПʼЯТЬ СЬОГОДНІШНІХ СЕСІЙ ЗАКРИТІ:** «Питання» доведено (#1013-#1016) · голос
спільноти у «Стрічці» (#1017) · нічна тиша (#1018-#1019) · `authReady()` (#1020-#1021)
· непрочитане в «Питаннях». 🟢 **Відкритих критичних немає.** Далі — **не нова
логіка, а підготовка до запуску**.

🔀 **HOT_RULES №13** (робота вдвох): злиття ДО роботи · ніколи `-X theirs/-X ours` ·
«а чи є в `main` ЧУЖЕ» — ПЕРША дія при злитті.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

### 25.08, пʼята сесія: непрочитане в «Питаннях» стало видимим

🔴 **Скарга:** «вкладка горить, відкриваю — незрозуміло, що саме». **Крапка не брехала**
— заміряно в базі: питання №84, мітка 14:03:57, чужа відповідь 14:04:12. Бракувало
ДРУГОЇ третини: крапка знала, яке питання нове, а список не казав нічим (текст тієї
відповіді був на картці надрукований).

🔑 **Модель Вови:** `new` (не відкривав) бачать УСІ · `answers` (нова чужа відповідь) —
ЛИШЕ автор і хто натиснув «цікавить». 🛑 Це ЗВУЖЕННЯ: досі нова відповідь світилась у
кожного, хто зазирнув — один тап із цікавості підписував назавжди. Набір тепер той
самий, що в push типів 1 і 3.

🔑 **Одне джерело правди `questionUnread()`** — крапка, картка і капсула лише питають
його (B-27). Прочитано в момент ВІДКРИТТЯ. Капсула: число + текст найновішого, тап веде
в питання; прочитав → випало. Слайдер відхилено — свайп усередині капсули конфліктував
би з прокруткою смуги. **Вхід у вкладку більше нічого не гасить** — це й був корінь.

🔴 **Три речі знайшов СТЕНД, а не я:** зсув 3px (список тремтів би) · підсвітка не
знімалась після відкриття · капсула замовкла б для новачка. **15/15**, контроль 9/15,
**мутант «зазирнув = підписаний» → 14/15**. Стендів **107**.

### 25.08, четверта сесія: «хто я» стає ФАКТОМ, а не перегонами

🔴 **«Не можу писати пости» + «лайк не підсвічений» + «немає перемикача» — ОДНА ВАДА,
архітектурна:** застосунок малював залежне від входу, **не знаючи ще, хто зайшов**.
`initAuth()` без `await` → `currentUserId()` = `null` → пʼять запитів із восьми в
`loadData()` чесно відповідали про ГОСТЯ; лікувалось поверненням на вкладку — звідси
«другий чи третій раз». 🛑 Слово Вови: «професійно та комплексно, а не як латка на
латці» — підписка на зміну входу лікує ПІСЛЯ факту, тобто блимне чуже.
🔑 **Гарантія в одному місці — `auth.js`, `authReady()`:** «хто я» стає відповіддю
(людина / гість / збій), споживач чекає її РАЗ до першого читання. ⚠️ Межа часу обовʼязкова — клас
`serviceWorker.ready` (не падає, ВИСНЕ); `settleAuth()` стоїть ПІСЛЯ `catch`, бо «не
змогли дізнатись» теж відповідь. Обидва механізми лишаються: `authReady()` — правдивий
ПЕРШИЙ показ, `onAuthChange()` — після СПРАВЖНЬОЇ зміни.
✅ Доведено ВИКОНАННЯМ: сесія → uid за 0мс · завислий виклик → 401мс. **15/15**,
контроль **4/15**.
🔴 **«Поле вводу не піднімається»** — моя ж перша редакція: смуга стала ТРЕТІМ
поверхом під клавіатурою. 🛑 Не лагодив `keyboard.js` — **прибрав поверх**: вибір
голосу на обличчі біля поля. **Стос 94px → 94px.**

### 25.08, третя сесія: нічна тиша + документи звірені з кодом

🔴 **НІЧНА ТИША 22:00–08:00** (`send-answer-push` **v6**, типи 1 і 3; тип 2 «відповіли на
ваш коментар» її не має навмисно — §12, особисте звернення). 🔑 **Пропущене не губиться:**
уночі не пишемо в `answer_push_targets`, а `missed()` рахує від останнього запису —
ранковий push САМ скаже «3 нові». Тому вихід стоїть ДО `mark()`: після нього тиша стала
б ВТРАТОЮ.
⚠️ Вікно перетинає північ → умова через `||`; через `&&` тиша дала б **нуль** годин.
✅ Живі виклики: чужий секрет → **401**, справжній + неіснуючий коментар → **200 `answer
gone`**. 🆕 Сторож `night-silence.mjs` **18/18**, мутант `||`→`&&` → 10/14. 🟢 **B-33
ЗАКРИТО**. 📄 Документи звірені з кодом; знахідка: `tests/README.md` описував 43 стенди
зі 105 при зелених прогонах.

### 25.08, друга сесія: голос спільноти у «Стрічці» (#1017)

🔑 `author_uid` — ЗАВЖДИ людина, що натиснула «Надіслати»; `as_page_id` — від чийого імені
це ЗВУЧИТЬ. 🛑 **`actor_type`/`actor_id` (ChatGPT) ВІДХИЛЕНО:** без сліду людини сліпне
модерація, а антиспам, видалення, згадки, капсули і push ключуються на `author_uid`.
🛑 Default — ОСОБИСТИЙ. 🔴 Три витоки закрито · бейдж «Адмін» → `page_team_flags()`.

### Раніше 25.08 (PR #1001-#1016)

🔴 **Розбір — `SESSION_STATE_VOVA.md` і журнали `-23b/-23c/-24/-24b`.** Тут навмисно
адреса, а не переказ: друга копія списку розходиться з першою.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| Тип 4: перше справжнє спрацювання | 25.08 нічого не підійшло під вікно 24год–7діб | чекаємо 26.08, 11:00; у базі всього **одна** push-підписка |
| `CSTL_BUGS.md` = 40.5 KB при стелі 40 KB | запасу **0.5 KB** | наступний баг писати стисло або виносити історію |
| Стеля «5 push із Питань на добу» (§12) | **в коді немає** | немає для ЖОДНОГО типу; тип 4 тримає себе межею «раз на 3 доби» |
| Гілка префів в автобусах · відкладений push (batching) | не робили | підписок нема; потрібен планувальник |
| Категорії питань | не робили | без них персоналізація типу 4 не має на чому стояти |
| **Android не тестувався ВЗАГАЛІ** | 📌 відкладено рішенням Вови | «Протестуємо коли доробимо все» |
| Класифікатор новин (**B-28**) | відкрито | ~94% статей → «Суспільство»; блокує B-14 |
| ПД в ІСТОРІЇ git | не стерто | перезапис історії ламає посилання на коміти |
| Тестовий збір **на проді** | у базі | адмінка → «Збори» → «Видалити» |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

✅ **Перевірено пальцем 24-25.08:** синхронізація новин · перемикання акаунта · видалення
коментаря · «Вийти» · прохід push · відповіді, лінія, сортування · **вимикачі (B-33)**.

1. 🔴 **Тип 4: чи прийде 26.08 об 11:00?** 25.08 не спрацював НЕ через ваду: розклад
   пройшов двічі, журнал порожній — під вікно 24год–7діб не потрапило жодне питання.
2. 🆕 **Нічна тиша:** відповідь після 22:00 → push НЕ приходить; вранці перший
   push каже «N нових відповідей», а не одну.
3. 🆕 **Антиспам:** «Окей» одразу вдруге → блокує · через 20с → пропускає.
4. 🆕 **Непрочитане в Питаннях:** нове питання сусіда підсвічене всім · нова
   відповідь — лише тобі як автору або якщо натиснув «цікавить» · зазирнув у чуже
   без «цікавить» → мовчить · відкрив → підсвітка зникла ОДРАЗУ, решта лишилась.
5. 🆕 **Голос спільноти:** відповідь як спільнота · бейдж «Адмін» на ОСОБИСТІЙ
   відповіді · перемикача немає з другого акаунта · push каже назву спільноти.
6. **Вхід через Google:** +1 екран. Заважає — відкотимо одним рядком.
7. **ПД в історії git** — стираємо? Чекаю «роби». · 8. Ключ `cstl-olyka-agent` — відкликати.

📌 **Android — рішення Вови: «Протестуємо коли доробимо все».** Свідомо відкладений крок.

## ➡️ ДАЛІ

**Великі потоки закриті**, відкритих критичних немає. Далі — **не нова логіка, а
підготовка до запуску**: заслінка і вміст Дошки. _(Android — в кінці.)_
**Черга Вови** (`_ai-tools/BACKLOG_VOVA_2026-08.md`): 💬 теплота чату Дошки
`#F3EFE3`/`#FCFAF4` · 📐 відступ Громади · 🚌 капсули проти автобусів.

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) · 🛑 **коди заслінки в репозиторій НЕ писати**.
- 🛑 **Знімки `tests/tools/_out/` НЕ комітити** (імена й телефони людей) · 🔴 **`verify_jwt`
  у `send-bus-push` — УВІМКНЕНИЙ** (деплой із `false` тихо зняв би перший рубіж).
- 🔴 **TEXT проти `uuid`:** `reactions.user_id` і `push_subscriptions.user_uuid` — TEXT;
  перед `.in('uid', …)` **обовʼязковий фільтр форми UUID** — один сторонній рядок глушить
  УСІ сповіщення теми.
- 🛑 **Персональні сповіщення вимикача НЕ мають** · **`upsert` без `.select()`** (№11-БІС)
  і **SELECT-політика на свої рядки** · **кирилиця в назвах змінних bash** падає з кодом
  127 · **`Одеради АЗ` ≠ `Одеради`** · **`@media` погоди — у КІНЦІ `style/home.css`**.
- 🔴 **Клавіатура на iOS** (`style/feed.css:630,971`) — два фікси провалились (#638, #641)
  · **Три механізми закриття** · **`main` напряму не чіпати** · **дампи БД у репозиторій
  не класти** (ПУБЛІЧНИЙ) · **`serviceWorker.ready` без SW не виконується НІКОЛИ**.
- 🛑 **`pg_cron` живе в UTC і про літній час не знає** — час доби звіряти в коді.
  І **секрет у текст розкладу не вписувати**: читати з `app_secrets` у мить виклику.
- 🔴 **ОБРОБНИК НЕ ДІСТАЄ ТУДИ, КУДИ ЗДАЄТЬСЯ** — дві знахідки 25.08: делегати на
  `document` **не спиняють поширення** (один тап відкрив ДВОЄ), а `wireCards()` висить
  на контейнері стрічки, тоді як лист коментарів живе окремим шаром у `body` — `data-*`
  гачки картки там **не працюють**. Нова дія — перевір обидва.
- 🛑 **Поки повний набір стендів іде — файлів НЕ чіпати** (24.08 три прогони з чотирьох
  були недостовірні через це) · ⚠️ **«Unverified» на GitHub — очікувано** · **`git diff`
  «що не в main» — на ДВІ крапки** · **стелю бюджету в `docs-fresh` не піднімати**.

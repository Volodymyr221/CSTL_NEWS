# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-17 07:10 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-tt38jj` |
| останній коміт людини | `0d54a2f4` 17.08 07:10 — docs: сторожі знову зелені — 82/82, обидві знахідки закриті |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260817-0830` |
| останній PR у `main` | #941 |
| стендів на диску | 82 |
| незакомічених файлів | 0 ✅ |
| журнал за 2026-08-17 | є ✅ |
| потік `/byyou` | done — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `CSTL NEWS VOVA/NOW.md`
- `_session-log/vova-2026-08-17.md`
- `style/fund-screen.css`
- `sw.js`
- `tests/sidebar-ghost-tap.mjs`
- `tests/sidebar-overlay.mjs`

**Останні кроки (без автокомітів парсерів):**

- `0d54a2f4` 17.08 07:10 — docs: сторожі знову зелені — 82/82, обидві знахідки закриті
- `88b95aa0` 17.08 06:48 — fix(fund): екран «Збори» не їде вбік від довгого посилання в описі
- `294ef99b` 17.08 06:26 — fix(tests): sidebar-overlay 25/36 → 36/36 — тап по елементу замість координат
- `d0b92c99` 17.08 06:17 — docs: розслідування червоних сторожів — що доведено і три хибні гіпотези
- `717d53da` 17.08 06:09 — feat(fund): розділ «Збори» — заявки від жителів і створення з адмінки (#941)

<!-- AUTO:END -->---

## 🎯 ЗАРАЗ

✅ **Розділ «ЗБОРИ» зроблено повністю (17.08).** Збори переїхали з файлу в
Supabase, зʼявився розділ у бургер-меню («Розділи» → Збори), форма заявки для
жителів і розділ «Збори» в адмінці: заявки + створення/редагування зборів.

🔑 **Як це працює:** житель подає заявку → Вова бачить її в адмінці з телефоном →
дзвонить → «Схвалити» однією кнопкою створює збір на головній. Позначку
«перевірено» треба ставити ОКРЕМО. Нічого не публікується автоматично.

🔴 **Зібраної суми, відсотка і «донатів» на картці немає** — рішення Вови 16.08:
*«якщо суму не можемо просити напряму, то її краще прибрати»*.

⚠️ **На проді лишається ТЕСТОВИЙ збір** (уже в базі, не у файлі) — прибрати або
замінити реальним. Тепер це робиться з адмінки: розділ «Збори» → «Видалити».

**Повний аудит застосунку зроблено і задеплоєно 16.08**
(PR **#935**, деплой **v4498 · 16.08 09:23**). Розбір — `_ai-tools/AUDIT_FULL_2026-08-15.md`,
план і підсумок — `_ai-tools/BYYOU_PLAN.md` (статус `done`).

✅ **СПОВІЩЕННЯ ПРО АВТОБУС ПРАЦЮЮТЬ — ПІДТВЕРДЖЕНО ВОВОЮ НАЖИВО 16.08**
(«Є працює»). Ланцюг закритий наскрізь: тап → рядок у базі → cron → Web Push →
сповіщення на iPhone. До цього дня таблиця `push_subscriptions` мала **0 рядків
за весь час** — функція не працювала ніколи, і сторожі цього не бачили.

🔴 **ЧЕКАЄ ЖИВОЇ ПЕРЕВІРКИ НА iPhone** (список у звіті сесії): автобусні
сповіщення при пізній підписці · скасування рейсу з другого пристрою ·
сповіщення про відповідь на питання · швидкість відкриття · масштабування тексту
двома пальцями. **Спершу закрити застосунок повністю** — потрібен новий SW.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

_16.08, потік `/byyou` — повний аудит застосунку._

🔑 **Головне відкриття аудиту:** чотири місця, де сповіщення **тихо не
приходять**, жили при **80 зелених стендах**. Сторожі стерегли вигляд, розкладку
і рух, а життєвий цикл push-підписки не перевіряв ніхто. Тепер є
`tests/push-subscription.mjs` (**21/21**, контроль на коді до фіксу → **2/21**).

**Сповіщення й автобуси:** повторне відстеження більше не тягне старі зупинки й
прапорці (`insert` → `upsert`) · скасування діє на АКАУНТ, а не на один браузер
(був відтворюваний сценарій «скасував на планшеті — push летить на телефон, а
рейс повертається у список») · подвійний тап не лишає висячої підписки (черга на
рейс) · ротація push-адреси більше не вбиває сповіщення мовчки
(`pushsubscriptionchange` + звірка при старті) · попередження про автобус більше
не губиться при пізній підписці (вікно 13-17 → **2-17 хв** із фактичним числом
хвилин) · теги розрізняють дату й сегмент · розклад із `castlelife.org`.

**🆕 Сповіщення про ВІДПОВІДЬ НА ПИТАННЯ** — його не існувало взагалі: на
`comments` не було жодного тригера, тобто цикл «запитав громаду → тобі
відповіли» був розірваний. Накатано `answer_push_log` + `notify_new_answer` +
Edge Function `send-answer-push` (v1).

**Швидкість:** заставка **4.1с → ~0.7-1.1с** (зникає за готовністю кадру, не за
розкладом) · `bundle.js` **957 → 638 КБ** (мініфікацію було вимкнено) ·
фонове фото автобусів **1567 → 18.5 КБ** (WebP, різниця заміряна 0.52%) ·
установка кешу не валиться через один файл · полагоджено витік слухача `resize`
і серіалізовано лайки.

**Доступність:** усі поля вводу піднято до 16px (нижче цього iOS сам зумить
сторінку при фокусі). 🔴 **Заборону масштабування я знімав — і Вова її повернув**
(розбір у `START_HERE.md`, розділ «РІШЕННЯ ВОВИ 10.08»): жест зумить не лише
текст, а й шапку з таб-баром, і застосунок виїжджає за межі екрана. Рішення
власника було записане ще 10.08, я його порушив, не звірившись. Тепер стереже
окрема перевірка.

🛑 **ДВА УРОКИ ПРО МІРКУ, обидва спіймані до деплою:**
**(1)** мій же фікс заставки зробив червоним стенд `notif-backstack` — він чекав
**4600мс**, число зі старої тривалості, і замір поїхав у ХВІСТ підсвітки.
➡️ Те, що ЗГАСАЄ, міряй ПОДІЄЮ, а не числом.
**(2)** перевірка «viewport на проді» знайшла `user-scalable=no` у моєму ж
КОМЕНТАРІ й мало не дала хибний висновок про зламаний деплой.
➡️ Міряй ЗНАЧЕННЯ атрибута, а не наявність рядка в файлі.

## ✅ СТОРОЖІ ЗНОВУ ЗЕЛЕНІ — 82/82, 1438 перевірок (17.08)

Два стенди були червоні, і **обидва знайшли справжнє**, хоч я спершу вважав їх шумом:

- **`sidebar-overlay` 25/36 → 36/36** — вада в САМОМУ стенді: тапав по координатах
  (30, 500), а там лежить живий контент новин; тап відкривав хаб і накривав бургер.
  ➡️ Тап по числах = перевірка, залежна від даних, про які вона не знає.
- **`drift` 8/24 → 24/24** — вада в МОЄМУ коді: екран «Збори» з'їжджав на 738px від
  довгого посилання в описі (`overflow-y: auto` мовчки робить `auto` і другу вісь).
  Описи пишуть жителі — випадок реальний.

🛑 Я мало не списав другу знахідку на шум одразу після того, як годину доводив, що
частина сигналів шумна. **Після такого доведення найлегше відкинути справжній.**
Розбір і три хибні гіпотези — `_session-log/vova-2026-08-17.md`.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| Сторож на свайп модалки статті | **немає** | доводив разовим приладом; стенда на `#article-modal` нема — тримається на збірці й `motion` |
| Перетікання тла вкладки | знайдено, **не лікували** | `.app-main` має `transition: background-color 0.3s`, фони вкладок різні — 18 кадрів ≈300мс. 🔴 **15.08 ЦЕЙ КЛАС УЖЕ ВКУСИВ:** саме він давав «блимання» після сповіщення. Там прибрано зайве перемикання, але САМ перехід між вкладками лишається |
| **Android не тестувався ВЗАГАЛІ** | 0 перевірок | усе живе тестування — iPhone Вови. Перед запуском у громаді це діра |
| Тестовий збір **на проді** | у базі, не у файлі | прибрати або замінити реальним: адмінка → «Збори» → «Видалити» |
| Класифікатор новин (**B-28**) | відкрито | 94.5% статей → «Суспільство»; блокує тематичний фільтр B-14. Корінь у `scripts/parse_rss.py` |
| Юрист на правові тексти | відкрито | єдине, чого закрити не можу |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

1. 🔴 **Знімаємо заслінку?** `DEV_LOCK = true` — один рядок. Це єдине, що
   відділяє нас від Етапу 3 (запуск у громаді). Рішення твоє.
2. **Іконка «Стрічка» — форма остаточна?** Замовлення звучало «змінити і
   **зафіксувати нарешті**»; зроблено сітку 2×2 (варіант 4 з макета).
3. **Стенд `ad-report`** упав на CI 15.08, локально 16/16, моїми змінами не
   чіпався. Схоже на нестабільність раннера — глянути, якщо повториться.
4. 🔴 **Чекають перевірки пальцем на iPhone.** (а) Сповіщення: чат → назад →
   Повідомлення → назад → Громада, без плями. (б) Деплой #4285: банер автобусів
   має рушати з-під пальця одразу · картка Дошки на Громаді не лишатись
   збільшеною · модалка статті — закриватись коротким кидком. ⚠️ Сторожа на
   свайп статті **немає** — та перевірка тримається на тобі.

## ➡️ ДАЛІ

**Черга Вови** (`_ai-tools/BACKLOG_VOVA_2026-08.md`, порядок задав 05.08):

1. ✅ **Віджет і розділ зборів — ЗРОБЛЕНО** (16-17.08): макет, переїзд у базу,
   заявки від жителів, адмінка. Лишилось замінити тестовий збір реальним.
2. 💬 Чат Дошки — теплота `#F3EFE3` / `#FCFAF4`
3. 📐 Спадковий відступ контейнера Громади
4. 🚌 Капсули проти віджета автобусів

**Плани руху** (`plans/`) — ✅ усі три закриті 15.08; лишилась жива перевірка на
iPhone (див. питання 4).

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) — застосунок навмисно закритий.
  Знімати не можна: у деяких людей є доступ.
- 🔴 **Клавіатура на iOS** (`style/feed.css:630,971`, `transition: height`) — два
  фікси там уже провалились (PR #638, #641).
- 🔴 **Три різні механізми закриття** (`#article-modal` · `core/modal.js` ·
  `core/layers.js`) — плутати не можна.
- 🔴 **`main` напряму не чіпати** — тільки через PR.
- ⚠️ **Дампи БД у репозиторій не класти** — репозиторій ПУБЛІЧНИЙ.
- ⚠️ **«Unverified» на GitHub — очікувано, ігнорувати.**
- ⚠️ **`git diff` для «що не в main» — на ДВІ крапки.** На три після squash-мерджа
  показує роботу, яка вже в `main`.
- ⚠️ **Не піднімати стелю бюджету читання** в `tests/docs-fresh.mjs` — вікно
  контексту від цього не росте.

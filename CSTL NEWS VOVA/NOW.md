# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-24 20:12 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-bn0us5` |
| останній коміт людини | `4ad5aa33` 24.08 20:11 — Merge remote-tracking branch 'origin/main' into claude/startuem-bn0us5 |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260824-1926` |
| останній PR у `main` | #1009 |
| стендів на диску | 102 |
| незакомічених файлів | 1 ⚠️ |
| журнал за 2026-08-24 | є ✅ |
| потік `/byyou` | idle — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- ✅ нічого. Уся робота гілки вже в `main`.

**Останні кроки (без автокомітів парсерів):**

- `4ad5aa33` 24.08 20:11 — Merge remote-tracking branch 'origin/main' into claude/startuem-bn0us5
- `20fa02ad` 24.08 19:37 — Мітки «бачив» синхронізуються між пристроями (#1009)
- `2798adc0` 24.08 19:36 — docs(пульс): звів роботу двох сесій за правилом №13
- `1235fae3` 24.08 19:34 — Merge remote-tracking branch 'origin/main' into claude/startuem-bn0us5
- `6ee03b3c` 24.08 19:31 — docs: пульс, літопис і 102 стенди після синхрону міток

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

🔀 **ВОВА ПРАЦЮЄ НА ДВОХ АКАУНТАХ ОДНОЧАСНО — ЧИТАЙ ЦЕ ПЕРШИМ.**
24.08 обидва писали в ті самі файли, і це коштувало **девʼяти конфліктів злиття**.
Правила, куплені тим днем:

1. 🔴 **`git fetch origin main && git merge origin/main` ДО роботи, а не перед PR.**
   Тоді конфлікти лягають на порожнє місце, а не поверх свіжого коду.
2. 🔴 **`_ai-tools/SESSION_STATE_VOVA.md` — єдине місце, де «вибрати» = ЗАГУБИТИ:**
   там у кожної сесії СВОЇ розділи. Зводити руками по межах заголовків і після
   злиття перерахувати розділи.
3. `NOW.md` при конфлікті — брати **чужу** версію за основу і переписувати прозу
   заново (пульс однаково пишеться під нову сесію).
4. Журнал дня: глянути, які літери за сьогодні зайняті — 24.08 обидві сесії
   взяли `-b` і довелось перейменовувати.
5. 🛑 **Ніколи `-X theirs` / `-X ours`** — тільки розбір по одному конфлікту.

Станом на **20:10 UTC робота обох акаунтів уся в `main`**, обидві гілки зведені.
🔴 **Урок дня, дорожчий за самі правила:** при четвертому злитті в `main` уперше
лежала ЧУЖА робота (#1007, #1008), і звичний `--ours` затер би її. У трьох
попередніх злиттях `--ours` був правильний — бо там була лише власна squash-нута
робота. ➡️ Перевірка «а чи є в `main` щось чуже» мусить бути ПЕРШОЮ дією при
кожному злитті, а не звичкою з минулого разу.

🏁 **MVP ВКЛАДКИ «ПИТАННЯ» ЗАКРИТО ПОВНІСТЮ** — 24.08 доїхав останній, шостий
крок: **тип 4 «у громаді питання без відповіді»**. Чекав на робочі вимикачі
(B-33): сповіщення, яке не можна вимкнути, — це спам за визначенням.
🛑 **І це не розсилка:** коло звужене до тих, хто **вже відповідав у Питаннях за
90 днів**. ✅ Заразом закрито **аудит account-scoped state** (другий акаунт) —
подробиці нижче.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

_(23-24.08 · журнал `_session-log/vova-2026-08-23c.md` · PR #1004)_

> 🔀 **ЦЬОГО ДНЯ ПРАЦЮВАЛИ ДВІ ПАРАЛЕЛЬНІ СЕСІЇ** на різних акаунтах — нижче
> робота обох (друга: `claude/startuem-bn0us5`, журнал `…-23b.md`).

### 24.08, пʼята частина: мітки «бачив» синхронні між пристроями (PR #1009)

Питання Вови: «якщо прочитаю з телефону і зайду з компʼютера, то і там буде
рівно те саме прочитано?» Борг того ж ранку закрито. **База:**
`user_seen_marks` + `user_seen_threads`. 🔑 **Мітка рухається ТІЛЬКИ ВПЕРЕД**
(`greatest`), час бере **сервер**: інакше старий таб дописав би вчорашню мітку і
все «непрочиталось» би назад. Доведено **6/6** на базі, **5/5** у браузері.
**Два шари:** база це правда, сховище пристрою — швидкий кеш (мітку питають
СИНХРОННО при малюванні капсул). 🛑 Гість — на пристрої.

### 24.08, четверта частина: тип 4 — питання без відповіді

**`send-unanswered-push`** (нова Edge Function, `verify_jwt` УВІМКНЕНО) ·
`qa_unanswered_push_log` · `pg_cron`. Межі §14-§15 `QA_CONCEPT.md`, порожній
список → **мовчимо**; подробиці — журнал 23c.

🔑 **Розклад дзвонить ДВІЧІ (`0 8,9 * * *` UTC), а працює РАЗ:** 11:00 Києва це
08:00 UTC влітку і 09:00 взимку, а `pg_cron` про літній час не знає. Годину
звіряє сама функція. 🔴 **Заведено `#/tab/<назва>`** — deep-link умів лише
`#/post/<де>/<id>`, тож зведений push довелось би вести в одне з пʼяти питань і
збрехати про решту. Стенд **83/83**, контроль **79/83**.
### PR #1001-#1003 (усе в `main`)

**🔵 Гніздо імені** (v4917) · **🖼 Пара фото жителя** (v4924) · **🚌 Капсула
автобуса** казала «ЗАВТРА» при рейсі СЬОГОДНІ. Подробиці — журнал 23b.
🛑 **Пастка звідти:** сторож автобусів ваду не пропускав, а **ВИМАГАВ** — сцена
звалась «НІЧ — рейсів немає», а грала на рейсі, який сьогодні ще попереду.

### 24.08: персональне переїхало в акаунт (PR #1006)

Слово Вови: «**все персональне має зберігатись в акаунті, а не на пристрої**».
Привід: вийшов з «Олександра», зайшов «Володимиром» — push прилетів на попередній.
Чотири знахідки, стенд `account-scope.mjs` (10/10): push-пристрій · збережені
статті (`saved_articles` з `uid`) · гейт «Збережених» · мітки «бачив».
🔴 **`sidebar.js` ДВІЧІ стверджував, що перевірка є, — а її не існувало жодної.**
🛑 `signOut()` зависав **назавжди** на `serviceWorker.ready` (без SW ця обіцянка
не виконується НІКОЛИ — не падає, а мовчки висить).

### 24.08: видалення коментаря не працювало ВЗАГАЛІ (PR #1005)

Ні в Питаннях, ні в Стрічці. Тост казав «Текст порожній», а падав **сам
`deleted_at`** (42501): політика ЧИТАННЯ вимагала `deleted_at is null`, і автор
робив рядок невидимим ДЛЯ СЕБЕ. 🔴 Тригера антиспаму **не було в жодному
SQL-файлі** — жив у базі з 25.07. **Правило №11 стосується схеми теж.**

### Питання і B-33 (PR #1004)

**«Цікавить» — ПУБЛІЧНА дія, не підписка** (якщо різниця лише в тому, чи прийде
push, людина її в момент вибору не бачить). **B-33: усі чотири вимикачі
сповіщень були декоративні** — `notif_prefs` писався в `localStorage` і не
читався ніким. Подробиці — `docs/QA_CONCEPT.md` і журнал 23c.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| Тип 4: перше справжнє спрацювання | **ще не було** | найближче — завтра об 11:00 за Києвом. Доведено лише відбір, воротар часу і межа 3 діб |
| Стеля «5 push із Питань на добу» (§12) | **в коді немає** | немає для ЖОДНОГО типу; тип 4 тримає себе межею «раз на 3 доби». Записано, а не забуто |
| Гілка префів в автобусах | **не виконувалась наживо** | `checked:0`, підписок нема; виконається при першому відстеженні |
| Живий прохід push у Питаннях | **не перевірено пальцем** | потрібні два акаунти й телефон Вови |
| Відкладений push (batching) | не робили | потрібен планувальник; А2 закриває 80% болю за 5% роботи |
| Категорії питань | не робили | без них персоналізація типу 4 не має на чому стояти |
| **Android не тестувався ВЗАГАЛІ** | 0 перевірок | усе живе тестування — iPhone Вови |
| Класифікатор новин (**B-28**) | відкрито | 94.5% статей → «Суспільство»; блокує B-14 |
| ПД в ІСТОРІЇ git | не стерто | перезапис історії ламає посилання на коміти |
| Тестовий збір **на проді** | у базі | адмінка → «Збори» → «Видалити» |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

✅ **24.08, 20:00 — Вова перевірив пальцем: «Все працює».** Закрито: синхронізація
новин телефон↔компʼютер · перемикання акаунта (push замовк на попередньому) ·
видалення коментаря в Питаннях і Стрічці · «Вийти».

1. 🔴 **Живий прохід у Питаннях, два акаунти:** постав «Цікавить» з А → відповідь
   з Б → має прийти push → тап веде до **тієї самої** відповіді, підсвіченої.
2. 🔴 **Вимикачі:** вимкни «Питання» в кабінеті → push не приходить; вимкни на
   телефоні → на компʼютері теж вимкнено.
3. **Вхід через Google:** тепер +1 екран вибору акаунта. Заважає — відкотимо одним рядком.
4. 🔴 **Завтра об 11:00** має прийти «Потрібна відповідь» тим, хто відповідав у
   Питаннях. Скажи, чи прийшло і чи не дратує.
5. **ПД в історії git** — стираємо? Чекаю «роби». · 6. Ключ `cstl-olyka-agent` — відкликати.

## ➡️ ДАЛІ

**Обидва великі потоки закриті** — MVP «Питань» і account-scoped state. Далі —
**не нова логіка, а підготовка до запуску**: заслінка, вміст Дошки, тестування
на Android (0 перевірок досі).

**Черга Вови** (`_ai-tools/BACKLOG_VOVA_2026-08.md`): 💬 чат Дошки — теплота
`#F3EFE3`/`#FCFAF4` · 📐 спадковий відступ контейнера Громади · 🚌 капсули проти
віджета автобусів.

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) · 🛑 **коди заслінки в репозиторій НЕ писати**.
- 🛑 **Знімки `tests/tools/_out/` НЕ комітити** — на них імена й телефони людей.
- 🔴 **`verify_jwt` у `send-bus-push` — УВІМКНЕНИЙ**; деплой із `false` тихо зняв би перший рубіж.
- 🔴 **TEXT проти `uuid`:** `reactions.user_id` і `push_subscriptions.user_uuid` — TEXT.
  Перед `.in('uid', …)` **обовʼязковий фільтр форми UUID**, інакше один сторонній
  рядок глушить УСІ сповіщення теми.
- 🛑 **Персональні сповіщення вимикача НЕ мають** · **`upsert` без `.select()`**, де результат не потрібен (№11-БІС).
- 🛑 **Кирилиця в назвах змінних bash** — падає з кодом 127 · **`Одеради АЗ` ≠ `Одеради`** · **порядок `@media` погоди в `style/home.css`** — у КІНЦІ секції.
- 🔴 **Клавіатура на iOS** (`style/feed.css:630,971`) — два фікси провалились (#638, #641).
- 🔴 **Три механізми закриття** · **`main` напряму не чіпати** · **дампи БД у репозиторій не класти** (він ПУБЛІЧНИЙ).
- 🔴 **`serviceWorker.ready` без SW не виконується НІКОЛИ** — не падає, а висить; бери `getRegistration()`.
- 🛑 **`pg_cron` живе в UTC і про літній час не знає** — час доби звіряти в коді, а не в рядку розкладу.
  І **секрет у текст розкладу не вписувати**: читати з `app_secrets` у мить виклику.
- ⚠️ **«Unverified» на GitHub — очікувано** · **`git diff` для «що не в main» — на ДВІ крапки** · **не піднімати стелю бюджету читання** в `tests/docs-fresh.mjs`.

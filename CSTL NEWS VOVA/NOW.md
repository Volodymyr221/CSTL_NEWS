# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-27 18:15 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-wvx8sj` |
| останній коміт людини | `2c46e78d` 27.08 18:14 — Merge remote-tracking branch 'origin/main' into claude/startuem-wvx8sj |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260827-1753` |
| останній PR у `main` | #1053 |
| стендів на диску | 113 |
| незакомічених файлів | 0 ✅ |
| журнал за 2026-08-27 | є ✅ |
| потік `/byyou` | idle — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `CSTL NEWS VOVA/CLAUDE.md`
- `CSTL NEWS VOVA/NOW.md`
- `CSTL NEWS VOVA/_ai-tools/SESSION_STATE_VOVA.md`
- `_session-log/vova-2026-08-27b.md`
- `bundle.js`
- `scripts/ai_news_agent.py`
- `scripts/parse_rss.py`
- `scripts/sync_cms.py`
- `scripts/test_markup_polish.py`
- `src/tabs/home-caps.js`
- `sw.js`
- `tests/home-caps.mjs`

**Останні кроки (без автокомітів парсерів):**

- `2c46e78d` 27.08 18:14 — Merge remote-tracking branch 'origin/main' into claude/startuem-wvx8sj
- `da104ff1` 27.08 18:12 — docs(session): діагноз простою планувальника GitHub
- `016b9eb2` 27.08 18:09 — docs(session): журнал — виконано «роби все»
- `3c579cec` 27.08 18:08 — docs: пульс + журнал сесії
- `19aab523` 27.08 18:08 — Стаття агента: живі посилання, без «повернутися», обкладинку міряємо

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

📦 **27.08, вечір. Уся робота в `main`** — останній PR **#1054**, `CACHE_NAME`
**`cstl-20260827-1753`** (капсула — клієнтський код, тож піднято).

🔴 **ВЕЧІР ПРО ДВІ РЕЧІ:** капсула Громади більше не губить відстежуваний рейс, коли
людина вже їде · до статей кабінету нарешті доходять фікси розмітки (їх не брало
самолікування — воно пропускає ексклюзиви).

🔴 **ДЕНЬ ПРО ОДНЕ — СТАТТЯ МАЄ ВИГЛЯДАТИ ЯК СТАТТЯ.** Шість деплоїв поспіль: живі
посилання · цілі цитати · заглушки-замки геть · фото в тілі · мініатюри 4:3 · і вечірній
розбір, де питання про ОДНУ статтю виявилось вадою цілого джерела (Волинь Post, 62 статті).

🟢 **Відкритих критичних немає.** ✅ «Блим» зник — запис «НЕ вилікувано» був хибний.
🛑 **ФІКСИ ПАРСЕРА ЩЕ НЕ ПРАЦЮВАЛИ НІ РАЗУ — І ЦЕ НЕ НАШ КОД.** 📐 Заміряно через
GitHub API о 18:10 UTC: `rss-parser` за розкладом мовчить із **07:27** (10 год 43 хв),
`ai-news-agent` — з 09:50, `cms-sync` при розкладі «кожні 5 хв» дав за добу **два**
прогони. **Усі прогони, що відбулись, — `success`**: GitHub просто не запускає.
🔴 Ручний запуск мені закритий (**403 `Resource not accessible by integration`**) —
тисни **Run workflow** сам.

🔀 **№13:** злиття ДО роботи · ніколи `-X theirs` · маркери конфлікту ПЕРЕД `git add` ·
🛑 `node --check` на файлах `src/` не перевіряє **нічого** — лише `scripts/check-syntax.mjs`.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

### 27.08 НІЧ: КАПСУЛА ГУБИЛА МІЙ РЕЙС, ЩОЙНО Я В НЬОГО СІВ (скрін Вови 20:32)

📐 **Числа збіглись до хвилини — і саме це виказало ваду.** Вова відстежує «Струмівка →
Олика 20:17→21:15», капсула каже «Олика → Жорнище · зараз у Піддубці». По `schedule.json`:
до Струмівки **−15 хв**, до Олики **+43 хв** — **той самий рейс**, описаний як чужий.
🔴 **Корінь — один рядок:** `minsToBoard` рахує ПОСАДКУ, і `left < 0` викидав відстеження
ЦІЛКОМ; далі гілка «рейс через моє село» вигадувала поїздку.
🛑 **Ламалось правило №12:** те, що людина САМА ввімкнула, програвало виведеному з її села.
✅ Запис живе, поки попереду ВИСАДКА, а рейс `enroute`; капсула міняє питання:
**«У ДОРОЗІ · ВИСАДКА О 21:15» / «Струмівка → Олика · зараз у Піддубці»** («ПРИБУТТЯ», якщо
зупинку виходу не обрано). 🧪 `home-caps` **143/143**, контроль `origin/main` **140/143** —
падає дослівно скарга.

### 27.08 НІЧ: ДО СТАТЕЙ КАБІНЕТУ НЕ ДОХОДИВ ЖОДЕН ФІКС РОЗМІТКИ

🔴 Самолікування починається з `if a.get("exclusive"): continue`, а `sync_cms` ставить
`exclusive: True` кожній статті кабінету → статті агента не перебирались **ніколи**.
🛑 **Заборону не знято свідомо** (ексклюзив — це й тексти Вови). Замість неї
`polish_markup()`: прохід **без мережі**, який не міняє змісту — робить натискним написане
і прибирає навігацію джерела. 🔑 **Адреса без `https://` — теж адреса** (з трьох у статті
ставала одна); доменна зона з ПЕРЕЛІКУ, бо `\w+\.\w{2,}` ловив би «index.html» і «ст.12».
🛑 **«« повернутися» не можна в `_TAIL_RE`** — вона ріже текст від збігу, а це звичайне
слово. 🔴 **Фото не міряли жодного разу:** `image_size()` читає розмір із ЗАГОЛОВКА (32 КБ),
`best_cover()` лишає вибір видавця при ≥800px. **«Не змогли зміряти» ≠ «замале».**
🧪 `scripts/test_markup_polish.py` **33/33** — на СПРАВЖНЬОМУ тілі статті 1000012.

### 27.08 ДЕНЬ І ВЕЧІР: СТАТТЯ МАЄ ВИГЛЯДАТИ ЯК СТАТТЯ (потік 2А + Волинь Post)

📐 **Питали про одну статтю — виявилось джерело цілком:** Волинь Post **62 статті — 0 з
фото, 0 з посиланням** при 62 з 62 `page`. 🔎 Стара верстка (`<div>` з `<br>`) ішла ПЛОСКИМ
шляхом, а один `<img>` робив список блоків непорожнім — стаття втрачала текст ЦІЛКОМ.
Обкладинку брали лише з RSS; тепер `fetch_article_page` віддає пару з того самого супу.
🔑 **Адресу пише ПАРСЕР** (`_safe_href`); розділювач абзаців не лізе в тег, `<a>` і пряму
мову. 🛑 **`node --check` НЕ ловить синтаксису у `src/`.** 🧪 `article-links` 11/11 ·
`test_rich_html` 57 · `test_fulltext` 21/21.

### 26.08 і раніше (#1001-#1045)

📰 Редизайн блоку новин. 🔴 SDK Supabase зі свого домену (клієнт бази створювався ОДИН РАЗ
і лише з чужого CDN). 🔴 Аналітика: стеля 1000 брехала — правда **6 людей**, не 203.
📖 Усе — `_ai-tools/SESSION_STATE_VOVA.md`.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| Волинь Post: фото й посилання | ✅ код у `main`, **жодного разу не виконувався** | парсер за розкладом мовчить з 07:27; ручний запуск мені закритий (403) — тисни **Run workflow** на `rss-parser.yml` сам |
| Полірування розмітки старих статей | ✅ код у `main`, **не виконувалось** | той самий простій парсера: `polish_stored_articles` живе в його прогоні |
| Обкладинка за розміром | ✅ у `main`, **не перевірено на живому фото** | розмір файлу зі скріна з контейнера не зміряти — `curl` до `rada.info` = 403 від проксі пісочниці |
| Розклад GitHub кидає прогони | 🔴 відкрито | пропозиція: `cms-sync` `*/5 → */15` або перевести на подію; Вова рішення не давав |
| Потік 2Б-2: галерея і відео в тілі | не почато | прикладу галереї так і не трапилось — робити на здогад = гадати |
| Фото в тілі статті (#1050) | ✅ у `main` | iPhone-смоук Вова ще не робив |
| Логотип може просочитись у тіло | фільтр лише за розміром | розмір знаємо, лише якщо джерело написало його в розмітці |
| `analytics_daily` | борг **з умовою** | при >1 млн подій або чистці історії; 11 тис. рахуються на льоту |
| Retention D1/D7/D30 · baseline | після запуску | рахуються з сирих подій заднім числом |
| Віджет Стрічки не бачений пальцем | ✅ борг каруселі закрито | скрін не долетів |
| Тип 4: перше спрацювання | **27.08** | питанню буде 21 год при потрібних 24 |
| `CSTL_BUGS.md` = 40.5 KB при стелі 40 | запасу 0.5 KB | наступний баг — стисло |
| Стеля «5 push із Питань на добу» (§12) | **в коді немає** | заміряно по 6 |
| **Android не тестувався ВЗАГАЛІ** | 📌 відкладено Вовою | «протестуємо коли доробимо все» |
| Класифікатор новин (**B-28**) | відкрито | ~94% статей → «Суспільство» |
| ПД в ІСТОРІЇ git · тестовий збір на проді | не стерто / у базі | перезапис історії ламає посилання |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

✅ **Перевірено пальцем 24-26.08:** новини · перемикання акаунта · видалення коментаря ·
«Вийти» · push · сортування · **вимикачі (B-33)** · віджет Стрічки.

0. 🔴 **НЕ ПЕРЕВІРЕНЕ — гарантія «хто я» (холодний старт, PWA):** крапка непрочитаного
   ОДРАЗУ · push веде в чат · збережені позначені · ❤️ зафарбоване · привітання з ІМЕНЕМ ·
   ⚠️ **Автобуси малюють розклад МИТТЄВО**. 🔴 Чи відкривається `castlelife.org` — з
   контейнера не перевірити.
0б. 🔴 **Запусти `rss-parser.yml` руками** (Actions → Run workflow): мені 403. Тільки після
   прогону дивись **13114** — фото і натискне посилання. Не спрацює — `[FETCH]` у логах
   (живі сторінки джерел із контейнера НЕ дістати). ⚠️ Черга — усі 400 статей по 40 за
   прогін, тож Волинь Post доїде не з першого разу.
0в. 🔴 **Капсула «У ДОРОЗІ»** — перевіряється лише пальцем: сядь у відстежуваний рейс і
   глянь Громаду після часу посадки. Має бути «У ДОРОЗІ · ВИСАДКА О HH:MM» і ТВІЙ відрізок.
0г. ❓ **Знижуємо частоту `cms-sync`?** `*/5` це 288 запусків на добу — найгучніший
   споживач планувальника, а GitHub першими кидає саме найчастіші розклади.
1. 🔴 **Тип 4 — 27.08.** ⚠️ У базі ОДНА push-підписка на всіх, автор питання випадає.
2-9. Нічна тиша · антиспам · непрочитане · редагування питання · голос спільноти ·
   Google-вхід · ПД в історії git · ключ `cstl-olyka-agent` — у `SESSION_STATE_VOVA.md`.
📌 **Android** — «протестуємо коли доробимо все».

## ➡️ ДАЛІ

🔴 **ПЕРШЕ — аналітику пальцем, залогіненим.** До 26.08 жоден захід не мав акаунта, тож
«Людей» покаже правду лише з нових заходів. ✅ **Пункт 0 закрито 25.08 (#1029).**
**Далі:** 📰 потік 2Б-2 (галерея + відео в тілі, коли трапиться приклад) · 💬 чат Дошки ·
📐 відступ Громади · 🚌 капсули проти автобусів. _(Android — у кінці.)_
❓ **Не дописане Вовою:** «щотижневий AI-аналіз динаміки» — усе для нього вже є.

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** · коди заслінки в репозиторій НЕ писати · знімки
  `tests/tools/_out/` НЕ комітити · 🔴 `verify_jwt` у `send-bus-push` — УВІМКНЕНИЙ.
- 🔴 **TEXT проти `uuid`:** `reactions.user_id`, `push_subscriptions.user_uuid` — TEXT;
  перед `.in('uid', …)` **обовʼязковий фільтр форми UUID** — один сторонній рядок глушить
  УСІ сповіщення теми. · `upsert` без `.select()` · SELECT-політика на свої рядки ·
  **кирилиця в іменах змінних bash** = код 127 · `@media` погоди — у КІНЦІ `home.css`.
- 🔴 **Клавіатура на iOS** (`feed.css:630,971`) · **`main` напряму не чіпати** · дампи БД
  не класти (репо ПУБЛІЧНИЙ) · `serviceWorker.ready` без SW не виконується НІКОЛИ ·
  `pg_cron` живе в UTC · **у транзакції `now()` однаковий для всіх операторів**.
- 🛑 **НЕ МІРЯЙ ПОСЕРЕД РОБОТИ** · «Unverified» — очікувано · `git diff` «що не в main» —
  на ДВІ крапки · **стелю бюджету в `docs-fresh` НЕ піднімати**.
- 🔑 Решта — `HOT_RULES.md`, `CSTL_BUGS.md`.

# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-25 14:52 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-upyy76` |
| останній коміт людини | `d365f321` 25.08 14:51 — Merge remote-tracking branch 'origin/main' into claude/startuem-upyy76 |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260825-1442` |
| останній PR у `main` | #1024 |
| стендів на диску | 107 |
| незакомічених файлів | 1 ⚠️ |
| журнал за 2026-08-25 | є ✅ |
| потік `/byyou` | idle — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `CSTL NEWS VOVA/NOW.md`
- `_session-log/vova-2026-08-24b.md`
- `bundle.js`
- `src/tabs/board-discussions.js`
- `src/tabs/board.js`
- `sw.js`
- `tests/qa-unread.mjs`

**Останні кроки (без автокомітів парсерів):**

- `d365f321` 25.08 14:51 — Merge remote-tracking branch 'origin/main' into claude/startuem-upyy76
- `2471f2bc` 25.08 14:50 — Питання: крапка гасне на ВИХОДІ з вкладки, а не на вході
- `bbf49b7f` 25.08 14:24 — docs: передача в новий чат — 25.08.2026 (#1024)
- `21f5a7b0` 25.08 14:21 — Merge remote-tracking branch 'origin/main' into claude/startuem-upyy76
- `4ebe74f2` 25.08 14:18 — Питання: непрочитане стало видимим у списку, а не лише крапкою (#1023)

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

🏁 **ПʼЯТЬ СЬОГОДНІШНІХ СЕСІЙ ЗАКРИТІ:** «Питання» (#1013-#1016) · голос спільноти
(#1017) · нічна тиша (#1018-#1019) · `authReady()` і перемикач голосу (#1020-#1022) ·
непрочитане в «Питаннях». 🟢 **Відкритих критичних немає.** Далі — **не нова логіка,
а підготовка до запуску**.

🔴 **ОБРОБНИК НЕ ДІСТАЄ ТУДИ, КУДИ ЗДАЄТЬСЯ** — дві знахідки 25.08: делегати на
`document` **не спиняють поширення** (один тап відкрив ДВОЄ), а `wireCards()` висить на
контейнері стрічки, тоді як лист коментарів живе окремим шаром у `body` — `data-*` гачки
картки там **не працюють**.

🔀 **HOT_RULES №13** (робота вдвох): злиття ДО роботи · ніколи `-X theirs/-X ours` ·
«а чи є в `main` ЧУЖЕ» — ПЕРША дія при злитті.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

### 25.08, сесія «c»: голос спільноти · «хто я» як ФАКТ (#1017 · #1020 · #1022)

🔑 **Голос спільноти:** `author_uid` — ЗАВЖДИ людина, що натиснула «Надіслати»;
`as_page_id` — від чийого імені це ЗВУЧИТЬ. 🛑 `actor_type`/`actor_id` (порада ChatGPT)
ВІДХИЛЕНО: без сліду людини сліпне модерація, а антиспам, видалення, згадки, капсули і
push ключуються на `author_uid`. **Default — ОСОБИСТИЙ.**

🔴 **«Не можу писати пости» + «лайк не підсвічений» + «немає перемикача» — ОДНА ВАДА,
архітектурна:** застосунок малював залежне від входу, **не знаючи ще, хто зайшов**.
`initAuth()` без `await` → `currentUserId()` = `null` → пʼять запитів із восьми чесно
відповідали про ГОСТЯ. 🛑 Слово Вови: **«професійно та комплексно, а не як латка на
латці»** — підписка лікує ПІСЛЯ факту. 🔑 Гарантія тепер в `auth.js`: **`authReady()`**,
з обовʼязковою межею часу (клас `serviceWorker.ready` — не падає, ВИСНЕ).
🔴 **«Поле вводу не піднімається»** — моя ж перша редакція: смуга «Відповідати як» стала
ТРЕТІМ поверхом під клавіатурою. ⚠️ У Chromium не відтворилось (HOT_RULES №9) → не
лагодив `keyboard.js`, а **прибрав поверх**. 🔴 **«Стрілочку погано видно»** → кільце ·
**⇄** 16px · підказка один раз. **Стос 94px → 94px.**
Деплої **#5053 · #5058 · #5063**, `CACHE_NAME` `cstl-20260825-1331`.


### 25.08, пʼята сесія: непрочитане в «Питаннях» стало видимим

🔴 **Скарга:** «вкладка горить, відкриваю — незрозуміло, що саме». **Крапка не брехала** —
заміряно: питання №84, мітка 14:03:57, чужа відповідь 14:04:12. Бракувало ДРУГОЇ третини:
крапка знала, яке питання нове, а список не казав нічим.
🔑 **Модель Вови:** `new` (не відкривав) бачать УСІ · `answers` (нова чужа відповідь) —
ЛИШЕ автор і хто натиснув «цікавить». 🛑 ЗВУЖЕННЯ: досі вона світилась у кожного, хто
зазирнув — один тап із цікавості підписував назавжди. 🔑 **Одне джерело правди
`questionUnread()`** (B-27). Капсула: число + текст найновішого; слайдер відхилено.

🔴 **Три речі знайшов СТЕНД, а не я:** зсув 3px · підсвітка не знімалась після відкриття
· капсула замовкла б для новачка. Стендів **107**.

🔴 **ДРУГА РЕДАКЦІЯ ТОГО САМОГО ВЕЧОРА — Вова показав на телефоні:** крапка горіла, тап
по вкладці — і підсвітка згасла, НЕ ВСТИГНУВШИ показатись. Поріг ставився на `Date.now()`
при вході, тобто заднім числом робив свіже питання «історією». Тепер поріг успадковує
останній візит (ключ **v2** — у `v1` на телефонах лежить зіпсоване значення), а
🔑 **гасне все на ВИХОДІ з вкладки**: «я дивлюся, читаю їх, і коли переходжу в іншу
вкладку, тоді вони згасають». Тап по окремому питанню гасить його одразу.
⚠️ Порядок у `board.js` — частина поведінки: `markQaFloorOnce()` ПЕРЕД `markChatSeen()`,
бо другий зрушує ту саму позначку, яку читає перший.
**19/19**, два мутанти → 18/19 і 16/19. 🛑 Стенд брехав: перемикався на вкладку `home`,
якої НЕМАЄ (вона `community`) — три перевірки падали на справному коді.

### 25.08, третя сесія: нічна тиша + документи звірені з кодом

🔴 **НІЧНА ТИША 22:00–08:00** (`send-answer-push` **v6**, типи 1 і 3; тип 2 її не має
навмисно — §12). 🔑 **Пропущене не губиться:** уночі не пишемо в `answer_push_targets`, а
`missed()` рахує від останнього запису. Тому вихід стоїть ДО `mark()`: після нього тиша
стала б ВТРАТОЮ. ⚠️ Вікно перетинає північ → умова через `||`; через `&&` тиша дала б
**нуль** годин. ✅ Живі виклики: чужий секрет → 401, неіснуючий коментар → 200 `answer
gone`. 🆕 `night-silence.mjs` **18/18**, мутант → 10/14. 🟢 **B-33 ЗАКРИТО**.

### Раніше 25.08 (PR #1001-#1016)

🔴 **Розбір — `SESSION_STATE_VOVA.md` і журнали `-23b/-23c/-24/-24b`.** Навмисно адреса,
а не переказ: друга копія списку розходиться з першою.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| Тип 4: перше спрацювання | 25.08 нічого не підійшло під вікно | 26.08, 11:00; у базі **одна** push-підписка |
| `CSTL_BUGS.md` = 40.5 KB при стелі 40 KB | запасу **0.5 KB** | наступний баг — стисло або виносити історію |
| Стеля «5 push із Питань на добу» (§12) | **в коді немає** | 24 і 25.08 заміряно по **6** — стеля спрацювала б двічі |
| Гілка префів в автобусах · batching | не робили | підписок нема; потрібен планувальник |
| Категорії питань | не робили | без них персоналізація типу 4 не має на чому стояти |
| **Android не тестувався ВЗАГАЛІ** | 📌 відкладено рішенням Вови | «Протестуємо коли доробимо все» |
| Класифікатор новин (**B-28**) | відкрито | ~94% статей → «Суспільство»; блокує B-14 |
| ПД в ІСТОРІЇ git | не стерто | перезапис історії ламає посилання на коміти |
| Тестовий збір **на проді** | у базі | адмінка → «Збори» → «Видалити» |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

✅ **Перевірено пальцем 24-25.08:** синхронізація новин · перемикання акаунта · видалення
коментаря · «Вийти» · прохід push · відповіді, лінія, сортування · **вимикачі (B-33)**.

1. 🔴 **Тип 4: чи прийде 26.08 об 11:00?** 25.08 не спрацював НЕ через ваду: розклад
   пройшов двічі, журнал порожній — під вікно 24год–7діб не підійшло жодне питання.
2. 🆕 **Нічна тиша:** відповідь після 22:00 → push НЕ приходить; вранці перший
   push каже «N нових відповідей», а не одну.
3. 🆕 **Антиспам:** «Окей» одразу вдруге → блокує · через 20с → пропускає.
4. 🆕 **Непрочитане в Питаннях:** нове питання сусіда підсвічене всім · нова відповідь
   — лише тобі як автору або якщо натиснув «цікавить» · зазирнув у чуже без «цікавить»
   → мовчить · відкрив → підсвітка зникла ОДРАЗУ, решта лишилась.
5. 🆕 **Голос спільноти:** відповідь як спільнота · бейдж «Адмін» на ОСОБИСТІЙ
   відповіді · перемикача немає з другого акаунта · push каже назву спільноти.
6. **Вхід через Google:** +1 екран. Заважає — відкотимо одним рядком.
7. **ПД в історії git** — стираємо? Чекаю «роби». · 8. Ключ `cstl-olyka-agent` — відкликати.

📌 **Android — рішення Вови: «Протестуємо коли доробимо все».** Свідомо відкладений крок.

## ➡️ ДАЛІ

🔴 **ПЕРШЕ ЗАВДАННЯ НОВОЇ СЕСІЇ — `BACKLOG_VOVA_2026-08.md` ПУНКТ 0** (він угорі файлу):
«гарантія «хто я» на всі вкладки». Слово Вови 25.08: «запиши це в головне завдання для
нової сесії, щоб вона… відразу почала робити».
🔑 Не нова фіча: `authReady()` написаний і перевірений виконанням, але застосований
**лише до Стрічки**. Решта вкладок можуть мигнути виглядом гостя на холодному старті.
🛑 Не одним заходом: одна вкладка = один крок = один вимір. Сторож уже стоїть.

**Далі за чергою Вови:** 💬 чат Дошки `#F3EFE3`/`#FCFAF4` · 📐 відступ контейнера
Громади · 🚌 капсули проти віджета автобусів. _(Android — у самому кінці.)_

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) · 🛑 **коди заслінки в репозиторій НЕ писати**.
- 🛑 **Знімки `tests/tools/_out/` НЕ комітити** (імена й телефони) · 🔴 **`verify_jwt` у
  `send-bus-push` — УВІМКНЕНИЙ** (деплой із `false` тихо зняв би перший рубіж).
- 🔴 **TEXT проти `uuid`:** `reactions.user_id` і `push_subscriptions.user_uuid` — TEXT;
  перед `.in('uid', …)` **обовʼязковий фільтр форми UUID**, інакше один сторонній рядок
  глушить УСІ сповіщення теми.
- 🛑 **Персональні сповіщення вимикача НЕ мають** · **`upsert` без `.select()`** (№11-БІС)
  і **SELECT-політика на свої рядки** · **кирилиця в назвах змінних bash** падає з кодом
  127 · **`Одеради АЗ` ≠ `Одеради`** · **`@media` погоди — у КІНЦІ `style/home.css`**.
- 🔴 **Клавіатура на iOS** (`feed.css:630,971`) — два фікси провалились (#638, #641) ·
  **Три механізми закриття** · **`main` напряму не чіпати** · **дампи БД у репозиторій не
  класти** (ПУБЛІЧНИЙ) · **`serviceWorker.ready` без SW не виконується НІКОЛИ**.
- 🛑 **`pg_cron` живе в UTC і про літній час не знає** — час доби звіряти в коді.
  І **секрет у текст розкладу не вписувати**: читати з `app_secrets` у мить виклику.
- 🔴 **Обробник не дістає туди, куди здається** — розбір угорі, в «ЗАРАЗ».
- 🛑 **НЕ МІРЯЙ ПОСЕРЕД РОБОТИ:** файлів не чіпати, поки йде повний набір (24.08 три
  прогони з чотирьох), і не ганяти стенди на незавершеному мерджі (25.08) · ⚠️
  **«Unverified» — очікувано** · **`git diff` «що не в main» — на ДВІ крапки** ·
  **стелю бюджету в `docs-fresh` не піднімати**.

# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-24 17:44 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-gqj5ae` |
| останній коміт людини | `3bdc17a6` 24.08 17:44 — feat(питання): тип 4 — «у громаді питання без відповіді» (крок 6 MVP) |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260824-1740` |
| останній PR у `main` | #1006 |
| стендів на диску | 100 |
| незакомічених файлів | 0 ✅ |
| журнал за 2026-08-24 | **НЕМАЄ** ⚠️ |
| потік `/byyou` | idle — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `CSTL NEWS VOVA/CLAUDE.md`
- `CSTL NEWS VOVA/NOW.md`
- `CSTL NEWS VOVA/START_HERE.md`
- `CSTL NEWS VOVA/_ai-tools/SESSION_STATE_VOVA.md`
- `CSTL NEWS VOVA/docs/QA_CONCEPT.md`
- `_session-log/vova-2026-08-23b.md`
- `_session-log/vova-2026-08-23c.md`
- `bundle.js`
- `scripts/check-imports.js`
- `scripts/supabase_account_scoped_state.sql`
- `scripts/supabase_soft_delete_visibility.sql`
- `scripts/supabase_unanswered_push.sql`
- `src/app.js`
- `src/core/auth.js`
- `src/core/board-shared.js`
- `src/core/saved-hub.js`
- `src/core/sidebar.js`
- `src/core/supabase.js`
- `src/tabs/board-discussions.js`
- `src/tabs/news.js`
- …ще 10

**Останні кроки (без автокомітів парсерів):**

- `3bdc17a6` 24.08 17:44 — feat(питання): тип 4 — «у громаді питання без відповіді» (крок 6 MVP)
- `5da0ab69` 24.08 10:55 — docs(журнал): фініш — PR #1004, деплой #4981 success, три інциденти
- `4b1575bb` 24.08 10:40 — docs(пульс): зріз після злиття
- `6fda3d2b` 24.08 10:40 — merge: origin/main — злито з паралельною сесією (гніздо імені, фото, капсула автобуса)
- `cefee7b0` 24.08 10:31 — docs(пульс): зріз після коміту

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

📦 **Сесія 23-24.08: вкладка «Питання» перебудована + закрито B-33.** Два потоки,
дев'ять кроків, усе задеплоєно. Наступний чат може братись за нове.

🔴 **ГОЛОВНЕ: кнопка «Мене теж цікавить» три тижні нічого не робила.** Вона писала
рядок у базу — і все: ні сповіщень, ні наслідків. Тепер це **публічна дія**:
лічильник видно всій громаді (`Потрібна відповідь · 3 чекають`), а той, хто
позначив, отримує push, коли зʼявиться відповідь.

🔴 **І другий, ширший за Питання: усі чотири вимикачі сповіщень у кабінеті були
декоративні** (B-33). `notif_prefs` писався в `localStorage` і **не читався
ніким** — жодною Edge Function. Людина вимикала «Автобуси», push однаково
приходив. Слово Вови: «**Декоративного в нас нічого не має бути, у нас все має
бути робоче**». Тепер налаштування в базі, читають усі чотири функції.

🔑 **Два уроки сесії, обидва про мовчазні провали.** `reactions.user_id` і
`push_subscriptions.user_uuid` мають тип TEXT, а `notif_prefs.uid` і
`profiles.uid` — `uuid`: **один нестандартний рядок відкинув би ВЕСЬ запит**, і
замовкли б усі сповіщення теми. Виглядало б це не як збій, а як «push чомусь не
приходять». Скрізь стоїть фільтр форми.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

_(23-24.08 · журнал `_session-log/vova-2026-08-23c.md` · PR #1004)_

> 🔀 **ЦЬОГО ДНЯ ПРАЦЮВАЛИ ДВІ ПАРАЛЕЛЬНІ СЕСІЇ**, на різних акаунтах. Нижче —
> робота обох. Друга (гілка `claude/startuem-bn0us5`, журнал
> `_session-log/vova-2026-08-23b.md`) змерджилась першою, її зміни вже в `main`.

### Сесія на `claude/startuem-bn0us5` (PR #1001-#1003)

**🔵 Гніздо імені** (PR #1001, деплой **v4917**) — синя галочка стала в розмір
імені й на одну лінію на всіх семи поверхнях. Сторож `verified-badge.mjs` 37/37.

**🖼 Пара фото жителя** (PR #1002, деплой **v4924**) — `avatar.webp` 256px для
кружечків + `profile.webp` 1700px для картки й лайтбокса. Сторож
`avatar-quality.mjs` 14/14.
🛑 **Пастка, доведена виміром:** `toBlob` з непідтриманим форматом не падає і не
віддає null — мовчки повертає **PNG**. Запит AVIF на 1024px дав **1273.9 KB**
замість ~48, у 26 разів важче, без жодної помилки.

**🚌 Капсула автобуса казала «ЗАВТРА», коли рейси були СЬОГОДНІ** (PR #1003).
Корінь: умовою входу в довідкову гілку було `!hit`, а `hit` не ставився, якщо
рейс далі за стелю `SOON_MAX_MIN = 120`. Код питав «чи є рейс у найближчі 2
години», гілка вважала це відповіддю на «чи є рейс сьогодні». Ціна — **≈6 год
50 хв брехні на добу**.
🔴 **Сторож не пропускав ваду — він її ВИМАГАВ:** сцена звалась «НІЧ — на
сьогодні рейсів немає», а грала на рейсі, який сьогодні ще попереду.
➡️ Патерн на майбутнє: **назва сцени розходиться з її фікстурою**.

### Сесія на `claude/startuem-gqj5ae` (ця, PR #1004)

**📄 Концепція вкладки** — `docs/QA_CONCEPT.md` (770 рядків): призначення розділу,
ієрархія картки, «Цікавить» vs «Зберегти», чотири типи сповіщень, захист від
спаму, MVP і «пізніше». Усі шість спірних рішень закриті словом Вови.

**🔑 Головне продуктове рішення — «Цікавить» це ПУБЛІЧНА дія, не підписка.**
Вова спершу пропонував «Цікавить = push, Зберегти = закладка». Заперечив: якщо
різниця тільки в тому, чи прийде push, людина її в момент вибору не бачить.
Ухвалено: **«Цікавить» — сказати громаді, «Зберегти» — сказати собі.** Вова:
«Це сильніше за мою попередню модель із підпискою на push».

**А1** — кнопка каже словами, що сталось (`Цікавить · 4 · Збережено`),
автододавання у «Збережені» (асиметрично: зняття НЕ прибирає), попит у списку
при ≥2. Плюс тип 3 `answer-interest` і таблиця `answer_push_targets`.

**А2** — debounce **обʼєднує**, а не губить: «5 нових відповідей на ваше
питання» замість тиші. Раніше 10 відповідей за 20 хв давали 2 push, і про решту
8 автор не дізнавався ніколи.

**А3** — ієрархія картки: чотири рівні ваги замість двох (20/600 питання ·
15/600 стан · 15/400 відповідь · 13/400 довідка), аватар 20px, цитата
`текст — Автор`.

**А4** — посилання зі сповіщення веде **до самої відповіді** (`?c=<id>`), вона
підсвічується; працює і на холодному старті.

**🔴 B-33** — `notif_prefs` у базі, читають `send-bus-push` v16 ·
`send-answer-push` v5 · `send-comment-push` v4 · `send-page-push` v5.
Набір тем: **Автобуси · Питання · Дошка · Стрічка**. «Світло» і «Новини»
**прибрані** — таких push не існує взагалі, тумблер під них був вадою з
фальшивим підтвердженням («вимкнув → не приходить → отже працює»).

**🔧 Вхід через Google** тепер завжди питає, яким акаунтом зайти
(`prompt: 'select_account'`) — скарга Вови, що закидає в останню пошту без вибору.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| Тип 4 «питання без відповіді» | **можна вмикати** | вимикач тепер працює. Умови — §14/§15 `QA_CONCEPT.md`: лише тим, хто відповідав за 90 днів, 1 раз на 3 доби, прохід об 11:00 |
| Гілка префів в автобусах | **не виконувалась наживо** | зараз `checked:0` — підписок на рейс немає. Виконається при першому ж відстеженні |
| Живий прохід push у Питаннях | **не перевірено пальцем** | потрібні два акаунти й телефон Вови |
| Відкладений push (справжній batching) | не робили | потрібен планувальник; крок А2 закриває 80% болю за 5% роботи |
| Категорії питань | не робили | без них персоналізація типу 4 не має на чому стояти |
| **Android не тестувався ВЗАГАЛІ** | 0 перевірок | усе живе тестування — iPhone Вови |
| Класифікатор новин (**B-28**) | відкрито | 94.5% статей → «Суспільство»; блокує фільтр B-14 |
| ПД в ІСТОРІЇ git | не стерто | перезапис історії ламає посилання на коміти |
| Тестовий збір **на проді** | у базі | адмінка → «Збори» → «Видалити» |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

1. 🔴 **Живий прохід у Питаннях, два акаунти:** постав «Цікавить» з А → відповідь
   з Б → має прийти push → тап веде до **тієї самої** відповіді, підсвіченої.
2. 🔴 **Вимикачі:** вимкни «Питання» в кабінеті → push не приходить; вимкни на
   телефоні → на компʼютері теж вимкнено.
3. **Вхід через Google:** тепер +1 екран вибору акаунта. Заважає — відкотимо одним рядком.
4. **Тип 4** («у громаді питання без відповіді») — вмикаємо?
5. **ПД в історії git** — стираємо? Чекаю «роби». · 6. Ключ `cstl-olyka-agent` — відкликати.

## ➡️ ДАЛІ

Питання закриті на рівні механіки. Логічне продовження — **не нова логіка, а
підготовка до запуску**: заслінка, вміст Дошки, тестування на Android.

**Черга Вови** (`_ai-tools/BACKLOG_VOVA_2026-08.md`): 💬 чат Дошки — теплота
`#F3EFE3`/`#FCFAF4` · 📐 спадковий відступ контейнера Громади · 🚌 капсули проти
віджета автобусів.

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) · 🛑 **коди заслінки в репозиторій НЕ писати**.
- 🛑 **Знімки `tests/tools/_out/` НЕ комітити** — на них імена й телефони людей.
- 🔴 **`verify_jwt` у `send-bus-push` — УВІМКНЕНИЙ.** Секрет там ДРУГИЙ рубіж; деплой
  із `false` тихо зняв би перший.
- 🔴 **TEXT проти `uuid`:** `reactions.user_id` і `push_subscriptions.user_uuid` — TEXT.
  Перед `.in('uid', …)` **обовʼязковий фільтр форми UUID**, інакше один сторонній
  рядок глушить УСІ сповіщення теми.
- 🛑 **Персональні сповіщення вимикача НЕ мають:** «вам відповіли», приватні чати, групи.
- 🛑 **`upsert` без `.select()`**, де результат не потрібен (правило №11-БІС, горіли двічі).
- 🛑 **Кирилиця в назвах змінних bash** — падає з кодом 127, `bash -n` не ловить.
- 🛑 **`Одеради АЗ` ≠ `Одеради`** · **порядок `@media` погоди в `style/home.css`** — у КІНЦІ секції.
- 🔴 **Клавіатура на iOS** (`style/feed.css:630,971`) — два фікси провалились (#638, #641).
- 🔴 **Три механізми закриття** (`#article-modal` · `core/modal.js` · `core/layers.js`).
- 🔴 **`main` напряму не чіпати** · ⚠️ **дампи БД у репозиторій не класти** (він ПУБЛІЧНИЙ).
- ⚠️ **«Unverified» на GitHub — очікувано** · **`git diff` для «що не в main» — на ДВІ крапки**.
- ⚠️ **Не піднімати стелю бюджету читання** в `tests/docs-fresh.mjs`.

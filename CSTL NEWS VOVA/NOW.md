# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-24 21:42 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-upyy76` |
| останній коміт людини | `c040cce2` 24.08 21:18 — Журнал: сторож проміжку і інцидент з памʼяттю |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260824-2108` |
| останній PR у `main` | #1011 |
| стендів на диску | 102 |
| незакомічених файлів | 1 ⚠️ |
| журнал за 2026-08-24 | є ✅ |
| потік `/byyou` | idle — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `CSTL NEWS VOVA/NOW.md`
- `CSTL NEWS VOVA/_ai-tools/SESSION_STATE_VOVA.md`
- `_session-log/vova-2026-08-24b.md`
- `bundle.js`
- `src/tabs/board-discussions.js`
- `style/board.css`
- `sw.js`
- `tests/questions.mjs`

**Останні кроки (без автокомітів парсерів):**

- `c040cce2` 24.08 21:18 — Журнал: сторож проміжку і інцидент з памʼяттю
- `3f442624` 24.08 21:18 — Сторож проміжку: міряти на КОРОТКІЙ відповіді, інакше не може впасти
- `ef50e279` 24.08 21:12 — Документи: картка питання — остання коренева відповідь (NOW, літопис)
- `25cb36e2` 24.08 21:10 — Merge remote-tracking branch 'origin/main' into claude/startuem-upyy76
- `5b7cfdf9` 24.08 21:10 — Картка питання: остання коренева відповідь + один лічильник на застосунок

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

🔀 **ВОВА ПРАЦЮЄ НА ДВОХ АКАУНТАХ ОДНОЧАСНО.** Повні правила — **HOT_RULES №13**
(заведено 24.08, коштувало девʼяти конфліктів злиття). Найкоротше:
`git fetch origin main && git merge origin/main` **ДО роботи**, ніколи
`-X theirs/-X ours`, у `SESSION_STATE_VOVA.md` «вибрати бік» = **загубити** чужий
день. 🔴 **Урок, дорожчий за самі правила:** перевірка «а чи є в `main` щось
ЧУЖЕ» мусить бути ПЕРШОЮ дією при кожному злитті — 24.08 звичний `--ours` мало
не затер #1007 і #1008.

🏁 **MVP ВКЛАДКИ «ПИТАННЯ» ЗАКРИТО ПОВНІСТЮ** — 24.08 доїхав останній, шостий
крок: **тип 4 «у громаді питання без відповіді»**. Чекав на робочі вимикачі
(B-33): сповіщення, яке не можна вимкнути, — це спам за визначенням.
🛑 **І це не розсилка:** коло звужене до тих, хто **вже відповідав у Питаннях за
90 днів**. ✅ Заразом закрито **аудит account-scoped state** (другий акаунт) —
подробиці нижче.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

### 24.08, сесія «b»: картка питання — остання КОРЕНЕВА відповідь

Скарга зі знімка: «чого відображається перше повідомлення» + «чому така велика
щіль від повідомлення до автора». _(журнал `-24b`, розбір — літопис)_

🔴 **Головне — знято латку, а не переставлено рядок.** «Скільки відповідей»
рахувалось у **ЧОТИРЬОХ** місцях незалежно, кожне своїм
`activeComments(...).length`. Поки визначення було спільне, вони сходились
**випадково**; зміна в одному місці = мовчазний розхід, рівно баг B-27. Тепер
`rootPredicate`/`rootAnswers` — **один** опис «що таке відповідь на питання».

🔑 **Слово Вови, що вирішило суперечку** (я захищав першу відповідь): «перша
коренева правильна відповідь… може бути частковою, і це можуть бути догадки».
Позначки «корисна/вирішено» немає → «перша» не давала гарантії якості й при
цьому втрачала свіжість. Друга половина: «коментарі на відповіді — підгрупа,
яку не потрібно висвітлювати» → **репліка в картку не потрапляє ніколи**.

🔑 **Дві осі не плутати:** «відповіді» (число + цитата) = ТІЛЬКИ кореневі ·
«активність/нове» (сортування, крапка вкладки) = будь-який коментар.

📐 **Щіль була ЗАЛИШКОМ, а не числом:** `flex: 1` = «забери весь вільний
простір». Заміряно **170px → 5px** (`flex: 0 1 auto`).

🔴 **Сторож двічі не міг упасти, і обидва рази це ловив контроль:** у фікстурі
була одна коренева відповідь (тож «перша» = «остання»), а щіль мірялась на
довгому тексті (рядок і так повний). Стенд **86/86**, контролі → 82/85 і 85/86.

### Раніше 24.08 (обидва акаунти, PR #1001-#1009)

🔴 **Подробиці — `_ai-tools/SESSION_STATE_VOVA.md` і журнали `-23b/-23c/-24`.**
Тут лише перелік, бо пульс пишеться під НОВУ сесію, а не накопичує історію.

| що | PR | найдорожчий урок |
|---|---|---|
| Мітки «бачив» синхронні між пристроями | #1009 | мітка рухається **тільки вперед** (`greatest`), час бере **сервер** — інакше старий таб дописав би вчорашню мітку |
| Тип 4 «питання без відповіді» | #1007 | `pg_cron` живе в UTC і про літній час не знає → розклад дзвонить двічі, годину звіряє сама функція |
| Персональне переїхало в акаунт | #1006 | `signOut()` зависав **назавжди** на `serviceWorker.ready`; `sidebar.js` ДВІЧІ стверджував, що перевірка є, — її не було |
| Видалення коментаря не працювало ВЗАГАЛІ | #1005 | падав `deleted_at` (42501): політика **ЧИТАННЯ** вимагала `deleted_at is null`, і автор ховав рядок ВІД СЕБЕ |
| «Цікавить» + B-33 (вимикачі сповіщень) | #1004 | усі чотири вимикачі були декоративні — `notif_prefs` писався в `localStorage` і не читався ніким |
| Гніздо імені · пара фото жителя · капсула автобуса | #1001-#1003 | сторож автобусів ваду не пропускав, а **ВИМАГАВ** — сцена звалась «НІЧ», а грала на денному рейсі |

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| Тип 4: перше справжнє спрацювання | **ще не було** (Вова 24.08: «ще не бачив») | деплой стався ~20:00 UTC 24.08, а розклад дзвонить о 08:00 UTC — тобто **після** сьогоднішнього проходу. Найближче — **25.08, 11:00 Києва**. Доведено лише відбір, воротар часу і межа 3 діб |
| Стеля «5 push із Питань на добу» (§12) | **в коді немає** | немає для ЖОДНОГО типу; тип 4 тримає себе межею «раз на 3 доби». Записано, а не забуто |
| Гілка префів в автобусах | **не виконувалась наживо** | `checked:0`, підписок нема; виконається при першому відстеженні |
| ~~Живий прохід push у Питаннях~~ | ✅ **перевірено пальцем 24.08** | Вова, два акаунти: «**Все окей**». Ланцюг «Цікавить → відповідь → push → тап у ту саму відповідь» доведено наживо |
| Відкладений push (batching) | не робили | потрібен планувальник; А2 закриває 80% болю за 5% роботи |
| Категорії питань | не робили | без них персоналізація типу 4 не має на чому стояти |
| **Android не тестувався ВЗАГАЛІ** | 0 перевірок · 📌 **відкладено рішенням Вови** | «Протестуємо коли доробимо все» (24.08). Усе живе тестування — iPhone Вови; Android — крок перед самим запуском, не зараз |
| Класифікатор новин (**B-28**) | відкрито | 94.5% статей → «Суспільство»; блокує B-14 |
| ПД в ІСТОРІЇ git | не стерто | перезапис історії ламає посилання на коміти |
| Тестовий збір **на проді** | у базі | адмінка → «Збори» → «Видалити» |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

✅ **24.08, 20:00 — Вова перевірив пальцем: «Все працює».** Закрито: синхронізація
новин телефон↔компʼютер · перемикання акаунта (push замовк на попередньому) ·
видалення коментаря в Питаннях і Стрічці · «Вийти».

✅ **24.08, 23:35 — живий прохід push у Питаннях на двох акаунтах: «Все окей».**
Ланцюг «Цікавить з А → відповідь з Б → push → тап веде до **тієї самої**
підсвіченої відповіді» доведено **пальцем**, а не лише стендом. Це був
останній недоказаний шматок MVP «Питань».

1. 🔴 **Вимикачі — єдине, що тримає B-33 відкритим:** вимкни «Питання» в
   кабінеті → push не приходить; вимкни на телефоні → на компʼютері теж
   вимкнено. ⚠️ «Все окей» Вова сказав саме про прохід push; чи входили в ту
   перевірку вимикачі — не уточнено, тож рядок **лишається відкритим**.
2. **Вхід через Google:** тепер +1 екран вибору акаунта. Заважає — відкотимо одним рядком.
3. 🔴 **Тип 4: перше спрацювання ще НЕ БУЛО — і це очікувано, не поломка.**
   Вова 24.08 ввечері: «ще не бачив». Розклад `qa-unanswered-push` = `0 8,9 * * *`
   **UTC**, а функція зʼявилась ~20:00 UTC 24.08 — тобто сьогоднішній прохід
   о 08:00 UTC стався **до деплою**. ➡️ **Найближче справжнє спрацювання —
   25.08, 08:00 UTC (11:00 Києва).** Тоді й питати «прийшло / чи не дратує».
4. **ПД в історії git** — стираємо? Чекаю «роби». · 5. Ключ `cstl-olyka-agent` — відкликати.

📌 **Android — рішення Вови 24.08: «Протестуємо коли доробимо все».** Нуль
перевірок на Android більше **не борг, який гасимо зараз**, а свідомо
відкладений крок перед самим запуском. У «НЕ ДОРОБЛЕНО» рядок лишається, з
черги «ДАЛІ» знятий.

## ➡️ ДАЛІ

**Обидва великі потоки закриті** — MVP «Питань» і account-scoped state, і 24.08
прохід push підтверджено пальцем. Далі — **не нова логіка, а підготовка до
запуску**: заслінка і вміст Дошки. _(Android — за рішенням Вови в самому кінці,
«коли доробимо все».)_

**Черга Вови** (`_ai-tools/BACKLOG_VOVA_2026-08.md`): 💬 чат Дошки — теплота
`#F3EFE3`/`#FCFAF4` · 📐 спадковий відступ контейнера Громади · 🚌 капсули проти
віджета автобусів.

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) · 🛑 **коди заслінки в репозиторій НЕ писати**.
- 🛑 **Знімки `tests/tools/_out/` НЕ комітити** — на них імена й телефони людей.
- 🔴 **`verify_jwt` у `send-bus-push` — УВІМКНЕНИЙ**; деплой із `false` тихо зняв би перший рубіж.
- 🔴 **TEXT проти `uuid`:** `reactions.user_id` і `push_subscriptions.user_uuid` — TEXT.
  Перед `.in('uid', …)` **обовʼязковий фільтр форми UUID**, інакше один сторонній
  рядок глушить УСІ сповіщення теми.
- 🛑 **Персональні сповіщення вимикача НЕ мають** · **`upsert` без `.select()`**, де результат не потрібен (№11-БІС).
- 🛑 **Кирилиця в назвах змінних bash** — падає з кодом 127 · **`Одеради АЗ` ≠ `Одеради`** · **порядок `@media` погоди в `style/home.css`** — у КІНЦІ секції.
- 🔴 **Клавіатура на iOS** (`style/feed.css:630,971`) — два фікси провалились (#638, #641).
- 🔴 **Три механізми закриття** · **`main` напряму не чіпати** · **дампи БД у репозиторій не класти** (він ПУБЛІЧНИЙ).
- 🔴 **`serviceWorker.ready` без SW не виконується НІКОЛИ** — не падає, а висить; бери `getRegistration()`.
- 🛑 **`pg_cron` живе в UTC і про літній час не знає** — час доби звіряти в коді, а не в рядку розкладу.
  І **секрет у текст розкладу не вписувати**: читати з `app_secrets` у мить виклику.
- ⚠️ **«Unverified» на GitHub — очікувано** · **`git diff` для «що не в main» — на ДВІ крапки** · **не піднімати стелю бюджету читання** в `tests/docs-fresh.mjs`.

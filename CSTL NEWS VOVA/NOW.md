# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-26 10:42 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-5nmz43` |
| останній коміт людини | `91601588` 26.08 10:42 — Аналітика: один період на весь екран, тап по числу → хто саме, збої поштучно |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260826-1041` |
| останній PR у `main` | #1039 |
| стендів на диску | 110 |
| незакомічених файлів | 0 ✅ |
| журнал за 2026-08-26 | є ✅ |
| потік `/byyou` | idle — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `CSTL NEWS VOVA/_ai-tools/AUDIT_ANALYTICS_2026-08.md`
- `_session-log/vova-2026-08-26.md`
- `admin.html`
- `bundle.js`
- `scripts/supabase_analytics_drilldown.sql`
- `src/app.js`
- `src/core/supabase.js`
- `sw.js`
- `tests/analytics-screen.mjs`

**Останні кроки (без автокомітів парсерів):**

- `91601588` 26.08 10:42 — Аналітика: один період на весь екран, тап по числу → хто саме, збої поштучно
- `2ea5a657` 26.08 10:07 — Merge старої гілки (вміст уже в main)
- `0e7b6342` 26.08 10:07 — Аналітика: години 00:00→23:00 підряд, рядок «—» прибрано з пристроїв
- `17a22474` 26.08 09:59 — Збої: імена акаунтів, кількість випадків і копіювання всього одним натиском (#1038)
- `a9e6ff67` 26.08 09:59 — ai(news): памʼять+чернетки+витрати 26.08 09:59 UTC

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

📦 **26.08. Уся робота в `main` і на сайті.** `CACHE_NAME` `cstl-20260826-0914`.

🔴 **ГОЛОВНЕ ЗА ДЕНЬ — АНАЛІТИКА.** Вранці полагоджено ДАНІ (стеля 1000: «203 за 7 днів»
це **6 людей**), увечері переписано ЕКРАН за голосовим текстом Вови. Дорогою знайдено:
**кожен захід писався «гостем»** — `session_start` ішов раніше, ніж `getSession()`
відповість. Розбір нижче.

🟢 **Відкритих критичних немає.** 🔴 **«Блим» при виході свайпом НЕ вилікувано** — Вова:
«хай буде так». 🔴 **Названий борг** — дві реалізації авто-каруселі, деталі в «ДАЛІ».

🔀 **№13:** злиття ДО роботи · ніколи `-X theirs` · після КОЖНОГО злиття
`grep -rn "^<<<<<<<"` ПЕРЕД `git add` · 🛑 `node --check` НЕ ловить відсутній імпорт.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

### 26.08 ВЕЧІР: АНАЛІТИКА — ЕКРАН ПЕРЕПИСАНО ЗА СЛОВАМИ ВОВИ

🗣️ Джерело правди — **оригінальний голосовий текст Вови**, не концепція ChatGPT:
«тільки правдива інформація… не все на кучу… базові видно зразу, можна відкрити детальніше».

🔴 **ВАДА, ЗНАЙДЕНА ЧИТАННЯМ ПОРЯДКУ ВИКЛИКУ: кожен захід писався «гостем».** `init()`
кличе `initAuth()` **без `await`**, а `passDevLock()` повертається з `localStorage`
синхронно — тож `session_start` писався раніше, ніж `getSession()` відповість.
📐 У ВСІХ 16 подій `session_start` `user_id` порожній, тоді як `tab_view` — уже з ним.
➡️ «Унікальні користувачі» показували б **нуль** назавжди. Фікс — `await authReady()`.
🔑 Друга знахідка: перезавантаження пише ДРУГИЙ `session_start` із тим самим
`session_id` — без `distinct on` один захід рахувався за два.

**Екран:** один період (Сьогодні · 7 днів · Місяць) керує **всім** (раніше картки згори
були за три різні періоди, а розділи нижче завжди за 7 днів — звідси «81 це за що?») ·
**тап по будь-якому числу → хто саме** (імʼя, пошта, село, заходи, дні) · три картки
замість суми: Людей · Заходів · Гостьових пристроїв · «Звідки заходять» (PWA/браузер)
через `display-mode` · збої: тап → кожен випадок поштучно · «хто заходив по селах»
окремо від зрізу анкет · **пояснено, звідки вік**: Google дати народження не дає.
🛑 **Чого немає — сказано на екрані:** PWA/браузер і «заходи» існують лише з 26.08;
домалювати за старі дні неможливо (UA однаковий).
🧪 `tests/analytics-screen.mjs` **48/48**, контроль `origin/main` → **1/48**.
📖 `AUDIT_ANALYTICS_2026-08.md` ч.2, журнал `-26`.

### 26.08 РАНОК: АНАЛІТИКА — ЦИФРИ СТАЛИ ПРАВДИВИМИ

🔴 Усі числа були обрізані стелею 1000 (`db-max-rows`, клієнт підняти НЕ МОЖЕ).
🔑 **Доказ видно без бази:** 544+164+134+82+76 = **рівно 1000**. 📐 Правда: сьогодні **1**
людина (казав 77) · 7 днів **6** (казав 203) · весь час **11** (казав 35). 736
«унікальних» = 11 акаунтів + 725 пристроїв, з них **628 з однією подією**.
**Зроблено:** агрегати рахує БАЗА · `user_id`/`anon_id`/`session_id` · події з 4 до 8 ·
година в поясі Києва · `discussions` — НЕ стара вкладка, це живі «Питання».
🛑 **Свідомо немає:** `session_end` · retention/DAU/MAU (правило №12).
📖 `_ai-tools/AUDIT_ANALYTICS_2026-08.md`

### 26.08: ЗБОЇ — ІМЕНА АКАУНТІВ І КОПІЮВАННЯ ОДНИМ НАТИСКОМ

🗣️ «щоб я міг скопіювати однією кнопкою… позначай імʼя акаунта… і кількість їх».
🛑 Скасовано попереднє СВІДОМЕ рішення («журнал збоїв не має бути журналом людей»):
📐 у ВСІХ збоїв стояло **`акаунтів: 0`** — імені не було з чого взяти.
🔑 Місток `setAnalyticsUid()` (прямий `currentUserId()` дав би **кільце імпортів**) ·
кнопка має **запасний шлях** (`navigator.clipboard` є лише в захищеному контексті).

### 25-26.08: ВІДЖЕТ СТРІЧКИ НА ГРОМАДІ (шість редакцій, #1032-#1036)

Порядок спільнот **за свіжістю** · ряд на всю ширину · картка 280→400px · виділення
**гасінням решти** · **тап веде в САМУ спільноту**, з допису — з наведенням.
🛑 **П'ЯТЬ УРОКІВ:** (1) у flex-колонці все, що має показатись ЦІЛКОМ, потребує
`flex-shrink: 0` — текст різала ВИСОТА, а не `line-clamp`; (2) висоту тексту тільки в
`em`, не в `px`; (3) знімок довантажується ПІСЛЯ прокрутки — чекати з МЕЖЕЮ ЧАСУ;
(4) 🔴 **«блим» НЕ вилікувано** — системний жест малює знімок попереднього запису історії;
(5) **обґрунтування переживає свою підставу** — перевіряй не лише код, а й **ЧОМУ**.
🔴 **СІМ ПОМИЛОК ВИМІРУ** — жодну не знайшло написання, усі контроль або скрін Вови.
➡️ **Наведення і «блим» — ЛИШЕ пальцем.** `home-feed-widget.mjs` **39/39**. 📖 `-25e`, `-26`.

### 25.08: заслінка (#1030) · пункт 0 — гарантія «хто я» (#1029) · раніше (#1001-#1026)

🔴 **Заслінка:** «з компʼютера код не підходить» — код був ПРАВИЛЬНИЙ: сторінка по
`http://`, а `crypto.subtle` є лише в захищеному контексті. 🛑 Вада не в замку, а в тому,
що він КАЗАВ: «помилився» і «не можу перевірити» давали однакову кару.
🔴 **Пункт 0:** `initAuth()` без `await` → вкладки малювали вигляд ГОСТЯ. ⚠️ **Той самий
корінь, що у вечірньої вади аналітики** — цей клас повертався вже двічі.
Раніше: непрочитане · голос спільноти · нічна тиша (**B-33 закрито**) · `authReady()` у
Стрічці · редагування свого питання. ⚠️ У `board.js` `markQaFloorOnce()` ПЕРЕД
`markChatSeen()`. 📖 `SESSION_STATE_VOVA.md`, журнали `-23b`…`-25e`.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| 🔴 **Дві реалізації авто-каруселі** + віджет **не бачений пальцем** | борг у шапці `core/auto-carousel.js` | новини не переведені; скрін не долетів |
| Тип 4: перше спрацювання | нічого не підійшло під вікно | **27.08** (питанню буде 21 год при потрібних 24) |
| `CSTL_BUGS.md` = 40.5 KB при стелі 40 KB | запасу **0.5 KB** | наступний баг — стисло або виносити історію |
| Стеля «5 push із Питань на добу» (§12) | **в коді немає** | заміряно по **6** — спрацювала б двічі |
| Префи автобусів · batching · категорії питань | не робили | підписок нема; потрібен планувальник |
| **Android не тестувався ВЗАГАЛІ** | 📌 відкладено Вовою | «Протестуємо коли доробимо все» |
| Класифікатор новин (**B-28**) | відкрито | ~94% статей → «Суспільство»; блокує B-14 |
| ПД в ІСТОРІЇ git · тестовий збір на проді | не стерто / у базі | перезапис історії ламає посилання |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

✅ **Перевірено пальцем 24-26.08:** новини · перемикання акаунта · видалення коментаря ·
«Вийти» · push · сортування · **вимикачі (B-33)** · віджет Стрічки (шість редакцій).

0. 🔴 **НЕ ПЕРЕВІРЕНЕ ПАЛЬЦЕМ — гарантія «хто я» (холодний старт, PWA):** крапка
   непрочитаного ОДРАЗУ · push веде в чат · збережені позначені · ❤️ зафарбоване ·
   привітання з ІМЕНЕМ · ⚠️ **Автобуси малюють розклад МИТТЄВО**.
0-БІС. 🔴 **Чи відкривається `https://castlelife.org`** — з контейнера не перевірити.
1. 🔴 **Тип 4 — 27.08:** питанню на завтра буде 21 год при потрібних 24.
   ⚠️ У базі ОДНА push-підписка на всіх, а автор питання з розсилки випадає завжди.
2-6. **Нічна тиша · антиспам · непрочитане · редагування питання · голос спільноти** —
   сценарії в журналах `-25b/-25c` і `SESSION_STATE_VOVA.md`.
7. Google-вхід: +1 екран · 8. ПД в історії git — стираємо? · 9. Ключ
   `cstl-olyka-agent` — відкликати. 📌 **Android** — «протестуємо коли доробимо все».

## ➡️ ДАЛІ

🔴 **ПЕРШЕ — аналітика пальцем:** відкрити адмінку і **зайти залогіненим**. До 26.08
жоден захід не мав акаунта (див. вечірній розділ), тож «Людей» покаже правду лише з нових
заходів. Перевірити: перемикач періоду · тап по числу → список людей · тап по збою.
🔴 **ДРУГЕ — названий борг:** перевести карусель новин (`startNewsCarousel` у
`tabs/community-blocks.js`) на спільний `core/auto-carousel.js`. ⚠️ Звіряй рядок у рядок:
у старій є запобіжники, куплені окремими багами.

✅ **Пункт 0 закрито 25.08 (#1029) — не бери його вдруге.**

**Далі за чергою Вови:** 💬 чат Дошки `#F3EFE3`/`#FCFAF4` · 📐 відступ Громади ·
🚌 капсули проти автобусів. _(Android — у самому кінці.)_

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** · 🛑 коди заслінки в репозиторій НЕ писати · 🛑 знімки
  `tests/tools/_out/` НЕ комітити (імена й телефони) · 🔴 **`verify_jwt` у
  `send-bus-push` — УВІМКНЕНИЙ**.
- 🔴 **TEXT проти `uuid`:** `reactions.user_id`, `push_subscriptions.user_uuid` — TEXT;
  перед `.in('uid', …)` **обовʼязковий фільтр форми UUID** — один сторонній рядок глушить
  УСІ сповіщення теми. · `upsert` без `.select()` (№11-БІС) · SELECT-політика на свої
  рядки · **кирилиця в назвах змінних bash** = код 127 · `Одеради АЗ` ≠ `Одеради` ·
  `@media` погоди — у КІНЦІ `style/home.css`.
- 🔴 **Клавіатура на iOS** (`feed.css:630,971`) — два фікси провалились · **`main`
  напряму не чіпати** · дампи БД не класти (репо ПУБЛІЧНИЙ) · `serviceWorker.ready` без
  SW не виконується НІКОЛИ · 🛑 `pg_cron` живе в UTC.
- 🛑 **НЕ МІРЯЙ ПОСЕРЕД РОБОТИ** — файлів не чіпати, поки йде повний набір · ⚠️
  «Unverified» — очікувано · `git diff` «що не в main» — на ДВІ крапки · **стелю бюджету
  в `docs-fresh` НЕ піднімати**.
- 🔑 Решта гострого — `HOT_RULES.md` і `CSTL_BUGS.md`. Тут лише те, що ламали НЕЩОДАВНО.

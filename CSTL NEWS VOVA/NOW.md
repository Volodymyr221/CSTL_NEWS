# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-25 11:36 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-upyy76` |
| останній коміт людини | `31981ce8` 25.08 11:36 — Merge remote-tracking branch 'origin/main' into claude/startuem-upyy76 |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260825-1012` |
| останній PR у `main` | #1016 |
| стендів на диску | 104 |
| незакомічених файлів | 0 ✅ |
| журнал за 2026-08-25 | **НЕМАЄ** ⚠️ |
| потік `/byyou` | idle — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `CSTL NEWS VOVA/CLAUDE.md`
- `CSTL NEWS VOVA/CSTL_BUGS.md`
- `CSTL NEWS VOVA/NOW.md`
- `CSTL NEWS VOVA/START_HERE.md`
- `CSTL NEWS VOVA/_ai-tools/AUDIT_2026-07.md`
- `CSTL NEWS VOVA/_ai-tools/SESSION_STATE_VOVA.md`
- `CSTL NEWS VOVA/docs/QA_CONCEPT.md`
- `_session-log/vova-2026-08-24b.md`
- `supabase/functions/send-answer-push/index.ts`
- `tests/README.md`
- `tests/night-silence.mjs`

**Останні кроки (без автокомітів парсерів):**

- `31981ce8` 25.08 11:36 — Merge remote-tracking branch 'origin/main' into claude/startuem-upyy76
- `5d713382` 25.08 11:35 — Журнал, літопис і пульс: нічна тиша, B-33, звірка документів
- `d004db6d` 25.08 11:30 — Сторож нічної тиші + B-33 закрито + чесність tests/README
- `cdd54400` 25.08 11:23 — Документи: звірено з кодом три застарілі записи
- `86d27e33` 25.08 11:13 — Нічна тиша 22:00-08:00 для типів 1 і 3 (QA_CONCEPT §12)

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

🔴 **ДРУГИЙ АКАУНТ ПРАЦЮЄ НАД «СТРІЧКОЮ»** (відповідати на коментарі від імені
СПІЛЬНОТИ або акаунта). Ця сесія — над **Питаннями**, файли різні, і це добре.
🛑 **АЛЕ `main` пішов далеко вперед (PR #1012-#1016).** ➡️ Перше там:
`git fetch origin main && git merge origin/main` **ДО** роботи, а не перед PR.

⚠️ **Де конфлікт означає ВТРАТУ, а не вибір** (перевірено чотири рази за день):
`SESSION_STATE_VOVA.md` і `_session-log/*.md` — зводити руками по межах
заголовків · `NOW.md` — брати чужу версію і переписувати прозу заново ·
`bundle.js` — не зводити, а перезібрати `node build.js`.
🔑 **Спільні файли, які чіпала ця сесія:** `src/core/utils.js`, `style/base.css`
(`--qa-line`), `tests/questions.mjs`. Решта — зона Питань.

🔀 Повні правила роботи вдвох — **HOT_RULES №13**: злиття ДО роботи · ніколи
`-X theirs/-X ours` · перевірка «а чи є в `main` ЧУЖЕ» — ПЕРША дія при злитті.

🏁 **Вкладка «Питання» за 24-25.08 доведена до ладу:** MVP закрито, картка
показує останню кореневу відповідь, відповідати можна на будь-яку, лінія
суцільна й чорна, гілки згортаються, імена ведуть у профіль, антиспам не
блокує «Окей» назавтра.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

### 25.08, третє: нічна тиша + документи звірені з кодом

🔴 **НІЧНА ТИША 22:00–08:00** (`send-answer-push` **v6**, типи 1 і 3).
🔑 **Пропущене не губиться:** уночі ми не пишемо в `answer_push_targets`, а
`missed()` рахує від останнього запису — перший ранковий push САМ скаже «3 нові
відповіді». Тому вихід стоїть ДО `mark()`: після нього тиша стала б ВТРАТОЮ.
⚠️ **Заміряно:** вікно перетинає північ → умова через `||`. Через `&&` тиша дала б
**нуль** годин. Тип 1 «вам відповіли» тиші не має навмисно (§12).
✅ Живі виклики: чужий секрет → **401**, справжній + неіснуючий коментар → **200
`answer gone`** (тобто задеплоєна саме ця редакція).

🆕 **Сторож `tests/night-silence.mjs` (14/14, стендів 104).** ВИКОНУЄ умову, а не
грепає: витягає текст `ніч()` і проганяє 24 години. Контроль двошаровий, і другий
важливіший — **мутант `||`→`&&` дає 10/14**, хоча проходить будь-яку регулярку.

📄 **Документи звірені з кодом, а не «причесані»:** B-33 ЗАКРИТО · §18 «Пошук по
питаннях» був зроблений давно · АУДИТ О-6 стосувався екрана, якого немає (лічильник
прибрано 11.08, стенд ВИМАГАЄ його відсутності) · О-7 🔴→🟡.
🛑 **Знахідка:** `tests/README.md` описує **43 стенди зі 104** і не дописувався з
серпня — `docs-refs` стереже ЧИСЛО, а не рядок на кожен, тож 61 стенд виріс
непоміченим при зелених прогонах. Дописано попередження, а не 61 рядок заднім числом.

### 25.08 — вкладка «Питання» доведена (PR #1013-#1016)

🔴 **Відповідати можна на БУДЬ-ЯКУ відповідь.** Кнопку ховали зі страху «росло б
дерево», а захист уже стояв поруч. 🛑 **І моя ж кнопка відкрила ваду:** підняття
до кореня робило ОДИН стрибок — у ланцюгу `R←A←B←C` рядок **зникав мовчки**.

📐 **Лінія:** бракувало третього правила зі «Стрічки» (стовбур наскрізь), колір
→ **чорний 28%** `#AAABAE`: контраст **1.06 → 1.98:1**. Суцільний чорний
відхилено — 18.07:1, втричі гучніше за основний текст.

🔑 **Список — за датою ПИТАННЯ** (вада була в РОЗХОДЖЕННІ: картка показує одну
дату, порядок будувався за іншою). **Згортання гілок** перенесено зі «Стрічки»
разом із двома вилікуваними там пастками прокрутки (тримаємось за КНОПКУ, не за
корінь; висоту НЕ анімуємо). 🛑 **Зловив у себе:** `comments.id` — `bigint`, а
`dataset` вертає рядок; набір не збігся б НІКОЛИ. Заведено `qaKey()`.

🔴 **Антиспам:** дубль не мав вікна і блокував «Окей» НАЗАВЖДИ. Тепер за 15с — не
більше 8 повідомлень і не двічі те саме. Шарів два: клієнт і тригери бази.

🔴 **Тап по імені → картка профілю.** Бракувало `data-av-uid` на іменах (аватар
його мав — звідси асиметрія). 🛑 **Заразом закрито ваду з першого дня картки:**
тап по аватару в списку відкривав ОДРАЗУ ДВОЄ — профіль І питання.
**98/98**, контроль → **90/98**.

### До 25.08 (PR #1001-#1012)

🔴 **Усе в літописі `_ai-tools/SESSION_STATE_VOVA.md` і журналах `-23b/-23c/-24/-24b`.**
Коротко: мітки «бачив» синхронні між пристроями · тип 4 «питання без відповіді» ·
персональне переїхало в акаунт · видалення коментаря не працювало ВЗАГАЛІ ·
«Цікавить» і B-33 · один лічильник відповідей замість чотирьох.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| Тип 4: перше справжнє спрацювання | 25.08 нічого не підійшло під вікно 24год–7діб | чекаємо 26.08, 11:00 Києва |
| `CSTL_BUGS.md` = 40.5 KB при стелі 40 KB на файл | лишилось **0.5 KB** запасу | наступний баг доведеться писати стисло або виносити історію |
| Стеля «5 push із Питань на добу» (§12) | **в коді немає** | немає для ЖОДНОГО типу; тип 4 тримає себе межею «раз на 3 доби» |
| Гілка префів в автобусах | не виконувалась наживо | `checked:0`, підписок нема |
| Відкладений push (batching) | не робили | потрібен планувальник |
| Категорії питань | не робили | без них персоналізація типу 4 не має на чому стояти |
| **Android не тестувався ВЗАГАЛІ** | 📌 відкладено рішенням Вови | «Протестуємо коли доробимо все» |
| Класифікатор новин (**B-28**) | відкрито | ~94% статей → «Суспільство»; блокує B-14 |
| ПД в ІСТОРІЇ git | не стерто | перезапис історії ламає посилання на коміти |
| Тестовий збір **на проді** | у базі | адмінка → «Збори» → «Видалити» |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

✅ **Перевірено пальцем 24-25.08:** синхронізація новин · перемикання акаунта ·
видалення коментаря · «Вийти» · прохід push на двох акаунтах · відповіді на
відповіді, лінія, сортування · **вимикачі сповіщень — «все працює чудово»
(B-33 ЗАКРИТО, відкритих критичних не лишилось)**.

1. 🔴 **Тип 4: чи прийде 26.08 об 11:00 Києва?** 25.08 НЕ спрацював не через
   ваду: у базі перевірено — розклад пройшов двічі, журнал порожній, бо **під
   вікно 24год–7діб не потрапило ЖОДНЕ питання** (три без відповіді старші за
   7 діб). Вова написав свіже питання — завтра буде перший чесний прогін.
2. 🆕 **Нічна тиша (25.08):** відповідь після 22:00 → push НЕ приходить; вранці
   перший push каже «N нових відповідей», а не одну.
3. 🆕 **Антиспам:** «Окей» одразу вдруге → блокує · через 20с → пропускає.
4. **Вхід через Google:** +1 екран вибору акаунта. Заважає — відкотимо одним рядком.
5. **ПД в історії git** — стираємо? Чекаю «роби». · 6. Ключ `cstl-olyka-agent` — відкликати.

📌 **Android — рішення Вови: «Протестуємо коли доробимо все».** Не борг, який
гасимо зараз, а свідомо відкладений крок перед запуском.

## ➡️ ДАЛІ

**Обидва великі потоки закриті** — MVP «Питань» і account-scoped state, прохід
push підтверджено пальцем, відкритих критичних немає. Далі — **не нова логіка, а
підготовка до запуску**: заслінка і вміст Дошки. _(Android — в самому кінці.)_

**Черга Вови** (`_ai-tools/BACKLOG_VOVA_2026-08.md`): 💬 чат Дошки — теплота
`#F3EFE3`/`#FCFAF4` · 📐 спадковий відступ контейнера Громади · 🚌 капсули проти
віджета автобусів.

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) · 🛑 **коди заслінки в репозиторій НЕ писати**.
- 🛑 **Знімки `tests/tools/_out/` НЕ комітити** (імена й телефони людей) · 🔴 **`verify_jwt`
  у `send-bus-push` — УВІМКНЕНИЙ** (деплой із `false` тихо зняв би перший рубіж).
- 🔴 **TEXT проти `uuid`:** `reactions.user_id` і `push_subscriptions.user_uuid` — TEXT.
  Перед `.in('uid', …)` **обовʼязковий фільтр форми UUID**, інакше один сторонній
  рядок глушить УСІ сповіщення теми.
- 🛑 **Персональні сповіщення вимикача НЕ мають** · **`upsert` без `.select()`** (№11-БІС)
  · **пишеш `upsert` — заведи SELECT-політику на свої рядки** · **кирилиця в назвах
  змінних bash** падає з кодом 127 · **`Одеради АЗ` ≠ `Одеради`** · **`@media` погоди
  в `style/home.css`** — у КІНЦІ секції.
- 🔴 **Клавіатура на iOS** (`style/feed.css:630,971`) — два фікси провалились (#638, #641).
- 🔴 **Три механізми закриття** · **`main` напряму не чіпати** · **дампи БД у репозиторій
  не класти** (ПУБЛІЧНИЙ) · **`serviceWorker.ready` без SW не виконується НІКОЛИ** —
  не падає, а висить.
- 🛑 **`pg_cron` живе в UTC і про літній час не знає** — час доби звіряти в коді.
  І **секрет у текст розкладу не вписувати**: читати з `app_secrets` у мить виклику.
- 🔴 **Делегати на `document` НЕ спиняють поширення** — один тап може відкрити ДВОЄ
  (доведено 25.08 приладом). Заводиш нову дію (у Стрічці теж) — перевір, чи не
  спрацює заразом старий делегат.
- 🛑 **Поки повний набір стендів іде — файлів НЕ чіпати** (24.08 три прогони з чотирьох
  були недостовірні саме через це). · ⚠️ **«Unverified» на GitHub — очікувано** · **`git diff` для «що не в main» — на ДВІ
  крапки** · **не піднімати стелю бюджету читання** в `tests/docs-fresh.mjs`.

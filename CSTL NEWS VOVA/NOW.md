# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-25 14:20 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-uh2hzo` |
| останній коміт людини | `4fce1efe` 25.08 14:16 — Merge remote-tracking branch 'origin/main' into claude/startuem-uh2hzo |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260825-1331` |
| останній PR у `main` | #1022 |
| стендів на диску | 106 |
| незакомічених файлів | 2 ⚠️ |
| журнал за 2026-08-25 | є ✅ |
| потік `/byyou` | idle — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- ✅ нічого. Уся робота гілки вже в `main`.

**Останні кроки (без автокомітів парсерів):**

- `4fce1efe` 25.08 14:16 — Merge remote-tracking branch 'origin/main' into claude/startuem-uh2hzo
- `336cef36` 25.08 14:07 — ai(news): памʼять+чернетки+витрати 25.08 14:07 UTC
- `9b940970` 25.08 13:59 — Перемикач голосу: кільце стану, значок ⇄, підказка один раз · гарантія «хто я» — пункт 0 беклогу (#1022)
- `43ead084` 25.08 13:58 — Пульс: авто-блок
- `43af5647` 25.08 13:58 — Злиття origin/main (#1021)

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

🏁 **ЧОТИРИ СЕСІЇ 25.08 ЗАКРИТІ, УСЕ В `main` І НА САЙТІ.** «Питання» (#1013-#1016) ·
голос спільноти (#1017) · нічна тиша (#1018) · підписи push (#1019) · гарантія «хто я»
(#1020) · кільце/значок/підказка (#1022). Деплої **#5053 · #5058 · #5063** — success.
🟢 Відкритих критичних багів немає. Повний набір: **106/106 · 2118/2118**.

🔴 **ПЕРШЕ, ЩО РОБИТИ НОВІЙ СЕСІЇ — `BACKLOG_VOVA_2026-08.md` ПУНКТ 0** (він угорі
файлу). Замовлення Вови 25.08: «запиши це в головне завдання для нової сесії, щоб вона
була в курсі контексту і всі одразу почала робити». Це **не нова фіча**, а доведення
початого: `authReady()` написаний і перевірений, застосований лише до Стрічки.

🔴 **ЗНАХІДКА, ЩО ДІЄ ДАЛІ:** `initAuth()` в `app.js` кличеться **БЕЗ `await`** — будь-яка
вкладка, що кешує «моє» при старті, зобовʼязана або чекати `authReady()`, або підписатись
на `onAuthChange`. Інакше вигляд ГОСТЯ для залогіненої людини.

🔀 **HOT_RULES №13:** злиття ДО роботи · ніколи `-X theirs/-X ours` · перевірка «чи є в
`main` ЧУЖЕ» — ПЕРША дія. Сьогодні зводив чотири рази, конфлікти були щоразу.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

### 25.08, четверта сесія: «хто я» стає ФАКТОМ (#1020, #1022)

🔴 **«Не можу писати пости» + «лайк не підсвічений» + «немає перемикача» — ОДНА ВАДА,
архітектурна:** застосунок малював залежне від входу, **не знаючи ще, хто зайшов**.
`initAuth()` без `await` → `currentUserId()` = `null` → пʼять запитів із восьми чесно
відповідали про ГОСТЯ. 🛑 Слово Вови: **«професійно та комплексно, а не як латка на
латці»** — підписка лікує ПІСЛЯ факту, тобто блимне чуже. 🔑 **Гарантія тепер в
`auth.js`: `authReady()`**; межа часу обовʼязкова (клас `serviceWorker.ready` — не
падає, ВИСНЕ). ✅ Доведено ВИКОНАННЯМ: сесія → 0мс · завислий виклик → 401мс.

🔴 **«Поле вводу не піднімається доверху»** — моя ж перша редакція: смуга стала ТРЕТІМ
поверхом під клавіатурою. ⚠️ У Chromium відтворити НЕ вдалось (HOT_RULES №9) → не
лагодив `core/keyboard.js`, а **прибрав поверх**.
🔴 **«Стрілочку погано видно»** → три засоби на три різні питання: **кільце** (що
зараз) · **⇄** замість ▾, 16px (це кнопка) · **підказка один раз** (воно є).
📐 **Стос під клавіатурою 94px → 94px.** `comment-as-page` **25/25**, `feed-auth-race`
**15/15** (контроль 4/15). Повний набір: **106/106 · 2118/2118**.

### 25.08, третя сесія: нічна тиша + документи звірені з кодом

🔴 **НІЧНА ТИША 22:00–08:00** (`send-answer-push` **v6**, типи 1 і 3).
🔑 **Пропущене не губиться:** уночі не пишемо в `answer_push_targets`, а `missed()`
рахує від останнього запису — перший ранковий push САМ скаже «3 нові відповіді».
Тому вихід стоїть ДО `mark()`: після нього тиша стала б ВТРАТОЮ.
⚠️ **Заміряно:** вікно перетинає північ → умова через `||`; через `&&` тиша дала б
**нуль** годин. Тиша — типам 1 і 3; тип 2 «відповіли на ваш коментар» її не має
навмисно (§12: особисте звернення).
✅ Живі виклики: чужий секрет → **401**, справжній + неіснуючий коментар → **200
`answer gone`** (задеплоєна саме ця редакція).
🆕 **Сторож `night-silence.mjs` (14/14)** ВИКОНУЄ умову, а не грепає: витягає текст
`ніч()` і проганяє 24 години. Контроль двошаровий, і другий важливіший — **мутант
`||`→`&&` дає 10/14**, хоча проходить будь-яку регулярку.
🟢 **B-33 ЗАКРИТО** («все працює чудово») — **відкритих критичних не лишилось**.
📄 **Документи звірені:** §18 «Пошук по питаннях» зроблено давно · АУДИТ О-6
стосувався екрана, якого немає · О-7 🔴→🟡. 🛑 **Знахідка:** `tests/README.md`
описує **43 стенди зі 105** — `docs-refs` стереже ЧИСЛО, а не рядок на кожен, тож
62 стенди виросли непоміченими при зелених прогонах.

### 25.08, друга сесія: голос спільноти у «Стрічці» (#1017)

🔑 `author_uid` — ЗАВЖДИ людина, що натиснула «Надіслати»; `as_page_id` — від чийого
імені це ЗВУЧИТЬ. 🛑 **`actor_type`/`actor_id` (ChatGPT) ВІДХИЛЕНО:** без сліду людини
сліпне модерація, а антиспам, видалення, згадки, капсули і push ключуються на
`author_uid`. 🛑 **Default — ОСОБИСТИЙ.** 🔴 Бейдж «Адмін» не було З ЧОГО малювати →
`page_team_flags()`: підтверджує роль, списку не віддає.

### Раніше 25.08 (PR #1001-#1016) — усе в літописі

🔴 **Розбір — `SESSION_STATE_VOVA.md` і журнали `-23b/-23c/-24/-24b`.** Коротко: лінія
чорна 28% · відповідь на будь-яку відповідь · сортування за датою ПИТАННЯ · згортання
гілок · антиспам (дубль не мав вікна) · мітки «бачив» між пристроями · тип 4 ·
персональне в акаунті · «Цікавить» і B-33.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| Тип 4: перше справжнє спрацювання | 25.08 нічого не підійшло під вікно 24год–7діб | чекаємо 26.08, 11:00; у базі всього **одна** push-підписка |
| `CSTL_BUGS.md` = 40.5 KB при стелі 40 KB | запасу **0.5 KB** | наступний баг писати стисло або виносити історію |
| Стеля «5 push із Питань на добу» (§12) | **в коді немає** | немає для ЖОДНОГО типу; тип 4 тримає себе межею «раз на 3 доби» |
| Гілка префів в автобусах | не виконувалась наживо | `checked:0`, підписок нема |
| Відкладений push (batching) | не робили | потрібен планувальник |
| Категорії питань | не робили | без них персоналізація типу 4 не має на чому стояти |
| **Android не тестувався ВЗАГАЛІ** | 📌 відкладено рішенням Вови | «Протестуємо коли доробимо все» |
| Класифікатор новин (**B-28**) | відкрито | ~94% статей → «Суспільство»; блокує B-14 |
| Тестовий збір **на проді** | у базі | адмінка → «Збори» → «Видалити» |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

✅ **Перевірено пальцем 24-25.08:** синхронізація новин · перемикання акаунта ·
видалення коментаря · «Вийти» · прохід push на двох акаунтах · відповіді, лінія,
сортування · вимикачі (**B-33 ЗАКРИТО**) · **голос спільноти і холодний старт після
#1020 — «працює нормально, правильно, логічно»**.

1. 🔴 **Тип 4: чи прийде 26.08 об 11:00?** 25.08 не спрацював НЕ через ваду: розклад
   пройшов двічі, журнал порожній — **під вікно 24год–7діб не потрапило жодне
   питання** (три без відповіді старші за 7 діб). Вова написав свіже.
2. 🆕 **Нічна тиша:** відповідь після 22:00 → push НЕ приходить; вранці перший
   push каже «N нових відповідей», а не одну.
3. 🆕 **Антиспам:** «Окей» одразу вдруге → блокує · через 20с → пропускає.
4. 🆕 **Кільце · значок ⇄ · підказка (#1022, ще не дивився):** підказка має
   вискочити РАЗ · кільце помітне · значок читається як дві стрілки.
5. **Вхід через Google:** +1 екран вибору акаунта. Заважає — відкотимо одним рядком.
6. **ПД в історії git** — стираємо? Чекаю «роби». · 7. Ключ `cstl-olyka-agent` — відкликати.

📌 **Android — рішення Вови: «Протестуємо коли доробимо все».** Не борг, а свідомо
відкладений крок перед запуском.

## ➡️ ДАЛІ

🔴 **ПЕРШЕ ЗАВДАННЯ НОВОЇ СЕСІЇ — `BACKLOG_VOVA_2026-08.md` ПУНКТ 0: «гарантія «хто я»
на всі вкладки».** Слово Вови 25.08: «запиши це в головне завдання для нової сесії,
щоб вона… відразу почала робити». Повний контекст — там, у пункті 0.
🔑 Це не нова фіча: `authReady()` (`core/auth.js`) написаний і перевірений виконанням,
але застосований **лише до Стрічки**. Решта вкладок на холодному старті ще можуть
мигнути виглядом гостя — та сама хвороба, тільки тихіша.
🛑 Не одним заходом: одна вкладка = один крок = один вимір. Сторож уже стоїть.

**Далі за чергою Вови:** 💬 чат Дошки `#F3EFE3`/`#FCFAF4` · 📐 відступ контейнера
Громади · 🚌 капсули проти віджета автобусів. _(Android — у самому кінці.)_

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) · 🛑 **коди заслінки в репозиторій НЕ писати**.
- 🛑 **Знімки `tests/tools/_out/` НЕ комітити** (імена й телефони людей) · 🔴 **`verify_jwt`
  у `send-bus-push` — УВІМКНЕНИЙ** (деплой із `false` тихо зняв би перший рубіж).
- 🔴 **TEXT проти `uuid`:** `reactions.user_id` і `push_subscriptions.user_uuid` — TEXT;
  перед `.in('uid', …)` **обовʼязковий фільтр форми UUID**, інакше один сторонній рядок
  глушить УСІ сповіщення теми.
- 🛑 **Персональні сповіщення вимикача НЕ мають** · **`upsert` без `.select()`** (№11-БІС)
  · **пишеш `upsert` — заведи SELECT-політику на свої рядки** · **кирилиця в назвах
  змінних bash** падає з кодом 127 · **`Одеради АЗ` ≠ `Одеради`** · **`@media` погоди
  в `style/home.css`** — у КІНЦІ секції.
- 🔴 **Клавіатура на iOS** (`style/feed.css:630,971`) — два фікси провалились (#638, #641)
  · **Три механізми закриття** · **`main` напряму не чіпати** · **дампи БД у репозиторій
  не класти** (ПУБЛІЧНИЙ) · **`serviceWorker.ready` без SW не виконується НІКОЛИ**.
- 🛑 **`pg_cron` живе в UTC і про літній час не знає** — час доби звіряти в коді.
  І **секрет у текст розкладу не вписувати**: читати з `app_secrets` у мить виклику.
- 🔴 **Делегати на `document` НЕ спиняють поширення** — один тап може відкрити ДВОЄ
  (доведено 25.08). Нова дія — перевір, чи не спрацює заразом старий делегат.
- 🛑 **Поки повний набір стендів іде — файлів НЕ чіпати** (24.08 три прогони з чотирьох
  були недостовірні через це) · ⚠️ **«Unverified» на GitHub — очікувано** · **`git diff`
  «що не в main» — на ДВІ крапки** · **стелю бюджету в `docs-fresh` не піднімати**.

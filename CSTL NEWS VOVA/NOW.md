# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-23 14:33 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-bn0us5` |
| останній коміт людини | `4d890c9f` 23.08 14:32 — Merge remote-tracking branch 'origin/main' into claude/startuem-bn0us5 |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260823-1405` |
| останній PR у `main` | #1001 |
| стендів на диску | 100 |
| незакомічених файлів | 1 ⚠️ |
| журнал за 2026-08-23 | є ✅ |
| потік `/byyou` | idle — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `CSTL NEWS VOVA/CLAUDE.md`
- `CSTL NEWS VOVA/NOW.md`
- `CSTL NEWS VOVA/START_HERE.md`
- `CSTL NEWS VOVA/_ai-tools/SESSION_STATE_VOVA.md`
- `_session-log/vova-2026-08-23b.md`
- `bundle.js`
- `src/core/account-ui.js`
- `src/core/profile-card.js`
- `src/core/supabase.js`
- `src/core/upload.js`
- `src/core/utils.js`
- `sw.js`
- `tests/avatar-quality.mjs`

**Останні кроки (без автокомітів парсерів):**

- `4d890c9f` 23.08 14:32 — Merge remote-tracking branch 'origin/main' into claude/startuem-bn0us5
- `b06ddbc0` 23.08 14:29 — docs: стенд якості фото в карті файлів, пульс, 99 -> 100 стендів
- `4360c07a` 23.08 14:08 — docs(літопис): якість фото жителя — дві версії замість однієї
- `1a83b467` 23.08 14:06 — docs(журнал): якість фото жителя — діагноз, заміри, три брехливі мірки
- `3a013c40` 23.08 13:55 — Merge remote-tracking branch 'origin/main' into claude/startuem-bn0us5

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

📦 **Сесія 23.08 закрита двома роботами, обидві в `main`.** Робочого коду в
гілці не лишилось.

🖼 **ЯКІСТЬ ФОТО ЖИТЕЛЯ (друга робота).** Скарга Вови зі знімком: «відкриваю
карточку, і фотографія дуже неякісна, розмита, піксельна». Причина вимірна:
аватар зберігався ОДНИМ файлом **256×256**, а показувався двома місцями, і обом
його бракувало — кружечок картки 120px × щільність 3 = **360 точок**, фото на
весь екран доходить до **1238×1651** (розтяг у 4.5 раза).
✅ Тепер **дві версії**: `avatar.webp` 256px квадрат (**9 KB**) для кружечків +
`profile.webp` **1700px** у пропорціях оригіналу (**74-87 KB**) для картки й
лайтбокса. У базу йде адреса дрібної — усі списки працюють без правок; велика
лежить поруч під іменем `@lg`, тож **міграція бази не знадобилась**.

🔵 **СИНЯ ГАЛОЧКА (перша робота, PR #1001, деплой v4917).** «Десь знизу,
зверху, і не в розмір імені» — вада була структурна й ОДНА на сім поверхонь:
знак стоїть СУСІДОМ імені, тобто в контейнері БАТЬКА. Лікує **гніздо імені**
(`nameSlot`): розмір іде від кегля імені, вирівнює рівно один механізм.

🔑 **УРОК СЕСІЇ — ЧОТИРИ ВЛАСНІ БРЕХЛИВІ МІРКИ ЗА ОДИН ДЕНЬ.** Контроль сторожа
галочки дав 37/37 зелених над зламаною формою; сторож фото тричі поспіль міряв
не те (`parseFloat('96%')`=96 · картинка менша за стелю не розтягується ·
без `<meta viewport>` Chromium бере 980px). Щоразу перевірка світилась зеленим.
🛑 **Мірку перевіряй так само, як код** — і саме останній вимір завалив мій же
вибір 1600 як «майже вистачає», через що `large` став 1700.

🔴 **ГОЛОВНЕ ПРАВИЛО (22.08, слово Вови):** застосунок ще **не вийшов**.
12 профілів у базі — це Вова, його другий акаунт і кілька знайомих. Збирати
дані — лише коли Вова напише **«ДОДАТОК ЗАПУСТИВСЯ»**. Повністю — №12.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

_(23.08 · журнал `_session-log/vova-2026-08-23b.md`)_

**🔵 Гніздо імені** (PR #1001, деплой **v4917 success**) — 13 місць розмітки на
семи поверхнях. `markOfficial` не змінювався. Сторож `verified-badge.mjs`
(37/37), контроль `LEGACY=1` падає 26/37.

**🖼 Пара фото жителя** — `avatarPairBlobs` (одне читання файлу на дві версії),
`encodeCanvas` (WebP + звірка типу + відкат на JPEG), `largePhotoUrl`,
`uploadAvatarPair`, картка просить велику з відкатом, лайтбокс дістав
`fallbackUrl`. Сторож `avatar-quality.mjs` (14/14).

🛑 **Пастка формату, доведена виміром:** `toBlob` з непідтриманим форматом не
падає і не віддає null — мовчки повертає **PNG**. Запит AVIF на 1024px дав
**1273.9 KB** замість ~48, у 26 разів важче, без жодної помилки.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| Капсула «збережене знято» | **рішення: не робимо** | дві перешкоди по коду (порожній `savedIds` на Громаді, потрібна позначка «вже показав»). Рецепт збережено в правилі №12 |
| Перемикач «Actions може відкривати PR» | не увімкнено | Settings → Actions → General. Не обовʼязково: тепер посилання на PR є в логу |
| ПД в ІСТОРІЇ git | не стерто | перезапис історії ламає посилання на коміти — окреме рішення |
| Опис погоди на 320px | обрізається | або назва села, або опис; обрано назву |
| **Android не тестувався ВЗАГАЛІ** | 0 перевірок | усе живе тестування — iPhone Вови |
| Тестовий збір **на проді** | у базі | адмінка → «Збори» → «Видалити» |
| Класифікатор новин (**B-28**) | відкрито | 94.5% статей → «Суспільство»; блокує фільтр B-14 |
| Локальний `main` у середовищі | **неспоріднена історія** | 🛑 не лікуй скиданням, джерело правди — `origin` |
| Капсула Зборів | не робили | законна там рівно одна — «вашу заявку схвалено» (правило «я створив»). «Новий збір» — не моя дія, не проходить |
| Капсула «вам відповіли» — базовий випадок | ✅ **перевірено Вовою на проді** | працює; далі знайдені й полагоджені три окремі вади (агент · число · живе оновлення) |
| Капсула «число по кількох дописах» на телефоні | **не перевірено пальцем** | стенд 130/130, але Вова ще не тапав. Сценарій — у журналі 23.08 |
| Журнал збоїв — контур «застосунок» | **жодного разу не спрацював наживо** | після деплою нічого не падало. Це добре, але означає, що контур доведений лише стендом |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

1. 🔴 **Живий телефон, два сценарії:** (а) геолокація → у капсулі автобуса має
   стояти **твоє село**, не Олика (`МЕТЕЛЬНЕ`, `ХРОМ'ЯКІВ` у розкладі);
   (б) **два коментарі під різними дописами** → «2 НОВІ КОМЕНТАРІ» + «у 2 ваших
   дописах», тап веде на вкладку Стрічка.
2. 🔴 **Стираємо ПД з історії git?** Відповідь дана: варто, бо репозиторій
   публічний і це ЧУЖІ дані. Чекаю «роби». ⚠️ Після перезапису треба писати в
   GitHub Support.
3. **Голос бренду** — вичитати `editor/voice/olyka_castle.md` (поки чернетка).
4. **Прогін по профілях?** Тепер є доступ до бази — можу показати, скільки людей
   реально бачать хоч одну капсулу. ⚠️ До запуску це скаже про розробників, не
   про людей.
5. **Невикористаний ключ `cstl-olyka-agent`** — відкликати.

## ➡️ ДАЛІ

Капсули закриті на пʼяти темах — за словом Вови 22.08: «давай робимо так, як ти
кажеш, й закриваємо це питання вже». Логічний наступний крок — **не нова логіка, а
підготовка до запуску**: заслінка, вміст Дошки, тестування на Android.

**Черга Вови** (`_ai-tools/BACKLOG_VOVA_2026-08.md`): 💬 чат Дошки — теплота
`#F3EFE3`/`#FCFAF4` · 📐 спадковий відступ контейнера Громади · 🚌 капсули проти
віджета автобусів.

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) — застосунок навмисно закритий.
- 🛑 **Коди заслінки в репозиторій НЕ писати** — ні в коді, ні в журналі, ні в коміті.
- 🛑 **Знімки інструментів (`tests/tools/_out/`) НЕ комітити** — вони фотографують
  живий застосунок разом з іменами й телефонами людей.
- 🛑 **Кирилиця в НАЗВАХ змінних bash** — `bash` читає таке як імʼя команди й падає
  з кодом 127. `bash -n` цього НЕ ловить. У JS і Python — можна, у shell — ні.
- 🛑 **`Одеради АЗ` ≠ `Одеради`** — у розкладі це дві різні зупинки (84 і 70 рейсів).
- 🛑 **Порядок правил у `style/home.css`:** вузькоекранні `@media` для погоди мусять
  стояти в КІНЦІ секції, інакше програють за позицією і тихо не діють.
- ⚠️ **`page_admins` в аудит доступів НЕ рахувати** — команда сторінки не є
  доступом до системи (правило Вови 20.08).
- 🔴 **Клавіатура на iOS** (`style/feed.css:630,971`) — два фікси провалились (#638, #641).
- 🔴 **Три механізми закриття** (`#article-modal` · `core/modal.js` · `core/layers.js`).
- 🔴 **`main` напряму не чіпати** — тільки через PR.
- ⚠️ **Дампи БД у репозиторій не класти** — репозиторій ПУБЛІЧНИЙ.
- ⚠️ **«Unverified» на GitHub — очікувано, ігнорувати.**
- ⚠️ **`git diff` для «що не в main» — на ДВІ крапки.**
- ⚠️ **Не піднімати стелю бюджету читання** в `tests/docs-fresh.mjs`.

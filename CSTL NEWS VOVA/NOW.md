# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-20 05:11 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-v2m8mz` |
| останній коміт людини | `c318b571` 20.08 04:56 — feat(агент): спільнота OLYKA CASTLE — чернетки постів у стрічку, Opus 5 |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260820-0511` |
| останній PR у `main` | #961 |
| стендів на диску | 88 |
| незакомічених файлів | 6 ⚠️ |
| журнал за 2026-08-20 | **НЕМАЄ** ⚠️ |
| потік `/byyou` | done — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `.github/workflows/olyka-agent.yml`
- `CSTL NEWS VOVA/CLAUDE.md`
- `CSTL NEWS VOVA/NOW.md`
- `_session-log/vova-2026-08-18b.md`
- `bundle.js`
- `editor/core/registry.py`
- `editor/core/spend.py`
- `editor/images/none.py`
- `editor/missions/olyka_castle.json`
- `editor/plans/olyka_castle.json`
- `editor/sinks/page_draft.py`
- `editor/sources/plan.py`
- `editor/voice/olyka_castle.md`
- `editor/writers/brand_writer.py`
- `scripts/supabase_page_post_drafts.sql`
- `src/core/supabase.js`
- `src/tabs/feed.js`
- `style/feed.css`
- `sw.js`
- …ще 1

**Останні кроки (без автокомітів парсерів):**

- `c318b571` 20.08 04:56 — feat(агент): спільнота OLYKA CASTLE — чернетки постів у стрічку, Opus 5
- `b94af125` 20.08 04:22 — docs: пульс
- `044c75cd` 20.08 04:21 — fix(жести): зона свайпу назад — ліва половина екрана
- `f494ee0f` 20.08 04:19 — Merge remote-tracking branch 'origin/main' into claude/startuem-v2m8mz
- `2abad662` 19.08 20:46 — feat: свайп назад «звідки завгодно» + підготовка до Google Play (#960)

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

**Три речі цієї сесії, усі в одній гілці.**

1. 🤖 **Агент спільноти OLYKA CASTLE** — готовий і має ключ. Розділ нижче.
2. ✋ **Свайп назад «звідки завгодно»** (19-20.08) — сторінка закривається жестом з
   **лівої половини** екрана, а не з краю 28px. Скрол не зламано: рішення приймає
   домінанта напрямку (горизонталь > вертикаль × 1.5) після 10px, а всередині
   горизонтальних каруселей жест не починається взагалі. Плавність стара — той
   самий `createDragTracker`/`finishSwipe`. Один вхід `attachSwipeBack()` у
   `core/layers.js`, тож правило діє на ВСІХ шарах одразу. Стенд 12/12.
3. 📱 **Підготовка до Google Play** (19.08) — обгортка TWA: у `manifest.json`
   стабільний `id`, `privacy.html` генерується з `src/core/legal.js` при збірці,
   інструкція `docs/PLAY_TWA.md`. iOS свідомо лишається PWA (guideline 4.2).
   Стенд 17/17. 🔴 Далі — акаунт розробника $25 і закрите тестування
   (≈12 тестерів × 14 днів): це купує Вова, кодом не робиться.

✅ **Віджет погоди (19.08, чотири ітерації за знімками Вови).** Компактний рядок
51-53px замість 121px завжди; розгортається у список 7 днів. 🔑 Дані не чіпані:
той самий запит, той самий кеш, розгортання нового запиту НЕ робить.
Чотири правила віджета — в `HOT_RULES.md`; найважливіші три: **жест живе лише на
компактному рядку**, **стрілка — в самому лівому краю перед іконкою**,
**вузькоекранні `@media` для погоди — в КІНЦІ секції `home.css`**.
🔴 На 320px найдовший опис обрізається: там або назва села, або опис — обрано назву.

✅ **Персональні дані з дерева прибрані (18.08).** Аудит по ВСІЙ історії (40 105
обʼєктів): ключів немає ніде і ніколи. Знайдене видалено, `tests/tools/_out/` у
`.gitignore`. 🔴 Файли лишаються в ІСТОРІЇ git — стерти = `git filter-repo` + force
push, це переписує всі коміти. **Рішення Вови, окремою операцією.**

✅ **Заслінка має ДВА коди** (18.08) — обидва дійсні. `DEV_LOCK = true`.

## 🤖 ШІ-АГЕНТ СПІЛЬНОТИ OLYKA CASTLE (20.08) — ГОТОВИЙ ДО ПРОГОНУ

Пише пости у Стрічку від імені сторінки **`pages.id = 3` OLYKA CASTLE** як
**ЧЕРНЕТКИ** (рішення Вови). Модель **Opus 5**, 3-4 пости на тиждень
(пн/ср/пт/нд 10:10), по одному за прогін.

```
план тем (Вова + Claude)  →  Opus 5 голосом бренду  →  ЧЕРНЕТКА у Стрічці
                                                            ↓
                            команда сторінки читає → «Опублікувати» → усі бачать
```

🛑 **ЧЕРНЕТКУ ХОВАЄ БАЗА, А НЕ КЛІЄНТ.** Політика читання:
`(deleted_at is null AND status='published') OR can_edit_page(page_id)`. Фільтр у
клієнті — друга лінія. Текст, якого Вова не вичитав, не існує для читача навіть
при помилці в застосунку.
✅ Міграцію накатано, наслідок звірено: видимих постів було 10 → лишилось 10.

🔑 **Агент не вигадує фактів:** бачить лише `facts` із `editor/plans/olyka_castle.json`.
Заборона стоїть ДВІЧІ — у голосі (`editor/voice/olyka_castle.md`) і в промпті.
👥 **Публікують усі з «Команди сторінки»** (власник + модератори, `page_admins`) —
окремого «тегу редактора» не заводили: і база, і клієнт питають те саме
`can_edit_page`. ⚠️ Слово «редактор» у проєкті зайняте кабінетом редактора новин.

🔑 **Ключ `ANTHROPIC_API_KEY` у репозиторії ВЖЕ Є** — той самий секрет живить
новинного агента і свята (перевірено: прогін новин 20.08 05:46 UTC успішний).
🛑 **Не перезаписувати його новим ключем:** секрет один на ТРИ процеси, і підміна
на ключ без балансу тихо зупинила б усі три. Мій ранній запис «чекає ключа» був
про МОЄ середовище, не про репозиторій.
💸 Лічильник витрат навчили розрізняти моделі — доти він рахував усе за Sonnet і
занизив би Opus **удвічі**. Пост ≈ $0.03.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

**Погода** — `renderWeatherBlock()` + `attachWeatherFold()` (висота, поворот
стрілки, свайп). 🔑 **Дні стали СПИСКОМ, а не сімома колонками** — причина
арифметична: у рядку дня шість значень, а в колонці 320/7 = 45px вони не
вміщаються ніяк. Заразом зникло питання горизонтального скролу: його немає ніде.

**Жести** — `attachSwipeBack()` + `opts.el` в `openLayer()` (`core/layers.js`).
**Play** — `scripts/gen_privacy.mjs` у збірці, `.nojekyll`, `docs/PLAY_TWA.md`.
**Агент** — `editor/{sources,writers,sinks,images,plans,voice}`, міграція чернеток,
позначка «ЧЕРНЕТКА» і кнопка «Опублікувати» у Стрічці.

**Аудит на ключі й ПД** (18.08) — ключів немає; ПД прибрано; `.gitignore` закриває
причину. Ціна приватного репозиторію порахована з рахунку: ≈$39/міс, або $4 після
переїзду парсерів у Supabase, або $0 і лишаємо публічним.

🛑 **УРОК СЕСІЇ — ДВІЧІ ПРО ПОРЯДОК CSS, і обидва рази наслідок вилітав не там, де
причина.** (1) При переносі медіа-блоку зрізав закривну дужку — браузер відкинув
увесь подальший CSS, і впали перевірки КАРТКИ ЗБОРІВ; спершу подумав на чуже
падіння, перевірив прогоном на чистому дереві — зламав я. (2) Медіа-блок двічі
програвав за позицією у файлі і працював НАПОЛОВИНУ.
➡️ Після ручного переносу CSS — рахувати баланс дужок; вузькоекранні
перевизначення тримати в КІНЦІ секції. ⚠️ **Фальшивий зелений:** проміжний прогін
показав «375px чисто» — на зламаному CSS. Зелений на зламаному стилі не значить нічого.

**Стенди:** нові `weather-fold` 51/51 · `swipe-back` 12/12 · `pwa-store` 17/17 ·
`olyka-agent` 32/32 · старі `weather` 26/26 · `weather-day` 46/46 · `home` 79/80.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| 🔴 **Сторож капсули відстав від рішення 17.08** | знайдено, **не чіпав** | `home.mjs:250` чекає рівно `МОЄ/ЗАРАЗ/НОВЕ`, а мітка навмисно двоскладова (`ЗАРАЗ · ЧЕРЕЗ 40 ХВ`). ⚠️ Червоніє **не завжди, а за годинником**: капсула автобуса є лише коли рейс ближчий за 120 хв, поза вікном перевірка проходить вхолосту. Лагодиться одним рядком — про це не просили |
| ПД в ІСТОРІЇ git | не стерто | перезапис історії ламає посилання на коміти — окреме рішення |
| Опис погоди на 320px | обрізається | або назва села, або опис; обрано назву. Слово «Сьогодні» там теж сховане — інакше різало числа |
| `sheet-close` під навантаженням | блимає | таймінговий замір; окремо 3/3 зелений |
| **Android не тестувався ВЗАГАЛІ** | 0 перевірок | усе живе тестування — iPhone Вови |
| Тестовий збір **на проді** | у базі | адмінка → «Збори» → «Видалити» |
| Класифікатор новин (**B-28**) | відкрито | 94.5% статей → «Суспільство»; блокує фільтр B-14 |
| Локальний `main` у середовищі | **неспоріднена історія** | 🛑 не лікуй скиданням, джерело правди — `origin` |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

1. 🔴 **Погода на живому телефоні** — перевірити пальцем: свайп по рядку вгору/вниз,
   тап по стрілці, чи не заважає жест гортанню сторінки.
2. **Лагодимо сторож капсули?** Один рядок, але поза замовленням.
3. **320px: назва села чи повний опис?** Зараз назва.
4. 🔴 **Стираємо ПД з історії git?** Окрема обережна операція.
5. 🔴 **Другий код заслінки лишаємо?** Він слабкий.
6. 🔴 **Закриваємо репозиторій?** ≈$39/міс · $4 після переїзду парсерів · $0 як є.
7. 🔴 **Знімаємо заслінку?** `DEV_LOCK = true` — єдине, що відділяє від Етапу 3.

## ➡️ ДАЛІ

**Черга Вови** (`_ai-tools/BACKLOG_VOVA_2026-08.md`):

1. ✅ Віджет і розділ зборів — ЗРОБЛЕНО (16-17.08)
2. 💬 Чат Дошки — теплота `#F3EFE3` / `#FCFAF4`
3. 📐 Спадковий відступ контейнера Громади
4. 🚌 Капсули проти віджета автобусів

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) — застосунок навмисно закритий.
- 🛑 **Коди заслінки в репозиторій НЕ писати** — ні в коді, ні в журналі, ні в коміті.
- 🛑 **Знімки інструментів (`tests/tools/_out/`) НЕ комітити** — вони фотографують
  живий застосунок разом з іменами й телефонами людей. Правило вже в `.gitignore`.
- 🛑 **Порядок правил у `style/home.css`:** вузькоекранні `@media` для погоди мусять
  стояти в КІНЦІ секції, інакше програють за позицією і тихо не діють.
- 🔴 **Клавіатура на iOS** (`style/feed.css:630,971`) — два фікси провалились (PR #638, #641).
- 🔴 **Три механізми закриття** (`#article-modal` · `core/modal.js` · `core/layers.js`).
- 🔴 **`main` напряму не чіпати** — тільки через PR.
- ⚠️ **Дампи БД у репозиторій не класти** — репозиторій ПУБЛІЧНИЙ.
- ⚠️ **«Unverified» на GitHub — очікувано, ігнорувати.**
- ⚠️ **`git diff` для «що не в main» — на ДВІ крапки.**
- ⚠️ **Не піднімати стелю бюджету читання** в `tests/docs-fresh.mjs`.

# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-24 16:03 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-bn0us5` |
| останній коміт людини | `183849d5` 24.08 16:03 — Merge remote-tracking branch 'origin/main' into claude/startuem-bn0us5 |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260824-1553` |
| останній PR у `main` | #1005 |
| стендів на диску | 101 |
| незакомічених файлів | 0 ✅ |
| журнал за 2026-08-24 | **НЕМАЄ** ⚠️ |
| потік `/byyou` | idle — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `CSTL NEWS VOVA/CLAUDE.md`
- `CSTL NEWS VOVA/NOW.md`
- `CSTL NEWS VOVA/START_HERE.md`
- `CSTL NEWS VOVA/_ai-tools/SESSION_STATE_VOVA.md`
- `_session-log/vova-2026-08-23b.md`
- `bundle.js`
- `scripts/check-imports.js`
- `scripts/supabase_account_scoped_state.sql`
- `src/core/auth.js`
- `src/core/board-shared.js`
- `src/core/saved-hub.js`
- `src/core/sidebar.js`
- `src/core/supabase.js`
- `src/tabs/board-discussions.js`
- `src/tabs/news.js`
- `sw.js`
- `tests/account-scope.mjs`
- `tests/home-caps.mjs`
- `tests/home.mjs`

**Останні кроки (без автокомітів парсерів):**

- `183849d5` 24.08 16:03 — Merge remote-tracking branch 'origin/main' into claude/startuem-bn0us5
- `22a51756` 24.08 16:02 — docs: пульс, літопис і кількість стендів після аудиту акаунтів
- `800178c4` 24.08 15:05 — test(головна): «СЬОГОДНІ» — дозволена роль підпису капсули
- `d46abe3a` 24.08 15:00 — test(капсули): позначка «бачив» міряється за наслідком, а не за іменем ключа
- `547301eb` 24.08 14:49 — fix: «Вийти» зависало назавжди + оновлення скидало б усі «N нових»

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

✅ **АУДИТ ACCOUNT-SCOPED STATE ЗРОБЛЕНО, ВСІ ЧОТИРИ ЗНАХІДКИ ЗАКРИТІ.**
Слово Вови: «**все персональне… має зберігатись в акаунті, а не на пристрої;
якщо користувач виходить, він може тільки переглядати публічну інформацію, а не
взаємодіяти в рамках додатку**». Привід — реальний випадок: Вова вийшов з
«Олександра», зайшов «Володимиром», а push прилетів на попередній акаунт.

🔴 **Найдорожче — push-пристрій не відвʼязувався при виході.** Доведено на живій
базі: один endpoint під ДВОМА акаунтами (Володимир 26.07, Олександр 24.08 08:09
UTC — рівно коли Вова перемикався). **Це витік — у тілі push лежить текст
повідомлення.** Два рубежі: вихід віддає пристрій, `claim_push_device()` забирає
при наступному вході (бо виходу може не бути ЗОВСІМ — застосунок убили).

📦 **Сесія 23-24.08: вкладка «Питання» перебудована + закрито B-33.** Два потоки,
дев'ять кроків, усе задеплоєно.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

_(23-24.08 · журнал `_session-log/vova-2026-08-23c.md` · PR #1004)_

> 🔀 **ЦЬОГО ДНЯ ПРАЦЮВАЛИ ДВІ ПАРАЛЕЛЬНІ СЕСІЇ**, на різних акаунтах. Нижче —
> робота обох. Друга (гілка `claude/startuem-bn0us5`, журнал
> `_session-log/vova-2026-08-23b.md`) змерджилась першою, її зміни вже в `main`.

### PR #1001-#1003 (усе в `main`)

**🔵 Гніздо імені** (v4917) · **🖼 Пара фото жителя** (v4924) · **🚌 Капсула
автобуса** казала «ЗАВТРА» при рейсі СЬОГОДНІ. Подробиці — журнал 23b.
🛑 **Дві пастки звідти, чинні й далі:** `toBlob` з непідтриманим форматом мовчки
повертає **PNG**. І сторож автобусів не пропускав ваду, а **ВИМАГАВ** її: сцена
звалась «НІЧ — рейсів немає», а грала на рейсі, який сьогодні ще попереду.

### 24.08, третя частина: персональне переїхало в акаунт (PR #1006)

**Чотири знахідки, кожна доведена виміром.** Стенд `account-scope.mjs` (новий,
10/10): акаунт А зберігає статтю І оголошення → на тому самому пристрої заходить
Б. **Контроль усередині сцени** — оголошення в базі з `uid` протікати не можуть,
тож витік в ОБОХ означав би, що стенд не вміє перемикати акаунт.

Закрито: push-пристрій (`signOut()` віддає + `claim_push_device()` забирає) ·
збережені статті (`saved_articles` з `uid`, старі перенесено) · гейт на аркуші
«Збережені» · мітки «бачив» стали іменними.

🔴 **`sidebar.js` ДВІЧІ стверджував, що «openSavedHub має власну перевірку» — а
її не існувало жодної.** Коментар про неіснуючий запобіжник знімає обережність
там, де вона потрібна (як сторожі `kb-guard.test.js`, яких не було в git).

**🛑 Дві власні вади, спіймані стендами, обидві були б справжніми:** `signOut()`
зависав **назавжди** на `serviceWorker.ready` (без SW ця обіцянка не виконується
НІКОЛИ — не падає, а мовчки висить). І оновлення тихо скинуло б усі «N нових».
Тепер перенос одноразовий: гість бере мітку копією, акаунт — із видаленням.

### 24.08, друга частина тієї ж гілки (PR #1005)

**🗑 Видалення коментаря не працювало ВЗАГАЛІ** — ні в Питаннях, ні в Стрічці.
Тост казав «Текст порожній», але падав **сам `deleted_at`** (42501): політика
ЧИТАННЯ вимагала `deleted_at is null`, і автор робив рядок невидимим ДЛЯ СЕБЕ.
У Стрічці те саме падало **мовчки**. 🔴 Тригера антиспаму **не було в жодному
SQL-файлі** — жив у базі з 25.07. **Правило №11 стосується схеми теж.**

**🧪 `news-widget` міряв клас `.nc-card`, якого в застосунку немає** — тож
правило «герой є тоді і лише тоді, коли в першої новини є фото» вимагало
**відсутності героя** і почервоніло рівно тоді, коли код спрацював правильно.

### Сесія на `claude/startuem-gqj5ae` (ця, PR #1004)

**📄 `docs/QA_CONCEPT.md`** (770 рядків), усі шість спірних рішень закриті словом
Вови. **Головне: «Цікавить» це ПУБЛІЧНА дія, не підписка** — якщо різниця лише
в тому, чи прийде push, людина її в момент вибору не бачить. Вова: «Це сильніше
за мою попередню модель із підпискою на push».

**А1-А4** — кнопка каже словами, що сталось · debounce **обʼєднує**, а не губить
(«5 нових відповідей» замість тиші) · посилання веде **до самої відповіді**.

**🔴 B-33 — усі чотири вимикачі сповіщень були декоративні:** `notif_prefs`
писався в `localStorage` і **не читався ніким**. Тепер у базі. «Світло» і
«Новини» прибрані — таких push не існує, і тумблер давав фальшиве підтвердження.

**🔧 Вхід через Google** завжди питає, яким акаунтом зайти.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| Мітки «бачив» між пристроями | свідомий борг | іменні, але на пристрої: прочитав на телефоні — на компʼютері бейдж висить. Ціна розсинхрону — зайвий бейдж, не чужі дані |
| Тип 4 «питання без відповіді» | **можна вмикати** | умови — §14/§15 `QA_CONCEPT.md` |
| Гілка префів в автобусах | **не виконувалась наживо** | `checked:0`, підписок нема; виконається при першому відстеженні |
| Живий прохід push у Питаннях | **не перевірено пальцем** | потрібні два акаунти й телефон Вови |
| Відкладений push (batching) | не робили | потрібен планувальник; А2 закриває 80% болю за 5% роботи |
| Категорії питань | не робили | без них персоналізація типу 4 не має на чому стояти |
| **Android не тестувався ВЗАГАЛІ** | 0 перевірок | усе живе тестування — iPhone Вови |
| Класифікатор новин (**B-28**) | відкрито | 94.5% статей → «Суспільство»; блокує B-14 |
| ПД в ІСТОРІЇ git | не стерто | перезапис історії ламає посилання на коміти |
| Тестовий збір **на проді** | у базі | адмінка → «Збори» → «Видалити» |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

0. 🔴 **Перемкни акаунт на телефоні** — push має перестати приходити на
   попередній. База каже, що дублікатів `endpoint` немає, але телефон — остання
   інстанція.
0б. 🔴 **Видалення коментаря** (Питання І Стрічка): свій коментар має зникати
   без тоста про помилку.
1. 🔴 **Живий прохід у Питаннях, два акаунти:** постав «Цікавить» з А → відповідь
   з Б → має прийти push → тап веде до **тієї самої** відповіді, підсвіченої.
2. 🔴 **Вимикачі:** вимкни «Питання» в кабінеті → push не приходить; вимкни на
   телефоні → на компʼютері теж вимкнено.
3. **Вхід через Google:** тепер +1 екран вибору акаунта. Заважає — відкотимо одним рядком.
4. **Тип 4** («у громаді питання без відповіді») — вмикаємо?
5. **ПД в історії git** — стираємо? Чекаю «роби». · 6. Ключ `cstl-olyka-agent` — відкликати.

## ➡️ ДАЛІ

**Аудит account-scoped state закрито.** Лишився один свідомий борг: мітки
«бачив» тепер іменні, але живуть на пристрої — прочитав новини на телефоні, на
компʼютері бейдж висить. Повна синхронізація потребує таблиці й походу в мережу
на кожне перемикання вкладки; ціна розсинхрону тут — зайвий бейдж, а не чужі
дані на екрані.

Далі — **не нова логіка, а підготовка до запуску**: заслінка, вміст Дошки,
тестування на Android.

**Черга Вови** (`_ai-tools/BACKLOG_VOVA_2026-08.md`): 💬 чат Дошки — теплота
`#F3EFE3`/`#FCFAF4` · 📐 спадковий відступ контейнера Громади · 🚌 капсули проти
віджета автобусів.

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) · 🛑 **коди заслінки в репозиторій НЕ писати**.
- 🛑 **Знімки `tests/tools/_out/` НЕ комітити** — на них імена й телефони людей.
- 🔴 **`verify_jwt` у `send-bus-push` — УВІМКНЕНИЙ**; деплой із `false` тихо зняв би перший рубіж.
- 🔴 **TEXT проти `uuid`:** `reactions.user_id` і `push_subscriptions.user_uuid` — TEXT.
  Перед `.in('uid', …)` **обовʼязковий фільтр форми UUID**, інакше один сторонній
  рядок глушить УСІ сповіщення теми.
- 🛑 **Персональні сповіщення вимикача НЕ мають** · **`upsert` без `.select()`**, де результат не потрібен (№11-БІС).
- 🛑 **Кирилиця в назвах змінних bash** — падає з кодом 127 · **`Одеради АЗ` ≠ `Одеради`** · **порядок `@media` погоди в `style/home.css`** — у КІНЦІ секції.
- 🔴 **Клавіатура на iOS** (`style/feed.css:630,971`) — два фікси провалились (#638, #641).
- 🔴 **Три механізми закриття** · **`main` напряму не чіпати** · **дампи БД у репозиторій не класти** (він ПУБЛІЧНИЙ).
- 🔴 **`serviceWorker.ready` без SW не виконується НІКОЛИ** — не падає, а висить; бери `getRegistration()`.
- ⚠️ **«Unverified» на GitHub — очікувано** · **`git diff` для «що не в main» — на ДВІ крапки** · **не піднімати стелю бюджету читання** в `tests/docs-fresh.mjs`.

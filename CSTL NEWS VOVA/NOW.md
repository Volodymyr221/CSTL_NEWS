# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-15 14:50 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-6v6car` |
| останній коміт людини | `b403f6df` 15.08 14:45 — Сповіщення про чат більше не веде на головну |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260815-1433` |
| останній PR у `main` | #926 |
| стендів на диску | 79 |
| незакомічених файлів | 0 ✅ |
| журнал за 2026-08-15 | є ✅ |
| потік `/byyou` | done — 15.08 — «Свіжа документація на переході: новий чат / другий акаунт» |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `CSTL NEWS VOVA/CLAUDE.md`
- `CSTL NEWS VOVA/START_HERE.md`
- `_session-log/vova-2026-08-15.md`
- `bundle.js`
- `src/tabs/board-chat.js`
- `sw.js`
- `tests/notif-thread-coldstart.mjs`
- `tests/tools/notif-thread-probe.mjs`

**Останні кроки (без автокомітів парсерів):**

- `b403f6df` 15.08 14:45 — Сповіщення про чат більше не веде на головну
- `f400ad18` 15.08 14:09 — docs(now): пульс під деплой #4309 (#926)
- `6b941fce` 15.08 14:07 — Edge Functions накатано на Supabase + пульс і журнал (#925)
- `48879ad5` 15.08 12:15 — Порожній FAB у «Питаннях» + сповіщення ведуть до самої дії (#924)
- `b8e3c73e` 15.08 11:47 — FAB: підйом над «Громадою» + іконка більше не перевернута (#923)

<!-- AUTO:END -->---

## 🎯 ЗАРАЗ

**Нічого в роботі.** 15.08 було ДВІ паралельні сесії — Вова працював на двох
акаунтах одночасно і попередив прямо. Обидві закриті й задеплоєні.

⚠️ Обидві сесії завели журнал з тим самим імʼям; другий перейменовано на
`_session-log/vova-2026-08-15b.md`. **Перед роботою звіряйся з `main`, а не з
памʼяттю** — цього дня одну й ту саму ваду вже лагодили двічі.

🔑 **Головне спостереження дня: обидва баги виявились НЕДОЛІКОВАНИМИ СТАРИМИ.**
І лист коментарів, і блимання Вова описував ще 26-27.07 — тоді полагодили по
одному місцю, а решту лишили. Це спосіб, у який вади тут виживають.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

**Три плани руху** `plans/002 → 003 → 001` — усі закриті (PR #918, деплой #4285).
🔴 Урок: **прилад мусить показувати вимір, а не вирок** — двічі завалював справний
код, врятувало те, що друкує число. Деталі — `SESSION_STATE_VOVA.md`.

**5. 🔴 ЛИСТ КОМЕНТАРІВ САМОВІЛЬНО ЗГОРТАВСЯ** (PR #920, деплой #4290).
`blur` поля стискав лист зі 100% у 82% (**≈150px**), кнопки списку самі забирали
фокус → під пальцем опинявся задній шар. Два рубежі: `attachBackdropClose()` (усі
4 листи) + `keepFocusOnButtons()`. Сторож **7/7**, контроль 5/7.

**4. 🎨 ІКОНКА «СТРІЧКА» — СІТКА 2×2** (PR #917, «4 варіант реалізуй»). Сторона
7.125 · радіус 2.05 · просвіт 1.75 = товщина лінії · площа 315. 🔴 **Урок:
`getBBox()` НЕ рахує товщину лінії** — перша спроба вийшла на 20% дрібнішою, і
сторож при цьому був би зелений. ⚠️ Моє заперечення (2×2 читається як «усі
застосунки») відхилене рішенням власника — щоб не «лікували» його вибір.

**6. 🔴 ПОВЕРНЕННЯ НА ВКЛАДКУ БІЛЬШЕ НЕ БЛИМАЄ** (PR #921 + #922, деплой #4299).
Заміряно: повна заміна `innerHTML` = **5 кадрів без жодної фотографії**, стало **0**.
🔑 Справжня причина — **відносний час** у картці («5 хв» → «6 хв») міняється сам раз
на хвилину, тож порівняння «список цілком» спрацьовувало щоразу. Лікує `patchList()`
+ `adoptLoadedImages()`. 🛑 Перенос `<img>` у картку поза документом **скидає
`complete`** — порядок критичний. Сторож `tab-return-repaint` **12/12**.

**7. 🔴 FAB: три скарги Вови закрито** (PR #923 + #924).
**(а)** Накладалась на «Громаду» — заміряно **52×10px**; `--fab-lift: 16px` →
зазор **6px**. **(б)** Іконка повідомлення була **догори ногами** — оберт був на
ПІВ круга, став повний (360°); заразом плавність стояла лише на плюсі.
**(в)** 🔴 **Порожній FAB у «Питаннях»** — `#board-trigger` спільний на дві зони,
але в Дошки плюс І конверт, у «Питань» лише плюс; `.has-unread` від ГЛОБАЛЬНОГО
числа ховав плюс там, де замінити нема чим. Два рубежі: клас не ставиться кнопці
без конверта + `:has()` у CSS. Сторожі: `fab-hint` **19/19**, `fab-visible` **5/5**
(контроль 4/5 — «видно 0 іконок», рівно знімок Вови).

**8. 🔴 СПОВІЩЕННЯ ВЕДУТЬ ДО САМОЇ ДІЇ** (PR #924 код, #925 журнал, деплой #4309).
Тап по сповіщенню про коментар відкриває **сам лист коментарів**, про приватне
повідомлення — **саму розмову**. Рвалось у двох ланках: зведене «Ще N коментарів»
вело лише до поста, а чат при ВІДКРИТОМУ застосунку не працював зовсім.
✅ **EDGE FUNCTIONS НАКАТАНО 15.08** (Вова: «Супабейс підключений, роби все сам»):
`send-chat-push` **v4 → v5**, `send-comment-push` **v2 → v3**, обидві ACTIVE.
🛑 `verify_jwt` зчитано з задеплоєних версій ДО накату і повторено один в один
(`comment-push` — **false**, бо його кличе БАЗА без токена людини; `chat-push` —
**true**). Переплутати = мовчки вбити всі сповіщення.
⚠️ Сторож накату НЕ бачить — він читає репозиторій; доказ дає `list_edge_functions`.

**9. 🔴 …І ВСЕ ОДНО ВЕЛО НА ГОЛОВНУ — ДРУГИЙ ЗАХІД.** Вова після деплою: «на
коментар перекидає, а тап по сповіщенню про приватне повідомлення закидає на
головну». Причина — **перегони з відновленням входу**, не маршрутизація:
`handleThreadHash()` зʼїдає хеш (`replaceState`) на **~465мс**, а вхід приїжджає
на **~500мс**, тож `openThreadById()` виходив на `if (!isLoggedIn()) return`.
Порядок структурний — `init()` не чекає на `initAuth()`. 🔑 Коментарі працювали,
бо `focusFeedPost()` входу не потребує. Лікування: намір **відкладається** і
доводиться до кінця в `onAuthChange` (вікно 15 с).
🔴 **Побічно стенд упіймав: чат відкривався ДВІЧІ** — обробників `notif-click`
було два (`app.js` + `board-chat.js`), дубль завівся того ж ранку.
🛑 **УРОК ДНЯ №2: зелений ТЕКСТОВИЙ сторож стояв над зламаною поведінкою.**
`notif-deeplink` 12/12 був правий — усі ланки цілі; ламався ПОРЯДОК У ЧАСІ,
якого в тексті не видно. ➡️ Замовлення «має відкритись» перевіряй ВІДКРИТТЯМ.
Новий `notif-thread-coldstart` — **браузерний**, 9/9, контроль **3/9**.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| ~~PR #916~~ | ✅ **ЗЛИТО в `main` 15.08** (коміт `2c98ab7d`) | нічого не лишилось |
| ~~`plans/001-003`~~ | ✅ **усі три виконані й злиті 15.08** | лишається жива перевірка на iPhone |
| Сторож на свайп модалки статті | **немає** | доводив разовим приладом (CDP-дотики). Постійного стенда на `#article-modal` у проєкті так і нема — зміна тримається на збірці й `motion` |
| `BYYOU_PLAN.md` | статус ✅ виправлено (`done`), **архівування лишилось** | переносити файл у `_archive/` з-під можливо ще відкритої сесії — колізія з HOT_RULES №11. Чекає підтвердження, що другий акаунт закрито |
| **Потік Б** (окрема сесія) | не почато | розрізати `SESSION_STATE_VOVA.md` (807 KB), звірити `RODMAP.md` (04.07), прибрати зі `START_HERE.md` стос блоків «попередній стан». Чіпає історію — окремим дозволом |
| Перетікання тла вкладки | знайдено, **не лікували** | `.app-main` має `transition: background-color 0.3s`, фони вкладок різні (Громада `#1A0A0E` ↔ Стрічка `#ECEEF1`) — заміряно 18 кадрів ≈300мс, коли під готовим вмістом повзе чуже тло. Ефект реальний, але це НЕ те, на що скаржився Вова |
| Юрист на правові тексти | відкрито | єдине, чого я закрити не можу |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

1. **Потік Б — коли?** Без нього `SESSION_STATE_VOVA.md` лишається 807 KB і новий
   чат далі не зможе його дочитати. Це найбільший лишок.
2. **Стенд `ad-report`** упав на CI 15.08, локально дає 16/16, моїми змінами не
   чіпався (востаннє правився в PR #902/#903). Схоже на нестабільність
   на повільнішому раннері — але варто глянути окремо, якщо повториться.
3. ~~Іконку «Стрічка» ти доробляєш на іншому акаунті~~ → ✅ **ЗАКРИТО 15.08:**
   зроблено шосту редакцію (сітка 2×2, варіант 4 з макета). Питання до Вови інше —
   **чи форма остаточна**, бо замовлення звучало «змінити і **зафіксувати нарешті**».
4. ✅ **`BYYOU_PLAN.md` — статус виправлено 15.08** (`paused` → `done`): потік
   справді в `main` (PR #916). 🛑 **Лишилось архівування** в `_ai-tools/_archive/` —
   переносити файл з-під можливо ще відкритої сесії це колізія з HOT_RULES №11.
   Зробити, **щойно Вова підтвердить, що другий акаунт закрито**.
5. 🔴 **Три рухи чекають перевірки пальцем на iPhone** (деплой #4285 вже на сайті):
   банер автобусів має рушати з-під пальця одразу · картка Дошки на Громаді не
   лишатись збільшеною після тапу · модалка статті має закриватись швидким
   коротким кидком. ⚠️ Постійного сторожа на свайп статті **немає** — доводилось
   разовим приладом, тож ця перевірка тримається на Вові.

## ➡️ ДАЛІ

**Черга Вови** (`_ai-tools/BACKLOG_VOVA_2026-08.md`, порядок задав 05.08):

1. 🎯 **Віджет зборів** (військові й загальні) на «Громаді» — ПЕРШИЙ У ЧЕРЗІ.
   ⚠️ `src/tabs/home-fund.js` уже вміє більшість — **не будувати з нуля**.
   ⚠️ У `data/fundraisers.json` лежить **ТЕСТОВИЙ запис і він на проді**.
2. 💬 Чат Дошки — теплота `#F3EFE3` / `#FCFAF4`
3. 📐 Спадковий відступ контейнера Громади
4. 🚌 Капсули проти віджета автобусів

**Плани руху** (`plans/`) — ✅ **усі три закриті 15.08**, лишилась жива перевірка
на iPhone: банер автобусів має рушати з-під пальця одразу, а картка Дошки на
Громаді не лишатись збільшеною після тапу. Модалку статті тепер можна закрити
швидким коротким кидком і тягнути за всю верхню смугу, а не лише за рисочку.

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** (`src/core/dev-lock.js`) — застосунок навмисно закритий.
  Знімати не можна: у деяких людей є доступ.
- 🔴 **Клавіатура на iOS** (`style/feed.css:630,971`, `transition: height`) — два
  фікси там уже провалились (PR #638, #641).
- 🔴 **Три різні механізми закриття** (`#article-modal` · `core/modal.js` ·
  `core/layers.js`) — плутати не можна.
- 🔴 **`main` напряму не чіпати** — тільки через PR.
- ⚠️ **Дампи БД у репозиторій не класти** — репозиторій ПУБЛІЧНИЙ.
- ⚠️ **«Unverified» на GitHub — очікувано, ігнорувати.** Коміти підписуються «Вова».
- ⚠️ **`git diff` для «що не в main» — на ДВІ крапки.** На три після squash-мерджа
  показує роботу, яка вже в `main`.
- ⚠️ **Не піднімати стелю бюджету читання** в `tests/docs-fresh.mjs` — вікно
  контексту від цього не росте. Прибирати файли в ситуативне читання.

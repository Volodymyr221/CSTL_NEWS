# NOW — пульс проєкту CSTL

> 🔴 **ЦЕЙ ФАЙЛ ЧИТАЄТЬСЯ ПЕРШИМ У КОЖНОМУ НОВОМУ ЧАТІ.**
> Він навмисно малий: його самого має бути ДОСИТЬ, щоб зрозуміти, де ми стоїмо.
> Історія живе окремо (`_ai-tools/SESSION_STATE_VOVA.md`, `_session-log/`) і
> читається **вибірково за темою**, ніколи цілком.

<!-- AUTO:START -->

> 🤖 **Блок нижче генерує `scripts/now_update.mjs`. Руками не правити** —
> при наступному прогоні правку зітре. Проза під блоком не чіпається ніколи.

**Зріз на:** 2026-08-27 14:41 UTC

| поле | значення |
|---|---|
| гілка | `claude/startuem-5nmz43` |
| останній коміт людини | `5e4506be` 27.08 14:41 — Merge remote-tracking branch 'origin/main' into claude/startuem-5nmz43 |
| `CACHE_NAME` (з `sw.js`) | `cstl-20260827-1306` |
| останній PR у `main` | #1051 |
| стендів на диску | 113 |
| незакомічених файлів | 0 ✅ |
| журнал за 2026-08-27 | є ✅ |
| потік `/byyou` | idle — потік не названо |

**Не доїхало в `main`** (чесна мірка — `diff` на дві крапки):

- `CSTL NEWS VOVA/NOW.md`

**Останні кроки (без автокомітів парсерів):**

- `5e4506be` 27.08 14:41 — Merge remote-tracking branch 'origin/main' into claude/startuem-5nmz43
- `413dcab3` 27.08 14:40 — docs: пульс — авто-блок після коміту передачі
- `ac3fef99` 27.08 14:40 — docs: передача в новий чат — 27.08.2026
- `7308a317` 27.08 14:32 — Волинь Post: стара верстка більше не з'їдає фото й посилання (#1051)
- `d1d4265e` 27.08 14:31 — Пульс: авто-блок після журналу

<!-- AUTO:END -->

---

## 🎯 ЗАРАЗ

📦 **27.08. Уся робота в `main` і на сайті** — останній PR **#1051**, деплой **#5159**.
`CACHE_NAME` НЕ піднімався: зміна суто серверна (парсер), клієнтських файлів у діффі немає.

🔴 **ДЕНЬ ПРО ОДНЕ — СТАТТЯ МАЄ ВИГЛЯДАТИ ЯК СТАТТЯ.** Шість деплоїв поспіль: живі
посилання · цілі цитати · заглушки-замки геть · фото в тілі · мініатюри 4:3 · і вечірній
розбір, де питання про ОДНУ статтю виявилось вадою цілого джерела (Волинь Post, 62 статті).

🟢 **Відкритих критичних немає.** ✅ «Блим» зник — запис «НЕ вилікувано» був хибний.
🛑 **ФІКС ЩЕ НЕ ПРАЦЮВАВ НІ РАЗУ.** Парсер за розкладом не запускався з **07:27**, тобто
жодного разу після деплою о 14:33. Дані в `main` — дофіксові. Дивитись у застосунку **поки
нема чого**.

🔀 **№13:** злиття ДО роботи · ніколи `-X theirs` · маркери конфлікту ПЕРЕД `git add` ·
🛑 `node --check` на файлах `src/` не перевіряє **нічого** — лише `scripts/check-syntax.mjs`.

## ✅ ЗРОБЛЕНО В ЦІЙ СЕСІЇ

### 27.08 ВЕЧІР: ВОЛИНЬ POST — НІ ФОТО, НІ ПОСИЛАНЬ (питання Вови по 13114)

📐 **Питали про одну статтю — виявилось джерело цілком:** Волинь Post **62 статті — 0 з фото,
0 з посиланням** (Конкурент 143/143 і 18, УП 121/157 і 26), і при цьому 62 з 62 `page`.
🔎 **Причина 1 — стара верстка:** тіло голим текстом у `<div>` з `<br>`, жодного `<p>`.
Запасний шлях був **плоским** (`get_text()` стирає `<a>`, `html.escape` вбиває голу адресу).
🛑 **І гірше:** один `<img>` робив список блоків непорожнім → поверталось **саме фото без
тексту**, поріг «>300» не проходив, стаття втрачала повний текст **ЦІЛКОМ**.
🔎 **Причина 2 — обкладинка:** брали лише з RSS; `fetch_og_image` **не звалась жодного разу**.
Тепер `fetch_article_page` віддає пару (тіло, обкладинка) і читає `og:image` з **того самого
супу** — другий запит зʼїв би пів бюджету (40 сторінок/прогін).
🔑 **Суддя міряє текст БЕЗ пробілів:** `strip_html` замінює кожен тег пробілом, тож багата
розмітка сама себе «доважувала».
⚠️ **Живих сторінок із пісочниці не дістати** (403) — усе на коді, даних і макеті верстки.
🧪 `test_rich_html` **42→57**, `test_fulltext` **21/21**.

### 27.08 ДЕНЬ: СТАТТЯ ЯК НА ДЖЕРЕЛІ — ЖИВІ ПОСИЛАННЯ, ЦІЛІ ЦИТАТИ (потік 2А)

🗣️ Скріни статті **12248** («Культурний код»): «там є клікабельне посилання… можемо так само?»
📐 **Втрачено ЧОТИРИ речі:** посилання (**0 із 400** статей мали `<a>`) · цитата розірвана
посеред фрази · рамка цитати · фото в тілі.
🔑 **Адресу пише ПАРСЕР, а не джерело.** `href` — перший атрибут, який ми впускаємо в тіло:
`_safe_href` — лише `http/https`, без керівних символів, відносні → абсолютні, екранування.
🔴 **Розділювач абзаців став небезпечним:** заборонені зони — нутрощі тега, `<a>…</a>` і
**пряма мова** («…»). 🔑 **`RICH_ALGO_VERSION`** — 400 статей переберуться самі.
🛑 **Стенд знайшов ТРИ пастки задуму і ЧОТИРИ мої хибні мірки** (розбір у літописі).
🛑 **`node --check` НЕ ловить синтаксису у `src/`.** 🧪 `article-links` **11/11**, контроль 9/11.

### 27.08: РЕДИЗАЙН БЛОКУ НОВИН (потік /byyou, 7 кроків, PR #1045)

📐 **Рішення з заміру** (`news-redesign-measure.mjs` міряє ВСІ віджети поруч): картка
**161→242px** (2:1→4:3), заголовок 2→3 рядки, опис поверх фото, віджет **357→477px**.
🔴 **Картка без фото — окремий стан** (**114 з 400 = 29%**): велика — без слоту взагалі;
одна розмітка на обидва стани (`handleImgError` вішає `.nc--noimg`).
🛑 **Сторожі переписані з форми на шкоду** · контроль одного правила стояв порожнім.
📖 `_ai-tools/BYYOU_ARCHIVE_2026-08-27_novyny-redesign.md`

### 26.08 НІЧ: SDK ЗІ СВОГО ДОМЕНУ · ЗБОЇ — ЖИВІ Й ЗАКРИТІ

🔴 **Safari: «Немає звʼязку» при живому інтернеті.** Клієнт бази створювався ОДИН РАЗ і лише
за наявності `window.supabase` з чужого CDN → **застосунок без бази НАЗАВЖДИ**, і дізнатись
неможливо (журнал збоїв пише через ТОЙ САМИЙ клієнт). ✅ SDK у `vendor/` · вічний кеш ·
**лінивий** клієнт. 🧪 `sdk-local` **17/17**, контроль **1/12**.

### 26.08 ВЕЧІР: АНАЛІТИКА — ЕКРАН, МОДАЛКА, AI, ПОРІВНЯННЯ

🔴 **ТРИ ТИХІ ВАДИ:** кожен захід писався «гостем» (`initAuth()` без `await`) · AI коментував
неправдиві числа · висновок зникав при поверненні, а виклик коштує грошей.
🔑 **Розділили «зміну» і «сигнал»:** «5 людей стало 8» = +60% — бездоганно математично й
**порожньо** на десятку акаунтів. 🛑 **N/A ≠ 0**. 🧪 **106/106**. 📖 `AUDIT_ANALYTICS_2026-08.md` ч.2.

### 26.08 РАНОК: ЦИФРИ СТАЛИ ПРАВДИВИМИ · ВІДЖЕТ СТРІЧКИ (#1032-#1036)

🔴 Числа обрізала стеля 1000 (`db-max-rows`); доказ без бази: 544+164+134+82+76 = **рівно
1000**. Правда: 7 днів **6 людей** (казав 203). 🛑 **СІМ ПОМИЛОК ВИМІРУ**.

### 26.08 і раніше (#1001-#1044)

✅ Карусель новин → спільний `core/auto-carousel.js` (дві копії розходились **двічі**).
🔴 **Заслінка:** код був ПРАВИЛЬНИЙ — сторінка по `http://`, а `crypto.subtle` є лише в
захищеному контексті; вада не в замку, а в тому, що він КАЗАВ. 🔴 **Пункт 0:** `initAuth()`
без `await` → вкладки малювали вигляд ГОСТЯ. 📖 `SESSION_STATE_VOVA.md`, `-23b`…`-25e`.

## 🚧 НЕ ДОРОБЛЕНО

| що | стан | чому |
|---|---|---|
| Волинь Post: фото й посилання | ✅ код у `main`, **жодного разу не виконувався** | парсер за розкладом мовчить з 07:27; ручний запуск мені закритий (403 `actions: write`) — тисни **Run workflow** на `rss-parser.yml` сам |
| Потік 2Б-2: галерея і відео в тілі | не почато | прикладу галереї так і не трапилось — робити на здогад = гадати |
| Фото в тілі статті (#1050) | ✅ у `main` | iPhone-смоук Вова ще не робив |
| Логотип може просочитись у тіло | фільтр лише за розміром | розмір знаємо ТІЛЬКИ якщо джерело написав його в розмітці; побачиш — додамо відсів за назвою |
| `analytics_daily` | борг **з умовою** | робити при >1 млн подій **або** якщо чистимо історію; 11 тис. рахуються на льоту |
| Retention D1/D7/D30 · 4-тижневий baseline | після запуску | рахуються з сирих подій заднім числом |
| Віджет Стрічки не бачений пальцем | ✅ борг каруселі закрито | скрін не долетів |
| Тип 4: перше спрацювання | **27.08** | питанню буде 21 год при потрібних 24 |
| `CSTL_BUGS.md` = 40.5 KB при стелі 40 | запасу 0.5 KB | наступний баг — стисло |
| Стеля «5 push із Питань на добу» (§12) | **в коді немає** | заміряно по 6 |
| **Android не тестувався ВЗАГАЛІ** | 📌 відкладено Вовою | «протестуємо коли доробимо все» |
| Класифікатор новин (**B-28**) | відкрито | ~94% статей → «Суспільство» |
| ПД в ІСТОРІЇ git · тестовий збір на проді | не стерто / у базі | перезапис історії ламає посилання |

## ❓ ВІДКРИТІ ПИТАННЯ ДО ВОВИ

✅ **Перевірено пальцем 24-26.08:** новини · перемикання акаунта · видалення коментаря ·
«Вийти» · push · сортування · **вимикачі (B-33)** · віджет Стрічки.

0. 🔴 **НЕ ПЕРЕВІРЕНЕ — гарантія «хто я» (холодний старт, PWA):** крапка непрочитаного
   ОДРАЗУ · push веде в чат · збережені позначені · ❤️ зафарбоване · привітання з ІМЕНЕМ ·
   ⚠️ **Автобуси малюють розклад МИТТЄВО**. 🔴 Чи відкривається `castlelife.org` — з
   контейнера не перевірити.
0б. 🔴 **Запусти `rss-parser.yml` руками** (Actions → Run workflow): мені 403. Тільки після
   прогону дивись **13114** — фото і натискне посилання. Не спрацює — `[FETCH]` у логах
   (живі сторінки джерел із контейнера НЕ дістати). ⚠️ Черга — усі 400 статей по 40 за
   прогін, тож Волинь Post доїде не з першого разу.
1. 🔴 **Тип 4 — 27.08.** ⚠️ У базі ОДНА push-підписка на всіх, автор питання випадає.
2-6. **Нічна тиша · антиспам · непрочитане · редагування питання · голос спільноти** —
   сценарії в `-25b/-25c` і `SESSION_STATE_VOVA.md`.
7-9. Google-вхід · ПД в історії git · ключ `cstl-olyka-agent` відкликати.
📌 **Android** — «протестуємо коли доробимо все».

## ➡️ ДАЛІ

🔴 **ПЕРШЕ — аналітику пальцем, залогіненим.** До 26.08 жоден захід не мав акаунта, тож
«Людей» покаже правду лише з нових заходів. ✅ **Пункт 0 закрито 25.08 (#1029).**
**Далі:** 📰 потік 2Б-2 (галерея + відео в тілі, коли трапиться приклад) · 💬 чат Дошки ·
📐 відступ Громади · 🚌 капсули проти автобусів. _(Android — у кінці.)_
❓ **Не дописане Вовою:** «щотижневий AI-аналіз динаміки» — усе для нього вже є.

## 🛑 НЕ ЧІПАТИ

- 🔴 **`DEV_LOCK = true`** · коди заслінки в репозиторій НЕ писати · знімки
  `tests/tools/_out/` НЕ комітити · 🔴 `verify_jwt` у `send-bus-push` — УВІМКНЕНИЙ.
- 🔴 **TEXT проти `uuid`:** `reactions.user_id`, `push_subscriptions.user_uuid` — TEXT;
  перед `.in('uid', …)` **обовʼязковий фільтр форми UUID** — один сторонній рядок глушить
  УСІ сповіщення теми. · `upsert` без `.select()` · SELECT-політика на свої рядки ·
  **кирилиця в іменах змінних bash** = код 127 · `@media` погоди — у КІНЦІ `home.css`.
- 🔴 **Клавіатура на iOS** (`feed.css:630,971`) · **`main` напряму не чіпати** · дампи БД
  не класти (репо ПУБЛІЧНИЙ) · `serviceWorker.ready` без SW не виконується НІКОЛИ ·
  `pg_cron` живе в UTC · **у транзакції `now()` однаковий для всіх операторів**.
- 🛑 **НЕ МІРЯЙ ПОСЕРЕД РОБОТИ** · «Unverified» — очікувано · `git diff` «що не в main» —
  на ДВІ крапки · **стелю бюджету в `docs-fresh` НЕ піднімати**.
- 🔑 Решта — `HOT_RULES.md`, `CSTL_BUGS.md`.

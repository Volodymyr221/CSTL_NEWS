node_modules/
bundle.js.map
__pycache__/
*.pyc

# macOS / Windows / IDE сміття
.DS_Store
**/.DS_Store
Thumbs.db
.idea/
.vscode/

# Архіви і логи не комітимо
*.zip
*.log
*.tmp
.env
.env.local
graphify-out/

# /byyou — рантайм-маркери (не комітимо)
.claude/.byyou-release
.claude/hooks/.byyou-handoff-warned
.claude/hooks/.byyou-warn-35
.claude/hooks/.byyou-warn-55

# токен-запобіжник — прапорець паузи сесії (не комітити)
.claude/.token-halt

# Живі дані Дошки для прототипу — оголошення реальних людей, репо публічне
tests/tools/_board-live.json
tests/tools/badge-shot.png
tests/tools/badge-compare.png

# 🔴 Знімки інструментів перегляду (`tests/tools/*-shot.mjs` пишуть сюди).
# Вони фотографують ЖИВИЙ застосунок, тож у кадр потрапляють справжні імена,
# телефони й тексти приватних повідомлень. Саме так 18.08.2026 у публічному репо
# опинились чотири знімки чату з номером телефону жителя — знайдено аудитом на ПД.
# Знімки регенеруються інструментом за потреби, тримати їх у git незащо.
tests/tools/_out/

# Наміри синку кабінету між тактами «записав файл» і «підтвердив пуш» —
# транзитний файл раннера, у репозиторії йому робити нічого.
data/.cms_pending.json

# 🔴 Звіт vibe-audit — НІКОЛИ в репозиторій. Репо публічне, а звіт цитує рядки
# коду і шляхи файлів. Скрипт маскує секрети, але правило простіше за виняток:
# за замовчуванням він пише `vibe-audit-report.md` у поточну теку.
vibe-audit-report.md

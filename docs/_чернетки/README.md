# Чернетки, прибрані з кореня (13.08.2026)

Перенесено сюди при підготовці репозиторію до зовнішнього перегляду: у корені
вони виглядали як покинуті файли і викликали питання «це живе?».

🛑 **Нічого не видалено** — лише переміщено. Правило проєкту: файл прибирають з
очей, а не з історії, поки власник не сказав видалити.

| файл | що це |
|---|---|
| `_new-hub.png`, `_new-widget.png` | знімки макетів хаба новин і віджета (липень 2026) |
| `ARCHITECT-PLAN-with-Gemini.md` | ранній план архітектури, складений з Gemini |

⚠️ Перевірено перед переносом: **жодного посилання** на ці файли в коді,
розмітці, конфігах чи workflow немає.

**Що НЕ переносили, хоч воно теж лежить у корені:**
- `courier.html`, `market.html` — **живі**, на них посилається `src/market/*`;
- `install.html` — посилань у коді немає, але це сторінка, доступна за прямою
  адресою; чіпати без окремого рішення власника не можна.

# /startuem — Швидкий старт нової сесії CSTL NEWS

**Коли використовувати:** перша команда у кожному новому чаті проекту CSTL NEWS. Один виклик — і Claude готовий до роботи з повним контекстом проекту.

**Без аргументів.** Просто `/startuem`.

**Відрізняється від `/start`:** глобальний `/start` (для інших проектів Вови) НЕ чіпаємо. Але **в репозиторії CSTL є локальний `.claude/commands/start.md`** — він виконує ТУ САМУ логіку що `/startuem` (реюз `NEW_SESSION_PROMPT.md`). Тобто у CSTL і `/start`, і `/startuem` стартують однаково (власник визначається за поштою). Поза CSTL глобальний `/start` працює як раніше.

---

## Крок 0 — Хто на зв'язку (Рома чи Вова) + гілка + конфлікти

> Над проектом працюють ДВОЄ власників — **Вова і Рома**, кожен зі свого Claude. Зроби це ПЕРШИМ, автоматично, нікого не питай. Повна процедура — у `NEW_SESSION_PROMPT.md` (Крок 0). Коротко:

1. **Визнач людину** за поштою сесії (контекст, блок `userEmail`): `roma.haranin.ru1@gmail.com` → **Рома**; `vitocorleone191@outlook.com` → **Вова** (другий акаунт Вови — рішення 20.07); інша → **Вова**.
2. **Підпиши коміти:** `git config user.name "<Рома/Вова>"` + `git config user.email "<пошта>"`.
3. **Гілка під людину — ОДРАЗУ безпечна:** пересядь на сесійну гілку `roma/session-<дата>` або `vova/session-<дата>` (напр. `git checkout -B vova/session-2026-07-05 origin/main`). Далі під конкретну задачу можна зробити `roma/<тема>`/`vova/<тема>`. Потік: іменована гілка → `/finish` → `main`. Авто-гілку `claude/startup-...` НЕ лишай робочою — «поекспериментував» більше не має бути на авто-прод-гілці.
   - **🔄 Спершу — paused /byyou.** Глянь `_ai-tools/BYYOU_PLAN.md`: якщо статус `paused` і `Гілка:` — твого власника, **НЕ сідай на нову session-гілку і НЕ питай абстрактно про гілку** — пересядь на ТУ гілку (`git checkout <гілка> && git pull origin main`) і в першій відповіді запропонуй продовжити (/byyou). Деталі — `NEW_SESSION_PROMPT.md` Крок 0 п.3.
4. **Перевір конфлікти:** `git fetch origin` + `git pull origin main` (свіжий стан) + `git log --oneline --all -15` + `BOARD.md` (хто над чим зараз) + свій `SESSION_STATE_<ІМʼЯ>.md`. Якщо рішення іншої людини суперечать — скажи про це у першій відповіді.
5. **Взаємне автостеження (для ОБОХ власників):** запусти Monitor `SELF='<твій regex>' bash .claude/owner-watch.sh` (persistent) — повідомляє про активність ІНШОГО власника. SELF: Рома=`Рома|roma\.haranin\.ru1@gmail\.com`, Вова=`Вова|Vova|VShevchukkk|Volodymyr221`. Деталі — `NEW_SESSION_PROMPT.md` Крок 0 п.5.
6. **Заяви активність у `BOARD.md`:** після читання файлів онови `🟢 АКТИВНІСТЬ` своїм рядком (`**Рома/Вова:** <дата> — <що робиш>`) + клейм у `🔨 В РОБОТІ` за потреби. Лише своя підсекція, дрібний коміт прямо в `main`. Деталі — `NEW_SESSION_PROMPT.md` Крок 0 п.6.
7. **🔴 Заведи живий журнал сесії:** створи `_session-log/<роль>-<РРРР-ММ-ДД>.md` і **дописуй по ходу після кожної значущої дії** (файл/функція · навіщо · що не спрацювало · слова власника дослівно · PR#/деплой#/CACHE). Комітируй кожні кілька кроків. При `/finish` звіт для brain іде з цього журналу, не з пам'яті. Деталі — `NEW_SESSION_PROMPT.md` Крок 0 п.7.

> ✅ **Auto-merge ВИМКНЕНО (05.07.2026).** Раніше пуш у `claude/**` тихо мерджився в `main` через `-X theirs` (затирав чужу роботу). Тепер шлях у `main` — **тільки через PR + `/finish`**. Пуш у `claude/*` більше нічого не деплоїть. Архів: `.github/_archive/auto-merge.yml.disabled`. Працюй на `roma/...`/`vova/...`.

---

## Крок 1 — Прочитай стартовий промпт

Відкрий через Read tool файл:
```
CSTL NEWS VOVA/_ai-tools/NEW_SESSION_PROMPT.md
```

Усередині цього файлу — повний стартовий промпт (сам текст між маркерами ⬇️/⬆️). Він містить:
- Список файлів які ти маєш прочитати
- Ключові правила роботи з Вовою
- Стиль комунікації (пояснення термінів у дужках, короткі повідомлення, сперечатись чесно)
- Формат очікуваної першої відповіді

**Виконуй інструкції з цього файлу як свої.** Він для тебе — офіційний контракт на всю сесію.

---

## Крок 2 — Виконай обов'язкове читання проекту

Згідно з інструкцією у `NEW_SESSION_PROMPT.md`, прочитай у такому порядку:

1. `CSTL NEWS VOVA/HOT_RULES.md` — 8 болючих правил що порушуються найчастіше (читай ПЕРШИМ)
2. `CSTL NEWS VOVA/START_HERE.md`
3. `CSTL NEWS VOVA/_ai-tools/BOARD.md` — дошка координації: хто над чим ЗАРАЗ + питання одне одному (читай ОБИДВІ секції)
4. `CSTL NEWS VOVA/_ai-tools/SESSION_STATE_<ІМʼЯ>.md` — свій стан (Роми або Вови). Стан іншого — за потреби глянь його файл.
5. `CSTL NEWS VOVA/_ai-tools/AUDIT_2026-07.md` — майстер-список задач по зонах
6. `CSTL NEWS VOVA/CSTL_BUGS.md`
7. `CSTL NEWS VOVA/CLAUDE.md`
8. `CSTL NEWS VOVA/ВОВА_ПРОФІЛЬ.md`

Додатково — подивись git-історію:
```
git log --oneline -10
git branch --show-current
```

Щоб зрозуміти що відбувалось нещодавно і на якій гілці зараз працюємо.

---

## Крок 3 — Дай відповідь у правильному форматі (звертайся на ім'я — Рома або Вова)

Формат описано у `NEW_SESSION_PROMPT.md`. Коротко:

> **Прочитав.** Привіт, [Рома/Вова]. CSTL NEWS, гілка [назва], [твій стан з SESSION_STATE_<ІМʼЯ>].
>
> **Топ-3 пріоритетні задачі твоєї зони (AUDIT_2026-07.md):**
> 1. [задача]
> 2. [задача]
> 3. [задача]
>
> **Критичні баги** (🔴 з CSTL_BUGS.md): [перелік або "нема"]
>
> **Що робить інший власник зараз** (BOARD.md → 🟢 Активність + 🔨 В роботі): [коротко]
>
> **Питання/конфлікти** (BOARD.md → ❓ + Крок 0): [що чекає відповіді або суперечить, або "нема"]
>
> **Що робимо сьогодні?**

---

## Крок 4 — Чекай "Роби" від людини (Рома або Вова)

**НЕ** починай жодних змін у коді поки людина не скаже явне **"Роби"**. Це правило №1 проекту (див. `CSTL NEWS VOVA/CLAUDE.md` секція "Процес роботи").

Якщо не впевнений чи фраза — це "Роби" чи просто обговорення, перепитай: **"Це Роби на [назва задачі]?"**. Краще уточнити ніж зробити зайве.

---

## Нагадування перед стартом

- **Пояснення англомовних термінів у дужках** — у кожному повідомленні, без винятків. Вова вчиться програмуванню через цей проект.
- **Короткі повідомлення** — одна думка за раз.
- **Сперечайся чесно** — не погоджуйся заради ввічливості.
- **Автокоміт через stop-hook — потрібна фіча**, не переписуй.
- **Двогілковий потік:** робота на іменованій гілці (`roma/...` або `vova/...`) → `/finish` (PR → squash) → `main`. У `main` напряму не пушити.

Повний список правил — у `CSTL NEWS VOVA/CLAUDE.md` секція "ВСІ ПРАВИЛА".

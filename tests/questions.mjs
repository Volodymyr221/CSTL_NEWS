// tests/questions.mjs — ВКЛАДКА «ПИТАННЯ» (Q&A замість чату), 11.08.2026.
//
// 🔑 ЩО САМЕ МІРЯЄМО І ЧОМУ САМЕ ТАК.
//
// Замовлення Вови було НЕ про кольори, а про семантику: «щоб користувач за 1-2
// секунди зрозумів — тут можна поставити питання жителям громади». Тому стенд
// НЕ перевіряє наявність класів і не звіряє тексти з файлом. Він міряє ЧОТИРИ
// наслідки, які побачить людина:
//
//   1) на екрані є ВИДИМА головна дія зі словом «Запитати» — не схована у FAB;
//   2) картка списку каже «ЩО запитали · ХТО · КОЛИ · скільки відповідей», і
//      НЕ каже мовою чату (немає прев'ю останніх повідомлень і «N учасників»);
//   3) відкрите питання — це ЕКРАН із відповідями, а не месенджер: жодної
//      бульбашки `.pm-bubble`, жодного часу `HH:MM`, жодного «Написати
//      повідомлення…», і низ підписаний словом, а не стрілкою «↑»;
//   4) питання без відповіді видно як ЗАКЛИК («Ще ніхто не відповів»), а не як
//      мовчазний нуль — саме це має оживляти Q&A у малій громаді.
//
// 🔴 ЧОМУ ПУНКТ 3 СФОРМУЛЬОВАНО ЧЕРЕЗ ВІДСУТНІСТЬ. Бо в цьому й був корінь: до
// 11.08 екран мав правильні НАЗВИ («Обговорення») і при цьому чотири незалежні
// маркери месенджера. Перевірка «є заголовок Питання» пройшла б і на старому
// коді — тобто нічого не доводила б. Ловити треба саме те, чого не має бути.
//
// 🔴 КОНТРОЛЬ (обовʼязковий): на старому коді стенд МУСИТЬ упасти.
// Трьох змінних, а не однієї, бо зміна розкидана трьома шарами: логіка в
// `bundle.js`, вигляд у `style/board.css`, підпис вкладки та іконка — в
// `index.html`. Стенд, який підмінив би лише бандл, показав би зелень на
// старому таб-барі — і збрехав би рівно так само, як `sidebar-menu.mjs` до 10.08.
//
// ⚠️ КОНТРОЛІВ ДВА, і плутати їх не можна — вони доводять РІЗНЕ.
//   1) ВЕСЬ ПЕРЕХІД «чат → Q&A» (ревізія ДО 11.08, коли вкладка ще була чатом):
//        BUNDLE_REV=19f88243 CSS_REV=19f88243 HTML_REV=19f88243 node tests/questions.mjs
//      → **9/53**. Саме це число доводить, що стенд ловить модель, а не назви.
//   2) ОСТАННЯ ЗМІНА (підказка FAB), проти чинного `origin/main`:
//        BUNDLE_REV=origin/main CSS_REV=origin/main HTML_REV=origin/main …
//      → **51/53**: падають рівно `2в` (кнопка не згортається) і `2д` (підказка
//      грає щоразу). Дві перевірки — і саме ті дві, що описують нову поведінку.
// 🛑 `origin/main` уже містить редакцію 5, тож як контроль ПЕРЕХОДУ він більше
// не годиться і показував би оманливу зелень. Ревізію в пункті 1 не «освіжати».

import { chromium } from 'playwright';
import { launch, serve, reporter, projectFile } from './_lib.mjs';
import { mockSupabase } from './_board-fixture.mjs';

const { ok, done } = reporter();

const BUNDLE_REV = process.env.BUNDLE_REV || '';
const CSS_REV    = process.env.CSS_REV    || '';
// 🔴 24.08 — ЧЕТВЕРТИЙ ШАР КОНТРОЛЮ. Колір звʼязувача задає ТОКЕН `--qa-line`, а
// він живе в `style/base.css`, який `CSS_REV` не підміняв. Через це перевірка
// контрасту (7.7в) у контрольному прогоні світилась зеленою над старим блідим
// кольором — тобто не могла впасти. Правило проєкту: контроль вимагає стільки
// змінних, скільки шарів зачепила зміна (у `sidebar-menu.mjs` їх теж три).
const BASE_REV   = process.env.BASE_REV   || '';
const HTML_REV   = process.env.HTML_REV   || '';

// ── Дані: два питання. Одне з відповідями (зокрема вкладеною), одне без жодної.
// Саме пара, а не одне: інакше стан «без відповіді» нічим було б відрізнити від
// «список ще не намалювався».
const t0 = Date.now() - 5 * 864e5;
const POSTS = [
  { id: 701, type: 'chat', text: 'Коли буде концерт на День міста?', title: null,
    author: 'Олена', owner_uid: 'u-olena', status: 'published', location: null, tags: [],
    ts: t0, created_at: new Date(t0).toISOString(), published_at: new Date(t0).toISOString() },
  { id: 702, type: 'chat', text: 'Хтось знає, коли ремонтуватимуть дорогу в Митильному?', title: null,
    author: 'Петро', owner_uid: 'u-petro', status: 'published', location: null, tags: [],
    ts: t0 + 6e4, created_at: new Date(t0 + 6e4).toISOString(), published_at: new Date(t0 + 6e4).toISOString() },
];
const COMMENTS = [
  // ⚠️ Імʼя З ПРІЗВИЩЕМ навмисно: 📐 на живій базі 7 профілів із 12 записані
  // саме так, тож коротке «Віктор» перевіряло б найлегший випадок і пропускало
  // головний. Саме на короткому імені перша редакція цієї перевірки й показала
  // зелень над карткою, де на знімку стояло «— Дмитро Сел…».
  { id: 5001, post_id: 701, author: 'Віктор Пилипчук', text: 'Начебто 24 серпня.', sender_uid: 'u-viktor',
    reply_to_id: null, created_at: new Date(t0 + 36e5).toISOString(), edited_at: null, deleted_at: null, client_tag: null },
  // 🔴 24.08 — ДРУГА КОРЕНЕВА, НОВІША ЗА ПЕРШУ. Без неї сцена не вміла
  // відрізнити «першу» від «останньої»: коренева була рівно одна, тож обидві
  // редакції коду давали ОДИН І ТОЙ САМИЙ рядок і сторож світився зеленим над
  // будь-якою з них. Перевірка, яка не може впасти, не перевірка.
  { id: 5003, post_id: 701, author: 'Оксана Ковальчук', text: 'Афішу вже повісили біля клубу.', sender_uid: 'u-oksana',
    reply_to_id: null, created_at: new Date(t0 + 40e5).toISOString(), edited_at: null, deleted_at: null, client_tag: null },
  // 🛑 ВКЛАДЕНА — І НАВМИСНО НАЙНОВІША З УСІХ ТРЬОХ. Це пастка на наївне
  // «взяти останній коментар»: за часом вона перша в черзі, але вона —
  // репліка під чужою відповіддю, тобто в картку не сміє потрапити НІКОЛИ.
  { id: 5002, post_id: 701, author: 'Марія', text: 'Так, підтверджую — бачила афішу.', sender_uid: 'u-maria',
    reply_to_id: 5001, created_at: new Date(t0 + 44e5).toISOString(), edited_at: null, deleted_at: null, client_tag: null },
  // 🔴 24.08 — ВІДПОВІДЬ НА ВКЛАДЕНУ ВІДПОВІДЬ (у сирих даних це ТРЕТІЙ рівень:
  // 5004 → 5002 → 5001). Такого рядка тут не було, бо його НЕ БУЛО ЯК СТВОРИТИ:
  // «Відповісти» стояло лише на кореневій. Тепер кнопка є на кожній, і цей шлях
  // відкрився — а з ним відкрилась і вада підняття до кореня «на один щабель»,
  // від якої така відповідь **зникала з екрана мовчки**.
  { id: 5004, post_id: 701, author: 'Галина', text: 'А я чула, що перенесли на 25-те.', sender_uid: 'u-halyna',
    reply_to_id: 5002, created_at: new Date(t0 + 48e5).toISOString(), edited_at: null, deleted_at: null, client_tag: null },
  // 🛑 І ЩЕ ОДИН РІВЕНЬ — 5005 → 5004 → 5002 → 5001. Саме ТУТ вилазила вада, а
  // не на третьому: старе підняття робило РІВНО ОДИН стрибок, тобто від 5005
  // приходило до 5002 — а 5002 сам не корінь, і до малювання рядок не доходив.
  // 📐 Перевірено обома прогонами: на трьох рівнях перевірка 7.6б була зеленою
  // і на старому коді теж, тобто не могла впасти. Четвертий рівень її оживив.
  { id: 5005, post_id: 701, author: 'Петро', text: 'Ні, 24-те, я дзвонив у сільраду.', sender_uid: 'u-petro',
    reply_to_id: 5004, created_at: new Date(t0 + 52e5).toISOString(), edited_at: null, deleted_at: null, client_tag: null },
];

// 🆕 23.08 — РЕАКЦІЇ «МЕНЕ ТЕЖ ЦІКАВИТЬ» у фікстурі.
// 🔑 Числа підібрані так, щоб довести ПОРІГ, а не просто наявність рядка:
//   • 702 — ТРИ голоси (≥2) → попит у картці мусить бути видно;
//   • 701 — ОДИН голос      → видно НЕ мусить, бо «1 чекає» це майже завжди
//     сам автор, і рядок не додає нічого до вже показаного.
// 🛑 Жоден голос не належить `u-me`: інакше кнопка на старті була б уже
// увімкнена, і перевірка «тап вмикає» міряла б вимикання.
const REACTIONS = [
  { post_id: 702, user_id: 'u-olena',  emoji: '❤️' },
  { post_id: 702, user_id: 'u-viktor', emoji: '❤️' },
  { post_id: 702, user_id: 'u-maria',  emoji: '❤️' },
  { post_id: 701, user_id: 'u-petro',  emoji: '❤️' },
];

const { url, stop } = await serve();
const b = await launch(chromium);
const ctx = await b.newContext({ viewport: { width: 390, height: 844 }, isMobile: true,
                                 hasTouch: true, serviceWorkers: 'block' });
const p = await ctx.newPage();

// Підміна шарів для КОНТРОЛЬНОГО прогону (див. шапку). Без змінних — нічого не робимо.
if (BUNDLE_REV) {
  const old = projectFile('bundle.js', BUNDLE_REV);
  await p.route('**/bundle.js', r => r.fulfill({ contentType: 'application/javascript', body: old }));
}
if (CSS_REV) {
  const old = projectFile('style/board.css', CSS_REV);
  await p.route('**/style/board.css', r => r.fulfill({ contentType: 'text/css', body: old }));
}
if (BASE_REV) {
  const old = projectFile('style/base.css', BASE_REV);
  await p.route('**/style/base.css', r => r.fulfill({ contentType: 'text/css', body: old }));
}
if (HTML_REV) {
  const old = projectFile('index.html', HTML_REV);
  await p.route(url, r => r.fulfill({ contentType: 'text/html', body: old }));
  await p.route(url + '/', r => r.fulfill({ contentType: 'text/html', body: old }));
}

await mockSupabase(p, { posts: POSTS, comments: COMMENTS, announcements: [],
                        reactions: REACTIONS, saved_posts: [] },
                  { user: { id: 'u-me', name: 'Я' } });
await p.route('**://api.open-meteo.com/**', r => r.abort());
await p.goto(url, { waitUntil: 'domcontentloaded' });
await p.waitForTimeout(2500);
await p.evaluate(() => document.querySelector('.consent-accept')?.click());
await p.waitForTimeout(300);
await p.evaluate(() => window.switchTab && window.switchTab('discussions'));
await p.waitForTimeout(1500);

// ── 1. ТАБ-БАР КАЖЕ «ПИТАННЯ» ────────────────────────────────────────────────
const tab = await p.evaluate(() => {
  // ⚠️ Селектор ОБОВʼЯЗКОВО `button.tab-item[...]`, а не просто `[data-tab=...]`.
  // Перша редакція цього стенда брала голий атрибут — і показала «підпис=«»,
  // шляхів=0» на цілком правильному коді: той самий `data-tab` носить ПУНКТ
  // БУРГЕР-МЕНЮ, а `<aside class="sidebar">` лежить у розмітці ВИЩЕ за таб-бар,
  // тож `querySelector` віддавав його. Черговий випадок «прилад бреше частіше за
  // код» — правило проєкту: якщо перевірка завалила те, що працює, першою
  // підозрюваною є перевірка.
  const btn = document.querySelector('button.tab-item[data-tab="discussions"]');
  return {
    підпис: btn?.querySelector('.tab-label')?.textContent.trim() || '',
    // 🔑 Міряємо не «скільки шляхів», а те, що іконка БІЛЬШЕ НЕ БУЛЬБАШКА чату.
    // Стара іконка мала два коротких горизонтальних штрихи (рядки тексту) —
    // саме вони й читались як «листування». Ловимо їх за формою шляху.
    // ⚠️ Перша редакція цієї перевірки рахувала шляхи (=3) — і зеленіла б на
    // будь-якій іншій іконці з трьох ліній, тобто нічого не доводила.
    шляхи: [...(btn?.querySelectorAll('.tab-icon path') || [])].map(x => x.getAttribute('d') || ''),
  };
});
ok('1а. таб-бар підписаний «Питання»', tab.підпис === 'Питання', `підпис=«${tab.підпис}»`);
// Бульбашку впізнаємо за «хвостиком» — ламаною, що виходить з кутка коробки
// (`l-5 3v-3` у старому шляху). Знак питання — за дугою гачка над крапкою.
const єБульбашка = tab.шляхи.some(d => /l-5 3v-3|l-5 3 v-3/.test(d));
const єЗнакПитання = tab.шляхи.some(d => /\.01/.test(d)) && tab.шляхи.length >= 2;
ok('1б. 🔴 іконка вкладки — НЕ бульбашка чату', !!tab.шляхи.length && !єБульбашка,
   єБульбашка ? 'знайдено хвостик бульбашки' : `шляхів: ${tab.шляхи.length}`);
ok('1в. в іконці є знак питання (крапка + гачок)', єЗнакПитання);

// ── 2. ГОЛОВНА ДІЯ ВИДИМА І ПІДПИСАНА ───────────────────────────────────────
// 🔑 Міряємо ВИДИМІСТЬ (`offsetParent` + розмір), а не наявність у DOM: саме на
// цьому вже спіткнувся `dev-lock.mjs` — `!!querySelector` казав «ок» там, де
// людина не бачила нічого.
//
// ⚠️ 11.08, РЕДАКЦІЯ 2 — ПЕРЕВІРКУ ПЕРЕНАЦІЛЕНО З МІСЦЯ НА ДІЮ.
// Була прив'язана до `[data-qa-ask]` — кнопки в бордовому банері згори. Банер
// прибрано (Вова: «фейсбук 2006»; прилад `qa-audit` показав 220px шапки і першу
// інформацію на 36% екрана), а дія переїхала у FAB, який ТЕПЕР ПІДПИСАНИЙ.
// Вимога лишилась та сама і не послабилась: головна дія мусить бути ВИДИМОЮ і
// названою ДІЄСЛОВОМ, а не ховатись за голим «+». Змінилось лише, де вона живе.
//
// 🔴 11.08, РЕДАКЦІЯ 7 — І ТУТ ПРИЛАД МАЛО НЕ ЗБРЕХАВ УП'ЯТНАДЦЯТЕ.
// Кнопка стала розгортатись лише на ПЕРШОМУ вході за запуск (замовлення Вови:
// «трошки завелика… анімовано розгортається, тримається декілька секунд і
// згортається»). Стенд після цієї зміни лишився ЗЕЛЕНИЙ 50/50 — але не тому, що
// поведінка правильна, а тому, що він міряв на 1500мс і випадково потрапляв у
// вікно підказки. Тобто перевірка «дія підписана» пройшла б і тоді, коли підпис
// не згортається взагалі, і тоді, коли він не показується вдруге — обидва
// сценарії їй однаково зелені.
// ➡️ Тому перевірка розкладена на ЧОТИРИ моменти часу, кожен зі своїм змістом:
//   2б  ПІД ЧАС підказки  — слово видно (голого «+» без пояснення не буває);
//   2в  ПІСЛЯ підказки    — кнопка знову КРУГ (це і є замовлення Вови);
//   2г  у згорнутому стані — дія все одно НАЗВАНА (aria-label), інакше вона
//                            зникає для читача екрана і для голосового керування;
//   2д  ДРУГИЙ вхід       — підказки більше немає («коли він вже в додатку і
//                            знов заходить туди, то це вже не потрібно»).
const ask = await p.evaluate(() => {
  const el = document.querySelector('#board-trigger');
  if (!el) return null;
  const r = el.getBoundingClientRect();
  const підпис = el.querySelector('.qa-fab-label');
  // 🔴 НЕ `offsetParent`. У елемента з `position: fixed` він ЗАВЖДИ `null` — тобто
  // перевірка «видно» завалила б будь-який FAB, хоч би як добре той виглядав.
  // Саме це й сталось із першою редакцією цієї перевірки: «висота=56px», а
  // висновок «кнопки немає». Міряємо те, що справді робить елемент невидимим.
  const реальноВидно = (n) => {
    while (n && n.nodeType === 1) {
      const s = getComputedStyle(n);
      if (s.display === 'none' || s.visibility === 'hidden' || parseFloat(s.opacity) === 0) return false;
      n = n.parentElement;
    }
    return true;
  };
  return {
    текст: (підпис?.textContent || el.getAttribute('aria-label') || '').trim(),
    ярлик: (el.getAttribute('aria-label') || '').trim(),
    підписВидно: !!підпис && реальноВидно(підпис) && підпис.getBoundingClientRect().width > 0,
    видно: реальноВидно(el) && r.height > 0 && r.top < window.innerHeight,
    висота: Math.round(r.height),
    ширина: Math.round(r.width),
    уНижнійПоловині: r.top > window.innerHeight / 2,   // зона великого пальця
  };
});
ok('2а. головна дія видима на екрані', !!ask && ask.видно,
   ask ? `висота=${ask.висота}px` : 'кнопки немає');
ok('2б. на ПЕРШОМУ вході вона підписана ДІЄСЛОВОМ, а не голим «+»',
   !!ask && ask.підписВидно && /Запитати/.test(ask.текст),
   ask ? `«${ask.текст}» ${ask.ширина}×${ask.висота}px` : '—');

// ── 2в-2г. ПІСЛЯ ПІДКАЗКИ кнопка згортається в круг, але лишається названою ───
// Чекаємо довше, ніж уся підказка: 420 (пауза) + 440 (розгортання) + 2600
// (утримання) + 440 (згортання) ≈ 3.9с від входу. Стенд стоїть тут уже ~1.5с,
// тож добираємо із запасом.
await p.waitForTimeout(3200);
const askПотім = await p.evaluate(() => {
  const el = document.querySelector('#board-trigger');
  if (!el) return null;
  const r = el.getBoundingClientRect();
  const п = el.querySelector('.qa-fab-label');
  return {
    ширина: Math.round(r.width), висота: Math.round(r.height),
    підписВидно: !!п && п.getBoundingClientRect().width > 0,
    ярлик: (el.getAttribute('aria-label') || '').trim(),
    широка: el.classList.contains('qa-fab-wide'),
  };
});
// 🔑 Міряємо КРУГЛІСТЬ (ширина ≈ висота), а не «немає класу»: клас можна лишити,
// а ширину протягти стилем звідки завгодно — людина побачить плашку, а стенд
// зелень. Допуск 2px — на субпіксельне округлення.
ok('2в. 🔴 через кілька секунд кнопка ЗГОРТАЄТЬСЯ назад у круг',
   !!askПотім && Math.abs(askПотім.ширина - askПотім.висота) <= 2 && !askПотім.широка,
   askПотім ? `${askПотім.ширина}×${askПотім.висота}px` : '—');
ok('2г. 🛑 згорнута кнопка все одно НАЗВАНА (для читача екрана й голосу)',
   !!askПотім && /Запитати/.test(askПотім.ярлик),
   askПотім ? `aria-label=«${askПотім.ярлик}»` : '—');

// ── 2д. ДРУГИЙ вхід у вкладку за той самий запуск — підказки вже немає ────────
// Пряма вимога Вови: «коли він вже в додатку і знов заходить туди, то це вже не
// потрібно робити». Виходимо на іншу вкладку і вертаємось тим самим шляхом, яким
// ходить людина (`switchTab`), а не смикаємо внутрішню функцію.
await p.evaluate(() => window.switchTab && window.switchTab('board'));
await p.waitForTimeout(500);
await p.evaluate(() => window.switchTab && window.switchTab('discussions'));
await p.waitForTimeout(1400);   // рівно те вікно, у якому підказка була б розгорнута
const askДругийВхід = await p.evaluate(() => {
  const el = document.querySelector('#board-trigger');
  if (!el) return null;
  const r = el.getBoundingClientRect();
  return { ширина: Math.round(r.width), висота: Math.round(r.height),
           широка: el.classList.contains('qa-fab-wide') };
});
ok('2д. 🔴 на ДРУГОМУ вході підказка вже не програється',
   !!askДругийВхід && !askДругийВхід.широка &&
   Math.abs(askДругийВхід.ширина - askДругийВхід.висота) <= 2,
   askДругийВхід ? `${askДругийВхід.ширина}×${askДругийВхід.висота}px` : '—');
ok('2е. тап-ціль ≥ 44px (Apple HIG, аудиторія 40-70+)', !!ask && ask.висота >= 44,
   ask ? `${ask.висота}px` : '—');
ok('2ж. стоїть у нижній половині — зоні великого пальця', !!ask && ask.уНижнійПоловині);

// ── 3. КАРТКА — ПРО ПИТАННЯ, А НЕ ПРО ЧАТ ────────────────────────────────────
const card = await p.evaluate(() => {
  const el = document.querySelector('#disc-content [data-question-open="701"]');
  if (!el) return null;
  const cs = (sel) => el.querySelector(sel);
  const q = cs('.qa-card-q');
  return {
    питання: q?.textContent.trim() || '',
    розмірПитання: q ? parseFloat(getComputedStyle(q).fontSize) : 0,
    автор: cs('.qa-card-name')?.textContent.trim() || '',
    коли: !!cs('.qa-card-when'),
    відповіді: cs('.qa-row-n')?.textContent.trim() || '',
    // 🆕 редакція 3: перша відповідь показується прямо в списку.
    цитата: cs('.qa-row-answer')?.textContent.trim() || '',
    // ЧАТ-МАРКЕРИ, яких не має бути:
    превюЧату: !!cs('.bd-chat-last'),
    учасники: !!cs('.bd-chat-participants'),
    лайкНаКартці: !!cs('[data-like-id]'),
  };
});
ok('3а. картка показує сам текст питання', !!card && /концерт/i.test(card.питання),
   card ? `«${card.питання.slice(0, 40)}»` : 'картки немає');
ok('3б. питання — найбільший текст картки (≥16px)', !!card && card.розмірПитання >= 16,
   card ? `${card.розмірПитання}px` : '—');
ok('3в. є автор і час', !!card && !!card.автор && card.коли, card ? `автор=«${card.автор}»` : '—');
ok('3г. є кількість ВІДПОВІДЕЙ (мова Q&A, не «повідомлень»)',
   !!card && /відповід/i.test(card.відповіді) && !/повідомл/i.test(card.відповіді),
   card ? `«${card.відповіді}»` : '—');
// 🔴 РЕДАКЦІЯ 7 (24.08): у списку стоїть ОСТАННЯ КОРЕНЕВА відповідь.
// ⚠️ Це НЕ повернення прев'ю чату (перевірка 3д нижче його далі забороняє): там
// були ДВА останні повідомлення з іменами й часом, тобто хід розмови; тут — ОДИН
// рядок, без часу, і лише кореневий.
// 📐 Сцена підібрана так, щоб перевірка МОГЛА впасти: у питання 701 дві кореневі
// відповіді (5001 стара, 5003 новіша) і вкладена репліка 5002, найновіша з усіх.
//   • стара редакція («перша»)        → «Начебто 24 серпня.»            ✗
//   • наївне «останній коментар»      → «Так, підтверджую — бачила…»    ✗
//   • правильно («остання коренева»)  → «Афішу вже повісили…»           ✓
// ⚠️ Автор 5003 навмисно З ПРІЗВИЩЕМ («Оксана Ковальчук»), бо тепер саме його
// імʼя стоїть у цитаті, а перевірка 13з вимагає скорочення до «Оксана». Дав би
// автора без прізвища — 13з мовчки перевіряла б найлегший випадок.
// ⚠️ І в тексті 5003 НЕМА «19:00»: сторож 5н забороняє HH:MM у відповідях, і
// перша редакція цієї фікстури завалила його власним текстом сцени.
ok('3ґ. 🔴 у списку стоїть ОСТАННЯ коренева відповідь, а не перша',
   !!card && /Афішу вже повісили/.test(card.цитата) && !/Начебто 24 серпня/.test(card.цитата),
   card ? `«${card.цитата.slice(0, 46)}»` : '—');
ok('3ґ2. 🛑 репліка під чужою відповіддю в картку НЕ потрапляє, навіть найновіша',
   !!card && !/підтверджую/.test(card.цитата),
   card ? `«${card.цитата.slice(0, 46)}»` : '—');
// 🔑 Число і цитата мусять описувати ОДИН набір. Інакше картка каже «3 відповіді»,
// а рядок під ним узятий із двох — слово «відповіді» має два значення в сусідніх
// рядках. Тут коренів рівно 2 при трьох живих коментарях.
ok('3ґ3. 🔴 лічильник рахує ТІЛЬКИ кореневі (2), а не всі коментарі (3)',
   !!card && /\b2\b/.test(card.відповіді) && !/\b3\b/.test(card.відповіді),
   card ? `«${card.відповіді}»` : '—');
// 🔴 24.08 — ПРОМІЖОК МІЖ ВІДПОВІДДЮ І АВТОРОМ. Скарга Вови зі знімка: «чому
// така велика щіль від повідомлення і до автора».
// 📐 Міряємо ВІДСТАНЬ ВІД КІНЦЯ НАМАЛЬОВАНОГО ТЕКСТУ до автора, а не від краю
// його коробки. Це не педантизм: вада була саме в тому, що КОРОБКА розтягувалась
// (`flex-grow: 1`), а текст лишався в її лівому краї — тобто коробка впритул до
// автора стояла ЗАВЖДИ, і вимір «край коробки → автор» показував би 5px і на
// зламаному коді. `scrollWidth` теж не годиться: при коробці ширшій за вміст він
// віддає ширину КОРОБКИ. Чесну ширину рядка дає лише `Range`.
// 🔴 МІРЯЄМО НА КОРОТКІЙ ВІДПОВІДІ, І ЦЕ НЕ ДРІБНИЦЯ. Перша редакція цієї
// перевірки міряла штатний текст сцени (219px у коробці 221px) — рядок і так
// майже повний, розтягуватись `flex-grow` нікуди, і КОНТРОЛЬ НА СТАРОМУ CSS
// давав 7px, тобто зеленів над зламаним кодом. Вада видима лише тоді, коли в
// рядку є вільне місце — саме такий випадок Вова й обвів на знімку («Взавтра»).
// Тому підставляємо короткий текст, міряємо, і повертаємо як було.
const проміжок = await p.evaluate(() => {
  const row = document.querySelector('#disc-content .qa-row[data-post-id="701"]');
  const txt = row?.querySelector('.qa-row-answer-text');
  const who = row?.querySelector('.qa-row-answer-who');
  if (!txt || !who) return null;
  const було = txt.textContent;
  txt.textContent = 'Взавтра';            // рівно випадок зі скарги
  const міра = () => {
    const r = document.createRange();
    r.selectNodeContents(txt);
    const tr = txt.getBoundingClientRect();
    const wr = who.getBoundingClientRect();
    const ширинаТексту = r.getBoundingClientRect().width;
    return {
      коробка: Math.round(tr.width),
      текст: Math.round(ширинаТексту),
      щіль: Math.round(wr.left - (tr.left + ширинаТексту)),
    };
  };
  const out = міра();
  txt.textContent = було;                 // сцена лишається такою, як була
  return out;
});
// Стеля 12px = `gap: 5px` плюс запас на округлення і згладжування шрифту.
// На старому коді тут десятки пікселів — саме те, що Вова обвів на знімку.
ok('3ґ4. 🔴 автор стоїть ОДРАЗУ за відповіддю, а не притиснутий до краю картки',
   !!проміжок && проміжок.щіль <= 12,
   проміжок ? `щіль ${проміжок.щіль}px (текст ${проміжок.текст}px у коробці ${проміжок.коробка}px)` : '—');

// ── 3е-порядок. 🔴 СПИСОК СТОЇТЬ ЗА ДАТОЮ ПИТАННЯ (24.08) ──────────────────
// Скарга Вови зі знімка: «саме перше питання — це питання, яке було опубліковане
// 11 серпня, а друге питання — це питання, яке було опубліковане 11 годин тому…
// це вже починається хаос».
// 🔑 Вада була не в осі сортування самій по собі, а в РОЗХОДЖЕННІ: картка
// показує дату ПИТАННЯ, а порядок будувався за датою останньої ВІДПОВІДІ. Порядок
// неможливо було пояснити нічим, що видно на екрані.
// 📐 Сцена доводить це числами: 702 СТВОРЕНО пізніше за 701 (t0+6e4 проти t0),
// але в 701 лежить свіжа відповідь (t0+48e5). Тобто дві осі дають ПРОТИЛЕЖНИЙ
// порядок, і перевірка не може бути зеленою для обох.
const порядок = await p.evaluate(() =>
  [...document.querySelectorAll('#disc-content .qa-row')].map(r => r.dataset.postId));
ok('3е-порядок. 🔴 новіше ПИТАННЯ вище, хоч свіжа відповідь лежить у старішому',
   порядок.indexOf('702') >= 0 && порядок.indexOf('702') < порядок.indexOf('701'),
   `порядок: ${порядок.join(' → ')}`);

ok('3д. 🔴 НЕМА прев\'ю останніх повідомлень (розмітка списку чатів)',
   !!card && !card.превюЧату);
ok('3е. 🔴 НЕМА лічильника «учасників» (метрика чату)', !!card && !card.учасники);
ok('3є. ❤️ прибрано з картки (переїхало всередину як «мене теж цікавить»)',
   !!card && !card.лайкНаКартці);

// ── 4. ПИТАННЯ БЕЗ ВІДПОВІДІ — ЗАКЛИК, А НЕ МОВЧАЗНИЙ НУЛЬ ───────────────────
const none = await p.evaluate(() => {
  const el = document.querySelector('#disc-content [data-question-open="702"]');
  if (!el) return null;
  const c = el.querySelector('.qa-row-n');
  return { текст: c?.textContent.trim() || '', позначка: el.classList.contains('qa-row--unanswered') };
});
ok('4а. без відповідей показано словами, а не «0»',
   !!none && /потрібна відповідь/i.test(none.текст) && !/\b0\b/.test(none.текст),
   none ? `«${none.текст}»` : '—');
ok('4б. картка має власну позначку стану', !!none && none.позначка);

// ── 5. ВІДКРИТЕ ПИТАННЯ — ЕКРАН, А НЕ МЕСЕНДЖЕР ──────────────────────────────
await p.evaluate(() => document.querySelector('[data-question-open="701"]')?.click());
await p.waitForTimeout(900);

const screen = await p.evaluate(() => {
  const s = document.querySelector('.qa-screen');
  if (!s) return null;
  const r = s.getBoundingClientRect();
  const inp = s.querySelector('.qa-input');
  const send = s.querySelector('.qa-send');
  return {
    наВесьЕкран: Math.round(r.width) >= window.innerWidth - 1 && Math.round(r.height) >= window.innerHeight - 1,
    заголовок: s.querySelector('.qa-head-title')?.textContent.trim() || '',
    питання: s.querySelector('.qa-question-text')?.textContent.trim() || '',
    розмірПитання: (() => { const q = s.querySelector('.qa-question-text'); return q ? parseFloat(getComputedStyle(q).fontSize) : 0; })(),
    цікавить: s.querySelector('.qa-interest')?.textContent.replace(/\s+/g, ' ').trim() || '',
    відповідей: s.querySelectorAll('.qa-answer').length,
    вкладених: s.querySelectorAll('.qa-answer--sub').length,
    кнопкаВідповісти: !!s.querySelector('[data-answer-reply]'),
    плейсхолдер: inp?.getAttribute('placeholder') || '',
    розмірПоля: inp ? parseFloat(getComputedStyle(inp).fontSize) : 0,
    підписКнопки: send?.textContent.trim() || '',
    // МЕСЕНДЖЕР-МАРКЕРИ, яких не має бути:
    бульбашки: s.querySelectorAll('.pm-bubble').length,
    роздільникиДнів: s.querySelectorAll('.pm-daysep').length,
    пігулкаНових: !!s.querySelector('.bd-chat-newpill'),
    годинник: /\b\d{2}:\d{2}\b/.test(s.querySelector('.qa-answers')?.textContent || ''),
  };
});
ok('5а. відкрився ПОВНОЕКРАННИЙ шар, а не модалка поверх сторінки',
   !!screen && screen.наВесьЕкран);
ok('5б. шапка називає екран «Питання»', !!screen && screen.заголовок === 'Питання',
   screen ? `«${screen.заголовок}»` : '—');
ok('5в. питання — найбільший текст екрана (≥20px)', !!screen && screen.розмірПитання >= 20,
   screen ? `${screen.розмірПитання}px` : '—');
ok('5г. є «Мене теж цікавить» (замість лайка теми)',
   !!screen && /Мене теж цікавить/i.test(screen.цікавить), screen ? `«${screen.цікавить}»` : '—');
// ⚠️ На ЕКРАНІ питання видно ВСІ ТРИ (дві кореневі + репліка) — і це не
// суперечність із лічильником «2 відповіді». Картка каже, скільки ВІДПОВІЛИ на
// питання; екран показує всю розмову, включно з репліками під відповідями.
// ⚠️ 25.08 — ЧИСЛА ЗМІНИЛИСЬ ЧЕРЕЗ ЗГОРТАННЯ, і це не регрес. У фікстурі
// пʼять відповідей (2 кореневі + 3 вкладені), але гілка при відкритті
// ЗГОРНУТА: видно 2 кореневі + 1 вкладену, решта під «Ще 2 відповіді».
ok('5д. видно 3 з 5 (гілка згорнута: 2 кореневі + 1 вкладена)',
   !!screen && screen.відповідей === 3, screen ? `${screen.відповідей}` : '—');
ok('5е. вкладені відповіді показані з відступом, а не цитатою в бульбашці',
   !!screen && screen.вкладених === 1, screen ? `вкладених=${screen.вкладених}` : '—');
ok('5є. «Відповісти» — ЯВНА кнопка, а не прихований свайп', !!screen && screen.кнопкаВідповісти);
ok('5ж. поле підписане «Ваша відповідь…», не «Написати повідомлення…»',
   !!screen && /Ваша відповідь/i.test(screen.плейсхолдер) && !/повідомлення/i.test(screen.плейсхолдер),
   screen ? `«${screen.плейсхолдер}»` : '—');
ok('5з. кнопка надсилання підписана СЛОВОМ, а не стрілкою «↑»',
   !!screen && /Надіслати/.test(screen.підписКнопки) && !/[↑➤]/.test(screen.підписКнопки),
   screen ? `«${screen.підписКнопки}»` : '—');
ok('5и. поле вводу ≥16px (менше — iOS зумить сторінку на фокусі)',
   !!screen && screen.розмірПоля >= 16, screen ? `${screen.розмірПоля}px` : '—');
ok('5к. 🔴 НЕМА бульбашок чату', !!screen && screen.бульбашки === 0,
   screen ? `знайдено ${screen.бульбашки}` : '—');
ok('5л. 🔴 НЕМА роздільників днів «Сьогодні/Вчора»', !!screen && screen.роздільникиДнів === 0);
ok('5м. 🔴 НЕМА пігулки «N нових повідомлень»', !!screen && !screen.пігулкаНових);
ok('5н. 🔴 НЕМА годинникового часу HH:MM у відповідях', !!screen && !screen.годинник);

// ── 6. ЗАКРИТТЯ ЙДЕ ЧЕРЕЗ ІСТОРІЮ (системний жест «назад» на iPhone) ────────
// 🔑 Міряємо не «зникло», а ЧИМ зникло: якщо екран прибирається без запису в
// історії, системний жест зʼїсть чужий запис і винесе людину з вкладки — саме
// цей клас багів і закриває `core/layers.js`.
const beforeLen = await p.evaluate(() => history.length);
await p.goBack();
await p.waitForTimeout(700);
const afterClose = await p.evaluate(() => ({
  екранЗник: !document.querySelector('.qa-screen'),
  вкладка: document.querySelector('.app-main')?.dataset.tab || '',
}));
ok('6а. системний жест «назад» закриває екран питання', afterClose.екранЗник,
   `history.length було ${beforeLen}`);
ok('6б. і НЕ виносить людину з вкладки', afterClose.вкладка === 'discussions',
   `вкладка=${afterClose.вкладка}`);

// ── 7. ФОРМА СТВОРЕННЯ ПИТАЄ ПРО ПИТАННЯ, А НЕ ПРО «ТЕМУ» ───────────────────
// Шлях той самий, що в людини: тап по FAB → пункт «Запитати громаду» в меню.
await p.evaluate(() => document.querySelector('#board-trigger')?.click());
await p.waitForTimeout(400);
await p.evaluate(() => document.querySelector('[data-fab="disc-create"]')?.click());
await p.waitForTimeout(900);
const compose = await p.evaluate(() => {
  const w = document.querySelector('.app-modal--disc');
  if (!w) return null;
  return {
    заголовок: w.querySelector('.disc-sheet-title')?.textContent.trim() || '',
    мітка: w.querySelector('.disc-compose-label')?.textContent.trim() || '',
    плейсхолдер: w.querySelector('.disc-compose-input')?.getAttribute('placeholder') || '',
    кнопка: w.querySelector('.disc-compose-submit')?.textContent.trim() || '',
    підказка: w.querySelector('.disc-compose-hint')?.textContent.trim() || '',
    полів: w.querySelectorAll('.disc-compose input, .disc-compose textarea, .disc-compose select').length,
  };
});
ok('7а. аркуш зветься «Запитати громаду»', !!compose && /Запитати громаду/i.test(compose.заголовок),
   compose ? `«${compose.заголовок}»` : 'аркуша немає');
ok('7б. питає «Що ви хочете дізнатись?», а не «Тема обговорення»',
   !!compose && /дізнатись/i.test(compose.мітка) && !/тема/i.test(compose.мітка),
   compose ? `«${compose.мітка}»` : '—');
ok('7в. кнопка — «Запитати громаду», не «Створити»',
   !!compose && /Запитати громаду/i.test(compose.кнопка), compose ? `«${compose.кнопка}»` : '—');
ok('7г. є живий приклад питання (для тих, хто не знає, що писати)',
   !!compose && compose.підказка.length > 10, compose ? `«${compose.підказка}»` : '—');
ok('7д. 🔑 поле РІВНО ОДНЕ — подати питання швидше, ніж оголошення',
   !!compose && compose.полів === 1, compose ? `полів=${compose.полів}` : '—');

// ── 7.5 🔴 «ХТО КОМУ НАПИСАВ» — ВИДНО БЕЗ ЗДОГАДОК (редакція 5) ─────────────
//
// Скарга Вови: «коли відкриваю карточку запитання, я не до кінця розумію, що, хто,
// кому написав, чи це якісь нові карточки… це геть незрозуміло».
// 🔑 Причина була в тому, що кожна відповідь малювалась ОКРЕМОЮ БІЛОЮ КАРТКОЮ з
// тінню — тобто читалась як самостійний обʼєкт, а не як репліка під питанням.
// Вкладеність показував лише відступ, зв'язку «хто кому» не було взагалі.
// Виправлено за патерном коментарів «Стрічки», який Вова вже приймав.
//
// Міряємо ТРИ наслідки, а не класи:
//   1) відповідь — НЕ картка (немає власної білої підкладки й тіні);
//   2) вкладена має ЛІНІЮ-ЗВʼЯЗУВАЧ до батьківської;
//   3) якщо відповідають ІНШІЙ людині — її імʼя стоїть на початку тексту.
await p.evaluate(() => document.querySelector('[data-question-open="701"]')?.click());
await p.waitForTimeout(1000);

const репліки = await p.evaluate(() => {
  const корінь = document.querySelector('.qa-thread > .qa-answer');
  const вкладена = document.querySelector('.qa-branch .qa-answer');
  const гілка = document.querySelector('.qa-branch');
  if (!корінь) return null;
  const cs = getComputedStyle(корінь);
  const прозорий = (c) => !c || c === 'rgba(0, 0, 0, 0)' || c === 'transparent';
  const лінія = гілка ? getComputedStyle(гілка, '::before') : null;
  return {
    безПідкладки: прозорий(cs.backgroundColor),
    безТіні: cs.boxShadow === 'none',
    аватарЗліва: !!корінь.querySelector('.qa-answer-ava'),
    імʼя: корінь.querySelector('.qa-answer-name')?.textContent.trim() || '',
    єВкладена: !!вкладена,
    відступГілки: гілка ? Math.round(parseFloat(getComputedStyle(гілка).marginLeft)) : 0,
    лініяЄ: !!лінія && parseFloat(лінія.borderLeftWidth) > 0,
    згадка: вкладена?.querySelector('.qa-answer-to')?.textContent.trim() || '',
    загадковеЩе: !!document.querySelector('.qa-answer-more'),
  };
});
ok('7.5а. 🔴 відповідь — репліка, а НЕ картка (без підкладки й тіні)',
   !!репліки && репліки.безПідкладки && репліки.безТіні,
   репліки ? `фон прозорий=${репліки.безПідкладки} тінь=${репліки.безТіні}` : 'відповідей немає');
ok('7.5б. аватар автора стоїть при репліці (видно, ХТО написав)',
   !!репліки && репліки.аватарЗліва && репліки.імʼя.length > 0,
   репліки ? `«${репліки.імʼя}»` : '—');
ok('7.5в. 🔴 вкладена відповідь має відступ І лінію-звʼязувач (видно, ЩО це відповідь)',
   !!репліки && репліки.єВкладена && репліки.відступГілки >= 30 && репліки.лініяЄ,
   репліки ? `відступ=${репліки.відступГілки}px лінія=${репліки.лініяЄ}` : '—');
ok('7.5г. 🛑 немає кнопки «Ще» — усі дії підписані словами',
   !!репліки && !репліки.загадковеЩе);

// ── 7.55. 🔴 ЗГОРТАННЯ ГІЛОК, ЯК У «СТРІЧЦІ» (25.08) ───────────────────────
// Слово Вови: «чому відповіді на коментарі не згортаються так як в стрічці? Це
// ж по факту система та сама, логіка та сама».
// 📐 У сцені в кореня 5001 ТРИ відповіді, а видно має бути ОДНУ + кнопка.
const згорнуто = await p.evaluate(() => {
  const нитка = [...document.querySelectorAll('.qa-screen .qa-thread')]
    .find(t => t.querySelector('.qa-answer--sub') || t.querySelector('[data-more-parent]'));
  const btn = нитка?.querySelector('[data-more-parent]');
  return {
    субів: нитка ? нитка.querySelectorAll('.qa-answer--sub').length : -1,
    підпис: (btn?.textContent || '').trim(),
    кнопкаВГілці: !!btn?.closest('.qa-branch'),
  };
});
ok('7.55а. 🔴 у згорнутому стані видно ОДНУ відповідь, решта під кнопкою',
   згорнуто.субів === 1, `видно ${згорнуто.субів} вкладених`);
ok('7.55б. кнопка називає ЧИСЛО схованого і узгоджує слово',
   /^Ще 2 відповіді$/.test(згорнуто.підпис), `«${згорнуто.підпис}»`);
// 🔑 Кнопка мусить бути ЕЛЕМЕНТОМ ГІЛКИ — інакше лінія обірветься на останній
// видимій відповіді, а кнопка висітиме збоку, ні до чого не приєднана.
ok('7.55в. 🛑 кнопка лежить усередині гілки (отримує гачок на лінії)',
   згорнуто.кнопкаВГілці === true);

// Розгортаємо — далі всі перевірки йдуть на РОЗГОРНУТІЙ гілці.
await p.evaluate(() => document.querySelector('.qa-screen [data-more-parent]')?.click());
await p.waitForTimeout(400);
const розгорнуто = await p.evaluate(() => {
  const нитка = [...document.querySelectorAll('.qa-screen .qa-thread')]
    .find(t => t.querySelector('.qa-answer--sub'));
  return {
    субів: нитка ? нитка.querySelectorAll('.qa-answer--sub').length : -1,
    підпис: (нитка?.querySelector('[data-less-parent]')?.textContent || '').trim(),
  };
});
// ⚠️ Умова `згорнуто.субів < розгорнуто.субів` тут ОБОВʼЯЗКОВА, а не для краси:
// без неї перевірка зеленіла б і на старому коді, де гілка й так розгорнута
// завжди — тобто «показує всі три» було б істинним НІ НАД ЧИМ. Тап мусить
// саме ЗМІНИТИ картину, а не збігтися з нею.
ok('7.55г. 🔴 тап по «Ще N» показує ВСІ відповіді гілки (і це саме зміна)',
   розгорнуто.субів === 3 && згорнуто.субів < розгорнуто.субів,
   `було ${згорнуто.субів} → стало ${розгорнуто.субів}`);
ok('7.55д. і кнопка стає «Сховати відповіді» (дію можна відмінити)',
   /^Сховати відповіді$/.test(розгорнуто.підпис), `«${розгорнуто.підпис}»`);

// ── 7.6. 🔴 ВІДПОВІДАТИ МОЖНА НА БУДЬ-ЯКУ ВІДПОВІДЬ (24.08) ─────────────────
// Скарга Вови: «я відповідаю на цю відповідь, і автор мені не може відповісти,
// тому що немає кнопки… все прив'язано тільки до одної відповіді на коментар».
const гілка = await p.evaluate(() => {
  const нитки = [...document.querySelectorAll('.qa-screen .qa-thread')];
  const нитка = нитки.find(t => t.querySelector('.qa-answer--sub'));
  if (!нитка) return null;
  const субs = [...нитка.querySelectorAll('.qa-answer--sub')];
  const текст = (el) => (el.querySelector('.qa-answer-text')?.textContent || '').replace(/\s+/g, ' ').trim();
  // Геометрія стовбура між сусідніми відповідями: чи є ЛІНІЯ у проміжку.
  const обгортки = [...нитка.querySelectorAll('.qa-branch')];
  const продовження = обгортки.slice(0, -1).map(el => {
    const cs = getComputedStyle(el, '::after');
    return {
      товщина: parseFloat(cs.borderLeftWidth) || 0,
      верх: parseFloat(cs.top),           // мусить накрити 16px проміжку
      колір: cs.borderLeftColor,
    };
  });
  const остання = обгортки.length ? getComputedStyle(обгортки[обгортки.length - 1], '::after') : null;
  return {
    субів: субs.length,
    кнопокНаСубах: субs.filter(el => el.querySelector('.qa-answer-reply')).length,
    тексти: субs.map(текст),
    продовження,
    хвістНаОстанній: остання ? (parseFloat(остання.borderLeftWidth) || 0) : -1,
    фон: getComputedStyle(document.querySelector('.qa-body') || document.body).backgroundColor,
  };
});

ok('7.6а. 🔴 «Відповісти» стоїть на КОЖНІЙ відповіді, не лише на кореневій',
   !!гілка && гілка.субів > 0 && гілка.кнопокНаСубах === гілка.субів,
   гілка ? `кнопок ${гілка.кнопокНаСубах} з ${гілка.субів} вкладених` : '—');

// 🛑 НАЙВАЖЛИВІШИЙ СТОРОЖ ЦІЄЇ ЗМІНИ. Відповідь на вкладену (5004 → 5002 → 5001)
// у сирих даних лежить на ТРЕТЬОМУ рівні. Підняття «на один щабель» віддавало
// їй за батька 5002 — а 5002 сам не корінь, тож до малювання вона не доходила
// НІКОЛИ і зникала БЕЗ ЖОДНОЇ ПОМИЛКИ. Саме такі вади найдорожчі: людина
// написала, побачила свій текст оптимістично, а після перемальовування його нема.
ok('7.6б. 🛑 відповідь на ВКЛАДЕНУ не зникає — піднімається до СПРАВЖНЬОГО кореня',
   !!гілка && гілка.тексти.some(t => /перенесли на 25-те/.test(t))
           && гілка.тексти.some(t => /дзвонив у сільраду/.test(t)),
   гілка ? `у гілці: ${гілка.тексти.map(t => `«${t.slice(0, 24)}»`).join(' · ')}` : '—');

// ── 7.7. 🔴 ЛІНІЯ-ЗВʼЯЗУВАЧ: СУЦІЛЬНА І ВИДИМА (24.08) ──────────────────────
// «вона має бути з'єднана… якщо є коментар і на нього дається відповідь, потім
// за ним ще одна відповідь, вона має бути з'єднана».
ok('7.7а. 🔴 між сусідніми відповідями гілки стовбур НЕ переривається',
   !!гілка && гілка.продовження.length > 0 &&
   гілка.продовження.every(x => x.товщина >= 2 && x.верх <= -16),
   гілка ? гілка.продовження.map(x => `${x.товщина}px від ${x.верх}px`).join(' · ') || 'сусідніх пар нема' : '—');

// 🛑 А на ОСТАННІЙ лінія мусить обірватись — інакше з гілки звисає хвіст і
// впирається в аватар НАСТУПНОГО кореня (обидва на одній вертикалі x=16px).
// Цю помилку у «Стрічці» вже робили, Вова: «воно взяло поєднало перший коментар
// з коментарем іншої людини».
ok('7.7б. 🛑 на ОСТАННІЙ відповіді стовбур обривається (не звисає в порожнечу)',
   !!гілка && гілка.хвістНаОстанній === 0,
   гілка ? `товщина на останній: ${гілка.хвістНаОстанній}px` : '—');

// 📐 Видимість міряємо КОНТРАСТОМ до фону, а не назвою кольору: інакше перевірку
// обійшов би будь-який новий блідий hex. Було `#E6E7EB` = 1.06:1, тобто лінії
// фактично не існувало. Поріг 1.40 — домашній, той самий, що для обідка картки
// Дошки (`--board-line`), і взятий не зі стелі, а з наявного правила проєкту.
const контраст = await p.evaluate(() => {
  const el = document.querySelector('.qa-screen .qa-branch');
  if (!el) return null;
  const parse = (c) => (c.match(/[\d.]+/g) || []).slice(0, 3).map(Number);
  const lin = (v) => { v /= 255; return v <= 0.03928 ? v / 12.92 : Math.pow((v + 0.055) / 1.055, 2.4); };
  const L = ([r, g, b]) => 0.2126 * lin(r) + 0.7152 * lin(g) + 0.0722 * lin(b);
  const лінія = parse(getComputedStyle(el, '::before').borderLeftColor);
  // Фон беремо з першого предка, у якого він не прозорий.
  let n = el, фон = null;
  while (n && !фон) {
    const bg = getComputedStyle(n).backgroundColor;
    if (bg && !/rgba\(0, 0, 0, 0\)|transparent/.test(bg)) фон = parse(bg);
    n = n.parentElement;
  }
  if (!лінія.length || !фон) return null;
  const a = L(лінія), b = L(фон);
  return { к: (Math.max(a, b) + 0.05) / (Math.min(a, b) + 0.05), лінія, фон };
});
ok('7.7в. 🔴 лінію ВИДНО: контраст до фону ≥ 1.40 (було 1.06 — фактично невидима)',
   !!контраст && контраст.к >= 1.40,
   контраст ? `${контраст.к.toFixed(2)}:1 · лінія rgb(${контраст.лінія}) на rgb(${контраст.фон})` : '—');

// 🔴 25.08 — ЛІНІЯ НЕЙТРАЛЬНА, А НЕ БОРДОВА. Рішення Вови після живого екрана:
// «лінію з бордової треба замінити на чорну». Вага лишилась та сама (28%),
// змінився ВІДТІНОК — тому контраст тут нічого не доводить (він зелений в
// обох випадках), міряти треба ТЕПЛОТУ.
//
// ⚠️ І поріг «різниця каналів ≈ 0» був би НЕВІРНИЙ, у чому я вже помилився:
// чорний поверх злегка синюватого фону лишається злегка синюватим (`#AAABAE`,
// R−B = −4), бо прозорий шар успадковує відтінок того, що під ним.
// 🔑 Правильна міра — чи має лінія ВЛАСНИЙ відтінок понад фоновий: у фону
// R−B = −5, у чорної лінії −4 (різниця 1), у бордової було +17 (різниця 22).
ok('7.7г. 🔴 звʼязувач повторює відтінок фону, а не має власного (не бордовий)',
   !!контраст &&
   Math.abs((контраст.лінія[0] - контраст.лінія[2]) - (контраст.фон[0] - контраст.фон[2])) <= 6,
   контраст
     ? `лінія R−B = ${контраст.лінія[0] - контраст.лінія[2]}, фон R−B = ${контраст.фон[0] - контраст.фон[2]} (бордовий давав +17)`
     : '—');

await p.goBack();
await p.waitForTimeout(600);

// ── 8. 🔴 МЕЖІ ВІЗУАЛЬНОЇ ВАГИ (заведено 11.08 після відмови Вови) ───────────
//
// Першу редакцію вкладки Вова відхилив словами «це не схоже на стиль Apple, це
// схоже на щось на фейсбук 2006 року». Прилад `tests/tools/qa-audit.mjs` показав,
// що саме за цим стояло — і ці ж числа стають межами, щоб воно не повернулось:
//
//   ліній і рамок у першому екрані ... 43 → 4
//   різних кольорів тексту .......... 7  → 4
//   перша інформація ................ 36% екрана → 25%
//   тап-цілі < 44px ................. 6  → 0
//
// 🔑 Чому саме ці три числа, а не «схоже на Apple». «Схожість» перевірити
// неможливо, а ці величини — прямі наслідки того способу верстки, який дав
// відмову: ієрархія лініями і кольоровими плитами замість простору й тону.
// Межі поставлені з запасом до чинних значень, щоб дрібне доопрацювання їх не
// валило, але повернення банера чи рамок на картках — валило одразу.
// ⚠️ Рахуємо ЛИШЕ ВИДИМЕ: перша редакція приладу цього не робила і нарахувала
// 28 ліній, з яких усі лежали в ЗАКРИТОМУ меню FAB.
await p.evaluate(() => document.querySelector('.pm-actions-cancel, .app-modal-close')?.click());
await p.waitForTimeout(400);
await p.evaluate(() => { const l = document.querySelector('.qa-screen'); if (l) history.back(); });
await p.waitForTimeout(500);

const вага = await p.evaluate(() => {
  const H = window.innerHeight;
  const видно = (el) => {
    let n = el;
    while (n && n.nodeType === 1) {
      const s = getComputedStyle(n);
      if (s.display === 'none' || s.visibility === 'hidden' || parseFloat(s.opacity) === 0) return false;
      if (n.hasAttribute && n.hasAttribute('hidden')) return false;
      n = n.parentElement;
    }
    return true;
  };
  let ліній = 0;
  const кольори = new Set();
  const дрібні = [];
  for (const el of document.querySelectorAll('#disc-content *')) {
    const r = el.getBoundingClientRect();
    if (r.top > H || r.bottom < 0 || !видно(el)) continue;
    const s = getComputedStyle(el);
    if (r.width >= 2) {
      for (const side of ['Top','Right','Bottom','Left']) {
        if (parseFloat(s['border' + side + 'Width']) > 0
            && s['border' + side + 'Color'] !== 'rgba(0, 0, 0, 0)'
            && s.borderStyle !== 'none') ліній++;
      }
    }
    if ([...el.childNodes].some(n => n.nodeType === 3 && n.textContent.trim())) кольори.add(s.color);
  }
  for (const el of document.querySelectorAll('#disc-content button, #disc-content input')) {
    const r = el.getBoundingClientRect();
    if (r.top > H || r.bottom < 0 || !r.width || !видно(el)) continue;
    if (r.height < 44) дрібні.push(`${el.className.split(' ')[0]} ${Math.round(r.height)}px`);
  }
  const перша = document.querySelector('#disc-content [data-question-open]');
  return {
    ліній, кольорів: кольори.size, дрібні,
    доІнформації: перша ? Math.round(перша.getBoundingClientRect().top / H * 100) : 100,
  };
});
ok('8а. 🔴 видимих ліній і рамок ≤ 8 (у відхиленій редакції було 43)',
   вага.ліній <= 8, `${вага.ліній}`);
ok('8б. 🔴 різних кольорів тексту ≤ 5 (було 7 — розкид читається як «наосліп»)',
   вага.кольорів <= 5, `${вага.кольорів}`);
// ⚠️ 11.08, редакція 4 — МЕЖУ ПІДНЯТО з 30% до 33%, і ось чому це не підгонка
// під зелене. Вова прямо попросив додати коротке пояснення розділу («незрозуміло
// для чого це і що… треба якусь інструкцію, може таку мінімальну, в одне речення
// чи в два») — два рядки тексту коштують ~44px, тобто 5% екрана. Це ЗМІСТ, який
// замовлено, а не роздування шапки.
// 🛑 Межа 33%, а не «скільки вийде»: банер редакції 1 давав **36%**, і цей шлях
// лишається закритим. Тобто перевірка й далі ловить рівно те, від чого йшли, але
// не б'є по замовленій інструкції.
ok('8в. 🔴 перша картка вище 33% екрана (банер редакції 1 давав 36%)',
   вага.доІнформації <= 33, `${вага.доІнформації}%`);
ok('8г. 🔴 жодної тап-цілі < 44px (було 6)',
   вага.дрібні.length === 0, вага.дрібні.join(' · ') || 'усі ≥ 44px');

// ── 9. 🔴 РОЗДІЛ ПОЯСНЮЄ СЕБЕ СЛОВАМИ (замовлення Вови, редакція 4) ─────────
// Претензія «незрозуміло, що це і для чого» звучала ТРИЧІ, і три рази її лікували
// ВИГЛЯДОМ — банером, типографікою, щільністю списку. Не спрацювало жодного разу.
// Вова назвав рішення сам: коротка інструкція в одне-два речення.
// 🔑 Міряємо і НАЯВНІСТЬ, і ДОВЖИНУ: пояснення, що розповзлося на пів екрана, —
// це знову банер, тільки текстом (перша спроба зайняла 4 рядки і відсунула перше
// питання на 36%).
const пояснення = await p.evaluate(() => {
  const el = document.querySelector('#disc-content .qa-hero-note');
  if (!el) return null;
  const r = el.getBoundingClientRect();
  const lh = parseFloat(getComputedStyle(el).lineHeight) || 20;
  return { текст: el.textContent.trim(), рядків: Math.round(r.height / lh),
           висота: Math.round(r.height) };
});
ok('9а. 🔴 під заголовком є коротке пояснення, ЩО це за розділ',
   !!пояснення && пояснення.текст.length > 30,
   пояснення ? `«${пояснення.текст}»` : 'пояснення немає');
ok('9б. воно каже і про питання, і про допомогу (обидві ролі людини)',
   !!пояснення && /питайте|запита/i.test(пояснення.текст) && /допомож|відповід/i.test(пояснення.текст));
ok('9в. 🛑 не більше 2 рядків — інакше це знову банер, тільки текстом',
   !!пояснення && пояснення.рядків <= 2, пояснення ? `${пояснення.рядків} ряд. / ${пояснення.висота}px` : '—');

// ── 10. Рядок стану: скільки питань і скільки чекає допомоги ────────────────
const стан = await p.evaluate(() => {
  const el = document.querySelector('#disc-content .qa-hero-count');
  return el ? el.textContent.replace(/\s+/g, ' ').trim() : null;
});
ok('10. під пошуком видно, скільки питань і скільки без відповіді',
   !!стан && /\d+ питанн/.test(стан) && /без відповіді/.test(стан), стан || '—');

// ── 11. ПОВНИЙ СПИСОК: скільки екрана шапка займає НАЗАВЖДИ ─────────────────
//
// 🔴 ЧОМУ ЦЕ ОКРЕМИЙ СЦЕНАРІЙ, А НЕ ЩЕ ОДНА ПЕРЕВІРКА ВГОРІ.
// Усе вище міряється на фікстурі з ДВОХ питань. А біда, яку Вова побачив на
// проді, починається з ВОСЬМИ: саме там вмикаються чіпи (`QA_CHIPS_FROM`), і
// шапка доростає до 242px = **38% екрана**. Тобто стенд був зелений 53/53 рівно
// тому, що ЙОГО СЦЕНАРІЙ не збігався з життям — а перевірка «перша картка вище
// 33%» (8в) навіть існувала і показувала 31%.
// 🔑 Це вже **шістнадцятий** випадок брехливої мірки в проєкті, і найтихіший з
// усіх: прилад не помилявся в обчисленні — він міряв не ту сцену.
// ⚠️ Тому тут піднімається ОКРЕМИЙ браузер із 12 питаннями: підмінити дані на
// вже відкритій сторінці не можна, заглушка вшита в маршрут при старті.
const БАГАТО = Array.from({ length: 12 }, (_, i) => ({
  id: 800 + i, type: 'chat', text: `Питання номер ${i + 1} про життя громади?`,
  title: null, author: 'Іван', owner_uid: null, status: 'published', location: null, tags: [],
  ts: t0 + i * 6e4, created_at: new Date(t0 + i * 6e4).toISOString(),
  published_at: new Date(t0 + i * 6e4).toISOString(),
}));
// Відповіді лише на частину — інакше «без відповіді» = 0 і чіпи не вмикаються.
const ВІДПОВІДІ = БАГАТО.slice(0, 8).map((q, i) => ({
  id: 8100 + i, post_id: q.id, author: 'Галина', text: 'Бачила оголошення на дошці біля магазину.',
  sender_uid: 'u-g', reply_to_id: null, created_at: new Date(t0 + 36e5).toISOString(),
  edited_at: null, deleted_at: null, client_tag: null,
}));

const ctx2 = await b.newContext({ viewport: { width: 390, height: 844 }, isMobile: true,
                                  hasTouch: true, serviceWorkers: 'block' });
const p2 = await ctx2.newPage();
if (BUNDLE_REV) {
  const old = projectFile('bundle.js', BUNDLE_REV);
  await p2.route('**/bundle.js', r => r.fulfill({ contentType: 'application/javascript', body: old }));
}
if (CSS_REV) {
  const old = projectFile('style/board.css', CSS_REV);
  await p2.route('**/style/board.css', r => r.fulfill({ contentType: 'text/css', body: old }));
}
await mockSupabase(p2, { posts: БАГАТО, comments: ВІДПОВІДІ, announcements: [] },
                  { user: { id: 'u-me', name: 'Я' } });
await p2.route('**://api.open-meteo.com/**', r => r.abort());
await p2.goto(url, { waitUntil: 'domcontentloaded' });
await p2.waitForTimeout(2500);
await p2.evaluate(() => document.querySelector('.consent-accept')?.click());
await p2.waitForTimeout(300);
await p2.evaluate(() => window.switchTab && window.switchTab('discussions'));
await p2.waitForTimeout(4600);   // перечекати підказку FAB, щоб вона не заважала мірці

const повний = await p2.evaluate(() => {
  const шапка = document.querySelector('#disc-content .bd-controls');
  const тіло  = document.querySelector('#disc-content .bd-body') || document.querySelector('.bd-body');
  const карта = document.querySelector('[data-question-open]');
  const чіпи  = document.querySelector('#disc-content .qa-chips');
  const лічильник = document.querySelector('#disc-content .qa-hero-count');
  return {
    екран: window.innerHeight,
    шапка: шапка ? Math.round(шапка.getBoundingClientRect().height) : null,
    картка: карта ? Math.round(карта.getBoundingClientRect().top) : null,
    чіпиЄ: !!чіпи,
    чіпиТекст: чіпи ? чіпи.textContent.replace(/\s+/g, ' ').trim() : '',
    лічильникТекст: лічильник ? лічильник.textContent.replace(/\s+/g, ' ').trim() : '',
    скролер: !!тіло,
  };
});
ok('11а. перша картка вище 33% екрана і при ПОВНОМУ списку',
   !!повний.картка && повний.картка < повний.екран * 0.33,
   `${повний.картка}px = ${Math.round(100 * повний.картка / повний.екран)}%`);
// Дублювання числа — окрема претензія Вови: «без відповіді 4» стояло і в чіпі,
// і в рядку під пошуком, за 60px одне від одного.
ok('11б. 🛑 число «без відповіді» НЕ друкується двічі',
   повний.чіпиЄ && !/без відповіді/i.test(повний.лічильникТекст),
   `чіпи: «${повний.чіпиТекст}» · лічильник: «${повний.лічильникТекст || '—'}»`);

// 🔴 ГОЛОВНА ПЕРЕВІРКА ЦЬОГО БЛОКА: скільки екрана шапка займає ПІД ЧАС ПРОКРУТКИ.
//
// Саме це, а не її висота, було скаргою Вови: «дуже багато місця займає оце все
// зверху, а знизу там десь скроляться карточки». Шапка може бути якою завгодно
// заввишки на першому кадрі — важливо, щоб вона не з'їдала екран у КОЖНОМУ
// наступному. Тому міряємо не `height`, а скільки її ЛИШИЛОСЬ у видимій зоні
// після прокрутки. Було: 242px назавжди (шапка `fixed`). Стало: 0.
// ⚠️ Перша редакція цієї перевірки міряла саме висоту («≤130px») — і завалила б
// правильне рішення: шапка з заголовком усередині вища за 130, але при цьому
// їде геть цілком. Мірка мусить описувати НАСЛІДОК, а не спосіб його досягти.
const післяПрокрутки = await p2.evaluate(async () => {
  const скролер = (() => {
    let n = document.querySelector('#disc-content .bd-body') || document.querySelector('.bd-body');
    while (n && n !== document.body) { if (n.scrollHeight > n.clientHeight + 10) return n; n = n.parentElement; }
    return document.scrollingElement || document.documentElement;
  })();
  const назва = () => document.querySelector('#disc-content .qa-hero-title')?.getBoundingClientRect().top ?? null;
  const до = назва();
  скролер.scrollTop = 600;
  await new Promise(r => setTimeout(r, 450));
  const шапка = document.querySelector('#disc-content .bd-controls');
  const r = шапка ? шапка.getBoundingClientRect() : null;
  const верхЗони = (document.querySelector('.app-header')?.getBoundingClientRect().bottom) ?? 0;
  return {
    до: до === null ? null : Math.round(до),
    після: назва() === null ? null : Math.round(назва()),
    // Скільки шапки видно нижче шапки застосунку — тобто скільки вона реально їсть.
    видноШапки: r ? Math.max(0, Math.round(r.bottom - верхЗони)) : null,
  };
});
ok('11в. 🔴 при прокрутці шапка НЕ їсть екран (було 242px назавжди)',
   післяПрокрутки.видноШапки !== null && післяПрокрутки.видноШапки <= 8,
   `видно ${післяПрокрутки.видноШапки}px`);
ok('11г. заголовок і пояснення їдуть геть разом із нею',
   післяПрокрутки.до !== null && післяПрокрутки.після !== null &&
   післяПрокрутки.після < післяПрокрутки.до - 100,
   `було ${післяПрокрутки.до}px → стало ${післяПрокрутки.після}px`);

// ── 12. «МЕНЕ ТЕЖ ЦІКАВИТЬ» — ПУБЛІЧНА ДІЯ З ВИДИМИМ НАСЛІДКОМ (23.08) ───────
//
// 🔑 ЩО САМЕ ТУТ ДОВОДИТЬСЯ І ЧОМУ ЦЕ НЕ «ПЕРЕВІРКА КЛАСІВ».
// До 23.08 кнопка міняла рівно одне — заливку серця. Тобто єдиним свідченням
// «спрацювало» був колір іконки, а обіцянка «питання не загубиться» не
// виконувалась узагалі: у «Збережених» воно не зʼявлялось.
//
// Тому міряємо ТРИ наслідки, які бачить людина, і жодного разу не питаємо
// «чи є клас»:
//   а) до тапу кнопка кличе («Мене теж цікавить») і закладка порожня;
//   б) після тапу кнопка КАЖЕ СЛОВАМИ, що сталось («Цікавить» + «Збережено»);
//   в) закладка в ШАПЦІ заповнилась тієї ж миті — саме це й знімає закид
//      «дві дії раптом синхронізувалися десь у бекенді».
//
// 🛑 Контроль: на `origin/main` мусять упасти 12б, 12в і 12д — там кнопка
// в обох станах підписана однаково, закладка не рухається, а попит у списку
// не показується взагалі.

// Спершу — попит у СПИСКУ (екран питання ще не відкритий).
const попитУСписку = await p.evaluate(() => {
  const рядки = [...document.querySelectorAll('#disc-content .qa-row')];
  const взяти = (id) => {
    const r = рядки.find(x => x.dataset.postId === String(id));
    return r ? (r.querySelector('.qa-row-n')?.textContent || '').replace(/\s+/g, ' ').trim() : null;
  };
  return { трьох: взяти(702), одного: взяти(701) };
});
ok('12а. 🔴 попит ВИДНО в списку, коли людей більше однієї',
   !!попитУСписку.трьох && /3 чекають/.test(попитУСписку.трьох),
   `702: «${попитУСписку.трьох}»`);
ok('12б. 🛑 один голос попитом НЕ вважається (це майже завжди сам автор)',
   !!попитУСписку.одного && !/чекают/.test(попитУСписку.одного),
   `701: «${попитУСписку.одного}»`);

// Відкриваємо питання, якого я ще не позначав.
await p.evaluate(() => {
  document.querySelector('#disc-content .qa-row[data-post-id="702"]')?.click();
});
await p.waitForTimeout(700);

const зчитати = () => p.evaluate(() => {
  const b = document.querySelector('.qa-screen .qa-interest');
  const з = document.querySelector('.qa-screen [data-save-id]');
  const заповнена = (el) => !!el && (
    el.classList.contains('bd-bookmark--active') ||
    (el.querySelector('svg')?.getAttribute('fill') || 'none') !== 'none');
  return {
    підпис: b ? b.textContent.replace(/\s+/g, ' ').trim() : null,
    натиснута: b ? b.getAttribute('aria-pressed') : null,
    aria: b ? (b.getAttribute('aria-label') || '') : null,
    закладка: заповнена(з),
  };
});

const доТапу = await зчитати();
ok('12в. до тапу кнопка КЛИЧЕ, а закладка порожня',
   доТапу.підпис?.includes('Мене теж цікавить') && доТапу.натиснута === 'false' && !доТапу.закладка,
   `«${доТапу.підпис}» · закладка=${доТапу.закладка}`);

await p.evaluate(() => document.querySelector('.qa-screen .qa-interest')?.click());
await p.waitForTimeout(600);
const післяТапу = await зчитати();

ok('12г. 🔴 після тапу кнопка каже СЛОВАМИ, що позначку поставлено',
   післяТапу.підпис?.includes('Цікавить') && !післяТапу.підпис?.includes('Мене теж') &&
   післяТапу.натиснута === 'true',
   `«${післяТапу.підпис}»`);
ok('12д. 🔴 і що питання ЗБЕРЕЖЕНО — обіцянка «не загубиться» виконана',
   /Збережено/.test(післяТапу.підпис || ''),
   `«${післяТапу.підпис}»`);
ok('12е. 🔴 закладка в ШАПЦІ заповнилась тієї ж миті (дія не прихована)',
   післяТапу.закладка === true,
   `було ${доТапу.закладка} → стало ${післяТапу.закладка}`);
ok('12ж. число попиту зросло на мій голос',
   /4/.test(післяТапу.підпис || ''),
   `«${післяТапу.підпис}»`);
ok('12з. читач екрана чує те саме, що видно очима',
   /[Цц]ікавить/.test(післяТапу.aria || '') && /[Зз]бережено/.test(післяТапу.aria || ''),
   `aria: «${післяТапу.aria}»`);

// 🛑 АСИМЕТРІЯ (рішення Вови): зняття «Цікавить» НЕ прибирає із «Збережених».
// Це і є те, що дає двом механікам незалежність — і саме тому воно перевіряється
// окремо: «тогл в обидва боки» виглядав би на екрані так само правильно, і без
// цієї перевірки різницю не спіймав би ніхто.
await p.evaluate(() => document.querySelector('.qa-screen .qa-interest')?.click());
await p.waitForTimeout(600);
const післяЗняття = await зчитати();
ok('12и. 🛑 зняв «Цікавить» — питання лишилось у «Збережених»',
   післяЗняття.підпис?.includes('Мене теж цікавить') && післяЗняття.закладка === true,
   `«${післяЗняття.підпис}» · закладка=${післяЗняття.закладка}`);

// 🛑 ЗАКРИВАЄМО ЕКРАН ЗА СОБОЮ. `openChatModal()` має ранній вихід
// (`if (_chatModalEl) return;`), тож лишений відкритим екран мовчки НЕ ДАСТЬ
// відкрити наступне питання — і перевірка нижче міряла б не те, що думає.
// Саме на це вона й спіймалась при першому прогоні: шукала відповіді питання
// 701, а на екрані стояло 702.
await p.goBack();
await p.waitForTimeout(500);

// ── 13. ІЄРАРХІЯ КАРТКИ — ЧОТИРИ РІВНІ, А НЕ ДВА (23.08) ────────────────────
//
// 🔑 ЩО САМЕ ДОВОДИТЬСЯ. Скарга Вови була «зливається», і корінь виявився не в
// порядку рядків, а у ВАЗІ: під питанням стояли три рядки 15px одного кольору.
// Тому міряємо не «чи є класи», а обчислені розміри й вагу — тобто рівно те,
// від чого залежить, чи чіпляється око.
//
// 🛑 Контроль на `origin/main` мусить завалити 13а-13г: там метарядок 15px,
// стоїть ДРУГИМ згори, лічильник має вагу 400, а імʼя автора відповіді — 600.
const ієрархія = await p.evaluate(() => {
  const row = document.querySelector('#disc-content .qa-row[data-post-id="701"]');
  if (!row) return null;
  const g = (sel) => {
    const el = row.querySelector(sel);
    if (!el) return null;
    const cs = getComputedStyle(el);
    return {
      px: Math.round(parseFloat(cs.fontSize)),
      вага: Number(cs.fontWeight),
      верх: Math.round(el.getBoundingClientRect().top),
    };
  };
  const ava = row.querySelector('.qa-card-ava .bd-avatar');
  return {
    питання: g('.qa-card-q'),
    стан:    g('.qa-row-n'),
    цитата:  g('.qa-row-answer-text'),
    автор:   g('.qa-row-answer-who'),
    довідка: g('.qa-card-meta'),
    аватар:  ava ? Math.round(ava.getBoundingClientRect().width) : null,
  };
});

ok('13а. 🔴 питання домінує над усім у картці',
   !!ієрархія?.питання && ієрархія.питання.px >= 19 && ієрархія.питання.вага >= 600,
   `${ієрархія?.питання?.px}px / ${ієрархія?.питання?.вага}`);
ok('13б. 🔴 «скільки відповідей» тепер ВАЖИТЬ (було 400 — найтихіше в картці)',
   !!ієрархія?.стан && ієрархія.стан.вага >= 600,
   `${ієрархія?.стан?.px}px / ${ієрархія?.стан?.вага}`);
ok('13в. 🔴 «хто і коли» стало довідкою: дрібніше за відповідь',
   !!ієрархія?.довідка && !!ієрархія?.цитата &&
   ієрархія.довідка.px < ієрархія.цитата.px,
   `довідка ${ієрархія?.довідка?.px}px проти відповіді ${ієрархія?.цитата?.px}px`);
ok('13г. 🔴 і поїхало ВНИЗ — під відповідь, а не між питанням і нею',
   !!ієрархія?.довідка && !!ієрархія?.цитата &&
   ієрархія.довідка.верх > ієрархія.цитата.верх,
   `відповідь ${ієрархія?.цитата?.верх}px → довідка ${ієрархія?.довідка?.верх}px`);
ok('13д. 🛑 автор відповіді ТИХІШИЙ за саму відповідь (було навпаки: 600)',
   !!ієрархія?.автор && !!ієрархія?.цитата &&
   ієрархія.автор.вага <= ієрархія.цитата.вага,
   `автор ${ієрархія?.автор?.вага} проти тексту ${ієрархія?.цитата?.вага}`);
ok('13е. 🛑 аватар автора питання — 20px, а не типові 36px `.bd-avatar`',
   ієрархія?.аватар !== null && ієрархія.аватар >= 18 && ієрархія.аватар <= 22,
   `${ієрархія?.аватар}px`);

// 🔴 ПАСТКА ФОРМАТУ «текст — Автор», заради якої рядок і зроблено flex.
// При звичайному `text-overflow: ellipsis` на всьому абзаці першим обрізається
// ХВІСТ — тобто саме імʼя. Тоді новий порядок ховав би те, що мав показати.
// Перевіряємо наслідком: подовжуємо текст відповіді просто в DOM і дивимось,
// чи автор лишився на екрані цілим.
const приДовгомуТексті = await p.evaluate(() => {
  const row = document.querySelector('#disc-content .qa-row[data-post-id="701"]');
  const txt = row?.querySelector('.qa-row-answer-text');
  const who = row?.querySelector('.qa-row-answer-who');
  if (!txt || !who) return null;
  txt.textContent = 'Начебто 24 серпня, але точно скажуть у сільраді ближче до кінця місяця, бо ще не затвердили кошторис і чекають на рішення';
  const r = who.getBoundingClientRect();
  const rowR = row.getBoundingClientRect();
  return {
    видно: Math.round(r.width),
    цілком: who.scrollWidth <= who.clientWidth + 1,
    уМежах: r.right <= rowR.right + 1,
    текст: who.textContent.replace(/\s+/g, ' ').trim(),
  };
});
ok('13ж. 🔴 автор видно НАВІТЬ при довгій відповіді (обрізається текст, не імʼя)',
   !!приДовгомуТексті && приДовгомуТексті.видно > 20 &&
   приДовгомуТексті.цілком && приДовгомуТексті.уМежах,
   `«${приДовгомуТексті?.текст || '—'}» ширина ${приДовгомуТексті?.видно}px`);

// 🔴 І ГОЛОВНЕ ПРО ІМʼЯ — ЙОГО НЕ РІЖЕ КРАПКАМИ, ЙОГО СКОРОЧУЄ ЗМІСТ.
// Знімок картки показав те, чого не показали числа: «— Дмитро Сел…». Стеля
// ширини (`max-width: 42%`) обрізала прізвище посеред слова І заразом віддавала
// половину рядка тихому підпису, через що текст відповіді обривався зарано.
// Лікується не шириною, а вмістом: у цитаті стоїть ЛИШЕ ІМʼЯ.
ok('13з. 🛑 у цитаті стоїть імʼя БЕЗ прізвища — і воно ціле, без крапок',
   !!приДовгомуТексті &&
   /^—\s*Оксана$/.test(приДовгомуТексті.текст) && !/…/.test(приДовгомуТексті.текст),
   `«${приДовгомуТексті?.текст || '—'}»`);

// 🔑 І скорочення ВИБІРКОВЕ, а не глобальне: у самому питанні місця вдосталь,
// там імʼя мусить лишатись повним. Інакше вийшло б, що ми тихо перейменували
// людину по всьому застосунку заради одного тісного рядка.
await p.evaluate(() => {
  document.querySelector('#disc-content .qa-row[data-post-id="701"]')?.click();
});
await p.waitForTimeout(700);
const уПитанні = await p.evaluate(() => {
  const el = document.querySelector('.qa-screen .qa-answer-name');
  return el ? el.textContent.replace(/\s+/g, ' ').trim() : null;
});
ok('13и. 🛑 а на ЕКРАНІ питання імʼя лишається повним (скорочення лише в картці)',
   уПитанні === 'Віктор Пилипчук',
   `«${уПитанні}»`);
await p.goBack();
await p.waitForTimeout(400);

// ── 14. ПОСИЛАННЯ ЗІ СПОВІЩЕННЯ ВЕДЕ ДО САМОЇ ВІДПОВІДІ (крок А4, 23.08) ────
//
// 🔑 ЩО ДОВОДИМО. `comment_id` лежав у payload push із 16.08 і не
// використовувався: людина тапала «Марія відповіла на ваше питання», потрапляла
// в питання — і далі шукала репліку очима. Тепер посилання має хвіст `?c=<id>`
// (той самий, яким уже живе «Стрічка»), і відповідь підсвічується.
//
// Міряємо НАСЛІДОК, а не наявність коду: переходимо за справжнім посиланням і
// дивимось, чи стоїть підсвітка на ПОТРІБНІЙ репліці, а не на першій-ліпшій.

// Спершу переконаємось, що екран закритий (див. пастку в блоці 12).
await p.evaluate(() => document.querySelector('.qa-screen') && history.back());
await p.waitForTimeout(500);

const заПосиланням = await p.evaluate(async () => {
  location.hash = '#/post/disc/701?c=5002';
  await new Promise(r => setTimeout(r, 1400));
  const підсвічені = [...document.querySelectorAll('.qa-answer--focus')];
  return {
    екранЄ: !!document.querySelector('.qa-screen'),
    скільки: підсвічені.length,
    наКому: підсвічені[0]?.dataset.msg || null,
    текст: підсвічені[0]?.querySelector('.qa-answer-text')?.textContent.trim() || '',
  };
});
ok('14а. посилання зі сповіщення відкриває саме питання',
   заПосиланням.екранЄ, `екран=${заПосиланням.екранЄ}`);
ok('14б. 🔴 підсвічено РІВНО ту відповідь, про яку сповіщення',
   заПосиланням.скільки === 1 && заПосиланням.наКому === '5002',
   `підсвічених ${заПосиланням.скільки}, це #${заПосиланням.наКому} — «${заПосиланням.текст}»`);

// 🛑 ЗВЕДЕНЕ СПОВІЩЕННЯ («5 нових відповідей») ЯКОРЯ НЕ МАЄ.
// Підсвітити там довелось би навмання — і людина вирішила б, що решта вже
// прочитані. `?c=all` означає «просто відкрий».
await p.evaluate(() => history.back());
await p.waitForTimeout(500);
const зведене = await p.evaluate(async () => {
  location.hash = '#/post/disc/701?c=all';
  await new Promise(r => setTimeout(r, 1200));
  return {
    екранЄ: !!document.querySelector('.qa-screen'),
    підсвічених: document.querySelectorAll('.qa-answer--focus').length,
  };
});
ok('14в. 🛑 зведене «N нових» відкриває питання, але НЕ підсвічує навмання',
   зведене.екранЄ && зведене.підсвічених === 0,
   `екран=${зведене.екранЄ}, підсвічених=${зведене.підсвічених}`);

// 🔴 ХОЛОДНИЙ СТАРТ — головний сценарій цієї фічі і найризикованіший.
// Тап по сповіщенню зазвичай піднімає застосунок з нуля, і відповіді в цей
// момент ще їдуть із бази. Якби ми шукали якір лише раз, фіча мовчки не
// працювала б саме тоді, коли потрібна, а на теплому застосунку працювала б —
// тобто виглядала б як «іноді не спрацьовує». Відтворюємо: вантажимо сторінку
// ОДРАЗУ з хвостом у посиланні.
await p.goto(url + '#/post/disc/701?c=5001', { waitUntil: 'domcontentloaded' });
await p.waitForTimeout(1200);
await p.evaluate(() => document.querySelector('.consent-accept')?.click());
// ⚠️ ЧЕКАЄМО ПОЯВИ, А НЕ СТАНУ ЧЕРЕЗ N СЕКУНД. Підсвітка живе 2.4с (це підказка,
// а не стан), а холодний старт із заставкою довший і плаваючий — фіксований
// `waitForTimeout` міряв би момент, коли вона вже ЗАКОННО знялась, і валив би
// робочий код через раз.
// 🔬 Перша редакція цієї перевірки падала з іншої причини, і вона повчальна:
// шукала відповідь `5003`, якої у фікстурі просто немає (взято з чернетки, де
// коментарів було три). Тобто брехала САМА перевірка — той випадок, про який
// у проєкті домовлено: якщо стенд завалив те, що працює, перший підозрюваний
// він, а не код.
// 🔑 Беремо КОРЕНЕВУ відповідь (5001), тоді як теплий сценарій вище бере
// ВКЛАДЕНУ (5002) — так обидва рівні вкладеності перевірені.
// 🔑 Ловимо селектором Playwright, а НЕ власним циклом усередині сторінки.
// Підсвітка живе 2.4с, а холодний старт плаває (заставка показується не завжди —
// на повторному візиті згоди вже не питають, і застосунок піднімається швидше).
// Власний поллінг стартував після фіксованої паузи і встигав розминутись із
// вікном; `waitForSelector` опитує з першої мілісекунди.
const наКому = await p.waitForSelector('.qa-answer--focus', { timeout: 12000 })
  .then(el => el.evaluate(e => e.dataset.msg))
  .catch(() => null);
const холодний = { екранЄ: !!(await p.$('.qa-screen')), наКому };
ok('14г. 🔴 працює і на ХОЛОДНОМУ старті (дані ще їхали, коли екран малювався)',
   холодний.екранЄ && холодний.наКому === '5001',
   `екран=${холодний.екранЄ}, підсвічено #${холодний.наКому}`);

// ── 15. ПОСИЛАННЯ НА ВКЛАДКУ ЦІЛКОМ (тип 4, 24.08) ─────────────────────────
//
// 🔑 ЩО ДОВОДИМО І НАВІЩО. Сповіщення «у громаді питання без відповіді» буває
// двох виглядів: одне питання → веде в НЬОГО, кілька → каже число і мусить
// вести У ВКЛАДКУ. Доти deep-link умів лише перше, тож зведене сповіщення не
// мало куди вести чесно: відкривши одне з пʼяти, воно збрехало б про решту.
// Це те саме правило, що дало `?c=all` у блоці 14 — **тап веде туди, що обіцяє
// текст**.
//
// Міряємо НАСЛІДОК: чи справді змінилась активна вкладка, а не чи є в коді
// потрібний рядок.

// Виходимо з екрана питання і стаємо на ІНШУ вкладку — інакше перевірка була б
// порожньою: `switchTab` на активній вкладці має ранній вихід, і «спрацювало»
// не відрізнити від «нічого не зробило».
await p.evaluate(() => { if (document.querySelector('.qa-screen')) history.back(); });
await p.waitForTimeout(500);
await p.evaluate(() => window.switchTab('community'));
await p.waitForTimeout(400);

const теплийПерехід = await p.evaluate(async () => {
  const до = document.querySelector('.app-main')?.dataset.tab;
  location.hash = '#/tab/disc';
  await new Promise(r => setTimeout(r, 800));
  return {
    до,
    після: document.querySelector('.app-main')?.dataset.tab,
    хвіст: location.hash,
  };
});
ok('15а. посилання #/tab/disc перемикає на вкладку «Питання»',
   теплийПерехід.до === 'community' && теплийПерехід.після === 'discussions',
   `${теплийПерехід.до} → ${теплийПерехід.після}`);

// 🛑 ХЕШ МУСИТЬ ПРИБИРАТИСЬ ЗА СОБОЮ. Якби він лишався, наступний «назад» знову
// кинув би людину на цю вкладку — рівно та вада, яку 15.08 ловили в
// `handleThreadHash` (чат відкривався вдруге).
ok('15б. 🛑 хвіст прибрано за собою — «назад» не кине сюди вдруге',
   теплийПерехід.хвіст === '',
   `хеш після переходу: «${теплийПерехід.хвіст}»`);

// ⚠️ НЕВІДОМА НАЗВА НЕ ЛАМАЄ НІЧОГО. Сповіщення, надіслане новішою версією
// функції, старіший застосунок мусить пережити мовчки: вкладка лишається та, що
// була, помилки немає.
const невідома = await p.evaluate(async () => {
  const до = document.querySelector('.app-main')?.dataset.tab;
  location.hash = '#/tab/цьогонемає';
  await new Promise(r => setTimeout(r, 600));
  return { до, після: document.querySelector('.app-main')?.dataset.tab };
});
ok('15в. ⚠️ невідома назва вкладки ігнорується, а не валить застосунок',
   невідома.до === невідома.після && невідома.після === 'discussions',
   `${невідома.до} → ${невідома.після}`);

// 🔴 ХОЛОДНИЙ СТАРТ — головний сценарій, як і в блоці 14: тап по сповіщенню
// зазвичай піднімає застосунок з нуля.
await p.goto(url + '#/tab/disc', { waitUntil: 'domcontentloaded' });
await p.waitForTimeout(1200);
await p.evaluate(() => document.querySelector('.consent-accept')?.click());
const холодніВкладки = await p.waitForFunction(
  () => document.querySelector('.app-main')?.dataset.tab === 'discussions',
  { timeout: 12000 },
).then(() => true).catch(() => false);
ok('15г. 🔴 працює і на ХОЛОДНОМУ старті (застосунок піднімався з нуля)',
   холодніВкладки,
   `вкладка=${await p.evaluate(() => document.querySelector('.app-main')?.dataset.tab)}`);

await stop();
await b.close();
done();

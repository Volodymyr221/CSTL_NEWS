-- 0001_contact_column_lockdown.sql — Б1: ТЕЛЕФОНИ З ДОШКИ БІЛЬШЕ НЕ ЇДУТЬ УСІМ.
-- Автор: аудит 24.09.2026. Накочує Вова через Supabase → SQL Editor.
--
-- 🔴 ЦЕ ЄДИНА МІГРАЦІЯ З ЧЕРГИ, ЯКА ВИМАГАЄ ЗМІНИ КЛІЄНТА ПЕРЕД НАКАТОМ.
--    Порядок обовʼязковий і описаний у кінці файлу. Накат «просто так», без
--    задеплоєного клієнта, ЗЛАМАЄ Дошку для гостя.
--
-- ── ЩО НЕ ТАК (заміряно від ролі `anon` 24.09) ──────────────────────────────
--     set role anon; select count(*), count(contact) from posts;  →  12 і 6
-- Тобто шість телефонів жителів качає будь-хто, кому вистачило публічного
-- ключа з `bundle.js`. Не відкриваючи жодного оголошення, одним запитом.
-- Частина А (`scripts/supabase_post_contact.sql`) уже на проді: є RPC
-- `get_post_contact()` із рейт-лімітом, є кнопка «Показати номер» у клієнті
-- (`src/tabs/board.js`, задеплоєна). Лишилось закрити сам стовпчик.
--
-- ── ЧОМУ ВІТРИНА, А НЕ КОЛОНКОВИЙ REVOKE ───────────────────────────────────
-- 🔑 Напрошується `revoke select (contact) on posts from anon`. Не годиться з
-- двох причин, і обидві заміряні, не уявні:
--   1. GRANT діє на РОЛЬ цілком. Відрізавши `contact` у `anon`, ми відрізаємо
--      його і в кожному `select('*')` — а PostgREST на заборонену колонку
--      відповідає помилкою на ВЕСЬ запит. Гість побачив би порожню Дошку.
--   2. Обійти це можна лише переліком колонок у клієнті — а в цьому проєкті
--      стоїть протилежне правило, і воно оплачене поломкою:
--      `src/core/supabase.js` ~1108 описує, як перелік колонок із неіснуючим
--      `author_name` мовчки вимкнув секцію оголошень у картці профілю, і що
--      фікстура `tests/_board-fixture.mjs` ігнорує `.select()`, тобто СТЕНД
--      ЦЬОГО НЕ ЛОВИТЬ. Повертати перелік колонок — повертати той самий клас
--      тихої поломки.
-- ➡️ Вітрина обидві проблеми знімає: клієнт і далі пише `select('*')`, просто
--    до обʼєкта, в якому `contact` немає.
--
-- ⚠️ RLS ХОВАЄ РЯДКИ, А НЕ КОЛОНКИ. Це і є коренева причина, чому тут потрібен
-- окремий обʼєкт, а не ще одна політика.

-- ── 1. ВІТРИНА ──────────────────────────────────────────────────────────────
create or replace view public.posts_public as
  select
    p.id, p.type, p.category, p.title, p.text, p.price, p.color, p.location,
    p.photos, p.author, p.owner_uid, p.status, p.published_at, p.created_at,
    p.bumped_at, p.deleted_at,
    -- ⚠️ ЧИ Є номер, а не сам номер. Без цього поля клієнт не відрізнив би
    -- «телефон є, попроси» від «телефону немає взагалі» — і кнопка «Показати
    -- номер» висіла б на оголошеннях, за якою порожнеча.
    (nullif(btrim(coalesce(p.contact, '')), '') is not null) as has_contact
  from public.posts p
  where p.deleted_at is null
    and p.status = 'published';

comment on view public.posts_public is
  'Вітрина Дошки без колонки contact (міграція 0001). Свідомо обходить RLS: сама
   таблиця posts більше не читається публічно, бо RLS ховає рядки, а не колонки.
   Той самий розкрій, що ads → ads_public.';

grant select on public.posts_public to anon, authenticated;

-- ── 2. УЧАСНИК РОЗМОВИ ──────────────────────────────────────────────────────
-- 🔴 ПАСТКА, ЯКУ ЗАКРИВАЄМО ТИМ САМИМ КРОКОМ. Список розмов тягне оголошення
-- вкладено, з `contact` серед колонок (`src/core/supabase.js` ~1351). Звузивши
-- читання `posts`, ми лишили б ПОКУПЦЯ без картки оголошення, про яке він же й
-- листується. Ця політика віддає йому рядок цілком, разом із номером: людина
-- вже в розмові саме про це оголошення, це не масове вигрібання.
drop policy if exists "thread party reads its post" on public.posts;
create policy "thread party reads its post" on public.posts
  for select using (
    exists (
      select 1 from public.threads t
      where t.post_id = posts.id
        and (t.author_uid = auth.uid() or t.buyer_uid = auth.uid())
    )
  );

-- ── 3. ЗВУЗИТИ ЧИТАННЯ САМОЇ ТАБЛИЦІ ────────────────────────────────────────
-- 🔴 УВАГА НА НАЗВУ. У старих планах політика називалась
-- «Public can read published posts» — ТАКОЇ НА ПРОДІ НЕМАЄ. Є одна політика
-- читання, `posts read`, у яку три старі давно зведені:
--     using (((deleted_at is null) and post_visible_row(status, owner_uid)) or is_admin())
-- а `post_visible_row()` це `status='published' or owner=auth.uid() or is_admin()`.
-- Хто піде «знімати публічну політику» за старою назвою — не знайде її,
-- вирішить, що вже зроблено, і лишить діру відкритою.
-- ➡️ Тому тут не `drop`, а ПЕРЕПИСУВАННЯ: лишаємо власника й адміна, знімаємо
--    «будь-хто бачить опубліковане» (це тепер робота вітрини).
drop policy if exists "posts read" on public.posts;
create policy "posts read" on public.posts
  for select using (
    (deleted_at is null and owner_uid = auth.uid()) or public.is_admin()
  );

-- ── ПОРЯДОК НАКАТУ (порушення = зламана Дошка) ──────────────────────────────
-- 1. КЛІЄНТ ПЕРШИМ. Перевести на `posts_public` читання, де телефон не потрібен:
--      fetchPublishedPosts   (src/core/supabase.js ~146)
--      fetchPostById         (~163)
--      fetchAuthorAds        (~1125)
--    `fetchMyPosts` (~1143) ЛИШАЄТЬСЯ на `posts`: людині показують її власні
--    чернетки й відхилені, яких у вітрині немає за означенням.
--    Вкладене читання в розмовах (~1351) теж лишається на `posts` — його
--    покриває політика учасника розмови вище.
-- 2. Задеплоїти клієнта і ПЕРЕВІРИТИ ЖИВЦЕМ на телефоні.
-- 3. Лише тоді накотити цей файл.
--
-- ── ПЕРЕВІРКА ПІСЛЯ НАКАТУ ──────────────────────────────────────────────────
--   set local role anon;
--   select count(*) from public.posts;                    -- очікуємо 0
--   select count(*), count(*) filter (where has_contact)
--     from public.posts_public;                           -- очікуємо 12 і 6
-- 🛑 І ОБОВʼЯЗКОВО живцем: відкрити список розмов з акаунта ПОКУПЦЯ (не
-- власника оголошення) — назва оголошення має лишитись на місці.

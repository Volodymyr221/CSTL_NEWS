-- scripts/supabase_analytics_rate_limit.sql
-- ДЗЕРКАЛО ПРОДА: рейт-ліміт вставки подій аналітики. Знято 24.09.2026.
--
-- 🔴 ЧОМУ ЦЕЙ ФАЙЛ ЗʼЯВИВСЯ ЛИШЕ ЗАРАЗ — І ЧОМУ ЦЕ ПОВЧАЛЬНО.
-- Аудит 24.09, читаючи ТІЛЬКИ файли, записав знахідку: «`analytics_events`:
-- анонімний INSERT без жодного ліміту, один скрипт наливає мільйони рядків».
-- Підстава виглядала міцною: `supabase_analytics.sql:31` справді має
-- `with check (true)`, а `supabase_anon_lockdown.sql:161` сам себе позначає
-- «⬜ Ліміт частоти… зараз CHECK = true, тобто кількість подій не обмежена».
--
-- ➡️ Звірка з живою базою показала, що ліміт Є. Він стоїть не в політиці, а
--    ТРИГЕРОМ — і тому його не видно жодним грепом по `with check`.
--
-- 🔑 УРОК КЛАСУ, вартий більше за саму знахідку: **відсутність правила у файлі
--    не доводить відсутності правила в базі.** Політика, грант і тригер — три
--    різні місця, де живе захист, і читач файлів бачить лише перше. Саме тому
--    поруч тепер лежить `dump_prod_mirror.sql`, який показує всі три.
--
-- ⚠️ ЦЕ ДЗЕРКАЛО, А НЕ МІГРАЦІЯ. На проді все це вже стоїть.

-- create or replace function public.analytics_rate_limit()
--   returns trigger language plpgsql security definer set search_path to 'public'
-- as $$
-- declare
--   v_recent integer;
-- begin
--   select count(*) into v_recent
--     from public.analytics_events
--    where visitor_id = new.visitor_id
--      and created_at > now() - interval '1 minute';
--
--   if v_recent >= 120 then
--     -- Тихо відкидаємо, а не кидаємо помилку: аналітика не має ламати застосунок
--     -- людині, у якої щось зациклилось. Метрика — не критичний шлях.
--     return null;
--   end if;
--
--   return new;
-- end;
-- $$;
--
-- create trigger analytics_events_rate_limit before insert
--   on public.analytics_events for each row execute function analytics_rate_limit();

-- ── ЩО ЦЕЙ ЛІМІТ ЗАКРИВАЄ, А ЩО НІ ──────────────────────────────────────────
--
-- ✅ Закриває: зациклений клієнт і груба спроба залити події з одного
--    `visitor_id`. 120 подій за хвилину — стеля, вище якої рядки тихо зникають.
--
-- 🛑 НЕ закриває — і це чинна знахідка аудиту (В5):
--    1. **Ретеншен.** `rollup_content_views()` прибирає лише `content_open` і
--       `content_seen` старші за 90 днів (`supabase_content_views_rollup.sql:90`).
--       Решта типів подій не видаляється НІДЕ. Живий замір 24.09: 16 378 рядків
--       і 6 МБ на 15 профілів — це ≈1090 рядків і 400 КБ на людину. На 5000
--       жителів виходить ≈2 ГБ при ліміті бази 500 МБ, з яких 210 МБ уже зайнято.
--    2. **Ліміт на `visitor_id`, а не на пристрій чи IP.** `visitor_id` приходить
--       із клієнта (`getAnonId()`, `src/core/supabase.js:380`) і лежить у
--       `localStorage`. Той, хто хоче лити навмисне, просто міняє його щоразу —
--       і кожен новий id отримує свої 120 на хвилину. Тобто це захист від
--       ВИПАДКОВОСТІ (зациклився код), а не від НАМІРУ.
--
-- ➡️ Обидва пункти лікуються окремою міграцією: ретеншен на всі типи подій
--    (90 днів, для дрібних 30) після згортання в добові підсумки. Саме там, а
--    не тут — цей файл нічого не змінює, він лише фіксує, що вже є.

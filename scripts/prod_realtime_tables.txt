# Дзеркало: які таблиці віддають realtime-події на проді.
# Знято 24.09.2026, оновлено 24.09 після накату 0005. Запит:
#   select tablename from pg_publication_tables where pubname='supabase_realtime';
#
# 🔴 НАВІЩО ФАЙЛ. Підписка в коді і публікація в базі — дві половини однієї
# фічі, і половина в базі невидима з репозиторію. Саме через це `subscribePosts`
# прожив невідомо скільки, не отримавши жодної події: підписка була, публікації
# не було, а симптом збігався зі станом «фічі ще немає».
# Стенд `tests/realtime-publication.mjs` звіряє цей файл із кодом.
#
# ⚠️ ДВА СПИСКИ НАВМИСНО. Верхній — те, що на проді ЗАРАЗ (звірено запитом).
# Нижній — те, що чекає накату міграції. Змішати їх в один означало б написати
# у дзеркалі неправду про живу базу, а дзеркало, якому не можна вірити, гірше
# за його відсутність.
# ➡️ ПІСЛЯ НАКАТУ міграції рядок переносять із нижнього списку у верхній.
chat_group_messages
comments
messages
page_comment_reactions
page_comments
page_posts
page_reactions
posts
reactions
threads

# --- чекає накату ---
# (порожньо — 0005 накатано 24.09, `posts` переїхав у верхній список)

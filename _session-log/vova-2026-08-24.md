# Журнал — 24.08.2026 (акаунт 1, гілка `claude/startuem-gqj5ae`)

> 🔑 **Це журнал ДНЯ, а не сесії.** Сама сесія почалась 23.08 і її повний живий
> журнал — `_session-log/vova-2026-08-23c.md` (там усе по кроках: концепція
> «Питань», потоки А і Б, заміри, інциденти, обидва фініші). Тут — зріз того, що
> сталось саме 24.08, щоб наступний чат не мусив шукати по датах.
>
> ⚠️ **Того ж дня паралельно працював ДРУГИЙ акаунт Вови** — гілка
> `claude/startuem-bn0us5`, журнали `vova-2026-08-23b.md`. Його PR: **#1001-#1003**
> (гніздо імені · пара фото жителя · капсула автобуса), **#1005** (видалення
> коментаря не працювало взагалі), **#1006** (персональне переїхало з пристрою
> в акаунт). Усе це вже в `main`.

---

## Що зроблено 24.08 на цій гілці

### 1. Потік Б — B-33: вимикачі сповіщень стали робочими (PR #1004)
Таблиця `notif_prefs` у базі (RLS «лише свої»), кабінет пише в базу з відкатом,
чотири Edge Functions читають її перед надсиланням. Набір тем перероблено:
**Автобуси · Питання · Дошка · Стрічка**; «Світло» і «Новини» прибрані — таких
push не існує взагалі, тобто тумблер під них був вадою з фальшивим
підтвердженням. Деплой **#4981** success.

### 2. Вхід через Google (той самий PR)
Скарга Вови: «мене зразу закидає через пошту, якою я авторизовувався останній
раз». Причина не наша: `signInWithOAuth` кликався без параметрів, а Google при
одній активній сесії пропускає екран вибору. Фікс — `prompt: 'select_account'`.
Ціна названа до правки: для жителя з одним акаунтом це +1 екран при кожному вході.

### 3. Тип 4 — «у громаді питання без відповіді» (PR #1007)
Останній, шостий крок MVP вкладки «Питання». Був заблокований доти, доки
вимикачі були декоративні: сповіщення, на яке людина не підписувалась і не може
вимкнути, — це спам за визначенням.

- нова Edge Function `send-unanswered-push` (v2, ACTIVE, **`verify_jwt` УВІМКНЕНО**);
- `scripts/supabase_unanswered_push.sql` — `qa_unanswered_push_log` (RLS без
  політик), обгортка `notify_unanswered_questions()`, розклад `pg_cron`
  `qa-unanswered-push` `0 8,9 * * *`;
- `src/app.js` — новий маршрут `#/tab/<назва>`;
- `tests/questions.mjs` — блок 15, 79 → **83**.

🔑 **Розклад дзвонить двічі, а працює раз** — `pg_cron` живе в UTC і про літній
час не знає (11:00 Києва = 08:00 UTC влітку, 09:00 взимку). Годину звіряє сама
функція. 🔑 **Секрет у текст розкладу не вписаний** — обгортка читає його з
`app_secrets` у мить виклику.

🔴 **Знайдена дірка:** вести у ВКЛАДКУ не було чим — deep-link умів лише
`#/post/<де>/<id>`, тож зведене «5 питань чекають» довелось би вести в одне з
пʼяти і збрехати про решту. Через це й заведено `#/tab/`.

Деплой **#4999** success. `CACHE_NAME` = `cstl-20260824-1740`.

---

## Інциденти дня

1. **Девʼять конфліктів злиття** з паралельним акаунтом перед PR #1007. Розібрано
   по одному, `-X theirs`/`-X ours` не вживались. Найважливіше: **літопис
   `SESSION_STATE_VOVA.md` — єдине місце, де «вибрати» означало б ЗАГУБИТИ**, бо
   там у кожної сесії свої розділи; зведено руками по межах заголовків.
2. **Сторож документації завалив фініш тричі** — пульс 220 рядків при стелі 200 і
   18.3 KB при стелі 16. Стиснуто прозу про чужі й вчорашні роботи (їхній повний
   текст у літописі). Далі падала перевірка «пульс не відстав», бо генератор
   крутили ДО коміта злиття. Правильний порядок: коміт → генератор → коміт.
3. **`deno` у контейнері не встановлено** — перевірку стилю серверного коду цього
   разу прогнати не вдалось. Синтаксис підтверджений самим деплоєм і живими
   викликами.
4. **Живі дані рухаються під ногами:** перший замір відповідачів дав три людини,
   другий (через півгодини) — чотири. Не помилка — між замірами зʼявились нові
   відповіді. Фінальні числа беру з останнього заміру.

---

## Заміри 24.08 (на живій базі, не з памʼяті)

- сухий прогін типу 4: HTTP 200, план — питання 74, **3 людини**; автор питання
  зі свого ж списку випав; питання 63 і 67 теж без відповідей, але старші 7 діб —
  «протухли» і не потрапили;
- межа «раз на 3 доби»: заглушки в журналі → `nobody to ask`; заглушки видалено;
- воротар часу: справжній виклик о 20:24 Києва → `not the hour`, `kyiv_hour:20`
  (при 17:24 UTC — тобто пояс справді застосовується, а не лишається UTC);
- ланцюг розкладу: `select public.notify_unanswered_questions();` → HTTP 200;
- стенди: `questions.mjs` **83/83**, контроль `origin/main` **79/83**;
  `home-caps` 135/135 · `account-scope` 10/10 · `account-cabinet` 10/10 ·
  `verified-badge` 37/37 · `docs-fresh` 20/20 · `docs-refs` 10/10.

---

## 🛑 Чесні межі

- **справжнього надсилання типу 4 ще не було** — найближче спрацювання об 11:00
  за Києвом. Доведено лише: відбір, воротар часу, межа 3 діб, ланцюг розкладу;
- **стеля «5 push із Питань на добу» (§12) у коді відсутня** — її немає для
  жодного типу. Тип 4 тримає себе межею «раз на 3 доби». Записано, не забуто;
- B-33 **чекає живої перевірки Вови** — доки не перевірив, не закриваю.

---

## Передача (`/onovy`, 24.08 18:16 UTC)

Робота обох акаунтів за сьогодні **вся в `main`**. Гілка `bn0us5` відстала на мій
тип 4 — якщо та сесія продовжить, хай зробить `git merge origin/main` ДО роботи,
інакше впіймає ті самі девʼять конфліктів.

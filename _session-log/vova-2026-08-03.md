# Журнал сесії — 2026-08-03 (гілка `claude/startuem-u9b752`)

> Живий журнал. Дописується ПО ХОДУ після кожної значущої дії, не з пам'яті наприкінці.
> Формат рядка: що зробив (файл/функція) · навіщо · що НЕ спрацювало · слова Вови дослівно ·
> PR# · деплой# · CACHE_NAME.

---

## Крок 0 — старт сесії (`/startuem`)

- `git config user.name "Вова"` + `user.email vitocorleone191@outlook.com` — авторство комітів.
- `git fetch origin` — пройшов (хук на старті сесії скаржився, що fetch не вдався; повторний
  ручний fetch спрацював, підтягнув гілки `vova/*`).
- Звірка з `origin/main` (HOT_RULES №11):
  - `git log --oneline origin/main ^HEAD | grep -v "auto("` → **порожньо**
  - `git log --oneline HEAD ^origin/main` → **порожньо**
  - Тобто гілка сесії стоїть рівно на `origin/main` = `c81f744e` (PR #756, хендовер).
- Останній прод-PR коду: **#755** (`58b6d484`), `CACHE_NAME = cstl-20260803-0841`.
- `_ai-tools/BYYOU_PLAN.md` — статус **`done`** (закрито 02.08, PR #747). Незавершеного
  потоку `/byyou` нема, пересідати на іншу гілку не треба.

## Крок 1-2 — прочитано

`NEW_SESSION_PROMPT.md` · `HOT_RULES.md` (11 правил) · `START_HERE.md` ·
`SESSION_STATE_VOVA.md` (сесія U-7, блоки 10-12) · `AUDIT_2026-07.md` (шапка + відкриті 🔴) ·
`CSTL_BUGS.md` · `CLAUDE.md` · `ВОВА_ПРОФІЛЬ.md`.

## Знайдені розбіжності в документації (доповів Вові у першій відповіді)

1. **За 02-03.08 (сесія U-7, десять блоків роботи) журналу в `_session-log/` НЕМА** —
   останній файл `vova-2026-08-01.md`. HOT_RULES №11 п.2 вимагає пушити журнал кожні
   кілька кроків. Тобто сама вимога журналу минулою сесією не виконана; хендовер
   тримається лише на `SESSION_STATE_VOVA.md`.
2. Дрібне: `SESSION_STATE_VOVA.md` (блок 12) пише стенди **604/604**, `START_HERE.md`
   і `CLAUDE.md` — **605**. Розбіжність на 1 перевірку, не критично.

## Стан

Чекаю «Роби» від Вови. Коду не чіпав.

---

## /byyou — редизайн «Громади» як Home-екрана

**Замовлення Вови** (дослівно, ключове): «Потрібно повністю переосмислити та зробити
редизайн сторінки "Громада". Не роби косметичні зміни… Вона має стати персональною
домашньою сторінкою користувача… "Це моя громада. Тут зібрано все, що важливо саме
для мене."» + чотири вимоги: новини як точка входу, блок «Актуальні збори», бекап
до змін, не ламати бізнес-логіку.

### Фаза 0-1 (до брами старту)
- Прочитано: `community.js` (439), `community-blocks.js` (1289), `news.js`,
  `news-hub.js`, шапка `index.html`. Розміри: `style/community.css` — 5655 рядків.
- `npm install` (node_modules у пісочниці не було) + `node build.js` — зелено,
  `bundle.js` не змінився.
- Написано інструмент виміру `tests/tools/community-audit.mjs`, знято 4 скріншоти.
- **Вимір 390×844 (видима зона 731px):** hero 560px/76.6% · greeting 412 · новини
  430 (починаються на **662px** = нижче першого екрана) · дошка 215 · події 247 ·
  автобус 199 · погода 78 (стан «Завантаження…», зовнішній API з пісочниці
  недоступний) · контакти 420/57.5%. **Уся сторінка 2446px = 3.3 екрана.**
- ⚠️ Хибне припущення, зняте перевіркою: думав, що погода дублюється (шапка + блок).
  Звірив `index.html:27-28` — погоду з шапки прибрали 08.07, тобто вгорі її НЕМА.
  Отже погода в шапці-привітанні = повернення, не дубль.
- Попередній `BYYOU_PLAN.md` (Дошка, `done`) заархівовано в
  `_archive/BYYOU_PLAN_2026-08-02-doshka-done.md`; заведено новий план на 15 кроків,
  статус `active`, точка цілісності після кроку 11.
- Три питання на брамі: джерело даних для зборів (JSON у git vs Supabase) ·
  доля «ШО В СЕЛІ?» · згода на фото 560→200px.

**Стан:** чекаю «ок» на брамі старту. Коду не змінював. Пуш заблоковано замком
`/byyou` до слова «деплой» — коміти поки локальні.

### Потік /byyou — кроки 1-15 виконано (чекаю «деплой»)

Коміти (усі локальні, замок `/byyou`): `d0476a5e` бекап+аудит · `906d6d5e`
дизайн-система · `613728ce` каркас · `5ad7b27f` погода · `d03bfb38` смуга ЗАРАЗ ·
`5337abc9` збори · `80ccc619` новини · `900958da` події+оголошення · `74286095`
телефони · `b77b0ad0` стани · `12069db6` доступність+стенд · `ebb30d97` смоук.

**Рішення Вови по ходу:** збори — варіант **А** (`data/fundraisers.json` у git).
На два інші питання відповіді не було; вирішив сам і сказав уголос: «ШО В СЕЛІ?»
лишив (зняв лише липкість), фото 560→200px зробив.

**Заміряно:** 2446→1788px · новини з 662→399px · контакти 420→195px ·
вкладені скролери 1→0 · автоміни 4→1 · тап-цілі 40→44px · контраст 4.31→4.83:1.

**Що НЕ спрацювало і чому (для brain):**
1. Стенд `community-home.mjs`: контроль «з активним збором блок зʼявляється» давав
   0px на робочому коді — запити другої вкладки йшли через **Service Worker**, а
   `page.route` їх не перехоплює. Лікується `serviceWorkers: 'block'` у контексті.
   **Восьмий випадок брехливої перевірки в проєкті.**
2. Смоук падав на 5 кроках із 8 — дві спроби латання поспіль, потім стоп і
   діагноз: у застосунку **три** різні механізми закриття (стаття — власна
   `#article-modal` + `closeArticleModal()`; аркуші — `.app-modal-close`; шари —
   тільки «назад»). Escape не закриває нічого з них. **Девʼятий випадок.**
3. Хибна тривога: думав, що знайшов витік замка прокрутки після сторінки
   оголошення. Перевірив окремо — `page.goBack()` відпускає замок правильно, а
   не відпускав `history.back()` зсередини `evaluate`. Продукт справний, баг був
   у мірці. **Не завів фальшивий B-XX.**
4. `check-imports.js` двічі зловив зворотну лапку в HTML-коментарі всередині
   шаблонного рядка — власний сторож проєкту, спрацював як треба.

**Стенди:** 37/37 · 621 перевірка. Смоук головного: 8/8 переходів, помилок нема.
`CACHE_NAME = cstl-20260803-1052`.

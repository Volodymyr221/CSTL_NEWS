# Журнал сесії — Вова · 2026-08-10 (сесія U-20)

**Гілка:** `claude/startuem-1rgetk` · **Production:** `main`
**Стан на старті:** `HEAD == origin/main` (звірено двома командами, обидва списки порожні).
`CACHE_NAME = cstl-20260810-2340` · останній коміт `461b923c` (PR #886).

---

## Крок 0 — старт сесії (`/startuem`)

- `git config user.name "Вова"` + `user.email vitocorleone191@outlook.com` — виконано.
- `git fetch origin` — синхронно, розбіжностей із `origin/main` немає.
- Прочитано: `HOT_RULES.md` · `NEW_SESSION_PROMPT.md` · `START_HERE.md` ·
  `SESSION_STATE_VOVA.md` (хендовер) · `AUDIT_2026-07.md` · `BACKLOG_VOVA_2026-08.md` ·
  `CSTL_BUGS.md` · `CLAUDE.md` · `ВОВА_ПРОФІЛЬ.md` · `_ai-tools/BYYOU_PLAN.md`.
- Заведено цей журнал (тека одна — коренева `_session-log/`).

**Знайдена розбіжність (доповів Вові у першій відповіді):**
за 10.08 працювала сесія U-19 (9 деплоїв, PR #878…#886), але журналу
`_session-log/vova-2026-08-10.md` у репозиторії **не було** — останній
`vova-2026-08-09c.md`. Тобто рівно той випадок, від якого застерігає HOT_RULES №11.
Цей файл і є журнал 10.08 (новий, від сесії U-20).

**Стан `/byyou`:** `BYYOU_PLAN.md` = `paused`, брама старту, потік
«Бургер: меню згортається саме, блюр лишається». 13 кроків, жоден не виконано.

---

## Хід сесії

_(дописується після кожної значущої дії)_

### [/byyou 1/13] Прилад «привид тапу» — заміряно

- Вова сказав «Так» на брамі старту → `BYYOU_PLAN.md` статус `active`, гілка поправлена на `claude/startuem-1rgetk`.
- `npm install` + `node build.js` — `bundle.js` не змінився (зібране збігається з `src/`).
- Прилади: `tests/tools/_ghost-probe.mjs` (що під пальцем по кадрах) і `tests/tools/_overlap.mjs` (перекриття бургера і ✕).
- **Заміряно:** під точкою бургера `351×28` у перші 0-25мс лежить ЗАТЕМНЕННЯ, а з ~150мс — **`#sidebar-close`**. Панель доїжджає на 266мс.
- 🔴 **Знахідка поза планом:** хрестик меню перекриває бургер на **1235px² = 86%** його площі (бургер `332,9 · 38×38`, ✕ `332,14 · 44×44`). Тобто вікон, у яких привид-тап закриває меню, **ДВА**, а план бачив одне — це й пояснює, чому два попередні заходи не допомогли.
- ⚠️ Прилад збрехав першою редакцією двічі (міряв `#splash`; умова доїзду записана не в той бік) — виправлено до того, як числа пішли у висновок.
- **Рішення:** крок 2 переписано — замість «затемнення не приймає тап 350мс» одне правило «поки панель не доїхала, меню не закриває ніщо».

### [/byyou 2/13] Фікс А — «привид тапу» більше не закриває меню

- `src/core/sidebar.js`: додано `panelArriving()` + `closeByUser()`; хрестик і затемнення тепер ходять через сторож, бургер і Escape — ні (обґрунтування в коментарі коду).
- **Дві умови, і обидві знадобились.** Сама геометрія («панель доїхала») пропускала привид на 250мс: крива `cubic-bezier(0.22,1,0.36,1)` підводить панель на 1px до місця вже на ~230мс, коли анімація ще йде. Додано тривалість, **прочитану з обчисленого CSS** (`transitionDuration`), а не вписану числом — третьої копії `0.28s` у проєкті не заводимо.
- `CEILING_MS = 600` — це запобіжник, не механізм: якби панель застрягла на півдорозі, правило «не доїхала — не закриваємо» замкнуло б людину в меню назавжди.
- **Заміряно приладом до/після** (привид через N мс → меню лишилось відкритим?):
  - ДО: `0мс ❌ · 150мс ❌ · 250мс ❌ · 400мс ❌`
  - ПІСЛЯ: `0 ✅ · 30 ✅ · 80 ✅ · 150 ✅ · 200 ✅ · 250 ✅ · 280 ❌ · 400 ❌ · 700 ❌`
  - Тобто межа лягла рівно на кінець анімації; після появи меню закриття хрестиком працює як раніше — і це правильно, тап по готовому меню це рішення людини, а не привид.
- Регресу немає: `sidebar-overlay` 34/34 · `sidebar-menu` 41/41 · `sidebar-account` 11/11.
- ⚠️ `CACHE_NAME` НЕ чіпав — за планом він піднімається один раз на кроці 11, пушу до «деплой» немає.

### [/byyou 3/13] Фікс Б — розмиття існує лише у відкритому стані

- `style/sidebar.css`: `backdrop-filter` перенесено з базового правила в `.sidebar-overlay--show`; у закритому стані — `none`, тобто фільтра НЕМА і застигати нема чому.
- **Не просто перенесено, а з затримкою на зникнення** (`0s linear 0.25s`, той самий прийом, що вже вимикає `visibility` у цьому файлі): інакше при закритті розмиття зникало б ривком посеред плавного згасання затемнення. Пряма вимога Вови «не поламай плавність».
- **Заміряно:** закрито → `none`; на 60мс після відкриття → вже `blur(3px)`; під час згасання блюр тримався до **275мс** (тобто весь час, поки його видно), проміжних кадрів згасання **11/30**; після — `none`.
- Сторожі зелені: 34/34 · 41/41 · 11/11.

🔎 **ЗНАХІДКА ПОБІЧНА (не чіпав, доповідаю Вові).** У живому застосунку затемнення при закритті НЕ згасає — воно зникає миттєво. Причина: `paintState()` знімає клас `sidebar-overlay--show` і **в тому ж виклику** кличе `syncOverlay()`, а той ховає шар, якщо класу немає. Умова «згасання вже закінчилось» перевіряється через рядок після того, як клас зняли, тож вона істинна ЗАВЖДИ. Панель при цьому їде 0.28с. Стенд №55 цього не бачить, бо міряє згасання, знімаючи клас напряму, без `paintState`. **Це стан ДО моїх змін**, не регрес — і за HOT_RULES №9 сам не чіпаю.

### [/byyou 4/13] Фікс В — запобіжник знімає і розмиття

- `style/sidebar.css`, правило `body:not(:has(.sidebar--open)) .sidebar-overlay`: додано `backdrop-filter: none !important` (+ `-webkit-`). До цього запобіжник лікував половину — у залиплому стані клас `--show` лишався, а з ним і фільтр, тобто найдорожче переживало і меню, і сам запобіжник.
- 🛑 **`display: none` з плану СВІДОМО не додано.** Правило стає чинним у ту саму мить, коли з панелі знімається `.sidebar--open`, тобто НА ПОЧАТКУ згасання. `display` плавного переходу не має → затемнення зникало б ривком при кожному звичайному закритті. А потреби нема: `visibility: hidden` знімає малювання, `backdrop-filter: none` — причину компонування; у спокійному закритому стані `display: none` і так ставить атрибут `hidden` із `syncOverlay()`.
- **Заміряно залиплий стан** (клас показу є, меню закрите): `filter=none · opacity=0 · visibility=hidden · тапи не перехоплює`.
- ⚠️ Прилад збрехав ВДРУГЕ за сесію: перша редакція показала `blur(3px)` у залиплому стані — бо я зняв клас лише із затемнення, а на панелі `.sidebar--open` лишився, тож для `:has()` меню було ВІДКРИТЕ і запобіжник законно не діяв. Виправлено: спершу чесне закриття, потім імітація залипання.
- Сторожі зелені: 34/34 · 41/41 · 11/11.

### [/byyou 4б/13] Підвал бургер-меню статичний, як шапка (замовлення Вови зі знімка)

Дослівно: «низ… треба відділити так само як шапку зверху. Тобто воно має бути статично, а скролитись тільки ця частина».

- **Корінь:** блок соцмереж дописувався в кінець `.sidebar-nav`, тобто ВСЕРЕДИНУ скролера, і притискався донизу через `margin-top: auto`. Поки пунктів мало — виглядало як підвал; щойно список переростав панель, `margin-top: auto` ставав нулем, підвал робився звичайним останнім блоком і їхав зі списком, а «Політика і приватність» ховалась за ним. Саме це на знімку.
- **Зроблено:** новий вузол `#sidebar-foot` у `index.html` (брат шапки і списку) · `renderNav()` пише соцмережі туди · `.sidebar-foot` у CSS дзеркалить шапку (`flex-shrink: 0`, `border-top` тим самим токеном `--news-line`, власне тло) · запас під домашню смужку iPhone переїхав із `.sidebar-nav` у підвал · `margin-top: auto` прибрано.
- **Запасний шлях:** якщо вузла підвалу нема (стара розмітка в кеші PWA — `index.html` і `bundle.js` доїжджають нарізно, урок 09.08), соцмережі кладуться в кінець списку, як було.
- **📐 Заміряно (390×844):** шапка `0-71` · список `71-759` (прокрутка: вміст 820 при вікні 688) · підвал `759-844`, висота **85px**. Соцмереж у підвалі **2**, у списку **0**. «Політика і приватність» після прокрутки донизу має нижній край **750** проти верху підвалу **759** — тобто видима повністю, а не під ним (до фікса — 882, за межею).
- Сторожі зелені: 34/34 · 41/41 · 11/11.

### [/byyou 5,6,6б/13] Сторожі + обовʼязкові контрольні прогони

**Новий стенд `tests/sidebar-ghost-tap.mjs` (17 перевірок)** — привид тапу + розмиття:
- привид через 0 / 80 / 150 / 250мс не закриває меню;
- 🛡 контроль «не перестаралися»: свідомий тап по ✕ і по затемненню ПІСЛЯ доїзду меню закриває (без цієї пари попередній блок доводив би лише те, що меню взагалі не закривається);
- 🛡 запобіжник стелі: при штучно застряглій панелі (анімація 5с) закриття відновлюється після `CEILING_MS`;
- розмиття: закрито → `none`, відкрито → `blur(3px)`, тримається весь час згасання, зникає в кінці; згасання плавне (13 проміжних кадрів із 31);
- залиплий стан: фільтр знято, шар не видно, тапи проходять.
- **КОНТРОЛЬ `BUNDLE_REV=origin/main CSS_REV=origin/main` → 12/17.** Падають рівно: привид 0мс, привид 250мс, «закрито: розмиття нема», «зникає, коли зникло затемнення», «ЗАЛИПЛО: розмиття знято».
- ⚠️ **Записав чесно в шапку стенда:** на старому коді 80мс і 150мс лишаються ЗЕЛЕНИМИ, тобто несучі перевірки — 0 і 250. Не викидаю їх (прилад ловив хрестик і на 150мс, попадання залежить від кадру), але й доказом не вважаю.

**`tests/sidebar-menu.mjs` — +3 перевірки на підвал** (41 → 44): підвал не зрушив при прокрутці списку (`0px`); останній пункт видно повністю (низ `750` проти верху підвалу `759`); соцмережі рівно в одному місці (підвал 2 · список 0).
- **КОНТРОЛЬ трьома змінними → 41/44**, падають рівно ці три.

⚠️ Прилад збрехав ВТРЕТЄ за сесію: перевірка стелі червоніла на СПРАВНОМУ коді, бо розтягнуті 5с діяли й на ЗАКРИТТЯ — меню було закрите станом, а панель ще їхала за екран, і геометрія чесно казала «видно». Ламалась мірка, не код. Лікування: сповільнення знімається одразу після тапу. Урок записаний у самому стенді.

### [/byyou 7,10/13] Плавність не змінилась + аудит змінених ділянок

**Крок 7 — відбиток руху до/після** (`tests/tools/_motion-cmp.mjs`, лишив у репозиторії як загальний прилад):
- 🔴 **Спершу заміряв ШУМ** (правило проєкту №1 про мірку): `origin/main` дав доїзд панелі `262 · 271 · 276 · 310`мс, поточний — `258 · 275 · 287 · 290`. Діапазони перекриваються, тобто розбіжність це тремтіння кадрів, а не зміна. Без цього заміру числа нічого не доводили б.
- **Твердий доказ — сам опис руху.** Панель: `transform 0.28s cubic-bezier(0.22,1,0.36,1)` + затримка видимості `0.28s` — байт-у-байт як було. Затемнення: `opacity 0.25s ease` без змін; додались ЛИШЕ рядки `backdrop-filter` з тривалістю `0s`. Жодне наявне число руху не чіпав.

**Крок 10 — аудит:**
- `node --check` по всіх змінених `.js` + `node build.js` — чисто, `check-imports` 54 файли без проблем.
- Дублів немає: `panelArriving` / `openAnimMs` / `closeByUser` — по одному оголошенню; обробник `.sb-social-btn` вішається рівно один раз (через `ціль`).
- Баланс `<div>` у розмітці меню: 3 відкритих / 3 закритих.
- **Прибрано власне сміття:** правило `.sb-social-foot` лишилось було з єдиним `padding: 0` (мертве після переїзду) — видалив цілком, пояснення перенесено в коментар `.sidebar-foot`. Обгортка в розмітці лишилась: вона групує підпис і ряд кнопок.
- **Прибрано разові прилади**, які повністю поглинули сторожі: `_ghost-probe.mjs`, `_overlap.mjs`, `_blur-probe.mjs`, `_foot-probe.mjs`. Лишив `_motion-cmp.mjs` — він загальний («чи не зламав я плавність»), таке в проєкті знадобиться ще.
- Після прибирання перепрогнав: `sidebar-menu` 44/44 · `sidebar-ghost-tap` 17/17.

### [/byyou 8,9,11/13] Повний прогін, смоук, CACHE_NAME + документи

**Крок 8 — `npm test`: 64/67 стендів, 1134/1139 перевірок.** Три червоні розібрані ПООДИНЦІ, з доказом чиї вони (окреме робоче дерево на чистому `origin/main`):

| стенд | вирок |
|---|---|
| `docs-refs` 8/10 | **МІЙ** — стендів стало 67, а документи писали 66. Виправлено. |
| `press-feedback` 7/8 | **МІЙ РЕГРЕС**, на `origin/main` 8/8. Розібрано нижче. |
| `weather-day` 44/46 | **НЕ мій** — на `origin/main` те саме 44/46. Не чіпав. |

🔴 **`press-feedback` — стенд спіймав СПРАВЖНЮ зміну поведінки, не флак.** Він тисне бургер (меню відкривається), а прибираючи за собою клікає ✕ через **150мс** — рівно у вікно, де мій сторож тап по ✕ свідомо ігнорує. Меню лишалось відкритим, накривало шапку, і наступна кнопка («Збережене») мірялась крізь панель → «не реагує» на робочому коді.
- **Лікував стенд, а не сторож** — бо поведінка саме така, як задумано: людина не встигне натиснути бургер і за 150мс свідомо тицьнути ✕, це підпис привида.
- ⚠️ Одного лише очікування доїзду геометрії ВИЯВИЛОСЬ МАЛО (сторож тримається ще й на тривалості з CSS) — тому прибирання не вгадує момент, а **добивається результату**: тисне ✕, доки меню справді не закриється. Причина записана в самому стенді.
- Після правки: **8/8 і на моїй гілці, і на `origin/main`** — тобто стенд не «підігнаний під зелене», він однаково чесний на обох.

**Крок 9 — браузерний смоук** (`tests/tools/_smoke.mjs`): меню відкривається · підвал видно і він не перекриває останній пункт · переходи «Стрічка/Дошка/Автобуси» працюють і меню за собою закривається · соцмережі в підвалі на місці · помилок консолі немає.
- ⚠️ Прилад збрехав ВЧЕТВЕРТЕ: два кроки «впали» на робочому коді, бо після переходу на Дошку її **гейт правил** (`dismissible: false`) накриває екран і перехоплює тапи, включно з бургером. Урок про це вже записаний у `CLAUDE.md` — спрацював удруге. Приймаю гейт, як людина. Заразом відсіяв «помилки консолі» від навмисно заглушеної зовнішньої мережі — це не помилки застосунку.

**Крок 11:** `CACHE_NAME` → `cstl-20260810-2123`; лічильник стендів у `CLAUDE.md` і `START_HERE.md` 66 → **67 / 1139** (`docs-refs` знову 10/10).
- 🔎 **Заразом виправив неправду в `START_HERE.md`:** там стояло «`npm test` — усе зелене, у т.ч. `weather-day`». Насправді `weather-day` дає 44/46 і падає так само на чистому `origin/main`. Рядок «усе зелене» знімає обережність рівно так, як обіцянка неіснуючого сторожа в HOT_RULES №9.

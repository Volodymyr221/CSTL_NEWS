# Журнал сесії — Вова, 2026-07-22

> Продовження великої сесії 21–22.07 (табло новин + обговорення). Тут — фініш-батч 22.07.

## Задача A — новини: чому «Україна та світ» показували плашку «Читати повністю» + фікс

- **Скрін Вови (IMG_3476/3477):** статті «Україна та світ» досі показували плашку «Джерело надає лише анонс через RSS — Читати повністю». «Нам такого не треба! Нам потрібен повний текст. Скажи, чому так сталося, якщо ми це виправляли?»
- **Причина:** статті спаршені РАНІШЕ (перші налаштування парсера), ДО появи логіки дотягування повного тексту → зберігся лише короткий анонс (<500 симв.), без прапорця `fullText`. Парсер дедуплікує за URL → вже бачені посилання пропускає → старі «анонси» ніколи не перепарсовувались. Клієнтська умова плашки: `!exclusive && sourceUrl && !fullText && text<600` → усі збіглись.
- **Фікс (системний, `scripts/parse_rss.py`, коміт `c231071d` прямо в main):**
  - `rehydrate_short_articles(existing_articles)`: на КОЖНОМУ прогоні доганяє повний текст для збережених «анонсів» на місці (той самий `fetch_full_article`/`fetch_rayon_article` що й для нових). Лічильник спроб `_fullTries` (макс 3) + бюджет 40 запитів/прогін — щоб не смикати нескінченно джерела без повного тексту.
  - offline-бекфіл прапорця `fullText=True` для 44 старих статей що вже мали текст ≥500 симв., але без прапорця → плашка на них зникла одразу (без мережі), закомічено в `data/articles.json`.
  - save в `main()` тепер спрацьовує і коли нових статей нема, але re-hydrate щось змінив (`if new_articles or rehydrated`).
- **Обмеження середовища:** пісочниця не має зовнішнього інтернету (egress policy) → мережеві дотяги роблю НЕ я, а CI (workflow rss-parser). Вова запускає workflow вручну (workflow_dispatch через API дає 403).
- **Статус:** parser-фікс + offline-бекфіл задеплоєно (прямий коміт у main). Решта ~28 коротких статей дотягнуться на наступному прогоні парсера в CI.

## Задача B — обговорення: видалені повідомлення повністю приховані (як Telegram)

- **Скріни Вови (IMG_3478 модалка / IMG_3479 картка):** тема де Вова написав і видалив повідомлення. У картці списку показувалось «Володимир:» + пустий текст і рахувалось «1 повідомлення»; у відкритому чаті — плашка «🗑 Повідомлення видалено», зверху «1 повідомлення». «Якщо я видалив повідомлення, то воно не має відображатися в карточці.»
- **Причина:** код рахував/показував повідомлення не відкидаючи `deleted_at` — скрізь `comments.length` і `slice(-2)`.
- **Рішення Вови (через AskUserQuestion):** «Прибрати повністю (як Telegram)» — видалене зникає скрізь, і з картки, і з чату.
- **Зміни (`src/tabs/board-discussions.js`):**
  - `activeComments(postId)` — хелпер: невидалені.
  - Картка `renderChatCard`: лічильник, прев'ю останніх 2, список учасників — з `activeComments`.
  - Модалка: лічильник шапки (`replyCount`, `updateChatHeaderCount`) — з `activeComments`. Стрічка `chatMessagesHtml` рендерить лише невидалені; плашку-слід `pm-bubble--deleted` прибрано; `byId` будуємо з УСІХ, щоб цитата-відповідь на видалене показувала «Видалене повідомлення».
  - Тема лише з видаленим повідомленням → чат показує «Поки порожньо. Напишіть перше повідомлення».
- ⏸️ Приватний чат (`board-chat.js`/`messages-ui.js`) НЕ чіпав — там плашка видаленого лишається (окрема фіча).

## Задача C — пошук: заокруглення як на Дошці + векторна лупа

- **Скріни Вови (IMG_3480 Обговорення / IMG_3481 Дошка):** заокругли краї б-блоку пошуку і самого пошуку в Обговореннях — до заокруглення як на Дошці. Емодзі-лупу 🔍 → векторну (в Обговореннях і Дошці).
- **Причина різниці:** «варіант C» (Задача 35, 21.07) зробив Обговорення гострими: `#disc-content .bd-controls` кути 0 + розділювач замість тіні; `#disc-content .bd-search` radius 6px. Дошка = базові `.bd-controls` (нижні кути 12px + тінь) + `.bd-search` 14px.
- **Зміни:**
  - `style/community.css`: прибрано override `#disc-content .bd-controls` (повернувся базовий вигляд Дошки — 12px + тінь); `#disc-content .bd-search` 6px→14px.
  - `src/tabs/board.js` `renderHeader`: `<span class="bd-search-icon">🔍</span>` → `${ICONS.search}` (спільний компонент → одразу Дошка + Обговорення).
  - `style/community.css` `.bd-search-icon`: inline-flex + `svg{1em}` + color для вирівнювання SVG.

## Технічне

- **Гілка:** `vova/disc-hide-deleted` (Задачі B+C). Задача A — прямий коміт у main (parser/data).
- **Коміти гілки:** `ed47bbc4` (deleted hidden) → `1456c2b5` (search rounding + vector icon).
- **Перевірка:** `node --check` board.js/board-discussions.js/bundle.js/sw.js OK; `node build.js` exit 0 (32 файли, check-imports чисто).
- **CACHE:** `cstl-20260721-2207` → `cstl-20260722-0547`.

## Задача D — новини: склеєні абзаци + ненадійне дотягування повного тексту (прямий коміт у main)

- **Скріни Вови (IMG_3482 Конкурент / IMG_3483 оригінал eurointegration / IMG_3484 наш UP):**
  1. Конкурент знову лише анонс + плашка «Читати повністю» — «ми це фіксили сто разів, треба надійно щоб ВСЯ парсила повний текст».
  2. Українська/Європейська правда — повний текст Є, але БЕЗ абзаців (стіна: «протоці.Про це»).
- **Причина 2 (абзаци):** RSS `content:encoded` деяких видань віддає тіло без меж `</p><p>` — усе в одному `<p>`. `_blocks_to_html` зберігав як є → стіна.
- **Фікс 2 (надійний, `scripts/parse_rss.py`):** `_split_runon_html()` / `_split_runon_paragraphs()` — розбиває `<p>` назад на абзаци за втраченою межею (кінець речення `.!?»"` впритул до великої кирилиці/лапки, БЕЗ пробілу — у нормальному тексті після крапки завжди пробіл, тож ловить лише склейки). Захист: сегмент <40 симв. або хвіст-скорочення (`вул.`/`м.`/...) → не межа. Застосовано в кінці `_blocks_to_html` (усі джерела, обидва шляхи) + фолбек `get_full_content`. Перевірено на реальних даних: pravda 1→6 абзаців (точно як оригінал), 0 хибних розбивок. Разова полагодка наявних (8 статей сумарно, з урахуванням бот-прогону).
- **Причина 1 (Конкурент без повного тексту):** `fetch_full_article` не знаходив тіло. Старий фолбек шукав контейнер лише на ВЕРХНЬОМУ рівні DOM, а тіло статті зазвичай вкладене.
- **Фікс 1:** фолбек тепер шукає тіло за масою тексту в `<p>` серед УСІХ (вкладених) контейнерів. + тимчасовий `DEBUG_FETCH` (env `CSTL_DEBUG_FETCH`, default on) — логи CI показують для кожного дотягу: статус/байти/який селектор чи фолбек спрацював → діагностика Конкурента.
- **⚠️ Конкурент не підтверджено:** пісочниця без egress → перевірити фетч можна ЛИШЕ в CI. Треба ручний прогін rss-parser → читаю логи DEBUG_FETCH → якщо блок (403) чи інша причина — доопрацьовую.
- **Коміт:** `c7496b8c`→(rebase на бот)→`908862f7` у main. Деплой стартував (абзаци — одразу; повний текст Конкурента — після прогону парсера).

## Задача E — «Стрічка»: узгодження концепту + Етап 0 (таб-бар)

- **Контекст:** Вова повернувся до затвердженого плану перебудови вкладки «Шо в селі» → стрічка сторінок-каналів громади (`CSTL NEWS VOVA/docs/SHOTAM_PAGES_PLAN.md`). Просив переказати концепт (переказав із доку, не з пам'яті) і чітко продумати ПОВНУ логіку («не ладка на ладці»).
- **Зафіксовані рішення (22.07):**
  1. Назва вкладки — **«Стрічка»** (правильно укр.; «лента» — калька з рос.). Внутрішній id `shotam` не чіпаємо.
  2. **Порядок таб-бару:** Стрічка · Обговорення · [Громада] · Дошка · Автобуси (зліва інфо, справа сервіси; Громада — центральна кнопка).
  3. UI/UX — сучасно (2020-х), не «Facebook 2006».
  4. Публікація — **як у FB, прямо зі сторінки** (не з кабінету редактора): власник/адмін пише пост від імені сторінки з підписом автора-людини. Кабінет Алли лишаємо лише для системної «Афіша громади».
  5. Ролі: сторінки+власників створює адмін вручну (v1); власник може призначати адмінів; право писати = власник/адмін цієї сторінки.
  6. Дзвіночок = простий перемикач push (без «вступу»). Лічильник підписників НЕ показуємо.
- **Модель даних (майбутні таблиці):** `pages`, `page_admins`, `page_posts`, `page_subscriptions` + реюз reactions/comments.
- **Етапи:** 0 таб-бар · 1 база+RLS · 2 стрічка(Екран1) · 3 сторінка(Екран2) · 4 композер · 5 push · 6 Афіша громади.
- **Етап 0 зроблено (гілка `vova/strichka-tabbar`):** `index.html` — переставлено кнопки таб-бару в новий порядок, `shotam` label «Шо в селі»→«Стрічка», іконку оновлено на feed-стиль. Перевірено: `switchTab` за id (не позицією), CSS таб-бару без nth-child → перестановка безпечна; «ШО В СЕЛІ?» на дашборді Громади (community.js:211) — окрема секція, НЕ чіпав. CACHE `cstl-20260722-0547`→`cstl-20260722-1046`.

## Задача F — «Стрічка» Етапи 1-2 (база даних + клієнт стрічки)

- **Етап 1 — база (SQL, `scripts/supabase_pages.sql`, прямий коміт main `cee675b0`+email-fix):** 6 таблиць `pages`/`page_admins`/`page_posts`/`page_comments`/`page_reactions`/`page_subscriptions` + `can_edit_page()` SECURITY DEFINER + RLS (усі читають; пишуть у сторінку лише власник/адмін; коментар/лайк реюз патерну; підписка своя) + realtime (page_posts/comments/reactions) + seed 2 тестові сторінки. Вова застосував (email власника `volodymyrshevchuk19@gmail.com` — виправив після того як я поставив session-email `vitocorleone191@outlook.com`). Результат: pages=2, page_admins=2. Ідемпотентний, не руйнівний.
- **Етап 2 — клієнт (гілка `vova/strichka-feed`, цим `/finish`):**
  - `src/tabs/feed.js` (НОВИЙ) — модуль Стрічки: кружечки-канали (stories), вертикальна стрічка постів усіх сторінок, картка (аватар+назва каналу+час, фото, текст, підпис автора «— Ім'я», лайк, коментарі); екран сторінки (Екран 2: банер+аватар+назва+дзвіночок+пости каналу); лайк (оптимістично+відкат), коментарі (нижній лист), дзвіночок (підписка), композер поста для власника/адміна (текст+фото через uploadPhotoToStorage).
  - `src/core/supabase.js` — дата-шар: fetchPages, fetchPagePosts (nested `pages(...)`), fetchPageReactions/setPageReaction, fetchPageComments/addPageComment, fetchMyEditablePageIds, createPagePost, deletePagePost, fetchMySubscriptions/setPageSubscription.
  - `index.html` — вкладка shotam: `.fd-head` «Стрічка» + `#feed-circles` + `#feed-list`.
  - `src/app.js` — `initFeed()` замість `initEvents()` (events.js лишається для Етапу 6).
  - `style/feed.css` (НОВИЙ) + `style.css` @import + `sw.js` precache + CACHE `cstl-20260722-1046`→`cstl-20260722-1446`.
  - Архітектура: окремі таблиці Стрічки (ізольовані від Дошки `posts`), не оверлоад; ключ реакції = uid або anonId; імена/аватари авторів — живі за uid (fetchAvatars); подвійне екранування liveName виправлено.
  - Реюз: uploadPhotoToStorage (bucket community-photos, папка pages/), get_avatars, patterns reactions/comments.
- **Демо:** Вова застосував seed 2 постів → «Зробив» → «Фініш».
- **⏸️ Лишилось:** Етап 5 (реальний push про нові пости — Edge Function send-page-push), Етап 6 (Афіша громади — events/holidays як системна сторінка). Push-дзвіночок поки лише зберігає підписку.
- **⚠️ Наживо не тестовано** (пісочниця без Supabase) — можливі дрібні візуальні баги, доводимо на місці.

## Задача G — «Стрічка»: кілька фото на пост (як FB/IG) + фікс зникання фото

- **Скрін Вови (IMG_3486):** екран сторінки «Відділ культури ОТГ» гарний, але опублікований пост з фото — БЕЗ фото (у картці нема фото-блоку → image_url порожній). Вимога: кілька фото на пост, «не відставати від FB/IG по UX/якості».
- **Причина зникання:** композер тихо ковтав помилку завантаження (`up.url` null → imageUrl null → пост без фото і без попередження).
- **Фікс + фіча (гілка `vova/strichka-photos`):**
  - БД: `page_posts.image_urls text[]` (кілька фото) + ідемпотентний ALTER для вже створеної таблиці; `image_url` лишається легасі (перше фото). Вова застосував `alter ... add column if not exists image_urls`.
  - `createPagePost(pageId, uid, text, imageUrls[])` — масив; `fetchPagePosts` тягне `image_urls`.
  - `feed.js`: композер — мультивибір (`<input multiple>`, MAX_PHOTOS=10), прев'ю-мініатюри що видаляються (×), паралельне завантаження, **показ помилок** (не ковтаємо). Галерея: 1 фото на всю ширину; 2+ свайп-карусель (scroll-snap) + крапки + лічильник «1/N» (`galleryHtml`/`wireGalleries`). Тап по фото → повноекранний перегляд зі свайпом (`openViewer`).
  - `style/feed.css`: `.fd-gallery`/`.fd-gal-*` карусель, `.fd-viewer*` перегляд, `.fd-comp-thumb*` мініатюри.
  - CACHE `cstl-20260722-1446`→`cstl-20260722-1511`.
- **⚠️ Наживо не тестовано** (пісочниця без Supabase). Тепер якщо фото не завантажиться — вискочить точна помилка (діагностика причини минулого фейлу).

## Задача H — навігація: не скидати вкладку на «Громаду» при згортанні/поверненні (весь додаток)

- **Скрін Вови (IMG_3487):** сидів в Обговореннях з відкритою модалкою, згорнув додаток (не закрив) → повернувся → модалка відкрита, але вкладка під нею стрибнула на «Громаду». «Скидати на головну тільки при закритті+відкритті або перезавантаженні. Це стосується ВСЬОГО додатку, не латка — фіксуємо повну логіку.»
- **Причина (корінь, `src/app.js`):** глобальний обробник `visibilitychange` → `if (visibilityState === 'visible') switchTab('community')` перекидав на Громаду щоразу при поверненні з фону — для БУДЬ-ЯКОЇ вкладки.
- **Фікс (весь додаток):** прибрано обробник повністю. Скидання на головну лишається лише при свіжому завантаженні/перезавантаженні (currentTab за замовч. = 'community', сторінка community видима стартово). boot.js `visibilitychange`/`pageshow` лише перевіряють оновлення Service Worker (вкладку не чіпають) — не займав.
- **Гілка `vova/keep-tab-on-resume`.** `node --check`+build чисто. CACHE `cstl-20260722-1738`.

## Задача I — «Стрічка»: перебудова вигляду (рівень FB/IG) + редагування сторінки + пошук

- **Запит Вови:** каркас ок, але не ідеал. Проаналізувати FB/IG (пости як картки чи ні), змінити кольори/фон/стиль, шапку («Стрічка» зліва висить одна — іконки справа), підписи кружечків (не влазить назва → 2 рядки), + власник не може поставити банер/аватар. «Максимально удосконалити, щоб не відчували що переходять з кращого на гірше.»
- **Аналіз (переказано Вові):** IG — пости edge-to-edge (на всю ширину, без картки), фото від краю до краю, чистий фон. FB — майже на всю ширину, білі блоки на світло-сірому. Наш поточний (вузькі кремові картки 18px+відступи) = «material 2018». Головна зміна — пости на всю ширину.
- **Макет:** відрендерив візуальний Artifact-макет (телефон, світла/темна тема) → Вова «Роби».
- **Реалізація (гілка `vova/strichka-redesign`):**
  - `style/feed.css` ПОВНІСТЮ переписано: окремі `--fd-*` токени (світло-сірий фон #ECEEF1 + білі пости, бордо акцент — не чіпають глоб. змінні); пости edge-to-edge (0 радіус, 0 бічних відступів, 8px сірий проміжок); фото на всю ширину; більші іконки дій (24px); шапка flex (назва + іконки); кружечки-підписи 2 рядки (`-webkit-line-clamp:2`). Фон вкладки `.app-main[data-tab="shotam"]` (прецедент — Автобуси).
  - `index.html`: шапка `.fd-head` = назва + кнопка пошуку; `.fd-search` рядок.
  - `src/tabs/feed.js`: пошук (feedSearch, фільтр у renderFeed за назвою/текстом, кнопка розгортає поле); редактор сторінки `openPageEditor` (банер+аватар+тема, upload, updatePage, оновлення кешу pages+постів, перемалювання); кнопки редагування (📷 на банері/аватарі + «Редагувати сторінку») лише canEdit; іконки IC_EDIT/IC_CAMERA.
  - `src/core/supabase.js`: `updatePage(pageId, patch)`.
  - `scripts/supabase_pages.sql`: RLS pages update → `is_admin() OR can_edit_page(id)` (власник/адмін редагує свою). Вова застосував `drop+create policy`.
  - CACHE `cstl-20260722-1738`→`cstl-20260722-1827`.
- **⏸️ Лишилось:** Етап 5 (push), Етап 6 (Афіша громади). Доведення вигляду наживо.

## Задача J — «Стрічка»: компактний верхній band

- **Скрін Вови (IMG_3489):** новий вигляд гарний, але верхній блок з іконками спільнот завеликий. Прибрати заголовок «Стрічка», вертикально зменшити блок, іконки горизонтально + горизонтальний скрол. (Розмір іконок «впринципі норм були» — за потреби повернемо.)
- **Зміни (гілка `vova/strichka-compact-top`):** `index.html` — прибрано `.fd-head` із заголовком; новий `.fd-topbar` = `#feed-circles` (flex:1) + кнопка пошуку в один ряд. `style/feed.css` — `.fd-topbar` sticky top:0; кружечки менші (кільце 62→52px, ширина 70→58, підпис 11→10.5px), паддінги менші → блок нижчий. Заголовок дублював підсвічену вкладку в таб-барі.
- CACHE `cstl-20260722-1827`→`cstl-20260722-1845`.

## Задача K — «Стрічка»: підписи кружечків + тінь на банер (гілка vova/strichka-circle-labels)

- **Скрін IMG_3490:** повернути розмір кружечків (компакт був замалий); у 2-му кружечку перенос по БУКВІ («Туристичн-а») — має по словах; у 1-му повна назва не влазить у 2 рядки.
- **Фікс підписів:** `style/feed.css` — розмір повернуто (кільце 52→62px, сторі 58→74px, шрифт 10.5→11px); підпис `word-break: normal; overflow-wrap: normal` (перенос ЛИШЕ по словах) + `-webkit-line-clamp: 3` → повна назва влазить, буква не переноситься.
- **Скрін IMG_3491 (редагування сторінки працює — банер+аватар+опис поставлені):** «зроби щоб тінь зі сторінки та напівкруглої аватарки падала на банер».
- **Фікс тіні:** `feed.js` — контент екрана сторінки обгорнуто в `.fd-screen-body`; `feed.css` — `.fd-screen-body` заокруглений верх 18px + `margin-top:-18px` (наїжджає на банер) + `box-shadow: 0 -7px 18px` (тінь угору на банер); аватарка `box-shadow: 0 2px 20px` (радіальна — вгору на банер і вниз на лист).
- **⚠️ Гілка vova/strichka-circle-labels НЕ була змерджена після Задачі J** (Вова перейшов до тіні замість «Фініш») → доклав тінь сюди, фінішимо все разом (3 правки).
- CACHE `cstl-20260722-1857`→`cstl-20260722-1905`.

## Задача L — «Стрічка»: тінь силуету верху+аватарки на банер (правильно)

- **Скрін IMG_3492:** попередня тінь неправильна — я підняв сірий фон і заокруглив верх (Вова цього не просив). Вимога: відкотити форму (пряма лінія + опукла аватарка), а тінь зробити «вирізану у формі самого верху разом з іконкою», щоб падала прямо на банер. Вова попросив СПЕРШУ детально описати план (описав) → «Роби».
- **Фікс (гілка `vova/strichka-banner-shadow`):**
  - Відкат: у `.fd-screen-body` прибрано `border-radius: 18px` + `margin-top: -18px` + box-shadow → верх контенту пряма лінія під банером, аватарка опукла (як було).
  - `feed.js`: список постів `.fd-screen-list` винесено ЗА `.fd-screen-body` (щоб `filter` не тягнув усю стрічку — перф).
  - `.fd-screen-body { filter: drop-shadow(0 -3px 5px rgba(0,0,0,0.34)); }` — обводить САМ силует блоку (пряма верхня лінія + коло аватарки, що виступає через `.fd-screen-id` margin-top -42) і кидає розмиту копію ВГОРУ на банер. Прибрано власну box-shadow аватарки (її силует уже в drop-shadow).
- **Урок:** не міняти форму/структуру, якщо просили лише тінь; спершу узгодити план коли власник незадоволений.
- CACHE `cstl-20260722-1905`→`cstl-20260722-1920`.

## Задача M — «Стрічка»: повернути ПЕРШИЙ варіант тіні на банер

- **Вова:** drop-shadow силует (Задача L) теж не те → «поверни самий перший варіант. Роби і фініш».
- «Перший варіант» = Задача K: контент-лист із заокругленим верхом що наїжджає на банер + тінь угору + власна тінь аватарки.
- **Відкат до K (гілка `vova/strichka-shadow-v1`):** `.fd-screen-body` → `border-radius: 18px 18px 0 0; margin-top: -18px; box-shadow: 0 -7px 18px rgba(0,0,0,0.20)` (прибрано filter drop-shadow); `.fd-screen-ava` → `box-shadow: 0 2px 20px rgba(0,0,0,0.32)`; список постів повернуто ВСЕРЕДИНУ `.fd-screen-body`.
- CACHE `cstl-20260722-1920`→`cstl-20260722-1930`.

## Задача N — «Стрічка»: повернути вигляд IMG_3491 (без тіні на банер узагалі)

- **Вова:** «Не цей варіант, а цей що на фото» + IMG_3491 — це стан ДО експериментів із тінню (чиста перебудова: пряма межа банер/контент, м'яка тінь під аватаркою, БЕЗ заокругленого піднятого листа й тіні на банер).
- **Відкат (гілка `vova/strichka-no-banner-shadow`):** `.fd-screen-body` — прибрано `border-radius`/`margin-top -18px`/`box-shadow` (лишився нейтральний wrapper: фон+z-index); `.fd-screen-ava` box-shadow повернено на `0 4px 14px rgba(0,0,0,0.18)` (як у первинній перебудові). Пряма межа під банером, опукла аватарка з м'якою тінню.
- Тінь на банер (K/L/K-знову) остаточно ВІДКИНУТО — Вова обрав первинний вигляд без неї.
- CACHE `cstl-20260722-1930`→`cstl-20260722-1935`. (bundle не змінювався — лише CSS.)

## Задача O — «Стрічка»: верх екрана сторінки — до найпершого вигляду (Етап 2)

- **Вова (роздратований):** «Немає того варіанту що був з самого початку. Поверни самий перший, коли ти тільки створював з головного промпта.» Через повторні промахи з тінню — уточнив через AskUserQuestion → обрав «лише верх сторінки (банер+аватар+межа)», картки/фон стрічки НЕ чіпати.
- **Дія:** підняв з git точну першу версію `.fd-screen*` (коміт df9c5d08, Етап 2) і повернув розміри верхнього блоку: `.fd-screen-top` 158→150px, `.fd-screen-id` margin -42→-40px, аватар 88→84px (wrap+ava+ph 34→32), назва 21→20px, тема 13.5→13px. Тінь аватарки лишилась 0 4px 14px (як в Етап 2). Палітру/картки/фон стрічки не чіпав (як просив). Лише CSS.
- CACHE `cstl-20260722-1935`→`cstl-20260722-1948`.

## Задача P — «Стрічка»: сіра сторінка має починатись ПОСЕРЕДИНІ аватарки (справжня причина — margin collapse)

- **Вова (дуже роздратований, кілька промахів):** «Поверни першу версію відображення банера і верхнього КРАЮ сторінки. Опусти саму сторінку нижче, аватарку НЕ опускай. Верх сторінки має кінчатися ПОСЕРЕДИНІ аватарки, аватарка накладається зверху — одне ціле. Прочитай перший промт як реалізували внутрішню сторінку.»
- **СПРАВЖНЯ ПРИЧИНА (нарешті знайдено):** margin collapse — `.fd-screen-body` не мав нічого, що зупиняє схлопування, тож від'ємний `margin-top` `.fd-screen-id` (аватарки) «протягував» сірий фон сторінки вгору до ВЕРХУ аватарки. Тому сіра сторінка починалась зверху аватарки, а не посередині — і мої зміни розмірів «нічого не міняли» (їх з'їдав колапс).
- **Фікс (гілка `vova/strichka-avatar-overlap`):** `.fd-screen-body { display: flow-root }` — новий BFC зупиняє колапс → сіра сторінка починається рівно під банером = посередині аватарки. `.fd-screen-id` margin -40→-42 (точно половина 84px-аватарки → центр аватарки на межі). Аватарка накладається навпіл (верх на банері, низ на сторінці) — одне ціле.
- **Урок:** діагностувати РЕНДЕР (margin collapse), а не крутити значення наосліп.
- CACHE →cstl-20260722-1957. Лише CSS.

## Задача Q — «Стрічка»: додати тінь блоку на банер (реверс, новий акаунт vitocorleone191)

- **Вова (повернувся на основний акаунт):** «Тепер додай тінь від блоку на банер, не змінюй нічого більше.» Це реверс — 22.07 цю тінь відкидав (Задачі K/L/M/N), обрав вигляд без неї. Тепер повертаємо.
- **Фікс (лише CSS, `style/feed.css`):** до `.fd-screen-body` додано `box-shadow: 0 -7px 18px rgba(0,0,0,0.20)` — від'ємний Y → тінь падає ТІЛЬКИ вгору на банер; `z-index:1` малює її поверх банера. Геометрію (flow-root, пряма межа, margin аватарки -42, розміри), фон, тінь аватарки — НЕ чіпав. Оновлено коментар (був «без тіні на банер»).
- CACHE `cstl-20260722-1957`→`cstl-20260722-2014`. bundle не змінювався (лише CSS). Чекає перегляду наживо / `/finish`.

## Задача R — «Стрічка»: рівномірна тінь аватарки (одним силуетом із краєм блоку)

- **Вова (скрін IMG_3494):** тінь блоку на банер (Задача Q) є, але верхня половина кружечка-аватарки (виступає над краєм блоку на банер) такої тіні НЕ має → перепад на межі. «Навколо верхньої половини іконки-аватарки зробити таку саму тінь, це одне і те саме з верхньою частиною блоку… одним блоком, щоб верх іконки і верхні краї блоку були одне ціле… рівномірну навколо, без перепадів». Аналогія: на Громаді нижній блок зі зв'язком — одне ціле.
- **Фікс (лише CSS, `style/feed.css`):** `.fd-screen-ava` box-shadow `0 4px 14px rgba(0,0,0,0.18)` (спрямована вниз) → `0 0 18px rgba(0,0,0,0.20)` — зсув 0 = РІВНОМІРНА навколо кола; blur 18px + прозорість 0.20 = ті самі, що в `.fd-screen-body` (`0 -7px 18px 0.20`). Верхня половина кружечка виступає на банер (через `.fd-screen-id` margin-top -42) і кидає таку саму тінь → аватарка + верхній край блоку = один силует без перепаду. box-shadow слідує `border-radius:50%` → коло, не прямокутник.
- Аватарка/блок/геометрію не чіпав — лише параметри тіні кружечка (1 властивість).
- CACHE `cstl-20260722-2014`→`cstl-20260722-2028`. bundle не змінювався (лише CSS). Гілку переставлено на свіжий main (PR #518 уже змерджено). Чекає перегляду наживо / `/finish`.
<<<<<<< HEAD

## Задача S — «Стрічка»: синхронізація коментарів (пункт 1 великого плану) [Роби]

- **Контекст:** Вова описав велику логічну переробку «Стрічки» (ролі + справжня синхронізація як у соцмережі), НЕ точкові фікси. Узгоджено план з 6 пунктів + 3 рішення: (1) адмін-меню «⋯»; (2) deep-links на пост крос-вкладково (замість системного share); (3) коментарі І лайки — тільки через Google-вхід (анонімні реакції ламають статистику — визнав правоту Вови). SQL застосовує Вова (Supabase MCP не авторизований у сесії). Почали з пункту 1 (коментарі — бо зламане).
- **Корінь бага (2 структурні):** (а) тихий провал вставки — `else { input.value = text }` ковтав помилку бази, коментар не показувався і причина невідома; (б) нема живої синхронізації — коментарі вантажились раз при відкритті вкладки, новий бачив лише автор локально, інші — тільки після повного перезавантаження. Таблиця вже в realtime-publication (SQL), але клієнт не підписаний.
- **Зроблено:**
  - `supabase.js`: нова `subscribePageComments(onChange)` — жива підписка на `page_comments` (patern як `subscribeComments`).
  - `feed.js` — переписано блок коментарів:
    - `openCommentSheet` (модульний) — поточний відкритий лист, щоб realtime перемальовував його наживо.
    - `commentRowHtml`/`renderCommentList` — винесено рендер (перемальовування списку на місці, без close+reopen).
    - `applyCommentUpsert(c)` — додати/оновити з ДЕДУПОМ за id (оптимістичне додавання автора + realtime-подія не подвоюють); сортує за created_at; якщо автор не в кеші імен — `fetchAvatars([uid])` і перемалювати; оновлює лічильник карток `patchCommentCount`.
    - `applyCommentRemove(c)` — прибрати (soft-delete приходить як UPDATE з deleted_at → всередині upsert; hard-delete як fallback).
    - `send()`: замість тихого провалу — `alert('Коментар не надіслано: ' + error)` (тепер видно справжню причину, якщо база відхиляє); успіх → `applyCommentUpsert` + очистити поле + фокус (лист лишається відкритим, як у соцмережі).
    - `initFeed`: `subscribePageComments` один раз → INSERT/UPDATE → upsert, DELETE → remove. Коментар будь-кого зʼявляється у ВСІХ наживо + лічильник картки оновлюється live навіть при закритому листі.
- `node --check` feed.js+supabase.js OK; `node build.js` — import-чек 33 файли чисто; bundle перезібрано (esbuild поставив локально, 85 вставок).
- CACHE `cstl-20260722-2028`→`cstl-20260722-2054`. Чекає деплою — Вова наживо перевіряє: коментар зберігається + видно на 2 пристроях. Якщо база відхиляє — тепер покаже причину (діагностика пункту а).

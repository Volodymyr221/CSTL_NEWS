
# ✅ «БЛИМ» ПРИ ВИХОДІ СВАЙПОМ ЗНИК — І ЗАПИС «НЕ ВИЛІКУВАНО» БУВ ХИБНИЙ

🗣️ Вова 27.08: «Блим зник сам до речі, я не знаю що змінилось, але він пропав».

## Що каже код

Фікс **існує і стоїть на місці**: `await новийКадр()` (два `requestAnimationFrame`) між
`switchTab('shotam')` і відкриттям шару в `openFeedPage` (`src/tabs/feed.js`). Приїхав
26.08 07:57, PR #1035.

🔴 **А потім інша сесія записала протилежне:** «НЕ ВИЛІКУВАНО… Моя гіпотеза (два кадри
перед `pushState`) не спрацювала. Зникне в нативній обгортці». Звідти рядок і переїхав у
пульс.

## Чому запис хибний

🔑 **Після того вердикту жодна зміна коду цього шляху не торкалась.** Заміряно: у `main`
після PR #1040 йдуть #1041-#1045, і серед них `src/tabs/feed.js` не змінювався взагалі.
Тобто «зникло саме» неможливе — або фікс працював уже тоді, або на нього подіяла зміна
поза цим файлом.

Два пояснення, і перше сильніше:

1. **Вердикт міряв стару збірку.** SW має `skipWaiting()` + `clients.claim()`, але вже
   завантажена сторінка тримає РОЗІБРАНИЙ bundle до перезавантаження. PWA, піднята з
   фону, показує старий код при новому SW. Той самий клас помилки, що зі «скрінами 21:33»,
   які виявились учорашніми.
2. **Переїзд SDK з чужого CDN змінив час кадрів.** 212 КБ стороннього скрипта більше не
   конкурують за головний потік біля навігації, і два `rAF` тепер справді обіймають
   справжнє малювання. Механізм правдоподібний, доказу немає.

⚠️ **Довести звідси неможливо:** системний жест «назад» iOS у Chromium не відтворюється,
а стенди не мають знімка історії. Це розбір, а не замір.

## 🔴 ЩО З ЦЬОГО ВАРТО ЗАПАМʼЯТАТИ

**Вердикт «фікс не спрацював» вимагає такого самого доказу, як і сам фікс.** Тут його не
було — і хибний запис пережив три сесії, лежачи в пульсі як відкрита вада.

## ⚠️ Один шлях лишається незахищеним

Захист стоїть на `openFeedPage`. Але капсула Громади кличе
`focusFeedPost(postId, 'all')` (`home-caps.js:979`) — а це `switchTab('shotam')` і далі
`openComments` → `openLayer` → `pushState`, **без очікування кадру**. Якщо «блим» колись
повернеться — дивитись сюди першим.

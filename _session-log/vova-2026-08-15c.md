# Журнал сесії — 15.08.2026 (сесія C)

> ⚠️ Третя сесія цього дня. Попередні: `vova-2026-08-15.md`, `vova-2026-08-15b.md`.
> Перед будь-якою роботою звірятись із `origin/main` — 15.08 вже було два
> паралельні чати і одну ваду лагодили двічі.

## Старт

- **Гілка:** `claude/startuem-tt38jj` (production: `main`).
- Коміти підписані `Вова <vitocorleone191@outlook.com>`.
- `git fetch origin` — розбіжностей з `origin/main` **нема** в обидві сторони
  (`origin/main ^HEAD` порожньо, `HEAD ^origin/main` порожньо). Останній коміт
  `main`: `13040915` — «Потік Б: документацію розрізано, --strict полагоджено,
  weather-day не червоніє опівночі» (PR #932).
- Прочитано обовʼязкове: `NOW.md` · `HOT_RULES.md` · `CSTL_BUGS.md` ·
  `BACKLOG_VOVA_2026-08.md` · `ВОВА_ПРОФІЛЬ.md`.
- `BYYOU_PLAN.md` — статус `none`, незавершеного потоку немає.

## Стан на старті

- Критичних (🔴) багів у `CSTL_BUGS.md` — **нема**. Відкриті середні: B-14, B-28,
  B-25, B-22.
- Перший у черзі Вови — **віджет зборів** на «Громаді»; блокер: потрібен
  справжній збір (назва · хто збирає · посилання на банку · ціль).
- У `data/fundraisers.json` лежить **тестовий запис і він на проді**.

## Кроки

- (чекаю «Роби» від Вови)

## Аудит /byyou (Вова спить, правок коду НЕ робив)

- Замовлення: повний автономний аудит усього застосунку. Вова прямо сказав:
  «в кінці напиши висновок… потім я напишу тобі "роби"… і тоді зробиш все і
  задеплоїш». ➡️ ця сесія = **тільки аналіз**.
- Базова лінія стендів: `npm install` доставив playwright (у пісочниці його не
  було, тому перший прогін дав 12/80 — це середовище, не код). Другий прогін —
  **79/80 зелені**; єдиний червоний `docs-fresh` = пульс `NOW.md` відстав від
  мого коміту журналу.
- Знахідки записані в `_ai-tools/AUDIT_FULL_2026-08-15.md` (7×P1, 8×P2 + розділ
  про продуктивність). План виправлень — у `BYYOU_PLAN.md`, статус `planned`.
- Головне: `savePushSubscription` робить `insert` там, де схема обіцяє `upsert`;
  скасування відстеження фільтрує по `endpoint`, а гідрація тягне по `user_uuid`;
  у `sw.js` немає `pushsubscriptionchange`; відповіді на ПИТАННЯ не мають пуша
  взагалі; заставка тримає екран 4.1с безумовно; перший запуск ≈ 4.8 МБ.
- Перевірити живу базу і cron НЕ зміг: MCP Supabase у цій сесії не авторизований.

## «Роби» від Вови (16.08, Supabase авторизований) — ВИКОНАНО

**Слова Вови дослівно:** «Супабейс тут авторизований, роби все по черзі, а в кінці
пиши мені короткий висновок, що зробив і що перевірити як задеплоїш вже».

**Жива база підтвердила три знахідки аудиту:**
- cron `send-bus-push` активний (`* * * * *`), `flush-comment-push` теж;
- унікальний індекс `push_subs_unique (endpoint, route_id, track_date)` — без зупинок;
- на `comments` немає ЖОДНОГО тригера сповіщень (лише antispam + identity).

**Потік А (сповіщення й автобуси):**
- A1 `savePushSubscription`: insert → upsert + скидання `notified_*`.
- A2 `deletePushSubscription`: фільтр по `user_uuid` замість `endpoint`; черга
  pendingUnsub переведена на uid (старі записи відкидаються при читанні).
- A3 черга операцій на рейс (`queuePushOp`) — подвійний тап більше не лишає рядок.
- A4 `pushsubscriptionchange` у `sw.js` + `healPushEndpoint()` у `core/push.js`
  + `migratePushEndpoint()` у `supabase.js` (обидві таблиці з endpoint).
- A5 вікно попередження 13..17 → 2..17 з фактичним числом хвилин.
- A6 `busTag()` — тег із датою і сегментом.
- A7 `SCHEDULE_URL` → castlelife.org (перевірено з бази: 200) + `scheduleOk`.
- A8 deep-link на видалений обʼєкт → тост замість мовчання (3 місця).
- A9 новий сторож `tests/push-subscription.mjs` — 19/19, контроль на коді до
  фіксу (`BUNDLE_REV=1b7a7458`) → **1/19**.

**Потік Б (швидкість і рендеринг):** minify (957→638 КБ) · WebP-геро
(1567→18.5 КБ, різниця 0.52%) · `allSettled` в install · витік resize-слухача ·
черга лайків.

**В1-В3:** сповіщення про відповідь на питання (таблиця + тригер + Edge Function
`send-answer-push` v1, перевірено 200/401 через pg_net) · заставка 4.1с → ~0.7-1.1с
· знято `user-scalable=no` після підняття трьох полів до 16px.

**Едж-функції задеплоєні:** `send-bus-push` **v15** (verify_jwt=true збережено,
код звірено зворотним читанням), `send-answer-push` **v1** (verify_jwt=false).

## Деплой (16.08)

- PR **#935** → squash-merge у `main` → деплой **success**, на сайті
  **v4498 · 16.08 09:23** (Київ).
- Перевірено з зовнішньої мережі (через `pg_net`, бо пісочниця ріже все):
  `castlelife.org` віддає viewport БЕЗ заборони зуму · `images/bus-hero2.webp`
  → 200 · `sw.js` має `CACHE_NAME = cstl-20260816-0552` і обробник
  `pushsubscriptionchange`.
- Edge Functions живі: `send-bus-push` (cron, усі виклики 200 на v15),
  `send-answer-push` v1.

## Уроки сесії (обидва про МІРКУ, обидва спіймані до звіту)

1. Мій фікс заставки зробив червоним `notif-backstack` — стенд чекав 4600мс,
   число зі старої тривалості, і замір поїхав у ХВІСТ підсвітки (вона живе 2.4с).
   ➡️ Те, що ЗГАСАЄ, міряй ПОДІЄЮ, а не числом.
2. Перевірка «viewport на проді» знайшла `user-scalable=no` у моєму ж КОМЕНТАРІ
   й мало не дала хибний висновок «деплой зламаний».
   ➡️ Міряй ЗНАЧЕННЯ атрибута, а не наявність рядка у файлі.
3. Власний регрес до деплою: upsert зі скиданням прапорців кликало й
   самолікування → повторні сповіщення. Полагоджено прапорцем `fresh`.

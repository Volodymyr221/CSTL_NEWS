# Журнал сесії — 11.08.2026 (сесія U-21)

**Гілка:** `claude/startuem-9cf4gf` · **Production:** `main`
**Старт:** 11.08, 01:52 за Києвом (10.08 22:52 UTC)

---

## Крок 0 — старт `/startuem`

- `git config user.name "Вова"` + `vitocorleone191@outlook.com` — виставлено.
- `git fetch origin` — гілка **збігається з `origin/main`** байт-у-байт:
  `git log --oneline HEAD ^origin/main` порожньо, зворотнє (без `auto(...)`) теж порожньо.
  Останній змістовний коміт: `a02119d7` — «Бургер: згасання затемнення, підвал 85→71px,
  розмиття на вкладеному шарі (#888)».
- `_ai-tools/BYYOU_PLAN.md` — статус **`done`** (не `paused`), тобто незавершеного
  потоку `/byyou` підхоплювати не треба.
- Заведено цей журнал (тека `_session-log/` у корені — рівно одна, за HOT_RULES).

## Прочитано

`HOT_RULES.md` · `START_HERE.md` · `_ai-tools/SESSION_STATE_VOVA.md` (хендовер U-20) ·
`_ai-tools/AUDIT_2026-07.md` · `_ai-tools/BACKLOG_VOVA_2026-08.md` · `CSTL_BUGS.md` ·
`CLAUDE.md` · `_ai-tools/NEW_SESSION_PROMPT.md`.

## Стан на вході

- Відкритих **🔴 критичних багів немає** (B-31 закрито 09.08).
- Відкриті 🟡: **B-14** (нема тематичного фільтра новин, заблокований B-28),
  **B-25** (верх смикається при підйомі клавіатури — свідомо лишено),
  **B-22** (семантичні дублі новин у `parse_rss.py`).
- Черга робіт `BACKLOG_VOVA_2026-08.md`: п.1 «Віджет зборів» **чекає даних від Вови**
  (справжній збір: назва · хто збирає · посилання на банку · ціль); п.2 фактично
  порожній; п.3 чекає слова Вови.
- ⚠️ У `data/fundraisers.json` **на проді досі ТЕСТОВИЙ запис** («ТЕСТ — перевірка
  блока зборів»). Прибрати = порожній `items`.
- ⚠️ Хвіст за Вовою з U-20: смоук на iPhone (Instagram → назад → бургер ×5).

## Далі

Чекаю «Роби» від Вови.

---

## Потік /byyou — «Обговорення → ПИТАННЯ» (стратегічний)

**Замовлення Вови:** бриф від ChatGPT — критично переосмислити вкладку «Обговорення»,
перевести з моделі «тема → чат» у «питання → відповіді». Режим: детальний стратегічний + /byyou.

### Аудит — що заміряно (не припущено)

🔴 **Живі дані з продової бази** (Supabase MCP, `posts`+`comments`) — вони перевернули задачу:
- **5 тем усього.** Дві («?» id 42 · «Бачиш його?» id 27) — 0 повідомлень, сміття.
- Тема «Коли плануються наступні вибори?» = **44 повідомлення, але лише 2 людини**;
  зміст: `Прил` · `Пр` · `Алат` · `Бабдссжажаажажа` · `Аааааа` · `Хурма`. Це прогін
  інтерфейсу, не розмова.
- **3 з 5 тем — уже ПИТАННЯ за формою** («Коли вибори?», «Чи буде концерт?», «Коли концерт?»).
- `reply_to_id` заповнений у **1** коментарі з 54 — гілки в чаті ніхто не спожив.
- Разом: 49 повідомлень · 11 профілів · 9 реакцій на теми · 5 закладок.

➡️ **Наслідок:** головний ризик редизайну («не вбий живу активність») знято ДАНИМИ —
ламати нічого. І гіпотеза Вови підтверджується поведінкою, а не смаком.

🔴 **RLS заміряно `pg_policies`:** `posts UPDATE` — **тільки `is_admin()`**, тобто автор
не може ні редагувати своє питання, ні позначити «вирішено». Отже статус «вирішено» =
міграція → окремий блок. Решта Q&A-моделі реалізовна **без жодної зміни схеми**.

### Де я не згоден із брифом
- «ЗАПИТАТИ» у таб-барі — відхилив: решта 4 пунктів іменники-місця, дієслово читається
  як кнопка; і ярлик бре­ше про вміст (більшість заходить ЧИТАТИ).
- Одного перейменування замало: чотири маркери чату (бульбашки «мої справа» · час HH:MM ·
  прев'ю двох повідомлень на картці · «Написати повідомлення…») перекривають будь-який напис.

**План — 13 кроків, у `_ai-tools/BYYOU_PLAN.md`. Статус `planning`, чекаю «ок».**

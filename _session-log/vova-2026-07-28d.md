# Вова — 2026-07-28 (сесія M) · Аудит репозиторію + Bootstrap Prompt

Гілка: `claude/repo-audit-bootstrap-016p8y`. Коду не чіпав — тільки читання + один новий `.md`.
Версію кешу (`CACHE_NAME`) НЕ міняв: зміни чисто документаційні (правило-виняток із `HOT_RULES` №2).

---

## Задача
Вова попросив: повний аудит усього, що впливає на поведінку Claude і структуру розробки
(скіли, правила, інструкції, конфіги, хуки, workflow, стандарти документації/коду/комітів/деплою,
правила токенів і контексту), а потім — **один універсальний Bootstrap Prompt** (стартовий промпт),
який відтворює це середовище в іншому репозиторії без ручного копіювання правил.

## Що прочитано (повний обхід, нічого не пропущено)
- Корінь: `CLAUDE.md`, `.claudeignore`, `.gitignore`, `.gitattributes`, `.gitleaks.toml`,
  `.mcp.json`, `package.json`, `build.js`, `.deploy-trigger`.
- `.claude/`: `settings.json`, 19 скілів у `commands/`, 1 архівний (`commands-archived/fullaudit.md`),
  2 скіли в `skills/` (`caveman`, `ui-ux-pro-max`), 8 хуків у `hooks/` (+ `lib/compute-context-pct.sh`).
- `CSTL NEWS VOVA/`: `CLAUDE.md` (307 рядків), `HOT_RULES.md`, `START_HERE.md`, `ВОВА_ПРОФІЛЬ.md`,
  `CSTL_BUGS.md`, `docs/RULES.md`, `docs/DESIGN_SYSTEM.md`, `docs/NEVERMIND_PATTERNS.md`,
  `docs/ARCHITECTURE.md`, `_ai-tools/NEW_SESSION_PROMPT.md`, `SESSION_STATE*.md`, `BACKLOG.md`,
  `BYYOU_PLAN.md`, `HEALTH.md`, друга копія `.claude/settings.json`.
- `.github/`: 8 активних workflow + `_archive/README.md` + `auto-merge.yml.disabled`.
- `tests/README.md`, `scripts/` (57 файлів), `_session-log/` (22 журнали).

## Що зроблено
Створено `CSTL NEWS VOVA/_ai-tools/BOOTSTRAP_PROMPT.md` — один самодостатній документ, 21 розділ:
змінні проєкту · ієрархія правил · **розбір 10 конфліктів між джерелами** · поведінка і комунікація ·
ворота дозволу · читання/редагування/створення файлів · архітектура · найменування · документація ·
задачі/баги/фічі · git · деплой · тести й аудит (три рівні) · токени й контекст · хуки · усі скіли
як інструкції · шаблони · best practices · автоматичні процеси · «ніколи» · покроковий бутстрап ·
контрольний чеклист.

## Знайдені конфлікти правил і як вирішені (коротко)
1. «Короткі відповіді» vs «пояснення в дужках» → пояснення сильніші.
2. Два `CLAUDE.md` (корінь стислий / VOVA повний) → у бутстрапі один файл.
3. Чеклист аудиту 7 пунктів (`docs/RULES.md`) vs 6 (`CLAUDE.md`) → об'єднано у 8.
4. `HOT_RULES` називаються «8 правил», а їх 10 → правил 10.
5. `BACKLOG` заявляє себе джерелом істини, хоча ним є `AUDIT_2026-07.md` → джерело одне.
6. Глобальний хук радить змінити git-автора → ігнорувати, проєктне правило сильніше.
7. Стара схема «Claude комітить у main» (діаграма в `START_HERE`) vs двогілковий потік → потік через `/finish`.
8. Дві копії `.claude/` (баг B-10) → лише коренева.
9. `caveman` vs повнота → ріже воду, не зміст.
10. SQL-файли vs жива база → джерело правди прод (урок 28.07).

## Що НЕ робив
- Не чіпав код, не міняв `CACHE_NAME`, не створював архівів, не переносив код — Вова цього не просив.
- Не редагував чинні правила (`CLAUDE.md`, `HOT_RULES.md`) — нове правило без «Роби» не заводжу.

## Наступний крок
Вова читає `BOOTSTRAP_PROMPT.md`. Якщо ок — вставляти у новий репозиторій як `CLAUDE.md` + сказати
там «Виконай Розділ 20 (бутстрап)».

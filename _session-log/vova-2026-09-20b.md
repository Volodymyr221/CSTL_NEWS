# Журнал сесії — 20.09.2026 «b»

**Гілка:** `claude/startuem-80n8ak` · production: `main`
**Старт:** `/startuem`

---

## Крок 0 — старт (зроблено)

- `git config user.name "Вова"` / `user.email "vitocorleone191@outlook.com"` — авторство комітів.
- `git fetch origin` — чисто. Звірка двома командами:
  - `git log --oneline origin/main ^HEAD` → **порожньо** (нічого нового на GitHub, чого нема в мене);
  - `git log --oneline HEAD ^origin/main` → **порожньо** (нічого незапушеного).
  Тобто гілка = `origin/main`, старт з чистого стану.
- Останній людський коміт у `main`: `b225e7c8` — «docs: передача в новий чат — 20.09.2026 (#1193)».
- `_ai-tools/BYYOU_PLAN.md` — статус `idle`, незавершеного потоку немає.
- Журнал заведено (цей файл; `vova-2026-09-20.md` зайнятий попередньою сесією → літера `b`).

## Прочитано

`NOW.md` · `HOT_RULES.md` · `CSTL_BUGS.md` · `BACKLOG_VOVA_2026-08.md` ·
`ВОВА_ПРОФІЛЬ.md` · `NEVYKONANI_ZAVDANNIA.md` · `_ai-tools/NEW_SESSION_PROMPT.md`.

## Стан на старті

- Сесія 19-20.09 закрита і вся в `main` (останній PR #1193, до нього #1192, деплой #6087).
- `CACHE_NAME` = `cstl-20260920-1242`, 163 стенди.
- Критичних відкритих багів немає (з 25.08).
- Найближчий борг: зняти `featureOn('skeleton_shapes')` з `src/core/screen-state.js`.

## Дії сесії

_(дописується по ходу)_

---

## 20.09«b» · Розбір двох питань (без «Роби», лише читання)

### 1. Вова натиснув «Прибрано з коду» на `skeleton_shapes`

📐 Звірено грепом, не з памʼяті: `src/core/screen-state.js:38,45,48` — `featureOn`
імпортується і `ФОРМИ_УВІМКНЕНО = () => featureOn('skeleton_shapes')` живий. Тобто
позначка в адмінці стоїть, а код ще не почищений.

🟢 **Нічого не зламалось:** `my_features()` про архівовану фічу віддає
`{on:true, stage:'all'}` (доведено 9 перевірками на живій базі 20.09), архів ховає
рядок **лише в адмінці**. Кістяки під форму екрана далі бачить громада.

🔴 **Але ризик виріс, і це мовчазний клас:** `featureOn()` (`features.js:89`) на
невідомий ключ повертає `false` — тихо, без помилки. Якщо архівований рядок колись
перестане приїжджати, кістяки відкотяться на старий вигляд, а перемикача в адмінці
вже НЕМА — побачити причину буде ніде. Чистка коду стала терміновішою, ніж була.

⏸ Чекає «Роби».

### 2. Питання 11 — діапазон номерів для дописів спільнот (`cms-chain` 12/13)

📐 Факти з коду і даних:
- `data/articles.json`: у діапазоні ≥1 000 000 рівно **24 статті**, з них
  **23 `editor`** і **одна `community`** — `1000022` «🏰 Олика готується до
  осіннього сезону».
- Номер їй видає `next_cms_id()` (`sync_cms.py:50`) — **той самий лічильник**, той
  самий процес (`cms-sync.yml`).
- Червоніє `tests/cms-chain.mjs:71`: «у діапазоні кабінету немає чужих статей»,
  де «чужа» = `kind !== 'editor'`. Правило написане 20.08, допис спільноти в
  Новинах зʼявився 17.09 (PR #1174) — сторож просто старший за фічу.
- 🔑 Причина, заради якої діапазон узагалі заводили (`sync_cms.py:30-45`): два
  РІЗНІ воркфлови пишуть один файл і кожен рахує `max+1` зі свого знімка. Дописи
  спільнот пише той самий процес, що й кабінет → зіткнення номерів неможливе.
- 🛡 Побічна користь одного діапазону: `parse_rss.py:902` вважає все `≥CMS_ID_BASE`
  недоторканним для ретро-дедупу. Допис спільноти — теж людська робота, і цей
  захист йому потрібен. Окремий третій діапазон довелось би окремо вчити парсер.

➡️ **Моя рекомендація:** лишити ОДИН діапазон, полагодити формулювання сторожа
(«діапазон належить `sync_cms.py`», а не «кабінету»: `editor` + `community`).
Рішення за Вовою. ⏸ Чекає «Роби».

---

## ✅ ЗРОБЛЕНО (Вова: «1. Роби · 2. Лиши»)

### 1. Прапорець `skeleton_shapes` знято з коду

`src/core/screen-state.js`: прибрано імпорт `featureOn`, `ФОРМИ_УВІМКНЕНО`, обидва
розгалуження і слухач `cstl-features-ready` у `paintLoading`.
🔑 Слухач існував рівно через прапорець (кістяк малюється ДО мережі, прапорці
приходять З мережі) — без прапорця він перемальовував би кістяк тим самим кістяком.
🛑 Загальний кістяк НЕ чіпав: він лишається запасним для екранів без форми
(`hasShape(kind) === false`).

**Сторож перевернуто, а не викинуто.** Блок 1 `tests/skeleton-shapes.mjs` стеріг
«`off` → стара форма»; тепер та сама сцена з рядком `off` ВИМАГАЄ нову форму —
поверне хтось `featureOn()`, цей рядок червоніє першим. Блоки 2-5 ідуть із
`app_features: []`, як у проді після архівування.

🧪 `skeleton-shapes` **14/14**. Зворотна мутація (повернув `featureOn` у
`loadingHtml` + перезбірка) — **4/14**. Суміжні: `offline-screens` 9/9,
`updates-flags` 13/13, `surface-flags` 9/9, `updates-archive` 19/19.

### 2. Питання 11 — «Лиши»

`tests/cms-chain.mjs`: правило `kind !== 'editor'` → `ВИДИ_СИНКУ = ['editor',
'community']` + **зустрічна межа**: стаття без `kind` (парсерська) у верхньому
діапазоні червонить окремим рядком. Без неї розширення списку видів дало б зелене
«чужих немає» просто тому, що дозволено все.

🧪 `cms-chain` **14/14** (було 12/13). Мутація: підкинув `id 1000999` без `kind`
у `articles.json` → **12/14**, червоніють обидва рядки; дані відновив із копії,
`git diff data/articles.json` порожній.

### 3. Документація

`SESSION_STATE_VOVA.md` — розділ 20.09«b» · `NOW.md` — борг і питання 11 зняті,
авто-блок перегенеровано (`now_update.mjs`) · `NEVYKONANI_ZAVDANNIA.md` — два
рядки в «Закрите», шлях прапорця дописано.

🔴 **ПОБІЧНА ЗНАХІДКА, ВАРТА ОКРЕМОГО РЯДКА.** Реєстр стояв на **40916 з 40960
байт** стелі сторожа `docs-fresh` — тобто **будь-який новий закритий рядок ламав
сторожа**, і реєстр фізично не міг більше приймати роботу. Винесено 17 рядків
(усе до 17.09 включно) дослівно в
`_ai-tools/_archive/NEVYKONANI_закрите_до_2026-09-17.md`, у реєстрі лишилось
посилання. Той самий прийом, що застосували до `CSTL_BUGS.md` 31.08 з тієї ж
причини. Стелю не піднімав.
📐 Після виносу: **37628 байт**, запас ~3 KB. `docs-fresh` 23/23, `docs-refs` 10/10.

`CACHE_NAME` = `cstl-20260920-2000`. Коміти: `792f5668`, `570c7c93`.
⏳ **Не задеплоєно** — усе лежить на гілці, чекає `/finish`.

---

## 🚀 ФІНІШ (20.09«b», 20:37 UTC)

**PR #1194** → squash у `main` (`09d9dc52`). **Деплой #6101** (run `35536143289`).
`CACHE_NAME` = `cstl-20260920-2036`.

**Повний прогін перед фінішем:** 161/163 стендів · 3311/3313 перевірок. Червоних два:
- `design-system` 18/19 — жирний 47% при стелі 45%. 🔴 **НЕ від сесії, доведено:**
  контроль на `origin/main` дає **54%**, а `git diff origin/main..HEAD` по
  `src/ style/ index.html admin.html` додав **нуль** оголошень `font-weight ≥700`
  (заміряно `grep -c`). Відомий борг, реєстр Г.
- `docs-fresh` — пульс відстав від git після моїх же комітів. Полагоджено.

⚠️ **Дрібний інцидент із пульсом, вартий рядка.** Після `now_update.mjs` авто-блок
виріс (більше комітів у переліку) і пульс вийшов за ОБИДВІ стелі — 203 рядки при
200 і 16.5 KB при 16. Стиснув свій же запис і вже закритий блок про кістяки;
стелю не піднімав (це прямо заборонено). Стало 199 рядків / 16.2 KB.

🔑 **Урок, який варто перенести в правила:** стеля пульсу рахує РАЗОМ прозу й
авто-блок, а авто-блок росте від кількості комітів сесії. Тобто **що більше
комітів — то менше місця лишається на текст**, і виявляється це вже після
`now_update`, коли писати ніколи. Писати прозу треба з запасом рядків.

## 💬 ОБГОВОРЕНО (без «Роби»): AI-агент CEO проєкту

🗣️ Замовлення Вови: «нам в наш додаток потрібно створити AI-агента CEO проєкту…
буде пропонувати якісь плани розвитку, відслідковувати показники… Це CEO. Він
займається розвитком проєкту, як внутрішнім, так і зовнішнім».

📐 **Головна знахідка — будувати з нуля не треба.** У проєкті вже працює
`supabase/functions/ai-analytics-summary` (модель `claude-sonnet-5`): агрегати
рахує БАЗА, у промпті стоїть блок «якість даних» із правилом №12 (запуску не
було), кожна метрика має сигнал «помітно/мало/недостатньо/збір_почався_пізніше»,
і агент **читає свій попередній висновок** та звіряє, що з тим сталось. Останнє —
готовий зародок памʼяті CEO.
➡️ Тому рішення: **один агент, два режими** (аналіз по кнопці + щотижневий звіт
CEO, який містить аналіз усередині), а не другий агент поруч. Два агенти на тих
самих даних розійшлись би у висновках — клас «дві копії антиспаму».

**Відповіді Вови на питання:**
1. Ритм — **раз на тиждень** (неділя).
2. Куди звітує — **в застосунок**.
3. Гроші — **ще окремо $5**.
4. Завдання — **«хай зводить»** (моя поправка: через PR, щоб реєстр не став
   наполовину агентським і Вова не перестав його читати).
5. Курс пише **Вова**, але «моментами цікаво почути субʼєктивну думку СЕО».
6. Вектор — **«Все»**: і внутрішнє, і зовнішнє (просування, партнерства).

**Що з цього випливає технічно:**
- 🔑 **Три рівні тексту з мітками** — `ЗАМІРЯНО` (факт із числом) · `ВИСНОВОК`
  (спирається на рядок вище) · 💭 `ДУМКА CEO` (субʼєктивне, і воно не
  прикидається фактом). Це пряма відповідь на замовлення «субʼєктивна думка»:
  дозволяємо її, але з міткою.
- 🌍 Зовнішній вектор реальний уже зараз: агент новин має `web_search`
  (`WEB_SEARCH_TOOL = "web_search_20250305"`, оплачується за 1k запитів).
  Вимога: зовнішнє твердження без посилання не друкується.
- 🧭 **Курс** (`KURS.md`) — ціль + 3-5 показників + заборони. Блокер для всього:
  без нього «вектор» нема від чого відштовхнути.

**План на 5 кроків:** 1) курс зі слів Вови · 2) збирач фактів БЕЗ моделі (пʼять
джерел: аналітика, виробництво, контент, гроші, зовнішнє; порожнє джерело друкує
«не заміряно», а не нуль) · 3) режим `ceo` в наявній Edge Function · 4) розділ
«Дирекція» в адмінці + неділя за розкладом + push · 5) заведення завдань через PR.

⏸ **Чекає:** Вова наговорює курс. «Роби» на крок 2 ще не було.

---

## 🎯 КРОК 1 АГЕНТА CEO — КУРС ЗАПИСАНО

Вова надиктував ціль → `CSTL NEWS VOVA/KURS.md` (135 рядків). Дослівні
формулювання збережені, посилання поставлені з `CLAUDE.md` і реєстру.

**Ціль №1:** не встановлення, а **звичка**. 5000 людей громади за 2-3 місяці —
🔑 читається як ДВІ цілі, і друга (втримати) важливіша.
**Друга половина:** живі спільноти з живими адмінами, яким потрібна віддача.
🔴 Найризикованіше місце курсу, і воно НЕ технічне: курка і яйце — без читачів
адмін не пише, без дописів не приходять читачі.
**Етапи:** звичка → статистика й гроші («маркет») → мережа громад із частковим
алгоритмічним обʼєднанням. Правило мережі: сусіднє підмішується ЛИШЕ коли своєї
інформації бракує.
**Економіка 20 млн грн/міс** записана як ВІЗІЯ, не показник: перший множник
(1000 громад) зʼявляється аж на етапі 3, рух до нього міряти нічим.

### 📐 Звірка показників із кодом — і що вона показала

`logEvent` (`src/core/supabase.js`) пише рівно девʼять подій: `session_start`,
`tab_view`, `pwa_install`, `feed_post_create`, `feed_post_edit`,
`board_post_submit`, `question_answer`, `js_error`, `db_refusal`.

✅ П1-П6 (встановлення, повернення, утримання, глибина, живі спільноти, віддача
авторам) рахуються з наявного.
🔴 **П7 — ЧИТАННЯ — НЕ МІРЯЄТЬСЯ ВЗАГАЛІ.** Застосунок знає лише, що людина
відкрила ВКЛАДКУ. Чи прочитала бодай одну новину — не знає ніхто. Наслідок
подвійний: не видно користі (а вся ціль про неї) **і нема чим показати адміну
спільноти, що його читають** — тобто друга половина цілі лишається без
підтвердження. Лікується однією подією `content_open` з видом і id.
⚠️ **Знаменник під «5000» відсутній у репозиторії:** у `HOT_RULES` стоїть «Олика
3 000» — це містечко, а число по ГРОМАДІ не зафіксоване ніде. Без нього ціль
неперевірна.

⏸ Три питання до Вови: знаменник · тижневі відсічки до 5000 · чи заводимо
`content_open` зараз.

---

## 🔴 21.09 — «РОБИ»: ПОДІЯ ЧИТАННЯ + ЗБИРАЧ ФАКТІВ

### 1. Читання стало вимірним (`src/core/content-views.js`)

**Дві події, бо поверхні різні:**
- `content_open` — Новини: картка показує лише заголовок, тап це НАМІР читати.
  Точка — всередині `openArticle()` (`news.js`), бо туди ведуть усі шляхи:
  делегований тап, повернення за посиланням, перехід із віджета Громади. У
  обробнику тапу подія рахувала б лише перший.
- `content_seen` — Стрічка і Дошка: текст видно прямо в списку, відкривати нема
  чого. Мірка — картка побула на екрані ≥1с (`IntersectionObserver`, поріг
  видимості 0.5).
🔑 **Міряли б обидві поверхні тапами — Стрічка давала б НУЛЬ при живих читачах**,
і ми зробили б хибний висновок «дописи нікому не цікаві».
🛑 Питання (`disc`) свідомо не включені: там картка це заголовок, а зміст
відкривається окремим екраном — інша поверхня.
🛑 У події лише вид і номер: ні імені, ні тексту. Показуємо ЧИСЛО, ніколи імена.

**Вага** — `scripts/supabase_content_views_rollup.sql`: підсумки по днях
(`content_views_daily`) назавжди, сирі рядки 90 днів, згортання о 02:10 UTC
(pg_cron живе в UTC). 📐 Порядок величини під ціль курсу: 5000 × ~10 матеріалів
≈ 50 тис. рядків на добу, тоді як усіх інших подій за ДВА місяці було 11 130.
⏳ **На живу базу НЕ накатано:** Supabase MCP у цій сесії не авторизований.

**Сторож** `tests/content-views.mjs` — **10/10**.
🧪 Мутації: поріг показу знято → 9/10 (прокрутка навиліт рахується читанням);
обидва пояси дедупу знято → 3 події стали 6.
🔴 **І чесний результат, який варто запамʼятати:** мутація «зняти ЛИШЕ дедуп-Set»
лишила стенд ЗЕЛЕНИМ. Розібрався окремим зондом: перехід на іншу вкладку і назад
вузлів НЕ перестворює (мітка на картці переживає повернення), тож одну подію на
вузол тримає `io.unobserve`, а Set страхує лише перестворення списку. Заявляти,
що стенд стереже Set, було б неправдою — записав це і в модулі, і в стенді.

### 2. Збирач фактів (`scripts/ceo_facts.py`) — без моделі взагалі

Пʼять джерел: виробництво · борги · контент · гроші · аналітика.
🛑 **Правило файлу:** джерело, яке не відповіло, друкує **«не заміряно»**, а не
нуль. Нуль — твердження про світ, воно веде до рішень; «не заміряно» — про нас,
воно веде до ремонту.

🔴 **Дві власні вади, спіймані на першому ж прогоні** (обидві — «число бреше
назвою»):
1. `незапушених_комітів: 14` — насправді все запушено, просто гілка попереду
   `main`. Розвів на два поля: `комітів_не_в_main` (нормальний стан гілки) і
   `комітів_не_на_GitHub` (оце вже тривога, правило №11).
2. `стендів_у_проєкті: 165` проти 164 у сторожа — я рахував своїм правилом,
   а не тим, що в `tests/run.mjs` (там відкинуто `run.mjs`). Звів до одного.

**Сторож** `tests/ceo-facts.mjs` — **14/14**. 🧪 Мутації: тиша віддана нулем →
11/14; власне правило рахунку стендів → 13/14.

`CACHE_NAME` = `cstl-20260921-0510`. Стендів у проєкті: **165**.

---

## 🚌 21.09 — ЧОМУ СЬОГОДНІ СПАРСИЛО ОДИН МАРШРУТ (розбір на запит Вови)

📐 **Заміряно по історії `data/vopas-fetched.json`** (це сирий дамп того, що
VOPAS віддав за «сьогодні» на кожному прогоні):

```
21.09 04:25  дата=21.09  сирих рейсів = 23
21.09 02:25  дата=21.09  сирих рейсів = 23
21.09 00:26  дата=21.09  сирих рейсів = 23
20.09 21:59  дата=20.09  сирих рейсів = 403
20.09 19:23  дата=20.09  сирих рейсів = 403
20.09 00:21  дата=20.09  сирих рейсів = 403   ← теж «сьогодні», теж опівночі
19.09 21:44  дата=19.09  сирих рейсів = 405
```

🔑 **Наш код не винен і не мінявся.** Ті самі фільтри вчора дали 30 рейсів: із
403 сирих `route_is_local()` лишає **24**, плюс 6 авто-зворотних = 30. Сьогодні
джерело віддало **23 сирих на весь день**, з них локальних — **один**
(«Ківерці Олика» 06:50). Решта 22 — далекі маршрути (Луцьк-Тернопіль, Луцьк-Луків
тощо), які фільтр відкидає правильно.
📐 У лозі прогону прямо: `routes: 23 · errors: 0` — тобто **сайт відповідав, не
падав**, просто віддавав майже порожньо. Тричі поспіль те саме число.

🛑 **ЧОГО Я НЕ МОЖУ ДОВЕСТИ З ЦЬОГО СЕРЕДОВИЩА:** причину на боці VOPAS. Живий
запит не проходить — проксі відповідає `403 Forbidden` на їхній домен. Тому
«чому саме 23» лишається на боці джерела і перевіряється лише з машини Вови.

### 🔴 А ось ЦЕ вже наша вада, і вона того ж класу, що ми ловимо весь місяць

`scripts/parse_vopas.py` має захист від обнулення — але він спрацьовує **лише
коли відповідь ПОВНІСТЮ порожня**:

```python
if not unique and day in (today, tomorrow) and iso in existing_days:
    days_result[iso] = existing_days[iso]   # зберігаємо старе
```

23 рейси — це не нуль, тож день перезаписався. ➡️ **Часткова відповідь пройшла
як повна**, і застосунок написав людині «СЬОГОДНІ РЕЙСІВ БІЛЬШЕ НЕ ЗАПЛАНОВАНО»
— тобто **видав «ми не знаємо» за «рейсів немає»**. Рівно та межа, яку проєкт
уже проводив собі двічі (порожньо ≠ не змогли).

💡 **Пропозиція фіксу** (чекає «Роби»): поріг правдоподібності. Якщо локальних
рейсів на день різко менше, ніж було в попередньому збереженому зрізі (скажімо,
менше третини), — **не перезаписувати**, лишити попередні дані, сказати в лозі і
позначити в даних, що зріз застарілий. 🔑 Показувати вчорашній розклад із
позначкою чесніше, ніж порожній екран: людина хоч знає, що рейси бувають.

### 🔴 ДРУГА ЗНАХІДКА, ПОБІЧНА — РОЗКЛАД ПАРСЕРА НЕ ТАКИЙ, ЯК НАПИСАНО

`.github/workflows/vopas-parser.yml` у шапці обіцяє: «Запускається кожні 30
хвилин: підхоплює скасування рейсів і зміни статусу». 📐 Насправді останні
прогони: **01:25 · 23:24 · 21:26 · 18:58 UTC** — тобто раз на ~2 години, а після
01:25 не було жодного вже 4 години.
🔑 Це відома поведінка GitHub Actions (планові прогони пропускаються під
навантаженням), але документ обіцяє те, чого немає. ➡️ Отже «скасування рейсу
підхопиться за пів години» — неправда, і на це не можна спиратись у фічі
відстеження рейсу.


---

## ⚠️ 21.09 — ПОПРАВКА ДО ВЛАСНОГО РОЗБОРУ ПАРСЕРА (важливо, не пропустити)

🗣️ **Вова зі знімка VOPAS:** «Але спарсило правильно вроді, сьогодні один рейс
тільки». І він має рацію — мій висновок вище був **передчасний**.

📐 **Що доводить поправку.** Я порівнював 21.09 (23 сирих) із 20.09 (403) і
зробив висновок «часткова відповідь пройшла як повна». Правильна перевірка інша
— історія ТІЄЇ САМОЇ дати:

```
зріз 21.09, зібраний ще 20.09 о 09:51  →  3 рейси (1 з VOPAS + 2 авто-зворотні)
зріз 22.09, зібраний 21.09 о 04:25     →  24 рейси з VOPAS (тим самим прогоном!)
```

🔑 **Дата 21.09 була майже порожня в джерелі ще з учорашнього ранку** — тобто
задовго до того, як стала «сьогодні». Отже це не «поточний день ріжеться», не
збій мережі і не наша вада: у VOPAS на цю дату виставлений ОДИН рейс. Знімок
Вови (Луцьк→Олика, 21.09, «ЗНАЙДЕНО РЕЙСІВ: 1», «продаж припинено») це
підтверджує з другого боку.

🛑 **МІЙ УРОК, І ВІН ТОГО САМОГО КЛАСУ, ЩО МИ ЛОВИМО В ПРИЛАДАХ:** я порівняв не
те з тим. Правильна контрольна пара для «чи зіпсувався день» — це ІСТОРІЯ ЦЬОГО
Ж ДНЯ, а не сусідній день. Сусідній день відповідає на інше питання.

### Що лишається справжнім — і воно не про парсер

📐 22.09 у джерелі **24 рейси**, 21.09 — **один**, обидва зібрані тим самим
прогоном. Якщо автобуси фізично ходять (найімовірніше — перевізник просто не
виставив день у системі продажу), то житель читає «СЬОГОДНІ РЕЙСІВ БІЛЬШЕ НЕ
ЗАПЛАНОВАНО» і не йде на зупинку.

💡 **Пропозиція (чекає «Роби»):** коли на день рейсів різко менше, ніж на сусідні
дні того ж тижня, писати під розкладом м'яку позначку — «джерело дало на цей
день лише N рейсів, можливо розклад ще не виставлено» — замість мовчазного
«немає». 🔑 Порожній екран і «ми не знаємо» мусять бути різні екрани; це те саме
правило, що вже живе в `core/screen-state.js`.
🛑 **І НЕ РОБИТИ те, що я пропонував уранці** — «не перезаписувати день, якщо
рейсів мало». Сьогодні такий запобіжник СХОВАВ БИ правдиву відповідь.

### Побічна знахідка лишається чинною

Прогони парсера йдуть не «кожні 30 хвилин», як обіцяє шапка воркфлову, а раз на
~2 години (01:25 · 23:24 · 21:26 · 18:58 UTC).

---

## 🤖 21.09 — АГЕНТ CEO: КРОКИ 3-5 ЗРОБЛЕНО

### 🔴 Зміна плану, і вона вимушена архітектурою

План казав «другий режим наявної `ai-analytics-summary`». Це правильно для
аналітики — але звіт CEO мусить бачити **курс, борги, git, стенди**, а вони
лежать у РЕПОЗИТОРІЇ, до якого Edge Function доступу не має в принципі.
Варіанти були два: возити репозиторій у базу заради агента — або запустити
агента там, де репозиторій уже є. Обрано друге: `scripts/ceo_agent.py` +
`.github/workflows/ceo-agent.yml`.

🔑 **Суть рішення збережено — думка про цифри лишається ОДНА.** Агент CEO не
перетлумачує аналітику: він бере останній висновок `ai-analytics-summary` з бази
і вбудовує як даність, із прямою забороною сперечатись із ним числами. Два
тексти про ті самі числа розійшлись би (це вже було з двома чергами завдань).

### Що зроблено

- **Крок 3** — агент: пʼять блоків звіту, три рівні тексту (`ЗАМІРЯНО` /
  `ВИСНОВОК` / `ДУМКА CEO`), курс і факти в запиті, спадкоємність через
  попередній звіт. Модель `claude-opus-5`, окремий гаманець $5
  (`data/ceo_spend.json`), пауза при вичерпанні пишеться в базу — не мовчить.
- **Таблиця** `ceo_reports` (`scripts/supabase_ceo_reports.sql`), RLS на `is_admin()`.
  ⏳ На живу базу не накатано — Supabase MCP у сесії не авторизований.
- **Крок 4** — розділ **«Дирекція»** в адмінці: останній звіт, підсвічені мітки
  рівнів, історія, окремий екран для станів «пауза» і «збій». Видно лише
  власнику і CEO: звіт закінчується «рішеннями на підпис», а підписує їх одна
  людина. 🛑 Новий пункт меню одразу занесено в реєстр `surface-flags`
  (сторож упіймав мене на цьому — саме так, як і задумано).
- **Крок 5** — завдання **через PR**: агент складає розділ
  `ЗАВДАННЯ-ДО-РЕЄСТРУ:`, воркфлов збирає з нього гілку й відкриває PR.
  🔑 Поправка до «хай зводить»: реєстр, наполовину заповнений агентом,
  перестають читати — полагодили б забування ціною уваги.

### Сторож і мутації

`tests/ceo-report.mjs` — **15/15**. Мутації: курс не подано → падає; мітку рівня
прибрано → падає; аналітику велено перерахувати → падає.

🔴 **Прилад брехав один раз, і це спіймала саме мутація.** Перша редакція шукала
мітку `ДУМКА CEO:` по ВСЬОМУ запиту — а вона згадується ще й в описі блоку 4.
Тому мутація «прибрати визначення рівня» лишала стенд зеленим: мітка знаходилась
не там, де потрібна. Тепер перевірка дивиться рівно в секцію визначень.
➡️ Це вже третій випадок за дві доби, коли мутація ловить мій власний прилад.

`ceo-facts` 14/14 (додано окремий рядок гаманця CEO), `surface-flags` 9/9,
`admin-shell` 14/14, `admin-access` 46/46, `docs-refs` 10/10.

---

## 🚀 ДРУГИЙ ФІНІШ (21.09) — АГЕНТ CEO В `main`

**PR #1196** → squash (`f7724a3b`) → деплой **#6119 `success`** (перевірено
`get_workflow_run`, не на слово). До нього **PR #1195** (курс, читання, збирач
фактів) → деплой **#6118 `success`**.
`CACHE_NAME` = `cstl-20260921-0638`. Стендів у проєкті: **166**.

**Повний прогін:** 164/166 · 3350/3352. Червоних два, обидва розібрані:
- `design-system` 18/19 — жирний 47% при стелі 45%. 🔴 **НЕ від сесії:** контроль
  на `origin/main` дає **54%**, а `git diff origin/main..HEAD` по `src/ style/
  index.html` додав **нуль** оголошень `font-weight ≥700` (заміряно `grep -c`).
  Відомий борг, реєстр розділ Г.
- `docs-fresh` — авто-блок пульсу відставав після власних комітів. Полагоджено.

⚠️ **Зведення з `main` після squash-merge PR #1195** дало конфлікти в `NOW.md`,
літописі, журналі, `bundle.js` і `sw.js` — очікувана поведінка (гілка продовжує
історію, якої в `main` уже немає в такому вигляді). Розведено по одному:
`bundle.js` перезібрано, `sw.js` — новий `CACHE_NAME`, документи звірені з
`origin/main` на унікальний вміст перед вибором версії.

### ⏳ Що НЕ зроблено і чому (передати далі)

🔴 **Дві міграції не накатані на живу базу** — Supabase MCP у цій сесії не
авторизований:
- `scripts/supabase_ceo_reports.sql` — без неї розділ «Дирекція» показує
  інструкцію замість звіту (екран це чесно каже);
- `scripts/supabase_content_views_rollup.sql` — без неї події читання
  накопичуються без згортання.
🛑 Перший прогін агента CEO до накату міграції запише звіт лише в
`data/ceo_report_last.json` і голосно скаже про це в лозі.

---

## 🗄 21.09 — ДВІ МІГРАЦІЇ НАКАТАНІ НА ЖИВУ БАЗУ

🗣️ Вова: «Супабейс підключений». Проєкт `uabyfecseqnemvcqhdem` (Olyka Castle),
`ACTIVE_HEALTHY`.

**Перед накатом звірено передумови** (щоб не впертись посеред міграції):
`is_admin()` є · `pg_cron` є · таблиць ще немає · задачі з такою назвою немає.

### ✅ `ceo_reports` + `content_views_daily` + `rollup_content_views()`

**Доведено на живій базі, не на слово:**

1. 📐 **Згортання працює на СПРАВЖНІХ даних:** `rollup_content_views(CURRENT_DATE)`
   → `рядків 1, прибрано 0`. У підсумку: `feed_post 58 · content_seen · людей 1`.
   🔑 Тобто подія читання вже прилетіла з проду після деплою — фіча жива, а не
   «має працювати».
2. 🛑 **Сторонній житель звітів НЕ бачить** — `0` рядків під чужим JWT.
3. 🛑 **Вставка руками відхилена:** `42501: new row violates row-level security
   policy for table "ceo_reports"`. Журнал, у який можна дописати, не варто вести.
4. ✅ **Адмін впізнаний:** `is_admin() = true` під поштою з таблиці `admins`
   (пошту в чат не виводив — підставив виразом у JWT, репозиторій публічний).
5. ✅ Підсумки читань бачить будь-який залогінений — так і задумано: з них автор
   побачить «прочитали N людей». У таблиці немає жодного поля, за яким можна
   впізнати читача.

⏰ `cron.job`: `rollup-content-views`, `10 2 * * *`, `active = true` (05:10 за
Києвом — добова межа вже минула).

⚠️ **Перша спроба доказу №3 була порожня:** я написав запит, який лише перевіряв
відсутність рядка, і він нічого не доводив про саму вставку. Переробив на пряму
спробу `INSERT` — і аж тоді отримав чесну помилку. 🔑 Той самий клас, що весь
день: перевірка, яка виглядає як доказ, але міряє не те.

➡️ **Розділ «Дирекція» тепер покаже звіт**, щойно агент його складе. Перший
прогін — у неділю або вручну: GitHub → Actions → `CEO agent` → Run workflow.

---

## 🤖 21.09 — ПЕРШИЙ ПРОГІН АГЕНТА CEO (run #1, запустив Вова)

**Прогін `success`, звіт склався:** `✓ звіт готовий (4979 символів, $0.1070)`.
📐 Отже реальна ціна звіту — **$0.107**, а не моя оцінка $0.05-0.07. За 4-5
звітів на місяць ≈ **$0.43-0.54** із $5. Оцінку я занизив удвічі — записую, щоб
надалі не видавати прикидку за замір.

### 🔴 ТРИ ВАДИ, І ВСІ ТРИ МОЇ

**1. Секрети бази порожні.** У лозі прямо: `SUPABASE_URL:` і
`SUPABASE_SERVICE_KEY:` — порожньо. Агент не бачив аналітики і не записав звіт у
базу (зберіг локально в контейнері раннера, тобто звіт існує ЛИШЕ в лозі).
✅ **Але механізм чесності спрацював ідеально:** агент сам написав
«`ЗАМІРЯНО:` аналітика — не заміряно… Не "нуль" — саме не виміряні» і поставив
відновлення доступу **пріоритетом №1** у векторі. Тобто правило «тиша ≠ нуль»
пройшло живу перевірку.

**2. PR не створився:** `Resource not accessible by integration
(createPullRequest)` — бракувало `permissions: pull-requests: write`.
🛑 **І це була ТИХА поразка:** я написав `check=False`, тож крок лишився зелений.
Рівно те, проти чого сам же писав коментар у сусідньому кроці.

**3. 🔴 НАЙГІРША — пропозиції поїхали ПРЯМО В `main`, повз PR.**
Механіка: крок «Commit spend ledger» пушить `HEAD:main`, а HEAD на той момент
уже був гілкою `ceo/tasks-2026-09-21`, створеною попереднім кроком. Тобто в
`main` поїхали ЗАОДНО правки реєстру (`fe09c797`) разом із витратами
(`eeaae2ca`).
➡️ **Межа, заради якої весь крок 5 і робився — «рішення за людиною» — впала
через ПОРЯДОК КРОКІВ.** Не через логіку, не через права: через порядок.

### ✅ Полагоджено

- кроки переставлені: витрати ПЕРШІ і явно `git checkout main`;
- гілка пропозицій створюється від `origin/main`;
- `permissions: pull-requests: write`;
- невдача `gh pr create` тепер **червонить крок**, а не мовчить.

🧪 `ceo-report` **20/20** (було 15). Нові перевірки стережуть саме порядок
кроків, перехід на `main`, право на PR і чесний червоний.
🧪 Мутація «повернути старий порядок кроків» → **18/20**, падає рядок про порядок.

🛑 **Пропозиції, що вже в `main`, НЕ відкочую:** обидві правдиві (доступ до бази
і пошук 3-5 авторів спільнот) і однаково мали б потрапити в реєстр. Відкочувати
`main` заради чистоти процесу дорожче, ніж лишити. Вова про це знає.

---

## ✍️ 21.09 — СЕСІЯ РІШЕНЬ ВОВИ (11 пунктів)

**Його відповіді:** 1 — уточнив текст сам · 2 так · 3 «агенти мають працювати,
а не стояти» · 4 лишити · 5 ні · 6 як рекомендую · 7 окремим завданням · 8 «роби
як краще» · 9 так · 10 «дав їм робочий код» · 11 «презентацію вже послав».

### Зроблено

**1. 🚌 Позначка «джерело дало мало рейсів».** Текст дослівно за Вовою:
«Джерело показує N рейсів на ДД.ММ.РРРР. Перевірте детальну інформацію на
VOPAS», посилання СЛОВОМ (як у підвалі), не голою адресою.
📐 Правило показу — **менше третини від сусіднього дня того ж тижня**, а не
зашите число: у неділю рейсів законно менше, і поріг із голови брехав би.
🧪 `bus-thin-day` 5/5 — стереже і зворотний бік: на звичайному дні рядка НЕМАЄ.

**2. 🖥 Розмітка в «Дирекції».** `##` і `**` тепер перетворюються, а не
показуються як є.

**3. 💸 Стелі: новини 3.4 → 4.8, кошик 5 → 6.4.** 📐 Заміряно перед правкою:
агент стояв НЕ через вичерпану стелю (2.5877 з 3.40), а через **гальмо рівного
темпу** (дозволено 2.38). 🔑 Тому підняли стелю, а гальмо лишили: послабити його
означало б повернути 2.35, зʼїдені за дві доби 01-02.09. Нова лінія — 3.36,
агент їде. `ai-budget` 51/51.

**6. 📊 `news-widget` став нечутливим до новин.** Стенд читав ЖИВИЙ
`articles.json`, тож висота віджета плавала з контентом: у реєстрі записаний
«74/75 червоний», сьогодні дав 75/75 — **сам позеленів**. Тепер фікстура:
**478px у двох прогонах поспіль**.

**8. 📚 Літопис розрізано втретє.** 533 KB → **144 KB** (вересень), серпень у
`_archive/SESSION_STATE_2026-08_13-31.md` (377 KB) дослівно.
➡️ Урок записав у `/startuem`: місяць роботи важить 300-400 KB, тож різати треба
**щомісяця**, а не «коли стане важко».

**9. 🎨 Жирний: 48% → 37%** (стеля 45%, стенд зелений 19/19).
🔑 Спершу зонд `tests/tools/bold-probe.mjs` — що САМЕ жирне, а не правки навмання.
Змінено рівно два місця, обидва де вага нічого не тримає:
- `.bus-week-day-name` 700→600 — у парі «ПН / 21» ієрархію тримає ЧИСЛО;
- `.btb-check` 700→600 — це символ ✓ у кружечку 16px, його помітність дає контраст.
🛑 **Таб-бар НЕ чіпав** (`.tab-label` 11px/700, 9 елементів) — зона, де іконки
переробляли по 6-7 редакцій; без ока Вови туди не лізу.

### Записано, не зроблено

**7. Push тип 4** — окремим детальним завданням у реєстрі, з порядком дій:
заміряти таблиці → відтворити випадання на живій базі → лише тоді код.
**10. Код заслінки** — нагадування змінити після розгляду гранту.
**5, 11, 4** — закриті рішенням «ні / лишити», винесені в «Закрите».


---

## 🛑 21.09 — ТАБ-БАР ЛИШЕНО ЖИРНИМ, І ЦЕ РІШЕННЯ, А НЕ ПРОПУСК

Вова сказав «роби все», і останнім кандидатом на зрізання жирного лишався
таб-бар (зонд показав 9 елементів по 700). **Прочитав код — і не чіпаю.**

📐 Що там насправді:
- `.tab-label` базово **500**, а не 700;
- `.tab-bar .tab-item.active .tab-label` → **700** — жирна лише АКТИВНА вкладка,
  тобто це єдина ознака «де я»;
- `.tab-label--home` → **700 !important** із записаною причиною: *«04.09 — 600 →
  700 на замовлення Вови (зроби трішки насиченішою)»*.

🔑 Обидва випадки — рівно той жирний, який правило Д3 називає «тримає
ієрархію», а не «щоб було помітніше». Зняти його заради відсотка означало б
зламати орієнтування в застосунку і переступити пряме замовлення власника,
записане в коді з датою.
➡️ Підсумок роботи Д3 за сесію: **48% → 37%** при стелі 45%, двома правками в
місцях, де вага не тримала нічого.

---

## 🔴 21.09 — ЗНАХІДКА ПЕРЕД ДЕПЛОЄМ: ЛІЧИЛЬНИК НОВИН НЕ ГАСНЕ

Повний прогін дав **166/167**, єдиний червоний — `news-badge-geo`:
«прочитана стаття Громади ЗМЕНШУЄ число — **25 → 25**».

🔑 **Спокуса була списати його на «стенд плаває від даних»** — так у журналі
20.09 і записано про цей самий стенд («червоний уранці, до обіду позеленів сам»).
🛑 Не став: контроль на `origin/main` дав **той самий червоний**, а це означає
лише «не від моєї сесії», а НЕ «приладу не вірити».

📐 **Двома зондами звузив діагноз:**
1. картка, яку відкриває стенд, **справді з Громади** — `id 1000023`,
   `geo: "Громада"` (тобто прилад клікає те, що заявляє);
2. `cstl_news_seen_ids` лишається **порожнім `[]`** і ПІСЛЯ читання — тобто
   позначка «прочитано» не пишеться взагалі.

➡️ **Головна підозра для наступної сесії** (`community-blocks.js:1923`):
`markArticleSeen(arts.find(a => a.id === id))` бере `arts` із ЗАМИКАННЯ на момент
дротування обробника. Перемалювався віджет свіжими даними — `find` віддає
`undefined`, і `markArticleSeen` мовчки виходить на `if (!art) return`.
🛑 Клас «мовчазний вихід»: ні помилки, ні сліду, лічильник просто не гасне.

🔴 **Чому не полагодив зараз:** Вова просив «роби все і деплой», а це окрема
робота з живою перевіркою (лічильник бачить кожен житель). Записав у реєстр
відкритим багом із доказами і назвою рядка — щоб наступний захід починався з
перевірки підозри, а не з нуля.

---

## 🚀 ТРЕТІЙ ФІНІШ (21.09«c») — СЕСІЯ РІШЕНЬ У `main`

**PR #1197** → squash (`264f4ede`) → **деплой #6127 `success`** (перевірено, не
на слово). `CACHE_NAME` = `cstl-20260921-1204`. Стендів: **167**.

**Прогін:** 166/167 · 3361/3362. Єдиний червоний — `news-badge-geo`, і він
**успадкований** (контроль на `origin/main` дає той самий), але **справжній**:
записаний відкритим багом із доказами і номером рядка.

**Три деплої за сесію:** #6118 (курс, читання, збирач) · #6119 (агент CEO) ·
#6127 (рішення Вови).

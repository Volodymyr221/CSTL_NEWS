# Журнал сесії — Вова, 24.09.2026

Гілка: `claude/startuem-7qq7rp` · production: `main`

- `/startuem`: fetch + pull main (up to date, останній PR #1203 — передача в новий чат). Прочитано NOW, HOT_RULES, CSTL_BUGS, профіль, NEVYKONANI. Критичних багів немає. Чекаю «Роби».

## 🗣️ «Роби всі завдання топ 3 по черзі» — і потім обговорити Play Market / App Store

### 1. `news-badge-geo` — ✅ корінь знайдено, це була ВАДА КОДУ, а не лише приладу
- Замір ДО: 3 прогони → 10/10, 9/10 (28 → **29**), 9/10 (28 → 29). Лічильник не гасне, а РОСТЕ.
- Корінь (підозра 21.09 підтверджена): `renderCommunityNews()` дротує слухачів один раз із замкненим `arts`; фоновий `доливДописівСпільнот()` підміняє масив і шле `cstl-news-reloaded` → віджет малюється з нового, а тап шукає статтю в старому → `find` = undefined → `markArticleSeen` мовчки виходить.
- Правка: `community-blocks.js` — `_cmNewsArts` (останній намальований список), обробник тапу і `cstl-news-seen` читають його.
- Прилад: стенд тепер чекає `cstl-news-reloaded` (причина `community`) перед тапом — сцена як у житті. Нова перевірка «долив приїхав ДО тапу».
- Після: 12/12 прогонів зелені (11/11). Мутація (старий код): 3 з 4 червоні (28 → 29). ⚠️ Один мутант зелений — коли долив устигає ДО дротування, замикання вже свіже; детермінувати до 100% можна лише затримкою відповіді бази в моку — не робив.
- Сусіди: `news-widget` 75/75, `instant-community-news` 8/8, `sw-slow-network` 17/17. CACHE → `cstl-20260924-1313`.

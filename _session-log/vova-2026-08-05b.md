# Живий журнал сесії — 2026-08-05 (сесія U-12, вечір)

Гілка: `claude/startuem-rcj6ax` · production: `main`
База на старті: `3cd0be39` (= `origin/main`, синхронно, незапушеного нема).

---

## Крок 0 — старт сесії

- `git config user.name "Вова"` + `vitocorleone191@outlook.com` — авторство комітів.
- `git fetch origin` → `origin/main ^HEAD` порожньо, `HEAD ^origin/main` порожньо.
  Тобто гілка сесії стоїть рівно на проді.
- Прод на старті: PR **#790** (`b2cb92fa`), поверх — `3cd0be39` (docs-хендовер #791).
  `CACHE_NAME = cstl-20260805-2019`, стенди 40/710, `DEV_LOCK = true`.
- `BYYOU_PLAN.md` = `idle` — незавершеного потоку `/byyou` немає, пересідати нікуди.
- Прочитано: HOT_RULES · START_HERE · SESSION_STATE_VOVA (хендовер) · AUDIT_2026-07 ·
  BACKLOG_VOVA_2026-08 · CSTL_BUGS · CLAUDE.md · ВОВА_ПРОФІЛЬ.

## Знайдені розбіжності в документації (доповів Вові у першій відповіді)

1. `START_HERE.md` секція «Поточний стан» стоїть на **03.08** (PR #767,
   `CACHE_NAME = cstl-20260803-1830`, стенди 36/605), а `SESSION_STATE_VOVA.md` —
   на **05.08** (PR #790, `cstl-20260805-2019`, стенди 40/710). Файл сам каже
   «новий чат починай звідси», тож застаріває найдорожче.
2. `START_HERE.md` карта файлів згадує `data/community-board.json` як живий, а
   його **видалено 05.08** (PR #790, демо-оголошення з вигаданими телефонами).
3. `START_HERE.md` каже, що `home-fund.js` / `style/home.css` «у репозиторії нема» —
   вони **повернулись 04.08** і живі.

## Крок 1 — правка `START_HERE.md` (Вова: «Прав, якщо це нам в користь»)

Тільки `.md` → `CACHE_NAME` **не чіпав** (правило-виняток HOT_RULES №2).
Перед кожним твердженням звіряв по коду, а не з пам'яті:

| що звіряв | команда / доказ | результат |
|---|---|---|
| `home-*.js` живі | `ls src/tabs/home-*.js` | 4 файли: caps · contacts · feed · fund |
| `style/home.css` живий | `ls style/home*.css` | є |
| `community-board.json` | `ls data/community-board.json` | нема — видалено 05.08 |
| хто читає `fundraisers.json` | `grep -rn` по `src/` | `home-fund.js:111` |
| таб-бар не змінився | `grep data-tab index.html` | Стрічка · Обговорення · Громада · Дошка · Автобуси — як і написано |
| `renderCommunityNews` живий | `grep -rn` | `community-blocks.js:1429` — блок новин на місці |
| скільки CSS підключено | `grep -c @import style.css` | 20 з 21 файла; **`market.css` не підключений** |

**Виправлено (4 розбіжності):**
1. Секція «Поточний стан» 03.08 → 05.08: PR #790 (`b2cb92fa`) + #791,
   `CACHE_NAME = cstl-20260805-2019`, стенди 36/605 → **40/710**.
2. «Файлів `home-*` у репозиторії НЕМА» → **повернулись 04.08**, живі. Так само
   рядок «`fundraisers.json` ніхто не читає» — читає `home-fund.js`.
3. `data/community-board.json` прибрано з карти файлів, поруч — рядок ЧОМУ
   (демо-фолбек із вигаданими телефонами).
4. Розвилка «Блок Актуальні збори втрачено відкатом» → зроблено, чекає першого
   справжнього збору.

**Знайшов сам і теж виправив:**
- блок «Незавершене — обмін кнопок на Дошці» обіцяв «одну правку стилів». Звірив:
  `git rev-list --count origin/vova/board-swap-2..origin/main` = **50 комітів**,
  і серед них PR #790, що переніс стилі Дошки в `style/board.css`. Тобто діагноз
  писався про `community.css`, якого там уже нема. Блок переписано з
  попередженням, а не викинуто (задум Вови лишається чинним).
- карта `src/tabs/` не знала `board-chat.js`, `board-discussions.js`,
  `news-hub.js`, чотирьох `home-*.js`; `style/` стояла на «8 модулів» від 13.05.

## Крок 2 — розвідка: категорії в одну кнопку + куди подіти конверт (ОБГОВОРЕННЯ, без «Роби»)

Вова прислав знімок Дошки (IMG_3866) і три питання: (1) звести категорії в одну
кнопку, як було раніше? (2) якщо фільтр стане на місце конверта — куди конверт?
(3) ідея: FAB «+» перетворюється на конверт, коли є непрочитані. Плюс розбір ChatGPT.

**Що знайшов у коді (не з пам'яті):**

| факт | доказ |
|---|---|
| категорії РАНІШЕ БУЛИ однією кнопкою-лійкою з меню | `board.js:1284-1293` — «🔴 01.08 КНОПКА-ЛІЙКА З ВИПАДНИМ МЕНЮ КАТЕГОРІЙ ВИДАЛЕНА, її замінив ряд чіпів `bd-types`… без зайвого тапу і з лічильниками» |
| конверт видно НЕ ВСІМ | `canSeeMessages()` (`board.js:1210`) = залогінений І (є активне оголошення АБО є розмова). Новачок конверта не бачить |
| у FAB-меню Дошки «Повідомлень» НЕМА | `renderFab()` `board.js:1183-1199` — три пункти: post · mine · saved |
| …бо їх звідти ПРИБРАЛИ 02.08 | PR #737 (`eca43c40`), у діффі видалено `data-fab="messages"` + прямий рядок «⚠️ Пункт «Повідомлення» сюди НЕ повертається — тепер це конверт у шапці» |
| мотивація #737 | «подати оголошення — головна дія… шлях до неї вів у ВЕРХНІЙ ПРАВИЙ КУТ — найважчу зону для великого пальця, тоді як низ — найлегша… дії вниз, листування вгору. Той самий розподіл у Facebook» |
| бейдж СТВОРЕНИЙ для FAB, не для конверта | `.board-trigger-badge` (`board.css:773`) + коментар «геометрія дзеркалить `.board-trigger-badge` (**головна кнопка FAB**)» |
| сирота в CSS | `board.css:781-791` — коментар описує бейдж пункту «Повідомлення» FAB-меню, а самого **селектора вже немає** (пішов із чисткою мертвого CSS 05.08). Коментар лишився без коду |

➡️ **Висновок: обидві половини задуму Вови — це відкат двох рішень**, категорії
(01.08) і конверт (02.08). Не «не можна», але вхідна нова: 02.08 місце в шапці ні
під що не було потрібне, а тепер під нього претендує фільтр.

**Замір висот** — зробити стендом НЕ вийшло: `node_modules` у контейнері немає
(`playwright` не встановлений). Тому міряв **по знімку Вови** — це його реальний
телефон (1290×2796 = 430×932pt @3x), чесніше за симуляцію, точність ±кілька pt:
шапка застосунку → 112pt · багряний блок 115pt (до 227) · ряд чіпів ~54pt ·
**перша картка починається на 301pt = 32% екрана** · таб-бар з 845pt ·
під список лишається **544pt**. Прибрати ряд чіпів = **+64pt ≈ +12%** робочої зони.

**Моя позиція (сперечаюсь):** проти «+ перетворюється на конверт». Дві причини,
сильніші за «користувач розгубиться» від ChatGPT: (1) кнопка міняє призначення
САМА, без дії людини, а повідомлення прилітають realtime → палець уже летить до
«+», за 200мс приходить лист, тап відкриває чужий екран — це HOT_RULES №10 у
чистому вигляді, лише міняється не позиція, а призначення; (2) поки не прочитав —
**подати оголошення неможливо**, головну дію екрана блокує другорядна.
ℹ️ ChatGPT радив, не бачачи коду: не знав ні про `canSeeMessages`, ні про #737,
і писав про «MISTO» — тобто порада загальна, не про наш застосунок.

Запропонував три варіанти + ASCII-мокап. Чекаю вибір Вови.

## Крок 3 — РОБИ: варіант «А» (Вова: «Роби і деплой, будем дивитись»)

Вова вибрав «A», на уточнення про порядок пункту відповів «Роби» — беру свою
рекомендацію: «Повідомлення» ОСТАННІМ у розмітці меню (= найнижчим на екрані,
найближче до пальця; меню викладається `column` і стоїть НАД кнопкою).

### Що змінено

| файл | що |
|---|---|
| `src/tabs/board.js` | конверт `bd-hero-msgs` → кнопка `#bd-cat-btn` у шапці; ряд `bd-types` знято; `catMenuHtml()`; пункт `data-fab="messages"` повернуто в FAB (останнім); `syncMsgFab()` тепер синхронізує пункт меню; `closeBoardMenus` знає нове меню; `updateAdCount` рахує з урахуванням категорії |
| `src/tabs/board-chat.js` | `paintUnreadBadge` малює ДВА бейджі з ОДНОГО числа (кнопка FAB + кружечок пункту) |
| `style/board.css` | мертвий CSS `.bd-types`/`.bd-type*` видалено; `.bd-hero-cat*`; слоган — 1 рядок з трикрапкою; повернуто селектор `.board-fab-badge` (коментар під нього лежав БЕЗ КОДУ з 02.08) |
| `tests/drift.mjs` | `bd-types` прибрано зі списку винятків разом із самим рядом |
| `sw.js` | `CACHE_NAME = cstl-20260805-2032` |

### 📐 Заміряно ДО і ПІСЛЯ (390×844, жива сторінка з підробленою базою)

| | було | стало |
|---|---|---|
| висота шапки `.bd-controls` | 168pt | **125pt** |
| перша картка починається | 236pt | **193pt** |
| hero | 38pt | 44pt (кнопка 44 = мінімум Apple HIG) |

➡️ **Чиста економія 43pt, а не 71**, як я прикидав в обговоренні: ряд чіпів
справді коштував 71pt, але hero підріс на 6pt під кнопку, і проявився нижній
відступ контейнера, який раніше перекривався рядом. **Вові сказати чесно.**

### 🔴 Головний ризик задачі — ширина, і його перевірено на НАЙГІРШОМУ випадку

Вільного місця праворуч від слогана було рівно **97pt** (hero 362 − текст 265).
Кнопка з найдовшою назвою («Знайдено 3») просить **118pt** — тобто не влазить.
Якби слоган переносився, hero ріс би **від вибору категорії** — розкладка «дихала»
б від стану (хвороба, лікована 30.07: число в плашці FAB розширювало пункт меню).
➡️ Слоган стиснуто в один рядок із трикрапкою. Перевірено на «знайдено»:
`btnW=118` · `subClipped=true` (232 з 265, видно 87% тексту) · **`subLines=1`** ·
**`heroH=44` не змінилась** · меню саме закрилось · фільтр дав рівно 3 картки.

При «Усі 19» кнопка 81pt і слоган **не обрізається зовсім** (269 = 269).

### Чому мертвий CSS прибрано, хоч HOT_RULES №9 каже «не чіпати зайве»

Тут це не «прибрав, щоб було чисто»: правила `.bd-type*` стали мертвими САМЕ
через цю зміну, і на них дивиться сторож `dead-css.mjs` з порогом 10 — лишити
означало б з'їсти запас чужої перевірки.

## Чекаю «Роби»

---

# Сесія 26.09.2026 — капсула рейсу на Громаді

Гілка: `claude/project-thread-qn6prq` (від `origin/main` @ `4f0aef03`).

## Замовлення Вови (дослівно, скорочено)

> «Коли я натискаю відстежити будь-який маршрут, він має в капсулі мати найвищу
> пріоритетність, а скасований рейс має відображатися в тій самій послідовності…
> потім минає цей час, коли мав виїжджати цей скасований рейс, і тоді повертається
> до звичайного відображення по пріоритетності, по черзі.»

Два знімки: 12:03 вкладка Автобуси (відстежено «Луцьк → Личани» 12:40) і 12:04
Громада — капсула каже «РЕЙС О 14:25 СКАСОВАНО · Луцьк → Рівне · наступний о 12:25».

## Що знайшли

`src/tabs/home-caps.js`, функція `nowCapsule()`. Блок «скасований рейс» (заведений
20.08) стояв **перед** усіма іншими гілками і мав власний `return`. У ньому не було
**жодного порівняння з часом** — ні стелі, ні звірки з рештою черги: будь-яке
скасування до кінця доби вигравало все, зокрема відстежуваний рейс через 36 хв.

Ламало правило №12 («через скільки рядок стане непотрібним»): 141 хв перебивало 36.

## Що зробили

1. Блок скасування більше не повертає капсулу — лише **рахує кандидата**
   (`скасованийМій`).
2. Рішення переїхало **нижче**, після того як пораховані `hit` (відстежуваний рейс
   або бік від моєї зупинки) і `далекийСьогодні`. Скасування виходить, лише якщо
   `скасований.m - nowMin <= пріоритет(hit)`.
3. Зупинка посадки для **відстежуваного** скасованого береться з самої позначки
   (`t.from`), а не з `myStop`.
4. «Наступний» у рядку тепер називає **власну чергу людини** (`hit`), а не окремий
   перебір `pickFor(myStop, '')`, який не знав ні про відстеження, ні про бік.
   Поїздка, що вже триває (`mode === 'ride'`), у «наступні» не годиться — час
   посадки позаду.
5. Бал `RANK.BUS_CANCELLED` (110) лишився: коли рядок таки вийшов, він каже
   «не йди на зупинку» і мусить стояти вище за все.

## Сторож

`tests/home-caps.mjs`, сцена 20 (три частини на одній миті 09:00):
- **20А** скасування об 11:25 проти відстежуваного о 09:40 → виграє відстежуваний;
- **20Б** скасування о 09:20 проти того самого о 09:40 → виходить скасування і
  називає відстежуваний як «наступний о 09:40»;
- **20В** скасування о 08:20 (минуло) → зникає, звичайна черга повертається.

Дві сцени навмисно: сама лише 20А зеленіла б і над кодом, який скасування не
показує ніколи.

## Статус

- `node build.js` — чисто.
- `tests/home-caps.mjs` — прогін.
- Деплою НЕ було. У `main` не мерджимо до слова Вови «деплой».

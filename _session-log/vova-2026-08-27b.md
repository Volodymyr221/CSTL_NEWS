# Журнал сесії — 27.08.2026 (сесія «b»)

**Гілка:** `claude/startuem-wvx8sj` · **Production:** `main`
**Старт:** 27.08 16:43 UTC · `CACHE_NAME` на старті: `cstl-20260827-1306`
**Останній PR у `main`:** #1052 · останній коміт у `main`: `ecaa93f7` (cms: статті кабінету у стрічку)

---

## Крок 0 — старт сесії (`/startuem`)

- `git config user.name "Вова"` + `vitocorleone191@outlook.com` — авторство комітів.
- `git fetch origin` виконано. Звірка на дві крапки:
  - `git log --oneline origin/main ^HEAD` → **порожньо** (усе з GitHub у мене є);
  - `git log --oneline HEAD ^origin/main` → **порожньо** (нічого незапушеного).
  - Тобто гілка сесії стоїть рівно на `origin/main` — зливати нічого (HOT_RULES №13 задоволено).
- Хук `SessionStart` попереджав, що локальний `main` відстає на 50 комітів — це про
  локальну гілку `main`, робоча гілка сесії при цьому свіжа.
- Потік `/byyou` — статус `idle`, продовжувати нічого.

## Прочитано (обовʼязкове читання)

`NOW.md` · `HOT_RULES.md` (14 правил) · `CSTL_BUGS.md` · `BACKLOG_VOVA_2026-08.md` ·
`ВОВА_ПРОФІЛЬ.md` · `_ai-tools/NEW_SESSION_PROMPT.md`.

**Стан на старті:** відкритих 🔴 критичних немає. Чекає перевірки очима — фото й
посилання у статтях Волинь Post після нічного прогону парсера (стаття 13114).

---

## Далі — дописувати по ходу

---

## 27.08 ~17:00 — РОЗБІР СКАРГИ ВОВИ ПО СТАТТІ АГЕНТА (без «Роби», лише діагноз)

**Скарга (два скріни, стаття «Всесвітній день прибирання 2026»):** погана якість фото ·
посилання не натискні · зайве «« повернутися» в кінці тіла.

**Стаття:** `data/articles.json` id **1000012**, `kind: editor`, `exclusive: true`,
`source: CSTL LIFE`, `sourceUrl: olytska-gromada.gov.ua/news/1786949978/`,
`image_type: source` → `rada.info/upload/users_files/…jpg`.
`ts` = 27.08 **08:30 UTC** (створення чернетки), `added_ts` = 27.08 **16:28 UTC** (синк у стрічку).

### Знайдено ТРИ різні причини, не одну

**1. Фото — це `og:image` самого сайту громади, і ми його НЕ міряємо.**
Шлях: `scripts/ai_news_agent.py:_enrich()` → `pr.fetch_og_image(src)` → `_og_image()`
(`parse_rss.py:1723`) бере `og:image`/`twitter:image` як є. Ні мінімальної ширини, ні
порівняння з фото в тілі — жодної перевірки розміру немає ніде (`editor/images/og.py`
теж лише реюзає ту саму функцію). Показуємо в модалці на всю ширину (~358 CSS px × щільність 3
≈ **1074 справжніх точок**) — файл, менший за це, браузер розтягує. Той самий клас, що
`avatar-quality.mjs` (23.08): пікселів менше, ніж місця під них.
⚠️ **Розмір файлу з контейнера НЕ ЗМІРЯТИ** — `curl` до rada.info дає `403` від проксі
пісочниці (той самий обман, що 12.08 із volynpost). Тобто «файл малий» — гіпотеза, не факт.

**2. Посилання: тіло взяте ДО фіксу, і перебрати його вже ніщо не може.**
- Чернетку зібрано **08:30 UTC**, а фікс живих посилань (`_linkify`, PR **#1047**) став у `main`
  о **13:16 UTC** — на 4,5 год пізніше. Тіло чернетки зберігається в кабінеті готовим HTML,
  публікація о 16:28 просто скопіювала старе.
- 🔴 **І це не разова невдача:** `parse_rss.py:2399` — `if a.get("exclusive"): continue`.
  Самолікування розмітки (`RICH_ALGO_VERSION`) **пропускає всі ексклюзиви**, а
  `sync_cms.py:206` ставить `exclusive: True` кожній статті кабінету. Отже **жодна стаття
  агента ніколи не перебереться** новими правилами — на відміну від 400 статей RSS.
- 🔴 **Друга, окрема вада — адреси БЕЗ схеми.** `_BARE_URL_RE` вимагає `https?://`, тож із трьох
  адрес цієї статті навіть новий код зробив би посиланням лише першу; `letsdoitukraine.org/digitalcleanup`
  і `recyclingpoints.org` лишились би текстом. На скріні Вови видно рівно їх.
- ℹ️ Клієнт голі адреси не лікує: `_linkify` живе **тільки в парсері**, у `src/` його немає.

**3. «« повернутися» — навігація сайту-джерела, і фільтр її не ловить.**
`_TAIL_RE` (`parse_rss.py:945`) зрізає хвіст за маркерами «Читайте також», «Теги:», «Поділитися»…
слова «повернутися» там немає, `_NOISE_RE` теж її не бачить (це звичайне посилання в контейнері).
🛑 **Після фіксу #1047 стане ГІРШЕ:** тепер це `<a>`, тобто «повернутися» буде НАТИСКНИМ
і вестиме на список новин чужого сайту.

➡️ Чекаю рішення Вови, що з цього беремо першим. Код не чіпав.

---

## 27.08 ~17:20 — ЧОМУ В КАПСУЛІ НЕ МІЙ ВІДСТЕЖУВАНИЙ РЕЙС (скрін Вови, 20:32)

**Скарга:** на Автобусах відстежується **Струмівка → Олика 20:17→21:15** (рейс
«Луцьк → Жорнище», у дорозі), а капсула на Громаді каже «ВИЇХАВ · У ВАС ЧЕРЕЗ 43 ХВ ·
Олика → Жорнище · зараз у Піддубці».

### Заміряно по `data/schedule.json` (`vopas_1972489`, 27.08)

Відправлення 20:05, тривалість 85 хв, 52.4 км. `getStopMins` = частка кілометражу × тривалість:

| зупинка | км | час |
|---|---|---|
| Струмівка | 7.5 | **20:17** ✔ збігається з карткою |
| Піддубці | 13.5 | 20:26 |
| Олика | 43.1 | **21:15** ✔ |

О **20:32**: до Струмівки **−15 хв**, до Олики **+43 хв**, поточна позиція — між
Піддубцями (13.5 км) і Звірівом (18 км) → «зараз у Піддубці». **Усі три числа капсули
збігаються до хвилини — вона показує ТОЙ САМИЙ автобус, але описує його як ЧУЖИЙ.**

### 🔴 Корінь — один рядок

`src/tabs/home-caps.js:560` — гілка «1. Відстежуваний рейс на сьогодні»:

```js
const from = t.from || myStop;
const left = minsToBoard(r, from);
if (left == null || left < 0) continue;   // ← ось тут
```

`minsToBoard` рахує час **посадки на моїй зупинці**. Щойно 20:17 минуло, `left` стає
відʼємним і **відстежуваний рейс викидається з розгляду цілком** — хоч він живий, їде
і прибуде до Олики о 21:15. Далі спрацьовує гілка 2 («рейс через моє село»), яка
заново вигадує поїздку: посадка в Олиці → кінцева Жорнище.

### Наслідок — дві окремі вади

1. **Відстеження зникає рівно тоді, коли стає найпотрібнішим** — коли ти вже в автобусі.
   Капсула вміє говорити лише про ПОСАДКУ; про висадку і прибуття вона не вміє нічого.
2. **Порушено правило №12 («МОЄ» вище за загальне»)**: рядок, який я сам увімкнув
   (позначка на рейсі), програв рядку, виведеному з мого села. Навіть влучивши в той
   самий автобус, капсула називає його «Олика → Жорнище» замість збереженого
   «Струмівка → Олика», тож не читається як «мій».

➡️ Напрям лікування (не робив, чекаю «Роби»): у гілці 1 не викидати запис, коли посадка
минула, а рейс `enroute` і висадка попереду — і в цьому стані говорити про ВИСАДКУ
(«У ДОРОЗІ · ВИСАДКА О 21:15 · Струмівка → Олика · зараз у Піддубці»).

---

## 27.08 ~17:55 — «РОБИ ВСЕ» → ЗРОБЛЕНО, ДВА КОМІТИ

**Слова Вови дослівно:** «Так , роби все».

### Коміт 1 — `fbcdedae` «Капсула не губить мій рейс, коли я вже їду»
`src/tabs/home-caps.js` · `tests/home-caps.mjs` · `sw.js` (`CACHE_NAME → cstl-20260827-1753`).
- гілка відстеження більше не викидає запис після часу посадки, якщо попереду висадка
  і рейс `enroute`; нова мітка «У ДОРОЗІ · ВИСАДКА О 21:15».
- «ВИСАДКА» лише коли зупинку виходу обрала людина, інакше «ПРИБУТТЯ».
- вибір між записами переведено на §12 (час до посадки / до висадки).
- 🧪 **143/143**, контроль `BUNDLE_REV=origin/main` → **140/143** і падає дослівно
  скарга Вови: «Олика → Ківерці · зараз у Гай» замість «Гай → Олика».
- 🛑 Дві правки в стенді після першого прогону: час у мітці не був великими літерами;
  а перевірка «сказано, де рейс зараз» падала не через код — на 390pt `fitLine`
  законно зрізає перший щабель значення. Назви зупинок у фікстурі скорочено і сцену
  переведено на 430pt: міряємо ЗМІСТ рядка, а не правило обрізки.

### Коміт 2 — `19aab523` «Стаття агента: живі посилання, без "повернутися", обкладинку міряємо»
`scripts/parse_rss.py` · `sync_cms.py` · `ai_news_agent.py` · новий `test_markup_polish.py`.
- `polish_markup()` — прохід БЕЗ мережі (`POLISH_VERSION`), лікує й ексклюзиви кабінету;
  заборону `if a.get("exclusive")` у самолікуванні **не знято свідомо** — вона захищає
  тексти, які Вова пише сам.
- адреси без `https://` → посилання (доменна зона з переліку, пошта вимкнена).
- `_лише_навігація()` відсіює «« повернутися» і рідню; у `_TAIL_RE` це класти НЕ можна.
- `image_size()` + `best_cover()` — розмір із заголовка файлу, заміна лише коли вужче 800.
- агент качає сторінку ОДИН раз замість двох.
- 🧪 `test_markup_polish.py` **33/33** (остання група — на справжньому тілі статті 1000012),
  `test_rich_html` 57/57, `test_fulltext` 21/21, `docs-refs` 10/10.

### Що лишилось поза кодом
- 🔴 **Розмір того фото не зміряно** — `curl` до `rada.info` = 403 від проксі пісочниці.
- ⏳ Стаття 1000012 полагодиться на **наступному прогоні `rss-parser`** (він і запише
  відполіровану розмітку в `data/articles.json`); нові статті кабінету — одразу при синку.
- ⏳ Капсулу видно після `/finish` + деплою.

---

## 27.08 ~18:15 — «ЧОМУ НОВИНИ НЕ ПАРСИЛИСЬ 5 ГОДИН?»

**Перевірено через GitHub API (`actions_list`), не за комітами** — коміт зʼявляється
лише коли є НОВІ статті, тож «немає коміту» ще не означає «не запускалось».

| воркфлов | розклад | останній ПЛАНОВИЙ прогін (UTC) | простій на 18:10 |
|---|---|---|---|
| `rss-parser` | `0,30 * * * *` (48/добу) | **07:27** | **10 год 43 хв** |
| `ai-news-agent` | `15 * * * *` (24/добу) | 09:50 | 8 год 20 хв |
| `vopas-parser` | `*/30` | ~11:02 | 7 год |
| `cms-sync` | `*/5` (288/добу) | 05:07 і 16:27 — **два за добу** | — |

🔑 **Усі прогони, які відбулись, — `success`. Нічого не падає: GitHub їх просто НЕ
ЗАПУСКАЄ.** Прогін `rss-parser` об 11:47 був `workflow_dispatch` (руками), а не за
розкладом. Тобто це не наш код і не наші джерела.

📐 Це та сама хвороба, що заміряна 12.08 («cron каже 30 хв, реально 48-134»), але
сьогодні в важчій формі — плановий запуск не стався ЖОДНОГО разу за 10 годин.
`schedule` у GitHub Actions — best-effort, і **першими відкидаються найчастіші
розклади**; наш `cms-sync` із `*/5` це 288 запусків на добу з одного репозиторію.

🛑 **Запустити руками з контейнера не вийшло:** `actions_run_trigger` → **403
Resource not accessible by integration** (інтеграція не має права `workflow_dispatch`).
Вова робить це сам: Actions → «RSS Parser» → Run workflow.

➡️ Пропозиції (не робив, чекаю рішення): знизити частоту `cms-sync` `*/5 → */15`
або перевести його на подію публікації; за потреби — зовнішній будильник у
`repository_dispatch`.

---

## 27.08 18:18 — /finish

- Злиття `origin/main` ДО PR (HOT_RULES №13): один конфлікт — `NOW.md`. Розведено за
  правилом: чужа версія за основу, проза переписана під цю сесію.
  ⚠️ У `main` тим часом приїхав **PR #1053** з паралельної сесії другого акаунта — вона
  незалежно заміряла той самий простій планувальника (07:27). Діагнози збіглись.
- 🛑 **`docs-fresh` завалив пульс двічі:** 241 рядок / 21.5 KB при стелі 200 / 16 KB.
  Стелю НЕ піднімав (пряма заборона) — стиснув старі розділи, історія й так у літописі.
  Після трьох заходів: **20/20**, 16.0 KB.
- **PR #1054 → squash у `main`** (`e96dfe2f`). Деплой **#5163** — **success** (18:19:26 UTC, `v5163`).
- `CACHE_NAME` = `cstl-20260827-1753`.

---

## 27.08 ~19:00 — ФОТО ДУБЛЮЄТЬСЯ: ОБКЛАДИНКА + ПЕРШЕ ФОТО ТІЛА

**Скарга Вови (два скріни, стаття 13153 «Роман Романюк перевірив готовність
Сенкевичівського ліцею», Конкурент):** у джерелі знімок стоїть ОДИН раз, у нас двічі —
«перший раз як фотографія карточки, другий раз як з опису».

### Чому наявний захист не спрацював

У парсері вже є відсів (`_figure_html` → `_same_photo`), і він порівнює **домен + шлях**.
📐 Заміряно по живих даних (`data/articles.json`, id 13153):

```
обкладинка : …/media/cache/bb/64/bb64f25fdb5e9c628109eaa8b2fe14d8.jpg
перше фото : …/media/cache/51/da/51da4b0f083142efe7d808047515dbd8.webp
```

🔑 **Це та сама фотографія, але сайт віддає кожен варіант під ВЛАСНИМ хешем і в іншому
форматі.** Тобто порівняння за адресою тут не спрацює **ніколи** і не почне спрацьовувати
від жодного доопрацювання адрес. Обкладинка ще й прийшла з RSS (`image_type` немає), а не
зі сторінки — тож це різні дороги до одного знімка.

### Лікування — за МІСЦЕМ, а не за адресою

Якщо обкладинка є, а перше фото стоїть **перед будь-яким текстом** — це провідний знімок,
який людина щойно бачила вгорі. Знімок усередині тексту не чіпається.
- `_blocks_to_html` і `_flow_to_html` — правило в **обох** збирачах, інакше воно працювало б
  через раз залежно від верстки джерела.
- `RICH_ALGO_VERSION` **3 → 4** — наявні статті переберуться.
- 🔴 **І те саме в `polish_markup` (`POLISH_VERSION` 1 → 2), БЕЗ мережі** — інакше
  виправлення чекало б, поки самолікування дійде до кожної з 400 статей по 40 за прогін
  (понад десять прогонів, а прогони GitHub кидає).
⚠️ **Свідомий розмін, названий прямо:** якщо провідне фото тіла НЕ дорівнює обкладинці, ми
його втрачаємо. Угорі статті фотографія однаково лишається, а якісна альтернатива (качати
обидва файли і порівнювати пікселі) коштувала б десятків запитів на статтю заради косметики.

### Доведено

📐 **На справжній статті 13153:** фото **14 → 13**, зникло саме `51da4b0f`, текст
**1975 → 1975** символів (не втрачено нічого).
🧪 `test_rich_html` **57 → 66** (+9), `test_markup_polish` **33 → 38** (+5), `test_fulltext` 21/21.
🔬 **Мутант** (правило вимкнено) → **63/66** — сторож уміє впасти саме на цій ваді.
🛑 Наявна перевірка «чуже фото з тіла не викидається, коли обкладинка інша» лишилась
зеленою: у її сцені знімок стоїть ПОСЕРЕД тексту, тобто нове правило її не зачіпає.

### ⚠️ ІНЦИДЕНТ ЗЛИТТЯ (19:10) — SQUASH ПОДВОЇВ БЛОК КОДУ

PR #1054 змерджено **squash**, тобто в `main` лягла НОВА історія, не пов'язана з гілкою.
При наступному `git merge origin/main` git побачив ті самі файли як «додані з обох боків»:
`sync_cms.py`, `test_markup_polish.py`, журнал — конфлікт `add/add` (розведено «мій бік»,
він надмножина), **а в `parse_rss.py` конфлікту не було взагалі — git просто вставив ДВІ
копії блоку полірування**. Друга копія (стара, `POLISH_VERSION = 1`, `polish_markup` з
одним аргументом) стояла НИЖЧЕ і перекривала нову.
🛑 **Мовчазна вада:** `test_rich_html` при цьому світився **66/66**, бо його правило живе
в іншому місці файлу; спіймав лише `test_markup_polish` — `TypeError: polish_markup() takes
1 positional argument but 2 were given`.
✅ Дубль (рядки 538-623) видалено, усі python-стенди зелені: **38/38 · 66/66 · 21/21** +
retention і dedup.
➡️ **Правило собі:** після злиття `main` у гілку, яка вже вливалась туди squash-ом,
перевіряти не лише маркери конфлікту, а й **подвоєні визначення** (`grep -n "^def "` на
змінених файлах).

---

## 27.08 ~19:40 — ПОШУК ФІЛЬТРА ГОДУВАВ КАПСУЛУ + ФІЛЬТР НЕ СКИДАВСЯ

**Скарга Вови (два скріни):** набрав у вкладці Автобуси «Звідки: **Дерно**», нічого не
відстежував — і на Громаді засвітилось «ЗАВТРА · О 07:32 · **Дерно → Носовичі**». Дослівно:
«користувач може шукати, забути скинути фільтр, і йому постійно буде світити Дерно —
Носовичі… капсула має реагувати, коли користувач зберігає рейс для відстеження».
Друге: «якщо користувач закриває додаток… цей фільтр не скидається — зроби, щоб скидався».
Третє: «натиснув Дерно — Рівне завтра, а в капсулу його не підтягнуло».

### Три зміни

**1. Пошуковий фільтр більше не входить у капсулу** (`home-caps.js`, три місця:
скасований рейс · гілка «через моє село» · гілка «завтра»).
🔑 **Пошук — це ПИТАННЯ, а не вибір:** людина дивиться, що взагалі їздить, і нічого про
себе не заявляє. Капсула каже «МОЄ» (§12), а єдина явна заява про свій рейс — кнопка
«відстежувати». Джерел лишилось два: **мої відстежувані рейси** і **рейс через МОЮ
зупинку** (геолокація → анкета → Олика). `getBusPrefs` з імпорту прибрано.

**2. Відстежуваний рейс на ЗАВТРА теж мій.** Гілка «завтра» дивилась лише на розклад від
моєї зупинки — тобто відстеження на завтра не важило нічого. Тепер спершу шукаємо
відстежуваний завтрашній, і лише якщо його немає — найближчий від моєї зупинки. Бал
лишається `BUS_TOMORROW` (10): завтрашній рейс не вимагає дії зараз.

**3. Фільтр живе в `sessionStorage`, а не в `localStorage`** (`buses.js`).
📐 Це рівно межа, яку назвав Вова: `sessionStorage` в iOS PWA **переживає згортання**
(вийшов подивитись повідомлення і повернувся — фільтр на місці) і **зникає при повному
закритті**. Старий ключ прибираємо один раз при завантаженні, інакше вічне «Дерно» з
`localStorage` пережило б зміну і виглядало б як «не працює».
ℹ️ `loadBusPrefs()` у `community-blocks.js` — мертва функція (0 викликів), не чіпав
(правило №9): скажеш — приберу окремо.

### 🛑 КОНТРОЛЬ СПЕРШУ БУВ ФАЛЬШИВИЙ

Перша редакція сцени писала фільтр лише в `sessionStorage` — і контрольний прогін на
`origin/main` **зеленів**, бо старий код туди не дивиться. Тобто перевірка не вміла
впасти. Тепер сцена пише в **обидва** сховища (це заразом сцена «людина оновилась зі
старою памʼяттю»), і додано окрему перевірку, що старий ключ прибрано.

🧪 `home-caps` **150/150** (+7, дві сцени), контроль `BUNDLE_REV=origin/main` → **145/150**
і падає дослівно скарга: «Дерно → Луцьк» замість «Олика → Луцьк», і «ЗАВТРА · О 06:10»
замість відстежуваного 07:10. `bus-stop-picker` 13/13.

**Опубліковано:** PR **#1058** → squash у `main` (`4fa5ce5e`), деплой **#5168**,
`CACHE_NAME` = `cstl-20260827-2009`.
⚠️ Злиття знову дало конфлікт `sw.js` (паралельна сесія другого акаунта поставила
`cstl-20260827-1954` через PR #1057) і `NOW.md` — розведено за правилом №13.
🛑 Пульс знову впирався в стелю (204 рядки / 18.5 KB) — стискав чотири рази, стелю не
піднімав. **20/20.**

---

## 27.08 ~20:50 — 🔴 ПОСИЛАННЯ ВЕЛИ НА ЧУЖУ СТАТТЮ (найгірший клас вади)

**Скарга Вови (4 скріни):** у новині «Україна та Румунія проведуть зустріч щодо нацменшин»
(id **13167**, розділ Україна) слова «день румунської мови» відкривають **«Ціна на газ для
населення стане менше на 7%» від 26 листопада 2019**. На джерелі те саме слово веде куди
треба. Плюс «Читати оригінал» відкриває **Європейську правду**, хоч підписано «Українська
правда». Його слова: **«ми обманюємо людей таким чином… або це пофіксити, або взагалі не
обманювати людей»**.

### Корінь — редирект, про який ми не знали

`sourceUrl` = `pravda.com.ua/news/2026/08/27/8050666/`, а сторінка живе на
**`eurointegration.com.ua`**: RSS «Української правди» роздає й матеріали сайтів-побратимів
через власні адреси, які **редиректять**.
🔑 Сторінку ми читали ПРАВИЛЬНУ, а відносні посилання (`/news/2026/03/12/7233078/`) зводили
до адреси **ЗАПИТУ** — тобто до `pravda.com.ua`. Той самий шлях на іншому сайті мережі
«Правди» — **інша стаття**.
🛑 **Найпідступніше: посилання виглядало живим і вело на СПРАВЖНЮ статтю — просто не ту.**
Ні помилки, ні 404, ні порожньої сторінки. Заміряно по збереженому тілу:
```
<a href="https://www.pravda.com.ua/news/2026/03/12/7233078/">день румунської мови</a>
```

### Три зміни

1. **База = КІНЦЕВА адреса після редиректів** (`r.geturl()`), а не адреса запиту.
   `fetch_article_page` віддає тепер **трійку** (тіло, обкладинка, кінцева адреса).
   `RICH_ALGO_VERSION` **4 → 5** — наявні статті переберуться.
2. **Підпис джерела за кінцевою адресою** (`_OUTLETS`, `справжнє_джерело`): стаття
   «Європейської правди» більше не виходить під чужим імʼям. 🛑 Невідомий домен підпис НЕ
   чіпає — здогад про чуже імʼя гірший за неточність, бо виглядає як факт.
3. **Проміжний захист без мережі:** поки стаття з `pravda.com.ua` не перебрана новою базою,
   її посилання **розгортаються в текст** (`_REDIRECT_HUBS`, `POLISH_VERSION` 2 → 3). Текст
   лишається цілим, зникає лише натискність — це і є «не обманювати».
   📐 Заміряно: із 400 статей **153 з pravda.com.ua**, з них **42 мають посилання** (усіх
   статей із посиланнями — 86). Тобто половина постраждалих, решта джерел не зачеплена.

### Доведено

🧪 `test_rich_html` **66 → 73** (+7): сцена з підміненим `geturl` доводить, що відносне
посилання стало `eurointegration.com.ua/...` і **не веде** на `pravda.com.ua`; контроль без
редиректу вимагає протилежного (інакше перевірка зеленіла б від будь-якої зміни бази).
🔬 **МУТАНТ** (повернути базу на адресу запиту) → **70/73**.
🧪 `test_markup_polish` **38 → 42**, `test_fulltext` 21/21, retention і dedup зелені.
🛑 Заглушку в стенді довелось навчити `geturl()` — без нього `rehydrate` мовчки ловив
`TypeError` і сторож показував «звернень: 1» замість поломки. Той самий клас, що вже двічі
ловив цей файл (розходження підписів = тиха брехня стенда).

---

## 27.08 ~21:10 — ВОВА ПОВТОРИВ СКАРГУ: ДАНІ ЩЕ НЕ ВИПРАВЛЕНІ

Фікс лежав у `main` (PR #1059), але **застосунок малює збережені дані**, а вони чекали
прогону парсера — якого немає з 07:27. Для Вови це виглядало як «мене не почули».

✅ **Виправив ДАНІ прямо зараз, без мережі** — прогнав по `data/articles.json` ту саму
функцію, яку кличе парсер (`polish_stored_articles`), і закомітив результат:

```
статей із посиланнями:            86 → 44   (знято 42 ненадійні з pravda.com.ua)
статей змінено:                   72
текст помітно скоротився:         0  ← нічого не втрачено
знято дубль обкладинки в тілі:    31 стаття
```

🔑 Слова в тексті лишились (**«день румунської мови»** читається), зникла лише натискність —
рівно те, що просив Вова: «або пофіксити, або не обманювати».
➡️ Правильні посилання повернуться самі, коли парсер перебере статті новою базою
(`RICH_ALGO_VERSION = 5`), по 40 за прогін.

---

## 27.08 20:36 — ВОВА ЗАПУСТИВ ПАРСЕР РУКАМИ. ПЕРЕВІРЕНО НА ЖИВИХ ДАНИХ

Прогін `rss-parser` (`workflow_dispatch`, 20:31→20:36), коміт `98637134`, деплой **#5171**.

### Стаття 13167 — та сама, зі скріна Вови

| | до | після |
|---|---|---|
| джерело | Українська правда | **Європейська правда** ✅ |
| «день румунської мови» | `pravda.com.ua/news/2026/03/12/7233078/` (газ 2019) | **`eurointegration.com.ua/news/2026/03/12/7233078/`** ✅ |
| «День румунської мови: що змінює…» | `pravda.com.ua/articles/…` | **`eurointegration.com.ua/articles/2026/03/23/7233754/`** ✅ |
| `_richAlgo` | 4 | **5** |

### По всьому файлу

```
перебрано новою базою (_richAlgo=5) ....... 52 з 400
статей із посиланнями ..................... 44 → 69   (повертаються ПРАВИЛЬНИМИ)
підпис виправлено на «Європейська правда» .. 8 статей
статей хаба, ще не перебраних ............. 128 — і в них 0 посилань (пастки немає)
перебрані статті з посиланнями на pravda ... 6 — усі СПРАВЖНІ статті УП (перевірено поштучно:
                                              лінки на УП, Facebook, Telegram, AP)
```

✅ **Ланцюг доведено від скарги до живих даних.** Решта 348 статей переберуться по 40 за
прогін; до того їхні посилання лишаються звичайним текстом — не пасткою.

# Журнал сесії — 27.08.2026 (сесія «b»)

**Гілка:** `claude/startuem-wvx8sj` · **Production:** `main`
**Старт:** 27.08 16:43 UTC · `CACHE_NAME` на старті: `cstl-20260827-1306`
**Останній PR у `main`:** #1052 · останній коміт у `main`: `ecaa93f7` (cms: статті кабінету у стрічку)

---

## Крок 0 — старт сесії (`/startuem`)

- `git config user.name "Вова"` + `vitocorleone191@outlook.com` — авторство комітів.
- `git fetch origin` виконано. Звірка на дві крапки:
  - `git log --oneline origin/main ^HEAD` → **порожньо** (усе з GitHub у мене є);
  - `git log --oneline HEAD ^origin/main` → **порожньо** (нічого незапушеного).
  - Тобто гілка сесії стоїть рівно на `origin/main` — зливати нічого (HOT_RULES №13 задоволено).
- Хук `SessionStart` попереджав, що локальний `main` відстає на 50 комітів — це про
  локальну гілку `main`, робоча гілка сесії при цьому свіжа.
- Потік `/byyou` — статус `idle`, продовжувати нічого.

## Прочитано (обовʼязкове читання)

`NOW.md` · `HOT_RULES.md` (14 правил) · `CSTL_BUGS.md` · `BACKLOG_VOVA_2026-08.md` ·
`ВОВА_ПРОФІЛЬ.md` · `_ai-tools/NEW_SESSION_PROMPT.md`.

**Стан на старті:** відкритих 🔴 критичних немає. Чекає перевірки очима — фото й
посилання у статтях Волинь Post після нічного прогону парсера (стаття 13114).

---

## Далі — дописувати по ходу

---

## 27.08 ~17:00 — РОЗБІР СКАРГИ ВОВИ ПО СТАТТІ АГЕНТА (без «Роби», лише діагноз)

**Скарга (два скріни, стаття «Всесвітній день прибирання 2026»):** погана якість фото ·
посилання не натискні · зайве «« повернутися» в кінці тіла.

**Стаття:** `data/articles.json` id **1000012**, `kind: editor`, `exclusive: true`,
`source: CSTL LIFE`, `sourceUrl: olytska-gromada.gov.ua/news/1786949978/`,
`image_type: source` → `rada.info/upload/users_files/…jpg`.
`ts` = 27.08 **08:30 UTC** (створення чернетки), `added_ts` = 27.08 **16:28 UTC** (синк у стрічку).

### Знайдено ТРИ різні причини, не одну

**1. Фото — це `og:image` самого сайту громади, і ми його НЕ міряємо.**
Шлях: `scripts/ai_news_agent.py:_enrich()` → `pr.fetch_og_image(src)` → `_og_image()`
(`parse_rss.py:1723`) бере `og:image`/`twitter:image` як є. Ні мінімальної ширини, ні
порівняння з фото в тілі — жодної перевірки розміру немає ніде (`editor/images/og.py`
теж лише реюзає ту саму функцію). Показуємо в модалці на всю ширину (~358 CSS px × щільність 3
≈ **1074 справжніх точок**) — файл, менший за це, браузер розтягує. Той самий клас, що
`avatar-quality.mjs` (23.08): пікселів менше, ніж місця під них.
⚠️ **Розмір файлу з контейнера НЕ ЗМІРЯТИ** — `curl` до rada.info дає `403` від проксі
пісочниці (той самий обман, що 12.08 із volynpost). Тобто «файл малий» — гіпотеза, не факт.

**2. Посилання: тіло взяте ДО фіксу, і перебрати його вже ніщо не може.**
- Чернетку зібрано **08:30 UTC**, а фікс живих посилань (`_linkify`, PR **#1047**) став у `main`
  о **13:16 UTC** — на 4,5 год пізніше. Тіло чернетки зберігається в кабінеті готовим HTML,
  публікація о 16:28 просто скопіювала старе.
- 🔴 **І це не разова невдача:** `parse_rss.py:2399` — `if a.get("exclusive"): continue`.
  Самолікування розмітки (`RICH_ALGO_VERSION`) **пропускає всі ексклюзиви**, а
  `sync_cms.py:206` ставить `exclusive: True` кожній статті кабінету. Отже **жодна стаття
  агента ніколи не перебереться** новими правилами — на відміну від 400 статей RSS.
- 🔴 **Друга, окрема вада — адреси БЕЗ схеми.** `_BARE_URL_RE` вимагає `https?://`, тож із трьох
  адрес цієї статті навіть новий код зробив би посиланням лише першу; `letsdoitukraine.org/digitalcleanup`
  і `recyclingpoints.org` лишились би текстом. На скріні Вови видно рівно їх.
- ℹ️ Клієнт голі адреси не лікує: `_linkify` живе **тільки в парсері**, у `src/` його немає.

**3. «« повернутися» — навігація сайту-джерела, і фільтр її не ловить.**
`_TAIL_RE` (`parse_rss.py:945`) зрізає хвіст за маркерами «Читайте також», «Теги:», «Поділитися»…
слова «повернутися» там немає, `_NOISE_RE` теж її не бачить (це звичайне посилання в контейнері).
🛑 **Після фіксу #1047 стане ГІРШЕ:** тепер це `<a>`, тобто «повернутися» буде НАТИСКНИМ
і вестиме на список новин чужого сайту.

➡️ Чекаю рішення Вови, що з цього беремо першим. Код не чіпав.

---

## 27.08 ~17:20 — ЧОМУ В КАПСУЛІ НЕ МІЙ ВІДСТЕЖУВАНИЙ РЕЙС (скрін Вови, 20:32)

**Скарга:** на Автобусах відстежується **Струмівка → Олика 20:17→21:15** (рейс
«Луцьк → Жорнище», у дорозі), а капсула на Громаді каже «ВИЇХАВ · У ВАС ЧЕРЕЗ 43 ХВ ·
Олика → Жорнище · зараз у Піддубці».

### Заміряно по `data/schedule.json` (`vopas_1972489`, 27.08)

Відправлення 20:05, тривалість 85 хв, 52.4 км. `getStopMins` = частка кілометражу × тривалість:

| зупинка | км | час |
|---|---|---|
| Струмівка | 7.5 | **20:17** ✔ збігається з карткою |
| Піддубці | 13.5 | 20:26 |
| Олика | 43.1 | **21:15** ✔ |

О **20:32**: до Струмівки **−15 хв**, до Олики **+43 хв**, поточна позиція — між
Піддубцями (13.5 км) і Звірівом (18 км) → «зараз у Піддубці». **Усі три числа капсули
збігаються до хвилини — вона показує ТОЙ САМИЙ автобус, але описує його як ЧУЖИЙ.**

### 🔴 Корінь — один рядок

`src/tabs/home-caps.js:560` — гілка «1. Відстежуваний рейс на сьогодні»:

```js
const from = t.from || myStop;
const left = minsToBoard(r, from);
if (left == null || left < 0) continue;   // ← ось тут
```

`minsToBoard` рахує час **посадки на моїй зупинці**. Щойно 20:17 минуло, `left` стає
відʼємним і **відстежуваний рейс викидається з розгляду цілком** — хоч він живий, їде
і прибуде до Олики о 21:15. Далі спрацьовує гілка 2 («рейс через моє село»), яка
заново вигадує поїздку: посадка в Олиці → кінцева Жорнище.

### Наслідок — дві окремі вади

1. **Відстеження зникає рівно тоді, коли стає найпотрібнішим** — коли ти вже в автобусі.
   Капсула вміє говорити лише про ПОСАДКУ; про висадку і прибуття вона не вміє нічого.
2. **Порушено правило №12 («МОЄ» вище за загальне»)**: рядок, який я сам увімкнув
   (позначка на рейсі), програв рядку, виведеному з мого села. Навіть влучивши в той
   самий автобус, капсула називає його «Олика → Жорнище» замість збереженого
   «Струмівка → Олика», тож не читається як «мій».

➡️ Напрям лікування (не робив, чекаю «Роби»): у гілці 1 не викидати запис, коли посадка
минула, а рейс `enroute` і висадка попереду — і в цьому стані говорити про ВИСАДКУ
(«У ДОРОЗІ · ВИСАДКА О 21:15 · Струмівка → Олика · зараз у Піддубці»).

---

## 27.08 ~17:55 — «РОБИ ВСЕ» → ЗРОБЛЕНО, ДВА КОМІТИ

**Слова Вови дослівно:** «Так , роби все».

### Коміт 1 — `fbcdedae` «Капсула не губить мій рейс, коли я вже їду»
`src/tabs/home-caps.js` · `tests/home-caps.mjs` · `sw.js` (`CACHE_NAME → cstl-20260827-1753`).
- гілка відстеження більше не викидає запис після часу посадки, якщо попереду висадка
  і рейс `enroute`; нова мітка «У ДОРОЗІ · ВИСАДКА О 21:15».
- «ВИСАДКА» лише коли зупинку виходу обрала людина, інакше «ПРИБУТТЯ».
- вибір між записами переведено на §12 (час до посадки / до висадки).
- 🧪 **143/143**, контроль `BUNDLE_REV=origin/main` → **140/143** і падає дослівно
  скарга Вови: «Олика → Ківерці · зараз у Гай» замість «Гай → Олика».
- 🛑 Дві правки в стенді після першого прогону: час у мітці не був великими літерами;
  а перевірка «сказано, де рейс зараз» падала не через код — на 390pt `fitLine`
  законно зрізає перший щабель значення. Назви зупинок у фікстурі скорочено і сцену
  переведено на 430pt: міряємо ЗМІСТ рядка, а не правило обрізки.

### Коміт 2 — `19aab523` «Стаття агента: живі посилання, без "повернутися", обкладинку міряємо»
`scripts/parse_rss.py` · `sync_cms.py` · `ai_news_agent.py` · новий `test_markup_polish.py`.
- `polish_markup()` — прохід БЕЗ мережі (`POLISH_VERSION`), лікує й ексклюзиви кабінету;
  заборону `if a.get("exclusive")` у самолікуванні **не знято свідомо** — вона захищає
  тексти, які Вова пише сам.
- адреси без `https://` → посилання (доменна зона з переліку, пошта вимкнена).
- `_лише_навігація()` відсіює «« повернутися» і рідню; у `_TAIL_RE` це класти НЕ можна.
- `image_size()` + `best_cover()` — розмір із заголовка файлу, заміна лише коли вужче 800.
- агент качає сторінку ОДИН раз замість двох.
- 🧪 `test_markup_polish.py` **33/33** (остання група — на справжньому тілі статті 1000012),
  `test_rich_html` 57/57, `test_fulltext` 21/21, `docs-refs` 10/10.

### Що лишилось поза кодом
- 🔴 **Розмір того фото не зміряно** — `curl` до `rada.info` = 403 від проксі пісочниці.
- ⏳ Стаття 1000012 полагодиться на **наступному прогоні `rss-parser`** (він і запише
  відполіровану розмітку в `data/articles.json`); нові статті кабінету — одразу при синку.
- ⏳ Капсулу видно після `/finish` + деплою.

---

## 27.08 ~18:15 — «ЧОМУ НОВИНИ НЕ ПАРСИЛИСЬ 5 ГОДИН?»

**Перевірено через GitHub API (`actions_list`), не за комітами** — коміт зʼявляється
лише коли є НОВІ статті, тож «немає коміту» ще не означає «не запускалось».

| воркфлов | розклад | останній ПЛАНОВИЙ прогін (UTC) | простій на 18:10 |
|---|---|---|---|
| `rss-parser` | `0,30 * * * *` (48/добу) | **07:27** | **10 год 43 хв** |
| `ai-news-agent` | `15 * * * *` (24/добу) | 09:50 | 8 год 20 хв |
| `vopas-parser` | `*/30` | ~11:02 | 7 год |
| `cms-sync` | `*/5` (288/добу) | 05:07 і 16:27 — **два за добу** | — |

🔑 **Усі прогони, які відбулись, — `success`. Нічого не падає: GitHub їх просто НЕ
ЗАПУСКАЄ.** Прогін `rss-parser` об 11:47 був `workflow_dispatch` (руками), а не за
розкладом. Тобто це не наш код і не наші джерела.

📐 Це та сама хвороба, що заміряна 12.08 («cron каже 30 хв, реально 48-134»), але
сьогодні в важчій формі — плановий запуск не стався ЖОДНОГО разу за 10 годин.
`schedule` у GitHub Actions — best-effort, і **першими відкидаються найчастіші
розклади**; наш `cms-sync` із `*/5` це 288 запусків на добу з одного репозиторію.

🛑 **Запустити руками з контейнера не вийшло:** `actions_run_trigger` → **403
Resource not accessible by integration** (інтеграція не має права `workflow_dispatch`).
Вова робить це сам: Actions → «RSS Parser» → Run workflow.

➡️ Пропозиції (не робив, чекаю рішення): знизити частоту `cms-sync` `*/5 → */15`
або перевести його на подію публікації; за потреби — зовнішній будильник у
`repository_dispatch`.

---

## 27.08 18:18 — /finish

- Злиття `origin/main` ДО PR (HOT_RULES №13): один конфлікт — `NOW.md`. Розведено за
  правилом: чужа версія за основу, проза переписана під цю сесію.
  ⚠️ У `main` тим часом приїхав **PR #1053** з паралельної сесії другого акаунта — вона
  незалежно заміряла той самий простій планувальника (07:27). Діагнози збіглись.
- 🛑 **`docs-fresh` завалив пульс двічі:** 241 рядок / 21.5 KB при стелі 200 / 16 KB.
  Стелю НЕ піднімав (пряма заборона) — стиснув старі розділи, історія й так у літописі.
  Після трьох заходів: **20/20**, 16.0 KB.
- **PR #1054 → squash у `main`** (`e96dfe2f`). Деплой **#5163** — `in_progress`.
- `CACHE_NAME` = `cstl-20260827-1753`.

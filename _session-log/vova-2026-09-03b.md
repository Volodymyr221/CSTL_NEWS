# Журнал сесії — 03.09.2026

> Гілка: `claude/startuem-31pmy4` (production: `main`).
> Живий журнал: дописується ПО ХОДУ після кожної значущої дії, комітиться і
> пушиться кожні кілька кроків (HOT_RULES №11).

---

## 1. Старт сесії (`/startuem`)

- Підпис комітів: `Вова <vitocorleone191@outlook.com>` — виставлено.
- `git fetch origin` — виконано. Звірка на дві крапки:
  - `git log --oneline origin/main ^HEAD` (без `auto(`) → **порожньо**
  - `git log --oneline HEAD ^origin/main` → **порожньо**
  ➡️ гілка стоїть рівно на `origin/main` (останній коміт `d028f876`, авто-новини 03.09 17:26 UTC).
- Обовʼязкове читання виконано повністю: `NOW.md` · `HOT_RULES.md` ·
  `CSTL_BUGS.md` · `BACKLOG_VOVA_2026-08.md` · `ВОВА_ПРОФІЛЬ.md`.
- Стан прочитано з `origin/main`, не з диска (HOT_RULES №11).

**Зріз стану:**
- 🟢 Відкритих критичних багів немає (з 25.08, коли закрився B-33).
- 🟡 Середні: **B-25** (верх смикається при ПІДЙОМІ клавіатури на iPhone) — дві
  спроби провалились, Вова погодився поки лишити.
- Не доробленого з попередньої сесії в `main` не висить — усе доїхало.

**Що далі — чекаю «Роби» від Вови.** Код не чіпаю (правило №1/№3).

---

## 2. Звірка з роботою другого акаунта (запит Вови)

🗣️ **Вова:** «працював з іншого акаунту, там робота обірвалася, скажи чи ти бачиш
всю пророблену роботу та зміни».

🔴 **Мірка комітами БРЕХАЛА б:** `origin/main..origin/<гілка>` показує 103 «відсутні»
коміти на `wzboev`, хоча більшість змісту в `main` є — PR мерджаться **squash**, тож
хеші не збігаються (HOT_RULES №13). Мірив **дифом на дві крапки** і наявністю файлів.

### Знайдено три активні гілки за 03.09

| гілка | що там | стан |
|---|---|---|
| `claude/startuem-wzboev` | 🔴 **робота, що НЕ доїхала в `main`** | **PR #1143 ВІДКРИТИЙ** |
| `claude/startuem-ztzxpr` | скіли перенесено в інший репозиторій `T-K-Performance` | коду CSTL не чіпала |
| `claude/startuem-31pmy4` | ця сесія | журнал |

### 🔴 PR #1143 — відкритий, чекає мерджу (створений 03.09 09:40)

«🔐 Немає прихованих входів у шапці + 📣 стаття кабінету йде туди, куди обрала людина»

Доведено, що в `main` цього НЕМА (`git cat-file -e origin/main:<файл>`):
- `tests/header-taps.mjs` ❌ · `tests/cms-target.mjs` ❌ · `scripts/supabase_cms_target.sql` ❌
- змінені й не доїхали: `admin.html` (+106) · `src/app.js` · `style/base.css` ·
  `scripts/sync_cms.py` (+137) · `sw.js` · `bundle.js`
- `CACHE_NAME`: у `main` `cstl-20260902-1947`, на гілці `cstl-20260903-0955` — тобто
  **сайт зараз показує вчорашній код**, ця робота на `castlelife.org` НЕ задеплоєна.

⚠️ Гілка відстала від `main` лише на два **авто**-коміти ботів (`ai(editor)`, `cms`).

### ⚠️ Друга знахідка — зіткнення імен журналів (правило №13)

Сесія `ztzxpr` уже зайняла імʼя `_session-log/vova-2026-09-03.md`. Я створив файл
з **тим самим імʼям** — при зведенні гілок це дало б конфлікт рівно того класу, що
24.08 (обидві сесії взяли `-b`). ➡️ Перейменував свій на `vova-2026-09-03b.md`
**до** того, як він десь зустрівся з чужим.

---

## 3. Мердж PR #1143 — злиття, прогін, знахідки

**Злиття `origin/main` у гілку — конфліктів 0** (гілка відставала лише на два авто-коміти ботів).
`node build.js` → **exit 0**, `check-imports` 64 файли чисто, `bundle.js` після
перезбірки **не змінився** — закомічений бандл збігається з джерелами.

📐 **Повний прогін: 136/137 стендів · 2901/2902 перевірок.**
Єдиний червоний — `docs-fresh` 22/23, і він **мій власний наслідок**: злиття з `main`
зсунуло історію, авто-блок пульсу відстав. Полагоджено штатним `node scripts/now_update.mjs`
(руками той блок правити не можна) → `docs-fresh` **23/23**, `docs-refs` **10/10**.
⚠️ Пульс після оновлення 15582 байт / 183 рядки — під стелею (16384 / 200), різати не довелось.

🐍 Python-сторожі: `history_routing` 53/53 · `editor_pools` 53/53 · `markup_polish` 42/42 ·
`fulltext` 21/21 · `mixed_layout` 13/13 · `categories`/`retention`/`dedup` зелені.
⚠️ `test_worker.py` не запускається без URL воркера — очікувано, воркера ще немає.
⚠️ **Дві дрібниці СЕРЕДОВИЩА, не коду:** у контейнері бракувало `esbuild` і `bs4` — доставив.

## 4. Питання Вови: чому дві статті пішли в «Історія Громади»

**Відповідь — правило `це_історія()` у `scripts/sync_cms.py` (заведене 28.08):**
категорія містить підрядок `істор` або `краєзнав` → стаття НЕ йде в новини, а лягає
дописом у спільноту 6. Обидві мали `git_id = null`, тобто в новини не потрапили зовсім.

| стаття кабінету | категорія | допис |
|---|---|---|
| №31 «Жорнище» | «Історія та краєзнавство» | 34 (28.08) |
| №46 «Личани» | «історія та культура» | 41 (03.09 18:33) |

🔴 **ВАДА НЕ В ТОМУ, ЩО ПРАВИЛО СПРАЦЮВАЛО, А В ТОМУ, ЧИМ ВОНО КЕРУЄТЬСЯ.** Маршрут
вирішує **вільний текст категорії, який пише AI-агент**. Доказ лежить поруч: №39
«Залісоче: село… де стоїть Луцька брама замку» — за жанром те саме краєзнавство, але
категорія «Туризм і культурна спадщина» без слова «істор» → поїхала **в Новини**
(`git_id 1000015`). Дві однакові за суттю статті розійшлись через випадкове формулювання.
➡️ Це саме те, що лікує **PR #1143** (селект «Куди публікувати» + `target_locked`).

⚠️ **Не доведено:** «Личани» лежала `published` з 02.09 11:21, а вийшла 03.09 18:33 —
**понад добу**. Схоже на відомий пункт «синк будиться ненадійним `*/15`, штовхання без
секрету `GITHUB_DISPATCH_TOKEN` не працює», але я цього НЕ міряв.

## 5. 🟡 ВІДКРИТА ПОЗНАЧКА: у стрічці видно один допис, у базі — два

🗣️ **Вова:** «в вкладці стрічка, було дві опублікованих статті, але вже одна, все окей».

Заміряно в базі — **розбіжність справжня, і вона не в даних**:
- `page_posts` сторінки 6: обидва дописи (34 і 41) `deleted_at = null`;
- `cms_articles`: №31 і №46 обидві `published`;
- `data/articles.json`: 400 статей, 21 кабінетна — усі на місці;
- видалень **сьогодні НЕ БУЛО ЖОДНОГО** в жодній спільноті (останнє взагалі 30.08).

🛑 Отже дані цілі, а екран показує менше. Вова зняв питання («все окей»), тому далі не
копав. ➡️ **Побачиш знову — копай КЛІЄНТА** (показ стрічки, `paintIfChanged` у
`core/list-patch.js`, кеш), **а не базу**: база вже перевірена і чиста.

---

## 6. ✅ PR #1143 змерджено, деплой №5452 success

Рішення Вови на червоний CI: «Мерджуй — червоні не наші». Доведено прогоном 05:30
(до обох частин): ті самі три (`drift` 13/24 · `news-widget` · `dev-lock`).
⚠️ `news-widget` за день погіршився 74/75 → 73/75 від СВІЖИХ новин («12 картинок на
4 сторінки»), код віджета PR не чіпав. Три червоні — відкриті, справжні, чужі для PR.
🛑 Мій локальний «зелений» цих трьох НЕ доводить: браузер у контейнері, найпевніше,
не дістає до Supabase — стенд зеленіє над порожнечею.

## 7. 🎨 ВЕЛИКИЙ ДИЗАЙН-АУДИТ — замовлення Вови (дослівно)

🗣️ «Відносно тих скілів по дизайну і тд які ми закачували в репозиторій, проведи дуже
детальний аудит всього застосунку з точки зору дизайну, чи все сучасно, просто та
комфортно і що можна взагалі покращити, зробити не «дитячий» вигляд, а преміальний,
але бажано в стилі додатку, не керуйся тим що є, типу «бо ми вже так зробили», якщо
бачиш що можна покращити — кажи, навіть по дрібницях, тіні і тд. Це стосується не
тільки вкладок таббара, а всіх вкладок і сторінок і модалок які є в додатку, починай,
не спіши, проведи цей аудит детально і зосереджено».

➡️ Це той самий «ВЕЛИКИЙ АУДИТ» з `NOW.md` ➡️ ДАЛІ п.5 («такий, який ти ще ніколи
не робив, по всіх параметрах», 01.09). **Код не чіпаю** — аудит, не «Роби».
Правило №0 діє напряму: думати «як найкраще», а не «як уже зроблено».

**План:** (1) скіли `apple-design` · `emil-design-eng` · `ui-ux-pro-max` як мірка ·
(2) підняти застосунок у Chromium 390pt, знімки УСІХ екранів/модалок · (3) зняти
ЧИСЛА з CSS: усі тіні, радіуси, кеглі, кольори — скільки різних, де розходяться ·
(4) звіт у `_ai-tools/AUDIT_DESIGN_2026-09.md` (урок AUDIT_STRICHKA: висновки одразу
у файл, не в чат).


### Що зроблено в аудиті

**Прилад:** `tests/tools/design-audit-shots.mjs` — 59 знімків усіх екранів (390×844,
iPhone UA, насичена підроблена база) + `measure.json` з ОБЧИСЛЕНИМИ стилями кожної сцени.
**Звіт:** `CSTL NEWS VOVA/_ai-tools/AUDIT_DESIGN_2026-09.md`.

🛑 **ДВІ ВЛАСНІ ПОМИЛКИ ПРИЛАДУ, обидві знайдені знімками, не міркуванням:**
1. **Порядок перехоплювачів мережі перевернутий.** Playwright питає `route` ВІД
   ОСТАННЬОГО до першого, а я поставив загальний «глушити все чуже» останнім — він
   з'їдав погоду і фото. На перших 59 знімках погода писала «тимчасово недоступна»,
   а картки новин були порожні. Лікує: загальний перехоплювач ПЕРШИМ, точкові — після.
2. **Скидання між сценами шукало кнопки закриття за класами** — і аркуш «Збережені»
   з переглядачем фото лишались відкритими на 20 сцен поспіль, накриваючи все нижче.
   Лікує: скидання = ПЕРЕЗАВАНТАЖЕННЯ сторінки. Чиста сторінка надійніша за будь-який
   перелік селекторів.

📐 **Головні числа:** 53 тіні · 29 радіусів · 33 кеглі · 36 tracking · 44 кольори
тексту · 38 фонів · 41% тексту вагою ≥700 · 97 капс-вузлів ≥13px · 515 вузлів ≤10.5px ·
475 тап-цілей менших за 44×44.

🔑 **Діагноз:** «дитячість» не в окремих елементах, а у ВІДСУТНОСТІ СИСТЕМИ + у тому,
що застосунок одночасно кричить (капс, 800-900 вага, кольорові тіні) і шепоче
(9.5px підписи таб-бару).

🔴 **Знайдено заразом дві справжні вади:** `--ink-mute #9C9080` на `--app-bg` дає
контраст **2.69:1** (недоступно, і це БЕЖ, який скасовано 11.08 як базовий колір) ·
мертвий `images/bus-hero2.png` **1.6 МБ** у репозиторії (живий лише `.webp` 19 КБ).


---

## 8. 🛠 «Роби все крім 4» — виконано кроки 1-3 черги аудиту

🗣️ Слово Вови дослівно: **«Роби все крім 4»**.

⚠️ **Прочитань було два, і я взяв перетин.** У звіті черга пронумерована 1-7, у моєму
повідомленні в чат — 1-4. Кроки **1, 2, 3 входять в обидва прочитання**, тому зроблено
рівно їх; про 5-7 (тап-цілі · зелені тумблери · перший екран Громади) перепитую окремо,
бо правило проєкту — без «Роби» код не чіпати, а тут не видно, на який саме список
Вова дивився.

### Крок 1 — типографіка

- **Капс знято з усього, крім міток категорій.** `.cm-sec-head h2` (20px/800 з
  `letter-spacing: 3px` — найгучніший елемент застосунку) · `.bd-hero-title` ·
  `.cm-ad-title` · `.bd-ad-title` (62 вузли) · `.cm-board-submit` ·
  `.cm-board-modal-title` · `.board-fab-label` · **20 викликів `toUpperCase()`** у
  `src/tabs/buses.js` (фрази і назви маршрутів; лишився тільки `bhv4-label` — це
  кінці шкали 10px).
- **Половинні кеглі — 127 → 0.** Ваги **800/900 — 83 → 0** (стеля 700).
- **Заміряно:** капс ≥13px на живих вкладках **10 → 0**, на всіх 59 екранах **97 → 0**;
  частка тексту вагою ≥700 **54% → 44%**.

### Крок 2 — тіні і радіуси

Заведено шкали в `style/tokens.css`: `--elev-1..4` + `--elev-dark-2/3` і
`--r-sm/md/lg/full`. Різних тіней на 59 екранах **53 → 41**.

🛑 **ВИНЯТОК, названий у трьох місцях (токени · сторож · звіт): бордова тінь
центральної кнопки «Громада» ЛИШАЄТЬСЯ.** Я її був замінив на `--elev-4` — і повернув
назад, прочитавши коментар у `tabbar.css` із прямим словом Вови 08.08: «пропав відтінок,
який був… він такий світло-бордовий, поверни назад». **Правило аудиту не скасовує
названого рішення власника.**

### Крок 3 — колір тексту

`--ink-mute` **`#9C9080` → `#636872`**. Це була подвійна вада: контраст на `--app-bg`
**2.69:1** (нижче норми 4.5) **і** це БЕЖ, який Вова скасував як базовий колір 11.08.
Стало **4.81:1**.

### 🆕 Сторож `tests/design-system.mjs` (16 перевірок)

Тримає межу з двох боків: **джерело** (половинні кеглі, ваги 800/900, кольорові тіні,
оголошені токени, нейтральність і контраст `--ink-mute`) і **живі екрани** (капс ≥13px,
частка жирного, кегль і регістр підпису таб-бару).

🔴 **Стенд знайшов те, чого не бачило око:** `.tab-bar .tab-label { font-size: 10px }`
у `community.css` перебивав `tabbar.css` більшою вагою селектора — підпис таба лишався
10px попри правку в його ВЛАСНОМУ файлі.

🛑 **І власну помилку сторожа:** перша редакція вклеювала `zoneCss(REV)` ПОВЕРХ свіжих
стилів, тож три живі перевірки світились зеленим і на старому коді — «контроль, який не
може впасти». Тепер при `CSS_REV` усі `<link rel=stylesheet>` вимикаються.
**16/16, контроль `CSS_REV=origin/main` → 0/16.**

📐 **Стеля жирного 45% ЗАМІРЯНА, не вигадана:** контроль на старому коді дає 54%,
свіжий — 44%.

### 🔴 Три стенди довелось оновити — і це не послаблення

Дизайн-зміни завалили вимоги, які самі ж і були предметом замовлення. Я не видаляв
перевірки, а **перевертав їх явно, з підписом чому**:

| стенд | було | стало |
|---|---|---|
| `board-card-row` | «усі заголовки капсом» · «капс має міжлітерний просвіт» · «заголовок важчий за опис ≥800» | «заголовки НЕ капсом» · критерій ваги лишився ВІДНОСНИМ, планка 600 |
| `ad-sheet-swipe` | «назва КАПСОМ» · «капсом рівно одна річ» | «назва НЕ капсом» · «капсу немає ЗОВСІМ» (сильніша вимога — нуль замість одного) |

🔴 **Дві справжні вади, які знайшли самі стенди, а не око:**

1. **`board-card-row`: розкид висот карток 47px при стелі 45.** Я підняв
   `.bd-ad-title` 16 → 17px, і це здавалось безкоштовним — а воно розігнало висоти.
   **Повернув 16px:** сенс правки (без капсу, вага 600) досягається без підняття кегля.
2. **`ad-sheet-swipe`: назва і ціна зрівнялись у 22px.** Правило «заголовок —
   найбільший елемент екрана завжди» стояло в CSS ще з 02.08, і зламалось МОВЧКИ, самим
   лише рухом іншого числа: аудит опустив назву 23 → 22 (знявши капс), а ціна лишилась
   22. **Лікує ціна 22 → 20px** — тримати треба ПАРУ, а не число.

✅ Обидва стенди зелені: **22/22** і **55/55**.

---

## 9. 🛠 Кроки 5, 6, 7 — за уточненням Вови («черга у звіті 1-7»)

🗣️ На пряме питання, на який список він дивився, Вова відповів: **«Черга у звіті
1-7 — роби 5, 6, 7»**. Тобто зроблено 1-3, пропущено 4 (фото автобуса), взято 5-7.

### 🔴 СПЕРШУ — ДВІ РЕГРЕСІЇ ВІД МОЄЇ Ж РОБОТИ, які знайшов повний прогін

1. **`news-seen`: «Громада ще й ЩІЛЬНІША за вагою — 700 → 700».** Виділення Громади
   в підписі розділу Вова замовляв 31.08, і воно тримається ТОНОМ І ВАГОЮ: було
   700 проти 800. Мій крок 1 зняв ваги 800 з усього застосунку — і пара мовчки
   зрівнялась, тобто **половина замовленого виділення зникла без жодної правки в
   тому блоці**. 🔑 Крок повернуто ВНИЗ, а не вгору: тихий розділ **500**, Громада
   лишається 700. Різниця та сама, гучності в застосунку поменшало.
2. **`sidebar-menu`: «тінь підвалу спрямована ВГОРУ, а шапки — ВНИЗ».** Тут винен
   був СТОРОЖ: він тримався за точні пікселі (`/-4px/` і `/0px 4px/`) замість своєї
   ж вимоги про НАПРЯМОК. Шапка дістала двошарову `--elev-2` — обидва шари вниз,
   вимога ціла, сторож червоний. Критерій переписано на **знак вертикального
   зсуву** кожного шару. ⚠️ Заразом власна помилка в регулярці: шар тіні це
   три-чотири довжини поспіль, і «кожна друга» читала пару «розмиття + розтяг» як
   ще один шар зі зсувом 0.

### Крок 6 — тумблери (найдешевший, зроблено першим)

`.acc-tog` `#2E9E5B` → `var(--brand-grad-sm)`. Зелень була **єдиною зеленою
поверхнею всього кабінету** і нічого не означала — це просто типовий колір
перемикача iOS. 🔑 Береться градієнт, а не `--red`: правило проєкту каже, що
`--red` — колір ТЕКСТУ й тонких іконок, а площини бренду малює градієнт.
⚠️ Вимкнений стан лишається нейтральним сірим навмисно.

### Крок 5 — зони дотику 44×44

📐 Заміряно ДО на семи найчастіших порушниках: **влучань 1 із 5** у кожного.
Стало **5 із 5** у всіх семи. Вигляд не змінився ніде — росте лише невидима
накладка `::after` 44×44 без фону й рамки.

🛑 **Псевдоелемент, а не `padding` + `background-clip`** (прийом, яким уже
розширені круглі кнопки оголошення): ці сім живуть У РЯДКАХ — у шапці, у смузі
дій допису, на картці Дошки. Виріс `padding` → поїхала розкладка сусідів.
Накладка `absolute` місця не займає взагалі.

🔴 **Зустрічна перевірка знайшла справжню ваду, якої я не шукав:** центри закладки
і бургера в шапці стояли за **41px** один від одного, а дві цілі по 44 за такої
відстані **фізично мусять перетнутись** — бургер забирав два дотики з пʼяти в
закладки. Тобто «розширив зону» тут означало б «украв сусідову», а це ГІРШЕ за
дрібну ціль: людина влучає, і відкривається не те. Лікує `gap` шапки 8 → **11px**,
і число **виведено** (44 − 14 − 19), а не підібрано.

### 🛑 МОЯ ПОМИЛКА, яку варто прочитати наступній сесії

Я створив стенд командою `cat > tests/tap-targets.mjs` — **а файл із такою назвою
вже існував у репозиторії з 09.08** (122 рядки, зони дотику крапок Автобусів). Я
його мовчки затер. Помітив лише тому, що почав звіряти кількість стендів і числа
не зійшлись.

✅ Оригінал відновлено **байт-у-байт** (`git checkout HEAD --`, звірено `diff`),
працює 5/5. Моя робота переїхала в `tests/tap-targets-hig.mjs`, і обидва файли
тепер посилаються один на одного з поясненням, ЧОМУ їх два: у крапок тижня
Автобусів крок 11px, і 44×44 там недосяжні В ПРИНЦИПІ — той сторож це чесно
називає і має НИЖЧУ ціль навмисно. Два різні критерії в одному файлі читались би
як суперечність.

➡️ **Урок:** `cat >` мовчки затирає. Перед створенням файлу — `ls` або
`git ls-files`, навіть коли впевнений, що назва вільна.

### Крок 7 — привітання Громади в один рядок

`.hm-hi` `700 25px/1.14` + `line-clamp: 2` → **`500 17px/1.2` + `line-clamp: 1`**.

📐 **Заміряно на одній і тій самій сцені, до і після:** коробка привітання
**292×57 → 292×20**; погода 173 → **144**; капсули 276 → **247**; перша секція
392 → **363**; уся сторінка 1713 → **1684**.

🔴 **ЧЕСНО ПРО РЕЗУЛЬТАТ — звіт обіцяв більше, ніж вийшло.** У ньому написано «дія
піднімається у видиму зону». Насправді виграш **29px**: блок «Актуальний збір»
починався на 392 і був видимий і до правки. Правка робить видимим на 29px більше
його, а не «вводить дію у перший екран». Число мале, але справжнє — і краще, щоб
воно стояло тут, ніж щоб наступна сесія шукала обіцяний стрибок.

⚠️ `line-clamp: 1`, а не `nowrap`: довге імʼя з кабінету мусить обрізатись
трикрапкою. Інакше вся економія зникла б на першому ж довгому імені — і не в мене
на сцені, а в живої людини.

### 🆕 Сторож `tests/tap-targets-hig.mjs` — 12/12, контроль `CSS_REV=origin/main` → **5/12**

Прилад — `tests/tools/home-first-screen.mjs` (що встигає на перший екран Громади).

---

## 10. 🎨 Правки після перегляду на телефоні (04.09, PR #1144 уже на проді)

🗣️ **Відгук Вови:** «в принципі ти реалізував це дуже круто. Це вже тепер виглядає
трохи преміальніше, а не якийсь такий застосунок дитячий». Далі — дві конкретні
претензії.

### 1. Привітання в шапці Громади — 17/500 → 18/600

🗣️ Дослівно: «треба трішки збільшити, тому що в нас є місце справа, і воно так ледве
помітно, а це має кидатися трішки відразу в очі, тобто що це персональне таке
вітання… трішки збільшити, не сильно збільшити, так як було це раніше».

Вимога рівно посередині: помітніше за 17/500, але **не назад** до 25/700 у два рядки
(це те, що ми щойно прибрали кроком 7).

📐 **СТЕЛЮ ЗАМІРЯНО, А НЕ ВГАДАНО.** Спершу поставив 20px — і клон показав, що
«Добрий ранок, Володимир» дає **306px при коробці 292**, тобто **імʼя обрізало б
трикрапкою**. Рівно те, заради чого привітання й існує. Відкинуто виміром, не оком.
На 18px те саме привітання **275px**, найдовші перевірені варіанти 262-275px, запас
~17px. Чинне — **18px/600**.

🔴 **ПРИЛАД СПЕРШУ ЗБРЕХАВ.** Я міряв `scrollWidth` проти `clientWidth` — обидва
дали рівно 292. Причина: у `-webkit-box` із `line-clamp` `scrollWidth` **завжди**
дорівнює ширині коробки, тобто цією міркою обрізку не побачити НІКОЛИ. Природну
ширину дає лише клон-`<span>` із `white-space: nowrap` і тим самим шрифтом.

⚠️ **І поправка до самого спостереження Вови:** «місця справа» майже немає —
праворуч стоїть кнопка кабінету, у рядку лишається 292px. Порожнеча читалась через
**дрібний і тихий** кегль, тож лікує її передусім ВАГА (500 → 600), а не ширина.

### 2. «ГРОМАДА» в таб-барі великими літерами

🗣️ Дослівно: «в таб-барі, там, де знаходиться вкладка Громада, треба всі букви
великими буквами написати».

🛑 Це **виняток із правила аудиту 03.09** «капс лише на дрібних мітках», і виняток
названий прямо в коді: рішення власника правило аудиту не скасовує. 🔑 Формально
суперечності майже немає — межа аудиту стоїть на **13px**, а підпис таба 11px, тобто
це і є «дрібна мітка»; капс тут працює як позначка ЦЕНТРАЛЬНОЇ кнопки.

⚠️ `letter-spacing` лишається **0** свідомо: великі літери зазвичай просять просвіту,
але підпис стоїть у ряду з чотирма сусідами фіксованої ширини, і будь-який додатний
трекінг штовхає слово за межі клітинки.

### 🔑 Сторож став СИЛЬНІШИМ, а не слабшим

Перевірка «підпис таб-бару не капсом» суперечила замовленню, тож її **перевернуто
явно**. Але раніше вона питала лише **перший** підпис — тепер обходить **усі пʼять** і
вимагає, щоб капс був **рівно на одному, центральному**. Тобто червоніють обидві
помилки: і «капсу на Громаді немає» (мовчки скасоване замовлення), і «капс поповз на
сусідів» (повернення того, що аудит прибирав).

`design-system` **17/17** (було 16).

---

## 11. 🪪 СТАРЕ ІМʼЯ НА ПЕРШОМУ КАДРІ — ДВА ДЖЕРЕЛА ПРАВДИ (B-27 знову)

🗣️ **Скарга Вови:** «заходжу в додаток… спочатку підгружає зверху: Добрий день і
англійською мовою Володимир. Це було моє колись старе ім'я. Я його замінив ще десь
місяць назад… І без фотографії. Тобто воно так секунду-дві і підгружає моє нове
ім'я». І окремо, як вимога до якості: **«не просто латкою… потрібно виправити
максимально технічно»**.

### Причина — не мережа, а ДРУГЕ ДЖЕРЕЛО

| джерело | де лежить | швидкість |
|---|---|---|
| `profiles.name` — те, що людина справді вписала | у базі | запит по мережі, 1-2с |
| `user_metadata.name` від Google — знімок при ПЕРШОМУ вході | у сесії **на пристрої** | миттєво |

`_profileName` (`core/auth.js:16`) — **звичайна змінна модуля**, вона вмирає з кожним
повним закриттям застосунку. Тож на холодному старті вона порожня, `currentUserName()`
падає на метадані — і перший кадр малює імʼя, змінене місяць тому. `_profileAvatar`
так само лише в памʼяті, звідси й «без фотографії».

А метадані застигли тому, що **перейменування пише ТІЛЬКИ в `profiles`** — оновлювати
метадані нікому й ніколи.

🔑 Це рівно клас **B-27** — «два лічильники того самого стану», від якого проєкт уже
страждав. Тому лікування не «показати щось інше», а **прибрати друге джерело**.

### Що зроблено

**(А) Миттєве джерело стало КЕШЕМ ПРАВДИ.** `profiles` дзеркалиться в `localStorage`
під `cstl_profile:<uid>`; пишеться в `getProfile()` (правда з бази) і в `saveProfile()`
(перейменування доїжджає одразу — без цього рядка весь фікс був би латкою: у памʼяті
нове імʼя, на пристрої старе, і наступний старт показав би ту саму ваду вже з нашого
власного кешу). Знімається у `signOut()` — **до** обнулення `_user`, інакше ключа вже
не порахувати.

**(Б) Метадані вийшли з ланцюжка** для того, чий профіль уже відомий. Їхня законна
роль лишилась одна — передзаповнити «Доповніть профіль» при ПЕРШОМУ вході, коли
рядка в `profiles` ще не існує.

⚠️ Ключ **обовʼязково з `uid`** — правило `account-scope`: інакше другий акаунт на
тому самому телефоні побачив би чуже імʼя. Гостьового простору (`:anon`) тут немає і
бути не може: без входу немає чийого імені кешувати.

⚠️ В `currentAvatarUrl()` кеш питається **лише поки профіль не прочитано взагалі**:
`_profileAvatar === null` це ОСМИСЛЕНЕ «фото немає», і без цієї умови свіже
«фото видалили» перебивалось би старим кешем.

### 🛑 Чесна межа, записана окремою перевіркою

На пристрої, де кешу ще **немає** (перший запуск після цієї зміни або новий телефон),
старе імʼя один раз усе одно зʼявиться. Інакше довелось би показати **порожнє
привітання** людині, яка щойно зареєструвалась і в якої `profiles` ще не існує.
Перевірка №2 у стенді стоїть зеленою на ОБОХ ревізіях навмисно — вона фіксує межу,
а не досягнення.

### 🆕 Сторож `tests/profile-first-paint.mjs` — 8/8, контроль `BUNDLE_REV=origin/main` → 5/8

🔑 Міряє **перший кадр**, а не наявність коду: перевірка «у `auth.js` є localStorage»
зеленіла б і тоді, коли кеш пишеться, але не читається. Відповідь `profiles` навмисно
**сповільнена** (`slow: { profiles: 2500 }`) — без затримки розриву не існує, і навіть
зламаний код «встигав» би показати правильне імʼя. Контекст браузера **один** на
прогін, бо саме він тримає `localStorage` між завантаженнями.

🛑 Окрема перевірка сіє запис під **чужим** uid і вимагає, щоб привітання його
проігнорувало.

### ⚠️ Це було не тільки у привітанні

`currentUserName()` читають ще пʼять місць: картка профілю в бургер-меню, підпис
оголошення, автор питання, підпис коментаря. Тобто допис, надісланий у перші 1-2
секунди після холодного старту, міг піти в базу підписаний **старим імʼям**.
У `feed.js` від цього був окремий захист (перепитує імʼя після `authReady()`), у решті
— не було. Тепер джерело одне, і захист більше ні від чого рятувати.

### 🛑 РЕГРЕСІЯ ВІД ЦЬОГО Ж ФІКСА, ЯКА ДОЇХАЛА ДО ПРОДУ (04.09)

Перша редакція вимикала метадані Google **за самою наявністю запису в кеші**:
`if (кеш) return кеш.name || 'Житель'`. А `getProfile()` пише запис і тоді, коли
рядка в `profiles` **немає** (`{name: null}`) — тобто людина, яка щойно
зареєструвалась і ще не заповнила анкету, діставала **«Житель»** замість свого
Google-імені. Я вбив рівно той випадок, заради якого метадані й лишались у
ланцюжку.

🔑 **Правильне правило:** кеш перебиває метадані **лише коли в ньому Є ІМʼЯ**.
Запис без імені — це факт «профіль без імені», і для нього метадані і є найкраща
відповідь.

🔴 **Спіймав `sidebar-menu` (48/49) у повному прогоні — уже ПІСЛЯ деплою.** Причина
не в поспіху з публікацією, а у виборі, які стенди перезапустити: після зміни
`auth.js` я прогнав `live-profile`, `account-scope`, `sidebar-account`,
`account-cabinet`, а `sidebar-menu` перевіряв **раніше**, до правки — хоча він
читає те саме `currentUserName()`.

➡️ **Урок:** міняєш функцію — перезапускай усіх її ЧИТАЧІВ (`grep` по імені
функції), а не тих, чиї назви звучать доречно.

✅ У сторож додано перевірку **саме на цей випадок** (порожній профіль не глушить
імʼя входу) → **9/9**, контроль **6/9**.

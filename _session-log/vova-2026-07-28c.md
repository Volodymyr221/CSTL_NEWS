# Журнал сесії — 28.07.2026 (сесія L)

**Гілка:** `claude/startuem-yxqfd9` · **Production:** `main`
**Стартовий стан:** HEAD = `ad7aa917` = `origin/main` (0/0 ahead/behind).
**CACHE_NAME на старті:** `cstl-20260728-1203` (з хендовера сесії K).

---

## Крок 0 — старт сесії (`/startuem`)

- `git config user.name "Вова"` + `user.email vitocorleone191@outlook.com` — виконано.
- `git fetch origin` — виконано. Гілка сесії збігається з `origin/main` рівно.
  ⚠️ Попередження SessionStart-хука «локальний main відстає на 50 комітів» стосується
  локального ref `main`, а не HEAD — сама робоча гілка свіжа.
- `BYYOU_PLAN.md` — статус `idle`, незавершеного потоку немає. Пересідати нікуди не треба.
- Прочитано: `HOT_RULES.md` · `START_HERE.md` · `SESSION_STATE_VOVA.md` (хендовер K + J) ·
  `AUDIT_2026-07.md` · `CSTL_BUGS.md` · `CLAUDE.md` · `ВОВА_ПРОФІЛЬ.md` · `BYYOU_PLAN.md`.
- Заведено цей журнал.

**Статус:** чекаю «Роби» від Вови. Код не чіпав.

---

## Хронологія

### «Роби» → Дошка, потік 2 (ціна + анатомія картки)

Вова сказав «Роби» без уточнення задачі → перепитав через вибір. Відповідь: **«Дошка,
потік 2»**.

**Крок 1 — анатомія картки** (коміт `3f2c2b5d`), пункти 2-3 плану:
- `src/tabs/board.js` · `renderBoardCard` — прибрано `<p class="cm-board-text">` (опис).
  ⚠️ Лишено запобіжник: у постів БЕЗ заголовка (старі, до Д-16 від 08.07) замість
  заголовка показуємо текст — інакше картка була б порожня.
- `renderCardFoot(p, {actions})` — кнопки 📞/💬 з картки прибрано (були 34px, менше за
  мінімум Apple HIG 44px; конкурували з тапом «відкрити»; робили картки нерівними).
  Прапорець лишає стару поведінку доступною.
- `renderContactBar(p)` — нова широка кнопка «Написати» (44px) + дзвінок у зум-модалці.
- `renderPrice(p)` — ціна НАД заголовком, важча за нього (17px/800 проти 15px/700).
- `style/community.css` — `.cm-board-price`, `.cm-board-contact-bar`, `.cm-board-write-btn`.
  Футер модалки став дворядковим через `flex-wrap` + `flex-basis:100%` — щоб НЕ переписувати
  робочий верхній рядок (перехід на grid переклав би обидва блоки).
  ⚠️ Селектор кнопки дзвінка навмисно на 4 рівні: вище у файлі лежить правило
  `.cm-board-modal-note .cm-board-modal-foot .cm-board-call { width: 42px }` (0,3,0) —
  без четвертого рівня воно б виграло і кнопка лишилась би 42px замість 44px.
- `src/tabs/community-modal.js` — прев'ю подачі теж без опису (Д-6: прев'ю = реальна картка).
- `CACHE_NAME` → `cstl-20260728-1254`.

**Крок 2 — ціна** (коміт `e33f0fb1`), пункт 1 плану:
- 🔑 **Знахідка: колонки `posts.price` + `posts.currency` УЖЕ ІСНУЮТЬ** у схемі
  (`scripts/supabase_schema.sql:72`) — план вважав це «🔴 колонка в базі», а насправді
  нової колонки не треба. Проблема була інша: обидві RPC їх ігнорували.
- `scripts/supabase_reputation.sql` (`submit_board_post`) і `scripts/supabase_board_edit.sql`
  (`update_board_post`) — навчено писати price/currency + серверна валідація (регулярка
  на формат, стеля 100 млн, білий список валют). Валідація саме на сервері, бо форму
  можна обійти прямим викликом API.
  ⚠️ У `update_board_post` розрізняються ТРИ випадки, а не два: ключа `price` нема →
  ціну не чіпаємо; ключ є, порожній → ціну знято; ключ є, число → нова ціна. Без цього
  будь-яке редагування старого поста мовчки стирало б ціну.
- Тіла функцій правлені **в оригінальних файлах**, окремої копії в новому файлі свідомо
  НЕ робив — дві копії вже коштували проєкту розʼїзду списків антиспаму.
- `src/core/utils.js` · `formatPrice(price, currency)` — ОДИН форматувальник на застосунок.
  Чому в utils, а не в board.js: потрібен і формі подачі, а `board.js` уже імпортує
  `community-modal.js` → зворотний імпорт дав би коло.
- Поле «Ціна» у формі: лише цифри + одна крапка (2 знаки), кома→крапка (iOS-клавіатура
  дає українську кому, база чекає крапку). Чистка просто в полі, щоб людина бачила, що
  саме збережеться.
- Перевірено прогоном `formatPrice` у node: `2500 → «2 500 ₴»` · `0 → «Безкоштовно»` ·
  `null/''/-5/'abc' → ''` (рядок не малюється) · `1234567 → «1 234 567 ₴»` · USD/EUR.

**Стенди:** `npm test` — **19/19 стендів, 227/227 перевірок** зелені після обох кроків.
Заразом звірив число з хендовера сесії K — воно правдиве (я його не брав на віру).

### 🔴 Блокер, який я НЕ можу зняти сам
Supabase MCP у цій сесії **не авторизований** (сесія неінтерактивна, OAuth недоступний) —
міграцію накотити не можу. **Обидва SQL-файли мусить накотити Вова ПЕРЕД деплоєм**,
інакше ціна введеться, але мовчки не збережеться (стара RPC просто ігнорує зайвий ключ
у payload — помилки не буде, що гірше за помилку).

### ⚠️ Знайдено і НЕ чіпав (чекає рішення Вови)
- **Пункт 4 плану «заголовок без капсу» — предмета нема.** `.cm-board-title` капсу не має
  (`community.css:1420`, лише `font-weight: 700`). Капс на картці носять ДВА інші місця:
  пігулка категорії (`.cm-board-cat`) і локація (`.cm-board-loc`) — а локація великими це
  **пряме рішення Вови від 09.07 (Д-20)**. Міняти його «заодно» не став.
- Прев'ю подачі більше не показує набраний опис (це вірно за Д-6, але людина набирає
  опис і не бачить його у прев'ю) — сказати Вові, чи це ок.
- Пункти 5-7 плану (сортування · FAB на «+» · онбординг) — не починав.

### `/finish` (13:09 UTC)
- Вова: **«Фініш»** — після того, як я прямо попередив про ненакатану міграцію.
  Тобто рішення деплоїти прийнято свідомо; блокер винесено у хендовер великим шрифтом.
- `CACHE_NAME` → `cstl-20260728-1309` (перебито з `-1254`: після того бампу був ще один
  коміт із кодом — ціна — і кеш мусить відповідати найсвіжішій збірці).
- `git merge origin/main` — `Already up to date`, конфліктів немає.
- `node build.js` — `bundle.js` перезібрано, `check-imports` 40 файлів чисто.
- `npm test` — 19/19 стендів, 227/227 перевірок.
- `SESSION_STATE_VOVA.md` — додано хендовер сесії L першим блоком, K посунуто нижче.
<<<<<<< HEAD
- **PR #697** → squash-мердж у `main`, мердж-коміт `1d032785`.
- **Деплой run 3481 — success ✅** (13:14 UTC). Попередній стан прода був run 3480.
  ⚠️ Як і в попередніх сесіях, це підтвердження зі **статусу GitHub Actions**, а не з живого
  лічильника версії: GitHub Pages із пісочниці недосяжний. Як воно виглядає на iPhone —
  може сказати лише Вова.

### ⚠️ ІНЦИДЕНТ на фініші — локальний `main`
Пішов дописати номер деплою в документацію, зробив `git checkout main` + `git pull` —
**pull провалився** (`fatal: Need to specify how to reconcile divergent branches`), і робоче
дерево лишилось на СТАРОМУ локальному `main` від 25.07. На кілька хвилин виглядало так,
ніби роботи сесії немає (у `sw.js` стояв `CACHE_NAME` від 25.07).
- **Втрати нема:** робота вже лежала і в `origin/main` (мердж `1d032785`), і в гілці
  `origin/claude/startuem-yxqfd9`. Пропав лише вміст робочого дерева, і то тимчасово.
- **Причина — давня, не з цієї сесії:** локальний `main` розійшовся з `origin/main`
  (50 проти 51 коміта). Локальні 50 — це коміти від 25.07, чий ВМІСТ є в `origin/main`,
  але з іншими SHA (наслідок squash-мерджів). Саме про це хук `SessionStart` попереджав
  на самому старті: «локальний main ВІДСТАЄ на 50 комітів, origin = джерело правди».
- **Виправлення:** `git checkout` назад на робочу гілку — дерево відновилось повністю
  (звірено: `CACHE_NAME = cstl-20260728-1309`, `formatPrice` на місці в усіх трьох файлах).
- **НЕ виправлено свідомо:** запропонував `git reset --hard origin/main` для локального
  `main` — **Вова відмовив у дозволі**. Лишив як є; на сервер і деплой це не впливає,
  але хук скаржитиметься й наступної сесії. **Рішення за Вовою.**
- **Урок:** не ходити на `main` заради косметичної дописки в документи. Робочої гілки
  і `origin` достатньо; локальний `main` у цьому проєкті — відома пастка.

---

## Живий фідбек Вови після деплою (скріншоти IMG_3707 + FullSizeRender)

Три пункти, усі про зум-модалку оголошення. Спершу перепитав, бо «задуже широкий» читалось
двозначно (висота чи ширина) — **Вова: «1. Занадто високий»**, тобто висота. Добре, що
перепитав: я мало не пішов звужувати кнопку «Написати».

### 1. Футер занадто високий → компактніший (`style/community.css`)
| Що | Було | Стало |
|---|---|---|
| Відступи футера | `14px 20px 16px` | `10px 20px 12px` |
| Зсув кнопок 🔖/↗ (`margin-top`+`padding-top`) | 5+6 = 11px | 0 |
| Проміжок до смуги контакту | 10px | 7px |
| Ім'я автора в модалці | 11px | **13px** |
- Разом панель нижча на **≈22px**, і рівно на стільки ж її верхній край їде ВНИЗ —
  це і є «блок трошки опустити»: опис більше не обрізається на півслові.
- Кнопки лишив **44px**. Вова просив компактніше, але 44px — мінімальна тап-ціль за
  Apple HIG, і саме через 34px ми ці кнопки сьогодні прибрали з картки. Зменшити їх
  зараз = повернути щойно виправлену проблему. Сказав це прямо, висоту виграв відступами.
- Ім'я в модалці — фіксовані 13px окремим правилом. На картці розміром керує JS
  `fitBoardAuthors` (8.5–12.5px під ширину, бо там тіснились кнопки); у модалці кнопок
  поряд уже нема, підганяти нема під що.

### 2. **Вова: «Прибери»** — лінію-роздільник над 🔖/↗ видалено
Це був `::before` із затухаючим градієнтом. Відділяв кнопки від рядка «ім'я / телефон»,
поки футер був ОДНИМ рядком (мердж 11.07). Після переїзду контакту у власну смугу футер
став дворядковим — лінія опинилась усередині одного рядка й не розділяла вже нічого.
Разом з нею знято зсув 5+6px, який тримав під неї місце.

### 3. Тінь під липким блоком (категорія · ціна · заголовок) — з'являється плавно
**Вова дослівно:** «коли вже фіксується цей верхній блок і починає текст скролитись під
цей блок, то тоді вона має з'являтися плавно… тому що зараз вона виглядає негарно».
- Було: `box-shadow` прибитий у сам блок → тінь висіла ЗАВЖДИ, навіть коли під блоком
  нічого не проїжджає і відділяти нема чого.
- Стало: `attachSubheadShadow(modal)` у `board.js` ставить клас `.is-stuck`, плавність
  дає CSS `transition: box-shadow 0.22s`. Розмір самої тіні НЕ чіпав (`0 3px 5px -3px`
  / 12% — підібрано Вовою 11.07), змінилась лише її наявність.
- Підключено в ОБИДВОХ місцях, де народжується модалка: `expand()` (тап по картці) і
  `openAdModalStandalone()` (відкриття з чату). Одне місце = баг у половині випадків.
- ⚠️ **Тонкий випадок, через який умови ДВІ, а не одна:** у оголошення БЕЗ фото липкий
  блок стоїть угорі від самого початку, тобто «прилип» уже при нульовій прокрутці —
  перевірка лише на «уперся у верх» показала б тінь одразу, тобто рівно той баг, який
  лагодимо. Тому друга умова `scrollTop > 2`.
- `border-bottom-color: transparent` замість зняття рамки — щоб не поїхала розкладка
  (1px висоти лишається на місці).
- Запобіжник у CSS: базовий `box-shadow` із нульовою альфою стоїть ПІСЛЯ звичайного
  оголошення рамки, тобто якщо браузер чогось не зрозуміє — поведінка відкотиться до
  старої (тінь просто буде завжди), а не зникне зовсім.

### 🧪 Новий стенд №20 — `tests/board-subhead-shadow.mjs` (10 перевірок)
Міряє **альфу тіні** (те, що бачить око), а не наявність класу і не форму запису CSS.
- Перед КОЖНИМ виміром чекає 400мс — інакше міряв би перший кадр переходу 0.22s
  (та сама пастка, на якій 28.07 уже падав `board-header`).
- Окремо перевіряє випадок БЕЗ ФОТО і проміжну точку «прокрутили 60px, блок ще не прилип».
- Плюс сторож проводки: `attachSubheadShadow` мусить згадуватись у `board.js` ≥3 рази
  (оголошення + два виклики) — ловить «підключили лише в одному місці з двох».
- 🔴 **КОНТРОЛЬ ЗРОБЛЕНО (не на віру):** тимчасово повернув стару постійну тінь →
  стенд дав **5/10, червоніють рівно ті 5 перевірок, що описують скаргу Вови**. Після
  відновлення — знову 10/10. Тобто стенд уміє червоніти, а зелене світло щось означає.
- ⚠️ **Чесне обмеження, записане в шапку стенда:** умову «блок прилип» стенд відтворює
  СВОЄЮ копією, бо `board.js` тягне пів застосунку і в голий браузер не імпортується.
  Тобто перевіряється ПРИЙОМ, а не проводка (її тримає сторож присутності). Якщо колись
  логіку в board.js змінять, а копію тут ні — сторож не врятує.

**Разом:** `npm test` — **20/20 стендів, 237/237 перевірок**. `CACHE_NAME` → `cstl-20260728-1335`.

### 4. Модалка ширша (Вова: «саму по собі модалку можна трішки розширити»)
`.cm-board-modal-note` · `width: min(88vw, 360px)` → **`min(92vw, 400px)`**.
⚠️ **Тісно робила СТЕЛЯ 360px, а не 88vw** — на великому iPhone (430px) 88vw дає 378px,
тобто стеля вигравала і лишала по 35px порожнечі з боків. Якби підняв лише відсоток
(88vw → 92vw), стеля з'їла б увесь виграш і візуально нічого б не змінилось. Тому
підняв обидва числа.

| Екран | Було | Стало | Поля з боків |
|---|---|---|---|
| 430px (15 Pro Max — телефон Вови) | 360px (83.7%) | **396px (92%)** | 35 → 17px |
| 393px (15/14 Pro) | 346px (88%) | 362px (92%) | 24 → 16px |
| 375px (SE / 13 mini) | 330px (88%) | 345px (92%) | 22 → 15px |

Ширина тексту всередині (padding 20px з боків): на телефоні Вови **320 → 356px**.
Ширина одна на обидва випадки відкриття (тап по картці і відкриття з чату) —
`.cm-board-modal--sheet` ширину не перекриває, тому правка в одному місці.

`npm test` — 20/20 стендів, 237/237. `CACHE_NAME` → `cstl-20260728-1341`.
=======
>>>>>>> origin/main

---

## `/finish` №2 (13:42 UTC) — друга хвиля на прод

- Вова: **«Фініш»** після чотирьох доробок модалки.
- `CACHE_NAME` уже `cstl-20260728-1341` (виставлений хвилину тому разом із зміною ширини) —
  код відтоді не мінявся, тож повторно не бампав.
- ⚠️ **`git merge origin/main` дав КОНФЛІКТИ** у `sw.js` і `style/community.css`.
  **Причина не помилка, а нормальний наслідок squash-мерджу:** PR #697 пішов у `main`
  одним склеєним комітом, тобто той самий вміст прийшов туди ІНШИМ комітом, ніж лежить
  у гілці. Далі обидві сторони змінили ті самі рядки → git не може вибрати сам.
  - `sw.js`: `CACHE_NAME` — гілка `1341` проти main `1309` → лишив **1341** (новіший).
  - `community.css`: `margin-top` смуги контакту — гілка `7px` проти main `10px` →
    лишив **7px** (це і є компактування футера).
  - `-X theirs` НЕ використовував (заборонено правилами проєкту, і тут би мовчки
    відкотив обидві правки).
  - Після злиття: маркерів конфлікту нема, `node --check` чисто, `node build.js` чисто,
    `npm test` — **20/20 стендів, 237/237 перевірок**.
- Оновлено `SESSION_STATE_VOVA.md` (друга хвиля + два нових уроки в блок «головне про код»)
  і `CLAUDE.md` (рядок `tests/`: 19/227 → **20/237**).
<<<<<<< HEAD

---

## 5. 🔴 НАПИСИ ДОШКИ — ЗАЛИШКИ ВІД ТЕМНОГО КОРКА (скріншоти IMG_3711/3712)

**Вова:** «той колір тексту він не адаптований під цей фон… зробити що один світліший,
другий темніший, але вже темніші їх зробити… Всі надписи на дошці потрібно стандартизувати».

**Діагноз — це не «недогледіли колір», це ХВІСТ ЗМІНИ ФОНУ 28.07 (потік 1).**
Коли Дошка мала темний корок, написи були кремові зі світлим ореолом — правильно.
Корок зняли (фон став `--board-bg: #E1E5EB`), а написи лишились світлими.
**Заміряний контраст: 1.02–1.08:1 при нормі WCAG AA 4.5:1** (1.00 = повний збіг із фоном,
тобто напису для ока фактично не існувало).

| Напис | Було | Стало | Токен |
|---|---|---|---|
| «В розділі X оголошень не знайдено» | **1.02:1** | **12.00:1** | `--ink` |
| «Перегляньте всі оголошення громади» | **1.08:1** | **5.83:1** | `--ink-soft` |
| Заголовок групи (ЖОРНИЩЕ / ОЛИЦЬКА ГРОМАДА) | **1.04:1** | **5.83:1** | `--ink-soft` |
| Порожній стан Дошки (`.bd-empty`) | **1.02:1** | **5.83:1** | `--ink-soft` |
| Лінії обабіч заголовка групи | невидимі | `rgba(95,84,72,0.30)` | — |

- **Ієрархію збережено, як просив Вова:** факт («немає оголошень») — найтемніший 12:1,
  підказка під ним — світліша 5.83:1, але вже в ТЕМНОМУ діапазоні.
- **Темний ореол (`text-shadow: 0 1px 3px rgba(0,0,0,0.55)`) знято з усіх трьох місць** —
  під ТЕМНИМ текстом на світлому фоні він лише каламутить літери.
- Взято **токени проєкту** (`--ink`, `--ink-soft`), а не нові hex — те саме правило, що
  для фонів («додаєш колір — бери токен»).

### 🔍 Зроблено ПОВНИЙ обхід, а не точковий фікс
Прогнав скриптом усі правила `color:` у селекторах `.bd-*` / `#board-content` / `.board-bg` /
`.cm-board-note` і порахував контраст кожного проти `--board-bg`.
- ⚠️ **`.cmbw-*` (віджет Дошки в Громаді) НЕ чіпав** — він лишається ТЕМНИМ за рішенням
  Вови від 28.07, там кремовий текст правильний. Це рівно та пастка, де «стандартизувати
  все» зламало б навмисне рішення.
- ⚠️ **Один збіг виявився ХИБНИМ і його врятувала перевірка руками:**
  `.bd-msg-group--mine .bd-msg-time` = `rgba(255,255,255,0.7)`, контраст проти фону Дошки
  1.26:1. Але цей текст лежить на **бордовій бульбашці** чату (`background: var(--red)`),
  а не на фоні Дошки — там білий правильний. Моя мірка міряла його проти НЕ ТОГО фону.
  Той самий урок, що переслідує проєкт: критерій має міряти те, що бачить око.
  Якби «виправив» за числом — зіпсував би робочий чат.

`npm test` — 20/20 стендів, 237/237. `CACHE_NAME` → `cstl-20260728-1353`.
=======
>>>>>>> origin/main

---

## `/finish` №3 (13:56 UTC) — третя хвиля на прод

- Вова: **«Фініш»**.
- `CACHE_NAME` уже `cstl-20260728-1353` (виставлений разом із правкою написів).
- ⚠️ **Знову конфлікт у `sw.js`** при `git merge origin/main` — той самий механізм, що й
  минулого разу: PR #698 пішов у `main` squash-ом, тобто той самий вміст прийшов туди
  іншим комітом. `CACHE_NAME`: гілка `1353` проти main `1341` → лишив **1353**.
  `style/community.css` цього разу злився САМ (auto-merging), без конфлікту.
  📌 **Це вже закономірність, а не випадковість:** при двогілковому потоці зі squash-мерджем
  `sw.js` конфліктуватиме на КОЖНОМУ повторному `/finish` у межах однієї сесії, бо
  `CACHE_NAME` — єдиний рядок, який змінюють обидві сторони. Рішення завжди одне:
  лишити НОВІШИЙ час. (Це рівно та задача, яку описує СТ-7 в аудиті — «генерувати
  `CACHE_NAME` у CI»; поки не зроблено, конфлікт руками.)
- Після злиття: маркерів нема, `node --check` чисто, `node build.js` чисто,
  `npm test` — **20/20 стендів, 237/237 перевірок**.
- Деплой попереднього PR #698 підтверджено: **run 3482 success ✅**.
- Оновлено `SESSION_STATE_VOVA.md` — додано третю хвилю + урок «зміна фону це хвіст».

---

## 6. 🔴 ЗВІРКА З ЖИВОЮ БАЗОЮ — Я БУВ НЕПРАВИЙ ПРО ЦІНУ

Вова підключив Supabase-конектор («Супабейс підключена») — щоб я накотив міграцію,
про яку попереджав чотири рази. **Накат НЕ відбувся, і це правильно.**

### Що виявилось при звірці ПЕРЕД накатом
1. `submit_board_post` і `update_board_post` у базі **вже містять price/currency**.
2. `list_migrations`: **`board_price_support_submit` (20260728122041)** і
   **`board_price_support_update` (20260728122127)** — накатані **12:20-12:21 UTC**,
   тобто **ДО старту моєї сесії** (я стартував ~12:45).
3. Живі дані: пост **id 53 «Продаю машину», price = 1500.00, currency = UAH,
   створений 13:24** — тобто ціна зберігалась уже кілька годин.

### 🔴 МОЯ ПОМИЛКА, ЧЕСНО
Я читав SQL-**файли в репозиторії**, побачив що RPC не пишуть ціну, і зробив висновок
«ціна мовчки не зберігається». **Файли відставали від бази.** Я повторив це попередження
Вові щонайменше чотири рази, зокрема великим шрифтом у хендовері й у двох PR.
Гірше: **на скріншотах Вови (IMG_3710, IMG_3712) було видно «1 500 ₴»** — прямий доказ
проти моїх слів, і я його не помітив, бо був упевнений у висновку з файлів.
**Урок: файл у репозиторії — це не стан системи. Перед висновком про ПРОД — питай ПРОД.**

### 🛑 ЧОГО ВДАЛОСЯ УНИКНУТИ
Якби я накотив свій файл (а саме цього від мене й чекали), то **знищив би робочу
реалізацію**, бо серверна версія БАГАТША за мою:
- **гейт за категорією** `v_price_cats = ['продам','послуга','куплю']` — для «віддам» /
  «шукаю» / «знайдено» / «загубилось» ціна витирається (у мене такого не було);
- **`price_negotiable`** — прапорець «Договірна», окрема колонка, з правилом
  взаємовиключності: якщо є число, «договірна» скидається (у мене цього не було зовсім);
- **`currency` жорстко 'UAH' на сервері**, значення від клієнта ігнорується
  (у мене клієнт міг прислати валюту);
- ремонт заднім числом при редагуванні: перемкнув категорію «Продам» → «Загубилось» —
  стара ціна пішла.
Врятувала лише звичка звірити стан ПЕРЕД зміною (HOT_RULES №1 і №4).

### Що зроблено натомість
1. **`scripts/supabase_reputation.sql` і `scripts/supabase_board_edit.sql` переписані
   під ЖИВИЙ код прода** (знято через `pg_get_functiondef`). У кожен вписано блок-
   попередження: перед накатом RPC — спершу зняти живий код і порівняти.
   Це прибирає міну: до цього в репозиторії лежав SQL, який при накаті зламав би прод.
2. **Хибні шапки виправлено** — вони кричали «🔴 ПОТРІБЕН ПОВТОРНИЙ НАКАТ», що неправда.
3. **Блок перевірок у `board_edit.sql` переписано під реальну поведінку** (мої пункти
   описували неіснуючу логіку `v_has_price`).

### 🐛 І СПРАВЖНЯ ВАДА, ЯКУ Я ВНІС СЬОГОДНІ (знайдена цією ж звіркою)
Форма показувала поле «Ціна» для **всіх** категорій, а база витирає ціну поза
`['продам','послуга','куплю']`. Тобто людина під «Віддам» вписала б ціну, побачила б її
у прев'ю — а в збереженому оголошенні її не було б. Рівно та «тиха втрата даних», якою
я лякав Вову, тільки справжня і моя.
**Виправлено:** `PRICE_CATEGORIES` + `categoryHasPrice()` у `core/board-categories.js`
(дзеркало серверного `v_price_cats`, з попередженням «міняєш тут — міняй у двох RPC»);
`syncPriceVisibility()` у `community-modal.js` ховає секцію і **стирає значення**
при перемиканні на категорію без ціни; `.bm-section[hidden] { display: none }`.
Заразом прибрано з payload зайвий `currency` — сервер його ігнорує.

### ⬜ ЗНАЙДЕНО, НЕ РОБЛЮ БЕЗ СЛОВА ВОВИ
**«Договірна» (`price_negotiable`) є в базі, але клієнт її не вміє** — ні надіслати,
ні показати на картці. Тобто половина серверної фічі простоює. Потрібне рішення Вови:
додавати прапорець у форму і напис «Договірна» на картку?

`npm test` — 20/20 стендів, 237/237. `CACHE_NAME` → `cstl-20260728-1646`.

---

## 7. «ДОГОВІРНА» — клієнт наздогнав базу (Вова: «Звичайно»)

Колонка `posts.price_negotiable` існувала в базі з міграції 12:20, але клієнт її не вмів
ні надіслати, ні показати. Половина серверної фічі простоювала.

**Зроблено:**
- `core/utils.js` · `formatPrice(price, currency, negotiable)` — третій аргумент.
  ⚠️ Порядок перевірок НЕ довільний: **число має пріоритет над «Договірною»**, бо рівно
  так вирішує сервер (`if v_price is not null then v_negot := false`). Якби клієнт
  вирішував інакше, картка написала б «Договірна» там, де в базі лежить сума.
- `board.js` · `renderPrice` — передає `p.price_negotiable`; слова («Договірна»,
  «Безкоштовно») отримують клас `--word` і менший кегль, щоб не важили як цифра.
- `community-modal.js` — прапорець «Договірна» ПОРУЧ із полем числа (а не окремою
  секцією нижче: це та сама відповідь на питання «скільки», лише інакша).
  **Взаємовиключність зроблено ВИДИМОЮ:** поставив «Договірна» → поле числа гасне і
  очищається; вписав число → галочка сама знімається. Обидва напрямки дзеркалять сервер,
  тож форма ніколи не обіцяє того, чого база не збереже.
- `syncPriceVisibility` тепер скидає і `negotiable` — для категорій без ціни сервер
  витирає обидва поля.
- Прев'ю дзеркалить клас `--word` (Д-6), інакше «Договірна» у прев'ю була б більшою.
- `buildPayload` шле `price_negotiable`. Ключ звірено з живим кодом RPC.

**Перевірено прогоном `formatPrice` у node — 9 випадків:**
`2500 + договірна=true → «2 500 ₴»` (число перемагає, як на сервері) ·
`null + true → «Договірна»` · `0 + true → «Безкоштовно»` (нуль теж перемагає) ·
`null + false → ''` (рядка немає) · `'abc'/-5 + true → «Договірна»`.

`npm test` — 20/20 стендів, 237/237. `CACHE_NAME` → `cstl-20260728-1916`.

## 8. «БЕЗКОШТОВНО» — галочкою, а не введенням нуля (Вова)

**Вова:** «може для безкоштовно поставити просто місце для галочки… користувач натискає
біля фрази безкоштовно і це буде означати, що оголошення безкоштовне».

Стало ТРИ взаємовиключні стани ціни: **число · Договірна · Безкоштовно**.

- 🔑 **Окремого стану для «безкоштовно» НЕ заводив.** «Безкоштовно» — це ціна **0**
  у базі, тобто ознака ВИВОДИТЬСЯ з ціни (`isFreePrice()`), а не зберігається поруч.
  Один стан = нема чому розсинхронитись. ⚠️ Порівняння ЧИСЛОМ, а не рядком: база
  віддає numeric як «0.00», і `=== '0'` мовчки не спрацював би при редагуванні вже
  збереженого безкоштовного оголошення.
- 🔑 **Один вхід на всі переходи** — `setPriceMode('num'|'negot'|'free')`. Три окремі
  обробники рано чи пізно розійшлись би в уявленні «що зараз стоїть» — класична хвороба
  двох джерел правди, яку цей проєкт уже проходив.
- Вписав число → обидві галочки знімаються (дзеркалить сервер: число перемагає).
- Галочки — власним рядком під полем: «₴ ☐ Договірна ☐ Безкоштовно» в один рядок на
  вузькому екрані не влазить.
- Заразом: при редагуванні в полі стояло «1500.00» — прибрано копійки, якщо їх нема.

### 🔴 ЗНАЙДЕНО СТЕНДОМ І ВИПРАВЛЕНО: тап-ціль 18px
Перший замір показав **висоту клікабельної зони галочки 18px** — тобто рівно та помилка,
за яку я цього ж дня прибирав кнопки 34px з картки і відмовився зменшувати кнопки модалки.
Ставити 18px після таких аргументів було б непослідовно.
**Виправлено:** `min-height: 44px` на `.bm-negot` (тягнеться padding-ом, сам квадратик
лишається 18px — він виглядає правильно). Перезаміряно: **44px на 430/375/320px**,
нічого не вилазить.

⚠️ **Чесно про покриття:** наявний стенд №6 `board-compose` має СТАТИЧНУ сцену і моєї
нової розмітки не містить — його зелене світло про ці галочки не каже нічого. Тому
міряв окремою разовою пробою (у git не пішла). Постійного сторожа на цей рядок нема.

`npm test` — 20/20 стендів, 237/237. `CACHE_NAME` → `cstl-20260728-1924`.

### ❌ Рішення Вови: «Безкоштовно» для категорії «Віддам» НЕ показуємо
Я запропонував дописати рядок «Безкоштовно» на картки категорії «Віддам» (там поле ціни
не показується взагалі, тож рядка про ціну немає зовсім).
**Вова: «Не треба, буде дублювати».** І він має рацію: пігулка категорії вгорі картки
вже каже «Віддам» — другий напис говорив би про те саме іншими словами.
Рішення вписано **в код** (`renderPrice` у `board.js`), а не лише сюди: у самій RPC
є коментар «показ "Безкоштовно" — справа клієнта», тож спокуса дописати цю гілку
виникне в наступної сесії знову. Тепер там прямо написано «не дописувати».
Зміна лише коментарем — `CACHE_NAME` не чіпав (поведінка не змінилась).

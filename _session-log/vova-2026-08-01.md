# Журнал сесії — 2026-08-01 (гілка `claude/startuem-6ecnk0`)

> Живий журнал: дописую ПО ХОДУ після кожної значущої дії. При `/finish` звіт іде звідси.

---

## Старт сесії (`/startuem`)

- `git config user.name "Вова"` + `vitocorleone191@outlook.com` — виконано.
- Гілка: `claude/startuem-6ecnk0`. Звірка з GitHub:
  - `git log --oneline HEAD ^origin/main` → **порожньо** (нічого незапушеного).
  - `git rev-list --count HEAD..origin/main` → **0** (гілка = вершина `origin/main`).
  - Хук `SessionStart` попереджав «локальний main відстає на 25 комітів» — це про
    локальну гілку `main`, а не про робочу. Робоча стоїть на свіжому origin/main.
- Прочитано: `HOT_RULES.md` · `START_HERE.md` · `SESSION_STATE_VOVA.md` (сесія T) ·
  `AUDIT_2026-07.md` (пріоритети) · `CSTL_BUGS.md` · `CLAUDE.md` · `BYYOU_PLAN.md`.

### Стан прода на момент старту
- Останній продуктовий мердж: **PR #734** (`4a5e6425`) — «Безпека (анонім лише
  переглядає · бекапи · фото чатів) + новий вигляд табла новин».
- `CACHE_NAME = cstl-20260801-1521`.
- `DEV_LOCK = true` (додаток досі закритий заслінкою розробки).
- Відкритих 🔴 багів нема.

### 🔴 Знайдені розбіжності в документації (доповів Вові у першій репліці)
1. **`BYYOU_PLAN.md` суперечить сам собі.** Таблиця кроків: 6-14 = 🟢 (зроблено),
   але розділ «Де зупинились» нижче каже «кроки 1-4 зроблено, крок 5 — 🔴 СТОП,
   питання Вові» і «⬜ Досі без відповіді: ВІДЖЕТ — дорога 1 чи 2». Статус файлу —
   `active`, гілка вказана стара (`claude/startuem-yky235`).
   Насправді відповідь Є і вона в коді: `style/community.css` (PR #734) —
   «🔴 01.08.2026 — «ПРИЛАД» ЗНЯТО (дорога 2, вибір Вови зі знімків)».
2. **`SESSION_STATE_VOVA.md` (сесія T) описує лише безпеку** — про зняття «приладу»
   з табла новин (половина PR #734: `community.css` −113/+113, `news-card.css` +37,
   `news.js`) у файлі **нема жодного рядка** (`grep "прилад"` → 0 збігів).
3. **`START_HERE.md` блок «Поточний стан» відстає** — стоїть «на 2026-07-31, сесія S»,
   PR #732, деплой #3600, `CACHE_NAME = cstl-20260731-2155`. Той самий файл сам
   попереджає, що застарілий стан тут дорожчий за застарілий деінде.

---

## Дія 1 — «варіант 1 табло новин глянем» (Вова)

**Прочитання:** «варіант 1» = **дорога 1** з `BYYOU_PLAN.md` — лишити табло багряним
(«приладом») і навести лад у його межах. Дорога 2 (прилад знято) вже на проді, PR #734.

### Чому не підійшов наявний інструмент
`tests/tools/news-roads.mjs` писався ДО мерджу #734, коли віджет ще був багряним:
його `ROAD_1` лише прибирав обідок картки, розраховуючи, що прилад на місці.
Після #734 той самий CSS показує **не** дорогу 1, а дорогу 2 без обідка.
➡️ Написав `tests/tools/news-road1.mjs` — він ПОВЕРТАЄ прилад накладеним `<style>`
(градієнт + глянець + віньєтка + силует замку + перевернуті назад кольори тексту,
дослівно з `git show 4a5e6425^:style/community.css`). Бойовий код не чіпається.

### 🔴 Фото підмінено локальними — свідомо
Урок 01.08, записаний у самому плані: попередні знімки варіантів малювались із
БИТИМИ зображеннями (у пісочниці нема мережі) → список виглядав голішим, ніж він є,
і з поганого рендера народився хибний висновок аудиту №7. Тому інструмент
перехоплює всі зовнішні картинки і віддає `photos/olyka.*.jpg`.

### Заміряно
```
ЗАРАЗ (дорога 2)   406px  55.5%   картки 92/92/92  фото 2 монограм 1
ДОРОГА 1 (прилад)  408px  55.8%   картки 92/92/92  фото 2 монограм 1
```
Знімки: `shots/road1/` (вирізаний віджет + увесь екран, обидві дороги).

### Висновки, які дав замір (а не смак)
1. **Різниця у висоті — 2px.** Дорога 1 місця не економить; питання лише характеру.
2. **Рвані висоти 100/71/100 закриті вже зараз** — картки 92/92/92 в ОБОХ дорогах,
   бо монограма (`nc-img--mono`, `news.js:280`) у бойовому коді. Те саме з
   розвантаженим верхнім рядком і форматом часу. ➡️ **Усе, що аудит назвав дефектом,
   від вибору дороги не залежить** — воно вже полагоджено.
3. **«Подвійної межі» у віджеті ніколи не було.** Аудит писав про неї, але до #734
   стояло `--nc-line: transparent` — обідок зʼявився РАЗОМ зі світлим тлом. Тобто
   пункт дороги 1 «зняти обідок картки» лагодить те, чого на багряному нема.
4. **Головне видно лише на знімку ЦІЛОГО екрана:** віджет стоїть одразу під hero-фото
   Олики (темний вечірній кадр). Світлий віджет дає чистий розрив «фото скінчилось —
   почався контент»; багряний продовжує темну масу, і межа hero зникає.

⬜ Чекаю рішення Вови. Бойовий код не чіпав.

---

## Дія 2 — «Давай багряний, і добав ободок такий як в відкритій сторінці новини»

**Рішення Вови:** прилад повертаємо (дорога 1), картці — обідок сторінки новин.
`CACHE_NAME` 1521 → **`cstl-20260801-1811`**.

### 🔴 Знахідка, яку виявило саме це замовлення: обідка не було ВЗАГАЛІ
Коментар у бойовому `community.css` стверджував «картка стала БІЛОЮ З ОБІДКОМ —
так само, як у хабі». **Заміряно на живій сторінці: `border-top-width` = `0px`.**
Токен `--nc-line` віджет виставляв, але споживати його не було кому — правило
`border` мали лише `.nc--row` / `.nc--lead` (варіанти хаба), а варіант табла
`.nc--mini` — ні. Тобто опис і код розійшлись, і саме цю розбіжність Вова побачив
оком. ➡️ Правило додано в `style/news-card.css` (КОМПОНЕНТ), а не у віджет: за цим
стежить `tests/news-widget.mjs` («табло НЕ перевизначає властивості картки»).

### Змінено
| Файл | Що |
|---|---|
| `style/news-card.css` | `.nc--mini` дістав `border: 1px solid var(--nc-line)` |
| `style/community.css` | повернуто градієнт + віньєтка + глянець + `::before` (силует замку); назад перевернуто кольори: шапка `#F3DECB`, бейдж «N нових» напівпрозорий, світіння «екрана», лінія контролів світла, «УСІ НОВИНИ» кремове; токени картки → `--news-card` / `--news-line` |
| `sw.js` | `CACHE_NAME` |

### 📐 Опис наслідку ДО пушу (HOT_RULES №6) — і він справдився точно
Обіцяв: картка 92 → 94px (по 1px обідка згори й знизу), віджет 408 → 414px.
Заміряно після: **94/94/94, віджет 414px (56.6%)**. Збіг до пікселя.

### 🔴 Дві пастки виміру, які ледь не дали хибний висновок
1. **«Рвані висоти повернулись» — ні.** Перший прогін показав 102/102/94, і це
   виглядало як мій регрес. Перевірка порівнянням з чистим HEAD (`git stash`)
   дала **100/100/92 без моїх змін** — тобто рваність існувала й до мене, а моя
   зміна додала рівно +2px усім трьом. Причина рваності інша: плейсхолдер битого
   фото `.nc-img.img-fallback` міряє **80px**, а монограма — **72px**.
   ⬜ **НЕ чіпав** (HOT_RULES №9): це видно лише коли фото не завантажилось.
2. **Підміна фото мовчки не працювала.** Лічильник показав `підмінено: 0`, хоча
   правило `page.route` стояло: запити через **Service Worker** Playwright не
   перехоплює, а `sw.js` обслуговує саме картинки. Знімок при цьому виглядав
   правдоподібно — брехав би тихо. Полагоджено `serviceWorkers: 'block'`;
   після цього підмінено 2 картинки і висоти стали рівними.

### ✅ Перевірено
`node build.js` (check-imports 47 файлів чисто) · **`npm test` — 31/31 стендів,
462/462 перевірок зелені**, зокрема `news-widget 59/59`.
Знімки бойового стану — `shots/road1/0-зараз*.png`.

⬜ **Не перевірено мною:** живий iPhone (шрифт, реальні кольори OLED, як обідок
читається оком на багряному). Із пісочниці недосяжно.

---

## Дія 3 — `/finish` (Вова: «Фініш»)

- `git merge origin/main` — приїхали лише автокоміти парсерів (новини × 3,
  автобуси × 2), конфліктів нема.
- `CACHE_NAME` лишено `cstl-20260801-1811` — він новіший за той, що на `main` (1521),
  і відповідає саме цим змінам; вигадувати другий не треба.
- `node build.js` — check-imports 47 файлів чисто.
- **`npm test` після мерджу свіжих даних — 31/31 стендів, 462/462 перевірок.**
- `CSTL NEWS VOVA/CLAUDE.md` — у таблицю файлової структури додано два рядки:
  `style/news-card.css` (форма картки + новий обідок `.nc--mini`) і віджет
  «ТАБЛО НОВИН» у `community.css` (прилад багряний; знімаєш прилад — знімай і силует).
  Причина: рішення про колір за один день перевертали двічі, і без запису наступна
  сесія могла зняти прилад «наосліп» утретє.

# Журнал сесії — 31.08.2026

**Гілка:** `claude/startuem-8csg49` · production `main`
> ⏭️ **Продовження сесії, що почалась 30.08** — див. `vova-2026-08-30b.md`.
> Новий файл заведено тому, що змінилась дата (сторож `docs-fresh` вимагає журнал
> за поточний день, і правильно: інакше робота двох діб лежала б одним шматком).

## Стан на ранок 31.08

Контейнер сесії **перезапустився**. Дві фонові задачі зупинились і **не
перезапускались — обидві були вже неактуальні**:
- прогін 122 стендів — релевантні прогнані окремо і зелені; повний `npm test` у
  пісочниці не вкладається в ліміт часу, а `tests.yml` навмисно не ганяє стенди
  на документаційних PR (тригер лише `src/**` і `style/**`);
- опитування живого сайту — деплой **#5288 підтверджено `success`** через GitHub API,
  а `castlelife.org` із пісочниці не читається взагалі (HTTP 000, проксі).

✅ **Хвостів немає:** незапушеного 0, `git diff origin/main HEAD` порожній,
`/byyou` — `idle`, `docs-refs` 10/10.

⚠️ Після рестарту знайшлись 3 незапушені коміти (два merge + нічний автокоміт
парсера) при порожньому diff — вміст уже був у `main` через squash. Запушено,
щоб гілка не розходилась із origin.

## Що зроблено вчора і стоїть на проді

🔍 **Пошук за змістом** — морфологія, синоніми, намір, змішана розкладка
(PR #1118, деплой #5288). Підключений **лише до Дошки** за словом Вови
(«давай хай поки буде до дошки»).
📄 Документація сесії — PR #1119.
⏳ **Телеметрія пошуку** збережена з умовою «як дізнаєшся що додаток запустився,
тоді через деякий час».

## Далі
Чекаю завдання від Вови.

---

## Роби: назва спільноти не переноситься інакше при виділенні

Вова, три знімки: «переноситься одна буква — ТУРИСТИЧН / А ОЛИКА»; «Олицька міська
рада» наведена ставала трьома рядками з трьома крапками. «Щоб воно так, як пише,
так і було». І окремо: «має стосуватися не тільки цих трьох, а всіх, які будуть».

### 🔴 ДВІ ПРИЧИНИ, І ДРУГА ВИЯВИЛАСЬ ВАЖЛИВІШОЮ

**Причина 1 — жирність.** Кегль однаковий 9.5px в обох станах; мінялась ВАГА
500 → 700, а жирні літери ширші на ≈9%. Заміряно на живій сцені (колонка 61px):
«Туристична» — 58.2px при 500 (влазить), **63.4px при 700** (ні). А в назви стоїть
`overflow-wrap: anywhere`, тож слово, ширше за колонку, РВЕТЬСЯ посередині.
🔑 І важливе: «збільшується» насправді КІЛЬЦЕ (`scale 1.13`), а не напис. Тому
зняття жирності нічого не забрало — виділення тримають прозорість .45→1, білий
колір і збільшене кільце. Замість ваги — тінь: вона не чіпає метрики.

**Причина 2 — колонка звужується з кожною спільнотою.** Ряд це grid `1fr`:
📐 **5 кружечків → 61px · 6 кружечків → 50px**, і при шести рвуться ТРИ назви вже
без жодної жирності. `MAX_CIRCLES = 6` — тобто шоста можлива сьогодні.
➡️ Тому самого лише зняття жирності було НЕ досить для вимоги «всіх, які будуть».
Додано `fitCircleNames()`: якщо найдовше слово ширше за колонку — кегль ряду
стискається (підлога 8px). Один кегль на ВЕСЬ ряд: різні розміри в сусідніх
колонках читаються як недогляд верстки, однаковий дрібніший — як свідомий масштаб.

### 🔬 ДВІЧІ ЗБРЕХАВ ВЛАСНИЙ ВИМІР — обидва рази спіймано

1. **Реконструкція замість живої сцени.** Перша версія інструмента будувала свій
   HTML (flex, gap 8px) — колонка вийшла 89px, «усе влазить», тобто вимір
   ЗАПЕРЕЧУВАВ ваду, яку Вова бачив на екрані. Реальний контейнер — grid `1fr`.
   ➡️ Переписано на підйом ЖИВОГО застосунку.
2. **Міряв висоту замість ширини.** `-webkit-line-clamp: 2` уже обрізав текст, тож
   `scrollHeight` ЗАВЖДИ дорівнює двом рядкам — вада невидима. Рве рядок ширина
   слова, її й треба міряти.

### Перевірка
Заведено `tests/fd-name-fit.mjs` — сцена на ШЕСТИ спільнотах (на пʼяти друга
причина не відтворюється взагалі). **7/7**, і головне — **КОНТРОЛЬ уміє впасти**:
з поверненою жирністю дає **5/7** і показує рівно скаргу Вови
(«Туристична Олика: 54.4px > 50px»).
`home-feed-widget` **49/49** — не зламано. Стендів 126 → **127**.

⚠️ **Межа названа:** слово довше ~11 літер при шести кружечках однаково розірветься —
спрацьовує підлога 8px, нижче напис не читається. Реальні назви Вови вкладаються.

### Доробка: назва знову виділяється — але масштабом, не вагою

🗣️ Вова після першого фіксу: «погано, що сама назва не виділяється… можливо,
приближалась чи трошки ставала жирнішою трішки». Він правий: знявши `font-weight`,
я прибрав ваду РАЗОМ із виділенням — це була втрата, а не чистий виграш.

🔑 **Рішення — `transform: scale`, і воно єдине, що не ламає перенос.**
Вага і кегль міняють ШИРИНУ ЛІТЕР, тобто розкладку. `transform` застосовується
ПІСЛЯ розкладки: текст уже розбитий на рядки, масштаб лише малює його більшим.

📐 **Чому 1.06, а не на око.** Між колонками `gap: var(--hm-s1)` = **4px**, тобто
по 2px з боку. Найтісніший випадок — назва на всю колонку (50px при шести
кружечках): 50 × 1.06 = 53px, виступ 1.5px, запас 0.5px.
При 1.08 виступ дорівнював би рівно gap — назви торкалися б сусідніх, а Вова прямо
просив «без накладання один на одного».
✅ Заміряно на живій сцені: приріст **×1.06**, найменший зазор **2.49px**.

🔴 **КОНТРОЛЬ УМІЄ ВПАСТИ:** при `scale(1.25)` стенд червоніє із зазором **−2.29px**.
Тобто межа охороняється числом, а не смаком.

⚠️ **Стенд спершу збрехав і на цьому.** Перша редакція міряла ширину ОДРАЗУ після
додавання класу — а масштаб їде 400ms, тож виходив приріст ×1, і перевірка
«доводила», що виділення немає, над цілком робочим кодом. Лікує вимкнення
`transition` на час виміру.

`fd-name-fit` **9/9** (було 7) · `home-feed-widget` **49/49**.

---

## 🔢 «N НОВИХ» У НОВИНАХ: МОДЕЛЬ «БАЧИВ СТАТТЮ» + ГРОМАДА ЯК «НАШЕ» (вечір)

🗣️ **Скарга Вови:** «писало плюс сім нова. Я перенажимаю усі новини, читаю статті…
закриваю додаток, заходжу і знову пише сім нових». Плюс вимога до відмінків і
прохання виділити Громаду в підписі розділу.

### Що зʼясував ДО коду

⚠️ **Головну скаргу відтворити НЕ вдалося.** Прилад `tests/tools/news-badge-probe.mjs`
підняв живий застосунок і пройшов шлях Вови: «20 нових» → тап «Усі новини» →
бейдж зник → перезапуск → не повернувся. Механізм у пісочниці справний.

🔴 **Мою першу гіпотезу вимір СПРОСТУВАВ.** Я думав на перегони входу (бейдж
малюється під міткою гостя і не перемальовується після відновлення сесії) — але
ключ пишеться під `uid`, ключа `:anon` не зʼявляється взагалі.

Перевірив і відкинув: `bundle.js` у git збігається зі `src/` · час статей не
переписується (0 змін за 40 комітів) · дат у майбутньому 0 · `scope = 'news'` у
базі дозволений.

✅ **Заміряна СПРАВЖНЯ вада:** тап по самій статті у віджеті бейдж не міняв
зовсім — «20 нових» → «20 нових».

🔑 **Корінь структурний.** Стан «що я бачив» був ОДНИМ ЧИСЛОМ (коли востаннє
відкривав хаб). Виразити «прочитав одну з семи» таке число не вміє В ПРИНЦИПІ.
Тому міняв модель, а не шукав далі привид у старій.

### А. Модель «бачив статтю»

Мітка розділу лишилась **підлогою**, поверх неї — список прочитаних номерів:
`readSeenIds`/`writeSeenIds` у `core/board-shared.js` (там, де вже живе правило
«як зветься ключ акаунта» — друга копія розійшлася б із першою).

- `markArticleSeen(art)` у `news.js` — пише лише те, що впливає на лічильник
  (Громада **і** свіжіше за підлогу); решта була б мертвим вантажем біля стелі
- `countNewCommunity` тепер має ДВІ умови: свіжіше за підлогу **і** не в списку
- `markNewsSeen()` чистить список: підлога піднялась, усе старе під нею
- стеля `SEEN_IDS_MAX = 300`, ріжеться з хвоста

⚠️ **Межа названа чесно:** список живе на пристрої. Мітка розділу синхронізується
через базу, окремі номери — ні: власної таблиці немає, а `user_seen_threads`
зайняти не можна (там `post_id` Питань, номери мовчки збіглися б).

### Б. Громада в підписі розділу

`hm-kicker-cat--own` — тон `#FFF1F3` і вага 800 проти `.58` білого / 700 у решти.
🛑 **Не пігулка:** за 8px праворуч уже стоїть пігулка «N нових» із тим самим
заокругленням і тією самою вагою — дві поруч змагались би.
📐 Заміряно: яскравість **147.9 → 244.1 (+96.2)**, теплота R−B = 12.

### Відмінки

Вже працювали з 29.08 (`pluralNew`): «1 нова · 2 нові · 5 нових · 11 нових».
Вова бачив «сім нова» до цієї дати. Додав перевірку, щоб не з'їхало.

### 🛑 Прилад брехав мені ЧОТИРИ рази за цю задачу

1. сіяв стару мітку через `addInitScript` на **кожному** завантаженні — і
   «доводив» ваду на перезапуску, яку сам же туди й клав;
2. те саме повторилось уже в стенді, коли я знімав сентинель посіву;
3. перевірка «прочитав усі» тикала ту саму картку каруселі — падав ТЕСТ, не код;
   переписав на дослівну вимогу Вови («одна нова → прочитав → бейдж зник»);
4. яскравість рахувалась **без прозорості**, тож `rgba(255,255,255,.58)` давала
   255 — тобто тихий підпис виходив «яскравішим» за виділений.

**Стенд:** `tests/news-seen.mjs` **15/15**, контроль `BUNDLE_REV=CSS_REV=origin/main`.
**CACHE_NAME:** `cstl-20260831-1628`.

---

## 📰 ЗМІШАНА ВЕРСТКА: СТАТТЯ БЕЗ ЖОДНОГО АБЗАЦУ (ніч)

🗣️ **Скарга Вови зі скріншотів:** «Чому стаття не в повному обсягу?» — стаття
Волинь Post «Житло біля Луцька в розтермінування» показувала самі підзаголовки й
марковані списки, весь інший текст зник.

📐 **Заміряно на збереженій статті 13519:** `<h3>`×3, `<ul>`×2, `<p>`×**0**,
929 символів, `contentSource: page` — тобто парсер вважав витяг УСПІШНИМ.
🔑 Перевірка повноти цього спіймати не могла: вона міряє **довжину** (929 > 180),
а не **частку** захопленого.

🔎 **Ланцюг діагностики (важливий сам по собі).** Спершу підозрював фільтр
`_вердикт_блоку` — рекламна стаття, логічно було думати на відсів промо. Прогнав
усі вісім справжніх абзаців через нього: **усі вісім `ok`**. Фільтр невинний,
отже абзаци не дійшли до циклу взагалі.

🔴 **Справжня причина.** Волинь Post верстає тіло ЗМІШАНО: абзаци голим текстом
через `<br>`, а заголовки й списки — справжніми тегами. Блоковий збирач бачить
лише другу половину. Запобіжник «нема текстових блоків → вмикай потоковий шлях»
мовчить, бо заголовки підняли лічильник вище нуля.
➡️ **Той самий клас, що вада 27.08, на крок далі:** там роль «непорожнього
списку» грав `<img>`, тут її грають `<h3>` і `<ul>`.

✅ **Відтворено на макеті верстки** — дослівно та сама ознака:
```
текст у контейнері ЦІЛКОМ:  723
зібрав БЛОКОВИЙ шлях:       234 (32%)   ← те, що бачив Вова
зібрав ПОТОКОВИЙ шлях:      728 (100%)
```

🛑 **Просто перемкнутись на потоковий шлях було НЕ МОЖНА:** він давав увесь текст,
але сплющував усе в `<p>` — заголовок переставав бути заголовком, пункт списку
пунктом. Вова отримав би повний текст і втратив структуру, яку вже бачив.

**Тому дві правки разом:**
1. **Потоковий шлях шанує блокові теги** — `h2/h3/h4`, `ul/ol`, `blockquote`,
   `iframe` віддаються блоками, а не сплющуються. Суддя (`_вердикт_блоку`) і
   розмітка ті самі, що в блоковому збирачі — другої копії правил не заведено.
2. **Вибір збирача за ПОРІВНЯННЯМ ДВОХ ЗБИРАЧІВ**, а не за «часткою від тексту
   контейнера». 🔑 Причина: обидва законно ріжуть службовий хвіст, тож на сторінці
   з великим футером частка впала б і на справній блоковій верстці. У порівнянні
   двох збирачів хвіст ріжеться однаково і з розрахунку випадає.

📐 **Поріг 1.25 заміряний, не вибраний:**
```
чиста блокова   1.00
чиста потокова  1.00
змішана (Вови)  3.07
```
Лежить у порожнечі між ними — не балансує на межі.

🔴 **`RICH_ALGO_VERSION` 7 → 8.** Без бампа три вже збережені биті статті лишились
би такими назавжди: полірування чистить НАЯВНУ розмітку, а зниклих абзаців у ній
немає — їх знає лише сама сторінка. Ціна названа: ≈10 годин на повний перебір.

**Масштаб:** 3 статті з 400, усі Волинь Post, усі `contentSource: page`. Рідко —
але б'є саме по статтях ЗІ СТРУКТУРОЮ (інструкції, пояснювачі), тобто по
найкорисніших.

⚠️ **Межа чесно:** volynpost.com із пісочниці недосяжний (проксі → 403), тож макет
у стенді — РЕКОНСТРУКЦІЯ верстки, а не копія сторінки. Він доводить механізм;
що сторінка джерела влаштована саме так, підтвердить прогін у CI з живою мережею.

**Стенд:** `scripts/test_mixed_layout.py` **10/10**, контроль `REV=origin/main` →
**6/9** (падає повнота, структура лишається зеленою — тобто контроль показує саме
те, що старий код структуру беріг, а текст губив). Зареєстровано в `tests.yml`.

⚠️ **Знайдено побіжно:** `scripts/test_markup_polish.py` (42 перевірки) у CI
**не запускається** — його немає в списку `tests.yml`. Не чіпав, бо це поза
замовленням; сказати Вові.

### 🔴 РЕГРЕСІЯ, ЯКУ ЗНАЙШЛА ЖИВА СТОРІНКА (слідом за версією 8)

Після прогону парсера з `RICH_ALGO_VERSION = 8` стаття 13519 **полагодилась**:
```
довжина  929 → 3765     <p>  0 → 25     <h3>  4 (ціле)
```
✅ Абзаци повернулись, посилання в тілі теж (`Сімейна Фортеця` стало клікабельним).

🔴 **АЛЕ `<ul>` стало 0** — усі пункти перетворились на окремі абзаци. Текст цілий,
маркери зникли. Тобто, лагодячи повноту, я вніс регресію в структуру.

🔑 **Причина:** на тій сторінці `<li>` лежать **без батька-`<ul>`**. Блоковий
збирач цього не помічав — `find_all("li")` шукає на будь-якій глибині й батька не
питає. А мій обхід іде по дітях і такого сироту не впізнавав: кожен `li` ставав
власним абзацом.

🛑 **Чому стенд мовчав:** у моєму макеті список був «правильним» `<ul><li>`.
➡️ **Урок, який у цьому проєкті вже коштував роботи:** макет доводить МЕХАНІЗМ,
а не влаштування чужої сторінки. Тепер у стенді є сцена з навмисно «неправильною»
розміткою — сироти без обгортки.

✅ Лікує буфер пунктів у потоковому збирачі (той самий прийом, що `li_buf` у
блоковому). Заміряно на макеті сиріт: `<ul>` ×1, `<li>` ×3, пункти не розсипались,
список стоїть між своїм вступом і наступним абзацом.

🔴 **`RICH_ALGO_VERSION` 8 → 9** — інакше списки в трьох статтях не повернуться.

**Стенд:** `test_mixed_layout` **13/13** (було 10). Контроль `REV=origin/main` →
**9/12**, і падає рівно на трьох нових перевірках, відтворюючи живу регресію
дослівно: `<ul>` ×0, пункти розсипані на сім абзаців.
⚠️ Ціль контролю переїхала: спершу він падав на повноті (6/9), бо в `main` не було
фіксу зовсім; тепер у `main` лежить перша редакція, тож падає вже наступний шар.
Це записано в шапці стенда, щоб наступна сесія не «лагодила» контроль.

---

## 🟢 ДВА ЧЕРВОНІ СТЕНДИ В `main` — ЦЕ БУЛИ ЗАСТАРІЛІ ПЕРЕВІРКИ, А НЕ БАГИ

🗣️ Вова: «якщо це потрібно виправити стосовно якості роботи додатку то роби все».
⚠️ Сказав чесно перед роботою: це **не про те, що бачить житель**. Але воно таки
про продукт — `geo-scale` стереже голос агента, який пише пости жителям, а
`ai-budget` стереже витрати на модель. Червоне тут = ми не помітимо справжньої
поломки в тому, що читають люди.

### `ai-budget` 18/19 → **21/21**

Стенд грепав рядок `«пропускаю виклик API»`. Поведінка **є**: `ai_news_agent.py`
друкує «новини цього прогону пропускаю (економія)», і сама економія на місці
(`ліміт = 0 if новини_повні`, далі `continue` **до** виклику моделі).
🔑 Хтось переписав повідомлення — і сторож почервонів від правки речення.

✅ Переписано **з грепу на виконання**: стенд запускає справжній модуль агента і
питає число — скільки статей той замовить при повному кабінеті. Має бути 0.
🔑 Усередині перевірки власний контроль: на ПОРОЖНЬОМУ кабінеті ліміт мусить бути
> 0, інакше вона міряла б не те.
🔴 **Мутант:** `MAX_DRAFTS_TOTAL` 10 → 99 (повний кабінет перестає бути повним) →
перевірка червоніє, **20/21**. Отже вміє впасти.

### `geo-scale` 30/31 → **33/33**

Стенд шукав дослівно `/медіабренд \*\*Олицької громади\*\*/` — разом із зірочками
жирного. 28.08 самовизначення переписали («авторська медіа-спільнота про СУЧАСНУ
Олицьку громаду»), коли історія переїхала у власну спільноту. Зміст той самий.

✅ Тепер міряється ВИМОГА: у реченні-визначенні названо громаду, і назване місто
**і** «16 сіл» (саме через абстрактні села агент 24.08 написав «для нас, жителів
Олики» на День Незалежності й виключив 16 сіл з їхнього ж свята).

🛑 **Перша редакція цієї правки була напівфальшивою:** я шукав «громад» по всьому
розділу на 2521 символ — і вона лишалась ЗЕЛЕНОЮ на мутанті, де масштаб звузили до
містечка, бо слово трапляється в сусідніх реченнях. Звузив до самого речення-визначення.
🔴 **Мутант** (масштаб звужено до містечка) → **31/33**, падають ОБИДВІ.
✅ Голос після контролю відновлено байт-у-байт (`git diff` порожній).

⚠️ **Знову моя мірка збрехала:** `/\b16 сіл\b/` не спрацьовувало над правильним
текстом — у JavaScript `\w` це лише латиниця, тож кирилична «л» для нього НЕ
літера і межа слова після «сіл» не виникає взагалі.

### Сторожі, яких CI не запускав

📐 Звірив список на диску зі списком у `tests.yml`: **чотири** python-стенди не
запускались. Додано три (`test_markup_polish` 42 · `test_history_routing` 53 ·
`test_editor_pools` 53) — останні два стережуть РОЗДІЛЕННЯ КОНТЕНТУ між
спільнотами, тобто те, що читають жителі.
ℹ️ `test_vopas_access` живе у власному воркфлові, `test_worker` вимагає адресу
аргументом — це ручний інструмент. Обидва лишились поза списком навмисно, і це
записано в самому `tests.yml`.

➡️ **Урок класу:** обидва червоні стенди міряли ФОРМУ ЗАПИСУ замість НАСЛІДКУ —
рівно те, від чого застерігає `HOT_RULES`. Такий сторож червоніє від переписаного
речення і мовчить, коли поведінка справді ламається.

### ✅ ПІДТВЕРДЖЕНО НА ЖИВІЙ СТАТТІ (після прогону з версією 9)

```
_richAlgo 9 · 3801 символів
<p> 16 · <h3> 4 · <ul> 2 · <li> 9
```
Стаття 13519 зібрана повністю: абзаци, заголовки, **списки на місці**. Відкритий
пункт у пульсі знято — питання закрите не припущенням, а виміром на проді.

---

## 📦 ПЕРЕДАЧА В НОВИЙ ЧАТ — 31.08.2026, 19:52 UTC

Вова: «після того як задеплоєш `/onovy`, а я буду починати працювати в новому чаті».

**Гілка:** `claude/startuem-8csg49`. **Уся робота в `main`** — `git diff origin/main..HEAD`
по коду порожній. PR цієї сесії: **#1120 · #1121 · #1122 · #1123 · #1124 · #1125 ·
#1126 · #1127 · #1128**.

### Що закрито за цю сесію
1. **Назва спільноти** у віджеті Стрічки: перенос (дві незалежні причини — жирність
   і звуження колонки при шести кружечках) і повернуте виділення масштабом 1.06.
2. **«N нових» у новинах** переведено на модель «бачив статтю»; Громада виділена
   в підписі розділу.
3. **Змішана верстка** статей Волинь Post: абзаци більше не губляться між
   заголовками; слідом полагоджено власну регресію зі списками.
4. **Два червоні стенди в `main`** виявились застарілими перевірками, не багами;
   три сторожі повернуто в CI.

### Головний наскрізний урок сесії
🛑 **Власний прилад брехав ШІСТЬ разів за добу**, і щоразу першою підозрюваною
правильно виявлялась перевірка, а не код:
1-2. посів стану через `addInitScript` на КОЖНОМУ завантаженні (у приладі й потім
     у стенді) — «доводив» ваду, яку сам створював;
3. перевірка «прочитав усі» тикала ту саму картку каруселі — падав ТЕСТ, не код;
4. яскравість рахувалась без прозорості: `rgba(255,255,255,.58)` давала 255;
5. `/\b16 сіл\b/` не спрацьовувало над правильним текстом — у JS `\w` це лише
   латиниця, кирилична «л» не літера, межі слова немає;
6. перевірка масштабу голосу шукала слово по всьому розділу і лишалась ЗЕЛЕНОЮ
   на мутанті.

➡️ **І сьомий випадок, іншого роду:** макет у стенді доводить МЕХАНІЗМ, а не
влаштування чужої сторінки. Регресію зі списками знайшла жива сторінка, бо в моєму
макеті список був «правильним» `<ul><li>`.

### Не доробили
- 🔍 `core/search.js` підключено ЛИШЕ до Дошки; `board-chat.js` і `buses.js` досі
  на `hay.includes(q)` — **найближча технічна задача**.
- 🟡 Пальцем не перевірено: «N нових», виділення Громади, назва спільноти.
- ⏳ Телеметрія пошуку — чекає слів «ДОДАТОК ЗАПУСТИВСЯ».
- 🔴 Сусідні класи неправди в агентів («очолює» без дати, «нещодавно», «цьогоріч»).

### Обмеження середовища, які варто знати наступному чату
- 🔴 **Запуск воркфловів мені закритий** — `403 Resource not accessible by
  integration`. Парсер запускає лише Вова через GitHub UI або розклад (:47).
- 🔴 **Мережа з пісочниці до чужих сайтів ріжеться проксі (403)** — і до
  `castlelife.org` теж. Я двічі за сесію мало не зробив хибний висновок із цього.
  Правда про прод — у даних `main` і в логах CI, не в локальному `curl`.

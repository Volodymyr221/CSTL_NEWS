# Журнал сесії — 31.08.2026 (сесія «b»)

Гілка: `claude/startuem-n1w5v3` · production: `main`

## Старт

- `/startuem`. Автор комітів підписаний Вовою (`user.name=Вова`, `user.email=vitocorleone191@outlook.com`).
- `git fetch origin` — гілка **синхронна з `origin/main`**, розходжень нема в обидва боки
  (`git log origin/main ^HEAD` порожньо, `git log HEAD ^origin/main` порожньо).
- Прочитано обовʼязкове: `NOW.md`, `HOT_RULES.md` (15 правил), `CSTL_BUGS.md` (відкриті),
  `BACKLOG_VOVA_2026-08.md`, `ВОВА_ПРОФІЛЬ.md`, `NEW_SESSION_PROMPT.md`.
- Стан: відкритих 🔴 критичних немає. Останній PR у `main` — #1127.
- Чекаю «Роби» від Вови.

## Кроки

_(дописується по ходу)_

## Крок 1 — два сторожі, які от-от впали б тихо (31.08, за словом Вови «Роби»)

### Що було виміряно ПЕРЕД роботою

- `CSTL_BUGS.md` = **40623 байт** зі стелі **40960** (`tests/docs-fresh.mjs`, `HEAVY = 40*1024`).
  Запасу **337 байт** — один рядок тексту. Наступний записаний баг зробив би сторожа червоним.
- `node tests/docs-fresh.mjs` уже давав **19/20**: авто-блок `NOW.md` відстав від git.

### Що зроблено

**1. Розбори закритих багів винесено в архів — НЕ стиснуто.**
Новий файл `CSTL NEWS VOVA/_ai-tools/_archive/CSTL_BUGS_розбори_закритих.md` (21.7 KB).
Перенесено ДОСЛІВНО шість найважчих: B-33 (1908 б), B-32 (3800), B-31 (2727),
B-30 (4021), B-29 (4275), B-27 (2613). У `CSTL_BUGS.md` на їхньому місці — стислий
рядок «корінь + урок + посилання на архів».
🔑 **Чому архів, а не стиснення:** стискати означало б губити. Прецедент у проєкті вже
є — так само розрізали `SESSION_STATE_VOVA.md` 15.08.
✅ **Доведено:** усі **33** ID (B-01…B-33) на місці; кожен із шести перенесених рядків
звірено байт у байт із версією з `HEAD` — збіг повний.
📐 `CSTL_BUGS.md`: **40623 → 24556 байт**, запас 16404 (40% вільно).

**2. Сторож показав ДРУГУ стелю, яку ховала перша.**
Після `node scripts/now_update.mjs` перша перевірка позеленіла — і відкрився провал
`NOW.md` **16465 байт при стелі 16384** (перебір 81). Тобто пульс теж стояв за межею,
і побачити це можна було тільки полагодивши попереднє.
Полагоджено: у розділі ➡️ ДАЛІ шість рядків «✅ закрито 30.08» згорнуто в один.
🔑 Перед зняттям кожного — греп, чи висновок живе ще десь (правило пульсу «вітрина,
а не сховище»): аудит «чи веде туди» → `SESSION_STATE_VOVA.md` · B-14 і B-28 →
`CSTL_BUGS.md` · авторизація й екран компʼютера → таблиця ЗРОБЛЕНО ·
гостьові поверхні → `_session-log/vova-2026-08-30.md:92`. Усі знайшлись.
📐 `NOW.md`: **16465 → 15711 байт**, 172 рядки зі стелі 200.

### Результат

- `node tests/docs-fresh.mjs` — **20/20** (було 19/20).
- Обовʼязкове читання нової сесії: **43527 → 38019 токенів** (22% → 19% вікна).
- 🛑 `CACHE_NAME` НЕ чіпав — зміни лише `.md` (правило №2, виняток).

### 🆕 Знахідка на боці: два стенди падають, і це НЕ моє

`npm test` у пісочниці: 99 стендів зі 128 не запускаються взагалі — `playwright`
не встановлений (це середовище, не код). З тих, що пройшли, — **682/684**:

| стенд | що каже |
|---|---|
| `ai-budget` **18/19** | 🔴 «новинний агент не кличе модель, коли чернеток уже досить» — тобто агент **витрачає гроші** там, де не мав би |
| `geo-scale` **30/31** | «голос називає бренд медіа ГРОМАДИ, а не самого лише центру» |

✅ **Доведено, що не моє:** `git stash` → ті самі **18/19** і **30/31** на чистому `HEAD`.
⚠️ Не чіпав — «Роби» на них не було. Доповів Вові.

## Крок 2 — замовлення Вови по вкладці Громада (читання коду, до «Роби»)

Три задачі: (1) віджет оголошень · (2) чи потрібен віджет автобусів · (3) переробка телефонів.

### Що прочитано

- `src/tabs/community.js:214-241` — каркас секцій Громади (заголовки `hm-kicker`).
- `src/tabs/community-blocks.js:1398-1500` — віджет Дошки: `bwRowHtml()`, `bwShuffle()` (Fisher-Yates).
- `src/tabs/board.js:370-405` — картка Дошки: `bd-card bd-ad` + `renderCardDesc` + `boardActionsHtml`.
- `src/tabs/home-contacts.js` (210 рядків) — телефони.
- `src/tabs/home-caps.js:108-152` — шкала балів капсул `RANK`.
- `src/core/icons.js` — 40+ векторних іконок Tabler.

### 🔴 ГОЛОВНА ЗНАХІДКА — віджет автобусів НЕСУЧИЙ, видаляти наосліп не можна

У шкалі балів капсул (`home-caps.js:130-133`) обґрунтування `FEED_REPLIES: 65` вище за
`BUS_SOON: 60` написане ДОСЛІВНО так:

> «ВИЩЕ за BUS_SOON (60), бо рейс за 16-45 хв людина побачить **іще й у віджеті
> автобусів на цій самій сторінці**»

Тобто шкала капсул **спирається на існування віджета**. Прибрати віджет, не чіпаючи
шкалу, = мовчки зламати ланцюг: BUS_SOON лишиться шостим у черзі на три місця.
🔑 Це рівно клас вади з HOT_RULES №12 — «єдиний шлях, який МОВЧКИ не працює».

Друга відмінність: капсула ПЕРСОНАЛЬНА (`boardable(r, from)` + `routeCoversStops`) —
для сіл, куди автобус не заходить, вона не показує нічого. Віджет дає загальну відповідь.

### 📐 Заміряні дані телефонів (`data/community.json`)

Контактів **9**. Після розділення в «Службах громади» лишається **4**: сільрада, ЦНАП,
амбулаторія, Волиньобленерго. Вони розкладені на **3 категорії** — тобто три заголовки
на чотири номери. Ось чому блок виглядає важким: **структура більша за вміст**.

Емодзі в блоці: 🏛 🏥 ⚡ 📦 🎓 👮 📇 🚨 🏘 ⧉ 📍 — при тому що `src/core/icons.js` існує
і вся решта застосунку вже векторна. Це останній острівець емодзі.

### Стан: чекаю «Роби», план і думку викладено Вові.

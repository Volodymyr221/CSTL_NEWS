# Журнал сесії — Вова · 2026-07-30 (другий чат дня)

**Гілка:** `claude/startuem-washns` · **Production:** `main`
**Стан на вході:** `main` = `aec1cc9a` (автокоміт парсера), остання робота — PR **#721**
(docs), код — PR **#720** / деплой **#3553**, `CACHE_NAME = cstl-20260730-1154`.

> ℹ️ Чому суфікс `b`: перший чат 30.07 (сесія N, чотири частини, PR #718–#721) журналу
> **не залишив** — стан писався напряму в `SESSION_STATE_VOVA.md`. Щоб не вдавати, що
> цей файл покриває весь день, він названий другим.

---

## Крок 0 — старт сесії (`/startuem`)

- `git config user.name "Вова"` + `user.email vitocorleone191@outlook.com` — авторство комітів.
- Гілка сесії: `claude/startuem-washns`. `git fetch origin` + `git pull origin main` → **Already up to date**.
  (Хук `SessionStart` попереджав про відставання локального `main` на 50 комітів — це
  локальна гілка `main`, робоча гілка вже містить свіжий стан.)
- `_ai-tools/BYYOU_PLAN.md` → статус **`done`** (аудит Дошки, PR #718). Незавершеного потоку `/byyou` нема.
- Прочитано: `HOT_RULES.md` · `START_HERE.md` · `SESSION_STATE_VOVA.md` (верхні 180 рядків —
  сесія N, частини 2-4) · `AUDIT_2026-07.md` (шапка + ревізія + 🔴-пункти) · `CSTL_BUGS.md` ·
  `CLAUDE.md` (через `claudeMd`) · `ВОВА_ПРОФІЛЬ.md`.

## Знайдено при старті (не чіпав — HOT_RULES №9)

1. **`START_HERE.md` відстає на 4 дні** — блок «Поточний стан проекту» датований
   26.07 (сесія F, PR #656, `CACHE_NAME = cstl-20260726-1615`), тоді як реальний прод —
   30.07 (PR #720, `cstl-20260730-1154`). Той самий файл сам пише «новий чат починай звідси».
2. **`_session-log` розколовся на дві теки** — є `_session-log/` у корені (22 файли) і
   `CSTL NEWS VOVA/_session-log/vova-2026-07-29b.md` (один файл). Це рівно та хвороба,
   що закритий баг **B-10** (дві копії `.claude/commands/`).
3. **Журнал 30.07 відсутній** — за день три деплої (#3545, #3549, #3553), журналу нема
   (`_session-log/vova-2026-07-29.md` існував у якійсь гілці, у `main` його теж нема).

---

## Дії сесії

_(дописується по ходу)_

### 1. `/byyou` — архітектура блока новин у Громаді (аналіз, БЕЗ коду)

Вова дав великий промпт у ролі Staff Product Designer: переосмислити «Табло новин».
Перед аналізом **заміряв блок живцем** (Chromium 390×844, справжній застосунок):
блок **567px = 77.6%** видимої зони; вкладений скролер вікно **465px** / вміст
**6 933px**; **21 картка, повністю видно 1**; Волинь **163** картки / 49 805px;
Україна+Світ **216** карток / 69 029px / 162 `<img>`; **в інших шести віджетах
Громади — 0 вкладених скролерів**; `articles.json` = 400 статей / **1.31 МБ**
цілим на кожне відкриття; Громада — лише **21 стаття з 400**.

Знайшов у коді те, чого не було в замовленні: 25-рядковий хак у
`community-blocks.js:1209-1233` — невидима зона **30px від краю**, де `touchstart`
вимикає прокрутку віджета. Три латки на один симптом.

Віддав аналіз у 13 секціях + критику самої концепції (6 пунктів, зокрема:
суперечність «максимум інфо + мінімум місця»; 3 чіпи у віджеті — це вже заборонена
ним же «вкладка у вкладці»; найгостріше — **контент**, 0.7 новини/день Громади).
План 13 кроків → `_ai-tools/BYYOU_PLAN.md`, статус `active`. Коміт `504bb8ea`.
**Брама старту не пройдена** — Вова перемкнув на іншу задачу.

### 2. 🔴 ЗАСЛІНКА РОЗРОБКИ — зроблено (замовлення того ж дня)

Вова дослівно: «щоб коли інші користувачі відкривають додаток, їм вибивало додаток
знаходиться в процесі розробки. І він був доступний тільки мені, моїй головній пошті.
І моїй другорядній пошті… щоб він більше нікому не був доступний, поки я не скажу
тобі це зробити.»

Його рішення на моїх питаннях: **«Текст + кнопка Увійти»** · замикаємо **тільки
додаток** (`admin.html` лишається як є).

- `src/core/dev-lock.js` (новий) — рубильник `DEV_LOCK`, список **хешів SHA-256**
  замість відкритих пошт (репозиторій публічний), прапорець пристрою `cstl_dev_ok`,
  fail-closed (не змогли перевірити → не пускаємо).
- `style/dev-lock.css` (новий) + рядок у `style.css`. Вигляд від заставки —
  щоб чужий бачив завершений брендований екран, а не «застряглу завантажувалку».
- `src/app.js` — виклик **після `initAuth()` і до решти** `init()`; без доступу
  `init()` просто не доходить далі. Функцію `init` зроблено `async`.
- `scripts/check-imports.js` — до списку глобалів додано `TextEncoder`/`TextDecoder`
  (потрібні для `crypto.subtle.digest`), інакше `node build.js` падав.
- `sw.js` — `CACHE_NAME = cstl-20260730-1452`.
- `CLAUDE.md` — рядок про новий модуль у таблиці файлової структури.

**Рішення, яке варто запамʼятати: замок НЕ діє на localhost/127.0.0.1.** Сенс замка —
сховати публічний деплой; до локального сервера ніхто ззовні не дістанеться. Заразом
це тримає стенди зеленими **без секретного обходу в коді** — а обхідний ключ був би
справжньою дірою, бо жив би в `bundle.js` вічно.

**Сторож `tests/dev-lock.mjs` (13 перевірок).** Головна міряє не «чи намальована
заслінка», а **чи НЕ побудований застосунок під нею**. Підміняє домен через
`--host-resolver-rules=MAP cstl.local 127.0.0.1` — інакше замкнений стан не перевірити.
- **Контроль 1:** localhost → заслінки нема, застосунок будується.
- **Контроль 2 (провів):** навмисно зламав замок (`passDevLock()` без `await`/`return`)
  → сторож **упав саме на тих двох перевірках** (11/13), після відкату 13/13.
- ⚠️ **Моя помилка в самій мірці:** перша версія перевірки «усі записи — хеші»
  рахувала лапки **в коментарях-підказках** і впала на порожньому списку. Виправив
  (коментарі відрізаються до підрахунку). Це рівно те, від чого застерігає правило
  «мірку перевіряй так само, як код».

**Стенди: 29/29 · 372 перевірки** (було 28/359).

🔴 **НЕ ЗАКРИТО — чекає на Вову:** список допущених **порожній**, бо пошт він поки не
дав. Порожній список замикає і його самого → **деплоїти не можна, поки не впишу хеші.**

- **[старт]** Створено цей журнал. Код не чіпав — чекаю «Роби».

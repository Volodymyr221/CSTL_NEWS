# Журнал сесії — 2026-08-25 (друга сесія дня, акаунт claude/startuem-uh2hzo)

> Веду по ходу. Кожна значуща дія — окремий рядок.
> Літера `b`: за 25.08 журналу не було взагалі (перша сесія дня, PR #1013-#1015,
> його не завела), а вона могла ще працювати — лишаю їй просте імʼя.

---

## Старт сесії

- **Гілка:** `claude/startuem-uh2hzo`, стартувала синхронною з `origin/main`
  (`git log origin/main ^HEAD` і зворотній — обидва порожні).
- **`origin/main` на старті:** `c7c7bd37` (автокоміт новин 25.08 10:01 UTC);
  останній людський — `33ea4938` «Питання: лінія чорна 28% + згортання гілок
  як у «Стрічці» (#1015)».
- **`CACHE_NAME`:** `cstl-20260825-0857`.
- **Автор комітів:** `Вова / vitocorleone191@outlook.com`.
- **`/byyou`:** `idle`, активного потоку немає — продовжувати нічого.

## Прочитано (обовʼязкове за `/startuem` Крок 2)

`NOW.md` · `HOT_RULES.md` · `CSTL_BUGS.md` · `BACKLOG_VOVA_2026-08.md` ·
`ВОВА_ПРОФІЛЬ.md` + `NEW_SESSION_PROMPT.md` (контракт сесії).

## Розбіжності, знайдені при читанні (доповів Вові першою відповіддю)

1. **`NOW.md` авто-блок застарів.** Зріз на 09:24 UTC: гілка `claude/startuem-upyy76`,
   сім файлів «не доїхало в `main`» (`bundle.js`, `sw.js`, `board-discussions.js`,
   `base.css`, `board.css`, `tests/questions.mjs`, журнал `-24b`). Насправді все
   доїхало о 08:57 через PR #1015 — зріз просто зроблено ДО мерджу. Не помилка,
   а вік файлу.
2. **Скіл `/startuem` каже «`HOT_RULES.md` — 11 правил», у файлі їх 14** (+ `11-БІС`).
   Правила №12 (капсули) і №13 (два акаунти) — найсвіжіші й найдорожчі.
3. **Два списки обовʼязкового читання досі розходяться.** `/startuem` Крок 2 —
   пʼять файлів із бюджетом ≈23k токенів (сторож `tests/docs-fresh.mjs`).
   `NEW_SESSION_PROMPT.md` — вісім, зі словами «не пропускай жодного», тобто той
   самий перевантажений список, який 15.08 і різали. Це рівно та вада, про яку
   писали 06.08 у беклозі.
4. **Журналу за 25.08 не було**, хоча сесія зробила три PR (#1013, #1014, #1015).

## Стан: чекаю «Роби»

---

## Завдання від Вови (25.08, ~10:30) — «відповідати від імені спільноти» у Стрічці

**Слова Вови:** у Стрічці є спільноти, у кожної адмін/модератор. Пости йдуть від
імені спільноти. Коментар адміна йде від імені його ОСОБИСТОГО акаунта. Треба
дати вибір: відповідати як спільнота або як я. **Поки що тільки Стрічка.**
⚠️ Паралельно працює друга сесія у вкладці «Питання» — туди не заходити.
Вова приніс відповідь ChatGPT (модель `actor_type`/`actor_id`) на аналіз.

### Що прочитав у коді (перед тим як радити)

- `scripts/supabase_pages.sql` — `pages` · `page_admins(page_id, uid, role)` де
  `role in ('owner','admin')` · `can_edit_page(bigint)` SECURITY DEFINER ·
  політики `pcom insert` = `author_uid = auth.uid()`, `pcom update` = автор,
  адмін або `can_edit_page` поста.
- `page_posts` — `page_id NOT NULL` + `author_uid` + **`show_author`**.
  Тобто пост ЗАВЖДИ від сторінки, а `show_author` лише додає підпис «— Імʼя».
- `src/tabs/feed.js:2927-2933` — **перемикач ідентичності ВЖЕ Є** у композері
  поста: `.fd-comp-as` «Публікувати як [Назва спільноти] [Від себе]»,
  default `showAuthor = false` (тобто від спільноти). CSS `style/feed.css:1094`.
- `src/tabs/feed.js:1430` `commentRowHtml()` — імʼя/аватар суто з `author_uid`
  (`liveName`, `cachedAvatar`), `mine` для «Редагувати/Видалити» теж.
- `src/core/supabase.js:2534` `addPageComment()`; `myPageIds` у feed.js:72.
- `supabase/functions/send-comment-push/index.ts:95,137` — заголовок push
  будується з `nameOf(c.author_uid)`.

### Мій висновок (доповів Вові, «Роби» ще нема)

**Проти GPT у трьох місцях:**
1. 🔴 `actor_type`/`actor_id` замість `author_uid` — не робити. Загубиться слід
   людини (модерація), поламається антиспам (рахує за автором, B-26), видалення,
   `reply_to_uid`, капсули, push. Правильно: `author_uid` лишається ЗАВЖДИ,
   додається nullable `as_page_id` — рівно як уже влаштовані пости.
2. 🔴 Default «спільнота» — не робити. У Стрічці ВСІ пости від сторінок, тож це
   означає «адмін завжди говорить офіційно». Ціна помилки несиметрична.
3. 🟡 «Не привʼязувати до автора поста» — у БАЗІ привʼязати: `as_page_id` мусить
   дорівнювати `page_id` того поста, інакше адмін «ФК Олика» відповість як
   «ФК Олика» під постом сільради.

**Чого GPT не бачив:** push розкриє імʼя адміна · `as_page_id` не можна міняти
при правці (тригер `page_comments_guard_update`) · окремого «модератора» в базі
нема · вибір завжди рівно два, пошук по спільнотах не потрібен.

**Стан:** чекаю відповіді на три питання (default · бейдж «Адмін» · права).

# Журнал сесії — 14.08.2026 (гілка `claude/startuem-hcj90j`)

> Живий журнал: дописую після кожної значущої дії. Комітую і пушу кожні кілька кроків.

---

## Крок 0 — старт сесії (`/startuem`)

- `git config user.name "Вова"` + `«робоча пошта Вови (підпис комітів)»` — коміти підписані Вовою.
- `git fetch origin` → **HEAD == origin/main**, обидві звірки порожні
  (`origin/main ^HEAD` і `HEAD ^origin/main`). Нічого не загубилось, нічого не відстає.
- Останнє на проді: `59b35958` «Стрічка» звірена з референсом, «Питання» ще більша (#914).
  `CACHE_NAME = cstl-20260814-2013`.
- Прочитано: HOT_RULES · START_HERE · SESSION_STATE_VOVA · AUDIT_2026-07 ·
  BACKLOG_VOVA_2026-08 · CSTL_BUGS · CLAUDE.md · BYYOU_PLAN.

## Знайдені розходження в документації (доповів Вові першою реплікою)

1. **`START_HERE.md` відстав на три дні.** Секція «Поточний стан» стоїть на
   **11.08 (сесія U-22)**, тоді як `SESSION_STATE_VOVA.md` уже описує **14.08**
   (правова відповідність, `delete_my_account()`). START_HERE читається як карта
   проєкту — новий чат бере з неї застарілий стан.
2. **Журналів сесій за 12, 13 і 14.08 у `_session-log/` немає.** Останній —
   `vova-2026-08-11b.md`. За ці три дні пройшли: повний текст новин (парсер),
   правова відповідність (PR #909), іконки таб-бару (#910…#914). Правило
   NEW_SESSION_PROMPT крок 4 вимагає журнал на кожну сесію.
3. **`BYYOU_PLAN.md` каже статус `working` («чекаю браму деплою»)**, хоча робота
   вже в `main` — PR **#909** змерджено 13-14.08. Тобто потік фактично закритий,
   а файл показує його живим; за HOT_RULES №11 два чати не мають вести один
   BYYOU_PLAN, тож застряглий статус може ввести в оману наступну сесію.

## Стан на момент старту

- 🔴 Відкритих критичних багів **немає** (B-31 закрито 09.08).
- 🟡 Відкриті: B-28 (класифікатор, 93.2% «Суспільство») → блокує B-14 (тематичний
  фільтр); B-25 (клавіатура, дві спроби провалились); B-22 (семантичні дублі новин).
- Черга робіт (BACKLOG_VOVA_2026-08): збори (⏸ чекає перший справжній збір від
  Вови) · спадковий відступ `.cm-content` · капсули проти віджета автобусів.
- На проді лежить **тестовий запис** у `data/fundraisers.json` («ТЕСТ — перевірка
  блока зборів», з 05.08).

---

## «Роби» → ІКОНКА «СТРІЧКА», ПʼЯТА РЕДАКЦІЯ (скарга: «все що в рамці виглядає злито»)

**Слова Вови дослівно:** *«Треба іконку стрічки зробити 1в1 як на фото, зараз там
все що в рамці виглядає злито»*. Питання про порядок ліній (верхня довша чи
коротша) лишилось без відповіді — узято як у коді і як знято з фото раніше:
**верхня довша**.

### Діагноз — просвіти тонші за саму лінію

Іконка малюється **21px**, тобто масштаб viewBox 0.875, а лінія 1.75 × 0.875 = **1.53px**.

| просвіт | було | стало |
|---|---|---|
| квадрат ↔ праві лінії | **0.25px** | **1.63px** |
| між двома правими лініями | **0.50px** | **1.63px** |
| лінія ↔ рамка справа | 1.25px | 1.25px (не чіпав) |

Тобто зазори були **третиною** товщини лінії — згладжування країв домальовувало їх
сірим, і три деталі читались однією плямою.

### Що змінено (`index.html`, лише 3 числа)

- крок правих ліній `2.3 → 3.5` (центри y 8.5/10.8 → **7.6/11.1**)
- початок ліній `x 12.1 → 13.7`
- радіус кутів квадрата `1.17 → 0.75`

🛑 **Рамку, довгу нижню лінію і розмір квадрата НЕ чіпав** — чорнило (bbox) задає
саме рамка, тож центр 12,12 і площа 320 лишились. Сторож `tabbar-icons.mjs` — **18/18**.

⚠️ **Чесно про відступ від макета:** на фото штрих ТОНШИЙ відносно рамки (≈1.45 у
наших одиницях проти наших 1.75), тому там ті самі пропорції дихають, а в нас — ні.
Товщину міняти не можна — 1.75 це вся родина таб-бару, сторож вимагає її однаковою.
Дихання повернуто ВІДСТАНЯМИ.

### 🔴 Прилад збрехав з першого разу (дванадцятий такий випадок у проєкті)

Перша версія `icon-gaps.mjs` показала «просвіт між лініями = **0px**» на цілком
робочому малюнку. Причина: зрізи були прибиті цвяхами до **старих** позицій ліній
(y 8.5), а нові переїхали на 7.6/11.1 — зріз пройшов рівно **між** ними, по порожнечі.
✅ Лікування: прилад тепер САМ знаходить смуги фарби вздовж зрізу і рахує проміжки
між ними, а горизонтальний зріз бере з фактично знайденої верхньої лінії, а не з
мого очікування.

### ⚠️ Що вимір НЕ підтвердив

Третя моя претензія — «отвір квадрата зʼїдений радіусом кутів» — виміром **не
підтвердилась**: ширина отвору по центру **1.88px і до, і після** (радіус зʼїдає
кути, а не середину). Тобто зміну радіуса лишаю як наближення форми до макета
(на фото кути ледь заокруглені: 20% сторони проти наших 30%), але «злитість»
лікували не вона, а два просвіти вище.

### Стан

- `CACHE_NAME = cstl-20260814-2112` · `node build.js` пройшов
- Коміт `5b655929` запушено в робочу гілку. У `main` НЕ пушив.
- ⏸ Чекаю, що скаже Вова по знімку

### `npm test` — 71/73 стенди, 1279/1282 перевірки. Обидва червоні НЕ мої

**1. `sidebar-ghost-tap` 16/17 → при повторному прогоні 17/17.** Нестабільність
таймінгу (флап), не регрес. Падала перевірка «привид через 250мс НЕ закриває меню» —
вона за побудовою міряє вікно в чверть секунди.

**2. `weather-day` 44/46 — стабільно, але це ЖИВІ ДАНІ, а не код.**
🔑 **Доведено контролем:** `BUNDLE_REV=origin/main node tests/weather-day.mjs` дає
**ті самі 44/46 і ті самі дві перевірки**. Тобто на коді ДО моїх змін усе так само.
Причини обидві зовнішні:
- «стани по годинах РІЗНІ — **Ясно**»: стенд вимагає різноманіття погоди, а зараз
  до кінця доби прогноз рівний;
- «375px: видно щонайменше 3 картки — **1**»: прогін о 21:12 Києва, до кінця доби
  лишилось три години → три картки взагалі.

⚠️ **Це вже другий такий стенд у проєкті** (перший — `home.mjs`, який тримається за
живі дані парсера, записано 08.08). Спільна вада: перевірка міряє СВІТ, а не код,
тому червоніє за розкладом, а не за помилкою. 🛑 Коду не чіпав — за правилом №9
(знайшов зайве/зламане, про що не просили — скажи Вові, не лагодь мовчки).

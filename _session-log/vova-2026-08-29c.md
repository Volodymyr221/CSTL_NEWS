# Журнал сесії — 29.08.2026 «c»

**Гілка:** `claude/startuem-flaldh` · **Старт:** /startuem
**Стан на старті:** `git diff origin/main HEAD` порожній — гілка = `origin/main` (останній PR #1101).

---

## Крок 0 — старт сесії

- `git config user.name "Вова"` + `user.email vitocorleone191@outlook.com` — авторство комітів.
- `git fetch origin` — звірка двома командами: `HEAD ^origin/main` порожньо, `origin/main ^HEAD` (без `auto(`) порожньо. Розбіжностей нема.
- `/byyou` статус — `idle`, продовжувати нема чого.
- Прочитано обов'язкове: `NOW.md` · `HOT_RULES.md` · `CSTL_BUGS.md` · `BACKLOG_VOVA_2026-08.md` · `ВОВА_ПРОФІЛЬ.md`.
- Заведено цей журнал (літера «c» — за 29.08 уже є `vova-2026-08-29.md` і `-29b.md`).

## Далі

Чекаю «Роби» від Вови.

---

## Замовлення Вови — екран «тільки телефон» на компʼютері

**Дослівно:** «веб версія застосунку адаптована тільки для телефону, для компʼютера ні,
тому якщо користувач відкриває компʼютерну версію то потрібно написати що зараз доступна
тільки телефонна версія, але в стилі монобанку, І + QR код для сканування з телефону щоб
перекидало на сайт напряму з телефону».

**Три рішення Вови на мої питання:**
1. QR — **варіант Б**: веде на ТУ САМУ адресу, яку відкрили (не на головну).
2. Порядок — **після заслінки розробки**.
3. Обхідної кнопки — **ні**: «компʼютерної версії, поки вона не налаштована, вони не
   мають бачити взагалі, тільки після того як налаштуємо».
   ⚠️ Наслідок озвучено і прийнято: Вова на своєму Mac теж упреться в цей екран.

### Що зроблено

- 🆕 `src/core/qr.js` — власний кодувальник QR (байтовий режим, рівень корекції M,
  версії 1-10). Без бібліотеки з мережі: PWA має працювати офлайн, а сторонній скрипт
  ще й бачив би наших відвідувачів.
- 🆕 `src/core/desktop-gate.js` — сам екран. Розвилка **не за шириною**, а за вказівником:
  `(pointer: fine) and (hover: hover)` + ширина ≥ 900px. Дотикові пристрої проходять усі
  (iPhone із «Запросити версію для компʼютера» лишається телефоном, планшет — планшетом).
- 🆕 `style/desktop-gate.css` — `.dg-*`, фон `--brand-grad` (той самий, що на заставці).
- `src/app.js` — `if (!passDesktopGate()) return;` одразу після `passDevLock()`.
- `style.css` — імпорт після `dev-lock.css`.

### 🔬 Що НЕ спрацювало з першого разу

1. **Кодувальник давав код, який не сканувався б жодною камерою.** Службові біти формату
   малювались із переплутаними рядком і колонкою — розбіжність **9 точок із 625**, на око
   код виглядав бездоганно. Спіймано порівнянням із незалежною реалізацією (пакет `qrcode`
   у пісочниці, у проєкт НЕ додавався).
2. **Хибна тривога на тому ж порівнянні:** ще два випадки розходились, поки не з'ясував, що
   еталон розумніший — для цифр і великих літер він вмикає компактніші режими. У режимі
   «байти» проти «байтів» — **10 із 10 збігів точка-в-точку**.
3. **Низьке вікно притискало вміст угору** (`align-items: flex-start` у медіа-запиті) —
   при 650px висоти лишалась порожня нижня половина. Замінено на `margin: auto` у вмісті:
   центрує, поки вміщається, і чесно віддає прокрутку, коли перестало.

### 🧪 Виміряно

- `tests/qr.mjs` — **17/17**. Еталон **чужий**: відбитки матриць зняті з незалежної
  реалізації і заморожені, тож наш код не може підтвердити сам себе.
  **Контроль** `BUNDLE_REV=origin/main` → **0/1** (модуля там немає).
- `tests/desktop-gate.mjs` — **20/20**. Міряє наслідок, а не форму: екран є І застосунок
  під ним НЕ побудований; на телефоні й у вузькому вікні — без змін; QR збігається з
  кодом для адреси, яку браузер справді показує (перевірено і на `#/post/news/12345`);
  низьке вікно гортається до коду й адреси.
  ⚠️ Перша перевірка стенда — **сам прилад**: чи Chromium узагалі розрізняє мишу і дотик.
- Оглянуто знімками на 1440×900, 1280×650 і 1100×430 — саме так знайшлась вада №3.

### 🛑 Чого НЕ робив і чому

- `style/desktop-gate.css` **не додавав** у `STATIC_ASSETS` у `sw.js`. Той список і так
  неповний (`dev-lock.css`, `crop.css`, `install.css`, `news-hub.css` там теж немає), а
  кожен рядок — це файл, який КОЖЕН телефон качає заново при кожному деплої. Екран
  десктопний, офлайн на компʼютері не буває. **Сказати Вові.**

`CACHE_NAME` → `cstl-20260829-2055`.

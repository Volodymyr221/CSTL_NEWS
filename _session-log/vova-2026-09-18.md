# Журнал сесії — 18.09.2026 (Вова)

**Гілка:** `claude/startuem-kil67a` · production `main`

---

## §1. Що робили

Основний обсяг дня — **пітч-дек на грант** (18 слайдів, подано) і **креативи
для Instagram** (12 штук). Обидва лежать поза репозиторієм. Сюди записано лише
те, що торкнулось коду.

Наприкінці — **передзапускова перевірка «перший житель»** на замовлення Вови
(«роби А, дуже детально»), і з неї виросли всі правки нижче.

---

## §2. 🔴 ГОЛОВНА ЗНАХІДКА: ТРИ ВКЛАДКИ МОВЧАТЬ, КОЛИ БАЗА НЕ ВІДПОВІЛА

### Як знайшли

Прогін підняв застосунок із **недосяжною базою** — рівно те, що буває в селі зі
слабким сигналом. Заміряно на живому екрані:

| вкладка | що бачить людина |
|---|---|
| **Стрічка** | **0 знаків.** Порожнє полотно |
| **Питання** | **0 знаків** |
| **Дошка** | «Тут поки порожньо» — тобто **оголошень у громаді немає**. Неправда |
| Громада | ✅ «Погода тимчасово недоступна» + новини з локального файлу |
| Автобуси | ✅ розклад із локального файлу |

Другий прогін — **повільний** звʼязок (база думає 12 с): усі три мовчали
**щонайменше 6 секунд**. Ні скелета, ні «Завантажую», ні слова.

➡️ Це рівно правило пульсу: **«Якщо "не працює" неможливо відрізнити від "усе
гаразд" — це вада»**. Тепер уже вп'яте.

### Причина — одна на всі три

Відмова мережі приходить **ВИНЯТКОМ, а не значенням**. `fetchPublishedPosts()`
віддає `null` лише тоді, коли Supabase відповів `{ error }`. Обрив звʼязку — це
`reject`: `Promise.all` кидає, `loadData()` кидає, і виклик нагорі його ковтає
(`.catch(() => {})` в `openDiscussions`) або не ловить узагалі. **Малювання просто
не відбувається** — звідси порожньо.

А `fetchPages()` мала другу ваду того ж роду: при помилці повертала `[]`, тобто
збій був **нерозрізненний із «спільнот немає»**.

### 🛑 Чому це пройшло повз 155 стендів

Кожен із них підміняє Supabase заглушкою, яка **завжди відповідає**. Сцена
простіша за життя — та сама вада приладу, що вже тричі описана в пульсі.

### Що зроблено

- 🆕 `src/core/screen-state.js` — спільний стан «вантажу» і «не змогли».
  Скелет має **видимий напис «Завантажую…»**: сірі смужки нічого не кажуть тому,
  хто вперше тримає такий застосунок.
- `feed.js` — скелет **до** походу в мережу; `try/catch` навколо `loadData()`;
  стан збою з кнопкою «Спробувати ще».
- `feed.js` / `loadData` — `pg === null` тепер збій. Саме `pages`, а не `posts`:
  стрічка без жодної сторінки — це завжди збій (сторінки не зникають), а
  порожній список постів при живих сторінках — законний стан.
- `board.js` / `loadBoardData` — виняток тепер повертає `false`, тобто йде в той
  самий стан збою, що й `{ error }`.
- `board.js` / `renderBoard` — скелет **до** мережі, з `force`: інакше на екрані
  лишалось застаріле «Тут поки порожньо» і застосунок **першим кадром** казав
  неправду про громаду.
- `supabase.js` / `fetchPages` — збій віддає `null`, не `[]`.
- `home-feed.js` — підстраховано `|| []` (єдиний інший виклик).
- `board.js` — напис збою узгоджено з вкладкою: «Питання тимчасово недоступні»
  замість «Дошка…» на вкладці Питань.
- 🆕 `tests/offline-screens.mjs` — сторож. **9/9.** Ламає мережу і питає, що на
  екрані. Контроль — Громада (вона при тій самій зламаній мережі мусить
  говорити). **Мутація доведена:** повернув `fetchPages → []` — стенд червоніє
  на Стрічці; повернув фікс — зелений.

---

## §3. 🔴 СКЛЕЄНІ СЛОВА В НОВИНАХ — НА ПЕРШОМУ ЕКРАНІ

На головній, у першій картці: «підтверджено **загибельзахисника
УкраїниКозакаВолодимира** Анатолійовича».

📐 Заміряно по `data/articles.json`: **18 таких місць**, усі з «Конкурента».

**Причина** — `scripts/parse_rss.py`, `_paragraphs_fallback` → `flush()`:
`"".join(buf)` **без розділювача**. Кожен inline-тег (`<a>`, `<b>`) кладе в `buf`
окремий шматок, і коли в джерелі текст іде впритул до посилання
(`повідомляє<a>Камінь-Каширська громада</a>`), слова зліплюються.

**Полагоджено:** `" ".join(buf)`, а слідом прибирання пробілу перед розділовим
знаком і після відкривної лапки — інакше міняли б одну друкарську ваду на іншу
(«громада .», «20 %»).

🆕 `scripts/test_glue.py` — сторож, **6/6**. Контроль: ознака мусить спрацювати
на СТАРОМУ способі збирання. Стеля живого файлу — 18 (спадок витісниться сам за
7–30 днів, сторож ловить НОВИЙ ріст).

---

## §4. 🔴 СТЕНД, ЩО БРЕХАВ НА РОБОЧОМУ КОДІ

`tests/home.mjs` — дві перевірки про кнопку збору були червоні.

**Код правильний.** У фікстурі стояло `until: '2026-09-15'`, а сьогодні 18-те:
збір «завершився» три дні тому, код чесно малює «Збір завершено» замість кнопки,
а сторож шукав кнопку. **Бомба сповільненої дії, закладена в день написання і
спрацювала 16.09.**

**Полагоджено:** дата рахується від «сьогодні + 30 днів».
➡️ **Правило на майбутнє:** фікстура, що описує ЧИННИЙ стан, не може містити
мертву дату. Стенд — 82/82.

---

## §5. Документи

`CLAUDE.md` і `START_HERE.md` — кількість стендів приведено до диска.
`NOW.md` — авто-блок перегенеровано (`scripts/now_update.mjs`).

---

## §6. ❓ ЛИШИЛОСЬ БЕЗ РІШЕННЯ

🔴 **`cms-chain` — конфлікт номерів.** Стаття `id: 1000022`, `kind: 'community'`
(«🏰 Олика готується до осіннього сезону», ТУРИСТИЧНА ОЛИКА) сидить у **діапазоні
кабінету** (≥ 1 000 000). Тобто два джерела тягнуть номери з однієї скриньки —
рано чи пізно два записи зіткнуться й один перезапише інший. Це наслідок фічі
«допис спільноти → Новини» (#1179/#1180).
➡️ **Потрібне рішення Вови:** окремий діапазон для дописів спільнот — чи стенд
визнає їх законними мешканцями кабінетного?

🟡 **`design-system` — 46% жирного** при стелі 45% (на `main` було 54%).
Покращили, поріг не взяли. Це пункт Д3, і закривати його можна лише вручну.


---

## §7. ФІНІШ

**PR #1184** → squash-merge у `main` (`94d224b0`) → **деплой #6014**.
`CACHE_NAME`: `cstl-20260918-0812` → **`cstl-20260918-1758`**.
Наступні прогони #6015 і #6016 зелені — тобто код на сайті.

🗣️ **Зауваження Вови, яке виправило мій порядок роботи:** «Почекай, то як мені
перевірити якщо ти не зробив фініш. Я просто завжди думав, що ти мусиш зробити
фініш, а тоді я перевіряю тебе.»

Він має рацію, і я записую причину своєї помилки: я пропонував «перевір перед
мерджем», не подумавши, що **перевіряти немає де** — сайт збирається лише з
`main`, а staging (пункт А3) не зроблено. Тобто моя обережність була порожньою.
➡️ **Порядок: `/finish` → Вова дивиться на сайті.** Не навпаки.

---

## §8. ВІДЕОРЕКЛАМА (поза кодом репозиторію)

🗣️ Вова: «Став тут і зроби відео рекламу про застосунок» — про навик
`/brag` (github.com/latent-spaces/brag).

**Що перевірив ПЕРЕД установкою** (на його ж запит «він не шкідливий»):
- плагін **не має `hooks`, `permissions`, `mcpServers`** — тобто нічого не
  виконується само. Це головний вектор ризику в плагіні Claude Code, і він чистий;
- уся робота лягає на `npx hyperframes` — пакет **heygen-com**, Apache-2.0,
  публікація через GitHub Actions OIDC, **`postinstall` відсутній**;
- залежності: `puppeteer-core`, `onnxruntime-node` (ШІ **локально**, не через API),
  `sharp`, `esbuild`, `hono`.
- ⚠️ **Чого НЕ перевірив:** повний код `hyperframes` не читав. У довідці `init`
  прямо написано про **«anonymous telemetry»** — факт рендеру кудись повідомляється.

**Що зроблено.** Сам плагін не ставив (його слеш-команди в цій сесії не працюють) —
пройшов його шлях руками через `hyperframes`. У контейнері бракувало: FFmpeg
(поставив `ffmpeg-static` з npm), Chrome для рендеру (`hyperframes browser ensure`),
GSAP (йшов із CDN — проксі ріже, поклав локально).

Результат: **21 с, 1080×1920, H.264**. Шість сцен: гачок із трьох питань →
Громада → Автобуси → Дошка → Питання → логотип і `castlelife.org`.
`hyperframes check`: 0 помилок, контраст **13/13 WCAG AA**.
Звуку немає — локальні моделі голосу й музики важать гігабайти, не тягнув.

🗣️ Вова: «Ну таке, нормально, потім будемо корегувати, як для проби то норм».

**Усе це живе в теці сесії, а не в репозиторії** — свідомо: у git не кладемо ні
`node_modules` інструмента, ні відео.

# Журнал сесії — 06.09.2026 (гілка `claude/startuem-u1eemi`)

> Живий журнал: дописується ПО ХОДУ після кожної значущої дії.
> Формат рядка: що зробив (файл/функція) · навіщо · що не спрацювало · слова Вови
> дослівно · PR# · деплой# · CACHE_NAME.

## §0. Старт сесії (`/startuem`)

- Підписав коміти Вовою (`user.name "Вова"`, `user.email vitocorleone191@outlook.com`).
- `git fetch origin` — гілка `claude/startuem-u1eemi` стоїть **рівно на `origin/main`**
  (`c1fd6965`, автокоміт парсера новин 06.09 04:47 UTC). Звірка двома командами:
  `git log HEAD ^origin/main` — порожньо, `git log origin/main ^HEAD` — порожньо.
  🔑 Тобто «не доїхало в `main`» зі старого авто-блоку `NOW.md`
  (`NOW.md` + `_session-log/vova-2026-09-05b.md`) вже доїхало — PR #1169 змерджено.
- Хук `SessionStart` попередив, що ЛОКАЛЬНИЙ `main` відстає на 50 комітів — це не про
  робочу гілку, вона свіжа. Робочу гілку не чіпаю.
- Прочитано обовʼязкове: `NOW.md` · `HOT_RULES.md` · `CSTL_BUGS.md` ·
  `BACKLOG_VOVA_2026-08.md` · `ВОВА_ПРОФІЛЬ.md` · `NEVYKONANI_ZAVDANNIA.md`.
- `_ai-tools/BYYOU_PLAN.md` — статус `idle`, незавершеного потоку немає, пересідати
  на іншу гілку не треба.

## §1. Що далі

Чекаю «Роби» від Вови. Без нього код не чіпаю (правило №3 HOT_RULES).

## §2. Замір перед роботою (без «Роби» — лише читання)

**`news-widget` 74/75 — задачі НЕМАЄ.** Прогнав стенд: **75/75**. Червонів рядок
«віджет новин НЕ найбільший блок Громади» — він порівнює висоту з СУСІДНІМИ
блоками, а не з числом. Сьогодні: новини **478px** проти Телефонів **776px** →
зелено. Історія: 26.08 ✅ (сусід 576) · 31.08 ❌ (сусід 395) · 03.09 ❌ 73/75
(від свіжих новин) · 06.09 ✅. 🔑 Тобто результат гуляє від того, які статті
сьогодні в стрічці — код не змінювався. Запропонував Вові викреслити Б2 або
зробити прилад нечутливим до вмісту. **Відповіді ще не було.**

**Пошук.** Заміряно: `core/search.js` підключений лише до Дошки; на
`hay.includes(q)` лишались `board-chat.js:766` (Повідомлення) і `buses.js:975`
(зупинки). Порахував зупинки — **26 унікальних назв**, тому автобуси я НЕ радив.

🗣️ **Слова Вови:** «Роби 2 і ще в питаннях зроби таке, можливо хтось буде шукати
якесь питання». Іконки таббара зняті: 🗣️ «мені подобаються впринципі поки».

## §3. 🔴 ЗНАХІДКА, ЯКА ЗМІНИЛА ПЛАН: ДВИГУН НЕ ЗНАЄ ПРЕФІКСІВ

Перед підключенням прогнав двигун на живих прикладах:

```
велосипед  → знайшлось      вело → НІЧОГО
велосипеда → знайшлось      вел  → НІЧОГО
Оксана     → знайшлось      окса → НІЧОГО
```

🔑 Для Дошки це нормально (людина формулює запит і тисне «знайти»). Але в
Повідомленнях стояв ПІДРЯДОК, який префікси якраз ловив — тобто пряме
переведення на двигун було б **не покращенням, а регресом**: набрав «вело» — і
розмова зникла.

✅ Заведено `opts.prefix` у `core/search.js`: префікс **лише для ОСТАННЬОГО слова
запиту** (єдиного, яке людина в цю мить донабирає) і **від 3 літер** (на двох у
видачу лізе пів застосунку). За замовчуванням ВИМКНЕНО.

⚠️ **Поправка до самого себе, зроблена вголос.** Спершу написав у коментарі
«Дошку не чіпаємо». Це було б гірше: у Дошки й Питань **ОДНЕ поле вводу**, яке
лише міняє вкладку, і префікс на одній вкладці й не на іншій читався б як
поломка. Увімкнув на обох, коментар переписав під правду. Вові сказав окремо.

## §4. Блок А — ПИТАННЯ: пошук бере ВІДПОВІДІ

- `board-discussions.js` → новий експорт **`answersText(postId)`**: усі ЖИВІ
  відповіді одним рядком. Бере **всі**, не лише кореневі (на відміну від
  `answersCount`): людина могла запамʼятати слово з репліки під чужою
  відповіддю. Видалені не бере.
- `board.js` → `пошуковіПоля()`: для `activeType === 'chat'` поля
  перевизначаються **`title` = текст питання (вага 50)**, **`text` = відповіді
  (вага 15)**.
  🔴 До цього питання шукалось вагою 15 — у записі `type:'chat'` немає ні
  `title`, ні `tags`, текст лежить у `text`. Тобто головне поле розділу шукалось
  найтихішим.
  🔑 Ваги і є відповіддю на «а раптом знайдеться зайве»: питання зі словом У
  САМОМУ ПИТАННІ стоїть ВИЩЕ за те, де слово лише в чиїйсь відповіді.
- `renderQuestionCard(p, opts)` → підпис **«✦ знайдено за…»** на картці питання.
  ⚠️ Другий аргумент — ОБʼЄКТ, не рядок: у файлі вже стоїть попередження, що
  `.map(renderQuestionCard)` передав би ІНДЕКС; з обʼєктом `(2).why` це
  `undefined`, з рядком у картку надрукувалось би голе число.

## §5. 🛑 СТЕНД СПІЙМАВ СПРАВЖНЮ ВАДУ ДО ДЕПЛОЮ

Перша редакція стенда шукала питання в `#board-content` і дала «0 з 3». Пішов
дивитись — і виявилось не те, що стенд поганий, а що **питання малюються в
`#disc-content`** (`getBoardRoot()` віддає його при відкритому overlay).

🔴 А CSS підпису scoped `:is(#board-content, #cm-board-content)`. Тобто на картці
питання «✦ знайдено за…» вийшов би **голим `<div>` — чорним, 16px, урівні з
текстом питання**. Додано `#disc-content` у селектор.

🔑 Той самий клас промаху, що з хабом «Збережені» 05.09: правило було, прилад
був, а місце — поза полем зору (`shub-*` не підпадали під взірець сторожа).

## §6. Блок Б — ПОВІДОМЛЕННЯ

`board-chat.js`: `hay.includes(q)` → `smartSearch(base, q, { fields, prefix })`.
Пошук винесено ПІСЛЯ фільтрів (як на Дошці): «Архів» і «Непрочитані» — вибір
людини, пошук не має права його перебивати. Поля: імʼя співрозмовника → `title`
(50), оголошення розмови + останнє повідомлення → `text` (15). Правило «беремо
ВСІ оголошення розмови, не лише останнє» збережене з попереднього коду.

## §7. Сторож `tests/qa-chat-search.mjs` — 23/23

Нічого не імпортує з `core/search.js` навмисно: механізм уже доведений трьома
стендами (35 · 30 · 17), а вони НЕ доводять, що поверхня його кличе. Стенд
відкриває застосунок і друкує запит у справжнє поле.

| прогін | результат | що це доводить |
|---|---|---|
| свіжий код | **23/23** | — |
| `BUNDLE_REV=origin/main` | **16/23** | падають рівно нові вміння: збіг за відповіддю · вкладена відповідь · підпис · відмінок і порядок слів у Повідомленнях |
| **мутація** «прибрати `prefix`» | **21/23** | падають рівно дві перевірки незакінченого слова |

🔑 Мутація потрібна була саме тому, що «вело» і «окса» в контролі ЗЕЛЕНІ — старий
підрядок їх ловив. Без мутації ці дві перевірки не стерегли б нічого.

## §8. Перевірено сусідів (нічого не зламано)

`search-stem` 35/35 · `search-engine` 30/30 · `search-synonyms` 17/17 ·
`board-search-live` 13/13 · `questions` **105/105** · `board-filters` 17/17 ·
`board-saved-tab` 12/12 · `board-card-row` 22/22 · `design-system` 19/19 ·
`dead-css` 8/8 · `docs-refs` 10/10 · `news-widget` 75/75.

`CACHE_NAME` → `cstl-20260906-0517`. Кількість стендів у `CLAUDE.md` і
`START_HERE.md` піднято 146 → 147 (сторож `docs-refs` цього вимагає).

## §9. Фініш

**PR #1170** → squash у `main` (`4f8ac86f`) → деплой **#5563**.
`CACHE_NAME cstl-20260906-0517`. Документація перед мерджем: `docs-fresh` 23/23,
`docs-refs` 10/10.

⚠️ **Пульс упирався в стелю 16 KB** (18 085 після дописування). Стелю НЕ піднімав
(правило `NOW.md` → НЕ ЧІПАТИ): вирізав те, чий висновок живе в літописі —
розгорнуті рядки 05.09«b», абзац «перевірено пальцем 24-30.08» і **другу копію**
списку «пальцем (Вові)», яка дублювала реєстр Д5. Кінцево 16 223 → після
авто-блоку 16 134.

🔓 **Лишилось за Вовою (записано в пульс, питання 8-10):** префікс на Дошці
лишати чи вимкнути · пошук зупинок робимо чи закриваємо · доля Б2 `news-widget`.

⚠️ **Інцидент на фініші (нічого не втрачено).** Спробував дописати ці рядки з
локального `main` — і коміт ліг на **мертву гілку**: локальний `main` стояв на
**30.08** і має **51 власний коміт**, яких в `origin/main` немає ЖОДНОГО. Причина
структурна, не збій: ми мерджимо **squash**, тобто в `origin/main` лягає ОДИН
новий коміт замість наших, і локальна історія з ним не сходиться назавжди.
`git reset --hard` заблокував харнес (правильно зробив). Обійшов без затирання:
нова гілка від `origin/main`, правки заново, пуш у `main`.
🔑 **Для наступної сесії:** локальний `main` у цьому контейнері — не джерело
правди й ніколи ним не був (HOT_RULES №11). Працюй від `origin/main`.

---

## §10. 🧭 ІКОНКИ ХАБУ «ЗБЕРЕЖЕНІ» — СТАНДАРТИЗОВАНО

🗣️ **Скарга Вови зі знімка:** «Коли я натискаю кнопку Збережені, в шапці додатку
мені вилазить моделька і там деякі іконки не такі не такого вигляду, як зовсім
інша іконка, наприклад, на питання. Це треба стандартизувати, пофіксити.»

### Заміряно перед роботою

Хаб тримав **власний** набір значків (`CATS` у `core/saved-hub.js:49-52`), не
повʼязаний із розділами, куди рядки ведуть. Два з чотирьох суперечили застосунку:

| рядок | було | що показує сам розділ |
|---|---|---|
| Статті | `ICONS.newspaper` | ✅ збігається з «Новини» в бургер-меню |
| Автобуси | `ICONS.bus` | ✅ байт-у-байт той самий шлях, що в таб-барі |
| **Питання** | `ICONS.message` — бульбашка з рядками | ❌ таб-бар: **дві бульбашки зі знаком «?»** |
| **Оголошення** | `ICONS.pin` — мітка на карті | ❌ таб-бар «Дошка»: **планшет** |

🔴 **«Питання» — найгірший випадок:** `ICONS.message` це РІВНО той малюнок, який
**11.08 прибрали з таб-бару як «іконку ЧАТУ»** зі старих «Обговорень». Далі його
перемальовували ще шість разів до концепції «питання → відповідь». У хабі дожила
версія, від якої відмовились.

🔴 **«Оголошення»:** `pin` у цьому застосунку **вже означає МІСЦЕ** — він стоїть
на самій картці оголошення біля назви села. На знімку Вови обидва знаки видно
ОДНОЧАСНО: угорі pin = «Олицька громада», нижче pin = «Оголошення». Один знак,
два значення.

### Рішення: прибрати ДРУГЕ ДЖЕРЕЛО, а не підмінити значки

Бургер-меню цю задачу вирішило ще 10.08 — воно **читає значок із таб-бару**
(`tabIcon()`), і в його коментарі прямо записано чому: «джерело фізично одне, а не
"домовились однакові"». Хаб був єдиним місцем, яке цього не робило.

- `tabIcon()` **переїхала** з `core/sidebar.js` у `core/icons.js` (споживачів
  стало двоє). Копію малюнка не заводив свідомо: іконку «Питання» Вова
  перемальовував **сім разів**, і копія розійшлася б із оригіналом на першій же
  редакції — причому МОВЧКИ, бо обидві версії правильні самі по собі.
- `CATS` дістав поле `tab`; значок береться **в момент малювання** (`catIcon`), а
  не при завантаженні модуля — `CATS` це константа рівня файлу, і на той час
  таб-бару в документі може ще не бути.
- `icon` лишився в кожного рядка **запасним**: таб-бару може не бути (стенд, що
  будує лише аркуш).
- «Статті» лишились на `ICONS.newspaper` — Новини це НЕ вкладка (`openNewsHub`),
  брати значок нізвідки; збігається з пунктом бургер-меню, тобто джерело все одно одне.

### 🛑 Пастка, яку створила б сама стандартизація

Значки тепер приходять із ДВОХ джерел: таб-бар віддає **жорсткі 22px**
(`.tab-icon`), `ICONS` — 1em (19px). Без нормалізації два рядки з чотирьох стали б
більшими за сусідів — тобто виправлення значків спричинило б НОВИЙ розлад.
Додано `.shub-cat-ic svg` і `.shub-head-title svg` = 19px.
⚠️ Селектор по **тегу**, а не по класу `.cat-ico`: значок із таб-бару носить
`.tab-icon` і під `.cat-ico` не підпадає — рівно той промах, через який беж дожив
у цьому ж хабі до вересня.

### Стенд

Розширено `tests/saved-hub.mjs` **21 → 26 перевірок** (нового файлу не заводив —
поверхня та сама).

🔑 **Міряється ЗБІГ МАЛЮНКІВ, а не наявність виклику.** Перевірка «у файлі є слово
`tabIcon`» була б зеленою і тоді, коли функція повертає ЗАПАСНИЙ значок (селектор
промазав) — тобто рівно у зламаному випадку. Тому порівнюються самі шляхи `d`
намальованих `<svg>` у хабі й у таб-барі.

🛑 **Контроль на сам прилад:** порівняння двох `null` теж дало б «збіг», тому
окремо вимагається, щоб значки існували і були РІЗНІ між собою.

| прогін | результат | що доводить |
|---|---|---|
| свіжий код | **26/26** | значки збіглися, усі чотири 19×19 |
| `BUNDLE_REV=origin/main` | **24/26** | падають рівно «Питання» і «Оголошення»; «Автобуси» зелені навмисно — той малюнок збігався й до фіксу |
| **мутація** «прибрати `.shub-cat-ic svg`» | **25/26** | розміри стають `19x19 22x22 22x22 22x22` — тобто перевірка розміру справді стереже нормалізацію |

**Сусіди цілі:** `sidebar-menu` 49/49 (він живе тією самою функцією — головний
ризик переїзду) · `sidebar-account` 11/11 · `tabbar-icons` 18/18 ·
`modal-surface` 13/13 · `dead-css` 8/8 · `board-saved-tab` 12/12 ·
`account-scope` 10/10 · `docs-refs` 10/10.

`CACHE_NAME` → `cstl-20260906-0734`.

### 🔎 Знахідка збоку — НЕ чіпав

У `core/icons.js` три значки написані з подвійним доларом: `` `<svg $${A}>` ``
(`stethoscope`, `copy`, `bank` — усі три з блоку телефонів громади). Через це в
розмітку йде **`$width="1em"`** замість `width` — тобто атрибута ширини немає.
📐 **Заміряно в браузері: 19×19 в обох варіантах** — браузер бере ширину з
`viewBox`, тож оком різниці немає. За правилом №9 («не чіпати те, про що не
просили») лишив на місці й доповів Вові. Це латентна одруківка, не видима вада.

### ⚠️ Мердж на старті блоку

`git merge origin/main` дав два конфлікти, обидва розведені вручну (ніколи
`-X theirs` на весь мердж):
- `_session-log/vova-2026-09-06.md` — бік гілки був ПОРОЖНІЙ, §9 з `main`
  збережено цілком;
- `CSTL NEWS VOVA/NOW.md` — узято версію `main` (правило HOT_RULES №13 для
  пульсу). Перевірено дифом, що вона строгий надмір: уся розбіжність — авто-блок
  і номер PR, які `main` має свіжіші.

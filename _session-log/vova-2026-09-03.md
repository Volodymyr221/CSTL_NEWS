# Журнал сесії — 2026-09-03

**Гілка:** `claude/startuem-ztzxpr` · **Акаунт:** сесія Claude Code (web)

---

## 1. Старт сесії (`/startuem`)

- `git config user.name "Вова"` / `user.email vitocorleone191@outlook.com` — виконано.
- `git fetch origin` — гілка **синхронна з `origin/main`** (обидві звірки порожні:
  `origin/main ^HEAD` = порожньо, `HEAD ^origin/main` = порожньо).
- Прочитано повністю: `NOW.md`, `HOT_RULES.md` (усі 15 правил), `CSTL_BUGS.md`,
  `BACKLOG_VOVA_2026-08.md` (шапка + розмежування ролей із `NOW.md`), `ВОВА_ПРОФІЛЬ.md`.

### ⚠️ Помічено: авто-блок `NOW.md` застарів на ~4 години
Зріз у `NOW.md` стоїть на **03.09 05:29 UTC** і показує гілку `claude/startuem-wzboev`
та список «не доїхало в `main`» із сімома файлами. Насправді з того часу
**PR #1142 («Ретро-дедуп: правило діє й на вже збережену стрічку») уже змерджено**
в `main` (коміт `82464081`), тобто `parse_rss.py`, `tests/news-dedup.mjs`,
`tests/ai-budget.mjs`, `tests/cms-chain.mjs` **доїхали**. Перезапустити
`node scripts/now_update.mjs` — але тільки коли Вова дасть «Роби» на будь-яку роботу
(зараз файл читається як шпаргалка, не як істина).

**Стан:** чекаю «Роби» від Вови.

---

## 2. Новий проєкт T-K Performance — перенесення скілів

🗣️ **Вова:** «створив новий репозиторій… сайти для СТО, автомайстерні, яка знаходиться
в Англії… всі скіли, які я в тебе закачував… розпиши мені, чи ссилки мені скинь».
Далі: «T-K-Performance. Назва репозиторію, роби сам якщо можеш» — це «Роби».

### 🪤 Головна знахідка: симлінки
Сім із десяти скілів у `CSTL_NEWS` лежать **не файлами, а симлінками** з
`.claude/skills/` у `.agents/skills/` (`animate`, `animation-vocabulary`,
`apple-design`, `emil-design-eng`, `find-animation-opportunities`,
`improve-animations`, `review-animations`). Пряме копіювання `.claude/` дало б
у новому репо **сім битих ярликів** — скіли б мовчки не працювали.
➡️ Копіював через `cp -RL` (розіменування). Звірено: симлінків у `main` — **0**.

### Що зроблено
- `add_repo` + клон `volodymyr221/t-k-performance` (був **порожній**, 0 комітів).
- Перенесено **9 скілів**: `ui-ux-pro-max` (564 KB, база CSV) · `emil-design-eng` ·
  `apple-design` · `animate` · `review-animations` · `improve-animations` ·
  `find-animation-opportunities` · `animation-vocabulary` · `vibe-audit`.
- Перенесено **5 команд**: `/audit` `/mockup` `/fix` `/new-file` `/qa-explore`.
- Додано чистий `.claude/settings.json` (**без хуків** — у CSTL вони з абсолютним
  шляхом `/home/user/CSTL_NEWS/...` і в новому репо зламались би мовчки) + `.gitignore`
  + `SKILLS_README.md`.
- Коміт `a7573e6`, 52 файли, гілка `main`. Звірка `HEAD ^origin/main` = **0**.

### Що свідомо НЕ переносив (сказав Вові)
`/startuem` `/finish` `/onovy` `/byyou` `/ai-seo` `/seo-audit` `/social`
`/copywriting` `/content-strategy` `/community` `/launch` `/marketing-ideas`
`/analytics` + скіл `caveman` — усі зав'язані на документацію CSTL (`NOW.md`,
`HOT_RULES.md`, `SESSION_STATE_VOVA.md`) і на Олику. У порожньому репо впали б.
Для СТО їх треба **писати заново**, не переносити.

⚠️ **Дрібниця, полагоджено:** клон порожнього репо не створює `remote.origin.fetch`,
тож `git log origin/main` падав з `bad revision`. Дописав refspec руками.

**Відкрите питання до Вови:** чи робимо `/startuem` і `/finish` під СТО.
---

## 3. Джерела скілів знайшлись — Вова дав два репозиторії

🗣️ **Вова:** `github.com/leonxlnx/taste-skill` + `github.com/emilkowalski/skills` —
«Ці скіли є там? Якщо ні завантаж».

🔑 **Це закрило моє ж «посилань немає»** з попереднього кроку. Публічні джерела
існують, просто в `CSTL_NEWS` вони ніде не записані — ні в скілах, ні в git-історії
(перевіряв грепом по URL і `--diff-filter=A`). ➡️ **Висновок на майбутнє:** ставиш
чужий скіл — записуй джерело поруч, інакше через місяць його не знайти.

### Відповідь на питання Вови
- **`emilkowalski/skills`** — 12 скілів, я мав **7**. Бракувало 5: `animate-expo`,
  `ask-sonner`, `pick-ui-library`, `prototype`, `write-swift`.
- **`leonxlnx/taste-skill`** — 13 скілів, я мав **0**. Не було жодного.

### Звірка версій (робив diff, не на око)
6 із 7 скілів Еміла — **побайтово ідентичні**. Відрізнявся лише `animate`: один
рядок, у автора дописано посилання на `animate-expo`. Узяв версію автора.

### Зроблено
- `add_repo` (read) + клон обох репозиторіїв.
- Скілів **9 → 27**. Зіткнень імен немає — перевірив: тека ≠ `name:` у 8 випадках
  (`brutalist-skill` → `industrial-brutalist-ui`, `soft-skill` →
  `high-end-visual-design` тощо), але дублів у `name:` **0**.
- Ліцензії авторів (MIT × 3) збережено в `.claude/LICENSES/`.
- Коміт `da5e8d1`. Звірка: незапушено **0**, скілів у `main` **27**, ярликів **0**.

### 🛑 Що сказав Вові чесно, а не сховав
1. **`brandkit` і `imagegen-frontend-web` генерують зображення** — у Claude Code
   генератора картинок немає. Скіл увімкнеться і головного не зробить.
2. **`gpt-taste`, `image-to-code`, `stitch-design-taste`** написані під ІНШІ
   інструменти (GPT, Codex, Google Stitch), не під Claude Code.
3. **Пряме протиріччя між скілами:** `minimalist-ui` каже «жодних градієнтів і
   важких тіней», `high-end-visual-design` задає конкретні тіні як ознаку дорогого
   вигляду. Обидва можуть спрацювати на одному запиті. ➡️ Напрямок називаємо
   словами на старті задачі, не покладаємось на автовибір.
4. `animate-expo`, `write-swift`, `imagegen-frontend-mobile` — про мобільні
   ЗАСТОСУНКИ, не про сайт.

Поставив усе (Вова просив «завантаж»), але розписав у `SKILLS_README.md`, що з цього
ядро, а що баласт. Рішення прибирати — за Вовою.
---

## 4. Передача: CSTL закрито, T&K заведено

🗣️ **Вова:** «Роби все, щоб я зараз перейшов в новий чат з цим репозиторієм новим,
і без втрати контексту, щоб не змішувати з CSTL LIFE».

🛑 **Чому НЕ `/finish`:** він мерджить у `main` і **запускає автодеплой на
`castlelife.org`**. Коду CSTL я сьогодні не змінив — на гілці лише журнал.
Деплой живого сайту заради коміту в документацію = підняти лічильник версії ні за що.
➡️ Зробив те, що робить `/onovy`: авто-блок пульсу перегенеровано
(`node scripts/now_update.mjs`), у `ЗРОБЛЕНО` дописано рядок, сторож зелений.

⚠️ **`NOW.md` стояв за 477 байт від стелі** (15907 з 16384). Дописав рівно один
рядок → 16266. **Наступна сесія має різати, а не дописувати.**

### Зроблено в T&K (репозиторій `Volodymyr221/T-K-Performance`, коміт `cdc179b`)
- `CLAUDE.md` — бізнес, два типи клієнта, правила, **мова сайту — британська
  англійська** (`tyres`/`centre`/`specialise`), публічність репо.
- `NOW.md` — пульс: що є, що далі, 5 відкритих питань (стек, заявки, домен,
  мова, ціни), розділ «не чіпати».
- `/startuem` і `/finish` **написані** під проєкт, не скопійовані.
- `fix`, `qa-explore`, `audit` переписані; `mockup` адаптовано;
  **`new-file` видалено** (був цілком про стек CSTL: `sw.js`, `bundle.js`).
- 🔴 Прибрано `node --check` як брехливу перевірку — на `import`/`export` вона
  мовчки повертає 0.
- Звірка: незапушено 0 · скілів 27 · команд 6 · битих ярликів 0.

**Стан CSTL:** код не зачеплено. На гілці лише журнал і пульс.

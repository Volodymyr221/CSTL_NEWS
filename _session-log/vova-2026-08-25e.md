# Журнал сесії — 25.08.2026 (сесія «e»)

> 🔀 **ПЕРЕЙМЕНОВАНО ДВІЧІ ЗА ВЕЧІР: `-25c` → `-25d` → `-25e`.**
> Спершу літеру `c` взяла паралельна сесія другого акаунта (гілка
> `claude/startuem-xwy8jd`, її журнал о 15:51, мій о 17:14). Я перейменувався на `-25d` —
> і **та сама сесія взяла `-25d` теж**, для свого продовження після деплою #5079, і встигла
> змерджити його в `main` (PR #1031) раніше за мій PR.
> 🔴 **Тобто колізія імені журналу спрацювала ДВІЧІ за один вечір**, хоч HOT_RULES №13 прямо
> про неї попереджає («24.08 обидві сесії взяли `-b`»). Правило каже «глянь, які літери
> зайняті» — але глянути можна лише в те, що вже **запушене**, а паралельна сесія тримає
> свій журнал у себе, поки не зробить `/finish`. **Перевірка перед стартом принципово не
> може це зловити.**
> ➡️ **Що справді допомогло б:** літера від ГІЛКИ, а не від порядку — напр.
> `vova-2026-08-25-5nmz43.md`. Тоді дві сесії не можуть зіткнутись за визначенням.
>
> _(нижче — початковий підпис перейменування)_
> 🔀 **Перше перейменування з `-25c` о 18:4x.** Літеру `c` того самого дня взяла ПАРАЛЕЛЬНА
> сесія другого акаунта (гілка `claude/startuem-xwy8jd`) — її журнал створено о **15:51**,
> мій о **17:14**, тож перейменувався я. Рівно та колізія, про яку попереджає HOT_RULES
> №13 («24.08 обидві сесії взяли `-b` і довелось перейменовувати») — і цього разу вона
> спрацювала знову, бо жодна з сесій не бачить журналів іншої, поки та не запушить.

> Живий журнал. Дописується ПО ХОДУ після кожної значущої дії.
> Формат рядка: що зробив (файл/функція) · навіщо · що НЕ спрацювало · слова Вови дослівно · PR# · деплой# · CACHE_NAME.

## Старт

- **Гілка:** `claude/startuem-5nmz43`. Звірено з `origin/main`:
  `git log HEAD ^origin/main` — **порожньо**, `git log origin/main ^HEAD` (без `auto(`) — **порожньо**.
  Тобто гілка = `origin/main` (`b2f9f293`), нічого не загубилось і нічого не висить незапушеним.
- **Автор комітів:** `Вова <vitocorleone191@outlook.com>` (правило Крок 0; «Unverified» ігноруємо).
- **`/byyou`:** статус `idle` — незавершеного потоку немає, пересідати нікуди.
- **Прочитано:** `NOW.md` · `HOT_RULES.md` (0-13) · `CSTL_BUGS.md` (відкриті: B-14, B-28, B-25, B-22; критичних **нема**) ·
  `BACKLOG_VOVA_2026-08.md` (пункт 0 — гарантія «хто я» на всі вкладки) · `ВОВА_ПРОФІЛЬ.md`.
- **Стан:** чекаю «Роби» від Вови. Код не чіпав.

## Кроки

_(дописується по ходу)_

### Крок 1 — розбір задачі «віджет Стрічки на Громаді» (обговорення, без коду)

**Замовлення Вови (дослівно, стисло):** «збільшити сам віджет, бо пост обрізається» ·
«спільноти зверху не на всю ширину віджету… назви написати повністю, можна в два рядки» ·
«аранжування спільнот по тому, хто який пост закинув останній» · «слайд постів з виділенням
спільноти зверху… виділення плавно стає на місце, без ривків… збільшується наступна
спільнота і виділяється паралельно». Плюс розлогий розбір від ChatGPT (карусель, 5с,
350-500ms ease-in-out, `line-clamp: 2` на назвах, `latest_post.created_at DESC`).
⚠️ Скрін Вова згадав, але він **не долетів** — розбір зроблено по коду.

**Прочитано:** `src/tabs/home-feed.js` (133 рядки) · `style/home.css:1332-1392` (`.hm-fd-*`) ·
`src/tabs/community.js:203-213` (розмітка секції `#hm-feed`) ·
`src/tabs/community-blocks.js:1876-2007` (`paintCmNews` + `startNewsCarousel`) ·
`src/core/supabase.js:2179-2236` (`fetchPages`, `fetchPagePosts`).

**Що знайшов сам (чого в розборі GPT немає):**

1. 🔴 **Справжня причина «не на всю ширину»** — `.hm-fd-circles { grid-template-columns:
   repeat(6, 1fr) }` (`home.css:1341`). Колонок ЗАВЖДИ 6. Спільнот менше → вони сідають у
   перші N колонок, решта ширини порожня **праворуч**. Це не «центральна частина», як
   написав GPT, і лікується не розтягуванням, а кількістю колонок за фактом.
2. 🔴 **Діра в даних під нову логіку:** `fetchPagePosts(null, 12)` бере 12 останніх постів
   **ЗАГАЛОМ**, а не по одному з кожної спільноти. Одна активна спільнота з 12 постами
   поспіль → у віджеті буде ОДНА спільнота. Сьогодні не видно (постів мало), на живих
   даних вилізе одразу.
3. 🔴 **Колізія кольору:** бордове кільце (`--brand-grad-sm`, клас `hm-fd-c--new`) ВЖЕ
   означає «є новий допис за добу» (`FRESH_H = 24`). Вова просить бордовим позначати
   **активний слайд**. Один колір = два значення. Потрібне рішення Вови.
4. ⚠️ **Крапок під блоком Стрічки НЕМАЄ.** `hm-fdots` — це збори (`home-fund.js:446`),
   `hm-ndots` — новини (`community-blocks.js:1901`). GPT радив «синхронізувати наявні
   крапки» — синхронізувати нема чого. І другі крапки були б другим лічильником того
   самого стану (клас B-27, «одне джерело правди»).
5. 🔴 **Стабільна висота проти фото.** GPT правильно вимагає незмінної висоти віджета, але
   не сказав, як це поєднати з фото 16:9 (`.hm-fd-p-img`). Слайд без фото отримав би
   порожнечу в пів-картки — рівно те, що вже лікували в PR #943.
6. ✅ **Механізм каруселі вже є і перевірений** — `startNewsCarousel()`: `scroll-snap`,
   прокрутка браузером (`scrollTo` smooth), `visibleIndex()` за реальним положенням,
   пауза на `touchstart`/`pointerdown`, `IntersectionObserver`, `prefers-reduced-motion`.
   Правило №8 — не плодити дубль, а виносити спільне.
7. ❓ **Двозначність у замовленні:** «слайдиться на наступний пост цієї спільноти» — слайд
   = одна спільнота (5 слайдів) чи слайд = один пост (кілька підряд від однієї)?
8. ❓ **Дві автокаруселі на одному екрані:** новини вже крутяться самі кожні 7с
   (`NEWS_CYCLE_MS`). Стрічка додасть другу.

**Стан:** «Роби» ще не було. Код не чіпав.

---

## «РОБИ» отримано — 25.08

**Рішення Вови по ходу обговорення:**
- слайд = **спільнота** (не пост): 5 спільнот → 5 слайдів, у кожному її ОСТАННІЙ допис;
- автопрокрутка — **тільки коли блок у вікні** (`IntersectionObserver`, як у новин);
- 🔴 **бордовий лишається «є новий допис за добу»** — Вова відхилив мою пропозицію
  віддати колір активному слайду. Дослівно: «Бордовий означає є новий допис за добу, так
  людина та її так його побачить коли буде слайд».
- ✅ **Погоджено виділення активного через ГАСІННЯ РЕШТИ** (прозорість решти ~45%) +
  збільшення 46→52px + біла жирніша назва. Мій аргумент, який це вирішив: перша спільнота
  в ряду **завжди** має бордове кільце (вона ж писала останньою), тож бордове виділення
  поверх бордового кільця дало б **нуль видимої зміни на першому ж слайді**.
- Кільце-прогрес по колу активного — опція, Вова не відповів, **не роблю**.

⚠️ **Скрін так і не долетів** — жодного разу за три повідомлення. Працюю по коду.

### Крок 1 — ДАНІ: «останній допис кожної спільноти» ✅

- 🔀 Спершу `git merge origin/main` (правило №13) → `Already up to date`.
- **`src/core/supabase.js`** — нова `fetchLatestPostPerPage(maxPages, scan)` + константа
  `POST_COLS_OFFICIAL`.
  🔑 **Два запити:** зонд (`id, page_id, created_at`, limit 300 — три маленькі поля) дає
  ТОЧНИЙ порядок спільнот за свіжістю; добірка (`.in('id', …)`) тягне повні дані рівно тих
  кількох дописів, що підуть на екран.
  🛑 **Відхилено «один запит на спільноту»** — стільки походів у мережу, скільки спільнот,
  а головна це найдорожче місце.
  ⚠️ Порядок рахуємо САМІ, не покладаючись на `.order()` бази: групування по спільнотах
  усе одно на клієнті, а два джерела порядку розійшлись би. Заразом це зробило функцію
  придатною для стенда — заглушка `_board-fixture.mjs` не сортує і не обмежує ЗОВСІМ
  (`order`/`limit`/`is` там порожні заглушки, звужують лише `eq/neq/in/gt/lt`).
- **`src/tabs/home-feed.js`** — джерело замінено, `postHtml(p, page)` дістав другий аргумент.
  🔑 Кружечок і допис під ним тепер **одна одиниця з одного списку** (`slides`) — розсинхрон
  між рядом і карткою неможливий у принципі (клас B-27).
  ⚠️ Спільнота без дописів у віджет не входить: у неї немає ключа для впорядкування.
- `node --check` ✅ · `node build.js` ✅ (56 файлів, check-imports чисто).
  ⚠️ `node_modules` у контейнері не було — `npm install` перед білдом.

### 🆕 Заведено СТОРОЖ, якого не існувало: `tests/home-feed-widget.mjs`

🔴 **Знайшов при роботі:** `grep hm-fd-` по всій теці `tests/` давав **НУЛЬ**. Віджет жив
із 04.08 і не був покритий нічим — його можна було зламати мовчки.
📐 **Сцена навмисно ворожа:** `sort_order` СУПЕРЕЧИТЬ свіжості (Olyka Castle має
`sort_order: 3`, але писала останньою), одна спільнота написала **12 дописів поспіль**
(рівно та дірка, що з'їдала стару вибірку `fetchPagePosts(null, 12)`), і є спільнота
**без жодного допису**.
**10/10 ✅** — порядок `Olyka Castle | Олицька міська рада | Молодіжна рада | КЦ «Центр» |
Туристичний центр`.
➡️ Контрольний прогін (на `origin/main`) — ЩЕ НЕ ЗРОБЛЕНО, чекаю завершення фонового
набору: 🛑 «не міряй посеред роботи» — підміна `bundle.js` під час чужого прогону збрехала б.

**Контрольний прогін зроблено (підміна `bundle.js` на `origin/main`):**
- набір `home feed` на новому коді — **250/250 ✅** (6 стендів), нічого не зламано;
- сторож на СТАРОМУ коді — **6/10**, на новому — **10/10**.
- 🔬 **Контроль знайшов ваду в САМІЙ перевірці.** «Картка показує допис першої спільноти»
  зеленіла на старому коді, хоча ряд там починався з «Молодіжна рада», а картка показувала
  Olyka Castle. Причина: перевірка міряла проти ОЧІКУВАНОГО списку, а не проти фактично
  першого кружечка — тобто **називалась одним, а міряла інше**. Переписано на
  `зріз.авторКартки === зріз.імена[0]`, після чого контроль чесно показав
  `ряд: Молодіжна рада · картка: Olyka Castle` і став **6/10**.
  🔑 Це доказ, що розсинхрон ряду й картки був СПРАВЖНІЙ, а не гіпотетичний.

### Крок 2 — РЯД СПІЛЬНОТ: на всю ширину, назви повністю ✅

**`style/home.css`:**
- `.hm-fd-circles`: `repeat(6, 1fr)` → **`grid-auto-flow: column` + `grid-auto-columns: 1fr`**.
  🔑 Колонок рівно стільки, скільки прийшло елементів — **без жодного числа в JS**, тобто без
  другого джерела правди про кількість. Зазор s2→s1 (8→4px): на 390pt це +4px до колонки,
  і саме вони вирішують, чи вміститься другий рядок назви. `align-items: start` — інакше
  аватари стрибали б по вертикалі при різній довжині підписів.
- `.hm-fd-c-name`: `nowrap` + `ellipsis` → **`line-clamp: 2`** з ФІКСОВАНОЮ висотою двох
  рядків. Висота фіксована не для краси: без неї ряд різновисокий, а з кроку 5 по ньому
  ходить виділення — стрибок висоти під час руху видно найбільше.
  `overflow-wrap: anywhere` — назва одним довгим словом інакше вилазить за колонку.
- `CACHE_NAME` → `cstl-20260825-1749` (правило №2 — змінився CSS).

**📐 ЗАМІРЯНО, А НЕ ПРИКИНУТО.** Я прикидав, що «Олицька міська рада» у 2 рядки при 61px
НЕ вміститься, і збирався зменшувати шрифт. Заміряв у браузері — **вміщається** (22px = рівно
два рядки по 11). Прикидка була хибна, шрифт лишився 9.5px.

### 🔬 КОНТРОЛЬ ЗНАЙШОВ ДРУГУ ВАДУ В МОЇЙ ЖЕ ПЕРЕВІРЦІ

Перевірка «ряд на всю ширину» зеленіла на СТАРОМУ коді. Дві причини, обидві мої:
1. 🔴 **Мірив ширину КОНТЕЙНЕРА.** `.hm-fd-circles` блоковий — його ширина дорівнює ширині
   віджета ЗАВЖДИ, навіть коли кружечки тиснуться в лівій третині. Порожнеча праворуч — це
   недобір ЕЛЕМЕНТІВ. Переписано на `br.right − останній.right`.
2. 🔴 **Сцена не напружувала випадок.** У ній було рівно 6 спільнот, а стара сітка мала
   6 колонок — тобто виглядала цілком правильною. Сцену зменшено до **5 спільнот у базі,
   4 з дописами**: тепер стара сітка лишає порожню колонку, і це видно числом.

**Контроль після правки: 9/15 на `origin/main` проти 15/15 зараз.**
📐 Скарга Вови заміряна: **порожнеча праворуч 55px** на старому коді → **0px** на новому.
Довга назва: **11px (один зрізаний рядок)** → **22px (два повні)**.

🛑 **Урок дня, вже другий поспіль:** обидві вади в перевірках знайшов КОНТРОЛЬНИЙ прогін, а
не написання. Зелений сторож без контролю не доводить нічого — він може міряти не те.

### Два сторожі документації впіймали мене — обидва по ділу

Прогін набору (обірваний мною на середині — див. нижче) показав `docs-fresh 19/20` і
`docs-refs 8/10`. Обидва падіння мої:

1. **`docs-refs`** — я додав **109-й стенд**, а в `CLAUDE.md` і `START_HERE.md` стояло 108.
   Сторож звіряє число з диском і падає при розходженні. Оновив обидва файли з описом стенда.
2. **`docs-fresh`** — спершу «пульс відстав від git» (те, що я помітив ще на старті сесії),
   виправив `node scripts/now_update.mjs`. Після цього виліз ДРУГИЙ провал: **пульс 16.4 KB
   при стелі 16 KB** — авто-блок дописав сім рядків «не доїхало в `main`». Стиснув два
   розділи (пʼята й шоста сесії), які повністю переказані в `SESSION_STATE_VOVA.md` і
   журналі `-25b`, лишивши адресу повного розбору. **16815 → 14814 байт.**
   ✅ Обидва тепер **20/20** і **10/10**.

🛑 **ЧЕСНО ПРО ОБІРВАНИЙ НАБІР:** я запустив `npm test` (109 стендів) у фоні, а через
кілька хвилин вирішив, що Вова простоює, і вбив його `pkill`. Вивід при цьому завершився
`exited with code 0` — **але це код УБИТОГО процесу, а не «все пройшло»**. Встиг показати
28 стендів, з них два червоні (обидва вище). ⚠️ Не робити з такого виводу висновку «набір
зелений»: обірваний прогін нічого не доводить про решту 81 стенда.

### Кроки 3-5 — КАРТКА · КАРУСЕЛЬ · СИНХРОННЕ ВИДІЛЕННЯ ✅

**Дві форми картки однакової висоти** (`home-feed.js` + `home.css`). 📐 Заміряно ПЕРЕД
роботою: допис зі знімком **302px**, без знімка **123px** — різниця в рази, тож одна форма
дала б півкартки порожнечі (урок PR #943). Стало: висота живе на ДОРІЖЦІ (280px), а форма
підбирається під вміст — `--photo` (текст 2 рядки 13.5px, знімок на весь залишок) і
`--text` (9 рядків **15.5px**, `margin: auto 0` — короткий допис читається як цитата
посеред картки, а не як текст, під яким забули вміст).

**`src/core/auto-carousel.js` — НОВИЙ спільний модуль.** 🛑 **НАЗВАНИЙ БОРГ:** карусель
новин (`startNewsCarousel` у `community-blocks.js`) на нього ще НЕ переведена, тобто в
проєкті тимчасово ДВІ реалізації. Свідомо: той файл прямо зараз пише друга сесія
(`claude/startuem-xwy8jd`), і правка під нею забрала б її роботу конфліктом (HOT_RULES
№13). Переведення — окремий крок після злиття тієї гілки.

🔑 **Синхронність БЕЗ другого таймера.** Спокуса дати ряду кружечків власний відлік «щоб
ішли разом» — але два відліки синхронними не лишаються: гортнув пальцем, і кружечок
показує одну спільноту, а картка іншу. Джерело правди одне — реальне положення доріжки.
🛑 **Збільшення через `transform`, ніколи `width`:** зміна ширини підняла б висоту ряду на
6px, тобто ряд стрибав би на кожному переході.

**📐 ЗАМІРЯНО (усе числами, не на око):**

| що | значення |
|---|---|
| висота всіх 4 слайдів | **280px, розкид 0** |
| висота віджета після переходу | **280 → 280** |
| висота ряду спільнот після переходу | **73 → 73** |
| видимий слайд ↔ активний кружечок | **2 ↔ 2** (збігаються) |
| прозорість: активна / решта | **1 / 0.45** |
| форми | без фото 15.5px/9р · з фото 13.5px/2р |

`CACHE_NAME` → `cstl-20260825-1831`. Сторож **26/26**, контроль на `origin/main` — **9/26**.

### 🔬 КОНТРОЛЬ ЗНАЙШОВ ТРЕТЮ ВАДУ В МОЇХ ПЕРЕВІРКАХ (і це вже система)

1. 🔴 **Перший вимір сказав «карусель не працює»** — `scrollLeft: 0` після 6.5с. Я мало не
   пішов лагодити справний код. Діагностика показала: `paused: 1`, `трекTop: 911` при вікні
   **844** — блок був НИЖЧЕ екрана, і мій же запобіжник чесно спав. Тобто вимір міряв
   ВЛАСНИЙ запобіжник, а не карусель. Лікує `scrollIntoView` перед заміром — і це тепер
   окрема перевірка «поза екраном не рухається».
2. 🔴 **Стенд КРЕШИВ на контролі** замість рахувати падіння: на старому коді `.hm-fd-track`
   не існує, і `track.scrollLeft` кидав виняток. Контроль, що помирає, не дає порівняти
   «до/після». Тепер `evaluate` повертає `null`, а перевірки чесно червоніють.
3. ⚠️ Дрібне: два різні `ok()` мали ідентичний рядок коду, і заміна влучила не в той —
   зловив `node --check`.

---

## /finish — публікація

**Крок 0-4.** Журнал ведеться по ходу ✅ · `CACHE_NAME` бампнутий о 18:31, після нього код
не мінявся (лише `.md` і тест) · `node --check` на трьох `.js` ✅.

🔴 **ПЕРЕД PR ПРИЛЕТІЛА ЧУЖА РОБОТА:** друга сесія змерджила **PR #1029** («гарантія «хто я»
на всіх вкладках») у `main` за хвилини до мого фінішу. Тобто спрацював рівно той сценарій,
який я передбачив і про який попереджав Вову.

**Злиття (правило №13, жодного `-X theirs`):**
| файл | як звів |
|---|---|
| `sw.js` | обидві бампали `CACHE_NAME` (`1831` проти `1815`) → взято ПІЗНІШИЙ і зсунуто на час злиття: **`cstl-20260825-1852`** |
| `bundle.js` | **не зводився руками** — `node build.js` |
| `NOW.md` | взято **ЧУЖУ** версію за основу (`--theirs`), свій розділ дописано ПЕРЕД її, її розділів не переписував |

🔴 **Пульс після зведення двох сесій важив 22.9 KB / 264 рядки** при стелях 16 KB / 200 —
сторож `docs-fresh` не пускав. Стискав **сімома заходами** до **16382 байт / 200 рядків**:
обидва розділи «ЗРОБЛЕНО» зведено до кістяка з адресами журналів, «Раніше 25.08» — до самої
адреси (розділ і сам каже «адреса, не переказ»), старі питання 2-6 зведено в рядок.
⚠️ **Заразом мало не зламав сторож:** перейменував заголовок «ЗРОБЛЕНО В ЦІЙ СЕСІЇ» — і
`docs-fresh` одразу сказав «розділу немає». Він шукає розділ ЗА НАЗВОЮ. Повернув.
⚠️ **І виправив «ДАЛІ»:** там ще стояв пункт 0 як наступне завдання, хоча його закрито
PR #1029 — нова сесія пішла б робити зроблене.

**Документація:** `CLAUDE.md` — `core/auto-carousel.js` доданий у карту файлів із названим
боргом · `SESSION_STATE_VOVA.md` — повний розділ сесії «d» · `BACKLOG_VOVA_2026-08.md` —
віджет закрито, борг каруселі поставлено ПЕРЕД пунктом 3 · `BYYOU_PLAN.md` — `idle`, не
чіпав. `docs-fresh` **20/20**, `docs-refs` **10/10**.

### 🔴 ДРУГЕ ЗЛИТТЯ ПЕРЕД PR — і колізія журналу ВДРУГЕ

PR #1032 не змерджився: `405 Pull Request has merge conflicts`. Поки я ганяв стенди на
зведеному коді, паралельна сесія випустила ще два PR — **#1030** (заслінка: «не можу
перевірити» більше не видається за «код не той») і **#1031** (передача), плюс приїхали
автокоміти парсерів.

**Чотири конфлікти, кожен за своїм правилом:**
| файл | як звів |
|---|---|
| `_session-log/vova-2026-08-25d.md` | **add/add** — вона взяла ТУ САМУ літеру, на яку я щойно перейменувався. Її версія вже в `main`, тож мій журнал став **`-25e`**, її `-25d` не чіпав |
| `SESSION_STATE_VOVA.md` | 🔴 тут «вибрати бік» = **загубити чужий день**. Зведено по межах заголовків: спершу ЇЇ розділ (уже в `main`), далі мій. Обидва цілі |
| `bundle.js` | перезібраний, не зводився |
| `sw.js` | обидві бампали (`1852` проти `1846`) → **`cstl-20260825-1920`** |

🔴 **КОЛІЗІЯ ІМЕНІ ЖУРНАЛУ СПРАЦЮВАЛА ДВІЧІ ЗА ВЕЧІР** (`c`, потім `d`), хоч HOT_RULES №13
прямо про неї попереджає. 🔑 **І це не недбалість — правило непридатне:** воно каже «глянь,
які літери зайняті», але глянути можна лише в **запушене**, а паралельна сесія тримає журнал
у себе до `/finish`. Перевірка перед стартом принципово не може це зловити.
➡️ **Пропозиція до HOT_RULES №13: літера від ГІЛКИ, а не від порядку** —
`vova-2026-08-25-5nmz43.md`. Тоді дві сесії не можуть зіткнутись за визначенням.

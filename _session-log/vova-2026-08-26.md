# Журнал сесії — 26.08.2026 (продовження віджета Стрічки)

## Дві вади з живого телефона (скріни Вови)

### 1. 🔴 Наведення ховало ВЕРХ допису під липку шапку спільноти

🗣️ «якщо багато постів, то наводить так що верхню частину посту обрізає, можливо воно не
враховує що є шапка з назвою спільноти». Фото 1 — шапки допису (аватар · назва · дата)
немає взагалі; фото 2 — як має бути.

🔑 **ДВІ ПРИЧИНИ, і друга виявилась головною:**

**(а) `block: 'center'`.** Він ставить у центр вікна САМ ЕЛЕМЕНТ, і для допису ВИЩОГО за
екран верх виїжджає вгору, під `.fd-screen-title` (`position: sticky; top: 0`).
✅ Стало `block: 'start'` + `scroll-margin-top` на живу висоту шапки (вона стискається при
прокрутці через `--p`, тож число міряємо в останній момент, а не беремо наперед).

**(б) 🔴 ЗНІМОК ДОВАНТАЖУЄТЬСЯ ПІСЛЯ ПРОКРУТКИ.** Прокрутка рахується від ПОТОЧНИХ висот,
а фото на той момент ще не приїхало — картка низька. Знімок приходить, картка виростає на
сотні пікселів і підштовхує все, що нижче: те, що ми поставили під шапку, виїжджає під неї.
✅ `whenImagesReady(el, 2500)` — чекаємо знімки САМЕ ЦІЄЇ картки, тоді ведемо.
⚠️ Межа часу обовʼязкова: мережа може не віддати фото ніколи, а прокрутка через це не сміє
не статись (клас `serviceWorker.ready` — воно не падає, воно ВИСНЕ).
🔑 **Чому цього не бачив жоден мій замір:** у стенді знімок локальний або `data:`, тобто
готовий у той самий кадр. На телефоні він іде з мережі.

### 2. 🔴 Вихід СВАЙПОМ давав «блим» — спершу Громада, потім Стрічка

🗣️ «виходить ніби технічний баг такий, типу блим… А якщо вихожу натиском на стрілочку то
все добре». Фото 3.

🔑 **Різниця саме в системній анімації iOS.** Жест «назад» малює ЗНІМОК попереднього запису
історії. Запис створює `openLayer` (`history.pushState`), і якщо він лягає в ТОМУ САМОМУ
кадрі, що й `switchTab('shotam')`, браузер ще не перемалював — у знімок потрапляє **Громада**.
Далі шар зникає, DOM показує Стрічку, і людина бачить два різні екрани поспіль.
🛑 **Кнопка «назад» цього не показує**, бо там анімацію малюємо МИ (`animateOut`), а не
система, і знімка не бере ніхто — тому вада виглядала вибірковою.
✅ `await новийКадр()` (два `requestAnimationFrame`) між `switchTab` і відкриттям шару.

## 🛑 ЧЕСНО ПРО ПЕРЕВІРКИ — ШОСТА ПОМИЛКА ВИМІРУ ЗА ДВІ СЕСІЇ

Написав дві перевірки на ваду №1 — **вони зелені і на коді ДО фікса**, тобто саму ваду не
ловлять. 📐 Заміряно чому: у сцені картка виходить **822px при вікні 844**, тобто
ВМІЩАЄТЬСЯ, і центруванню нема куди її зсувати (`scrollTop 0`). На телефоні Вови картка
вища за екран.
**Три спроби відтворити:** локальний файл → `data:` портрет 800×1200 → повільний маршрут із
портретом 800×1900. Жодна не спрацювала: висоту знімка задає `--fd-ar` з РЕАЛЬНИХ розмірів
файлу, і підробити її з фікстури не вийшло.
➡️ Тому в стенді записано прямо: ці дві перевірки стережуть **РЕГРЕС**, а не доводять фікс.
**Фікс перевіряється ЛИШЕ пальцем.** Вада №2 (системна анімація iOS) у Chromium не
відтворюється в принципі.
🛑 **Фальшиво-зелена перевірка гірша за її відсутність** — тому межа названа в коді, а не
захована за числом «37/37».

## 📐 ПРОСВІТ НАД КАРТКОЮ: 12 → 22px

🗣️ **Вова по знімку:** «хай трошки нижче, щоб було видно верхній край, але щоб не було
видно наступного поста — чуть-чуть буквально».

🔑 **22 = зазор між картками (8px) + радіус заокруглення (14px)** — обидва ЗАМІРЯНІ, не
взяті на око. При колишніх 12px край різався рівно на заокругленні, і картка читалась
«прилиплою» до шапки.
⚠️ **Верхня межа жорстка і тримає другу половину прохання:** просвіт більший за зазор
показує низ попередньої картки. Заміряно окремою сценою (6 дописів): просвіт вийшов рівно
**22px** при низі шапки 23 і верху картки 45.

### 🔬 СЬОМА ПОМИЛКА ВИМІРУ — і я вчасно спинився

Написав перевірку «просвіт у вікні 14…22px» — вона впала з **70px**. Пішов дивитись і
знайшов: у сцені стенда цільовий допис **ПЕРШИЙ у списку**, тобто браузеру нема що
піднімати (`scrollTop 33` при можливих 9443), і просвіт виходить природний, а не заданий
`scroll-margin-top`. Тобто число залежить від того, чи є куди прокручувати.
🛑 **Не став підганяти сцену під число** (це вже було б четверте переписування фікстури за
дві сесії). Замість цього переписав перевірку на НАСЛІДОК, який однаковий скрізь:
1. просвіт **не менший за радіус заокруглення** — інакше край ріжеться на закругленні;
2. **у просвіт не влізла ЧУЖА картка** — саме те, що Вова просив не показувати.
➡️ Обидві половини прохання тепер вимірні незалежно від того, перший допис чи ні. **39/39.**

---

# 🌆 ВЕЧІР 26.08 — АНАЛІТИКУ ПЕРЕПИСАНО ЗА ОРИГІНАЛЬНИМИ СЛОВАМИ ВОВИ

Вранці працювали за концепцією, яку Вові написав ChatGPT. Увечері Вова надіслав
**свій власний голосовий текст** — те, що він насправді диктував. Він і став джерелом
правди; частина пунктів ChatGPT-концепції з ним не збігалась.

Повний розбір — `_ai-tools/AUDIT_ANALYTICS_2026-08.md`, частина друга. Тут — суть.

## 🔴 ГОЛОВНА ВАДА: КОЖЕН ЗАХІД ПИСАВСЯ «ГОСТЕМ»

Знайдено **читанням порядку виклику**, не здогадом. `init()` кличе `initAuth()` **без
`await`**, а `passDevLock()` повертається з `localStorage`, тобто синхронно. Отже до
запису `session_start` ми доходили раніше, ніж `supa.auth.getSession()` встигав
відповісти по мережі — і `currentUserId()` віддавав `null` **навіть залогіненій людині**.

📐 Відбиток у базі: у **всіх 16** подій `session_start` `user_id` порожній, тоді як
`tab_view` при перемиканні вкладок — уже з `user_id`. Різниця рівно в моменті виклику.

➡️ Тихо й назавжди це вбивало саме те, чого Вова просив: «унікальні користувачі» і
«хто саме заходив» показували б **нуль людей**. Виправлено `await authReady()`.

## 🔑 ДРУГА ЗНАХІДКА: ОДИН ЗАХІД РАХУВАВСЯ ЗА ДВА

Перезавантаження сторінки в тій самій вкладці пише **другий** `session_start` із тим
самим `session_id` (`sessionStorage` переживає F5) — така пара вже лежить у базі
(09:44:16 і 09:44:34). Без `distinct on (session_id)` «скільки разів заходили» брехало б
угору рівно на кількість перезавантажень.

## 📊 ЩО ТЕПЕР НА ЕКРАНІ

**Один період на весь екран** (Сьогодні · 7 днів · Місяць). Раніше картки згори були за
три різні періоди, а всі розділи нижче — завжди за 7 днів, і ніде це не було написано.
Питання Вови «81 унікальний — це за що?» виникало саме тому.

**Кожне число вміє назвати людей.** Тап по будь-якому рядку → вікно «хто саме»: імʼя,
пошта, село, скільки заходів, днів і подій. Поки число не розкладається на імена,
перевірити його неможливо — а Вова просив саме перевірності («тільки правдива
інформація»).

**Три різні картки замість однієї суми:** Людей (акаунтів) · Заходів (запусків) ·
Гостьових пристроїв. До 26.08 екран показував **суму** першого й третього одним числом —
тому й здавалось, що аудиторія велика.

**Збої** — тап по рядку відкриває кожен випадок поштучно: хто, пошта, що сталося, коли,
на якому пристрої (замовлення Вови дослівно).

**Вік** — відповідь просто на екрані: Google при вході віддає лише пошту, імʼя й
аватарку, дати народження **не дає взагалі**. Вік із поля анкети, яке людина заповнює
сама. Те саме з селом: поле анкети, не геолокація.

## 🛑 ЧОГО НЕМАЄ — І ЦЕ НАПИСАНО НА ЕКРАНІ

Розділення «додаток чи браузер» застосунок навчився писати 26.08 09:19. За липень-серпень
цих полів у базі **немає взагалі**, а вгадати по User-Agent неможливо — у PWA і в
браузері він однаковий. Тому за порожній період екран каже це вголос замість малювати
нуль. Те саме з «заходами»: `session_id` існує з 26.08.

## 🧪 СТЕНД, ЯКОГО НЕ БУЛО

`tests/analytics-screen.mjs` — **48 перевірок**. Розділ жив без жодного стенда, і всі
його вади знаходив Вова очима. Окремо стереже **узгодженість UI з міграцією**: кожне
поле, яке читає екран, мусить бути в SQL — інакше екран мовчки малював би порожньо, ніби
даних немає.

Контроль: `BUNDLE_REV=origin/main node tests/analytics-screen.mjs` → **1/48**.

---

# 🌃 ПІЗНІЙ ВЕЧІР 26.08 — МОДАЛКА ЯК МОДАЛКА + AI-АГЕНТ ПОЧАВ ЧИТАТИ ПРАВДУ

Вова прислав скрін відкритого вікна деталей і поставив дві задачі поспіль.

## 1️⃣ ВІКНО ДЕТАЛЕЙ БУЛО НАКЛАДКОЮ, А НЕ МОДАЛКОЮ

🗣️ **Вова:** «задній фон, він скролиться, і свайпом я не можу закрити цю модалку… зроби
можливість свайпом закрити… заблокуй, щоб задній екран не рухався, тому що це модальне
вікно, це неправильно, і **зроби так в всіх варіаціях**, де я можу тапнути».

✅ **«Усі варіації» вийшли безкоштовно** — і це не збіг, а наслідок рішення, ухваленого
кількома годинами раніше: усі тапи екрана (число, вкладка, село, пристрій, збій) ведуть
в **ОДНУ** `openDetail()`. Правка в одному місці накрила всі шість.

🛑 **`overflow: hidden` на body для iOS НЕ ДОСИТЬ** — Safari однаково прокручує документ
під фіксованим шаром. Тому `position: fixed` зі зсувом на поточну позицію, а при закритті
позиція повертається. Той самий підхід, що `lockBodyScroll()` у застосунку.

🔑 **Свайп — порт `attachSheetDismiss()`, а не нова вигадка.** Ті самі числа: поріг 90px,
кидок від 0.45 px/мс, крива `cubic-bezier(0.32, 0.72, 0, 1)`. Разом із двома уроками,
кожен з яких куплений окремим багом у застосунку:
1. **зона шапки** — граб за рисочку закриває навіть із прокрученого списку (інакше з
   довгого журналу випадків не вийти, не догорнувши його вгору);
2. **замок на жест** — рух, який хоч раз БУВ прокруткою, до відпускання пальця вже не
   може стати закриттям (без цього шапка відривалась від тіла аркуша на iOS).

🛑 **Спільний модуль підключити було НЕ МОЖНА:** `admin.html` — самостійна сторінка зі
своїм JS, вона не бере `bundle.js`. Тому це свідомий порт, і в коментарях названо, що це
копія і звідки вона.

⌨️ Додано Escape і тап по тлу: адмінку відкривають і з компʼютера, де свайпу немає взагалі.

## 2️⃣ AI-АГЕНТ КОМЕНТУВАВ НЕПРАВДИВІ ЧИСЛА

🗣️ **Вова:** «знизу є AI-агент, я закинув там 5 доларів на API-ключ для новин… нам треба
налаштувати, щоб він давав висновки по аналітиці. Для чого він був побудований — він має
працювати для цього».

🔴 **Пішов дивитись — і знайшов гіршу з можливих вад.** Edge Function
`ai-analytics-summary` рахувала агрегати **САМА**, тим самим способом, який ми того ж дня
визнали хибним:
```
.from('analytics_events').select(...).limit(20000)
```
Тобто вона впиралась у ту саму серверну стелю **1000 рядків** і згодовувала моделі числа,
пораховані по першій тисячі з одинадцяти тисяч — ще й без `ORDER BY`.

🛑 **Найгірше тут не помилка, а її невидимість:** текст виходив розумний, упевнений і
абсолютно неправдивий. Це прямо суперечить головній вимозі Вови до всього екрана —
«тільки правдива інформація».

✅ **Тепер джерело те саме, що в екрана:** `admin_analytics_overview()` (три періоди),
`admin_failures()`, `admin_profile_stats()`.
🔑 **Наслідок, заради якого це й зроблено:** AI більше не може розійтися з екраном. Якби
він рахував по-своєму, Вова бачив би «6 людей» угорі й «336 відвідувачів» у висновку — і
не мав би способу зрозуміти, хто з двох бреше.

🔑 **RPC кличемо від імені викликача, а не під `service_role`.** Усередині функцій стоїть
гейт `is_admin()`, який читає пошту з JWT; під `service_role` жодного JWT немає, і функція
чесно віддала б `not admin`. JWT ми до того вже перевірили двічі.

🔑 **У завдання моделі вписано правило №12 дослівно:** «ЗАПУСКУ ЩЕ НЕ БУЛО… не роби
висновків про поведінку аудиторії — аудиторії ще не існує… "нуль подій у розділі" означає
"нуль користувачів", а НЕ "розділ не потрібен"». Без цього модель на 11 акаунтах видавала
б упевнені поради, побудовані на шумі.

⚠️ **КЛЮЧ ЛЕЖИТЬ НЕ ТАМ, І САМЕ ТОМУ КНОПКА МОВЧАЛА.** `ANTHROPIC_API_KEY` живе в **GitHub
Secrets** (ним працює агент новин), а Edge Function читає **інше сховище** — секрети
Supabase. Це два різні місця. Код виправити тут нічим: **ключ має покласти Вова руками**,
Supabase → Project Settings → Edge Functions → Secrets. Текст помилки тепер каже це прямо,
з повним шляхом, замість «AI не налаштовано».

## 🧪 СТЕНД

`tests/analytics-screen.mjs` виріс 48 → **67 перевірок**. Стереже і замок сторінки, і
замок на жест, і те, що AI читає ті самі агрегати.
⚠️ Одна перевірка спершу почервоніла **на власному коментарі**: шукала рядок `limit(20000)`
у файлі, а він там є — у шапці, як ДОСЛІВНИЙ опис прибраної вади. Тобто перевірка карала
б за документування помилки. Полагоджено: міряємо код, коментарі відкидаємо.

Контроль на `origin/main` → **49/67**.

---

# 🔧 НАЗВАНИЙ БОРГ ЗАКРИТО — ОДНА КАРУСЕЛЬ НА ВЕСЬ ЗАСТОСУНОК

25.08 з'явився `core/auto-carousel.js`, але карусель новин Громади лишилась власною
копією. У шапці модуля це чесно записали як борг із причиною: у `community-blocks.js`
паралельно працювала друга сесія, і правка під нею забрала б у неї роботу конфліктом
злиття (HOT_RULES №13). Та гілка доїхала в `main` — перешкода зникла.

🛑 **Чому це не косметика.** Дві копії одного механізму тут уже розходились **двічі**:
два списки антиспаму і дві копії розмітки шкали автобуса. Розходяться вони не одразу, а
в мить, коли одну з них полагодять — і вада читається як «полагодили, а в другому місці
досі зламано».

## 📐 ЗВІРЕНО РЯДОК У РЯДОК ПЕРЕД ЗАМІНОЮ

Обидві речі, яких немає в підручниках, у модулі виявились на місці: прокрутку робить
**браузер** (`scrollTo` зі `smooth`), а поточний слайд рахується за **реальним положенням
прокрутки**, не власним лічильником. Пауза поза екраном, пауза на дотик і повага до
«зменшити рух» — теж.

⚠️ **Одна відмінність, і вона на краще:** старий код при одній сторінці виходив ДО
малювання підпису, модуль однаково кличе `onSlide(0)`. Екран не міняється (категорію вже
намалював `paintNewsCat` вище, перша крапка має `on` із розмітки), зате споживач
гарантовано дістає початковий стан.

🔑 Пара `_newsTimer` / `_newsIO` замінена **одним зупинювачем**: тримати таймер і
спостерігач окремо означало мати ДВА стани однієї каруселі й шанс погасити лише половину.
Зупинка попереднього екземпляра лишається у викликача — віджет перемальовується, і без
неї на тому самому треку працювало б два таймери.

🗑 З шапки модуля прибрано «карусель новин ще НЕ переведена» — правда 25.08, яка
перестала бути правдою. **Той самий клас, що вже коштував правки у віджеті Стрічки:**
обґрунтування пережило свою підставу.

## 🧪 СТЕНД, ЯКИЙ МІРЯЄ НАСЛІДОК

`tests/carousel-shared.mjs` — **15 перевірок**. Головне не «чи є виклик модуля», а що
карусель **справді рухається**: заміряно на живій сцені доріжка **0 → 342px**, крапка
**0 → 1**, категорія **«Громада» → «Волинь»**.

⚠️ **Два запобіжники, обидва — від помилок виміру, які в цьому проєкті вже були:**
1. модуль навмисно **спить поза екраном**, тож стенд спершу доводить блок у вікно і
   перевіряє це **окремою** перевіркою. Без цього він «довів» би поломку там, де все
   справне — рівно та помилка, на якій 25.08 спіймався сторож віджета Стрічки;
2. **контроль із «зменшити рух»** вимагає, щоб доріжка СТОЯЛА (`0 → 0`). Інакше перевірка
   руху зеленіла б від будь-якого зміщення.

📐 Контроль `BUNDLE_REV=origin/main` → **10/15**: червоніють рівно п'ять кодових
перевірок. Перевірки руху там зелені **навмисно** — стара копія теж крутила, тож вони
стережуть, щоб переведення не ЗЛАМАЛО рух. Так і записано в самому стенді, щоб наступна
сесія не подумала, що вони доводять факт переведення.

## ✅ Повний набір

**109/111 стендів, 2301/2305 перевірок.** Два червоні були суто документаційні (пульс
відстав від git і кількість стендів 110 замість 111) — виправлено.

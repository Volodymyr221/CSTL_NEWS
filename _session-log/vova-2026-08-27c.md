# Журнал сесії — 27.08.2026 (сесія «c»)

**Гілка:** `claude/startuem-2mp937` · **Старт:** 19:19 UTC
**Стан на старті:** гілка синхронна з `origin/main` (обидва списки `^` порожні),
робоче дерево чисте, останній PR у `main` — **#1054**, `CACHE_NAME` `cstl-20260827-1753`.

## Крок 0 — старт `/startuem`

- `git config user.name "Вова"` + `user.email vitocorleone191@outlook.com` — виконано.
- `git fetch origin` — виконано. `git log --oneline HEAD ^origin/main` — **порожньо**;
  `git log --oneline origin/main ^HEAD` (без `auto(`) — **порожньо**. Тобто нічого не
  загублено і нічого не відстає.
- Прочитано: `NOW.md`, `HOT_RULES.md` (13 правил), `CSTL_BUGS.md` (відкриті), 
  `BACKLOG_VOVA_2026-08.md`, `ВОВА_ПРОФІЛЬ.md`, пріоритети `AUDIT_2026-07.md`.
- `BYYOU_PLAN.md` — статус **`idle`**, замка пушу немає, продовжувати нічого.

## Знайдено при звірці документів

- `NOW.md` блок AUTO вказує гілку `claude/startuem-wvx8sj` і список «не доїхало в
  `main`» на 12 файлів — це зріз **18:15 UTC**, тобто ДО мерджу #1054. Зараз усе в
  `main`, список неактуальний. Пульс перегенерується `node scripts/now_update.mjs`.

## Далі

Чекаю «Роби» від Вови.

---

## 19:30 — «Роби»: картка Дошки — локація багряна, закладка без овалу

**Слова Вови дослівно:** «Ціна зліва знизу зображена бордова, така в колір додатку, а
населений пункт, тобто Жорнище, Олицька громада, Котів, зображений є сірим, і він
зливається з описом. Нам потрібно оцей значок піна і назву населеного пункту також
відображати в бордовому кольорі… Та в самій карточці зверху справа знаходиться кнопка
Зберегти. Це такий ніби як значок, але цей значок ще в такому овальному обрамленні ніби.
Його потрібно прибрати, треба залишити просто цей значок збереження і все.»

⚠️ **Вова працює паралельно з ДРУГОГО акаунта в цьому ж репо** — правило №13. Тому
`git fetch origin main && git merge origin/main` зроблено **ДО** роботи, не перед PR.
Злилось чисто (6 файлів, парсер/тести — чужа сесія), конфліктів 0.

### Що знайдено

- Локація: `style/board.css` `#board-content .bd-ad-loc { color: #7A7A7A }`.
  📐 Заміряно в браузері: локація `rgb(122,122,122)`, опис поруч `rgb(118,118,118)` —
  **різниця 4 одиниці на канал**. Скарга «зливається з описом» була буквально точна.
- Закладка: `saveBtnHtml()` вішає `.bd-icon-btn` + `.bd-bookmark`. Базовий `.bd-icon-btn`
  малює овал ЧОТИРМА властивостями. Scoped-правило `#board-content .bd-ad .bd-bookmark`
  скасовувало рівно ДВІ (`border: 0`, `background: none`).
  🔴 **Лишались і малювали овал:** `border-radius: 50%` (на 40×32 це еліпс),
  `box-shadow: 0 1px 2px`, `backdrop-filter: blur(4px)`. Саме розмиття давало видиму
  пляму на білій картці — рамки і заливки формально вже не було.
  🔑 **Урок:** «прибрав рамку і фон» ≠ «прибрав кружечок».

### Зроблено

- `style/board.css` — локація на `var(--news-accent, #722F37)`, той самий токен, що й
  ціна (не свій відтінок: інакше в одному рядку два «майже однакові» багряні).
  Пін не чіпав — у `PIN_ICON_SVG` стоїть `stroke="currentColor"`, фарбується сам.
- `style/board.css` — до scoped-правила закладки додано `border-radius: 0`,
  `box-shadow: none`, `backdrop-filter: none`, `-webkit-backdrop-filter: none`.
  🛑 Тап-ціль 40×32 НЕ чіпав — вона куплена окремим заміром 03.08.
- 🆕 `tests/board-card-accent.mjs` — сторож, **11/11**. Міряє ОБЧИСЛЕНИЙ стиль у
  браузері, не текст файлу: grep показав би `border: 0` і збрехав «овалу немає».
  **Контроль наклеює старий CSS і червоніє** — радіус `50%`, тінь є, розмиття `blur(4px)`,
  локація `rgb(122,122,122)` ≠ ціна.

### Перевірено

`node build.js` — clean (58 файлів, `check-imports` без проблем; `bundle.js` не змінився,
зміна суто в CSS). Стенди: `board-card-accent` 11/11 · `board-card-foot` 17/17 ·
`board-card-row` 23/23 · `board-cream` 38/38 · `board-owner` 14/14 · `board-rules` 29/29 ·
`modal-surface` 10/10.

`CACHE_NAME` піднято → `cstl-20260827-1954`.

### Не перевірено

🔴 **Пальцем на iPhone не дивились** — це справа Вови. Стенд каже, що обгортки в
обчисленому стилі немає; як воно виглядає живим оком на картці з фото — його слово.

---

## 20:05 — /finish

- **PR [#1057](https://github.com/Volodymyr221/CSTL_NEWS/pull/1057)** створено ПЕРЕД оновленням
  документації — навмисно, щоб номер у прозі був правдивий, а не вгаданий (правило №7).
- `git fetch origin main && git merge origin/main` перед PR — **Already up to date**
  (злиття вже робилось на початку роботи, правило №13).
- `node scripts/check-syntax.mjs tests/board-card-accent.mjs` — OK. Файлів у `src/` не
  чіпав, тож більше перевіряти нічого.

### 🛑 ІНЦИДЕНТ ДОКУМЕНТАЦІЇ: пульс прийшов уже за стелею

`node tests/docs-fresh.mjs` впав: **`NOW.md` = 16.1 KB при стелі 16**. Це не моя правка —
файл прийшов таким із паралельної сесії. Стелю **не піднімав** (пряма заборона: вікно
контексту від цього не росте).

🔴 **Знайдено при стисненні:** блок про дубль фото (**PR #1055**) жив **лише** в `NOW.md`
і в журналі `vova-2026-08-27b.md` — у літописі `SESSION_STATE_VOVA.md` його **не було**
(`grep` по `1055`, `13153`, `RICH_ALGO_VERSION`, «подвоєн» — 0 збігів). Тобто стиснути
його означало б **втратити запис**, включно з інцидентом «squash подвоїв блок коду».
✅ Спершу переніс у літопис повністю (з числами, розміном і правилом про `grep -n "^def "`),
і аж тоді стиснув у пульсі. Аналогічно перевірив капсулу і статті кабінету — ці в літописі
вже були, їх стискав одразу.

📐 `NOW.md` 16.1 KB → **15.7 KB**.

### Оновлено

- `NOW.md` — ЗАРАЗ (PR #1057, `cstl-20260827-1954`), новий блок «ЗРОБЛЕНО», рядок у
  «НЕ ДОРОБЛЕНО» (пальцем не дивились), питання **0в-1** Вові.
- `SESSION_STATE_VOVA.md` — розділ сесії «c» + перенесений розділ про #1055.
- `CLAUDE.md` і `START_HERE.md` — стендів **113 → 114** (це ловить `docs-refs.mjs`,
  і саме він почервонів: карта файлів звіряється з диском).
- `BYYOU_PLAN.md` не чіпав — статус `idle` весь час, потоку не було.

🧪 `docs-fresh` **20/20** · `docs-refs` **10/10**.

---

## 21:00 — «Роби»: РОЗДІЛЕННЯ КОНТЕНТУ НА ТРИ НАПРЯМКИ

**Замовлення Вови:** історичне → нова спільнота «Історія громади»; новини →
сьогодення; Olyka Castle → брендове сучасне. Головна умова дослівно: «ці статті
мають бути максимально правдиві, тобто нам не потрібно вигадувати ніякої
інформації… переказувати своїми словами, але щоб вся інформація була правдива».

**Формат вирішено Вовою:** «Це не має бути стаття, це має бути пост як в
інстаграмі чи в фейсбуці» — тобто варіант **Б**, не А. Мою пропозицію А
(пост-анонс + повна стаття) відхилено. Фото — варіант 1 (Wikimedia + чернетка),
на пряме «Роби» без окремої відповіді по фото.

### 🔴 Дві мої помилки в аналізі, обидві виправлені

1. **«Агент Olyka Castle історичних постів не пише»** — НЕПРАВДА. Я прочитав лише
   `editor/plans/olyka_castle.json` і не додивився `plan.py` до кінця. Джерел тем
   **три**: план + `data/events.json` + **`data/olyka-stories.json`**. Історичні
   пости йдуть із третього. Вова показав скрін поста про костел — це
   `story:костел-пресвятої-трійці-перлина`.
2. **«Supabase треба авторизувати»** — НЕПРАВДА. Працює. Я спирався на попередження
   на старті сесії замість того, щоб просто спробувати.

### 📐 Заміряно по живих даних (замість здогадів)

| | число |
|---|---|
| постів агента в OLYKA CASTLE | 9 → з них **історичних 5**, події 2, функції 2 |
| наших статей у новинах | 18 → з них **історичних 11** |
| збіг «історія і статтею, і постом» | **5 із 5** |
| історичних тем у `create_themes` новинного агента | **~22 із 42** |

`page_id` спільноти — **6** («Історія Громади», створена Вовою 20:35).

### Зроблено (крок 1 — розділення джерел)

- `editor/sources/plan.py` — ключ **`pools`** у місії: які пули тем брати
  (`plan` / `events` / `stories`). Без ключа — усі три, стара поведінка ціла.
  🔑 Розподіл на ВХОДІ, а не класифікатор на виході: пул теми відомий ДО звернення
  до моделі, тож платити за текст, щоб потім вирішувати куди його діти, не треба.
- `editor/sources/plan.py` — `mark_done` тепер памʼятає **місію**, а `_види`
  фільтрує за нею. 🔴 Без цього була б тиха вада: файл памʼяті спільний (навмисно —
  щоб тема не пішла двічі), і пост в «Історії Громади» **відкривав би квоту на пост
  про функцію в OLYKA CASTLE**, тобто правило Вови про частку обходилось би чужою
  спільнотою. Старим записам приписано `olyka_castle` — це не здогад, до 27.08
  іншої місії з постами в стрічку не існувало.
- `editor/writers/brand_writer.py` — префікс кошика витрат береться з місії.
  Було зашите `olyka:` → витрати нової спільноти лягали б у бюджет старої, а стеля
  читається саме за префіксом: одна спільнота глушила б другу.
- `editor/sinks/page_draft.py` — передає місію в памʼять.
- 🆕 `editor/missions/istoriya_hromady.json` · `editor/plans/istoriya_hromady.json`
  (page_id 6) · `editor/voice/istoriya_hromady.md`.
- `editor/missions/olyka_castle.json` — `pools: ["plan","events"]`, історії прибрано.
- 🆕 Сторож `scripts/test_editor_pools.py` — **15/15**, із контролем: без фільтра за
  місією чужі пости справді протікали б у бренд (3 замість 0).
  🛑 Один рядок сторожа я написав із `or True` — перевірка, яка не вміє впасти.
  Помітив і прибрав одразу.

### Міграція (SQL по живій базі)

5 постів `id 22, 23, 24, 26, 33` переведено `page_id 3 → 6`. Звірено до і після:
**OLYKA CASTLE 11 опублікованих, «Історія Громади» 5**.
🛑 Пости `6` і `9` НЕ чіпав — вони про «приїхати подивитись Олику», тобто саме те,
чим Olyka Castle має бути за словами Вови.

### 🔴 ГОЛОВНЕ, ЩО ВІДКРИЛОСЬ ПРИ ПЕРЕВІРЦІ

Сухий прогін обох місій дав **0 тем в обох**:
- «Історія Громади» — усі 5 історій уже написані (`done` 5 із 5), нових джерел немає;
- OLYKA CASTLE — у плані лишились самі теми про функції, а їх блокує частка.

Тобто розділення зроблено, але **потоку нових тем в історичну спільноту не існує**.
Це наступний блок, і без нього фіча — лише перейменування.

## 21:40 — крок 2: потік тем в історичну спільноту

🔴 **Корінь, який лікуємо:** розділення без потоку тем — це перейменування. Джерело
`data/olyka-stories.json` вичерпане (5 із 5 написані), нових історій нізвідки взяти.

**Рішення — розділити ЗБИРАЧА ФАКТІВ і ПИСАРЯ.** Новинний агент уміє веб-пошук,
вимагає джерела і перевіряє їх на живість (404 = ознака вигадки, стаття відкидається).
Ця машинерія лишається, міняється лише ПРИЗНАЧЕННЯ: історичний матеріал більше не
стає чернеткою новини, а лягає **темою з фактами** в `editor/plans/istoriya_hromady.json`.
Пост із цих фактів пише конвеєр `editor/` голосом спільноти.
🔑 **Чому саме так:** писар (`brand_writer`) бачить ЛИШЕ рядки `facts`. Піти за межі
переданого йому фізично немає звідки. Це технічна форма вимоги Вови «нічого не
вигадувати», а не ще одне речення в промті.

### Зроблено

- `scripts/hromada_config.json` — 42 теми розділено: **19 сучасних** лишились у
  «Громаді», **26 історичних** пішли в нову місію «Історія». Прибрано з новин також
  теми «історичні памʼятки» і «церкви» та історичні пошукові запити.
- `scripts/ai_news_agent.py`:
  - гілка промпта для місії «Історія» — роль **краєзнавця**, не редактора новин;
    вимога нічого не вигадувати, зберігати невпевненість джерела, і пряме
    «перша письмова згадка ≠ дата заснування»;
  - `seed_history_plan()` — кладе тему з фактами і джерелами в план спільноти.
    Дедуп подвійний: за id у плані і за памʼяттю вже написаних тем;
  - `history_plan_free_slots()` — стеля 6 ненаписаних тем. **Витрати самообмежені:**
    план повний → місія не робить ЖОДНОГО виклику API;
  - 🔴 повний кабінет новин більше не глушить історію (був `return` на весь прогін);
  - `existing_geos` переведено на `.get` — з прямим доступом за ключем нова місія
    впала б із `KeyError` ще до першого виклику.
- 🆕 `.github/workflows/istoriya-agent.yml` — вт/чт/сб 07:40 UTC. Дні НАВМИСНО не
  збігаються з Olyka Castle (пн/ср/пт/нд): два наші пости поспіль у стрічці — погано.

### 🧪 Сторож `scripts/test_history_routing.py` — 23/23

**Він одразу знайшов дві вади, і одна з них була в ньому самому:**
1. 🔴 **Справжній витік:** у новинній місії лишились історичні пошукові запити
   («замок Радзивіллів», «костел Пресвятої Трійці», «Олика Радзивілли історія»).
   Теми я прибрав, а запити — ні. Виправлено.
2. 🛑 **Помилка сторожа:** перевірка шукала «за переказами» в промті, а слово було
   розірване перенесенням рядка («за\nпереказами») — червоніло на СПРАВНОМУ коді.
   Це вже відомий клас пастки в проєкті. Тепер міряється текст без переносів.
3. ⚠️ Заразом виявилось, що міряти промпт ЦІЛКОМ не можна: у нього входить памʼять
   про вже написані статті («Покащів: село, яке колись очолювало цілу волость»), і
   вона давала хибне спрацювання. Памʼять — це список «не повторюй», тобто
   протилежність вказівці. Тепер міряються лише ВКАЗІВКИ.

**Прогін усіх python-стендів:** editor-pools 15/15 · history-routing 23/23 ·
markup-polish 42/42 · rich-html 73/73 · fulltext 21/21 · dedup і retention зелені.

### 🚧 Лишилось Вові

🔴 **Плану Olyka Castle бракує ЗВИЧАЙНИХ тем.** Там самі теми про функції, а їх
блокує частка — тобто місія чесно скаже «план вичерпано» і нічого не напише. Це не
збій, це наслідок розділення: історія пішла, а брендового сучасного контенту в план
ніхто не поклав. Потрібні теми в стилі «що цікавого в Олиці сьогодні».

## 06:20 (28.08) — /finish

- **PR [#1064](https://github.com/Volodymyr221/CSTL_NEWS/pull/1064)** створено ПЕРЕД
  оновленням документації — щоб номер у прозі був правдивий (правило №7).
- `git merge origin/main` перед PR — злилось **чисто**, без конфліктів (друга сесія
  додала `publish-now`, `README_publish_now.md`, журнал 28.08).
- 🔴 **Стенди ганяв ПІСЛЯ злиття, не до** (правило №13): editor-pools 15/15 ·
  history-routing 23/23 · markup-polish 42/42 · rich-html 73/73 · fulltext 21/21.
- **`CACHE_NAME` НЕ піднімав** і це свідомо: змінились лише `scripts/`, `editor/`,
  `.github/workflows/` і документи. Клієнтського коду (`src/`, `style.css`,
  `index.html`, `sw.js`) правки не торкнулись, а піднята мітка змусила б усі телефони
  перекачати незмінені файли.
- Синтаксис: шість змінених `.py` через `ast.parse`, пʼять `.json` через `json.load`,
  обидва `.yml` через `yaml.safe_load` — усе чисте. Змінених `.js` немає.

### 🛑 Пульс ЗНОВУ прийшов за стелею

`NOW.md` = **16.1 KB** при стелі 16 — наростила друга сесія. Стелю не піднімав.
📐 Спершу стиснув навмання і зробив **гірше**: 16.5 → 18.8 KB, бо мій блок додав більше,
ніж прибрали скорочення. Тоді заміряв розділи поштучно (`ЗРОБЛЕНО` 4133 Б, `ЗАРАЗ` 2536,
`НЕ ДОРОБЛЕНО` 2912) і різав уже за числом, а не на око → **15.8 KB**.
🔑 **Урок:** «стиснути документ» без заміру розділів — це та сама робота на око, проти
якої в проєкті є правило. Міряй, потім ріж.
✅ Перед стисненням звірив, що всі блоки (28.08 ніч, 27.08 ніч, Волинь Post) **вже є в
літописі** — інакше повторилась би вчорашня ситуація з #1055, який жив лише в пульсі.

### Оновлено

- `NOW.md` — ЗАРАЗ (PR #1064), новий блок ЗРОБЛЕНО, два рядки в НЕ ДОРОБЛЕНО
  (план Olyka Castle без звичайних тем · `official=false` і тестові пости).
- `SESSION_STATE_VOVA.md` — розділ сесії «c» від 28.08, з обома моїми помилками аналізу.
- `CLAUDE.md` — у карті файлів **не було рядка про `editor/` взагалі**; додано опис
  конвеєра, ключа `pools`, спільної памʼяті, нового workflow і двох сторожів.
- 🧪 `docs-refs` 10/10 · `docs-fresh` 20/20.

## 07:10 (28.08) — ТЗ по Olyka Castle для розмови з GPT

Вова: «Налаштуємо для Olyka Castle ще, як краще це зробити? Можливо напиши завдання
я ще з чатом гпт поговорю».

🔴 **Виправив власну вчорашню неточність.** Я казав, що план Olyka Castle «блокує
частка постів про функції». Заміряв — неправда і гірше: **ненаписаних тем там НУЛЬ**,
обидві (`weather-compact`, `swipe-back`) уже опубліковані. Пул подій теж сухий: єдина
майбутня подія (День знань 01.09) написана. Тобто спільнота стоїть не через правило,
а тому що черга порожня.

📐 Заміряно заразом: у сторінці 3 **11 опублікованих постів, із них 4 — сміття**
(три «тест» і випадково відправлене робоче повідомлення). Справжніх сім.

Створено `CSTL NEWS VOVA/_ai-tools/BRIEF_OLYKA_CASTLE.md` — самодостатнє ТЗ для
стороннього асистента: контекст проєкту · стан спільноти числами · вісім технічних
рамок, які пропозиція мусить поважати (пост без заголовка, чернетка на вичитку, агент
пише лише з переданих фактів, ≈4 теми на тиждень, бюджет, частка постів про функції,
правило адреси «Олика ≠ громада», тон) · чотири варіанти джерела тем із чесними
мінусами · моя позиція, щоб було з чим сперечатись · чого не пропонувати.

🔑 **Моя позиція в ТЗ:** для цієї спільноти НЕ робити те, що зробили для історії
(агент шукає теми в мережі). Про минуле джерела є; про сьогодення містечка на 3 тис.
людей їх немає, і агент писатиме гладенькі загальники. Пропоную джерелом фактів **сам
застосунок** (події, Дошка, збори — правда з нашої ж бази) плюс фото й репліки Вови.
⚠️ І названо сумнів у власній пропозиції: можливо, спільноті чесніше писати РІДШЕ, але
лише коли є що сказати. Порожня пауза краща за пост «ні про що».

## 08:00 (28.08) — «Роби»: крок 1 по Olyka Castle

**Контекст:** Вова показав відповідь GPT. Головне з неї прийнято, і одну річ я
виправив у себе: **вимоги «17 постів на місяць» у системі ніколи не було** — розклад це
дозволені слоти, а не квота, і порожній слот уже був нормальним станом. Це Я у своєму
ТЗ перетворив розклад на квоту і тим підштовхнув розмову не туди. GPT це вловив.

**Що з відповіді GPT змінило план:** веб як РАДАР для людини (а не джерело для агента) ·
фотоархів як ЦЕНТРАЛЬНЕ джерело, не додаткове · стеля 20-30% на контент із застосунку.
**Що відхилив:** кодувати пʼять резервуарів як квоти — при нулі тем це правило, що
обмежує ніщо. Тримаємо головою, закодуємо за фактом перекосу.

### 🔴 ЗНАЙДЕНО ВАДУ У ВЧОРАШНІЙ ВЛАСНІЙ РОБОТІ

**Стік `page_draft` не записував фото ВЗАГАЛІ.** Учора я поставив історичній місії
`image: wikimedia`, конвеєр чесно шукав картинку і клав у чернетку — а стік писав у базу
лише `text`. Тобто налаштування працювало «на папері».
🔑 **Помітив не з коду, а з наслідку:** усі 5 постів агента в базі стоять без жодного
знімка. Виправлено: `image_url` + `image_urls` (клієнт малює галерею саме з другого).

### Зроблено

- `editor/sinks/page_draft.py` — фото доїжджає в базу (вада вище).
- `editor/writers/brand_writer.py` — писар несе знімок теми далі. Без цього рядка
  власне фото Вови мовчки замінювалось би випадковим кадром із Wikimedia: конвеєр
  шукає картинку тоді, коли писар СВОЄЇ не дав.
- `editor/sources/plan.py` — новий пул **`photos`** із `data/olyka-photos.json`.
  Знімок без жодного спостереження темою не стає (і про це сказано в лог).
- 🆕 `data/olyka-photos.json` — заготовка з довідкою і прикладом запису.
- `editor/missions/olyka_castle.json` — `pools: plan + events + photos`.
- `editor/voice/olyka_castle.md` — **переписано визначення спільноти**: не «спільнота
  про Олику», а авторська медіа-спільнота про СУЧАСНУ громаду. Додано таблицю «три
  спільноти — три питання», пʼять тем про що писати, і окремим блоком —
  🛑 **порожній слот це нормальний стан, а не збій**.
- `editor/plans/olyka_castle.json` — довідка про пʼять резервуарів **усередині плану**.
  Окремого документа-беклогу свідомо НЕ заводив: у цьому проєкті два файли про одне вже
  розходились (історія журналів 06.08). Джерело правди одне.

### 🧪 Сторож `test_editor_pools` — 15 → 22

🔴 **Дві нові перевірки я спершу написав через `inspect.getsource` — тобто ГРЕП у масці
стенда.** Такий стенд зеленіє від коментаря і не помітить, що виклик прибрали.
Переробив на справжній вимір: підмінив `urllib.request.urlopen` і подивився, **що
доїхало до моделі й до бази**. Перевіряється `image_url`, `image_urls`, номер сторінки і
статус чернетки; є контроль — без фото полів фото в запиті немає взагалі.
🔬 **Мутант:** відкотив фікс стоку → **21/22**, падає саме та перевірка. Стенд уміє впасти.
✅ Памʼять агента стендом не зіпсована (`done` = 9, тестових id немає).

**Усі python-стенди:** editor-pools 22/22 · history-routing 23/23 · markup-polish 42/42 ·
rich-html 73/73 · fulltext 21/21.

### 🚧 Далі (крок 2 і 3, не робив)

Пул `app` (приводи з нашої бази, стеля 30%) · радар публічних джерел із підтвердженням
теми людиною. 🔴 **Крок 0 лишається за Вовою:** беклог тем. Без нього спільнота мовчить —
і це тепер задокументовано як нормальна поведінка, а не збій.

## 08:40 (28.08) — «Роби»: баланс «Олика / села» 60/40 (тільки Історія Громади)

**Слово Вови:** «всі пости мають стосуватися не тільки Олики, а всієї Олицької
громади… 60% Олика, 40% села громади, типу. Але все одно села громади мають бути
присутні». Уточнення після мого питання: **«зараз ми налаштовуємо історії громади»**,
Olyka Castle — потім, за тими самими критеріями.

### 📐 Заміряно перед правкою — і воно гірше, ніж здавалось

Із **СЕМИ** опублікованих постів про місце (обидві спільноти) — **сім про Олику, про
села НУЛЬ**. Перекос не 90/10, а **100/0**. У новинного агента правило балансу стоїть
від початку (50/50 у промті), а спільноти поїхали без нього взагалі — і це моя
недоробка вчорашня: історичну місію я завів без жодного рядка про баланс.

### Зроблено — джерело і гарантія

- `editor/core/geo.py` — **`місце_теми()`**: село / центр / громада.
  🛑 **Пастка, через яку це не один рядок:** «Олицька громада» містить «Олиц», тож
  наївна перевірка зарахувала б пост ПРО ВСЮ ГРОМАДУ до центру — і правило душило б
  саме ті пости, заради яких заводилось. Назву громади прибираємо з тексту ПЕРЕД
  пошуком центру. Друга пастка: «Ставок при Олицькому замку» — це село.
  🔑 Порядок перевірок не довільний: село першим. Незрозуміле → ГРОМАДА, не центр:
  помилка «порахував як загальний» коштує неточності, «порахував як Олику» —
  занижує частку сіл, тобто працює проти правила.
- `editor/sources/plan.py` — `частка_центру()` і **`зрівноважити()`**.
  🛑 **Правило ТІЛЬКИ переставляє чергу, ніколи не блокує.** Немає сільських тем —
  тема про Олику проходить. Інакше спільнота мовчала б заради пропорції, а це
  суперечить рішенню Вови «краще менше, але живе». Форма інша, ніж у правила про
  функції (там заслон), бо там альтернатива є завжди, а тут може не бути.
  🛑 Пости про ВСЮ громаду в знаменник не входять — кинути їх в одну з чашок означало
  б підробити число.
- `mark_done` памʼятає місце; писар несе його з теми, стік передає в памʼять.
  Місце визначає ДЖЕРЕЛО один раз — рахувати вдруге по написаному тексту означало б
  дві відповіді на те саме питання.
- `scripts/ai_news_agent.py` — у промт історичної місії доданий баланс 60/40 із
  прямою вказівкою шукати за назвами сіл і числом «сім із семи». ⚠️ І застереження:
  не знайшлось джерел про село — краще віддати менше, ніж підмінити село черговим
  текстом про замок.
- `editor/missions/istoriya_hromady.json` — `place_balance: 0.6`.
  Olyka Castle стелі НЕ отримала — за прямим уточненням Вови.

### 🗄 Заповнено памʼять за фактом (не здогадом)

Пʼятьом уже написаним постам проставлено `place` (усі — центр, обчислено з їхніх
назв) і `mission: istoriya_hromady` — вони **фізично переїхали** в сторінку 6 учора,
тож належать історичній місії. Без цього пропорція рахувалась би від нуля і правило
не спрацювало б аж поки не набереться статистика.
📐 **Спільнота стартує зі 100% Олики при стелі 60%** — тобто перша ж сільська тема
вийде наперед.

### 🧪 Сторож 22 → 32, обидва мутанти впіймані

Перевіряються три різні речі: розпізнавання місця (з обома пастками) · пропорція
(і що громада в неї не входить) · переставляння черги (і що воно НЕ блокує).
🔬 **Мутант 1** — правило не переставляє → 31/32. **Мутант 2** — громада потрапляє в
знаменник → 31/32. Обидва падають саме на відповідній перевірці.
🔬 Окремий контроль на ЖИВІЙ памʼяті: «Історія Громади» справді стоїть на 100%.

**Усі стенди:** editor-pools 32/32 · history-routing 23/23 · markup-polish 42/42 ·
rich-html 73/73 · fulltext 21/21 · dedup зелений.

### 🚧 Борг

Olyka Castle налаштувати за тими самими критеріями — **окремим кроком, коли скаже Вова**.

## 09:40 (28.08) — /finish (другий)

- **PR [#1065](https://github.com/Volodymyr221/CSTL_NEWS/pull/1065)** створено ПЕРЕД
  документацією — щоб номер був правдивий.
- `git merge origin/main` — **Already up to date**.
- **`CACHE_NAME` НЕ піднімав:** `git diff origin/main..HEAD --name-only` не дав жодного
  файла з `src/`, `style/`, `index.html`, `sw.js`. Перевірено командою, не памʼяттю.
- Синтаксис: 6 `.py` через `ast.parse`, 6 `.json` через `json.load` — чисто.
- 🔴 **Стенди ганяв ПІСЛЯ злиття** (правило №13): 32/32 · 23/23 · 42/42 · 73/73 · 21/21 ·
  dedup і retention зелені.

### ⚠️ ІНЦИДЕНТ: СТЕНД ПИСАВ У БОЙОВИЙ ЖУРНАЛ ВИТРАТ

Знайдено на `git diff` перед PR: у списку змінених файлів стояв `data/ai_spend.json`,
якого я не мав чіпати. `test_editor_pools` кликав СПРАВЖНЬОГО писаря, і той записав
**девʼять фальшивих прогонів** у журнал, який показує адмінка. Сума нульова, але це
брехня в журналі грошей.
✅ Стенд відводить журнал у тимчасовий файл (`_spend.SPEND_PATH` підміняється і
повертається у `finally`); сміття прибрано через `git checkout origin/main --`.
🔬 Звірено: `git diff origin/main -- data/ai_spend.json` **порожній**.
➡️ **Правило собі:** стенд, який зачіпає бойові дані, — не стенд, а тихий писар у прод.
🔑 Помітив лише тому, що дивився список файлів перед PR, а не самі лише тести.

### 🛑 Пульс: стискав ДВІЧІ, і перший раз зробив гірше

16.1 KB → додав блок → **17.8 KB**. Перше стиснення «на око» знову дало приріст. Тоді
заміряв розділи поштучно (`ЗРОБЛЕНО` 5617 Б, `ПИТАННЯ` 2819, `НЕ ДОРОБЛЕНО` 2655) і
ріжу вже за числом: звів два блоки 28.08 в один (обидва в літописі), прибрав із
«НЕ ДОРОБЛЕНО» три рядки, що дублювали «ПИТАННЯ» → **15.07 KB**.
🔑 Це вже другий раз за сесію, коли стиснення без заміру дає приріст. Записано як правило.

### Оновлено

`NOW.md` (PR #1065, новий блок, питання **0д** про непрацюючого агента і банер) ·
`SESSION_STATE_VOVA.md` (розділ «ранок-2» з обома вадами й інцидентом) ·
`CLAUDE.md` (карта файлів: `data/olyka-photos.json` і **розмір банера з числами з коду**).
🧪 `docs-fresh` 20/20 · `docs-refs` 10/10.

## 12:55 (28.08) — «Роби» (варіант А): віджет Стрічки на Громаді

**Скарга Вови зі скріна:** «в блоці віджеті блоку стрічки у громаді… букви більші,
по-друге, немає тих відступів, абзаців, нічого немає. Треба якось це стандартизувати.»

### 📐 Причин було ДВІ, і обидві заміряні

| | Стрічка | віджет без фото | віджет з фото |
|---|---|---|---|
| кегль | 15px | **17px** | 15px |
| абзаци | `white-space: pre-wrap` | **знищені** | знищені |

1. 🔴 **Абзаци вбивав ОДИН символ:** у `preview()` стояло `.replace(/\s+/g, ' ')`, а `\s`
   означає БУДЬ-ЯКИЙ пробільний символ — разом із переносом рядка. Порожній рядок між
   абзацами ставав звичайним пробілом ще до показу.
2. Навіть уцілілий перенос ковтався б: у віджета не було `white-space: pre-wrap`.
3. Кегль 17px — **не помилка, а рішення 25.08** проти порожнечі картки без фото.

**Рішення Вови — варіант «А»:** у віджеті абзац = ОДИН перенос, без порожнього рядка.
Причина вимірна: у картці з фото видно лише 4 рядки, порожній рядок зʼїв би чверть прев'ю.

### Зроблено

- `src/tabs/home-feed.js` `preview()` — стискає лише пробіли й табуляції; `\n{2,}` → `\n`.
  ⚠️ Заразом виправлено пошук межі слова: `lastIndexOf(' ')` після появи переносів міг
  відкинути цілий абзац. Тепер `cut.search(/\s\S*$/)`.
  🛑 Першу версію я написав через `match` + `lastIndexOf(символ)` — шукало символ, а не
  позицію збігу. Помітив одразу і переписав.
- `style/home.css` — `white-space: pre-wrap` + `overflow-wrap: anywhere` (у Стрічці
  довге посилання вже вирішене, тут лишалось неузгодженим); кегль **17 → 15**.
  🔑 Дві правки тримаються одна одної: абзаци повертають тексту висоту, тобто причина,
  заради якої піднімали кегль, відпала.
- `CACHE_NAME` → `cstl-20260828-1259` (клієнтський код змінився).

### 🧪 Сторож 40 → 42, і одна перевірка ПЕРЕПИСАНА

🔴 **Старий сторож стеріг саме ті 17px:** рядок «картка БЕЗ знімка дає тексту БІЛЬШИЙ
КЕГЛЬ» упав на моїй правці — і правильно зробив. Не викинув його, а змінив критерій:
причина (картка не має тримати півкартки порожнечі) лишилась, але відповідь тепер інша —
більше РЯДКІВ, а кегль однаковий. Плюс окремий рядок «кегль однаковий в обох форм».
🔑 **Кегль звіряється з САМОЮ Стрічкою** (`.fd-text`), а не з числом 15 у стенді: переїде
типографіка — стенд вимагатиме переїзду обох, а не стерегтиме застигле число.
🔑 **Абзац міряється НАСЛІДКОМ:** рахуються рядкові коробки через `Range.getClientRects()`.
Перший абзац у сцені навмисно короткий — якщо переноси зникли, обидва злипаються в ОДИН
рядок. Перевірка на `white-space === 'pre-wrap'` була б перевіркою властивості й лишилась
би зеленою, якби `preview()` знову почала стирати переноси.
⚠️ Фікстуру довелось перенести на спільноту 1 і зробити СВІЖІШОЮ: віджет показує лише
останній допис кожної спільноти, тож старіший не малювався і перевірка зеленіла б на
порожнечі.
🔬 **Два мутанти, обидва впіймані:** `preview` знову їсть переноси → 41/42 · знято
`pre-wrap` → 41/42. Тобто сторож ловить кожну з двох причин ОКРЕМО.

### ⚠️ `home-caps` не вкладався в час — і це НЕ моя правка

Стенд обривався таймаутом. **Контроль:** відкотив свої файли на попередній коміт —
обірвався так само (код 124). Тобто причина в тривалості стенда, а не у зміні.
При обриві було 112 перевірок і **жодної червоної**. З повним часом — **150/150**.

**Стенди:** home-feed-widget **42/42** · home-caps **150/150** · board-card-accent 11/11.

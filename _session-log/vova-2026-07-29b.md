# Журнал сесії — 2026-07-29 (сесія N)

**Гілка:** `claude/startuem-yky235` · production: `main`
**Старт:** `/startuem`

---

## Крок 0 — старт (виконано автоматично)

- `git config user.name "Вова"` + `user.email vitocorleone191@outlook.com` — коміти
  підписуються Вовою. «Unverified» на GitHub — очікувано, ігноруємо (рішення 10.07).
- `git fetch origin` + `git pull origin main` — підтягнуто автокоміти парсерів
  (новини `f3aa81d2`, розклад автобусів). Гілка сесії = `origin/main` (0 комітів різниці).
- Заведено цей журнал.

## Читання (обовʼязкове)

`HOT_RULES.md` · `CSTL NEWS VOVA/CLAUDE.md` · `SESSION_STATE_VOVA.md` ·
`AUDIT_2026-07.md` · `CSTL_BUGS.md` · `BYYOU_PLAN.md` · `BACKLOG.md`

## Знятий стан (факти, не з пам'яті)

| Що | Значення |
|---|---|
| `CACHE_NAME` у `sw.js` | `cstl-20260729-1839` |
| Стендів у `tests/*.mjs` | 24 файли (BYYOU_PLAN: 22 стенди / 272 перевірки зелені) |
| Останній змістовий коміт main | `605a7caf` fix(ci) · перед ним `f8621d0f` feat(chat) PR #715 |
| Відкритих 🔴 багів | нема (`CSTL_BUGS.md`) |

## ⚠️ Розбіжності в документах (доповів Вові у першій відповіді)

1. **`BYYOU_PLAN.md` статус `active`, «кроки 1-14 локально, БЕЗ push», гілка
   `claude/startuem-yxqfd9`** — але PR **#715** «розмова = людина, оголошення —
   контексти всередині» **уже змерджений у `main`** (коміт `f8621d0f`) і задеплоєний.
   Тобто потік фактично на проді; відкритим лишається тільки крок 15 — смоук на iPhone.
2. **`SESSION_STATE_VOVA.md` (сесія M) відстає:** пише `CACHE_NAME = cstl-20260729-1749`
   і «21 стенд / 254 перевірки», у файлах — `cstl-20260729-1839` і 22/272 (після #715).
3. У `SESSION_STATE_VOVA.md` двічі підряд той самий блок «Урок: липкий елемент НЕ
   успадковує фон свого аркуша» (рядки 49-54 і 56-61) — дубль після мерджу.

## Далі

Чекаю «Роби» від Вови.

---

## Аудит безпеки за чек-листом Вови (10 промптів зі сторонньої добірки)

**Запит Вови:** «проаналізуй їх чи вони корисні, якщо так то використай їх для аудиту».
Код НЕ чіпав — «Роби» не було. Тільки читання + заміри.

**Вирок:** список писаний під «свій сервер + своя БД + Stripe». У нас статика на GitHub
Pages + Supabase (BaaS) + Google OAuth. **3 закриті · 4 корисні · 3 не про нас.**

### ❗ Головне: п.2 виконувати НЕ МОЖНА
«Перепиши логіку логіну на bcrypt» → ШІ завів би ВЛАСНУ таблицю паролів там, де її
фізично нема. Паролі тримає Supabase Auth, наша БД їх не бачить. Пароль існує в одному
місці — вхід в адмінку (`admin.html:620`).

### Порядково (з доказами)
| # | Стан | Доказ |
|---|---|---|
| 1 HTTPS | ✅ | 0 посилань `http://` у `src/`+`index.html`+`sw.js`. Живі заголовки НЕ перевірив — проксі сесії дав 403 `host_not_allowed` на github.io |
| 2 паролі | ✅ не чіпати | `signInWithPassword` лише `admin.html:620` |
| 3 боти | 🟡 дірка | нижче |
| 4 сесії | 🟡 настройка | `supabase.js:26` persistSession/autoRefreshToken |
| 5 CSRF | ❌ н/з | JWT у заголовку, не cookie |
| 6 скидання | 🟡 настройка | `admin.html:676` signInWithOtp → `:1862` updateUser |
| 7 ключ БД | ✅ | `sb_publishable_…` (`supabase.js:15`), `service_role` у репо нема, є `gitleaks.yml` |
| 8 логи | ✅ | **0** `console.log`; 46 `console.warn` + 2 `console.info`, усі друкують `error.message` |
| 9 біллінг | 🟡 | свої стелі $1.20/прогін, $4/міс (`ai_news_agent.py:62-63`), алертів провайдера нема |
| 10 бекапи | 🔴 | нижче |

### 🔴 Знахідка 1 — бекапів живої БД НЕМА
У `.github/workflows/` жодного бекапу. `backup/` у репо = старі копії CSS/JS, не дані.
`data/*.json` у git (новини, розклад) — версіонуються. А пости Стрічки, коментарі, чати,
профілі, оголошення — ТІЛЬКИ в Supabase, на безкоштовному тарифі автобекапів нема.

### 🟡 Знахідка 2 — анонімні входи без ліміту
Rate limit 8/15с є лише на коментарях і чаті (`supabase_antispam_shared.sql:163,201`).
Нема на: подачі оголошення (`Anyone can submit a pending post`) і — дорожче —
`Anyone can upload to community-photos` для `public` (`supabase_schema.sql:262-264`):
анонім заливає файли у сховище без обмежень. CAPTCHA: 0 згадок у коді.

### 🟡 Знахідка 3 — чотири тумблери в консолі Supabase (коду не треба)
`signOut` глобальний (`scope:'global'`) · OTP TTL 3600с → 15-30 хв · білий список
Redirect URLs (у нас `emailRedirectTo: window.location.href`, `admin.html:678`) ·
`auth_leaked_password_protection` вимкнено.

### Чого в чек-листі НЕМА, а нам потрібніше
1. Наскрізний тест RLS анонімом/другим акаунтом (вже 🔴 у `BACKLOG.md`).
2. XSS: **185** `innerHTML =` проти **358** `escapeHtml(` — співвідношення здорове,
   але це не доказ що прикриті всі 185. Для нашого стеку ризик більший за 7 із 10 пунктів.

### Запропоновано Вові (чекає «Роби»)
1) бекап `pg_dump` за розкладом + тестове відновлення · 2) ліміт на анонімне фото ·
3) rate limit на подачу оголошення (тим самим двигуном, копію НЕ робити) · 4) тумблери.

⚠️ **Обмеження аудиту:** живу базу не перевіряв — MCP-конектор Supabase відвалився
(потребує авторизації, у неінтерактивній сесії пройти не можу). Усе знято з коду й SQL.
Урок 28.07 «файл ≠ стан системи» діє: перед накатом звірити з продом.

---

## 🔌 Конектор Supabase піднято — знято ЖИВИЙ стан прода

Проєкт `uabyfecseqnemvcqhdem` (Olyka Castle), Postgres 17.6, ACTIVE_HEALTHY.
Тепер висновки з бази, а не з SQL-файлів (урок 28.07 закрито по-справжньому).

**Правило Вови (дослівно):** «Робити будь які дії, лайкати, надсилати коменти,
повідомлення, фото, може тільки залогінений користувач. Не залогінений може тільки
переглядати публічну інформацію».

### ✅ Правило вже виконане на ~95%
Із 49 політик запису (`pg_policies`) **44 вимагають `auth.uid()`** — для аноніма це NULL,
перевірка не проходить фізично. Анонім НЕ може: лайкати (`preact insert`:
`auth.uid() IS NOT NULL`), коментувати (`pcom insert`: `author_uid = auth.uid()`),
писати в чат (`msg participants write`), створювати треди, підписуватись на сторінки,
міняти профіль, постити у Стрічку.

### Винятків рівно 5 (жива вибірка)
| # | Де | Що дозволено анонімно | Дані на проді |
|---|---|---|---|
| 1 | `push_subscriptions` | insert/update/delete з `USING true` | 0 рядків |
| 2 | `storage.objects` | залити будь-що в бакет | 103 файли, 49 МБ |
| 3 | `posts` | подати оголошення `status='pending'` | **18 із 25 постів анонімні** |
| 4 | `analytics_events` | записати подію | 6105 рядків |
| 5 | `ad_events` | записати подію | 0 рядків |

### 🔴 №1 — не «анонім пише», а гірше
`push_delete USING true` + `push_update USING true` = будь-хто може видалити/змінити
ЧУЖИЙ рядок, тобто стерти всі підписки на автобусні сповіщення одним запитом.
Шкоди ще нема (0 рядків), але це готова кнопка «вимкнути сповіщення всім».
Це помилка в політиці, не задум.

### 🟡 №2 — відповідь на питання Вови «який ліміт?»
Знято з живого бакета: `file_size_limit = null`, `allowed_mime_types = null`.
Доказ що клієнтське стиснення (800px JPEG q0.78) не тримає на всіх шляхах:
найбільший файл **PNG 3.2 МБ**, далі 3.2 / 3.1 / 2.4 МБ. Зловживань нема —
усі 103 файли справжні картинки наших людей.

### ⚠️ КОНФЛІКТ: правило Вови ламає робочу фічу
№3 — подача оголошення анонімом це НЕ діра, а чинний головний шлях: **18 із 25**
оголошень подані без входу (`owner_uid is null`). Буквальне застосування правила
= житель Олики не подасть оголошення без акаунта. Рішення продуктове → питання Вові:
(а) лишити анонімною · (б) закрити під вхід · (в) лишити + ліміт частоти.
Те саме м'якше з №4: аналітика анонімів за визначенням пишеться до входу.

### Запропоновано (1,2,4 — жду підтвердження на 3)
1) `push_subscriptions` — прив'язати до власника рядка замість `true`
2) бакет — `file_size_limit = 2 МБ` + mime лише jpeg/png/webp (конфіг, не код)
3) подача оголошення — ЧЕКАЄ РІШЕННЯ ВОВИ
4) аналітика — лишити анонімною, максимум ліміт частоти

---

## ✅ НАКАТАНО 01.08.2026 — «анонім лише переглядає» доведено до бази

**Рішення Вови:** п.3 — варіант **«б», закрити під вхід** («Анонім не має права нічого
робити»). П.4 — аналітику лишити, анонімні метрики збирати далі.

### 🔴 Виправлення власної помилки (сказано Вові прямо)
Я попередив: «правило зламає робочу фічу — 18 із 25 оголошень анонімні». **Хибно.**
Розріз за часом: анонімні — 17.05…16.06 (18 шт.), від 20.06 усі 7 постів мають
`owner_uid`. Гейт входу в UI вже стоїть (`board.js:824` requireAuth). Анонімною
подачею не користувались 6 тижнів. Взяв суму за весь час і видав за поточну поведінку.
➡️ Урок: перед «фічею користуються» — розрізи ЗА ЧАСОМ, не підсумок.

### 🔑 Знахідка, що врятувала накат від косметики
`submit_board_post` — **SECURITY DEFINER**, тобто RLS ОБХОДИТЬ. Закрив би лише
політику на `posts` — анонімна подача працювала б далі, а я б відзвітував «зроблено».
Прогін по всіх 36 SECURITY DEFINER функціях, доступних `anon`
(`has_function_privilege('anon', oid, 'execute')`): чотири `admin_*` мають `is_admin()`,
решта звіряються з `auth.uid()`; `submit_board_post` — ЄДИНА, що писала `owner_uid=null`.
`can_edit_page` перевірено окремо (насторожив у верифікації): `is_admin() or
page_admins.uid = auth.uid()` → для аноніма обидва хибні, не дірка.

### Міграції (4, усі success)
1. `board_submit_requires_login` — сторож `v_uid is null` ПЕРШИМ у begin. Тіло знято
   з прода (`pg_get_functiondef`) і збережено дослівно — не переписував по пам'яті.
2. `posts_insert_requires_login` — вставка лише `authenticated` + `owner_uid=auth.uid()`.
3. `push_subscriptions_own_rows_only` — 🔴 було `USING(true)` на DELETE = будь-хто
   стирає ЧУЖІ підписки. Тепер `user_uuid = auth.uid()::text` + REVOKE в `anon`.
   0 рядків на момент правки → міграції даних не треба.
4. `storage_upload_requires_login_and_limits` — вхід + 5 МБ + jpeg/png/webp.

**Чому 5 МБ, а не 2:** заміряно — 103 файли, понад 2 МБ — 6 (макс PNG 3229 кБ),
понад 5 МБ — 0. 2 МБ відрізали б 6 справжніх завантажень наших людей.

### ✅ Перевірка — бойовими спробами `set local role anon`, не читанням політик
```
submit_board_post          → {"ok":false,"error":"Треба увійти в акаунт"}
insert into posts          → ERROR 42501 row-level security policy
delete push_subscriptions  → ERROR 42501 permission denied
insert storage.objects     → ERROR 42501 row-level security policy
```

### Файли репозиторію приведено у відповідність до прода
- НОВИЙ `scripts/supabase_anon_lockdown.sql` — чинний стан + уроки + перевірки.
- `scripts/supabase_push_fix_rls.sql` — 🛑 позначено «не накочувати» (відкриє дірку назад).
- `scripts/supabase_schema.sql` — політика posts, бакет і політика storage оновлені;
  над `ad_events` пояснено, чому анонім лишений свідомо.
- `SESSION_STATE_VOVA.md` — блок сесії N.

**`CACHE_NAME` НЕ чіпано свідомо** — змін у `src/`/`index.html`/`style.css`/`sw.js` нема.

---

## ✅ ЗАВЕДЕНО БЕКАП ЖИВОЇ БАЗИ (01.08) — «Роби» від Вови

**Файли:** `.github/workflows/db-backup.yml` · `scripts/README_backup.md`.

### Перше, що визначило конструкцію
Перевірив репозиторій через GitHub API: **`"private": false` — ПУБЛІЧНИЙ.**
Дамп містить 209 приватних повідомлень жителів. Отже:
- у коміт класти не можна;
- у звичайний артефакт теж не можна (артефакти публічного репо доступні всім).
➡️ `gpg --symmetric AES256` ДО вивантаження, назовні лише `.gpg`.

### Тариф
`get_organization` → **`plan: free`**. Підтверджує: автобекапів Supabase немає,
це умова тарифу. Тобто копії справді не існувало.

### Що всередині бекапа
Три частини через Supabase CLI (ролі / схема / дані) — так це документує сам
Supabase, бо `auth` і `storage` службові й «просто pg_dump усього» на відновленні
конфліктує. `auth.users` обов'язково — усе в базі тримається на `uid`.
Окремий крок — **файли сховища**: дамп зберігає лише ПОСИЛАННЯ, без цього
відновлена база показувала б порожні рамки. Ключ не потрібен (бакет публічний).

### Секрети (створює Вова)
`SUPABASE_DB_URL` — ⚠️ саме **Session pooler**: Direct-підключення Supabase віддає
лише по IPv6, а сервери GitHub Actions ходять по IPv4 → з Direct не підключиться.
`BACKUP_PASSPHRASE` — загубиш, бекапи мертві назавжди.

### 🔑 Урок: я «знайшов» баг, якого не було
«Виправив» `[ -z "$X" ] && …` на `if`, стверджуючи що при `set -e` невдала
перевірка вбиває крок. **Прогін обох варіантів спростував**: `set -e` не спрацьовує
на лівій частині `&&` (стара версія дала вихід 0 і надрукувала повідомлення).
Форму `if` лишив — вона не залежить від позиції рядка, — але коментар у файлі,
що стверджував баг, переписав на правду.
➡️ «Першою підозрюваною є перевірка» діє і в інший бік: **твердження про баг теж
доводиться прогоном, а не міркуванням.**

### ✅ Перевірено локально
- YAML розібрано парсером (9 кроків)
- кожен `run`-крок → `bash -n` OK
- сторож секретів прогнано в ОБОХ станах: є → вихід 0; нема → вихід 1 з текстом
- витяг `ref` проєкту з рядка pooler → `uabyfecseqnemvcqhdem`
- **кругообіг шифрування повністю:** tar → gpg → без пароля НЕ читається →
  з паролем розпакувалось → команди перевірки з інструкції дали правильні числа

### ⬜ Черга Вови (я цього зробити не можу)
1. Створити два секрети в Settings → Secrets → Actions.
2. Actions → DB Backup → Run workflow (перевірити, що проходить).
3. Крок 3 інструкції — **тестове відновлення в ОКРЕМИЙ проєкт**.
   Бекап, який жодного разу не розгортали, бекапом не рахується.

**`CACHE_NAME` не чіпано** — змін у `src/`/`index.html`/`style.css`/`sw.js` нема
(перевірено `git status`).

---

## 🔴 ТРЕТЯ ЧАСТИНА (01.08) — фото приватних чатів були доступні будь-кому

**Питання Вови:** чи не видно десь пошту / персональні дані / приватні чати.

### ✅ Закрито (перевірено від імені anon)
`профілі_видно: 0 · приватні_повідомлення_видно: 0 · розмови_видно: 0`
`profiles` має email/phone/birth_date/surname/street/bio, але читання —
`uid = auth.uid()`. `get_public_profile` віддає лише uid/name/avatar_url/
settlement/trusted/created_at. Репозиторій чистий: пошт користувачів нема
(лише проєктна `olykacastle@gmail.com` і заглушка), знайдений телефон — з
тексту новини RSS.

### 🔴 Справдилось
`"Public read community photos"` давала анонімові SELECT на `storage.objects` =
**список файлів** (103 імені). У бакеті: **23 фото з приватних переписок** +
2 фото неопублікованих оголошень. Текст повідомлення RLS захищала, файл — ні.

### Зроблено
Міграція `storage_disable_anonymous_listing`: політику знято, лишився SELECT
власнику своїх об'єктів. **Було 103 → стало 0.** Бакет лишився `public = true`.
Безпечність доведена по коду: клієнт робить лише `.upload()` і `.getPublicUrl()`
(останній у мережу не ходить); `.list()/.download()/.remove()` нема ніде.

### ⬜ Чесні межі
- **Це півзаходу:** хто має посилання — відкриє. Справжнє рішення (окремий
  закритий бакет для чатів + тимчасові посилання) — наступний крок, разом
  із переглядом авторизації.
- **Завантаження картинки НЕ перевірив** — проксі сесії блокує зовнішні хости
  (`HTTP 000`). Візуальна перевірка за Вовою.
- Відкат однією командою записаний у `supabase_anon_lockdown.sql`.

### ⬜ Принагідно, не чіпав
Публічні оголошення показують 13 телефонів і 20 імен — за задумом, але їх можна
зібрати оптом одним запитом. Якщо захочемо — відкривати телефон лише залогіненим.

**`CACHE_NAME` не чіпано** — код не змінювався.

---

## 🏁 ЗАКРИТТЯ СЕСІЇ (01.08) — хендовер у новий чат

**Задеплоєно:** PR #734 → squash у `main` → прогін #3620.
`CACHE_NAME = cstl-20260801-1521` · стенди 31/31, 462/462.

### Дві великі теми за сесію
1. **Безпека** — анонім більше не може нічого; бекап бази заведено з нуля;
   фото приватних чатів у закритому сховищі. Деталі вище в цьому журналі.
2. **Вигляд табла новин** — «прилад» знято (дорога 2), віджет і хаб малюють
   новину однаково, монограма джерела вирівняла висоти.

### 🔴 Мої помилки цієї сесії — усі визнані вголос і виправлені
| Що | Як виявилось |
|---|---|
| «правило зламає фічу — 18 із 25 оголошень анонімні» | розріз ЗА ЧАСОМ показав: анонімною подачею не користувались 6 тижнів |
| «прогноз: віджет стисне до ~200px» | замір дав 362px, а варіант A взагалі 419px |
| «знайшов баг у шелі воркфлоу» | прогін обох варіантів довів, що бага не було |
| «формат часу непослідовний» | `formatTime` дає стандартні сходи relative→absolute |
| «список плашок — патерн 2015, треба лінії» | тестові знімки з БИТИМИ фото + Вова відхиляв лінії ще 31.07 |
| силует замку просто перефарбував | інверсія дала три сірі прямокутники |
| сховав мітку з боку віджета | сторож `news-widget.mjs` завалив — і за ділом |
| `git checkout main` | локальний main застарілий на 138 комітів, відкотив дерево |

➡️ Спільне в останніх трьох: **я діяв, не перевіривши наслідок**. Там, де
перевіряв (заміри, стенди, бойові спроби від імені anon) — помилок не було.

### Оновлена документація для нового чату
- `SESSION_STATE_VOVA.md` — блок «⭐ ХЕНДОВЕР сесія T» першим: стан проєкту
  загалом + що зроблено + що робити далі + **чого не робити**.
- `BYYOU_PLAN.md` — статус `paused`, кроки 11-14 розписані з деталями.
- `AUDIT_NEWS_2026-08.md` — дві хибні знахідки позначені як знято, з поясненням.

### ⬜ Черга Вови
1. Секрети для бекапу (`SUPABASE_DB_URL` через Session pooler + `BACKUP_PASSPHRASE`).
2. Тестове відновлення бекапу в окремий проєкт.
3. Смоук на iPhone: фото в приватному чаті (старі + нові).

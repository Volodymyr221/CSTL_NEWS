# Журнал сесії — 2026-07-29 (сесія N)

**Гілка:** `claude/startuem-yky235` · production: `main`
**Старт:** `/startuem`

---

## Крок 0 — старт (виконано автоматично)

- `git config user.name "Вова"` + `user.email vitocorleone191@outlook.com` — коміти
  підписуються Вовою. «Unverified» на GitHub — очікувано, ігноруємо (рішення 10.07).
- `git fetch origin` + `git pull origin main` — підтягнуто автокоміти парсерів
  (новини `f3aa81d2`, розклад автобусів). Гілка сесії = `origin/main` (0 комітів різниці).
- Заведено цей журнал.

## Читання (обовʼязкове)

`HOT_RULES.md` · `CSTL NEWS VOVA/CLAUDE.md` · `SESSION_STATE_VOVA.md` ·
`AUDIT_2026-07.md` · `CSTL_BUGS.md` · `BYYOU_PLAN.md` · `BACKLOG.md`

## Знятий стан (факти, не з пам'яті)

| Що | Значення |
|---|---|
| `CACHE_NAME` у `sw.js` | `cstl-20260729-1839` |
| Стендів у `tests/*.mjs` | 24 файли (BYYOU_PLAN: 22 стенди / 272 перевірки зелені) |
| Останній змістовий коміт main | `605a7caf` fix(ci) · перед ним `f8621d0f` feat(chat) PR #715 |
| Відкритих 🔴 багів | нема (`CSTL_BUGS.md`) |

## ⚠️ Розбіжності в документах (доповів Вові у першій відповіді)

1. **`BYYOU_PLAN.md` статус `active`, «кроки 1-14 локально, БЕЗ push», гілка
   `claude/startuem-yxqfd9`** — але PR **#715** «розмова = людина, оголошення —
   контексти всередині» **уже змерджений у `main`** (коміт `f8621d0f`) і задеплоєний.
   Тобто потік фактично на проді; відкритим лишається тільки крок 15 — смоук на iPhone.
2. **`SESSION_STATE_VOVA.md` (сесія M) відстає:** пише `CACHE_NAME = cstl-20260729-1749`
   і «21 стенд / 254 перевірки», у файлах — `cstl-20260729-1839` і 22/272 (після #715).
3. У `SESSION_STATE_VOVA.md` двічі підряд той самий блок «Урок: липкий елемент НЕ
   успадковує фон свого аркуша» (рядки 49-54 і 56-61) — дубль після мерджу.

## Далі

Чекаю «Роби» від Вови.

---

## Аудит безпеки за чек-листом Вови (10 промптів зі сторонньої добірки)

**Запит Вови:** «проаналізуй їх чи вони корисні, якщо так то використай їх для аудиту».
Код НЕ чіпав — «Роби» не було. Тільки читання + заміри.

**Вирок:** список писаний під «свій сервер + своя БД + Stripe». У нас статика на GitHub
Pages + Supabase (BaaS) + Google OAuth. **3 закриті · 4 корисні · 3 не про нас.**

### ❗ Головне: п.2 виконувати НЕ МОЖНА
«Перепиши логіку логіну на bcrypt» → ШІ завів би ВЛАСНУ таблицю паролів там, де її
фізично нема. Паролі тримає Supabase Auth, наша БД їх не бачить. Пароль існує в одному
місці — вхід в адмінку (`admin.html:620`).

### Порядково (з доказами)
| # | Стан | Доказ |
|---|---|---|
| 1 HTTPS | ✅ | 0 посилань `http://` у `src/`+`index.html`+`sw.js`. Живі заголовки НЕ перевірив — проксі сесії дав 403 `host_not_allowed` на github.io |
| 2 паролі | ✅ не чіпати | `signInWithPassword` лише `admin.html:620` |
| 3 боти | 🟡 дірка | нижче |
| 4 сесії | 🟡 настройка | `supabase.js:26` persistSession/autoRefreshToken |
| 5 CSRF | ❌ н/з | JWT у заголовку, не cookie |
| 6 скидання | 🟡 настройка | `admin.html:676` signInWithOtp → `:1862` updateUser |
| 7 ключ БД | ✅ | `sb_publishable_…` (`supabase.js:15`), `service_role` у репо нема, є `gitleaks.yml` |
| 8 логи | ✅ | **0** `console.log`; 46 `console.warn` + 2 `console.info`, усі друкують `error.message` |
| 9 біллінг | 🟡 | свої стелі $1.20/прогін, $4/міс (`ai_news_agent.py:62-63`), алертів провайдера нема |
| 10 бекапи | 🔴 | нижче |

### 🔴 Знахідка 1 — бекапів живої БД НЕМА
У `.github/workflows/` жодного бекапу. `backup/` у репо = старі копії CSS/JS, не дані.
`data/*.json` у git (новини, розклад) — версіонуються. А пости Стрічки, коментарі, чати,
профілі, оголошення — ТІЛЬКИ в Supabase, на безкоштовному тарифі автобекапів нема.

### 🟡 Знахідка 2 — анонімні входи без ліміту
Rate limit 8/15с є лише на коментарях і чаті (`supabase_antispam_shared.sql:163,201`).
Нема на: подачі оголошення (`Anyone can submit a pending post`) і — дорожче —
`Anyone can upload to community-photos` для `public` (`supabase_schema.sql:262-264`):
анонім заливає файли у сховище без обмежень. CAPTCHA: 0 згадок у коді.

### 🟡 Знахідка 3 — чотири тумблери в консолі Supabase (коду не треба)
`signOut` глобальний (`scope:'global'`) · OTP TTL 3600с → 15-30 хв · білий список
Redirect URLs (у нас `emailRedirectTo: window.location.href`, `admin.html:678`) ·
`auth_leaked_password_protection` вимкнено.

### Чого в чек-листі НЕМА, а нам потрібніше
1. Наскрізний тест RLS анонімом/другим акаунтом (вже 🔴 у `BACKLOG.md`).
2. XSS: **185** `innerHTML =` проти **358** `escapeHtml(` — співвідношення здорове,
   але це не доказ що прикриті всі 185. Для нашого стеку ризик більший за 7 із 10 пунктів.

### Запропоновано Вові (чекає «Роби»)
1) бекап `pg_dump` за розкладом + тестове відновлення · 2) ліміт на анонімне фото ·
3) rate limit на подачу оголошення (тим самим двигуном, копію НЕ робити) · 4) тумблери.

⚠️ **Обмеження аудиту:** живу базу не перевіряв — MCP-конектор Supabase відвалився
(потребує авторизації, у неінтерактивній сесії пройти не можу). Усе знято з коду й SQL.
Урок 28.07 «файл ≠ стан системи» діє: перед накатом звірити з продом.

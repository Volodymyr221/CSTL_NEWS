# Журнал сесії — 18.09.2026 «b»

> Друга сесія за 18.09 (перша — `vova-2026-09-18.md`, закінчилась PR #1184 і передачею).
> Ведеться ПО ХОДУ, не з памʼяті. Кожна значуща дія — окремий рядок.

## Старт

- **Гілка:** `claude/startuem-x0odbh`, збігається з `origin/main` (`da5f1ec8`) — розходжень нема,
  «не доїхало в main» порожньо.
- **Коміти підписані** Вовою (`git config user.name/email`).
- **Прочитано** (обовʼязкове читання `/startuem`): `NOW.md` · `HOT_RULES.md` ·
  `CSTL_BUGS.md` · `BACKLOG_VOVA_2026-08.md` · `ВОВА_ПРОФІЛЬ.md` · `NEVYKONANI_ZAVDANNIA.md`.
- **`BYYOU_PLAN.md`** — статус `idle`, незавершеного потоку немає, пересідати нікуди.
- **Стан:** чекаю «Роби» від Вови. Коду не чіпав.

## Хід сесії

_(дописується по ходу)_

## Замовлення Вови 18.09 — ЛОГІКА ЗБЕРЕЖЕНОГО (діагностика, «Роби» ще немає)

🗣️ Дослівно: «якщо я чи будь-який інший користувач натисну відстежувати щось… і воно
видалилось з додатку, то воно має пропасти із збереження, тому що його вже не існує.
А якщо, наприклад, користувач відстежує рейс якийсь автобусний, і він скасований, то
він має бути там, але так само з позначкою скасований… нам треба продумати детальну
логіку цих збережень».

### Заміряно на ЖИВІЙ базі (не з голови)

```
saved_posts — 5 рядків, з них 2 мертві:
  post 90 · type=chat · «Тест» · створено 29.08 · deleted_at 09.09
    зберегли ДВОЄ: volodymyrshevchuk19@ (admin=true) і olena2762@ (admin=false)
saved_articles — 2 рядки, 1 людина, найдавніший 24.08
```

🔴 **Симптом РІЗНИЙ у двох людей, а вада одна.** Політика `posts read`:
`(deleted_at is null and post_visible_row(...)) or is_admin()`.
- Вова (адмін) → база віддає видалений рядок → **мертва картка в хабі**;
- Олена (не адмін) → база не віддає → **картка тихо зникла**, рядок у `saved_posts`
  лишився назавжди. Вона навіть не дізналась.

### Чому тап кидає на ДОШКУ і каже «оголошення»

`saved-hub.js:405` → `openBoardItemById(id)` (`board.js:2941`). Там `allPosts` береться з
`fetchPublishedPosts()` — видаленого нема → гілка `if (!post)` робить
`switchTab('board')` + тост «Це оголошення більше недоступне».
🔑 **Тип запису на цей момент УЖЕ втрачено** — тому відповідь завжди «оголошення» і
завжди Дошка, хоч би що було збережене. А хаб тип ЗНАЄ (`data-shub-type="chat"`) і не
передає.

### Ще два місця того самого класу

- **Статті:** `getArticlesByIds` (`news.js:503`) робить `.filter(Boolean)` — стаття,
  змиту ротацією за віком, просто викидає. Тихо. Рядок у `saved_articles` лишається.
  ⚠️ Ротація працює постійно → це гарантоване майбутнє зникнення в УСІХ.
- **Автобуси:** `getSavedRoutesForUI` (`buses.js:2129`) не читає `status` рейсу взагалі.
  А `status === 'cancelled'` у даних Є (9 записів у `schedule.json`), і вкладка Автобуси
  (`buses.js:1616`) та капсули (`home-caps.js:565`) його знають. Хаб — єдине місце, де
  скасування невидиме. Рівно те, про що сказав Вова.

### 🛑 Пастка, через яку «просто чистити на клієнті» НЕ МОЖНА

Клієнт звичайної людини фізично НЕ відрізняє «видалено» від «не завантажилось» — RLS
ховає видалене так само, як обрив мережі. Почистити за відсутністю даних = стерти живі
закладки людини при поганому інтернеті. Той самий клас, що урок 18.09 «збій приходив
винятком, а не значенням».

**Стан:** ⏸ чекає «Роби». Питання Вові поставлені у відповіді.

## ✅ ЗРОБЛЕНО — «Роби» отримано, логіка збереженого

**Рішення Вови на чотири питання:** 1) прибирати + називати запис («Оголошення
«назва», яке ви зберегли, знято»); 2) завершене оголошення — «Знято» (лишається);
3) знімок для статей — «Окей»; 4) минулі рейси — прибрати, «все що пройшло».

### Серверна половина — `scripts/supabase_saved_lifecycle.sql`, міграція `saved_items_lifecycle`

- `saved_posts.snap_title/snap_kind`, `saved_articles.snap_title/snap_url` — ЗНІМОК
  у мить збереження. Без нього назвати видалений запис нічим.
- 🔑 **Разове заповнення спрацювало краще, ніж я обіцяв Вові:** мʼяке видалення
  лишає рядок у `posts`, тож знімки проставились і мертвим закладкам — «Тест»
  БУДЕ названий. Я попереджав, що старі записи лишаться безіменними — це
  виявилось надто песимістичним, і я це виправив у відповіді.
- `saved_post_state(exists, deleted_at, status)` — правило ВІД КОЛОНОК, одне місце.
  🔴 `pending` НЕ прибираємо: правка автора шле оголошення на повторну модерацію,
  і «зник із виду» там означало б мовчки нищити чужі закладки.
- `sync_saved_posts()` — звірка + чистка одним тактом, назви знімаються ДО `delete`.
- `save_post()` / `save_article()` — запис зі знімком; `save_post` відмовляє на
  вже видаленому («Запису вже немає»), тобто мертву закладку не створити й навмисно.

**Доведено на живій базі транзакцією з відкотом**, під JWT ОБОХ акаунтів:
`removed:[{kind:"chat",title:"Тест",post_id:90}]`, `items:[]`; після `rollback`
в таблиці ті самі 5 рядків.

### Клієнт

- `supabase.js`: `addSavedPost` → RPC `save_post`; `syncSavedPosts(uid)`;
  `addSavedArticle(uid,id,title,url)`; `fetchSavedArticleSnaps`.
- `news.js`: `getArticlesByIds` → **`collectSavedArticles`** — віддає `{ok, items}`
  зі станом. 🛑 `ok:false` при `newsLoadFailed()` = «не знаю», і тоді НЕ чистимо.
- `buses.js`: `routeOnDate()` + `state` у `getSavedRoutesForUI` (`cancelled` /
  `unknown` / `alive`), минулі відсіяні на показі.
- `board.js`: `openBoardItemById(id, focusCommentId, knownKind)`.
- 🔴 **`app.js` — ТА САМА ВАДА З ІНШОГО ВХОДУ, знайшов дорогою:** deep-link
  сповіщення `#/post/disc/<id>` знав тип із самої адреси і НЕ передавав його —
  push про видалене питання теж кидав на Дошку зі словом «оголошення».
- `saved-hub.js`: позначки (`STATE_BADGES`), іменоване повідомлення
  (`REMOVED_WORDING`, стеля 3 рядки), тап за типом, стаття `gone` → на джерело.
- `style/account.css`: `.shub-badge` (3 тони) + `.shub-notice`.

### Сторож — `tests/saved-lifecycle.mjs` **18/18**

- Контроль `BUNDLE_REV=origin/main` → **10/18**, і він ДОСЛІВНО відтворює скаргу:
  `вкладка=board`, тост «Це оголошення більше недоступне» — на ПИТАННІ.
- Мутація `NEWS_BLIND=1` (файл новин віддає 500) → 18/18 із захистом.

🛑 **ДВІ ВЛАСНІ ПОМИЛКИ СТОРОЖА, ОБИДВІ СПІЙМАНІ МУТАЦІЄЮ, НЕ ОКОМ:**
1. **Перевірка тапу міряла ЖИВЕ питання** — а живий запис і старий код вів
   правильно (він сам бачив `type`). Тобто вона була зелена на обох ревізіях:
   стерегла те, що ніколи не ламалось. Переписано на МЕРТВИЙ запис через
   deep-link — єдиний шлях, яким по ньому ще можна тапнути.
2. **Перевірка «при збої нічого не прибрано» міряла ЗАКРИТИЙ хаб.** Вузлів
   немає → умова істинна ЗАВЖДИ. Доведено мутацією: зі знятим захистом
   `newsLoadFailed()` стенд лишався **20/20**, тобто стеріг порожнечу. Тепер
   аркуш відкривається заново, і окремий рядок доводить, що прилад його бачить.
   ➡️ Після виправлення мутант дає **17/18** з причиною «Новина «Новина без
   джерела»… більше не в стрічці» — тобто перевірка справді вміє впасти.

`CACHE_NAME` → `cstl-20260918-2153`.

## 🔴 ВЛАСНА РЕГРЕСІЯ, СПІЙМАНА ПОВНИМ ПРОГОНОМ

`npm test` дав **151/157**. Розбір хвоста:

| стенд | вердикт |
|---|---|
| `modal-surface` 12/13 | 🔴 **МОЯ РЕГРЕСІЯ.** Я дав позначкам теплі підкладки `#FBF1D6` (+37) і `#F6E4E6` (+16) — і це через два рядки після власного коментаря «кольори ТОКЕНАМИ, не сирим hex». Виправлено: фон усіх трьох нейтральний `--board-press`, розрізняє їх колір ТЕКСТУ; «Скасовано» — бордова рамка замість заливки. **13/13** |
| `docs-refs` 8/10 | застарілий знімок: прогін стартував до того, як я оновив число стендів 156→157. Зараз **10/10** |
| `news-badge-geo` 9/10 | на моєму коді окремим прогоном — **10/10**. Впав під навантаженням повного набору, не від змін |
| `design-system` 18/19 | **не від сесії, доведено контролем:** `BUNDLE_REV=CSS_REV=origin/main` теж падає (47% при стелі 45%). Це відкритий пункт реєстру Г. Заразом знизив вагу своєї позначки 700→600, щоб не сперечатися з приладом через один підпис на 11px |

🔑 **Урок:** повний прогін коштував часу, але спіймав те, чого не спіймали ні
прицільні стенди, ні моє око — причому в місці, де я САМ щойно написав правило.

## 🚀 ФІНІШ (19.09, 08:34 UTC)

- Злито свіжий `main` у гілку ДО PR (20 комітів парсерів) → стенд `saved-lifecycle`
  після злиття лишився **18/18**.
- `CACHE_NAME` → **`cstl-20260919-0834`**.
- Сторожі документації: `docs-fresh` **23/23**, `docs-refs` **10/10**.
  ⚠️ Пульс довелось стискати ТРИЧІ — авто-блок додає рядки, і бюджет 200 рядків /
  16 KB тримається впритул. Стелю не піднімав (правило).
- **PR #1185 → squash у `main`** (`afca16db`) → **деплой #6037**, build і deploy
  обидва `success` (08:37:27 UTC).
- 🛑 Живий сайт із пісочниці не читається (проксі глушить `castlelife.org`) —
  висновок про деплой зроблено з GitHub Actions, а не з мовчазного `curl`.
  Це той самий урок 12.08 про volynpost: мережеві висновки з пісочниці робити не можна.
- ⏳ **Пальцем перевіряє Вова** — порядок, який він сам і задав: «ти мусиш зробити
  фініш, а тоді я перевіряю тебе».

## 🎚 ЗАМОВЛЕННЯ 19.09 — ШКАЛА РОЛЕЙ + СИСТЕМА ОНОВЛЕНЬ («Давай» отримано)

### Рішення Вови (дослівно, по пунктах)

- Назва розділу — **«Оновлення»**; перемикач «дивитись як житель» — **одразу**.
- Вмикати оновлення можуть **власники, розробники та адміни застосунку (НЕ спільнот)**;
  випускати «Усім» — **Власник + CEO + Розробник**.
- 🗣️ «Я мав на увазі **CEO** проекту» (я перепитав — він казав «SEO», а описував
  керівника; назва йде в базу, тож уточнення було обовʼязкове).
- Витрати AI розробнику — **«Давай»** (👁 дивиться, суми не міняє).
- 🗣️ **«треба додати можливість додавати дві ролі на одну людину. Тобто зараз я
  буду власником і буду CEO-проєкту»**.
- 🗣️ Про гнучкість: «треба зробити таку можливість, щоб певна роль мала
  обмеження, але в разі чого можна додати якийсь певний вид».
- 🗣️ Адміни спільнот: «вони взагалі не мають відношення до адмінки… це нижчий
  левел, вони ведуть просто сторінки».

### 🔴 ЩО ЗНАЙШЛОСЬ ПЕРЕВІРКОЮ СКАРГИ «не можу додати редактора»

Вова має рацію, і причина гірша за симптом:
- розділ «Адміни» має форму додавання (`insert into admins` по пошті);
- розділ «Доступи» має ЛИШЕ «Відкликати» і «Повернути» — **додати неможливо**;
- таблиця **`editor_invites` у базі Є**, а в коді (`admin.html`, `src/`) вона не
  згадується **жодного разу** (`grep` → нуль).

➡️ Тобто механізм запрошення редактора збудований наполовину: **сховище є, дверей
немає**, а документація весь час писала «кабінет і механізм запрошення готові».
Той самий клас, що `kb-guard.test.js` — документ обіцяє те, чого в коді нема.

### Модель, яку будую (не шкала, а НАБІР)

Кілька ролей на людину відкидає ідею «рівнів»: сказати «Вова = рівень 1» не можна,
бо він Власник І CEO. Тому:

```
user_roles(uid, role)      — ролей у людини може бути КІЛЬКА
role_perms(role, area, lvl)— заготовка дозволів ролі
user_perms(uid, area, lvl) — ОСОБИСТИЙ виняток, перебиває заготовку
                             (це і є «в разі чого додати певний вид»)
підсумок = НАЙВИЩИЙ рівень серед усіх ролей, потім особистий виняток
```

Рівні: `none` · `view` (бачить, не міняє) · `manage`.
🔑 `view` і є дослівне «він може це бачити, але не видаляти».

🛑 **Особливі права поза розділами:** керувати ВЛАСНИКАМИ — тільки Власник;
випускати «Усім» — Власник/CEO/Розробник.
🛑 **Адміни спільнот (`page_admins`, 8 рядків) у цю систему НЕ входять.**

### Ризик, який лікую сумісністю

`is_admin()` зараз = `exists (select 1 from admins where email = auth.email())`.
Переписати різко = ризик замкнути Вову з адмінки. Тому нова перевірка йде
**АБО** зі старою: поки старий рядок у `admins` живий, доступ тримається навіть
якщо нова гілка помилиться. Прибирання старого шляху — окремим кроком, після
того як Вова підтвердить пальцем.

### ✅ Крок 1 ЗРОБЛЕНО — ролі в базі й адмінці

**База** (`scripts/supabase_roles.sql`, міграції `app_roles_and_permissions` +
`team_list_and_people_search`):
`app_roles` · `user_roles` (кілька на людину) · `role_perms` (заготовка) ·
`user_perms` (особистий виняток) · `my_perm` / `has_perm` / `has_role` /
`is_owner` / `can_manage_owners` / `can_release_update` / `grant_role` /
`revoke_role` / `set_user_perm` / `my_access` / `team_list` / `people_search`.

**Доведено на живій базі транзакціями з відкотом — 14 перевірок:**
Вова = owner+ceo, усі 14 розділів manage · розробник: updates manage,
board/questions/spend/analytics view, people/access none · «дати собі owner» →
відмова · «дати собі admin» → відмова · «дописати собі розділ» → відмова ·
«зняти останнього власника» → відмова.

**Адмінка:** `my_access()` при вході → `permOf()` / `can()` / `hasRole()`;
усі 11 пунктів меню переведені з `show: isAdmin` на `show: can(<розділ>)`;
розділ «Доступи» дістав блок «Команда і ролі» — кнопки ролей (✓ = є), пошук
людей і «+ У команду».
🛑 `can()` при збої `my_access` падає на стару перевірку `isAdmin` — збій мережі
не сміє зачинити кабінет.
🛑 Кнопка ролі, якої людина не може натиснути, ВИМКНЕНА, а не схована: схована
читається як «такого не буває».
🔑 «+ У команду» дає найнижчу роль (редактор) — додати людину і видати їй права
це два різні рішення.

Стенди адмінки після зміни: `admin-access` 46/46 · `admin-shell` 14/14 ·
`admin-agents` 10/10 · `admin-reports` 11/11 · `admin-publish-truth` 26/26 ·
`analytics-screen` 106/106.

⏳ **Не зроблено (крок 2):** розділ «Оновлення» з прапорцями, коло раннього
доступу і перемикач «дивитись як житель».

---

## 19.09 «Фініш і дороби оновлення» — КРОК 2 ЗАКРИТО

**Міграція `feature_flags_and_testers`:** `app_features` (`key`, `label`,
`descr`, `stage`, `released_at`, `changed_by`) · `feature_testers` ·
`my_features()` · `set_feature_stage()` · `set_feature_tester()` ·
`feature_testers_list()` · `register_feature()`. Джерело —
`scripts/supabase_features.sql`.

**Клієнт:** `src/core/features.js` (`featureOn`, `inTestCircle`, `asResident`,
`setAsResident`), виклик `loadFeatures()` в `app.js` після `initSavedHub()`,
пункт «Дивитись як житель» у секції `info` бічного меню.
🛑 Перша спроба поклала пункт у секцію `cards` — там інший шаблон, без
`.sidebar-item-label`, і підпис виходив ПОРОЖНІЙ. Спіймано стендом, не оком.

**Адмінка:** `renderUpdates()` — картки фіч із трьома станами, коло з пошуком
людей, `confirm` на «Усім», нагадування прибрати прапорець через 7 днів.
🛑 Стан НЕ міняється оптимістично: у розділі, який вирішує, що побачить громада,
намальоване «готово» над відмовою сервера — найгірше з можливого.

### 🔴 Вада, знайдена ПЕРЕД деплоєм (і чому вона важлива)

Заміряно перед випуском: `select ... from app_features` → **0 рядків**.
`inTestCircle()` виводив «я в колі» з самих фіч, тож при нулі фіч власник
виглядав як сторонній — пункт «Дивитись як житель» не малювався б узагалі.
Система, замовлена щоб «протестити В РАМКАХ ДОДАТКУ», була б непроверяєма рівно
в мить, коли Вова вперше її відкриє.

✅ Міграція `my_features_reports_circle_membership`: відповідь стала
`{ in_circle, features }`. Доведено відкотними транзакціями на живій базі:
власник `in_circle: true` при нулі фіч, сторонній `false`.

### Сторож

`tests/updates-flags.mjs` **13/13**, пʼять сцен. Дві мутації спіймали дві мої ж
неправди: блок 4 у першій редакції стеріг порожнечу (перевіряв вихід із режиму
на людині В КОЛІ, де запобіжник ні до чого), а перевірка порожньої бази доведена
поверненням старої логіки — **12/13** саме тим рядком.

Сусідні стенди після зміни: `sidebar-menu` 50/50 · `sidebar-account` 11/11 ·
`sidebar-ghost-tap` 17/17 · `sidebar-overlay` 36/36 · `admin-access` 46/46 ·
`admin-shell` 14/14 · `saved-hub` 26/26 · `saved-lifecycle` 18/18 ·
`modal-surface` 13/13 · `drift` 25/25 · `docs-refs` 10/10 · `docs-fresh` 23/23.

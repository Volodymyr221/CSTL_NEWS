# Журнал сесії — 01.09.2026 (сесія «c»)

Гілка: `claude/startuem-wzboev` · production: `main`

---

## Старт

- `git config user.name "Вова"` / `user.email vitocorleone191@outlook.com` — виконано.
- `git fetch origin` — пройшов (SessionStart-хук скаржився на мережу, ручний fetch вдався).
- Звірка з `origin/main`: `origin/main ^HEAD` — **порожньо**, `HEAD ^origin/main` — **порожньо**.
  Гілка = `main` коміт-у-коміт (`0d227e18 docs: передача в новий чат — 01.09.2026 (#1136)`).
- Прочитано обовʼязковий список: `NOW.md` · `HOT_RULES.md` · `CSTL_BUGS.md` ·
  `BACKLOG_VOVA_2026-08.md` (шапка + розмежування з NOW) · `ВОВА_ПРОФІЛЬ.md` ·
  `NEW_SESSION_PROMPT.md`.
- `BYYOU_PLAN.md` — файлу нема, незавершеного потоку `/byyou` немає.
- Журнал за сьогодні: вже були `vova-2026-09-01.md` і `...-01b.md` → взято літеру **`c`**.

## 🔎 Розходження, помічене при читанні (не чіпав, доповідаю)

- `NOW.md` каже **B-22 ЗАКРИТО** (PR #1135, дедуп за подією), а в `CSTL NEWS VOVA/CSTL_BUGS.md`
  рядок **B-22** досі стоїть у 🟡 «Середні» як відкритий, з рекомендацією «додати containment».
  Це рівно та вада, за яку в беклозі записаний урок 26.08: «закриваєш борг у коді —
  гаси його в УСІХ документах». Правити чекаю «Роби».

## Чекаю "Роби"

---

## 1. 🧹 ЛАД У ДОКУМЕНТАЦІЇ — «Роби» від Вови

🗣️ Вова: «Наведи лад у документації і даю тобі нове завдання».

### Спершу заміряв, що саме розійшлось (не правив на здогад)

- `node tests/docs-fresh.mjs` → **19/20**, падає рівно одна: авто-блок пульсу відстав від git.
- `npm test` → **31/133** стендів, але це середовище: `playwright` не встановлений,
  браузерні стенди не стартують. `702/704` перевірок там, де стенд узагалі пішов.
  🔑 **Число «133 стенди» в `CLAUDE.md` і `START_HERE.md` виявилось ПРАВДИВИМ** — рахує
  сам `tests/run.mjs`, а `ls tests/*.mjs` дає 136, бо там ще й `run.mjs` з помічниками.
  Тобто підозра на розходження не підтвердилась — записую, щоб не перевіряли втретє.
- `HOT_RULES.md` каже «15 правил» — заголовків рівно 15 (0…13 + 11-БІС). ✅ Не розходження.

### 🔴 Що справді брехало і що виправлено

**1. `CSTL_BUGS.md` — B-22 стояв ВІДКРИТИМ**, хоча закритий учора (PR #1135).
Рядок перенесено в «Закриті» з датою `2026-09-01`, у шапці «Критичних» B-22 додано
до переліку закритих. В опис внесено те, чого в старому рядку не було: опис від
20.07 звинувачував поріг `0.65` (пара 0.55), а справжній випадок дав Jaccard
заголовків **0.14** — тобто діагноз у баг-трекері був НЕ ТОЙ, і його не можна було
лишати як історію.

**2. `docs/ALGORITHMS.md`, розділ «И. ДЕДУПЛІКАЦІЯ» — описував алгоритм, якого в коді
вже немає.** Стояло «`is_dup_title()`, поріг 0.65» і 🟡 «B-22, відкритий». Переписано
під те, що реально в `parse_rss.py`: **два рівні** (заголовковий Jaccard 0.65 для всіх
джерел + подієвий `dup_score()` з вагою за рідкістю слова для RSS), таблицею, з
поясненням ЧОМУ ділимо на вагу коротшої статті, а не на обʼєднання.

**3. Черга покращень у тому ж файлі — два застарілі пункти:**
- п.1 казав `ai-budget` **18/19**, насправді **20/21** (звірено прогоном).
- п.5 казав «шкала капсул спирається на віджет автобусів» як відкриту загрозу —
  борг закрито 01.09 (віджет прибрано, `BUS_SOON` 60→68, сторож `caps-rank-chain`).

**4. `docs/AI_AGENT_OLYKA_CASTLE.md` казав «Стенд `ai-budget` 19/19»** — тобто «все
зелено». Прогін дає **20/21**. Найгірший клас неправди в документації: він знімає
обережність. Замінено на чесне «НЕ зелений, падає перевірка добової кишені, не
розібрано».

**5. 📐 ЗАСТИГЛІ ЧИСЛА — розходились між собою у трьох місцях одразу.**
Коментар у `parse_rss.py` казав **14** спрацювань дедупу, `NOW.md` — **18**,
а сьогоднішній прогін сторожа дає **16**. Усі три «правдиві» — просто корпус
змінюється щогодини. Виправлено не звіркою чисел, а **зняттям числа зі статусу
факту**: у `parse_rss.py`, `tests/news-dedup.mjs` і `ALGORITHMS.md` тепер сказано,
що фіксується ЧАСТКА (стеля 6%), а число рухається. `CLAUDE.md` «зараз 4.5%» —
так само.
🔑 Це той самий клас, що урок беклогу 26.08 («рядок брехав 4 доби»), тільки
дешевший: число не радить зайву роботу, воно підриває довіру до сусідніх чисел.

**Перевірки після правок:** `python3 -m ast` на `parse_rss.py` ✅ · `tests/news-dedup.mjs`
**5/5** ✅ (сторож править себе сам не міг — коментар не впливає на логіку).

## 2. 🔴 ГОЛОВНА ЗНАХІДКА — ВИПРАВЛЕННЯ 15.08 БУЛО ЗРОБЛЕНЕ НАПОЛОВИНУ

Це не «неточність у документі», це **невиконанний наказ, який діяв 17 днів**.

**Ланцюг, як його проходить новий чат:**
1. `/startuem` Крок 2 → короткий список із пʼяти файлів (≈38 тис. ток. = 19% вікна). ✅
2. `/startuem` **Крок 1** → «прочитай `NEW_SESSION_PROMPT.md`… **Виконуй інструкції з
   цього файлу як свої. Він для тебе — офіційний контракт на всю сесію**».
3. А в контракті стояв **старий список із восьми файлів** із наказом «не пропускай жодного».

**Заміряно сьогодні:** 919 KB ≈ **325 тис. токенів при вікні 200 тис. = 162%.**
Тобто 15.08 скоротили список у скілі, а контракт, який той самий скіл оголошує
головним, лишили як був — і він знову посилав читати те саме.

🔑 **Урок, ширший за цей файл:** правило, що живе у двох місцях, полагоджене в
одному — **не полагоджене**. Той самий клас, що урок беклогу 26.08 («рядок брехав
4 доби і радив зроблену роботу»).

**Що зроблено:**
- `NEW_SESSION_PROMPT.md` — список зведено до тих самих пʼяти файлів, додано
  таблицю ситуативного читання і розбір, чому список саме такий.
- 🛡 **`tests/docs-fresh.mjs` — нова перевірка «два списки збігаються»** (+2 контролі).
  Раніше сторож дивився ЛИШЕ на `/startuem`, тому 17 днів світив зелене на
  половинчастому виправленні. Тепер розходження називається поіменно.
- **Контрольний прогін обовʼязковий і зроблено:** відтворив ваду (повернув вісім
  файлів) → сторож **21/23**, у виводі «зайві: SESSION_STATE_VOVA.md,
  AUDIT_2026-07.md, CLAUDE.md, START_HERE.md»; після відновлення — **22/23**
  (лишається тільки відомий пульс).
- ⚠️ **Перша версія перевірки сама була неточна** — регулярка добігала до заголовка
  розділу і хапала файли з пояснювальної цитати, доповідаючи про розходження, якого
  немає. Звужено до пронумерованих пунктів; причину записано в коментар, щоб
  наступний не «полагодив» назад.

## 3. 📐 ВАГИ ФАЙЛІВ У ТАБЛИЦІ БУЛИ ЗАНИЖЕНІ У ВІСІМ РАЗІВ

| файл | писало | насправді (01.09) |
|---|---|---|
| `_ai-tools/SESSION_STATE_VOVA.md` | 49 KB | 🔴 **389 KB ≈ 138k ток. = 69% вікна** |
| `CSTL NEWS VOVA/CLAUDE.md` | 172 KB | **272 KB ≈ 96k ток.** |
| `START_HERE.md` | 86 KB | 89 KB |
| `_ai-tools/AUDIT_2026-07.md` | 67 KB | 69 KB |
| `docs/ALGORITHMS.md` | 30 KB | 37 KB |

🛑 **Літопис розрізали 15.08 саме до 49 KB — за два тижні він набіг назад до 389 KB,
тобто живий зріз тепер ВАЖЧИЙ за власний архів** (315 KB). Сесія, яка бачить у
таблиці «49 KB», вирішує «загляну цілком» — і витрачає дві третини вікна.
Ваги виправлено в `.claude/commands/startuem.md`, у `NEW_SESSION_PROMPT.md`
таблиця написана вже з правдивими числами.
⚠️ **Сам розріз літопису НЕ робив — це рішення про вміст, а не про лад.** Виношу
Вові окремим питанням.

## 4. 💰 `data/fundraisers.json` — файла нема з 17.08, а документи вели до нього

`START_HERE.md` у чотирьох місцях (розділ про тестовий збір · список знахідок ·
дерево файлів · карта `src/tabs/`) і `BACKLOG_VOVA_2026-08.md` описували збори як
файл у git, з інструкцією «прибрати = зробити `items` порожнім масивом».
**Збори переїхали в таблицю `fundraisers` у Supabase 17.08** — код це знає
(`supabase.js:274`, `home-fund.js:71`), документи не знали.
🔑 Небезпечно тим, що інструкція **виконанна на вигляд**: людина шукає файл, не
знаходить, і не розуміє, чи це вона помилилась, чи документ. Правильний шлях —
адмінка → «Збори» → «Видалити». Виправлено в пʼяти місцях, ціну переїзду
(зникнення офлайну) перенесено з коментаря в коді в документ.

## 5. 📐 ЧИСЛА-ПЕРЕЛІКИ В КАРТІ ФАЙЛІВ

| що | писало | насправді |
|---|---|---|
| `src/core/` | 31 модуль («звірено 06.08») | **42** |
| `style/` | 21 файл, 20 через `@import` | **24 файли, 22 через `@import`** |
| `NOW.md` | «навмисно малий (≈110 рядків)» | 183; стеля — **200 рядків і 16 KB**, її тримає сторож |

У рядку про `src/core/` дописав, що це число застаріває швидше за все інше в карті,
і як його порахувати командою — щоб наступного разу не звіряли на око.
У кореневому `CLAUDE.md` замість «≈110 рядків» тепер названа сама **стеля** і хто її
тримає: число рядків рухається щодня, стеля — ні.

## 6. 🧹 ПУЛЬС — ДУБЛІ Й СТЕЛЯ

**У таблиці «ЗРОБЛЕНО В ЦІЙ СЕСІЇ» пʼять робіт стояли ДВІЧІ** — раз із номерами PR
(журнал `01.09b`), раз із рахунком стендів (журнал `08-31b`). Та сама робота, два
рядки, два різні посилання. Зведено: 13 рядків → 8, PR і стенди в одному рядку.

Далі пульс вибив стелю 16 KB (17.4 KB), і це виявилось найкориснішою частиною роботи:
щоб влізти, довелось питати про кожен блок **«а де ще живе його висновок?»** —
рівно те, що наказує правило під таблицею. Стиснуто чи прибрано лише те, що має
повний розбір деінде:
- розбір «приладу» і «пошук — переказ» → журнал `01.09b` (у пульсі лишився висновок);
- механіка B-22 → `docs/ALGORITHMS.md` «И» + `CSTL_BUGS.md`;
- ланцюг капсул ↔ віджет автобусів → `ALGORITHMS.md` ➡️ п.5;
- два рядки «дедуп не покриває все» → один, з посиланням на п.2 там само;
- рядок 29.08 (онбординг) → беклог, блок «29.08, сесія b»;
- рядок `CSTL_BUGS.md 40.6→24.5 KB` → сам `CSTL_BUGS.md` шапка «Закриті».

😅 **Останній прохід дав 16385 байтів при стелі 16384** — один байт. Показово:
саме через такі «майже влізло» файл і розповзається, і саме тому стеля машинна.

**Питання №11 у пульсі** — літопис виріс до 389 KB. Різати не став: це рішення про
вміст, не про лад.

## ✅ ПІДСУМОК

`node tests/docs-fresh.mjs` → **23/23** (було 19/20, і сама мірка не бачила половини вади).
`node tests/news-dedup.mjs` → **5/5**. Синтаксис `parse_rss.py` ✅.

**Правлено 11 файлів:** `NOW.md` · `CSTL_BUGS.md` · `CLAUDE.md` (корінь) ·
`CSTL NEWS VOVA/CLAUDE.md` · `START_HERE.md` · `NEW_SESSION_PROMPT.md` ·
`BACKLOG_VOVA_2026-08.md` · `docs/ALGORITHMS.md` · `docs/AI_AGENT_OLYKA_CASTLE.md` ·
`.claude/commands/startuem.md` · `tests/docs-fresh.mjs` (+ коментарі в
`scripts/parse_rss.py` і `tests/news-dedup.mjs`).

🛑 **Коду застосунку не чіпав** — жодного файлу в `src/`, `style/`, `index.html`,
`sw.js`. Тому **`CACHE_NAME` не бампав** (правило №2: тільки `.md`, коментарі та стенд).

**Чекаю нове завдання.**

---

## 7. 📞 НОВЕ ЗАМОВЛЕННЯ — ТЕЛЕФОНИ ГРОМАДИ, ТРИ ПРЕТЕНЗІЇ ВОВИ

🗣️ Дослівно: (1) «номер має бути під номером… номер А, далі вулиця Замкова, 17»;
(2) пін і копіювати «розташовані хаотично… якщо коротка назва, вона зразу за ним
стоїть, якщо довга — то далі… щоб копіювати було під копіювати, а пін під піном»;
(3) «іконки… відцентрувати вертикально, по центру самого тексту».

### 🔬 ЗАМІРЯНО ЖИВИМ БРАУЗЕРОМ (не на око)

⚠️ `playwright` у цьому середовищі не стояв — `npm test` давав 31/133. Поставив
локально, **`package.json` повернув назад** (`git checkout`): у CI він ставиться
своїм рядком `npx playwright install`, тож у залежності його класти не просили.
Стенд `tests/home-contacts.mjs` після цього — **17/17**.

**(1) Порядок рядків на картці ЦНАПу — підтверджено точно:**

```
y=1304  hm-ct-name   ЦНАП (адмінпослуги)
y=1341  hm-ct-phone  099 205 69 12
y=1356  hm-ct-meta   вул. Замкова, 17 — вхід ліворуч від головного   ← адреса
y=1392  hm-ct-alt    068 290 26 99                                   ← номер ПІД адресою
```

**(2) 🔴 МІЙ ПЕРШИЙ ЗДОГАД БУВ НЕВІРНИЙ — і прилад це показав.** Я вирішив, що
кнопки притиснуті праворуч (`.hm-ct-main` має `flex: 1 1 auto`), а винна лише
відсутність піна на картках без адреси. Замір дав **три різні позиції** однієї
кнопки:

| картка | copy x | правий край | пін |
|---|---|---|---|
| рада · ЦНАП · амбулаторія | **267** | 355 (край картки 356) | є |
| Соцзахист (довга назва) | **294.7** | 338.7 | нема |
| Волиньобленерго (коротка назва) | **249.2** | 293.2 | нема |

🔑 **Тобто Вова описав механіку ДОСЛІВНО правильно:** коротка назва → кнопка
лівіше (249), довша → правіше (294.7). Мій «flex:1 усе притискає» — хибний.

**Корінь знайдено:** `.hm-ct-row` (доданий учора в PR #1134 заради другого
номера) **не має `flex-grow`**. Усередині `.hm-ct` він `flex: 0 1 auto`, тобто
стискається **по вмісту**, а не на всю картку — і кнопки їдуть за текстом.
Раніше `.hm-ct-main` був прямим нащадком `.hm-ct` і його `flex: 1 1 auto`
працював. Три картки з адресою виглядають рівно **випадково**: їхня адреса
переносить рядок, вміст упирається в максимум, і край збігається з краєм картки.
➡️ **Це регрес учорашнього PR**, а не давня вада.

**Друга половина (2):** навіть із `flex: 1` картка без адреси лишить copy у
СТОВПЦІ ПІНА — бо `.hm-ct-acts` має лише одну кнопку і притисне її праворуч.
Треба зарезервувати слот піна (`min-width: 88px` + `justify-content: flex-start`).

**(3) Іконки.** Замір: значок **cy = cy тексту, різниця 0.0px на ВСІХ картках** —
відносно тексту вони вже центровані. Але на ЦНАПі значок і обидві кнопки стоять
**−16.0px від центру КАРТКИ**, бо центруються всередині `.hm-ct-row` (h=101.9),
а картка вища (h=135.9) через рядок другого номера.
🔑 **Тобто (3) — не окрема вада, а наслідок (1):** щойно другий номер стане
частиною тієї самої текстової колонки, центр збіжиться сам.

### 🔀 РОЗВИЛКА, ЯКУ МАЄ ВИРІШИТИ ВОВА

Другий номер — окреме `<a href="tel:">`. Вкласти його в `.hm-ct-main` (теж `<a>`)
**не можна**: посилання в посиланні браузер «лікує» сам, і номер може не
набратись. Тому щоб номер став під номером, велике посилання мусить перестати
обгортати всю картку — а з ним іде поведінка «тап будь-де по картці = дзвінок на
перший номер», яку Вова перевірив пальцем 01.09 (HOT_RULES №9: перевірене не
ламаємо без прямого дозволу).

Запропоновано: **stretched link** — перший номер лишається посиланням, але
розтягує невидиму ціль на всю картку через `::after { inset: 0 }`, а другий
номер і кнопки стають над ним. Тап по картці лишається дзвінком, розкладка не
рухається (важливо за HOT_RULES №10 — нічого не зникає з-під пальця).

**Коду не чіпав — чекаю «Роби».**

## 8. ✅ ТЕЛЕФОНИ ГРОМАДИ — ТРИ ПРЕТЕНЗІЇ ЗАКРИТО («Роби» дано)

### Що зроблено

**Картка перебудована в ОДИН рядок.** Обгортку `.hm-ct-row` (мою ж, з PR #1134)
прибрано — вона й була коренем (2). Текстова колонка тепер містить усе:
назва → перший номер → другий номер → адреса.

**Рішення: stretched link (розтягнуте посилання).** Другий номер мусив стати
всередину текстової колонки, а посилання в посиланні неприпустиме. Тому велике
посилання більше не обгортає картку: перший номер лишається `<a tel:>`, але
розтягує невидиму ціль на всю картку через `::after { inset: 0 }`. Поведінка
«тап будь-де = дзвінок на перший номер», яку Вова перевірив пальцем, збережена.
Другий номер і кнопки — над накладкою (`z-index: 1`), інакше тап по «копіювати»
набирав би номер.

### 📐 ЗАМІРИ ДО / ПІСЛЯ (живі вузли, 390pt)

| що | до | після |
|---|---|---|
| порядок ЦНАПу | name → phone → **адреса** → phone2 | name → phone → **phone2** → адреса ✅ |
| x «копіювати» | **267 / 294.7 / 249.2** (три різні) | **299 на всіх пʼятьох** ✅ |
| x піна | 311 | **255 скрізь, де є** ✅ |
| значок vs центр картки | 0.0 скрізь, **−16.0 на ЦНАПі** | **0.0 на всіх** ✅ |
| висота високої картки | 103.9 · ЦНАП 135.9 | 103.8 · ЦНАП 141.8 |
| висота короткої | 56 | 62 |

### 🔀 ДВА РІШЕННЯ, ДЕ ПЕРШИЙ ВАРІАНТ БУВ ГІРШИЙ — І ЗАМІР ЦЕ ПОКАЗАВ

**1. Резерв слота піна → замінено на порядок кнопок.** Спершу я тримав місце
піна через `min-width: 88px`. Стовпці вийшли рівні, але замір показав ціну:
колонка тексту на картках БЕЗ адреси звузилась 189.7 → 164px, назва «Соцзахист
(за записом)» переносилась у два рядки, картка росла 56 → **71.9px**.
✅ Замінено на «пін ПЕРЕД копіюванням + притискання праворуч»: ті самі рівні
стовпці, але там, де піна немає, ці 44px дістаються тексту. Картка 62px.
🔑 Вимога Вови виконується двома способами — взяв дешевший.

**2. 🛑 СТОРОЖ УПІЙМАВ МЕНЕ НА ПІДГАНЯННІ.** Центрування замість `stretch`
вимагає від кнопки власної висоти, і я знизив ціль другого номера 36 → 30px,
щоб картка не росла. Прогін: «❌ ціль пальця другого ≥ 34px — 30px».
Спокуса була знизити планку в стороже. **Так робити не можна:** 36 куплене
міркуванням (перший замір торік дав 22px, палець промахувався), а не зручністю
верстки. Повернув 36 — картка ЦНАПу +6px, і це правильна ціна.
📐 Заразом падінг картки 10 → 8px: без цього коротка картка виросла б 56 → 66,
а так 62, і ВИСОКА лишилась 103.8 проти 103.9 (тобто без змін).

### 🛡 СТОРОЖ: 17 → 23 перевірки

Додано три (по одній на претензію) + один контроль «пін і копіювати — РІЗНІ
стовпці» (щоб «рівно» не зазеленіло, коли кнопки просто злиплись).

🔴 **Контрольний прогін на коді з вадою (`origin/main`) — 20/23**, і сторож
назвав рівно ті числа, які я міряв окремо: `x = 267 / 295 / 249` і
`найбільший зсув 16.0px`.

🛑 **Найважливіше про сам стенд.** Сімнадцять зелених перевірок цього ж стенда
НЕ побачили регресу, який я вніс добою раніше — жодна не питала, ДЕ САМЕ стоїть
кнопка. І вада ховалась за збігом: на трьох картках із адресою кнопки стояли
рівно **випадково** (довга адреса переносить рядок, вміст упирається в максимум).
➡️ Тому нові перевірки дивляться на **ВСІ картки разом**: на одній картці ця
вада невидима в принципі.

**Прогони:** `home-contacts` 23/23 · `home` 80/80 · `home-board-widget` 14/14 ·
`caps-rank-chain` 11/11 · `dead-css` 8/8 · `css-alive` 3/3 · `clamp` 17/17 ·
`news-badge-geo` 10/10. `node build.js` ✅. `CACHE_NAME` → `cstl-20260901-1429`.

## 9. 🔴 ПОВНИЙ ПРОГІН — ТРИ ЧЕРВОНІ СТОРОЖІ, ЖОДЕН НЕ ВІД СЬОГОДНІШНЬОЇ ПРАВКИ

118 стендів (прогін повільний: проксі середовища ріже зовнішні картинки).

| стенд | що каже | чиє |
|---|---|---|
| `ai-budget` **20/21** | «стрічка не може вийти за свою кишеню» | відоме, вже в пульсі |
| `feed-auth-race` **41/42** | «`renderBusBlock` — функцію не знайдено» | 🆕 залишок #1130 |
| `news-seen` **15/16** | «бейдж пише число і відмінок разом — `«5 у Громаді»`» | 🆕 залишок #1132 |

🔴 **Обидва нові звірені на `origin/main` — падають ТАМ САМО**, тобто не від
сьогоднішньої перебудови картки.

🔑 **І обидва одного класу — того самого, який я весь день розгрібав у
документації.** Учора ми свідомо прибрали віджет автобусів (#1130) і свідомо
змінили текст бейджа на «N у Громаді» (#1132) — а перевірки, які стерегли старий
код, лишили як були. Тобто сторож перевіряє те, чого в застосунку вже нема,
і червонить назавжди.
➡️ **Правило те саме:** міняєш поведінку — гаси її сторожа тим самим заходом.
Лагодиться рядком у кожному, але це зміна ТЕСТІВ, а не верстки — чекаю «Роби».

**Решта 115 стендів зелені**, серед них усі, яких торкнулась правка:
`home-contacts` 23/23 · `home` 80/80 · `home-board-widget` 14/14 ·
`caps-rank-chain` 11/11 · `dead-css` 8/8 · `css-alive` 3/3 · `docs-fresh` 23/23 ·
`drift` 24/24 · `dev-lock` 44/44 · `sidebar-menu` 49/49.

## 10. 🛡 ДВА СТОРОЖІ ПОЛАГОДЖЕНО («Лагодити» від Вови)

🔑 **Головне рішення: жодного не видалив.** Обидва стерегли реальні вимоги Вови,
які нікуди не поділись — вони **ПЕРЕЇХАЛИ** разом із кодом. Видалити перевірку
означало б тихо зняти захист із живої вимоги; послабити регулярку — те саме.

### `feed-auth-race` 41/42 → **44/44**

Стерегло: «віджет автобусів чекає „хто я“ до читання відстежуваних рейсів».
Віджет прибрано (#1130) — але сам інваріант живий: рейси тепер читає
`getSavedRoutesForUI()` у `home-caps.js` (3 звернення), усередині ролі
`nowCapsule()`, яку `renderHomeCaps()` кличе в `Promise.allSettled` **після**
`await authReady()`.
➡️ Перевірку переписано під нове місце (2 перевірки замість 1) + **контроль**:
`renderBusBlock` справді немає в `src/`. Без нього хтось міг би повернути віджет
уже БЕЗ гарантії, і жодна перевірка не заперечила б.

### `news-seen` 15/16 → **18/18**

Стерегло: 🗣️ «хай воно закінчення підбирає — одна нова, дві нових».
#1132 змінив видимий текст на «20 у Громаді» (повна форма дає 119px і ламає
шапку 390px на два рядки: 323px при доступних 322; коротка — 83px). Відмінок при
цьому не втрачено — він у `aria-label` («5 нових у розділі Громада»).
➡️ Перевірка відмінка переїхала в `aria`, як прямо наказує **HOT_RULES №12**
(«узгодження форми слова перевіряй в `aria`, а не в мітці»). Додано **контроль**:
число в `aria` мусить збігатися з видимим — інакше обидві перевірки зеленіли б і
тоді, коли підпис для читача екрана відстав.

### 🧹 Дорогою — шапка `community-blocks.js` брехала у ДВІ сторони

Обіцяла `renderBusBlock` (прибрано) і `renderContactsBlock` (живе в
`home-contacts.js`), а `renderCommunityNews` не згадувала взагалі. Звірено з
`grep "^export .*function render"`. Шапка файлу — перше, що читають.

**Прогони:** `feed-auth-race` 44/44 · `news-seen` 18/18 · `home-contacts` 23/23 ·
`home` 80/80 · `caps-rank-chain` 11/11 · `drift` 24/24 · `dead-css` 8/8.
`CACHE_NAME` → `cstl-20260901-1543`.

🔑 **Урок дня, тепер уже втретє за сесію:** правило (чи перевірка), що живе у
двох місцях, полагоджене в одному — не полагоджене. Уранці це був контракт
нової сесії, вдень — карта файлів, увечері — два сторожі.
<<<<<<< HEAD

---

## 11. 🚀 ФІНІШ

- `CACHE_NAME` → **`cstl-20260901-1555`** (звірено в `main` після мерджу).
- Синтаксис усіх 8 змінених `.js`/`.mjs` — ✅ через `check-syntax.mjs`
  (не `node --check`: він на `export`-файлах виходить з 0 і не перевіряє нічого).
- Літопис `SESSION_STATE_VOVA.md` — дописано розділ «01.09 (сесія «c», вечір)».
- `git merge origin/main` **ДО** PR — без конфліктів, приїхали лише авто-дані
  парсерів. Тести перегнано **ПІСЛЯ** злиття (правило №13): `home-contacts` 23/23 ·
  `feed-auth-race` 44/44 · `news-seen` 18/18 · `docs-fresh` 23/23.
- `docs-refs` 10/10.
- **PR #1137** → squash-merge у `main` (`739ff24e`).
- Звірка вмістом (не комітами — squash дає один): `git diff origin/main HEAD`
  поза `data/` **порожній**.
- **Деплой #5367 — `completed · success`** (15:57 → 15:58:40 UTC).
- ⚠️ **Живий сайт із цього середовища перевірити НЕ ВДАЛОСЬ:** проксі середовища
  ріже `castlelife.org` (**403 Forbidden**, `connect_rejected`). Це обмеження
  оточення, а не деплою — статус узято з GitHub Actions, і `CACHE_NAME` звірено
  в самому `main` (`git show origin/main:sw.js`). 🔑 Тобто «зелено» тут означає
  «збірка й публікація пройшли», а не «я бачив сайт очима».
=======
>>>>>>> origin/main

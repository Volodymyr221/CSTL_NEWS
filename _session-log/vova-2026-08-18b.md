# Журнал сесії — 18.08.2026 (b)

**Гілка:** `claude/startuem-v2m8mz` · production: `main`
**Старт:** `/startuem`, друга сесія за день (перша — `vova-2026-08-18.md`, гілка `claude/startuem-f07n62`).

---

## Крок 0 — старт

- `git config user.name "Вова"` + `user.email vitocorleone191@outlook.com` — авторство комітів людське.
  «Unverified» на GitHub очікуване, ігнорую (HOT_RULES, шапка).
- `git fetch origin` — звірка двома командами:
  - `git log --oneline HEAD ^origin/main` → **порожньо** (нічого не загубилось локально);
  - `git log --oneline origin/main ^HEAD` (без `auto(`) → **порожньо** (нічого не пропустив).
  Тобто гілка стоїть рівно на `origin/main`, останній коміт людини — `8be9fb01` «docs: передача
  в новий чат — 18.08.2026 (#952)».
- `_ai-tools/BYYOU_PLAN.md` → статус `done` (не `paused`) — пересідати на іншу гілку не треба.
- Заведено цей журнал (`_session-log/vova-2026-08-18b.md`, тека рівно одна — коренева).

## Крок 1-2 — читання

Прочитано: `NEW_SESSION_PROMPT.md`, `NOW.md`, `HOT_RULES.md`, `CSTL_BUGS.md`,
`BACKLOG_VOVA_2026-08.md`, `ВОВА_ПРОФІЛЬ.md`.

**Зауваження при читанні:** авто-блок `NOW.md` знятий на 18.08 10:38 і показує гілку
`claude/startuem-f07n62` + три файли «не доїхало в `main`». Це зріз ПОПЕРЕДНЬОЇ сесії:
PR #952 їх уже змерджив, а `scripts/now_update.mjs` після мерджу не перезапускали.
Не суперечність у рішеннях — застарілий автоматичний зріз.

## Крок 3 — доповідь Вові, чекаю «Роби»

_(далі дописується по ходу сесії)_

---

## Перевірка репозиторію на ключі й персональні дані (на прохання Вови)

**Привід:** Вова хотів зробити репозиторій приватним. Порахували ціну (≈$39/міс:
Pro $4 + перевитрата Actions ~$35 при реальних ~7 400 хв/міс — цифра з рахунку
$35.68 за 18 днів, не з cron-розкладів). Домовились спершу перевірити, від чого
взагалі закриваємось.

**Обсяг скану:** уся історія — 40 105 обʼєктів git, 13 713 блобів
(10 706 текстових <400KB + 3 007 великих), плюс дерево `origin/main`.

### ✅ Ключів немає — ні зараз, ні в жодному коміті
- нема JWT, `service_role`-ключів, приватних ключів, токенів GitHub/OpenAI/AWS/Slack,
  рядків підключення postgres;
- 551 збіг слова `service_role` — тексти SQL-політик і документація, не ключі
  (перевірено контекст);
- `.env`, `.pem`, `.key`, дампів — не було ніколи;
- `sb_publishable_…` у `src/core/supabase.js:15` і `admin.html:435` — **publishable**
  ключ, за задумом публічний, захист у RLS. Не секрет;
- `db-backup.yml` шифрує дамп GPG ДО вивантаження — зроблено правильно.

### 🔴 Персональні дані — дві знахідки
1. **`tests/tools/_out/chat-1…4-*.png`** (у `main`, публічно): реальний номер
   `+380 50 888 84 88 — Володимир`, імена Dmytro Vasylchuk / Андрій Порт / Віктор /
   Катя Гордійчук, тексти приватних повідомлень. Застосунку не потрібні — знімки
   макета від `chat-contexts-shot.mjs`.
2. **`scripts/supabase_editor_invites.sql:62`** — реальна пошта Алли (вийшла 26.07).

🟡 Ще 6 реальних адрес у документації/журналах (Роми ×2, Іллі, Вови ×3).

⚠️ Видалення з дерева НЕ стирає з історії — для повного стирання потрібен
`git filter-repo` + force push (ламає посилання на коміти). Рішення відкладено.

➡️ **Висновок:** закривати репозиторій не треба — проблема це 4 картинки і один
рядок SQL. Чекаю «Роби» на прибирання.

## Другий код заслінки (Вова: «Добав ще один - devlock»)

- Спершу **звірив чинний код**: хеш `SpiderMan` збігся з тим, що лежить у
  `ALLOWED_CODE_PBKDF2` — тобто міняємо саме те, що думаємо.
- Порахував другий хеш тим самим інструментом `tests/tools/dev-code-hash.mjs`
  (він піднімає живий `core/dev-code.js`, тож «порахували» і «перевіряємо» не
  розійдуться). Сам код у репозиторій не пишу — ні в коді, ні тут.
- `src/core/dev-lock.js` — другий запис у списку, обидва дійсні одночасно.
- 🔴 **Сказано Вові прямо:** новий код слабший за перший — 7 символів проти 9,
  словниковий, і називає той самий механізм, до якого відкриває доступ. Хеш лежить
  у публічному `bundle.js`. Рішення Вови.
- `node --check` ✓ · `node build.js` ✓ (56 файлів, проблем нема) · обидва хеші в
  `bundle.js` ✓
- Стенд `tests/dev-lock.mjs`: **38/38** без коду, **41/41** з кожним із двох кодів
  (три додаткові перевірки — саме про успішний вхід). Тобто пускають обидва.
- `CACHE_NAME`: `cstl-20260818-0557` → `cstl-20260818-2016`.

## Прибирання ПД (Вова: «Роби найперше»)

Питання Вови перед стартом: «Щось зміниться чи все буде працювати як і працює?»
Відповідь: на сайті — **нічого**. `bundle.js` збирається лише з `src/`, а знімки
лежать у `tests/` — ця тека в браузер не їде. `CACHE_NAME` тому НЕ чіпав.

**Зроблено:**
1. `git rm` чотирьох знімків `tests/tools/_out/chat-1…4-*.png`. Перед видаленням
   перевірив, хто на них посилається: єдиний споживач — інструмент
   `tests/tools/chat-contexts-shot.mjs`, і він їх лише **пише**, а не читає, тож
   регенерує за потреби. Решта згадок — проза в журналах і `NOW.md`.
2. `scripts/supabase_editor_invites.sql` — реальна пошта редакторки замінена на
   `editor@example.com`, у файл додано 🛑-попередження, чому реальну адресу тут
   лишати не можна (репозиторій публічний) і що підставляти її треба в момент
   запуску скрипта.
3. `.gitignore` — додано `tests/tools/_out/` у вже наявну секцію «живі дані, репо
   публічне». Перевірено `git check-ignore`: новий знімок чату git більше не бере.
   ⚠️ Знімки Дошки, що вже лежать у `_out/` (демо-дані, ПД не містять — перевірив
   очима `board-1-top.png` і `olx-0-зараз.png`), лишаються відстеженими: `.gitignore`
   не знімає з обліку вже додані файли. Це очікуваний стан, не недоробка.

**Перевірки:** `docs-refs` 10/10 · `docs-fresh` 18/18 · `chat-screen` 10/10 ·
`chat-grouping` 18/18 · `board-fab-messages` 17/17. Тобто ані документація, ані
екран чату, ані Дошка від видалення не постраждали.

🔴 **НЕ ЗАКРИТО:** файли лишаються в ІСТОРІЇ git. Повне стирання — `git filter-repo`
+ force push, переписує всі коміти. Рішення за Вовою, окремою операцією.

---

## Переробка віджета погоди (замовлення Вови, 19.08)

**Замовлення:** віджет займає забагато вертикального простору → два стани:
компактний за замовчуванням і розгорнутий із прогнозом на 7 днів; тап по стрілці
+ свайп; анімація 250-350мс; не ламати отримання даних; не дублювати запити;
перевірити 320/375/390/430.

### Що зроблено
- `src/tabs/community-blocks.js` — `renderWeatherBlock()`: компактний рядок
  (іконка · температура · опис · мін/макс · капсула НП · стрілка) + панель
  `.hm-wx-panel` з підказкою і списком 7 днів. Нова функція `attachWeatherFold()`
  — перемикання, анімація висоти, свайп.
- `style/home.css` — компактний рядок, стрілка-перемикач, панель, список днів,
  адаптив ≤389 і ≤374.
- Дні з ГОРИЗОНТАЛЬНОГО ряду 7 колонок → ВЕРТИКАЛЬНИЙ список. Причина не смак:
  Вова просить у рядку дня 6 значень (підпис, дата, іконка, стан, макс, мін), а в
  колонці 320/7 = 45px вони не вміщаються ніяк. Список знімає і питання
  горизонтального скролу — його немає ніде і на жодній ширині.
- «Сьогодні» словом: скорочення 08.08 існувало через вузькі колонки; у списку
  колонка підпису вільна, причина зникла.

### Заміряно (прилад, потім знесений)
| ширина | згорнуто | розгорнуто | опис |
|---|---|---|---|
| 320 | 51px | 360px | обрізається (див. нижче) |
| 375 | 51px | 360px | ціле, запас 0 після правки |
| 390 | 54px | 363px | ціле |
| 430 | 54px | 363px | ціле |

Було **121px** завжди. Стало **51-54px** у згорнутому.

🔴 **320px — названа межа, а не недогляд.** Рядок 280px: іконка 22 + температура
44 + капсула 78 + стрілка 30 + проміжки → тексту лишається 82px, а найдовший опис
словника «Мінлива хмарність» це 133px. Вибір бінарний: або назва населеного
пункту, або повний опис. Лишив назву — саме через те, що вона губилась, її 05.08
виносили окремим елементом на пряме прохання Вови. Коротші описи там цілі.

### 🛑 ДВІ ВЛАСНІ ПОМИЛКИ, ОБИДВІ ПРО ПОРЯДОК CSS
1. **Осиротіла `}`.** Переносячи медіа-блок у кінець секції, зрізав його закривну
   дужку — вона лишилась на старому місці. Наслідок був НЕ там, де причина:
   впали чотири перевірки КАРТКИ ЗБОРІВ (`шар затемнення`, `фото окремим шаром`,
   `білий заголовок`, `скло`), бо браузер відкинув увесь CSS після зламаного
   місця. Спершу вирішив, що це чуже падіння, і перевірив прогоном на чистому
   дереві — там 0 помилок. Тобто зламав я.
   ➡️ Урок: після ручного переносу блоку CSS — рахувати баланс дужок скриптом.
2. **Медіа-блок програвав за порядком.** Двічі: спершу капсулі НП, потім стрілці.
   Специфічність однакова, тож вирішує позиція у файлі, а правила `.hm-wx-place` і
   `.hm-wx-toggle` описані нижче. Підступність у тому, що правило працювало
   НАПОЛОВИНУ: `gap` застосовувався (визначений раніше), а два інші рядки — ні.
   ➡️ Усі вузькоекранні перевизначення тепер стоять у КІНЦІ секції.

⚠️ **Фальшивий зелений.** Проміжний прогін показав «375px чисто» — але це було на
зламаному CSS. Після виправлення дужки 375px чесно впав із дефіцитом 8px. Зелений
на зламаному стилі не означає нічого.

### Стенди
- **НОВИЙ `tests/weather-fold.mjs`** — 38/38: компактність числом, опис на 4
  ширинах, свайп угору/вниз, короткий рух не спрацьовує, свайп ПОЗА віджетом не
  відкриває, тап по стрілці, тап по дню веде в стару модалку, і головне —
  **розгортання не робить нового запиту** (рахуються звернення до open-meteo).
- `weather.mjs` 26/26 · `home.mjs` 80/80 · `weather-day.mjs` 46/46 · `drift` 24/24.
  У кожному пояснено в коментарі, ЧОМУ критерій змінився, а не просто змінено.

### Повний прогін: 81/85 стендів — розбір кожного червоного

| стенд | причина | чиє |
|---|---|---|
| `docs-fresh` | пульс відстав від git після мого коміту | **моє** → `node scripts/now_update.mjs` |
| `docs-refs` | у `CLAUDE.md` і `START_HERE.md` стояло «84 стенди», а я додав 85-й | **моє** → виправлено на 85 |
| `sheet-close` | «рівне гальмування без ривків» — таймінговий замір | **шум навантаження**: окремо 3/3 зелений |
| `home` → «підпис капсули — роль» | очікує рівно `МОЄ/ЗАРАЗ/НОВЕ`, а капсула віддає `ЗАРАЗ · ЧЕРЕЗ 1 ГОД 49 ХВ` | **НЕ МОЄ** — доведено прогоном на чистому дереві (`git stash`): падає так само |

🔴 **ЗНАЙДЕНО ЧУЖИЙ ДЕФЕКТ — СТОРОЖ ВІДСТАВ ВІД РІШЕННЯ 17.08.** Тоді мітку капсули
навмисно зробили двоскладовою («мітка несе СТАН і КОНТЕКСТ» — записано в `NOW.md`,
приклад дослівно `ЗАРАЗ · ЧЕРЕЗ 40 ХВ`), а перевірка в `home.mjs:250` лишилась на
старій формі «тільки роль».
⚠️ **Чому його досі не помітили:** він залежить від ГОДИННИКА. Капсула автобуса
зʼявляється, лише коли найближчий рейс ближчий за 120 хв; поза цим вікном перелік
ролей порожній і `every()` проходить порожнім. Тобто стенд червоний не завжди, а
кілька годин на добу — і виглядає як «інколи блимає».
➡️ Полагодити — один рядок (звіряти ПЕРШЕ слово мітки до « · »). **Не чіпав: про це
не просили.** Чекає рішення Вови.

## Друга ітерація погоди (Вова: «Роби по своїй рекомендації і фініш відразу»)

**Скарга Вови на першу версію:** «неясно, що за день, яке число, це сьогодні,
понеділок, вівторок, як це взагалі позначається?» + питання, чи список найкращий.

**Діагноз:** день на екрані БУВ — рядок `.hm-date` («ВІВТОРОК · 19 СЕРПНЯ»), але
12px, прозорість 62%, і між ним та погодою стоять привітання й аватар. Тобто
інформація є, а звʼязку з погодою немає. Претензія справедлива.

**Зроблено три речі (мої рекомендації, які Вова затвердив):**

1. **День у компактному рядку** — `Сьогодні · 14° / 24°` замість голих чисел.
   📐 Ширини не коштує: колонку задає найдовший опис (133px), а новий рядок ≈118px.
   ⚠️ На ≤374px слово ховається — там обрізало б САМІ ЧИСЛА («Сьогодні · 14° / 2…»),
   а число тут і є зміст. Роздільник « · » усередині span-а, щоб рядок не починався
   з крапки.
2. **Смужка діапазону** замість двох чисел скраю. Шкала спільна на тиждень, тож
   смужки порівнюються між собою. `.hm-wx-bar` + `.hm-wx-bar-fill` (left/right у %).
3. **Ймовірність опадів** — нова функція `ймовірністьОпадівДня()`: максимум серед
   годин доби (не середнє: людину цікавить «чи потраплю під дощ»). Малюємо лише від
   30% — нижче це шум. Дані вже приходили в тій самій відповіді, новий запит НЕ
   додано. Іконка краплі додана в `core/icons.js` (Tabler `droplet`, той самий A).

🔑 **Обмін, названий вголос:** текстовий стан («Мінлива хмарність») із рядка дня
прибрано — він коштував ~90px і повторював іконку поруч. На ті пікселі стали дощ і
смужка. Для читача екрана стан лишився в `aria-label`, тобто зникла ДУБЛЬОВАНА
подача, а не інформація. Стенд це окремо перевіряє.

**Заміряно (320·375·390·430):** згорнуто 51-53px, розгорнуто 360-362px, смужки
17→80px залежно від ширини, горизонтального скролу немає ніде.

**Стенди:** `weather-fold` **44/44** (додано: день у компактному рядку, слово
ховається на вузьких, смужки є і РУХАЮТЬСЯ за даними — контроль на ліві межі,
крапля лише де ≥30%, стан у підписі) · `weather` 26/26 · `weather-day` 46/46 ·
`drift` 24/24 · `home` 79/80 (єдиний червоний — той самий чужий сторож капсули).

## Третя ітерація погоди (знімок Вови, 19.08 вечір)

**Дві правки, обидві його словами.**

1. **Рядки помінялись місцями.** Вова: «сьогодні і хмарно треба поміняти місцями…
   має бути хмарно, 13 slash 24 градуси, а зверху сьогодні». Було: опис зверху,
   «Сьогодні · 13°/24°» знизу — і справді нелогічно, бо «Сьогодні» стосувалось не
   своїх чисел, а всього блоку. Стало: зверху КОЛИ, знизу ЩО ТОГО ДНЯ.

   🔴 **Наслідок, який довелось вирішувати:** опис і числа тепер в ОДНОМУ рядку.
   📐 Заміряно: «Мінлива хмарність» 133px + числа 54px = ~187px, а колонці на
   телефоні дістається 135-174px. Тобто рядок не вміщається НА ЖОДНОМУ телефоні —
   питання лише в тому, що обрізати.
   ➡️ Опис отримав `flex: 0 1 auto` + трикрапку, числа `flex: 0 0 auto`. **Числа
   не ріжуться ніколи** (заміряно на 320·375·390·430, обидва описи). Слово
   впізнається з іконки поруч; обрізане «13°/2…» було б не менш зручним, а
   НЕПРАВДИВИМ про погоду.
   ⚠️ Заразом «13° / 24°» → «13°/24°» (без пробілів) — це дало 8px, після чого
   короткі описи («Хмарно», «Ясно», «Дощ») вміщаються цілими від 375px.

2. **Стрілка більше не кружечок.** Вова: «ця стрілочка… відчуття, ніби це позначає
   місце, де ти перебуваєш, локацію, бо вона якраз біля населеного пункту». Він мав
   рацію, і причина точна: кружечок стояв упритул до капсули «Луцьк» і мав ТУ САМУ
   заливку — око читало їх як один елемент про локацію.
   ➡️ Рішення його ж: заливку прибрано, стрілку відділено тонкою вертикальною
   лінією зліва (1px, 22% білого). Площа дотику лишилась 34×34 (30×30 на ≤389px) —
   зникла тільки заливка. Відгук на дотик тепер прозорістю, а не кружечком.

**Стенди:** `weather-fold` **48/48** (додано: зверху день, знизу опис+діапазон,
числа не ріжуться на жодній ширині, стрілка без заливки, лінія є, зона дотику
≥30px) · `weather` 26/26 (три перевірки переписано з «опис не обрізається» на
«числа не обрізаються» — з поясненням, чому стара вимога стала недосяжною за
побудовою) · `weather-day` 46/46 · `drift` 24/24 · `home` 79/80 (той самий чужий).

## Четверта ітерація погоди — стрілка переїхала + БАГ З ЛІНІЄЮ

**Замовлення Вови:** «перемістіть цю стрілочку на ліву сторону, там де знаходиться
скільки градусів, хмарність і сьогодні — там пише сама погода. А справа хай буде
кнопка місцезнаходження. Але так само перемістіть цю рисочку на праву сторону».
Плюс: «я натискаю на стрілочку, вона розвертається, і рисочка за нею прив'язана і
теж розвертається на інший бік, виправи це».

**1. Стрілка переїхала до погоди.** Логіка правки точна: стрілка керує ПРОГНОЗОМ,
а стояла в правому краю і читалась як частина капсули локації. Новий порядок:
`іконка · градуси · текст · СТРІЛКА │ капсула НП`. Лінія відповідно переїхала на
правий бік стрілки (`border-right`), бо відділяти треба те, що ПІСЛЯ неї.
⚠️ Довелось змінити `.hm-wx-txt` з `flex: 1 1 auto` на `0 1 auto`: поки колонка
тексту забирала весь залишок рядка, усе після неї притискалось у правий край, і
стрілка не могла стояти «при погоді». Тепер залишок збирає капсула своїм
`margin-left: auto`, а текст лишається здатним стискатись — обрізка опису не
змінилась.

**2. 🔴 БАГ, ЯКИЙ ЗНАЙШОВ ВОВА, І ЙОГО СПРАВЖНЯ ПРИЧИНА.** Лінія була **бордюром
самої кнопки**, а поворот на 180° застосовувався до кнопки цілком — тож разом зі
значком оберталась і межа, і `border-right` опинявся зліва.
➡️ Тепер крутиться ТІЛЬКИ значок усередині (`.hm-wx-toggle-ic`), кнопка з лінією
стоїть нерухомо.
🔑 Це не разова латка, а загальне правило: **якщо в елемента є і поворот, і межа,
вони мусять жити на різних вузлах.**

**Заміряно (320·390·430), обидва стани:** порядок елементів правильний, стрілка
стоїть одразу за текстом (166/200/206px від початку рядка), капсула далі
(202/266/306), лінія `зліва 0 / справа 1px` — і в згорнутому, і в розгорнутому
стані, поворот кнопки `none`, поворот значка `180°`. Зона дотику 30×30 / 34×34.

**Стенди:** `weather-fold` **51/51** — додано три перевірки, з них дві саме на цей
баг: «при розгортанні лінія НЕ перестрибує на інший бік» і «обертається значок, а
не кнопка з лінією» (друга потрібна, щоб латку не «полагодили» назад, лишивши
поворот на кнопці). `weather` 26/26 · `weather-day` 46/46 · `drift` 24/24 ·
`home` 79/80 (той самий чужий сторож капсули).

## П'ята ітерація — стрілка в САМИЙ лівий край

**Вова:** «ти зовсім не те поміняв… все треба було залишити на своїх місцях, так як
є. Тільки стрілочку розташувати самого ліва, ПЕРЕД самою іконкою погоди, перед
самим малюнком».

🛑 **Моя помилка в розумінні:** попереднього разу він сказав «на ліву сторону, там
де градуси, хмарність і сьогодні» — я прочитав це як «ліворуч від капсули, при
блоці погоди» і поставив стрілку МІЖ текстом і капсулою. Він мав на увазі буквально
лівий край рядка. Порядок тепер: `СТРІЛКА │ іконка · градуси · текст ⟶ капсула НП`.

🔑 Чому це справді краще за мої дві попередні спроби: у правому краю (і навіть
поруч із текстом) стрілка щоразу опинялась біля капсули локації й читалась як
частина керування нею. У лівому краю в неї просто немає сусіда, з яким її можна
сплутати.

Лінія лишилась праворуч від стрілки — відділяє її від погоди. Решту не чіпав.
**Стенди:** `weather-fold` 51/51 (оновлено очікуваний порядок елементів) ·
`weather` 26/26 · `weather-day` 46/46 · `drift` 24/24.

### ⚠️ ІНЦИДЕНТ ПРИ ЗЛИТТІ: git зробив ДВІ стрілки

При злитті свіжого `main` перед фінішем git **автоматично** звів
`community-blocks.js` без конфлікту — і результат був неправильний: у `main` лежала
стрілка МІЖ текстом і капсулою (PR #958), на гілці — стрілка ПЕРЕД іконкою. Git
порахував це двома незалежними додаваннями і лишив **обидві**.

🔑 Найважливіше: **конфлікту git не показав**. Тобто мовчазне «успішне» злиття дало
зламану розмітку — рівно той випадок, коли зелений `git merge` нічого не доводить.
Спіймав стенд `weather-fold` перевіркою порядку елементів
(«hm-wx-toggle · hm-wx-now · hm-wx-t · hm-wx-txt · **hm-wx-toggle** · hm-wx-place»).

➡️ Урок: після злиття гілки, де МІНЯВСЯ ПОРЯДОК елементів у розмітці, перевірка
порядку обовʼязкова — саме перестановки git зводить найгірше, бо бачить у них
«додано тут» + «видалено там», а не «перенесено».
✅ Дубль прибрано, стенди перегнано: `weather-fold` 51/51 · `weather` 26/26 ·
`weather-day` 46/46 · `drift` 24/24.

---

## Підготовка до Google Play (TWA) — код-частина

**Замовлення Вови:** Android — окремий додаток у Play, iOS лишається PWA.
Оцінку дав окремо; тут — що зроблено кодом.

**Одна його цифра виправлена:** «iOS розглядається три місяці» — ні, перевірка
днями. Справжній блокер інший: Apple системно відхиляє тонкі обгортки сайтів
(правило 4.2). Тому рішення лишити iOS на PWA правильне, але з іншої причини.

### 1. `privacy.html` — публічна політика (генерується, не дублюється)
Google Play вимагає посилання на політику, яке відкривається ЗЗОВНІ застосунку. У
нас вона жила лише в модалці — URL не існувало.
🛑 **Не завів другий файл із текстом.** Копія юридичного документа — найгірший вид
дубля: розходиться мовчки, і в застосунку одна редакція, а на публічній сторінці
інша. Тому `scripts/gen_privacy.mjs` генерує сторінку з того самого
`src/core/legal.js`, і генерація вбудована в `build.js` поряд із `check-imports`
— сторінку не можна забути перезібрати.
⚠️ Заразом прибрав шум: імпорт `legal.js` у Node друкував попередження
`MODULE_TYPELESS_PACKAGE_JSON` у кожній збірці. Погасив точково
(`--disable-warning=…`), а не `--no-warnings`: засмічений журнал ховає справжні
попередження. `"type": "module"` у package.json поставити не можна — зламається
сам `build.js` (він на `require`).

### 2. `manifest.json` — виправлено `id`
Було `/CSTL_NEWS/` — шлях старого дзеркала github.io, якого на власному домені НЕ
ІСНУЄ. Стало `/`.
🔴 **Чому саме зараз:** `id` це ПОСТІЙНА тотожність застосунку. Після виходу в Play
міняти його не можна — для системи це буде інший додаток. Зараз установок
практично нема, тому ціна зміни нульова; через місяць була б висока.

### 3. `.nojekyll`
Щоб GitHub Pages віддавав теку `.well-known` — без неї TWA не пройде перевірку
власності й запуститься з адресним рядком браузера зверху. Це найчастіша й
найнезрозуміліша поломка TWA: додаток «працює», але виглядає як сайт.
✅ Перевірено окремо: крок `Trim artifact` у `deploy.yml` теку `.well-known` НЕ
видаляє (видаляє `.github .agents .claude`).

### 4. `docs/PLAY_TWA.md` — покрокова інструкція
Bubblewrap, ключ підпису, assetlinks, картка в Play, анкети.
🔴 **Головне попередження в ній — не про код:** нові особисті акаунти Play мають
провести закрите тестування (≈12 тестувальників × 14 днів поспіль) ДО продакшну.
Це календар, а не робота, і його не можна прискорити. Плюс: перед тестуванням
треба зняти `DEV_LOCK`, інакше два тижні люди дивитимуться на замок.
⚠️ Правила Play міняються — в інструкції прямо сказано перевірити цифри в Console.

### 5. `tests/pwa-store.mjs` — новий сторож, **17/17**
Маніфест (id, display, іконки, maskable, файли існують), політика (є, не порожня,
**редакція збігається з `legal.js`** — тобто копія тексту не завелась), `.nojekyll`,
і assetlinks: поки файлу нема — це очікувано, а щойно зʼявиться — відбиток мусить
бути справжнім, а не заготовкою з інструкції.
🔑 Усе, що тут перевіряється, ламається ТИХО: жодна з цих поломок не дає ні
помилки в консолі, ні падіння — лише відмову в Play через тиждень очікування.

**Стендів у проєкті: 85 → 86** (оновлено `CLAUDE.md` і `START_HERE.md`).

---

## Свайп назад «звідки завгодно» (Вова: «палець треба ставити аж на самий край»)

**Діагноз спершу, бо він міняє суть задачі.** Цей жест **не наш** — його обслуговує
iOS, а ширину зони (≈20pt) задає Apple; з вебу вона не міняється. Тобто «трохи
збільшити діапазон» неможливо в принципі. Питання стояло інакше: заводити ВЛАСНИЙ
жест чи ні.

🛑 **А власний тут уже двічі ламали:** 24.07 (скріни IMG_3557/IMG_3559) і 02.08
(IMG_3816) — щоразу тим, що він жив у ТІЙ САМІЙ смузі, що системний: один рух
пальця робив дві дії, а iOS ще й малювала свою анімацію поверх нашої.
🔑 **Помилка була не в ідеї, а в накладанні.** Тому тепер зона починається ПІСЛЯ
системної: 0-28px лишаються iOS, від 28px — наші.

**Вимога Вови «щоб плавність була така як зараз є»** визначила спосіб: не
спрацювання по відпусканню, а **інтерактивне перетягування** — екран іде за
пальцем. Рішення і доїзд рахує `finishSwipe` з `core/sheet-motion.js` — той самий
модуль і та сама крива, що в усіх аркушів застосунку. Другої фізики не заводив.

**Зроблено:** `attachSwipeBack()` у `core/layers.js` + необовʼязковий `opts.el` в
`openLayer`. Шар без `el` поводиться рівно як раніше.
Підключено 7 екранів: чат/Повідомлення, Стрічка, Питання, Збір, Хаб новин,
модалка оголошення Дошки, Кабінет. **НЕ підключено** кропер фото і лайтбокс — там
горизонтальний рух належить самому фото.

**Чотири умови, щоб не зачепити скрол:**
1. старт далі за 28px (системна смуга);
2. `|dx| > |dy| × 1.5` і тільки вправо — діагональ лишається прокруткою;
3. рішення про напрям — після 10px руху, не раніше;
4. мовчимо там, де горизонталь уже щось означає: `data-swipe-own` (список Хабу
   новин — там свайп перемикає категорії) і бічні скролери, яким ще є куди їхати
   (`scrollLeft > 0`).

🔑 **Бульбашки чату позначати НЕ довелось:** там свайп тільки ВЛІВО (відповідь), а
наш тільки ВПРАВО — конфлікту немає за напрямом. Перевірив читанням коду
(`chat-core.js`: `Math.max(Math.min(dx, 0), -64)`), а не припущенням.

⚠️ Закриття йде через `closeLayer` ПІСЛЯ доїзду анімації: викликаний одразу, він
прибрав би екран з DOM і обірвав рух на льоту.

**Стенд `tests/swipe-back.mjs` — 10/10.** Піднімає живий `core/layers.js` на
порожній сторінці (міряє жест, а не збіг обставин екрана). Перевіряє насамперед
**де жест мовчить**: системна смуга, вертикаль, діагональ, рух уліво, короткий
рух, `data-swipe-own`, скролер із `scrollLeft > 0`. Плюс що після закриття історія
не лишила порожнього запису — саме через це 24.07 виїжджала Дошка.

⚠️ **Чого стенд НЕ доводить і що сказано Вові прямо:** Chromium не відтворює ні
системний жест iOS, ні його анімацію. «Чи не сваряться два жести на живому
iPhone» — перевіряє тільки палець.

Решта стендів після зміни: `home` 79/80 (той самий чужий) · `board-rules` 29/29 ·
`chat-screen` 10/10 · `chat-grouping` 18/18 · `board-fab-messages` 17/17 ·
`news-widget` 67/67. Стендів у проєкті: 86 → 87.

### Межа зони свайпу — ліва половина (Вова, після деплою)

**Вова:** «Можна зробити так щоб з лівої частини до половини був екран з закриттям
свайпом, даже з 40%».

⚠️ **Важливе уточнення, яке сказав йому прямо:** у задеплоєній версії жест уже
працює на ВСІЙ ширині, тобто його прохання — це не розширення, а **звуження**. Але
звуження виправдане, і я його зробив: на правому краю живе те, чим користуються
великим пальцем — кнопки, каруселі, галерея фото в оголошенні. Ліва половина
закриває, права не закриває ніколи — поведінка передбачувана.

`ZONE = 0.5` одним числом у `core/layers.js`. Повернути всю ширину — поставити 1.
🔴 **Названий ризик:** якщо на живому телефоні почне здаватись «іноді працює,
іноді ні» — причина буде саме в межі, і тоді повертаємо 1.

Стенд доповнено двома перевірками (старт у лівій половині закриває, у правій — ні),
а решту сценаріїв «не закриває» перенесено стартом У ЗОНУ: інакше вони проходили б
через межу, а не через ту причину, яку перевіряють. **12/12.**

---

## ШІ-агент спільноти OLYKA CASTLE (20.08)

**Замовлення:** агент, що пише пости в стрічку від імені офіційної спільноти
бренду — і звичайні, і про нові функції застосунку з поясненням, як користуватись.

**Рішення Вови:** 1) чернетка, не автопублікація · 2) теми і копірайтинг пишемо
разом, не тільки з історії змін · 3) 3-4 пости на тиждень · 4) Opus 5.

### Що знайшов у базі перед роботою (а не припустив)
Сторінка вже існує: `pages.id = 3`, «OLYKA CASTLE», «Офіційна спільнота
медіабренду», власник Володимир, **1 пост**. У всій Стрічці **17 постів**, з них
видимих 10 — тобто стрічка справді стоїть, і агент має сенс.

### Чернетки: захист подвійний і головний — у БАЗІ
`scripts/supabase_page_post_drafts.sql` (**накатано**): колонка `status`
(`draft|published`, за замовчуванням `published`) + політика читання
`(deleted_at is null AND status='published') OR can_edit_page(page_id)`.
🔑 Зразок узято не з голови — це рівно та сама форма, що вже діяла для мʼяко
видалених постів. Тобто чернетку ховає БАЗА, а не фільтр у клієнті: навіть
помилка в застосунку не віддасть читачеві невичитаний текст бренду.
✅ **Перевірено наслідок, а не факт виконання:** до міграції видимих постів 10 →
після 10. Нічого не сховалось.
⚠️ Фільтр у клієнті теж додано (`fetchPagePosts`) — як друга лінія. У ЗАПАСНОМУ
запиті його свідомо немає: там він перетворив би «нема колонки» на «порожня
Стрічка».

### Агент — у наявний конвеєр `editor/`, без другого двигуна
- `editor/sources/plan.py` — теми з `editor/plans/olyka_castle.json` (спільний
  файл Вови і Claude). Памʼять окремо (`data/olyka_agent_state.json`): план — це
  намір людини, памʼять — факт агента; два автори в одному файлі розходяться
  завжди (06.08 це вже коштувало розрізаної історії журналів).
- `editor/writers/brand_writer.py` — **Opus 5**, голос із файлу
  `editor/voice/olyka_castle.md`, `effort: medium`. Заборона вигадувати факти
  стоїть ДВІЧІ (у голосі і в промпті) — тут ціна помилки найвища.
- `editor/sinks/page_draft.py` — запис `status='draft'` через service_role.
  Памʼять позначається ЛИШЕ після підтвердженого запису.
- `editor/images/none.py` — 🔴 окремий плагін, бо конвеєр за замовчуванням тягне
  `wikimedia`: без цього під пост бренду поїхало б ВИПАДКОВЕ чуже фото.
- Місія + робота за розкладом (пн/ср/пт/нд 10:10 Києва, 1 пост за прогін).

### Шлях публікації (без нього агент марний)
`fetchPageDrafts` + `publishPagePost` у клієнті; у стрічці чернетка має пунктирну
рамку, позначку «ЧЕРНЕТКА · її бачиш лише ти» і кнопку «Опублікувати».
⚠️ Публікація оновлює `created_at`: інакше текст, що пролежав чернеткою три дні,
вийшов би одразу похованим під свіжими постами.

### 🛑 Пастка, яку ледь не проґавив: лічильник грошей
`editor/core/spend.py` мав ЗАШИТІ ціни Sonnet і жорсткий рядок моделі. З Opus 5
(вдвічі дорожчий) він **мовчки занижував би витрати вдвічі** — а це саме те
число, за яким Вова вирішує, чи агент по кишені. Тепер ціни за моделлю, невідома
модель рахується за Sonnet із гучним попередженням, а не зникає з обліку.

**Стенд `tests/olyka-agent.mjs` — 30/30.** Стереже обидві лінії захисту чернеток,
відсутність фактів у плані, заборону вигадувати, ціну Opus, вимкнене фото,
розклад 3-4 на тиждень. Перша версія однієї перевірки натрапляла на рядок
ІМПОРТУ замість виклику і падала на правильному коді — виправлено.
Решта: `home` 80/80 · `feed-composer` 6/6 · `feed-edge` 6/6 · `press-feedback` 8/8.
Стендів: 87 → 88.

🔴 **Чого бракує для першого поста:** секрет `ANTHROPIC_API_KEY` у репозиторії
(ключа в середовищі немає, тому живий зразок тексту згенерувати не зміг).

### Уточнення Вови: «перегляд і публікація — і для команди редакторів»

**Відповідь: будувати нічого не треба, механізм уже є — і навіть з інтерфейсом.**

У проєкті це називається **«Команда сторінки»**, ролі — **Власник** і
**Модератор** (таблиця `page_admins`). Додаються за поштою прямо з екрана
сторінки (`addPageModerator` → RPC `add_page_moderator`).

🔑 Чому модератор отримав права на чернетки автоматично: і політика бази
(`can_edit_page`), і клієнт (`myPageIds` ← `page_admins`) питають ОДНЕ І ТЕ САМЕ.
Тобто окремого «тегу редактора» заводити не треба — він був би третьою відповіддю
на те саме питання, а розсинхрон прав у цьому проєкті вже траплявся (24.07:
композер показувався глобальному адміну там, де вставка поста падала помилкою).

⚠️ **Про слово «редактор»:** воно в проєкті ЗАЙНЯТЕ іншим — це кабінет редактора
новин (`editor_invites` / `editor_users`), там пишуть статті в CMS. Для сторінки
правильне слово — **модератор**.

Зроблено одне уточнення: кнопку «Опублікувати» прив'язано до того самого
прапорця `canEditPost`, що й меню «⋯». База й так не віддасть чужу чернетку, але
кнопка, яка впала б помилкою прав, гірша за її відсутність.
Стенд доповнено двома перевірками — **32/32**.

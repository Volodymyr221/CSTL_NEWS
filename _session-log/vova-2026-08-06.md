# Журнал сесії — 2026-08-06 (вечір), гілка `claude/startuem-vqark2`

> Живий журнал. Дописується ПО ХОДУ після кожної значущої дії, не з памʼяті наприкінці.
> Формат рядка: що зробив (файл/функція) · навіщо · що НЕ спрацювало · слова Вови дослівно · PR# · деплой# · CACHE_NAME.

---

## Крок 0 — старт сесії (`/startuem`)

**19:30 UTC.** Підписав коміти Вовою (`git config user.name "Вова"` + `vitocorleone191@outlook.com`).
Гілка сесії — `claude/startuem-vqark2`, вона рівно на `origin/main` (звірка двома
командами: `HEAD ^origin/main` порожньо, `origin/main ^HEAD` порожньо). Нічого
незапушеного немає.

**Прочитано:** `HOT_RULES.md` · `START_HERE.md` · `_ai-tools/SESSION_STATE_VOVA.md`
(хендовер U-12) · `_ai-tools/AUDIT_2026-07.md` (заголовки) · `CSTL_BUGS.md` ·
`CLAUDE.md` (кореневий + `CSTL NEWS VOVA/`) · `_ai-tools/BACKLOG_VOVA_2026-08.md` ·
`_ai-tools/BYYOU_PLAN.md` (статус `done`, не `paused` — пересідати на іншу гілку не треба).

**Стан коду:** `CACHE_NAME = cstl-20260806-1556`, стенди 44/756, `DEV_LOCK = true`,
відкритих 🔴 багів немає.

---

## Перевірка деплою (перше, що просив хендовер)

Перевірив прогони `deploy.yml` через GitHub API:

| прогін | час (UTC) | результат |
|---|---|---|
| **#3838** | 15:41 | ✅ **success** — останній вдалий, це те, що зараз на сайті |
| #3839 | 15:46 | failure |
| #3840 | 15:53 | cancelled |
| #3841…#3846 | 15:57–17:58 | failure |
| **#3847** | 18:52 | **cancelled** |

Розібрав саме #3847 (id `31126460171`): завдання `build` створено 18:52:17,
завершено 19:08:35 — **16 хвилин**, `runner_name` порожній, `runner_id = 0`,
тобто **виконавця так і не призначили, кроків виконано нуль**; завдання `deploy`
пропущено. Це рівно той симптом, що описав хендовер → **аварія GitHub Actions
триває**, це не наш код.

➡️ Висновок за інструкцією хендовера: **retrigger-и не спамити** (вони лише
додають завдань у перевантажену чергу). Сайт живий на `castlelife.org` із вмістом
від 15:41.

⚠️ Прогону на коміт `a72745b1` (PR #823, хендовер) у списку API ще немає —
`deploy.yml` не має `paths-ignore`, тобто мав би запуститись. Але «API показує
пізніше, ніж я дивлюсь» — один із чотирьох хибних діагнозів сьогоднішнього дня,
тому висновків не роблю, просто фіксую.

---

## Далі

Чекаю «Роби» від Вови. До цього — тільки читання й обговорення.

---

## Потік `/byyou` — АУДИТ ДОКУМЕНТАЦІЇ (14 кроків)

**Замовлення Вови дослівно:** *«проведи детальний аудит документації. Там, можливо,
є щось несправне, якісь розбіжне — будь ласка, виправ все, налаштуй документацію
повністю ідеально»*. Рішення на брамі старту: **CLAUDE.md — тільки факти, правила не
чіпати**; `_session-log` звести **в кореневу** теку.

### Метод
Не читав очима — написав скрипт, який витягує з документації **всі шляхи до файлів** у
зворотних лапках і звіряє з диском. Карти файлів і числа звіряв командами `ls`/`wc`.

### Знайдено (реєстр — `_ai-tools/AUDIT_DOCS_2026-08.md`)
- **65** битих посилань, з них ~20 у навігаційному шарі
- **5** прямих суперечностей між документами
- **4** неправдиві факти в головних файлах
- **4** розділи застарілих карт + один відсутній
- **1** структурний дубль (дві теки журналів, 43 файли)

### Кроки і коміти
| # | крок | коміт |
|---|---|---|
| 1 | реєстр знахідок | `79f19e2c` |
| 2 | `CSTL NEWS VOVA/CLAUDE.md` — факти | `a99e019d` |
| 3 | кореневий `CLAUDE.md` | `a48316c4` |
| 4 | карта файлів (core 10→31, workflows 3→9, data 7→13, hooks 4→7, +commands) | `b0c14855` |
| 5 | обовʼязкове читання + дати + таб-бар `shotam` | `7ee45c91` |
| 6 | `HOT_RULES.md` — неіснуючі сторожі | `75d83591` |
| 7 | домен `castlelife.org` у 7 доках | `a527513d` |
| 8 | `CSTL_BUGS.md` — B-14, B-28, B-20 | `fbc32f02` |
| 9 | `docs/` — CONTENT_STRATEGY, ARCHITECTURE, ROLES_SURVEY | `56b1dbc4` |
| 10 | дві теки журналів → одна | `70119eeb` |
| 11 | беклог — fundraisers | `f16d9008` |
| 12 | три списки читання зведено | `5fec286f` |
| 13 | **сторож `tests/docs-refs.mjs`** | `1b40f882` |

### 🔴 Найважливіше, що знайшлось
`HOT_RULES.md` (читається ПЕРШИМ) обіцяв сторожі `kb-guard.test.js` і
`kb-tapinput.test.js` для клавіатури на iOS. **Обох ніколи не було в git**, і це прямим
текстом написано в `CLAUDE.md` — тобто два головні документи суперечили, і брехав той,
який читається першим. Небезпека не в неточності, а в тому, що обіцянка сторожа
**знімає обережність** у зоні, де два фікси вже провалились (PR #638, #641).

### ⚠️ Власна хибна перевірка (одинадцятий випадок у проєкті)
Перший контроль нового сторожа я зробив кирилицею в імені тестового файлу
(`src/core/неіснуючий-модуль.js`) — сторож сказав «✅ 10/10» і виглядав дірявим.
Діра була в КОНТРОЛІ: шаблон описує латиничні імена. Повторив з
`src/core/kb-guard-v2.js` — упав як мусив. Урок записаний у шапку самого стенда.

### Числа після потоку
`npm test` → **45 стендів / 767 перевірок, усе зелено**. До потоку було 44/756, і саме
ці числа розходились між `CLAUDE.md` (там стояло 36/605) і `START_HERE.md`.

### Не робив свідомо
- Не заводив 12-те правило в `HOT_RULES.md` — правила створює тільки Вова.
- Не чіпав літопис: `SESSION_STATE_VOVA.md` нижче хендовера, `_archive/`, журнали.
- Не чіпав секцію «ВСІ ПРАВИЛА» в `CLAUDE.md` — рішення Вови на брамі старту.

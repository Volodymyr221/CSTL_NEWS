# Журнал сесії — 26.07.2026 (гілка `claude/startuem-nteyl6`)

> Живий журнал: дописую ОДРАЗУ після кожної значущої дії. При `/finish` звіт для brain — звідси, не з памʼяті.

---

## Старт сесії

- **Крок 0 виконано.** `git config user.name "Вова"` + `vitocorleone191@outlook.com`.
  «Unverified» на GitHub — очікувано, ігнорую (правило HOT_RULES).
- **Гілка:** `claude/startuem-nteyl6` (авто-гілка сесії). Робочий стан чистий.
- **Свіжий стан:** `git fetch origin` + `git pull origin main` → `Already up to date`.
  Останній коміт `main`: `09713146 auto(news): оновлення новин та подій 26.07 11:43 UTC`.
  Останній людський: `90a28f9e Документація: повний хендовер у новий чат (стан 26.07) (#643)`.
- **`/byyou`:** `BYYOU_PLAN.md` статус `done` (потік мережевих збоїв завершено 26.07) — нічого не підхоплюю.
- **Прочитано:** HOT_RULES · START_HERE · SESSION_STATE_VOVA (блок хендовера) · AUDIT_2026-07 ·
  CSTL_BUGS · CLAUDE.md · ВОВА_ПРОФІЛЬ.

## Стан на вході

- Прод: деплой **run 3377**, `CACHE_NAME = cstl-20260726-1125`.
- Відкритих 🔴 багів нема. Єдина відкрита тема — **B-25** (верх смикається при ПІДЙОМІ клавіатури,
  лист коментарів «Стрічки», встановлений PWA на iPhone). Дві спроби провалились (PR #638, #641),
  обидві відкочені.
- Знайдена суперечність у документах (винесу Вові в першій відповіді): у `CSTL_BUGS.md` B-25 стоїть
  як 🟡 з поміткою «Вова знає і погодився поки лишити», а в хендовері `SESSION_STATE_VOVA.md` — як
  «🔴 задача, яку Вова хоче спробувати в новому чаті».

---

## «Роби» на B-25 — спроба 3 (клавіатура, верх при підйомі)

**Вова дослівно:** «роби, ми не маєм поламати те що вже працює, просто маємо покращити
щоб верхня частина не дьоргалась, ти вже робив так раз сьогодні, було ідеально, потім
знову поламалось. Не роби латку, роби комплексне рішення так як розробники інстаграм/фейсбук».

### Що прочитав перед правкою
`src/core/keyboard.js` (315р) повністю · `feed.js` місця виклику (975-1075, 1250-1276) ·
`chat-core.js:127-195` (`setupKeyboardResize`) · `style/feed.css` 425-490.

### Зміна 1 — розділив дві ролі, що жили в одній змінній `shift`
Було: `shift = max(0, vv.offsetTop, window.scrollY)` — і цим самим числом модуль І
**виявляв** клавіатуру, І **позиціював** оверлей.
Проблема: `window.scrollY` — стан, який ми САМІ скасовуємо (`onPageScroll` повертає
сторінку назад). Позиціювати за ним = компенсувати зсув, якого через кадр уже нема.
Стало: `viewShift` (=`vv.offsetTop`, реальний зсув видимої області) — для позиціювання;
`pageShift` (=`scrollY`) — тільки для детекту. `kb = max(shrink, viewShift, pageShift)`.

### Зміна 2 — синхронне застосування замість `requestAnimationFrame`
Було: `schedule() → requestAnimationFrame(apply)`. На кожну подію видимої області
перерахунок відкладався на наступний кадр. Поява клавіатури — це анімація, подій
десятки; кожна давала аркушу шанс опинитись на кадр позаду фону.
Стало: `apply()` синхронно в обробнику. Не прогноз (HOT_RULES №10 не порушено) —
ті самі числа, просто вчасно.

### Зміна 3 — ПІК-діагностика в панелі
Миттєві числа на скріні показують КІНЦЕВИЙ стан, а він і так ідеальний (`top=157=top0`).
Дефект живе кілька кадрів → на статичному скріні його не видно взагалі.
Додав рядок `ПІК за підйом: верх=… зсув=… стор=…` — найгірше значення за поточний
підйом клавіатури, скидається при закритті.

### Перевірка
- `node --check src/core/keyboard.js` ✓ · `node build.js` ✓ (39 файлів clean).
- Стенд `scratchpad/kb/run.mjs` — A/B на СПРАВЖНЬОМУ модулі (esbuild) + СПРАВЖНЬОМУ CSS
  (`base.css`+`feed.css`), розмітка 1-в-1 з `feed.js:929`, Chromium середовища.
  8 кадрів підйому 873→460 (числа з айфона Вови), фейковий `visualViewport`.
- Результат: ✓ замок фону на місці · ✓ замок знімається · ✓ фокус не рухає розкладку
  (0px) · ✓ кінцева геометрія байт-у-байт як ДО (`top=157 h=303`) · верх стоїть у
  кожному кадрі в ОБОХ версіях.

### 🔴 ЧЕСНО ПРО МЕЖУ ЦІЄЇ ПЕРЕВІРКИ — не приписувати собі перемогу
Стенд довів, що я **нічого не зламав**, і НЕ довів, що дьоргання зникло.
Причина: у цьому модулі верх аркуша дорівнює `top0` **за побудовою** в обох станах
(і в CSS-стані 82svh, і під клавіатурою) — тобто пік 0px виходить однаково і до, і
після правки. Chromium не відтворює ні зсув webview, ні зсув visual viewport iOS.
Отже мої зміни впливають рівно на сценарій, де `offsetTop`/`scrollY` ≠ 0 (спосіб Б і
транзитний зсув перед стиском). На останньому скріні Вови кінцевий стан був
`стиск=413 зсув=0` — тобто чи є транзитний зсув, ми поки НЕ ЗНАЄМО.
**Тому Зміна 3 (ПІК) — не додаток, а наступний крок:** один скрін із живого айфона
скаже, смикається наш аркуш (`верх>0` — є що чинити) чи фон за затемненням
(`верх=0` — зона iOS, і розмова вже про задник, не про геометрію).

### Не робив і чому
- **`interactive-widget=resizes-content` у `<meta viewport>`** — ВІДКЛАВ. Перевірив
  `chat-core.js:170`: там детект клавіатури = `documentElement.clientHeight - vv.height > 80`.
  При `resizes-content` layout стискається разом з видимою областю → різниця ~0 → Чат і
  Обговорення вирішать, що клавіатури нема. На iOS це, найімовірніше, no-op, але на
  Android/Chrome — реальна поломка робочої фічі. Робити тільки разом з міграцією
  `chat-core` на `core/keyboard.js` (окремий пункт черги).
- **Компенсацію зсуву для ФОНУ** (translate на `.app-main`/`.fd-screen`) — НІ:
  transform на предку робить його системою координат для `fixed`-нащадків, і це
  покадрова компенсація, яка за уроком 25.07 сама дає дьоргання.

**CACHE:** `cstl-20260726-1125` → `cstl-20260726-1220`.

---

## Деплой спроби 3 + ЖИВІ ЧИСЛА З АЙФОНА (скрін IMG_3644) — діагноз нарешті є

PR #645 → squash → **деплой run 3382 success** (`4f69be0b`). Після мерджу звірив, що правки
доїхали в `main` (git-пастка squash): `viewShift` на місці, `schedule` синхронний, замок
`bodyStyle.position='fixed'` цілий, CACHE `1220`.

### Скрін IMG_3644 (v3508 · 15:25, ДОДАТОК standalone, клавіатура Wispr Flow)
```
клавіатура: ВІДКРИТА kb=431
спосіб: стиск=431 зсув=29 стор=0
ПІК за підйом: верх=59 зсув=143 стор=143
ФОН зсув: app-main:+0 СТОРІНКА:+0
vv: h=442 offTop=29 pageTop=29
window: inner=844 client=873
scrollY=0 h0=873 top0=157
аркуш: top=186 h=285   оверлей: top=29.3px h=442px
```
**Скарга Вови:** «Вже не тільки дьоргається, а і шапка ховається за екран, а не на своєму місці».

### Що ці числа означають (діагноз, вперше не здогадка)
1. **Дьоргається ФОН, не аркуш.** `ПІК зсув=143` — iOS транзитно прокручує webview аж на
   143px. Аркуш ми компенсуємо (`оверлей top=29.3` → аркуш `186−29=157=top0` ✓), а шапка
   `position:fixed` прибита до РОЗМІТКИ і їде разом з нею.
2. **Зсув 29px ЛИШАЄТЬСЯ в кінцевому стані** (`offTop=29`) — тому шапка обрізана згори.
   На попередньому скріні (IMG_3641) було `зсув=0` — звідси й «стало гірше».
3. **Мої дві правки на це не впливали:** `scrollY=0`, тож і стара формула
   `max(offsetTop, scrollY)`, і нова `viewShift` дали б ті самі 29. Виграшу не доведено.
4. ⚠️ **Змінна, яку я мало не проґавив:** клавіатура на скріні — стороння **Wispr Flow**
   з власною панеллю (`kb=431` замість 413). Вова підтвердив: минулого разу («ідеально»)
   тестував зі **стандартною**. Тобто сценарій не той самий.

**Рішення Вови:** «Давай спробуємо ще Б, якщо не вийде, повернемо до стану, коли я казав
"ідеально"». → роблю Б, відкат тримаю напоготові.

## Захід Б — компенсація зсуву видимої області для ФОНУ

Та сама компенсація, що вже тримає аркуш, поширена на шапку. Два шари, бо прив'язані
по-різному:
- `--kb-vshift` (CSS-змінна на `:root`, пише `keyboard.js`) → `.app-header` і `.app-header::before`.
  `fixed` не бачить позиції body, тому компенсується власним `top`.
  Було: `top: env(safe-area-inset-top, 0px)`. Стало: `top: calc(env(...) + var(--kb-vshift, 0px))`.
  **Поза клавіатурою змінної нема → `0px` → позиція байт-у-байт та сама.**
- `body.top`: замок ставив `-pageTop0`, тепер `-pageTop0 + viewShift` — зсуває `.app-main`
  (увесь контент) на ті самі пікселі, щоб контент не роз'їхався з шапкою.
- ⚠️ Свідомо **НЕ через `transform`**: transform на предку робить його системою координат
  для `fixed`-нащадків і зламав би модалки. `top` такого ефекту не має.

### Заразом виправив ХИБНУ МЕТРИКУ, яку сам же і додав
`ПІК верх=59` вводив в оману: `getBoundingClientRect().top` міряє від РОЗМІТКИ, а вона
сама зсунута на `viewShift`. Аркуш стояв нерухомо, а число лякало. Тепер пік рахується в
ЕКРАННИХ координатах: `|(r.top − viewShift) − top0|`. Урок: метрика, побудована на власному
припущенні, узаконює хибний висновок (той самий урок, що 25.07 з лінією гілок).

### Перевірка — стенд тепер ВІДТВОРЮЄ дефект
Додав у стенд шапку `.app-header` і сценарій зсуву зі справжніх чисел айфона
(`shifts = 0→40→143→96→52→29→29→29` поряд із `frames` висоти).
| | ДО правки | ПІСЛЯ |
|---|---|---|
| ШАПКА на екрані | **−29px (за краєм)** ✗ | **0px** ✓ |
| АРКУШ на екрані | 157 = top0 ✓ | 157 = top0 ✓ |
| `--kb-vshift` | (нема) | `29px`, прибирається при закритті ✓ |
| замок фону / focus не рухає розкладку | ✓ / 0px | ✓ / 0px |

Тобто стенд спершу **показує дефект** (шапка за краєм), потім показує, що правка його
знімає, і що аркуш при цьому не зрушив. `node --check` ✓, `node build.js` ✓ (39 clean).

**CACHE:** `1220` → `cstl-20260726-1305`.

### 🔴 Межа доказу (та сама чесність, що й раніше)
Chromium не має клавіатури iOS — зсув видимої області я **емулюю** числами зі скріна.
Стенд доводить механіку компенсації, а не поведінку живого Safari. Перевірка — тільки
живим тестом Вови. Транзитний стрибок фону (143px) цим заходом **не лікується** — він
лікує залишковий зсув 29px, через який шапка стояла обрізаною.

### ✅ ЖИВИЙ ТЕСТ ЗАХОДУ Б — ЗАРАХОВАНО (скрін IMG_3645, v3509 · 15:41, деплой 3383)
Вова: **«Добре, хай буде поки так»**. На скріні шапка «CSTL LIFE» стоїть на своєму місці,
не обрізана — тобто компенсація `--kb-vshift` працює на живому айфоні.
Відкат не знадобився. B-25 у частині «шапка ховається за екран» — закрито.
⚠️ Транзитний стрибок фону (143px під час підйому) свідомо лишається — Вова його не
підняв повторно, окремої скарги нема.

---

## Нова задача (скрін IMG_3645, обведено червоним)

**Вова дослівно:** «Коли відкривається клавіатура, зображений такий овальний
горизонтально-овальне вікно за галочкою… Під ним прозоїсть, тобто видно, що відбувається.
Можна цей фонд заблюрити, щоб не було видно що там?»

**Що це насправді:** обведена пігулка з ↑ ↓ ✓ — системна панель iOS над клавіатурою
(form accessory bar), її вміст не наш. Але вона **напівпрозора**, і крізь неї видно те,
що намальовано під нею. А під нею зараз — зона НИЖЧЕ `.fd-sheet-vp` (модуль обрізає його
по видимій області), тобто просто затемнення `rgba(0,0,0,.45)` поверх тексту поста.
Звідси й читається «особливу атмосферу. Тут можна неквапливо прогулятися…».

**Історична пастка, яку треба назвати вголос:** домальовка під низ аркуша В ЦЬОМУ ПРОЄКТІ
вже була — `.fd-sheet-vp.kb-open::after`, і її прибрали 26.07 разом зі спробою 2
(попередження висить у `style/feed.css:456`). Але прибрали не за те, що вона погана, а
тому що вона обслуговувала **випереджальне стискання аркуша**, яке забирало ціль з-під
пальця. Сама по собі домальовка розкладку не рухає і HOT_RULES №10 не порушує.
Повертати можна — але свідомо, без прогнозу висоти.

**Показав два варіанти, Вова обрав 1 («Роби»):**
1. продовжити аркуш униз суцільним кольором — панель сідає на полотно, як у нативних;
2. матове скло (`backdrop-filter: blur`) — власне «заблюрити», але з сірим відтінком і
   помітним стиком з аркушем, плюс блюр рахувався б щокадру всю анімацію клавіатури.

### Що зроблено
- `keyboard.js`: нова опція **`overlayClass`** — клас на ОВЕРЛЕЙ, поки клавіатура відкрита
  (дзеркало наявного `kbClass` для аркуша). Знадобився, бо в самого аркуша `overflow: hidden`
  — його псевдоелемент обрізався б. Знімається і в `apply`, і в cleanup.
- `feed.js`: у виклику додано `overlayClass: 'fd-sheet-vp--kb'`.
- `feed.css`: `.fd-sheet-vp--kb::after` — `position:absolute; top:100%; height:100vh;`
  `background: var(--fd-surface); pointer-events: none`.
  `top:100%` = рівно від низу аркуша (оверлей прибитий до видимої області), `100vh` — запас,
  бо висоти клавіатури ми не знаємо і **не вгадуємо**.
- Замінив старе попередження в `feed.css` на пояснення, чому цього разу домальовка законна.

### Чому це НЕ повторення відкоченої спроби 2
Ту домальовку прибрали не саму по собі — вона обслуговувала випереджальне стискання аркуша,
яке рухало розкладку між `pointerdown` і `click`. Цей шар: з'являється **лише коли клавіатура
вже відкрита**, лежить **під** низом аркуша (зона клавіатури — пальцем не дістати), має
`pointer-events: none`. Розкладка не рухається взагалі.

### Перевірка — стенд розширено (14 перевірок, усі зелені)
Нові сторожі: підкладка з'являється при клавіатурі · непрозора · `pointer-events:none` ·
зникає при закритті листа · **під пальцем у полі вводу лишається САМЕ поле** (`elementFromPoint`
у центрі поля — сторож HOT_RULES №10) · до правки підкладки не було (тобто тест не самозбувний).
`node --check` × 2 ✓ · `node build.js` ✓ (39 clean).

**CACHE:** `1305` → `cstl-20260726-1253` (при мерджі main конфлікт → звів на `1310`,
щоб число зростало монотонно; `1253` було б «раніше» за вже задеплоєний `1305`).
Деплой **run 3384 success**.

---

## 🔴 РЕГРЕС ВІД ЦІЄЇ Ж ПРАВКИ — і як його полагоджено (IMG_3647 / IMG_3648)

**Вова:** «ти то зробив, щоб смуга була ця біла. Але тепер при відкритті клавіатури блок
Додати коментар вже не піднімається, так як раніше. Треба так, щоб цей блок був так як
раніше, піднімався і знизу було біле під клавіатурою».

На IMG_3647 (клавіатура відкрита) компоузер «Додати коментар…» **зник**: біла зона йде від
списку одразу до системної панелі. На IMG_3648 (клавіатура закрита) він на місці.

### Дві мої помилки, обидві варті запису
1. **Стенд перевіряв не той елемент.** Я написав у розмітці `.fd-com-bar`, а справжній клас
   компоузера — `.fd-com-compose` (`feed.js:939`). Тобто сторож «під пальцем поле» зеленів
   на елементі, якого в застосунку не існує. Розмітка «1-в-1» була 1-в-1 лише на вигляд.
   → Виправив розмітку стенду і додав окрему перевірку `компоузер за низом смуги`.
2. **Довів браузером те, що браузер довести не може.** Навіть із виправленою розміткою
   Chromium регрес НЕ відтворює: шар лежить нижче аркуша, компоузер у межах смуги.
   На айфоні — зник. Отже окремий шар усередині оверлея на iOS може лягти ПОВЕРХ аркуша,
   і сперечатись із цим зеленим тестом безглуздо.

### Рішення — підкладка стала ТІННЮ аркуша, а не окремим шаром
`.fd-com-sheet--kb { box-shadow: 0 100vh 0 0 var(--fd-surface); }` — зсув на 100vh униз,
нуль розмиття, нуль розтягу → рівний прямокутник кольору аркуша рівно під ним.
**Тінь малюється позаду свого елемента, тому накрити його вміст не може ФІЗИЧНО** — на
відміну від шару, порядок якого залежить від того, як браузер збирає композицію.
`overflow: hidden` аркуша тіні не обрізає (він ріже лише вміст).

Заразом прибрано те, що стало непотрібним: опція `overlayClass` у `keyboard.js`, її
передача у `feed.js`, правило `.fd-sheet-vp--kb::after` у CSS. Мертвого коду не лишаю.

**Перевірка:** стенд 14 перевірок зелені, серед них новий `Компоузер видимий над
клавіатурою (регрес IMG_3647)`. `node --check` × 2 ✓, `node build.js` ✓.
**CACHE:** `1310` → `cstl-20260726-1330` (при мерджі конфлікт → `1335`).
Деплой **run 3386 success**. Вова: «Вже працює нормально підкладка».

⚠️ **Відкритий хвіст:** «якось раз через раз, має бути стабільно». Тобто підкладка
з'являється не завжди. Найімовірніша причина: `kbClass` (а з ним і `box-shadow`) вішається
лише коли модуль визнав клавіатуру відкритою — `focused && kb > 80`. Якщо подія видимої
області не дійшла або `kb` не перетнув поріг, класу нема → нема й підкладки. Діагностується
тим самим рядком `ПІК за підйом`. **Не чіпав** — Вова перемкнув на іншу задачу.

### Ще одна помилка, знайдена при мерджі (варта запису)
Мій текст у `feed.css` опинився **поза** коментарем: рядок вище закінчувався `*/`, і мій
абзац пішов у CSS як сміття, а далі стояло ще одне `*/`. `node build.js` такого не ловить
(він збирає JS), браузер мовчки викидає невалідні токени — тобто помилка могла доїхати на
прод непоміченою. Виправив і додав до перевірок **баланс `/*` і `*/`** + грепи, що ключові
селектори лишились поза коментарями (зараз 127/127).

---

## Лінія гілок: варіант А + одна відповідь до згортання (скрін IMG_3651)

**Вова:** «від самого першого коментаря така віра лінія до самого низу просувається. Хоча
вона не має, вона має з'являтися тільки якщо дали відповідь саме на той коментар… воно взяло
поєднало перший коментар з коментарем іншої людини… це вже зовсім інший коментар, вже
зовсім своя сітка має будуватися».

### Спершу показав незручний факт, а не побіг правити
Довгу лінію малювало **правило 4-виняток, яке Вова сам затвердив 26.07** («Продовжується»):
остання відповідь із підгілкою не обривала стовбур батька. На живому екрані це дало те,
чого ніхто не хотів: стовбур доходив до низу гілки і впирався рівно в аватар НАСТУПНОГО
кореневого коментаря (обидва на вертикалі x=16px). Показав два макети (А — обривати лінію,
Б — лишити виняток, але обрізати хвіст), рекомендував А. **Вова обрав А.**

### Зроблено
- `feed.css`: прибрано `.fd-com-branch--has-sub:last-child::after` — рівно одне правило.
  Лишились недоторканими: стовбур кореня, продовження між відповідями, власний стовбур
  відповіді з підгілкою, обидва гачки (звірено грепом поза коментарями).
- `feed.js`: `REPLIES_VISIBLE` **2 → 1** («зменши показ відповіді на коментар до 1 коментаря,
  а далі якщо є то кнопка показати більше»). Діє на обидва рівні; кнопка «Ще N відповідей»
  і «Сховати відповіді» вже були.
- Оновив коментарі в обох файлах — із поясненням, чому виняток прибрано, і поміткою
  «не повертати без нового прямого прохання».

### Перевірка — новий сторож `scratchpad/kb/threads.mjs`
Розмітка 1-в-1 з `threadHtml()`: корінь → відповідь із підгілкою → під-відповідь → **другий
кореневий коментар іншої людини** (той самий випадок, що на скріні).
Перевіряє: лінії кореня повз вкладену розмову НЕМА · стовбур кореня, стовбур відповіді
з підгілкою, обидва гачки на місці · між гілкою і наступним коренем є зазор (16px).
🔑 **Сторож не самозбувний — довів A/B:** на CSS із `origin/main` (до правки) падає
(`лінія кореня повз вкладену розмову: Є`), на поточному — зелений. Це саме та перевірка,
якої бракувало вранці, коли зелений тест нічого не доводив.
Клавіатурний стенд після зміни CSS — 14/14 зелених.

**CACHE:** `1335` → `cstl-20260726-1345` (конфлікт при мерджі → `1350`).
Деплой **run 3387 success**.

---

## 🔴 ВІДПОВІДІ НА ВІДПОВІДІ НЕ ЗАВАНТАЖУВАЛИСЬ ВЗАГАЛІ (IMG_3652-3654)

**Вова:** «зверху пише 6 коментарів, а по факту їх 4»… далі: «написав зі свого основного
акаунта коментар у вигляді знака питання… зайшов, обновив сторінку — і не видно цього
коментаря».

### Мій попередній діагноз був ХИБНИЙ — і добре, що не побіг його реалізовувати
Я планував «звіряти кроковий лічильник з базою», бо вважав, що бреше лічильник.
Запит до бази показав протилежне: під постом 12 справді **6 живих** коментарів
(потім 8), тобто **заголовок казав правду**. Бреше не число — **бреше список**.

### Справжня причина (`supabase.js`, `fetchPostComments`)
```js
.in('parent_id', ids)   // ids = лише КОРЕНЕВІ коментарі
```
Тобто вантажився рівно ДРУГИЙ рівень. Показ ми зробили трирівневим 26.07 — а
завантаження лишилось дворівневим. Структура поста 12:
`95 корінь → 96 (2-й) → 97, 99, 101 (3-й) · 100 корінь`.
Клієнт отримував 95, 96, 100 → на екрані 3-4 замість 6.
**Чому «?» був видимий, а потім зник:** він приїхав живою синхронізацією (realtime) у
відкритий застосунок, а не із завантаження. Оновлення сторінки — і його нема. Тобто
відповіді на відповіді бачив ЛИШЕ той, хто сидів онлайн у момент їх появи.

### Фікс
Спускаємось по гілці, доки знаходяться нові: кожен крок — відповіді на тих, кого щойно
дістали. `MAX_REPLY_DEPTH = 8` (запобіжник від циклу на битих даних) + `seen`-дедуп, тож
цикл завершується гарантовано. `repCols` запам'ятовує розгортання без `reply_to_uid`,
щоб не бити в ту саму стіну на кожному рівні.

### Доказ — на ЖИВИХ даних, а не в браузері
SQL, що емулює обидві логіки для поста 12:
```
стара_логіка: 4   нова_логіка: 8   в_базі: 8
```
Тобто фікс доведено на тих самих даних, які Вова бачив на екрані.

**CACHE:** `1350` → `cstl-20260726-1400` (конфлікт при мерджі → `1405`).
Деплой **run 3388 success**.

---

## Пункт 3 — звірка лічильника з базою в реальному часі («Роби» Вови)

**Вова:** «3 роби» — після того, як я перерахував три незакриті пункти.
Початкова постановка: «має бути синхронізоване в прямому ефірі… В кожного користувача».

### Дві діри, які лишались після фіксу завантаження
1. **Два числа йшли різними шляхами.** Число під карткою малював `patchCommentCount`,
   а заголовок листа — тільки `renderCommentSheet`. Два шляхи до двох чисел, які мусять
   бути однакові, — саме так вони й розходились.
2. **Кроковий лічильник дрейфує за побудовою.** Подія може загубитись при обриві звʼязку
   або прийти двічі; видалення коментаря, якого ми не завантажували, взагалі нічого не
   відніме. Правда з бази бралась лише в момент ВІДКРИТТЯ листа.

### Зроблено
- `commentTitleText(total)` — одна функція на обидва місця. Обидва присвоєння заголовка
  тепер ідуть через неї (перевірено скриптом: 2 присвоєння, обидва через неї).
- `patchCommentCount` оновлює **і картку, і заголовок відкритого листа** одним викликом →
  розійтися вони більше не можуть, хоч би звідки прийшла зміна числа.
- `scheduleCountSync(postId)` — після кожної події про коментарі питаємо базу, скільки їх
  насправді. Затримка `COUNT_SYNC_DELAY = 400мс` на кожен пост: пачка подій (каскадне
  видалення гілки — це N подій поспіль) дає ОДИН запит, а не N. Патерн «спершу
  оптимістично, потім звірка».
- Якщо число розійшлося з тим, що лежить у пам'яті — значить подію ми пропустили, і
  список перезавантажується. ⚠️ **Тільки коли він завантажений повністю** (`!hasMore`):
  при частковому завантаженні довжина законно менша за загальну, і звірка ганяла б
  запити по колу.

### Перевірка — чесно про її межу
Статична (скриптом): заголовок формується лише через `commentTitleText` · `patchCommentCount`
чіпає обидва місця · звірка підключена до живих подій · захист `hasMore` на місці.
Обидва браузерні сторожі (`run.mjs`, `threads.mjs`) — зелені, нічого не зачеплено.
🔴 **Живого тесту realtime я НЕ робив** — він потребує двох пристроїв/акаунтів одночасно,
а вставляти коментарі в базу Вови заради тесту я не став. Остаточна перевірка — за Вовою.

**CACHE:** `1405` → `cstl-20260726-1415`.

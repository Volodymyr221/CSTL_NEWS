# Журнал сесії — 26.07.2026 (сесія F, гілка `claude/startuem-7g0why`)

> Веду по ходу, після кожної значущої дії. Звіт при `/finish` — з цього файлу, не з пам'яті.

---

## Крок 0 — старт (17:02 Київ)

- `git config user.name "Вова"` + `vitocorleone191@outlook.com` — коміти підписані Вовою
  («Unverified» на GitHub — очікувано, ігнорую).
- Гілка сесії: `claude/startuem-7g0why`. Робоче дерево чисте, гілка на рівні `origin/main`
  (`ff71a919` — «Перший тап по «Надіслати» більше не губиться…», PR #652).
- `git fetch origin` + `git pull origin main` → `Already up to date`.
- **`/byyou`:** `BYYOU_PLAN.md` статус `done` (потік мережевих збоїв, 26.07) — нічого не підхоплюю.
- **Прочитано:** `NEW_SESSION_PROMPT` · `HOT_RULES` (10 правил) · `START_HERE` ·
  `SESSION_STATE_VOVA` (блок хендовера) · `AUDIT_2026-07` · `CSTL_BUGS` · `CLAUDE.md` ·
  `ВОВА_ПРОФІЛЬ` · `BACKLOG` · хвіст `_session-log/vova-2026-07-26.md` (сесія E).

## Стан на вході

- `CACHE_NAME = cstl-20260726-1435` (у `sw.js`), останній деплой сесії E — **run 3390**
  за PR #652. Відкритих 🔴 багів у `CSTL_BUGS.md` нема.
- Сесія E (26.07) закрила: B-25 у частині «шапка ховається за екран» (захід Б, живий тест
  Вови «хай буде поки так») · підкладка клавіатури під низом аркуша · лінія гілки коментарів ·
  `REPLIES_VISIBLE 2→1` · **відповіді на відповіді реально завантажуються** (`fetchPostComments`
  спускався лише на 2 рівні) · звірка лічильника коментарів з базою (`scheduleCountSync`) ·
  перший тап по «Надіслати» при відкритій клавіатурі.
- ⚠️ **Транзитний стрибок фону при ПІДЙОМІ клавіатури (~143px) свідомо лишається** — Вова
  повторно не підняв, окремої скарги нема.
- 🔴 **Живого тесту realtime-звірки лічильника (PR #651) ще не було** — треба два акаунти
  / два пристрої. Перевірка за Вовою.

## Суперечності в документах (виніс Вові в першій відповіді)

1. **`SESSION_STATE_VOVA.md` хендовер застарів на пів дня.** Верхній блок датований
   26.07 11:40 і каже «прод run 3377, CACHE 1125, гілка `claude/startuem-k2kzci`, B-25 —
   головна відкрита задача». Насправді після нього пройшли PR #645–#652 і деплої до 3390,
   а частину B-25 закрито. Той самий розрив у `START_HERE.md` (секція «Поточний стан», run 3377).
2. **B-25 має різний статус у двох файлах.** `CSTL_BUGS.md` — 🟡 «Вова знає і погодився поки
   лишити»; хендовер — «🔴 задача, яку Вова хоче спробувати в новому чаті». Фактично: частина
   про шапку закрита, транзитний стрибок фону лишається без скарги.
3. **`BACKLOG.md` розділ «🔴 Роби зараз» частково мертвий.** «Захід 2 — Репутація +
   автопублікація» стоїть як 🔴 відкритий, хоча в `AUDIT_2026-07.md` це **Д-1 ✅ ЗРОБЛЕНО
   08.07 (PR #267)**. Сам BACKLOG у шапці визнає себе довідковим — але 🔴-блок збиває з пантелику.

---

## Далі

Чекаю задачу від Вови. Без явного «Роби» коду не чіпаю.

---

## /byyou — новий потік (Вова: «Продовжуєм byyou те яке робили в минулому чаті заново»)

**Фаза 0.** Гілка `claude/startuem-7g0why` ✓. `BYYOU_PLAN.md` був `done` → режим НОВИЙ,
не продовження. Минулий потік архівовано → `BYYOU_ARCHIVE_2026-07-26_network-errors.md`.
Ціль лишив ту саму (мережеві збої не доходять до людини), бо минулий потік накрив лише
частину застосунку.

**Фаза 1 — замір (не з пам'яті, grep + читання ядра):**
- `alert()` у `src/` — **0** ✅ (минулий потік прибрав усі 7).
- Записів у базу: **55** у `supabase.js`, через ядро `netCall` проходять **6** → **≈49 поза ядром**.
- Мутації поза `supabase.js` — хибні збіги (`Set.delete`, `swReg.update`), крім `auth.js:122/130`
  (справжній upsert профілю без ядра).
- Сира технічна помилка ще тече людині: `account-ui.js:254` і `:289` (`+ err.message`).
- 🔎 **`sendMessage` (приватний чат) повертає `error: error.message` — сирий текст** (`supabase.js:752-753`).
  Тобто чат — головний острівець.

**Дві знахідки, які змінили план (винесені Вові на браму старту):**
1. 🔴 **Повтор ВСТАВКИ може створити дубль.** `netCall` повторює запит при обриві; для
   `update/delete/upsert` це безпечно, для `insert` — ні (якщо запит доїхав, а відповідь
   загубилась). Минулий потік обгорнув `createPagePost` → ризик подвійного поста вже в проді.
   Три шляхи: (а) звірка перед повтором — у чату вже є `client_tag`; (б) unique index у базі
   (зміна схеми → твої руки); (в) вставки не повторювати. Рекомендація: (а)+(в) зараз, (б) пізніше.
2. ⚠️ **MCP Supabase у цій сесії НЕ авторизований** (docs проєкту кажуть «працює» — цього разу
   просить авторизацію). Отже ні міграцій, ні читання бази з мого боку — SQL за Вовою.

**Стан:** план на 14 кроків у `BYYOU_PLAN.md`, статус `active` (push-замок), ≈50% вікна.
Чекаю «ок» + рішення по вставках.

## Виконання потоку (кроки 1-13) — «Так» від Вови

Гілка `claude/startuem-7g0why`, усе локальними комітами (push-замок тримає).

| Коміт | Що |
|---|---|
| `924ff78d` | ядро `netInsert` + приватний чат (він же й сирий `error.message` віддавав) |
| `e6d91fbc` | коментарі й пости Дошки; реакції тихо; обговорення без повтору |
| `cc9ef156` | групові чати (повідомлення зі звіркою, створення групи/інвайта без повтору) |
| `1d009c2c` | «Стрічка» + **прибрано ризик подвійного поста** (`createPagePost` мав сліпий повтор) |
| `a894fc36` | push-підписки, закладки, профіль, фото — сирих помилок людині більше нема |
| `0f15946b` | `clientTag` у викликах Обговорень і груп; аналітика свідомо без ядра |
| `819e951d` | стенди, один словник помилок, реліз-нотатки, CACHE 1445 |

### Рішення по вставках (блок 🔴 з брами старту)
Вибрано (а)+(в), як і рекомендував. `netInsert(fn, {verify})`: перша спроба без повтору; якщо
обрив **тимчасовий** і є `verify` — питаємо базу, чи рядок уже там. Знайшли → віддаємо його
(`recovered: true`), не вставляючи вдруге. Не знайшли → повторюємо вставку. **Звірка сама не
доїхала → повтору НЕ робимо** (не знаємо стану = не пишемо). Немає ключа → рівно одна спроба.

### Що змінило план по ходу
1. **MCP Supabase підключився посеред потоку** (на старті просив авторизацію). Скористався для
   ЗВІРКИ з живою базою: `client_tag` є в `messages`/`comments`/`chat_group_messages`, немає в
   `page_comments`/`page_posts`/`posts`/`threads`. Дизайн збігся з дійсністю — не з припущенням.
2. **Ключ не передавали самі виклики.** У базі колонка є, а `addComment` (Обговорення) і
   `sendGroupMessage` (групи) її не шлють → звірка була б мертва. Додав `clientTag` у виклики.
3. **Свій власний регрес.** Перевівши `addPageComment` на ядро, я підмінив точний текст
   антиспаму загальним — бо в `feed.js` живе ДРУГИЙ словник (`commentErrorText`), який ловив
   сирий текст бази регулярками. Зловив це сторож присутності, а не я. Виправив як задумувалось
   у минулому потоці: формулювання одні (`netErrorText` +3 доменні випадки), а `commentErrorText`
   лишився тонкою обгорткою лише для відтінку «Коментар не надіслано».
4. **Читання коментарів теж пішло через ядро** — раніше падало з першої спроби і віддавало сирий
   текст. Тепер повтор + людський текст (читання ідемпотентне, повторювати безпечно).

### Перевірки (і чого вони НЕ доводять)
- `scratchpad/net/run.mjs` — **25/25** на СПРАВЖНЬОМУ ядрі (esbuild зі `src/`), включно з
  A/B «старий шлях → 3 вставки в базу, новий → 1» і «звірка не доїхала → дубля не робимо».
- `scratchpad/net/guard.mjs` — **51/51** сторож присутності по коду: кожен запис через ядро й
  саме через правильну функцію; сирих помилок у поверненнях нема; свідомі винятки на місці.
  🔑 Сторож не самозбувний — він **упав** на пропущеному `fetchPostComments`, і це його заслуга.
- Смоук у Chromium: **0 JS-падінь**, технічного тексту на екрані немає. Розділив у стенді
  JS-падіння (наша вина) і недоступні ресурси (мережа пісочниці) — інакше зелений був би самообманом.
- 🔴 **Не доведено:** справжній обрив мережі на iPhone і realtime між двома акаунтами. Стенд
  імітує обрив, а не відтворює iOS.

---

## ДЕПЛОЙ №1 сесії F — потік мережевих збоїв

Вова: «Деплой». PR **#653** → squash у `main` (`d6c7a6b6`) → **run 3393 completed/success**
(звірено `actions_get`, не на віру). CACHE `cstl-20260726-1445`.

---

## /byyou №2 — СИНХРОНІЗАЦІЯ ЛІЧИЛЬНИКА КОМЕНТАРІВ (Вова: «якщо ти цього не робив то запусти нове /byyou»)

**Вова дослівно:** «в самій картці поста, біля іконки коментарів, пише два коментаря. Коли я
натискаю, там зверху пише два коментарі, але коментар один, і воно не синхронізоване… якщо я
видаляю повідомлення якесь своє, то лічильник має зменшуватись… як і в мене, так і в інших
відразу, максимально швидко».

### Корінь знайдено з ЖИВОЇ бази (MCP), а не з міркувань
Пост 14, два живі рядки: `68` (корінь, видно) і `111` («Так») — відповідь на коментар `110`,
який **видалений**. Тобто сирота: живий, але недосяжний з екрана.

**Хронологія по секундах:** 13:45:24 створено 110 → **13:45:41 видалено 110** →
**13:45:48 створено 111 як відповідь на 110**. Відповідь під уже видаленого батька.

### Чотири окремі причини (жодна не «недороблена синхронізація»)
1. Подання `page_comment_counts` = `count(*) where deleted_at is null` → рахує й сиріт.
2. `fetchPostComments` спускається лише від ЖИВИХ коренів → сироту не покаже ніколи.
3. Жоден із 4 тригерів на `page_comments` не перевіряє, чи живий батько при INSERT.
4. Каскад «одна сітка» фізично не може закрити цей випадок — він прибирає гілку на МОМЕНТ
   видалення, а дитина народилась через 7 секунд ПОТІМ.

### 🔴 Чесно про сесію E
Її звірка з базою (PR #651) зроблена правильно, але **марно**: вона синхронізує клієнта з
джерелом, яке саме бреше. Причина була знайдена не та. Це і є відповідь на закид Вови
«я ж просив це ще з самого початку».

### Замір масштабу
Сиріт у «Стрічці» — **1** (пост 14). У Дошці — **0**, і там цей клас не працює взагалі
(відповіді показуються плоско, нічого не ховається) → Дошку не чіпаю, HOT_RULES №9.

**Стан:** план на 12 кроків у `BYYOU_PLAN.md` (спершу база як джерело правди, потім клієнт),
статус `active`. Чекаю «ок» + вибір поведінки при відповіді під видалений коментар (А/Б).

## Виконання потоку №2 (Вова: «А»)

**Порядок навмисний: спершу база, потім клієнт.** Якби навпаки — знову вийшла б синхронізація
з брехливим числом, як у сесії E.

### Міграції в живій базі (через MCP)
1. `forbid_reply_under_deleted_parent` — правило додано в НАЯВНИЙ BEFORE INSERT тригер
   «Стрічки» (`page_comments_antispam`), не другим тригером: саме через дві копії в проєкті
   вже розходились списки слів. Батько мусить бути живий **і з того самого поста** (батько
   з іншого поста дав би ту саму недосяжність). Маркер `parent_deleted` для клієнта.
2. `page_comment_counts_only_reachable` — подання рахує лише досяжні коментарі (рекурсивний
   спуск від живих коренів). `security_invoker = on` збережено, інакше подання обійшло б RLS.
3. Ремонт даних: 1 сирота (id 111, пост 14) мʼяко видалений — правило «одна сітка».

SQL зафіксовано у `scripts/supabase_comment_counts_reachable.sql` (конвенція проєкту) разом
із перевіркою, яку можна ганяти повторно.

### Клієнт
- `netErrorText`: `parent_deleted` → «Цей коментар уже видалено».
- `addPageComment` віддає прапорець `gone` — бо тексту замало: треба **перемалювати гілку**,
  інакше людина далі дивиться на коментар, якого нема.
- `feed.js`: на `gone` — тост + `clearReply()` + `loadComments()` + `renderCommentSheet()`.
- **Кроки 5-6 нічого не переробляв — клієнт сесії E був правильний.** Звірив: число для
  картки й заголовка йде через один `patchCommentCount`; `scheduleCountSync` скидає таймер,
  тож каскад із N подій дає один запит.

### Помилка в МОЄМУ тесті, яку зловив сам тест
Перша спроба позитивних перевірок дала «T4 ВІДМОВА ✓» — але то була відмова по **зовнішньому
ключу** (я підставив вигаданого автора), а не моєю перевіркою. Тобто зелене було несправжнє.
Переробив: реальний `author_uid` + звірка, що в тексті помилки саме `parent_deleted`.
Після цього: T1 відмова ✓ · T2 відповідь під живого проходить ✓ · T3 корінь проходить ✓ ·
T4 чужий пост відмова ✓. Усе в транзакції з відкатом → тестових рядків і **пушів** не лишилось
(`net.http_post` транзакційний, тому відкат скасовує й чергу сповіщень).

### Що НЕ доведено (кажу прямо)
- Живої синхронізації між двома пристроями не перевіряв — це за Вовою. Перевірив лише
  **конфігурацію** realtime: `page_comments` у публікації, ідентичність рядка за ключем.
- Крок 6 доведено читанням коду, не стендом (стенд вимагав би підміняти клієнта Supabase).
- Крок 9 (стенд на двох сторінках) — свідомо не зроблено, у плані позначено «НЕ зроблено».

**CACHE:** `1445` → `cstl-20260726-1510`. Баг заведено як **B-27** (закритий).

---

## ДЕПЛОЙ №2 — лічильник коментарів (B-27)
PR **#654** → `main` (`bf136bed`). ⚠️ Перший мердж упав: «merge conflicts» — `main` рухнувся
(автопарсер + мій же squash #653). Вирішив вручну `--ours` для `supabase.js`/`sw.js`/доків
(моя гілка = надмножина), `bundle.js` перерахував `node build.js`. **Звірив, що правки #653
не загубились** (`netInsert` 11 входжень, `parent_deleted` 2) — правило «не губи попередні фікси».

## БЛОК 2 (Вова: «Деплой і продовжуй працювати далі»)

**Замір, що жило наживо:** коментарі «Стрічки» ✅, лайки ✅, Обговорення ✅, чати ✅ —
а **самі пости (і «Стрічки», і Дошки) підписки не мали взагалі**. `cstl-posts-changed` —
локальна подія одного пристрою.

**Зроблено:**
- `subscribePagePosts` + `applyPostEvent` у «Стрічці»: редагування/видалення застосовуються
  одразу, НОВИЙ пост — через пігулку «↑ N нових публікацій».
- Чому пігулка: `renderFeed` перемальовує список цілком → вставка зверху зсунула б усе під
  пальцем (сім'я HOT_RULES №10). Пігулка з'являється миттєво, момент оновлення вибирає людина.
- `subscribePosts` + Дошка: перемальовуємо лише коли безпечно (вгорі списку або при вході),
  інакше `_boardStale` і чекаємо.
- Самозаліковування: `heal_orphan_page_comments()` + `pg_cron` щоночі 03:15 UTC (третій рубіж
  після заборони на вставці й подання, що рахує лише досяжне).

**Свій баг по ходу:** у пігулці чистив `pendingPosts` ДО вливання в список — пости губились би.
Виправив; сторож присутності тепер це перевіряє окремо (61/61).

**Не доведено:** поява поста на ДРУГОМУ пристрої наживо — мережа пісочниці не пускає до
Supabase. Тільки двома телефонами, за Вовою.

**CACHE:** `1510` → `cstl-20260726-1535`.

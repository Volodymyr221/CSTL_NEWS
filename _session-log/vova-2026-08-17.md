# Журнал сесії — 17.08.2026

## Розділ «Збори»: заявки від жителів + створення з адмінки

**Замовлення Вови:** «продумати в бургер-меню вкладку в правильній підкатегорії…
людина клацає туди, зможе звʼязатися з підтримкою та запропонувати збір. І як з
адмінки легко в такому самому форматі створити збір, якщо ми затвердили».
Плюс пряме питання: «чи як ти думаєш, правильно реалізувати це?»

### 🔴 Вузол, який визначив увесь обсяг

Збори лежали у файлі `data/fundraisers.json` **у git**. Адмінка (`admin.html`)
пише в **Supabase** і не може писати в git: для цього їй потрібен був би ключ
запису в репозиторій, а він лежав би у браузері, тобто у всіх на видноті.
➡️ Отже «створювати з адмінки» = спершу переїзд даних у базу.

✅ **Переїзд був передбачений 05.08.** У `home-fund.js` тоді записали: «форма
запису пласка, щоб переїзд у Supabase не зачепив цей файл: зміниться лише
джерело в `loadFundraisers()`». Так і сталося — змінився рівно один рядок.

⚠️ **Ціна названа чесно:** файл лежав у кеші Service Worker, тобто блок працював
офлайн. Тепер без мережі зборів немає. Прийнятно саме тут: збір це ДІЯ (перейти
на банку), а дія однаково вимагає мережі.

### 🔑 Головне рішення схеми: заявка і збір мають ОДНАКОВІ поля

Не «схожі» — однакові. Тому схвалення в адмінці це **одна кнопка**
(`approve_fundraiser_request`), а не переписування руками.

🔴 Якби поля різнились, Вова щоразу набирав би дані заново, і рано чи пізно
зʼявилась би друкарська помилка **в посиланні на чужу банку** — найдорожча
помилка, можлива в цьому розділі. Розходження полів ловить
`fundraiser_field_parity()`.

RPC ще й атомарний: двома запитами (insert + update) буває «збір створено,
заявка досі нова» — і адмін схвалює її вдруге, тобто на головній два однакові
збори. Повторний тап тепер віддає той самий збір.

### Що вирішено інакше, ніж просив Вова (і чому)

**Один пункт меню «Збори», а не два** («Актуальні збори» + «Подати збір»).
Вова назвав два, рішення лишив за мною. Аргумент, що переважив: **дія має стояти
там, де людина вже бачить приклад**. Відкривши розділ, вона бачить, як виглядає
збір, і аж тоді кнопку «Запропонувати збір» — тобто розуміє, що подає. Окремий
пункт вів би в порожню форму без контексту. Плюс у меню вже 14 пунктів.

### Права — доведено експериментом, а не міркуванням

Відтворив запити з підстановкою ролі й JWT, як це робить PostgREST:

| перевірка | результат |
|---|---|
| анонім бачить активні збори | 1 ✅ |
| анонім створює збір · подає заявку | відмова ✅ ✅ |
| житель пише в `fundraisers` напряму | відмова ✅ |
| заявка під ЧУЖИМ uid | відмова ✅ |
| заявка під собою | проходить ✅ |
| четверта заявка за добу | спинено (`freq_flood`) ✅ |
| житель схвалює свою заявку | відмова ✅ |
| адмін схвалює | збір створено, поля доїхали повністю ✅ |
| `verified` після схвалення | `false` ✅ |
| повторний тап «Схвалити» | той самий збір, дубля немає ✅ |

### 🛑 Дві вади, які я зробив сам і спіймав живою перевіркою

**(1) Зворотні лапки в HTML-коментарі всередині шаблонного рядка.** Написав у
коментарі назви класів у зворотних лапках — і вони **закрили шаблонний рядок**.
Адмінка застигала на «Завантаження…». `node --check` на витягнутому JS **мовчав**;
спіймала жива сторінка. Виправляючи, я вжив лапки ще раз — уже в самому
попередженні про них.
➡️ Урок: перевірка синтаксису не бачить семантики шаблонних рядків. Відкривай
сторінку.

**(2) Мірка тостів брехала.** Дві перевірки стенда червоніли, показуючи текст
**попереднього** тоста: `showToast` має ЧЕРГУ (друге повідомлення чекає 1.5с), а
я міряв фіксованою паузою 600мс.
➡️ Та сама сімʼя, що урок 16.08: **те, що зʼявляється й згасає, міряй ПОДІЄЮ, а
не числом**. Тепер стенд чекає появи потрібного тексту.

**(3) Сторож падав винятком замість звіту.** На контрольному прогоні (код до
розділу) стенд помирав зі стеком Node на першому ж кроці, і було не видно,
скільки перевірок він ловить. Додано ранній вихід: сторож мусить ЗВІТУВАТИ.

### 🔴 Знайдено принагідно: клас без стилю в адмінці

`btn--ghost` уже стояв на невибраних чіпах фільтра «Скарг», але **стилю не мав** —
розмітка розрізняла вибране й невибране, а на екрані вони виглядали однаково:
людина не бачила, який фільтр увімкнений. Помітив, бо хотів узяти цей клас для
свого фільтра і перевірив, чи він описаний. Виправлено для обох розділів разом.

### Підсумок

| етап | що |
|---|---|
| 1 | таблиці, RLS, RPC + віджет читає з бази; JSON видалено |
| 2 | розділ «Збори» в адмінці: заявки + керування зборами |
| 3 | форма заявки в застосунку (`fund-screen.js`) |
| 4 | пункт «Збори» в меню, у групі «Розділи» |

**Сторож:** `tests/fund-screen.mjs` — 23 перевірки, контроль → **0/4**.

---

## 🔬 Розслідування: чому стенди червоні (17.08, після деплою #941)

Замовлення Вови — «роби» на пропозицію взятися за стабільність сторожів.

### Що доведено

**`sidebar-overlay` = 25/36 СТАБІЛЬНО.** Не шум і не навантаження. Однаковий
результат на: моїй машині · GitHub-раннері (run 30) · порожній машині без жодного
іншого процесу · коді від 15.08 · коді від 16.08 · чинному `main`.

**При цьому саме меню працює правильно** — заміряно прямим приладом:

| що міряв | результат |
|---|---|
| `pointer-events` після закриття | знімається за **1мс** |
| стан через 6с | `opacity 0` · `display none` · панель за екраном (X=397) |
| бургер після закриття | **клікабельний** |
| закриття кліком по бургеру, той самий кадр | `eatsTap: false`, `pe: none` — правильно |

➡️ Тобто перевірка червона, а поведінка справна. Найімовірніше вада в самому
стенді — але **остаточно я цього не довів**, тому не чіпав ні стенд, ні меню.

### 🛑 Три мої гіпотези, і всі три хибні

**(1) «Стенд чекає 450мс на процес, що триває довше».** Підняв паузу до 900мс —
результат **не змінився ані на йоту**. Спрацювало правило 16.08: коли фікс не
рухає симптом, хибна гіпотеза, а не «мало полікували».

**(2) «Стенди течуть процесами браузера і отруюють наступні».** Під час прогону
справді видно **20 процесів замість 10** — тобто два Chromium одночасно, хоча
`run.mjs` послідовний. Але вимір після завершення: 4 процеси одразу → **0 через
10 секунд**. Витоку немає, це затримка прибирання; накладання триває секунди.

**(3) «Це нестабільність середовища».** Я сказав це Вові — і **помилився**:
GitHub-раннер дав рівно ті самі 25/36. Виправив уголос одразу, як побачив.

### 🔴 Головний урок — про власний метод

Мої «ізольовані» перевірки **не були ізольованими**: у фоні крутився повний
прогін, який я сам і запустив. Тобто я міряв завантажену машину, називаючи це
чистим виміром, і на цьому побудував дві гіпотези.
➡️ **Перед «ізольованим» виміром — переконайся, що машина порожня** (`ps` на
процеси, а не віра в те, що фонове завдання вже скінчилось).

Заразом власний прилад збрехав підписом: рядок «через 6с» друкувався одразу
після `break`, тобто показував стан через 2мс. Та сама хвороба, яку розслідував.

### Що лишається відкритим

- **`sidebar-overlay` 25/36** — не полагоджено. Потрібен окремий захід із
  холодною головою: пройти сцену стенда покроково і знайти, де саме розходяться
  його уявлення і реальна поведінка.
- **`home` 69/81 у повному прогоні** проти **81/81 поодинці** — те саме питання.
- **`drift` 8/24** і довгий — зависав і 16.08.

⚠️ **Для Вови практично:** меню на телефоні за всіма прямими вимірами працює.
Але поки сторож червоний, він не захищає — тож перевір пальцем: бургер → ✕ →
згорнути застосунок → повернутись.

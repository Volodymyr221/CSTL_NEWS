# Журнал сесії — 30.08.2026 (сесія «b»)

**Гілка:** `claude/startuem-8csg49` · production `main`
**Старт:** `/startuem`, віддалений контейнер (Claude Code on the web)

---

## Крок 0 — старт сесії

- `git config user.name "Вова"` + `user.email vitocorleone191@outlook.com` — виконано.
- `git fetch origin` — ок. `git diff --stat origin/main HEAD` **порожній**, тобто
  гілка синхронна з `main`, нічого не загубилось і нічого не висить незапушеним.
- Останній коміт людини в `main`: `90ffcd58` docs: передача в новий чат (PR #1116).
- `_session-log/vova-2026-08-30.md` уже зайнятий попередньою сесією → цей журнал
  отримав літеру **`b`** (правило №13).

## Обовʼязкове читання — виконано

`NOW.md` · `HOT_RULES.md` (14 правил) · `CSTL_BUGS.md` · `BACKLOG_VOVA_2026-08.md` ·
`ВОВА_ПРОФІЛЬ.md`. Плюс `_ai-tools/NEW_SESSION_PROMPT.md` і шапка `AUDIT_2026-07.md`.
`BYYOU_PLAN.md` — статус `idle`, продовжувати нічого.

## Знайдені розбіжності в документації (доповів Вові в першій відповіді)

1. 🔴 **`BACKLOG_VOVA_2026-08.md` тримає закритий борг як відкритий.** Блок від
   25.08 пише: «у проєкті **дві реалізації** авто-каруселі — новий
   `core/auto-carousel.js` і старий `startNewsCarousel()` у `community-blocks.js`»,
   і радить узяти цей борг **перед пунктом 3**. Перевірено грепом по `src/`:
   `community-blocks.js:1997` уже кличе `startAutoCarousel(track, …)`, тобто
   `startNewsCarousel` став тонкою обгорткою над спільним модулем. Те саме каже
   `CLAUDE.md` (стенд `carousel-shared.mjs`, «борг закритий 26.08»).
   ➡️ Борг **закритий**, рядок у беклозі застарів на 4 дні.
2. 🟡 **Дві різні «черги» на завтра.** `BACKLOG` каже «наступний — пункт 3 (теплота
   чату Дошки)», `NOW.md` у розділі ➡️ ДАЛІ каже «ПЕРШЕ — екран на компʼютері
   пальцем, ДРУГЕ — тематичний фільтр B-14, ТРЕТЄ — сусідні класи неправди в
   агентів». Пункт 3 у списку `NOW.md` не згадується взагалі. Питання до Вови:
   яка з двох черг чинна.
3. ℹ️ Верхній авто-блок `NOW.md` (зріз 15:44) називає гілку `claude/startuem-nju3bu`
   і три файли як «не доїхало в `main`» — це нормально, зріз знято до злиття
   PR #1116; зараз усе в `main`, звірено `git diff` на дві крапки.

## Далі

Чекаю «Роби» від Вови. Коду не чіпав.

---

## Роби №1 — «Виправи розбіжності в документації» (30.08)

Вова: **«Виправи розбіжності в документації»**. Коду не чіпав — лише `.md` і скіл,
тому `CACHE_NAME` НЕ рухав (HOT_RULES №2, виняток для чисто документаційних змін).

### Що виправлено

**(1) Закритий борг каруселі, який документація радила взяти.**
`BACKLOG_VOVA_2026-08.md` і `CLAUDE.md` обидва писали «у проєкті ДВІ реалізації
авто-каруселі… варто взяти перед пунктом 3». Звірено грепом по `src/`:
`community-blocks.js:1984` → `startAutoCarousel()`, у самому коді стоїть
`auto-carousel.js:9` «✅ 26.08 — БОРГ ЗАКРИТО». Тобто рядок брехав 4 доби, і брехав
найдорожчим способом — **радив зробити вже зроблене**.
🛑 `SESSION_STATE_VOVA.md:1440` навмисно НЕ чіпав: це літопис, запис від 25.08, який
тоді був правдою. Правило `docs-refs.mjs` — літопис не міряється й не переписується.

**(2) Чотири файли казали «що робити далі» і казали різне.**
Виявилось не два претенденти, а **чотири**: `NOW.md` ➡️ ДАЛІ · беклог («пункт 3») ·
`START_HERE.md:38` («AUDIT — джерело істини для що робити») · сам скіл `/startuem`
(«Топ-3 пріоритетні задачі (AUDIT_2026-07.md)») і `NEW_SESSION_PROMPT.md:143`.
🔑 **Розвів не вибором переможця** (Вова не сказав, яка черга чинна), **а ролями** —
і це опис ФАКТУ, а не вигадане правило: за серпень черга беклогу не зрушила з пункту 3
жодного разу, тоді як позачергових замовлень зробили вісім.
- `NOW.md` ➡️ ДАЛІ = живий порядок, чинний;
- беклог і `AUDIT` = повний перелік, з якого беруть, коли позачергового немає.
Перехресні посилання проставлені в обидва боки, щоб більше не розійшлись.
Заразом дописав у беклог **B-14** — він розблокований 30.08 і стоїть у `NOW.md`
другим, а в беклозі не значився взагалі (05.08 був заблокований).

**(3) Кількість правил у `HOT_RULES.md` розходилась у пʼяти місцях.**
Реально правил **15** (0…13 + 11-БІС, пораховано `grep -c "^## "`). Сам файл писав
«14», а `START_HERE.md` (×3), `NEW_SESSION_PROMPT.md` і скіл `/startuem` — «11».
Виправлено скрізь на 15.

**(4) `START_HERE.md:582`** називав чергою беклог і згадував у ній «блок "А" кремовий
`--bg-card`» — пункт про беж закритий 11.08 словом Вови.

### 🔴 Сторож упіймав мене на власній правці — і був правий

Після правок `tests/docs-fresh.mjs` впав: **`NOW.md` 17.2 KB при стелі 16 KB**.
📐 Перевірив, чи це не брехлива мірка (правило «першою підозрюваною є перевірка»):
`git show origin/main:NOW.md | wc -c` = **16342** байти, тобто файл стояв за **42 байти**
від стелі, і 1240 байт мого блоку його перекинули. Мірка чесна, винен я.
➡️ Ужав свій блок із 9 рядків до 2 і стиснув найстаріше в пульсі (сесія «b», заголовок
онбордингу, рядок про 403) — **факти не викидав**, лише прибрав номери деплоїв, які є
в літописі. Стало **16353 / 16384**.
🔑 **Урок:** пульс має ЖОРСТКУ стелю, і дописати в нього — означає щось із нього
прибрати. Наступна сесія, дописуючи в `NOW.md`, має закладати цей обмін одразу.

### Перевірка

`node tests/docs-fresh.mjs` → **20/20** ✅ (було 18/20 після моїх правок)
`node tests/docs-refs.mjs` → **10/10** ✅
Коду не торкався, `CACHE_NAME` не рухав.

### Перевірка після паузи (Вова: «Зараз все добре? Ти виправив?»)

Не відповідав з памʼяті — перегнав сторожі заново. `docs-fresh` показав **19/20**.
📐 Розібрав причину, а не «полагодив наосліп»: `main` зрушив на **3 автокоміти
парсерів**, авто-блок пульсу через це відстав. Оновив `now_update.mjs` — і пульс знову
переріс стелю, але **з іншої причини**: список «Не доїхало в `main`» роздувся до 8
файлів моєю ж роботою на гілці. Заміряв: блок важить **391 байт**, і після мерджу в
`main` пульс сам би став 16260 (під стелею).
🛑 **Лишати так не можна було:** на робочій гілці сторож світився б червоним у КОЖНОЇ
сесії, а проєкт має 19 записаних випадків, коли брехлива мірка відправляла лікувати
справне. Тому стиснув прозу так, щоб пульс тримався **разом із повним списком
«не доїхало»**: 16224 / 16384, **запас 160 байт**. `docs-fresh` **20/20** ✅.

🔴 **СИСТЕМНЕ, ДЛЯ НАСТУПНОЇ СЕСІЇ (не полагоджено, треба рішення Вови).**
Пульс живе впритул до стелі, а частина його ваги — **авто-блок, яким автор не керує**:
довжина повідомлень комітів і кількість незамерджених файлів. Тобто «скільки можна
написати прози» тепер залежить від того, скільки в тебе незапушених файлів.
➡️ Два чесні шляхи: (а) винести старі блоки «ЗРОБЛЕНО» (29.08 і раніше) у літопис,
звільнивши ~1.5 KB одразу; (б) навчити `docs-fresh` міряти прозу і авто-блок окремими
бюджетами. Стелю НЕ піднімати — вікно контексту від цього не росте.

---

## Роби №2 — варіант «А»: старі блоки «ЗРОБЛЕНО» → у літопис + деплой

Вова: **«А. Роби. І деплой»** — тобто варіант (а) з мого попереднього звіту.

### Крок 1 — злиття ДО роботи (правило №13)
Гілка відстала на 4 коміти. `git merge origin/main` — чисто, без конфліктів
(лише автокоміти парсерів: `articles.json`, `schedule.json`, `vopas-fetched.json`,
`HEALTH.md`).

### Крок 2 — 🔴 ПЕРЕВІРКА ПЕРЕД ВИНОСОМ, ЯКА ВРЯТУВАЛА ДВА УРОКИ

Перш ніж прибирати блоки, грепнув літопис — чи вони там узагалі є. Виявилось:
- розбір екрана компʼютера — у літописі повністю (рядки 114-198);
- онбординг — теж повністю (324-521);
- авторизація (199) і класифікатор (24) — теж.
Тобто в пульсі це був **дублікат**, і виносити безпечно.

🛑 **АЛЕ два висновки жили ТІЛЬКИ в пульсі — грепом по всьому репо 0 збігів:**
1. **«у заголовку те, що в ЛАПКАХ, — імʼя, а не тема»** — правило класифікатора,
   куплене вечірньою роботою 30.08 (PR #1115, «птахи» Мадяра). Ніде більше не існувало.
2. **«схему чужого інтерфейсу доводить знімок, а не памʼять»** — урок про меню
   «Поділитись» на живому iPhone.
➡️ Обидва **перенесено в літопис ДО чистки**, у відповідні розділи дат.
🔑 **Правило, яке з цього виведене і записане в пульс:** прибираєш блок із пульсу —
спершу грепни, чи його висновок існує ще десь. Пульс — вітрина, а не сховище.

### Крок 3 — чистка
Чотири розбори (4418 байт) → компактна таблиця з посиланнями на літопис (2074 байт).
📐 Пульс **16224 → 13740 байт**, запас під стелею зріс зі **160 байт до 2.6 KB**.

### 🔴 Сторож упіймав перейменування розділу
Після заміни `docs-fresh` дав **19/20**: «розділ "ЗРОБЛЕНО В ЦІЙ СЕСІЇ" — розділу
немає». Я перейменував заголовок на «ЗРОБЛЕНО (розбір — у літописі…)», а сторож
шукає його регуляркою `##[^\n]*ЗРОБЛЕНО В ЦІЙ СЕСІЇ\s*\n`.
🔑 **Назва розділу пульсу — це контракт зі сторожем, а не вільний текст.** Повернув
точну назву, уточнення поставив окремим рядком під заголовком.

### Перевірка
`docs-fresh` **20/20** ✅ · `docs-refs` **10/10** ✅ · пульс 13740/16384.
Коду не торкався — зміни лише `.md`, тому `CACHE_NAME` не рухав.
⚠️ `npm test` у пісочниці спершу дав 26/122: бракувало пакета `playwright`
(нестача контейнера, не поломка коду) — поставив `npm install` і перезапустив.

---

## Два підсумки вечора 30.08

### 1. Збої в адмінці — розібрано, обидва закриті

Вова показав знімок журналу збоїв і спитав, що це були за баги.

**🔴 `ReferenceError: Cannot access 'h' before initialization` — НАШ, уже полагоджений.**
Доведено адресою, а не здогадом: узяв `bundle.js` із коміту ДО фіксу (`5323c1c0^`) і
подивився рядок **1144**, позицію **298** з журналу:
- `addEventListener("click", h)` — позиція **271** (вішаємо на кнопку);
- `let h = ul(async()=>{ … "Перевір адресу пошти" … })` — позиція **363**.
Тобто оголошення на **92 символи НИЖЧЕ** за використання, а `h` — це мініфікований
`doSend`. Рівно вада зі стенда `join-screen-wiring.mjs`.
📐 **Час збігся:** помилки **08:24:31** і **08:24:54** (Київ), фікс змерджено
**08:32:21** — через 8 хвилин. У журналі стоїть «без акаунта» — це й був Вова, який
саме тому й не мав акаунта, що кнопка входу була мертва.
✅ «Востаннє 08:24:54» — після фіксу не повторювалось.

**🟢 `Script error.` `:0:0` — НЕ наш код.** Відповідь браузера на помилку в скрипті з
чужого домену (деталі ховає навмисно). Такий скрипт один: GoatCounter
`//gc.zgo.at/count.js` (`index.html:403`, без `crossorigin`).
🗣️ **Рішення Вови: «якщо це не так важливо, хай буде, будемо працювати далі».**
➡️ Записано в `NOW.md` → «НЕ ЧІПАТИ», щоб наступна сесія не розслідувала це втретє.
⚠️ Там же застереження: `crossorigin="anonymous"` НЕ додавати заради деталей — якщо
домен не віддає CORS-заголовок, скрипт перестане виконуватись і аналітика мовчки помре.

### 2. ✅ ЕКРАН НА КОМПʼЮТЕРІ — ПЕРЕВІРЕНО ПАЛЬЦЕМ

🗣️ Вова, зі знімка Mac: **«qr код працює для компʼютера»**. На екрані — «Застосунок
поки що живе в телефоні», QR і підпис `castlelife.org`.
🔑 Це той самий кодувальник, який мало не поїхав зламаним: **9 точок із 625**, і на
око бездоганний. Спіймала лише звірка з незалежною реалізацією — тепер підтверджено
живою камерою.
➡️ Пункт **0а** закрито, у ➡️ ДАЛІ **першим став B-14** (тематичний фільтр новин).

### Стан git
Після squash-мерджу PR #1117 гілка розійшлась із `main` — це норма. Звірка на дві
крапки показала різницю лише в автокомітах парсерів, тобто вся робота в `main`.
🛑 `git reset --hard` не застосовував (дозвіл відхилено — і правильно): звичайний
`git merge origin/main` розвʼязує це без ризику.

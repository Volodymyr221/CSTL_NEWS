# Журнал сесії — 19.09.2026

> Продовження сесії, що почалась 18.09 «b» (`vova-2026-09-18b.md`, закінчилась
> PR #1185 і деплоєм #6037). Гілка та сама — `claude/startuem-x0odbh`.
> Ведеться ПО ХОДУ, не з памʼяті.

## 🎚 ЗАМОВЛЕННЯ 19.09 — ШКАЛА РОЛЕЙ + СИСТЕМА ОНОВЛЕНЬ («Давай» отримано)

### Рішення Вови (дослівно, по пунктах)

- Назва розділу — **«Оновлення»**; перемикач «дивитись як житель» — **одразу**.
- Вмикати оновлення можуть **власники, розробники та адміни застосунку (НЕ спільнот)**;
  випускати «Усім» — **Власник + CEO + Розробник**.
- 🗣️ «Я мав на увазі **CEO** проекту» (я перепитав — він казав «SEO», а описував
  керівника; назва йде в базу, тож уточнення було обовʼязкове).
- Витрати AI розробнику — **«Давай»** (👁 дивиться, суми не міняє).
- 🗣️ **«треба додати можливість додавати дві ролі на одну людину. Тобто зараз я
  буду власником і буду CEO-проєкту»**.
- 🗣️ Про гнучкість: «треба зробити таку можливість, щоб певна роль мала
  обмеження, але в разі чого можна додати якийсь певний вид».
- 🗣️ Адміни спільнот: «вони взагалі не мають відношення до адмінки… це нижчий
  левел, вони ведуть просто сторінки».

### 🔴 ЩО ЗНАЙШЛОСЬ ПЕРЕВІРКОЮ СКАРГИ «не можу додати редактора»

Вова має рацію, і причина гірша за симптом:
- розділ «Адміни» має форму додавання (`insert into admins` по пошті);
- розділ «Доступи» має ЛИШЕ «Відкликати» і «Повернути» — **додати неможливо**;
- таблиця **`editor_invites` у базі Є**, а в коді (`admin.html`, `src/`) вона не
  згадується **жодного разу** (`grep` → нуль).

➡️ Тобто механізм запрошення редактора збудований наполовину: **сховище є, дверей
немає**, а документація весь час писала «кабінет і механізм запрошення готові».
Той самий клас, що `kb-guard.test.js` — документ обіцяє те, чого в коді нема.

### Модель, яку будую (не шкала, а НАБІР)

Кілька ролей на людину відкидає ідею «рівнів»: сказати «Вова = рівень 1» не можна,
бо він Власник І CEO. Тому:

```
user_roles(uid, role)      — ролей у людини може бути КІЛЬКА
role_perms(role, area, lvl)— заготовка дозволів ролі
user_perms(uid, area, lvl) — ОСОБИСТИЙ виняток, перебиває заготовку
                             (це і є «в разі чого додати певний вид»)
підсумок = НАЙВИЩИЙ рівень серед усіх ролей, потім особистий виняток
```

Рівні: `none` · `view` (бачить, не міняє) · `manage`.
🔑 `view` і є дослівне «він може це бачити, але не видаляти».

🛑 **Особливі права поза розділами:** керувати ВЛАСНИКАМИ — тільки Власник;
випускати «Усім» — Власник/CEO/Розробник.
🛑 **Адміни спільнот (`page_admins`, 8 рядків) у цю систему НЕ входять.**

### Ризик, який лікую сумісністю

`is_admin()` зараз = `exists (select 1 from admins where email = auth.email())`.
Переписати різко = ризик замкнути Вову з адмінки. Тому нова перевірка йде
**АБО** зі старою: поки старий рядок у `admins` живий, доступ тримається навіть
якщо нова гілка помилиться. Прибирання старого шляху — окремим кроком, після
того як Вова підтвердить пальцем.

### ✅ Крок 1 ЗРОБЛЕНО — ролі в базі й адмінці

**База** (`scripts/supabase_roles.sql`, міграції `app_roles_and_permissions` +
`team_list_and_people_search`):
`app_roles` · `user_roles` (кілька на людину) · `role_perms` (заготовка) ·
`user_perms` (особистий виняток) · `my_perm` / `has_perm` / `has_role` /
`is_owner` / `can_manage_owners` / `can_release_update` / `grant_role` /
`revoke_role` / `set_user_perm` / `my_access` / `team_list` / `people_search`.

**Доведено на живій базі транзакціями з відкотом — 14 перевірок:**
Вова = owner+ceo, усі 14 розділів manage · розробник: updates manage,
board/questions/spend/analytics view, people/access none · «дати собі owner» →
відмова · «дати собі admin» → відмова · «дописати собі розділ» → відмова ·
«зняти останнього власника» → відмова.

**Адмінка:** `my_access()` при вході → `permOf()` / `can()` / `hasRole()`;
усі 11 пунктів меню переведені з `show: isAdmin` на `show: can(<розділ>)`;
розділ «Доступи» дістав блок «Команда і ролі» — кнопки ролей (✓ = є), пошук
людей і «+ У команду».
🛑 `can()` при збої `my_access` падає на стару перевірку `isAdmin` — збій мережі
не сміє зачинити кабінет.
🛑 Кнопка ролі, якої людина не може натиснути, ВИМКНЕНА, а не схована: схована
читається як «такого не буває».
🔑 «+ У команду» дає найнижчу роль (редактор) — додати людину і видати їй права
це два різні рішення.

Стенди адмінки після зміни: `admin-access` 46/46 · `admin-shell` 14/14 ·
`admin-agents` 10/10 · `admin-reports` 11/11 · `admin-publish-truth` 26/26 ·
`analytics-screen` 106/106.

⏳ **Не зроблено (крок 2):** розділ «Оновлення» з прапорцями, коло раннього
доступу і перемикач «дивитись як житель».

---

## 19.09 «Фініш і дороби оновлення» — КРОК 2 ЗАКРИТО

**Міграція `feature_flags_and_testers`:** `app_features` (`key`, `label`,
`descr`, `stage`, `released_at`, `changed_by`) · `feature_testers` ·
`my_features()` · `set_feature_stage()` · `set_feature_tester()` ·
`feature_testers_list()` · `register_feature()`. Джерело —
`scripts/supabase_features.sql`.

**Клієнт:** `src/core/features.js` (`featureOn`, `inTestCircle`, `asResident`,
`setAsResident`), виклик `loadFeatures()` в `app.js` після `initSavedHub()`,
пункт «Дивитись як житель» у секції `info` бічного меню.
🛑 Перша спроба поклала пункт у секцію `cards` — там інший шаблон, без
`.sidebar-item-label`, і підпис виходив ПОРОЖНІЙ. Спіймано стендом, не оком.

**Адмінка:** `renderUpdates()` — картки фіч із трьома станами, коло з пошуком
людей, `confirm` на «Усім», нагадування прибрати прапорець через 7 днів.
🛑 Стан НЕ міняється оптимістично: у розділі, який вирішує, що побачить громада,
намальоване «готово» над відмовою сервера — найгірше з можливого.

### 🔴 Вада, знайдена ПЕРЕД деплоєм (і чому вона важлива)

Заміряно перед випуском: `select ... from app_features` → **0 рядків**.
`inTestCircle()` виводив «я в колі» з самих фіч, тож при нулі фіч власник
виглядав як сторонній — пункт «Дивитись як житель» не малювався б узагалі.
Система, замовлена щоб «протестити В РАМКАХ ДОДАТКУ», була б непроверяєма рівно
в мить, коли Вова вперше її відкриє.

✅ Міграція `my_features_reports_circle_membership`: відповідь стала
`{ in_circle, features }`. Доведено відкотними транзакціями на живій базі:
власник `in_circle: true` при нулі фіч, сторонній `false`.

### Сторож

`tests/updates-flags.mjs` **13/13**, пʼять сцен. Дві мутації спіймали дві мої ж
неправди: блок 4 у першій редакції стеріг порожнечу (перевіряв вихід із режиму
на людині В КОЛІ, де запобіжник ні до чого), а перевірка порожньої бази доведена
поверненням старої логіки — **12/13** саме тим рядком.

Сусідні стенди після зміни: `sidebar-menu` 50/50 · `sidebar-account` 11/11 ·
`sidebar-ghost-tap` 17/17 · `sidebar-overlay` 36/36 · `admin-access` 46/46 ·
`admin-shell` 14/14 · `saved-hub` 26/26 · `saved-lifecycle` 18/18 ·
`modal-surface` 13/13 · `drift` 25/25 · `docs-refs` 10/10 · `docs-fresh` 23/23.

---

## 🚀 ФІНІШ (19.09, 10:52 UTC)

**PR #1187** → squash у `main` (`a645608c`) → деплой **#6044**.
`CACHE_NAME` = `cstl-20260919-1048` (свіже значення, а не «вибрати сторону»:
конфлікт у `sw.js` виник саме на цьому рядку).

**Конфлікти при мерджі `origin/main`** — 6 файлів:
- `admin.html` — три хунки виду «мої рядки проти порожнечі з `main`»; лишено мої;
- `sw.js` — `CACHE_NAME`, вирішено новим значенням;
- чотири документи. 🔑 Перед тим, як брати свою сторону, ДОВЕДЕНО, що в
  `origin/main` вони байт у байт мої ж із коміту `6c4b3a94`
  (`git diff 6c4b3a94:<файл> origin/main:<файл>` — порожньо). Інакше це було б
  «вибрати зручніше», а не рішення.

**Журнал за 19.09 винесено в окремий файл** — раніше робота нового дня лежала
всередині `vova-2026-09-18b.md`, і сторож пульсу чесно писав «журнал за
2026-09-19: НЕМАЄ».

---

## 🛡 СТОРОЖ ДИСЦИПЛІНИ ПРАПОРЦІВ (19.09, після фінішу #1187)

🗣️ Вова: «Роби і давай зараз дам тестове завдання і перевірю як це працює, але
спочатку зроби і задеплой сторожа».

**Привід — моє ж зізнання в попередній відповіді:** «якщо я забуду поставити
фічу за прапорець — вона поїде всім одразу, і жодна кнопка в адмінці цього не
спинить. Запобіжника від моєї забудькуватості зараз немає».

**`tests/surface-flags.mjs` — 9/9.** Реєстр поверхонь: 37 штук (17 пунктів
бічного меню + 6 вкладок таб-бара + 14 розділів адмінки). Кожна мусить мати
ЗАПИСАНЕ рішення в одному з трьох списків:
- `ДО_ПРАПОРЦІВ` — жило в застосунку до 19.09 (34 поверхні);
- `ЗА_ПРАПОРЦЕМ` — ключ фічі, і цей ключ **перевіряється в коді**;
- `СВІДОМО_БЕЗ` — з ПРИЧИНОЮ, не коротшою за 40 символів (2 поверхні:
  `as-resident` і `admin:updates` — обидві частини самої системи оновлень, за
  власним прапорцем вони були б недосяжні).

🛑 **Межа, яку стенд не переступає і не вдає, що переступає:** він не вміє
відрізнити «нову» поверхню від «старої» — у коді такої ознаки немає. Він вміє
інше: помітити поверхню, про яку рішення НЕ ЗАПИСАНЕ. Тобто не змушує ставити
прапорець, а змушує подумати і записати відповідь.

🛑 **Свідомо НЕ прив'язано до `origin/main`.** Порівняння «що з'явилось проти
гілки» здавалось очевиднішим і було б пасткою: два мої контролі вже читали стан
гілки і після мерджу почали падати назавжди (правило в `NOW.md`).

### 🧪 Три мутації — прогін, не припущення

1. Пункт «Галерея громади» дописано в `SECTIONS` повз реєстр → **8/9**, падає
   блок 1 із підказкою, що робити.
2. `SECTIONS` перейменовано на `МЕНЮ_РОЗДІЛИ`, тобто прилад ОСЛІП → **7/9**.
   🔑 **Головна знахідка прогону:** блок 1 при цьому лишився ЗЕЛЕНИЙ —
   «перевірено 20 поверхонь» замість 37. Без блоку 0 («прилад бачить те, що
   заявляє») стенд мовчки стеріг би дві третини застосунку і рапортував успіх.
   Це рівно той клас брехні, який у цьому проєкті ловили вже чотири рази.
3. У `ЗА_ПРАПОРЦЕМ` вписано ключ, якого в коді немає → **8/9**, падає блок 4.

### Дрібниця, що ледь не стала вадою

Перша редакція обгортала `projectFile(...)` в `readFileSync` — а `projectFile`
віддає ВМІСТ, не шлях. Впало одразу на першому прогоні, але варте запису: той
самий виклик у `_lib.mjs` вміє читати ще й версію з git через другий аргумент,
і саме тому контроль тут вмикається змінною `SURFACES_REV`, без жодного
`git show` усередині стенда.

🛑 **`CACHE_NAME` НЕ чіпав** — свідомо. Змін у коді застосунку немає (новий файл
лежить у `tests/`, у бандл не потрапляє). Зайвий бамп змусив би всі телефони
перекачати кеш ні за чим.

159 стендів.

---

## 🦴 ТЕСТОВЕ ЗАВДАННЯ ВОВИ: КІСТЯК ЗАВАНТАЖЕННЯ ПІД ФОРМУ КОЖНОЇ ВКЛАДКИ

🗣️ Зі знімків: «зроби сторінку завантаження кожної сторінки в такому вигляді як
сторінка, до прикладу стрічка зараз завантажується як список, а може так як
елементи на стрічці… І так з кожною вкладкою треба зробити, взяти макет сторінки
і створити сторінку завантаження».

**Це ПЕРША фіча, що їде через систему оновлень** — прапорець `skeleton_shapes`,
зареєстрований у стані `off`.

### Що було не так

Кістяк був ОДИН на всі екрани: «фото 64px ліворуч + три смужки». А `screen-state.js`
при цьому запевняв у коментарі, що «скелет повторює ФОРМУ того, що приїде».
Намір правильний, виконання — ні. Той самий клас вади, що вже ловили двічі:
коментар описує намір, а не те, що код робить.

### Як знімав форму — не з голови

`tests/tools/skeleton-shapes.mjs` (новий разовий інструмент) обходить вкладки із
заглушкою і друкує дерево DOM із розмірами. 📐 Заміряно: картка Дошки 366×125 ·
рядок Питання 358×165 · картка рейсу 390×110 · банер автобусів 362×168 · кільце
Стрічки 62×62 у колонці 96px · смужка тижня 67 · пошук автобусів 60.

### Зроблено

- `src/core/skeletons.js` — форми під Стрічку, Дошку, Питання, Автобуси, Громаду;
- `style/base.css` — розміри кістяків (+86 рядків);
- `screen-state.js` — `loadingHtml(rows, kind)` за прапорцем; без прапорця стара форма;
- виклики: `feed.js` (`'feed'`), `board.js` (`discOpen ? 'questions' : 'board'`),
  `buses.js` (новий кістяк — до цього вкладка була просто порожня під час fetch).

🛑 **Громаду НЕ чіпав:** її блоки вже мають власні кістяки під свою форму
(`skeletonNews`, `skeletonRows`), зроблені свідомо і з коментарем.

### 🔴 Три вади, знайдені ПО ХОДУ (усі — мої)

1. **Прапорець і кістяк не зустрічались.** Кістяк малюється ДО мережі (в цьому
   його сенс), а `my_features()` приходить З мережі. Тобто в мить малювання ми
   ще не знаємо про прапорець, і перша ж фіча не вмикалась на екранах, заради
   яких робилась. Питання працювали ЛИШЕ тому, що відкриваються пізніше.
   ✅ Два запобіжники: кеш прапорців у `localStorage` (другий запуск уже
   правдивий) + перемальовка кістяка на подію `cstl-features-ready`.
2. **Кільця спільнот вийшли КВАДРАТНИМИ.** `.hm-sk` (home.css) дає
   `border-radius: 6px`, а home.css підключається після base.css — однакова вага
   селектора, виграє пізніший. ✅ `.hm-sk.sk-circle-ring` додає ваги, не чіпаючи
   закріплений порядок імпортів. 🔑 Спіймано ОКОМ на знімку, не стендом.
3. **Хром списків приходить разом зі списком.** Я вважав, що смужка тижня й пошук
   Автобусів, заголовок і пошук Дошки та Питань стоять з коду. Ні: `paintLoading`
   затирає корінь цілком, а хром малюється в тому ж `innerHTML`, що й дані.
   Без нього список стрибав би на 127px (Автобуси) і на 84-156px (Дошка,
   Питання) — рівно те, від чого кістяк рятує. ✅ Хром тепер у кістяку.

### Сторож

`tests/skeleton-shapes.mjs` — **10/10**. Чотири блоки: `off` показує старе ·
кожна вкладка зі своєю формою · форми різні між собою · **висота кістяка проти
висоти справжньої картки**.

🧪 Три мутації: знято перевірку прапорця → 9/10 · усі форми підмінено формою
Дошки → 6/10 · кістяк Дошки сплющено → 9/10 (розбіжність 82%).
🛑 **Чесно про межу приладу:** спроба зменшити лише фото кістяка з 96 до 18px
залишила стенд ЗЕЛЕНИМ (111 проти 109px) — висоту тримає текстовий стовпчик, а
не фото. Блок 4 стереже ПОРЯДОК ВЕЛИЧИНИ, а не точність до пікселя, і видавати
його за друге не можна.

### Стан

Прапорець `skeleton_shapes` = **`off`**. Код у проді мертвий, громада бачить
старе. Наступний крок — Вова вмикає «Лише колу» в адмінці й дивиться на телефоні.

160 стендів.


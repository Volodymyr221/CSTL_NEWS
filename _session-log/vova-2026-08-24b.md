# Журнал сесії — 24.08.2026 (сесія «b», акаунт 2)

> Живий журнал. Дописується ПО ХОДУ після кожної значущої дії.
> Гілка: `claude/startuem-upyy76` · production: `main`

---

## Старт сесії (`/startuem`)

- Підписав коміти Вовою (`user.name=Вова`, `user.email=vitocorleone191@outlook.com`).
- `git fetch origin` — гілка `claude/startuem-upyy76` **рівна `origin/main`**:
  обидві звірки (`origin/main ^HEAD` без `auto(` і `HEAD ^origin/main`) порожні.
  Тобто нічого чужого не пропущено і нічого свого не загублено.
- Хук SessionStart попередив, що ЛОКАЛЬНИЙ `main` відстає на 50 комітів — це не
  про робочу гілку, робоча гілка свіжа.
- Останній PR у `main`: **#1011** (пульс після передачі). Перед ним #1010, #1009.
- `_ai-tools/BYYOU_PLAN.md` — статус `idle`, незавершеного потоку немає.
- Журнал за сьогодні вже був (`vova-2026-08-24.md`), тож ця сесія бере літеру **b**
  (правило №13: перед створенням дивитись, які літери зайняті).

## Прочитано (обовʼязковий список `/startuem`)

`NOW.md` · `HOT_RULES.md` (14 правил) · `CSTL_BUGS.md` · `BACKLOG_VOVA_2026-08.md`
· `ВОВА_ПРОФІЛЬ.md` · `_ai-tools/NEW_SESSION_PROMPT.md` + шапка `AUDIT_2026-07.md`.

**Стан на вході:** MVP вкладки «Питання» закрито повністю (6/6 кроків, останній —
тип 4 «питання без відповіді»), account-scoped state закрито, мітки «бачив»
синхронні між пристроями (PR #1009). Далі за планом — не нова логіка, а
підготовка до запуску.

## Чекаю «Роби»

Код не чіпаю до явного слова Вови.

---

## Хід роботи

_(дописується по ходу)_

### 23:35 — Вова відповів на три відкриті питання

**Дослівно:**
1. «Живий прохід push у Питаннях на двох акаунтах — **Все окей**»
2. Тип 4 об 11:00 — «**Ще не бачив**»
3. Android — «**Протестуємо коли доробимо все**»

**Що з цього випливає і що зроблено (тільки документи, коду не чіпав — «Роби» не було):**

- **(1)** Закрито останній недоказаний шматок MVP «Питань». Рядок «Живий прохід
  push — не перевірено пальцем» знято з «НЕ ДОРОБЛЕНО» у `NOW.md`.
  ⚠️ **B-33 цим НЕ закритий** — «все окей» сказано про прохід push, а не про
  вимикачі. Питання про вимикачі лишив відкритим і написав у `NOW.md`, чому саме.
- **(2)** Перевірив, чи це поломка. **Не поломка.** Розклад `qa-unanswered-push`
  = `0 8,9 * * *` **UTC** (`scripts/supabase_unanswered_push.sql:104-106`), а
  функція зʼявилась ~20:00 UTC 24.08. Тобто сьогоднішній дзвінок о 08:00 UTC
  стався **за 12 годин до деплою**. Найближче справжнє спрацювання —
  **25.08, 08:00 UTC = 11:00 Києва**. Записав це в `NOW.md` окремим рядком, щоб
  наступна сесія не полізла лагодити справний код.
  🔑 Урок: «ще не бачив» про річ на розкладі звіряй із часом ДЕПЛОЮ, а не лише
  з часом розкладу.
- **(3)** Android переведено з «борг» у «свідомо відкладено рішенням Вови».
  Рядок у «НЕ ДОРОБЛЕНО» лишається (факт чинний), із черги «ДАЛІ» знятий.

**Змінені файли:** `CSTL NEWS VOVA/NOW.md` (проза під AUTO-блоком: відкриті
питання, таблиця «НЕ ДОРОБЛЕНО», розділ «ДАЛІ») ·
`CSTL NEWS VOVA/_ai-tools/SESSION_STATE_VOVA.md` (новий розділ сесії «b») ·
цей журнал. **Тільки `.md` — `CACHE_NAME` НЕ чіпав** (правило №2).

---

## Робота: картка питання — остання коренева відповідь + проміжок

**«Роби» від Вови** (дослівно, після обговорення з участю GPT):

> «ми не можемо налаштувати так, щоб воно відразу давало першу кореневу
> правильну відповідь. Воно може бути частковою, і це можуть бути догадки…
> А так ми будемо бачити останнє повідомлення. Але в нас є відповіді і коментарі
> на відповіді. Коментарі нам не потрібно писати. Нам потрібні тільки відповіді…
> відповіді на ці коментарі — це вже така підгрупа, яку по факту не потрібно там
> висвітлювати.»

І окремо, про підхід узагалі:

> «Ми не маємо будувати латками… все має бути взаємозв'язано і логічно. Якщо
> якийсь користувач пише коментар, це відразу лічильник додає показує іншій
> людині. Бери до прикладу, як працюють соцмережі і роби так само.»

Плюс відповідь на моє питання про лічильник: **варіант 1** — рахувати тільки кореневі.

### Мій аргумент, який програв (записую чесно)

Я захищав ПЕРШУ відповідь: «у Q&A перша майже завжди і є відповіддю». Вова
розбив це одним фактом: **ми не вміємо довести, що вона правильна** — позначки
«корисна/вирішено» в проєкті немає. Тобто «перша» не давала гарантії якості й
при цьому втрачала свіжість. Платили, не отримуючи.

### Що зроблено

🔴 **Головне — не сама зміна, а ЗНЯТТЯ ЛАТКИ.** «Скільки відповідей» рахувалось
у **чотирьох** місцях незалежно, кожне своїм `activeComments(...).length`:
`answersCount()` · картка · шапка екрана питання · модалка. Поки визначення було
спільне, вони сходились ВИПАДКОВО. Змінити його в одному місці = мовчазний
розхід — рівно баг B-27, про який попереджає сам файл.

**`src/tabs/board-discussions.js`:**
- `rootPredicate(items)` + `rootAnswers(postId)` — ОДИН опис того, що таке
  «відповідь на питання». `answersHtml` тепер кличе той самий предикат замість
  власної копії правила.
- `lastRootAnswer(postId)` — найновіша коренева. 🛑 Береться **максимумом за
  часом**, а не останнім елементом масиву: оптимістична вставка (своя відповідь
  малюється до підтвердження сервера) дописує в кінець, тож позиція бреше, а час ні.
- `answersCount()` → `rootAnswers().length`. Три прямі підрахунки замінено на
  виклик цієї функції — тепер лічильник на весь застосунок ОДИН.
- Картка: `відповіді[0]` → `lastRootAnswer(p.id)`.

**`style/board.css`** — `.qa-row-answer-text`: `flex: 1` → `flex: 0 1 auto`.
📐 Проміжок не був заданий числом, він був ЗАЛИШКОМ: `flex-grow: 1` = «забери
весь вільний простір», тож при короткій відповіді («Взавтра») коробка займала
майже всю ширину картки, а автор притискався до правого краю.

### 🔑 Розділення двох осей (щоб не було латки)

| ось | що рахує | де живе |
|---|---|---|
| **«відповіді»** — число і цитата | ТІЛЬКИ кореневі | `answersCount`, `lastRootAnswer` |
| **«активність / нове»** | будь-який коментар | `lastAnswerTs`, крапка вкладки |

Тому питання підіймається у списку і від репліки теж (там справді щось сталось),
а слово «відповіді» лишається строгим.

### ✅ Сумісність доведена, а не припущена

Поки є хоч один живий коментар, коренів теж ≥1 (сирота вважається коренем).
Тобто **«0 відповідей» означає рівно те саме, що й раніше** — і чіп «Без
відповіді», його лічильник у шапці, капсула Громади (`home-caps.js:755`) і push
«Потрібна відповідь» (тип 4) працюють без жодної зміни.

### 🔴 Сторож проходив і ДО, і ПІСЛЯ — це була дірка

`tests/questions.mjs` дав 83/83 на новому коді **і давав би на старому**: у
фікстурі була рівно ОДНА коренева відповідь, тож «перша» і «остання» — той самий
рядок. Перевірка, яка не може впасти, не перевірка.

**Сцену перебудовано так, щоб вона розрізняла ТРИ редакції:**

| редакція | що покаже картка | |
|---|---|---|
| стара («перша») | «Начебто 24 серпня.» | ✗ |
| наївне «останній коментар» | «Так, підтверджую…» (репліка!) | ✗ |
| правильна («остання коренева») | «Афішу вже повісили біля клубу.» | ✓ |

Додано 5003 (друга коренева, новіша) і 5002 зроблено **найновішим коментарем
узагалі** — пастка на наївний варіант. Три нові перевірки: 3ґ (остання коренева),
3ґ2 (репліка не потрапляє ніколи), 3ґ3 (лічильник 2, а не 3).

**Стенд 85/85. Контроль `BUNDLE_REV=CSS_REV=HTML_REV=origin/main` → 82/85** —
падають рівно 3ґ, 3ґ3 і 13з.

### ⚠️ Два падіння, які влаштувала МОЯ фікстура (не код)

- **5н** «немає HH:MM у відповідях» — я написав у тексті сцени «24-го о **19:00**»,
  і сторож справедливо це впіймав. Текст змінено на «біля клубу».
- **13з** «у цитаті імʼя без прізвища» — автором цитати став 5003, а я дав йому
  імʼя **без** прізвища. Перевірка стала б перевіряти найлегший випадок. Автор
  тепер «Оксана Ковальчук», очікування в 13з — «Оксана».

🔑 Обидва — той самий клас, що описаний у правилах: перевірка мусить міряти
складний випадок, інакше вона зеленіє ні над чим.

**`CACHE_NAME`** → `cstl-20260824-2108` (змінювався код).

### 🔴 Сторож проміжку — і як він СПЕРШУ не міг упасти

Скарга Вови була саме про **щіль**, а не міряв її ніхто. Додав `3ґ4`.

⚠️ **Перша редакція перевірки була брехливою.** Вона міряла штатний текст сцени
(«Афішу вже повісили біля клубу.» = **219px у коробці 221px**) — рядок і так
майже повний, розтягуватись `flex-grow` нікуди. Контроль `CSS_REV=origin/main`
дав **7px і зелень**, тобто перевірка світилась над зламаним кодом.

🔑 **Корінь:** вада видима лише тоді, коли в рядку Є вільне місце. Саме такий
випадок Вова й обвів на знімку — «Взавтра», сім літер. Тепер проба підставляє
рівно цей текст, міряє і повертає сцену як була.

📐 **Заміряно:** щіль **5px** (текст 56px у коробці 56px) проти **170px** на
`origin/main` (текст 56px у коробці 221px). Це і є та сама «велика щіль» у числах.

🛑 **І окремо про сам вимір:** відстань береться від **кінця намальованого
тексту** (через `Range`), а не від краю коробки. Коробка стояла впритул до
автора ЗАВЖДИ — вимір «край коробки → автор» показав би 5px і на зламаному коді.
`scrollWidth` теж не годиться: при коробці ширшій за вміст він віддає ширину
КОРОБКИ.

**Стенд `questions.mjs` — 86/86. Контролі:**
- `BUNDLE_REV=CSS_REV=HTML_REV=origin/main` → 82/85 (падають 3ґ, 3ґ3, 13з)
- `CSS_REV=origin/main` (сам CSS) → 85/86 (падає 3ґ4)

### ⚠️ Інцидент: контейнер перезапустився

Запустив другий браузерний прогін ПОВЕРХ повного набору — не вистачило памʼяті,
процес убито (код 137), контейнер перезапустився. **Нічого не втрачено — усе
було запушене** (правило №11 відпрацювало). Урок практичний: два Playwright
одночасно в цьому контейнері не живуть.

---

## Робота 2: відповіді на відповіді · лінія · сортування

**«Роби»** — три речі зі скріншотів, плюс дві відповіді Вови: колір **28%**,
**Стрічку не фарбуємо**.

### 1. «Відповісти» на КОЖНІЙ відповіді

Скарга: «я відповідаю на цю відповідь, і автор мені не може відповісти, тому що
немає кнопки… все прив'язано тільки до одної відповіді на коментар».

Причина була в рядку `board-discussions.js:432` з поясненням «інакше з другого
рівня росло б дерево». 🔑 **Страх чинний, але захист від нього вже стояв у тому
самому файлі** — підняття відповіді до кореня гілки. Тобто кнопки не було, а
захист був: ми платили функцією за ризик, якого не існувало.
🔑 Адресація не вигадується: `reply_to_id` = id тієї репліки, на яку тиснули, і
той самий рядок дає і згадку («Марія, …»), і місце в гілці. У «Стрічці» це
працює з липня (`feed.js:1459`, `data-reply-parent` + `data-reply-uid`).

### 2. 🛑 ВАДА, ЯКУ МОЯ Ж КНОПКА ВІДКРИЛА Б

Підняття до кореня робило **рівно один стрибок**. Ланцюг `R ← A ← B ← C`:
у C батько B, у B батько A, A живий → `parent = A`. **Але A сам не корінь**,
у `roots` його немає, і `subsOf.get(r.id)` до нього не доходить НІКОЛИ.

🔴 **Відповідь зникала б з екрана мовчки** — без помилки, без порожнього місця.
Людина написала, побачила свій текст оптимістично, а після перемальовування його
нема. Тепер лізем угору до справжнього кореня; стеля 32 кроки — захист від циклу
в даних (без неї биті дані повісили б малювання назавжди).

📐 **Доведено контролем:** на коді з `main` стенд малює **4 відповіді з 5**.

### 3. Лінія — правил було ТРИ, а скопіювали два

Скарга: «її погано видно» + «вона має бути з'єднана».

🔑 У Питаннях був стовбур уздовж кореня і гачок до кожної відповіді, а **третього
правила — того, що зшиває гачки між собою, — не було ВЗАГАЛІ**. Тому при двох і
більше відповідях лінія розпадалась навпроти кожного аватара. У «Стрічці» цю саму
ваду лікували 26.07 словами самого Вови: «суцільна лінія, яка заокруглюється
навпроти коментаря». Числа взяті звідти, де вони виміряні.

📐 **Колір, заміряно контрастом до фону `#ECEEF1`:**

| | контраст |
|---|---|
| було `#E6E7EB` | **1.06:1** — фактично невидима |
| стало `#CAB9BD` (бордовий 28%) | **1.62:1** |
| суцільний `--red` | **8.30:1** — як ОСНОВНИЙ ТЕКСТ |

🛑 Суцільний відхилено і Вова погодився: лінія повторюється десять разів згори
донизу і почала б змагатися зі згадкою адресата, а саме згадка і є смисловим
звʼязком «хто кому». **Стрічку не фарбуємо** — токен `--qa-line` вживається
ЛИШЕ цим звʼязувачем (звірено), тож зміна фізично не протікає.

### 4. Список — за датою ПИТАННЯ

Слова Вови: «саме перше питання — це питання, яке було опубліковане 11 серпня, а
друге — 11 годин тому… це вже починається хаос».

🔑 **Вада була не в осі сортування, а в РОЗХОДЖЕННІ:** картка показує дату
питання, а порядок будувався за датою останньої відповіді. Порядок неможливо
було пояснити нічим, що видно на екрані.
⚠️ Ціна названа і прийнята: питання, на яке щойно відповіли, більше не
підіймається. «Де щойно відповіли» лишається крапкою «нове» на вкладці.
🛑 `lastAnswerTs()` не видаляв — вона й далі описує живість; прибрав лише її
виклик і мертвий імпорт у `board.js`.

### 🔴 ДВІ ПЕРЕВІРКИ, ЯКІ СПЕРШУ НЕ МОГЛИ ВПАСТИ

Перший контрольний прогін дав 89/92 — тобто дві нові перевірки зеленіли й на
старому коді:

1. **Контраст (7.7в).** Контроль підміняв `style/board.css`, а токен `--qa-line`
   живе в **`style/base.css`**, який не підмінявся. ➡️ Додано **четвертий шар
   контролю** `BASE_REV`. Правило проєкту: контроль вимагає стільки змінних,
   скільки шарів зачепила зміна.
2. **Зникнення відповіді (7.6б).** Я поклав у фікстуру ТРИ рівні, а вада вилазить
   на **ЧЕТВЕРТОМУ**: з трьох рівнів один стрибок якраз доводить до кореня.
   ➡️ Додано 5005 → 5004 → 5002 → 5001.

**Стенд `questions.mjs` — 92/92. Контроль на всіх чотирьох шарах → 85/92**
(падають усі сім нових перевірок).

**`CACHE_NAME`** → `cstl-20260825-0058`.

### ✅ ОПУБЛІКОВАНО

| | |
|---|---|
| PR | **#1013** (squash) |
| коміт у `main` | `67cba25d` |
| деплой | **#5035** — `success`, 25.08 05:47 UTC (08:47 Києва) |
| `CACHE_NAME` | `cstl-20260825-0058` |

**Прогони перед мерджем:**
- `questions.mjs` — **92/92**
- контроль на ЧОТИРЬОХ шарах (`BUNDLE_REV` + `CSS_REV` + `HTML_REV` + `BASE_REV`)
  → **85/92**, падають усі сім нових перевірок
- повний набір `npm test` → **102/102 стендів · 2042/2042 перевірок**

⚠️ **Повний набір довелось ганяти ЧОТИРИ рази, і три з них були недостовірні —
з моєї вини.** Записую, бо це коштувало ночі:

1. прогін стартував ДО злиття з `main` і до фіксу пульсу — файли мінялись під
   ним, результат нічого не означав;
2. перезібрав `bundle.js` посеред наступного прогону — та сама помилка вдруге;
3. `pkill -f "node tests"` **убив власну оболонку**: рядок команди, яку я
   запускав, сам містив цей текст, тож pkill збігся з нею (код 144);
4. четвертий помер разом із перезапуском контейнера на 50 стендах.

🔑 **Правило, куплене цією ніччю: поки повний набір іде — ФАЙЛІВ НЕ ЧІПАТИ.**
Жодних перезбірок, жодних правок документів. І не запускати другий браузерний
прогін поверх першого — памʼяті контейнера на два Playwright не вистачає
(контейнер уже падав від цього о 21:0x).

---

## Робота 3: антиспам — дубль дістав вікно

**«Роби»** після скарги Вови: «я вчора написав повідомлення, сьогодні зранку не
можу те саме слово написати… зроби просто систему антиспаму, щоб не можна було
спамити швидко — за 15 секунд десь, чи за 10 секунд».

### 🔴 Причина була СТРУКТУРНА, а не випадкова

Правил було два, і вікно мало лише одне з них:

| правило | вікно |
|---|---|
| рейт-ліміт (8 повідомлень) | **15 секунд** ✅ |
| дубль (той самий текст) | **немає ЖОДНОГО** ❌ |

Порівнювався останній надісланий текст, і збіг блокував відправку **назавжди**,
поки людина не напише в тому ж місці щось інше.

🔑 **Наслідок, який робить ваду болючою саме там, де найменше очікуєш:**
найкоротші й найприродніші відповіді — «Окей», «Дякую», «Так» — блокувались
**НАЙНАДІЙНІШЕ**, бо саме їх і пишуть повторно.

🔴 **І гірше, ніж у скарзі:** у Дошці й Питаннях база порівнювала з останнім
текстом **по всьому застосунку**, без прив'язки до питання (`where sender_uid =
NEW.sender_uid order by created_at desc limit 1`). Тобто «Окей» в одному питанні
блокував «Окей» у будь-якому іншому.

### Стало — одне правило одним числом

> **За 15 секунд:** не більше 8 повідомлень і не двічі той самий текст.

Вікно взято ТЕ САМЕ, що вже мав рейт-ліміт. Два різні числа довелось би
пояснювати і людині, і наступній сесії.

### 🛑 ШАРІВ ДВА, І ОБИДВА ЗМІНЕНІ

Якби змінив лише застосунок — людина бачила б «надіслано», а база мовчки
відхиляла б (той самий клас, що HOT_RULES №11-БІС).

- **клієнт** — `src/core/utils.js`, `DUP_WINDOW = 15000`. Запис тепер
  `{t, at}` замість голого рядка. ⚠️ **Старий формат (рядок без часу) більше НЕ
  блокує** — інакше людина лишилась би замкненою рівно там, де скаржилась;
- **база** — міграція `antispam_duplicate_gets_time_window`, обидва тригери
  (`comments_antispam`, `page_comments_antispam`);
- **`scripts/supabase_antispam_shared.sql`** приведено у відповідність, щоб
  правило не жило лише в базі (25.07 тригер антиспаму вже так губився).

🔑 **Порядок був безпечний:** база стала МʼЯКШОЮ (дозволяє те, що раніше
блокувала), тож старий застосунок із нею не конфліктує. Навпаки було б небезпечно.

### ✅ Доведено на ЖИВІЙ базі, транзакцією з відкотом

| перевірка | результат |
|---|---|
| дубль **годину тому** | пропущено ✅ |
| **миттєвий** повтор | заблоковано **антиспамом** ✅ |
| інший текст одразу | пропущено ✅ |

⚠️ **Перша редакція цієї перевірки збрехала:** показала «заблоковано ✅», а
блокував насправді **зовнішній ключ** на вигаданого користувача, не антиспам.
Полагоджено: беремо справжній uid і звіряємо, чи `SQLERRM` містить `antispam`.
🔑 Перевірка, що зеленіє з чужої причини, доводить ніщо.

### 🛑 СТОРОЖА НА ЦЕ НЕ БУЛО ВЗАГАЛІ

`grep isDuplicateMsg tests/` давав **нуль**. Правило, яке щодня стоїть між
людиною і кнопкою «Надіслати», жило без жодної перевірки.

Заведено **`tests/antispam-window.mjs`** (стендів стало 103). Міряє НАСЛІДОК, а
не функцію: `isDuplicateMsg` лежить у бандлі й назовні не виставлена, а
виставляти її заради тесту означало б міняти код заради перевірки. Тому сцена
йде шляхом людини — пише відповідь, дивиться на тост і на список; годинник
підмінено (`clock.install`), бо половина перевірок про ЧАС.

⚠️ **Перша редакція стенда рахувала НАКОПИЧЕНУ суму** відповідей — одне падіння
зсувало числа трьох наступних перевірок, і три червоніли через одну ваду.
Переведено на **ПРИРІСТ**, тепер кожен крок незалежний.

**5/5, контроль `BUNDLE_REV=origin/main` → 3/5** (падають рівно перевірки 3 і 5 —
обидві про вікно).

**Повний набір:** 102/103 → єдине падіння `docs-fresh` (авто-блок пульсу відстав
від git, бо комітив після оновлення) → оновлено, **20/20**.

**`CACHE_NAME`** → `cstl-20260825-0634`.

### ✅ ОПУБЛІКОВАНО (антиспам)

| | |
|---|---|
| PR | **#1014** (squash) |
| коміт у `main` | `d30960a2` |
| деплой | **#5039** — `success`, 25.08 07:03 UTC (10:03 Києва) |
| `CACHE_NAME` | `cstl-20260825-0634` |
| міграція бази | `antispam_duplicate_gets_time_window` — накатана ДО мерджу |

🔑 **Порядок «спершу база, потім застосунок» був свідомий:** база стала
МʼЯКШОЮ (дозволяє те, що раніше блокувала), тож старий застосунок із нею не
конфліктував — він просто ще сам не пускав повтор. Навпаки було б небезпечно:
застосунок казав би «надіслано», а база мовчки відхиляла б.

---

## Робота 4: лінія чорна + згортання гілок

**«Роби»** + вибір Вови: **28%**.

### 1. Колір: бордовий → чорний

Слово Вови після живого екрана: «**лінію з бордової треба замінити на чорну**».
Тобто рішення про ВІДТІНОК змінилось (те саме, яке він сам просив уранці), а
про вагу — ні: лишили ті самі 28%.

📐 Заміряно контраст до фону `#ECEEF1`:

| | контраст |
|---|---|
| `#E6E7EB` (до 25.08) | 1.06:1 — фактично невидима |
| `#CAB9BD` бордовий 28% | 1.62:1 — прожив пів дня |
| **`#AAABAE` чорний 28%** | **1.98:1** — чинний |
| суцільний чорний | **18.07:1** — ВТРИЧІ гучніше за основний текст |

🛑 Суцільний відхилено з тієї самої причини, що й суцільний бордовий: лінія
повторюється десять разів згори донизу і змагалась би зі згадкою адресата.

### 2. Згортання — ПЕРЕНЕСЕНО, а не написано заново

Слово Вови: «чому відповіді на коментарі не згортаються так як в стрічці? Це ж
по факту система та сама, логіка та сама, **візьми все те саме і перемісти
сюди**». Він має рацію буквально — механізм у «Стрічці» живе з 26.07.

Перенесено: `QA_REPLIES_VISIBLE = 1` (його ж рішення 26.07) · «Ще N відповідей»
/ «Сховати відповіді» · стан у памʼяті модуля, не в DOM · при відкритті питання
гілки згорнуті.

🔑 **Кнопка САМА є `.qa-branch`** — тому отримує гачок на лінії й потрапляє під
правило «стовбур наскрізь». Поза гілкою лінія обірвалась би на останній видимій
відповіді, а кнопка висіла б збоку.

🔴 **Дві пастки прокрутки перенесені РАЗОМ із кодом, а не відкриті заново:**
- при ЗГОРТАННІ тримаємось за саму КНОПКУ, а не за корінь гілки — кнопка стоїть
  у кінці, тож після згортання прокрутці нема куди рухатись, щоб повернути
  далекий корінь (у «Стрічці» заміряли розбіжність **464px**);
- висоту списку НЕ анімуємо — саме вона давала ривок, через який 25.07 роботу
  вже відкочували.

➕ **Третє, чого Вова не питав, але воно з тієї ж сімʼї:** відповів у ЗГОРНУТУ
гілку → розгортаємо її КОРІНЬ **до** перемалювання. Інакше власна відповідь
сховалась би під «Ще N» і вийшло б «я написав, а нічого не зʼявилось».
⚠️ Саме КОРІНЬ, а не `replyId`: відповідь на вкладену лягає в гілку її кореня.

### 🔴 ПАСТКА, ЗЛОВЛЕНА В СЕБЕ: типи ключів

`comments.id` у базі це `bigint`, тобто в JS приходить **числом**, а з `dataset`
вертається **рядком**. Поклав би в набір число, а питав рядком — `has()` не
збігся б НІКОЛИ: гілка розгорталась би на один рендер і мовчки згорталась
назад. Заведено `qaKey()` — одна форма скрізь.

### ⚠️ ДВІ ВЛАСНІ ПЕРЕВІРКИ, ЯКІ СПЕРШУ НЕ МОГЛИ ВПАСТИ

1. **Відтінок.** Поставив поріг «різниця каналів ≈ 0» — і перевірка впала на
   ПРАВИЛЬНОМУ коді (`#AAABAE` дає R−B = −4). Причина: прозорий шар успадковує
   відтінок того, що під ним, а фон сам синюватий (R−B = −5). Правильна міра —
   різниця ВІДНОСНО фону: чорна лінія 1, бордова була 22.
2. **«Показує всі три».** Зеленіла й на старому коді, де гілка розгорнута
   завжди. Додано умову «згорнуто < розгорнуто» — тап мусить саме ЗМІНИТИ
   картину, а не збігтися з нею.

**Стенд `questions.mjs` — 98/98. Контроль на чотирьох шарах → 90/98** (падають
усі вісім нових). **Повний набір: 102/103** — єдине падіння `docs-fresh` (пульс
відстав від git) → оновлено.

**`CACHE_NAME`** → `cstl-20260825-0857`.

### ✅ ОПУБЛІКОВАНО (лінія + згортання)

| | |
|---|---|
| PR | **#1015** (squash) |
| коміт у `main` | `33ea4938` |
| деплой | **#5044** — `success`, 25.08 09:27 UTC (12:27 Києва) |
| `CACHE_NAME` | `cstl-20260825-0857` |

---

## 📋 ПІДСУМОК СЕСІЇ «b» (24-25.08)

| PR | деплой | що |
|---|---|---|
| **#1012** | #5015 | картка питання: остання коренева відповідь + ОДИН лічильник на застосунок |
| **#1013** | #5035 | відповіді на відповіді · лінія суцільна · список за датою питання |
| **#1014** | #5039 | антиспам: дубль дістав вікно 15с (+ міграція бази) |
| **#1015** | #5044 | лінія чорна 28% + згортання гілок як у «Стрічці» |

**Стендів: 102 → 103** (додано `antispam-window.mjs`).
**`questions.mjs`: 83 → 98 перевірок.**

### 🔴 ТРИ ВАДИ, ЯКИХ НІХТО НЕ ПРОСИВ ЛАГОДИТИ — ЗНАЙШЛИСЬ ПО ДОРОЗІ

1. **Чотири незалежні лічильники відповідей** сходились ВИПАДКОВО (ризик B-27).
2. **Підняття до кореня на ОДИН щабель** — щойно зʼявилась кнопка «Відповісти»
   на вкладених, відповідь четвертого рівня почала б **зникати мовчки**.
3. **Дубль в антиспамі без вікна** — блокував повтор НАЗАВЖДИ, і саме
   найкоротші відповіді («Окей», «Дякую») страждали найбільше.

### ⚠️ СІМ ВИПАДКІВ «ПЕРЕВІРКА НЕ МОЖЕ ВПАСТИ» ЗА ОДНУ СЕСІЮ

Усі спіймані КОНТРОЛЬНИМИ прогонами, жоден — мною при написанні:
- фікстура з ОДНІЄЮ кореневою відповіддю (перша = остання);
- щіль мірялась на ДОВГОМУ тексті (рядок і так повний);
- контроль не підміняв `base.css`, де живе токен кольору → **четвертий шар**;
- вада зникнення вилазить на ЧЕТВЕРТОМУ рівні, не на третьому;
- на живій базі «заблоковано ✅» давав **зовнішній ключ**, не антиспам;
- поріг відтінку «R−B ≈ 0» завалював ПРАВИЛЬНИЙ код;
- «показує всі три» істинне й на старому коді, де гілка розгорнута завжди.

🔑 **Висновок сесії:** контрольний прогін коштує дві хвилини і зловив СІМ
брехливих перевірок. Без нього всі сім лежали б у репозиторії як «захист».

### 🛑 ПРАВИЛА, КУПЛЕНІ ЦІЄЮ СЕСІЄЮ

- **Поки повний набір стендів іде — файлів НЕ чіпати.** Три прогони з чотирьох
  були недостовірні (правки під працюючим прогоном, другий Playwright поверх
  першого поклав контейнер, `pkill -f "node tests"` убив власну оболонку).
- **Ключі наборів — завжди рядки.** `bigint` у базі → число в JS, `dataset` →
  рядок; `has()` не збігається НІКОЛИ, і нічого при цьому не падає.
- **Прозорий шар успадковує відтінок фону** — «нейтральність» міряти ВІДНОСНО
  фону, а не абсолютно.
- **Фільтр «чужого в `main`» має ловити й `ai(`**, не лише `auto(`.

---

## Робота 5: тап по імені відкриває картку профілю

**Скарга Вови:** «в стрічці в коментарях я можу тапнути на імʼя і відкрити
профіль… у Питаннях я цього не можу, **ти не все скопіював, візьми повністю
всю логіку**».

### Причина простіша, ніж здавалась

🔑 Механізм **спільний і вже стояв**: делегат `initProfileCardTaps`
(`core/profile-card.js`) слухає `[data-av-uid]` на `document`. Бракувало самого
**атрибута** на іменах.

⚠️ І це пояснює, чому виглядало дивно: **аватар його вже мав** (ставить
`avatarCircle`), тож тап по кружечку працював, а по імені поруч — ні.

Додано на **чотири** поверхні: імʼя автора відповіді · згадка адресата
(«Марія,») · імʼя автора питання на екрані · імʼя в картці списку.

🛑 Атрибут на **обгортці** імені, а не всередині `nameSlot`: її кличуть **14
місць** по застосунку, і глобальна зміна перетворила б на кнопку кожне імʼя
скрізь — зміна, про яку ніхто не просив. У «Стрічці» зроблено так само.

### 🔴 ЗАРАЗОМ ЗАКРИТО ВАДУ, ЯКОЇ НІХТО НЕ ПОМІЧАВ

Написав прилад `tests/tools/qa-card-tap-probe.mjs`. Він довів:

```
до:    тап по АВАТАРУ в картці списку → {профіль: true, питання: true}
після:                                → {профіль: true, питання: false}
```

Один тап відкривав **двоє**. Обидва слухачі висять на `document` і жоден не
спиняє поширення; вада жила **з першого дня картки**, бо аватар отримує
`data-av-uid` від `avatarCircle`. Просто ніхто не тапав саме по кружечку.
➡️ Якби я лише додав імʼя, вада помножилась би. Тепер `[data-av-uid]` у
винятках обробника: **тап по ЛЮДИНІ відкриває людину, тап по решті — питання**.

### ⚠️ ПЕРЕВІРКА, ЯКА СПЕРШУ НАШКОДИЛА СЦЕНІ

Прибирання за собою закривало картку через `history.back()` — і це вивело зі
шару питання, через що посипались **УСІ 13 наступних** перевірок. Тепер
закриття лише кнопкою модалки; не знайшлась — краще впасти чесно, ніж рухати
навігацію.
🔑 Правило ширше за цей випадок: **прибирання за собою не сміє рухати
навігацію**.

**Стенд 105/105. Контроль на чотирьох шарах → 99/105.**

### 📐 `tab-scroll` 16/18 у повному наборі — НЕ моє

| прогін | результат |
|---|---|
| три попередні повні набори | 18/18 |
| цей набір | **16/18** (пік 117px, відношення 2.85) |
| **окремо, одразу після** | **18/18** (пік **63px**, відношення **1.61**) |

Стенд міряє дельту руху ПОКАДРОВО, а мій діф не торкається ні перемикання
вкладок, ні «Стрічки». Кадр загубився під навантаженням прогону.
🛑 Записую з числами, а не як «флак»: «флак» не діагноз.

**`CACHE_NAME`** → `cstl-20260825-1012`.

### ✅ ОПУБЛІКОВАНО (тап по імені)

| | |
|---|---|
| PR | **#1016** (squash) · коміт `13162534` |
| деплой | **#5049** — `success`, 25.08 |
| `CACHE_NAME` | `cstl-20260825-1012` |

---

## 🔀 ПЕРЕДАЧА ДРУГОМУ АКАУНТУ (25.08, ~13:00)

**Слова Вови:** «я почав працювати з іншого акаунту. Ми зараз з ним будемо
працювати над питанням, можливості додати, **відповідати від імені як
спільноти, так і у акаунту на коментарі в вкладці стрічка**».

🛑 **`main` за 24-25.08 пішов далеко вперед: PR #1012-#1016.** Гілка другого
акаунта відстала рівно на цю роботу. Перше, що там треба, — `git merge
origin/main` **ДО** роботи.

**Файли різні:** та сесія — `feed.js` (Стрічка), ця — `board-discussions.js`
(Питання). Зіткнуться лише документи · `src/core/utils.js` (антиспам) ·
`style/base.css` (токен `--qa-line`) · `tests/questions.mjs`.

🔴 **ЗНАХІДКА, ЯКА СТОСУЄТЬСЯ ЇХНЬОЇ ЗАДАЧІ НАПРЯМУ:** делегати на `document`
у цьому застосунку **не спиняють поширення**. Сьогодні через це один тап
відкривав ДВОЄ. На коментарі Стрічки вже висять делегати на лайк, відповідь,
редагування, видалення і **тап по імені → профіль**. Нова дія «відповісти від
імені спільноти» мусить бути перевірена на те, чи не спрацьовує ЗАРАЗОМ старий
делегат. Видно лише пальцем, у коді — ні.

💡 **Зачіпка, яка вже є:** у Стрічці згадка адресата тримає `data-reply-uid`
(uid), а не текст імені. Якщо відповідь від спільноти має показувати НАЗВУ
СПІЛЬНОТИ замість людини — джерело «від кого» вже виражене ідентифікатором,
а не написом, тож підміняти треба одне поле, а не розбирати текст.

---

## 🌙 НІЧНА ТИША 22:00–08:00 (25.08)

**Слова Вови:** «Ну давай зробимо нічну тишу тоді».

**Зроблено:** `send-answer-push` **v6**, типи **1 і 3**. `NIGHT_FROM = 22`,
`NIGHT_TO = 8`, годину дає `kyivHour()` (`Europe/Kyiv`, `hourCycle: 'h23'`).

🔑 **ГОЛОВНЕ РІШЕННЯ — ДЕ САМЕ СТОЇТЬ ВИХІД.** Мовчання не сміє лишати сліду.
Уночі ми **не пишемо в `answer_push_targets`**, а `missed()` рахує «від
останнього запису в цьому журналі» — тому перший ранковий push САМ скаже
«3 нові відповіді». Якби вихід стояв ПІСЛЯ `mark()`, ми записали б адресатів,
`missed()` зсунув би точку відліку, і нічні відповіді зникли б із ранкового
числа: тиша перетворилась би на **втрату**. Це не стиль — це поведінка, і
тримається вона лише порядком рядків.

⚠️ **ПАСТКА, ЯКУ ЗАМІРЯВ, А НЕ ВГАДАВ.** Вікно перетинає північ, тому умова
через `||`. Через `&&` (`h >= 22 && h < 8`) вона дає **нуль** годин тиші —
правило не спрацювало б жодного разу за добу, і виглядало б це як «сповіщення
приходять нормально». Прогнав у Node усі 24 години: `||` → 10 тихих годин,
`&&` → 0.

🛑 **Тип 2 «відповіли на ваш коментар» тиші НЕ отримав — навмисно.** §12
`QA_CONCEPT` виводить особисте звернення з-під правила: це те саме рішення, за
яким у типу 2 немає й вікна тиші. Тип 4 перевірки не потребує взагалі — його
прохід один, об 11:00.

⚠️ **Тиша діє і для Питань, і для Дошки.** «Не дзвонимо вночі» не залежить від
того, про що сповіщення; протилежне дало б дивну асиметрію.

✅ **ПЕРЕВІРЕНО НАЖИВО, а не «задеплоїлось без помилки».** Два виклики
`net.http_post`: чужий секрет → **401 `bad secret`**; справжній секрет +
неіснуючий коментар → **200 `{"sent":0,"reason":"answer gone"}`**. Друга
відповідь доводить, що файл доїхав цілим і тіло обробника справді виконується.
`verify_jwt: false` збережено (функцію кличе Postgres, не браузер).

## 🛡 СТОРОЖ `tests/night-silence.mjs` (14/14 · стендів 103 → 104)

🔴 **Навіщо.** Тиша поїхала на прод БЕЗ сторожа, а ламається вона мовчки в
обидва боки: не спрацює — розбудить людину о 3-й ночі; спрацює зайвий раз —
сповіщення просто не прийде, і ніхто не дізнається чому.

🔑 **Стенд ВИКОНУЄ умову, а не грепає її.** Витягає живий текст `ніч()` із
файлу, підставляє свій годинник і проганяє всі 24 години. Він не питає «чи
схожий код на правильний» — він рахує, **скільки годин на добу телефон
мовчить**, і вимагає рівно 10 та рівно ті самі години.

🔴 **КОНТРОЛЬ ДВОШАРОВИЙ, і другий важливіший за перший:**

| шар | команда | результат |
|---|---|---|
| немає коду | `FN_REV=origin/main node tests/night-silence.mjs` | **1/14** |
| **мутант** | у `ніч()` замінити `\|\|` на `&&` | **10/14** |

Мутант — це і є доказ. Він проходить **будь-яку регулярку**: усі слова на
місці, `NIGHT_FROM` є, `ніч()` є, `Europe/Kyiv` є. Падають рівно чотири
перевірки вікна («мовчить 0 год: []»). Файл після заміру повернуто байт-у-байт
(`git diff` порожній).

Окремо стережуться: порядок «вихід ДО `mark()`», вихід через `finish()` (голий
`return` лишив би журнал без підсумку `sent`), відсутність тиші в типі 1,
`hourCycle: 'h23'` (без нього частина реалізацій віддає «24» замість «0»
опівночі — година поїхала б рівно в найтихішу мить доби) і `-1` як «НЕ ніч»
(збій поясу мусить ВІДКРИВАТИ ворота, інакше функція замовкла б назавжди).

## 📄 ДОКУМЕНТИ ЗВІРЕНІ З КОДОМ (25.08)

**Слова Вови:** «роби документи, виправляй все в документах, якщо там треба
щось. Але нічого, ну не зіпсуй головне».

Не «причесав текст» — звіряв кожен рядок із кодом і лишав, ЧИМ доводиться.

| що | було | стало | доказ |
|---|---|---|---|
| **B-33** | 🟡 чекає живої перевірки | ✅ **ЗАКРИТО 25.08** | слова Вови: «Все працює, все працює чудово» |
| `QA_CONCEPT` §12 | план | ✅ ЗРОБЛЕНО 25.08 | `send-answer-push` v6 |
| `QA_CONCEPT` §18 «Пошук по питаннях» | «оживе, коли питань стане два десятки» | ✅ ЗРОБЛЕНО | `board.js:1674`, плейсхолдер «Пошук серед питань…» |
| `AUDIT` О-6 «лічильник учасників» | ⬜ відкрито | 🗑 **ЗАСТАРІЛО** | лічильник прибрано 11.08; `tests/questions.mjs:428` ВИМАГАЄ його відсутності |
| `AUDIT` О-7 «клавіатура перекриває» | 🔴 | 🟡 «виглядає закритим, чекає пальця» | `attachSheetKeyboardFix` підключений (`board-discussions.js:783`) |

🔑 **Закреслене НЕ видаляв.** §18 показує, що прогноз не справдився — пошук
завели раніше, ніж питань стало два десятки. Початкові тексти О-6 і О-7 лишені
під новими рядками: видно, з чого пункт починався, а не лише чим закінчився.

🛑 **ЗНАХІДКА ПРО ВЛАСНІ ДОКУМЕНТИ: `tests/README.md` описує 43 стенди зі 104.**
Таблицю перестали дописувати на початку серпня, і **61 стенд виріс тут
непоміченим при зелених прогонах** — бо `docs-refs` стереже ЧИСЛО стендів у
документах, а не рядок на кожен. Дописав чесне попередження зверху і вказав, де
живий перелік (`ls tests/*.mjs` + рядок `tests/` у `CLAUDE.md`). Дописувати 61
рядок заднім числом не став: це переказ своїми словами того, що вже описано
детальніше в іншому місці — тобто ще одна копія, яка почне розходитись.

⚠️ **`CSTL_BUGS.md` = 40.5 KB при стелі 40 KB на файл (`docs-fresh`).** Стеля
спрацювала одразу після закриття B-33 — і зловила справжній дубль: я вписав
опис ДВІЧІ (під `<details>` і рядком у «Закриті»). Прибрав дубль за
конвенцією B-31/B-32. **Лишилось 0.5 KB запасу** — наступний баг доведеться
писати стисло або виносити історію окремо. Стелю НЕ піднімав.

## 🔢 ТИП 4 — ЧОМУ 25.08 НІЧОГО НЕ ПРИЙШЛО (це не поломка)

Вова: «Сьогодні мені нічого не прийшло». Перевірив у базі: розклад пройшов
двічі (`0 8,9 * * *` UTC), журнал `qa_unanswered_push_log` порожній, і причина
не в коді — **під вікно «старше 24 год і молодше 7 діб» не потрапило ЖОДНЕ
питання**: усі три без відповіді старші (7.7 · 14 · 20 діб). Вова написав
свіже питання — **26.08 об 11:00 буде перший чесний прогін**.

⚠️ Заразом видно інше: у всій базі **одна** push-підписка. Тип 4 не має кому
дзвонити, навіть коли питання підійде під вікно.

---

## 🏁 ФІНІШ (25.08, 12:14 UTC)

| що | значення |
|---|---|
| PR | **#1018** — squash merge у `main` |
| коміт у `main` | `2c21e980` |
| деплой | **#5055** — `build` + `deploy` обидва `success` |
| `CACHE_NAME` | `cstl-20260825-1122` — **не мій**, приїхав із #1017 |
| стендів | **105** (мій `night-silence` + їхній `comment-as-page`) |

**Прогони:** повний набір ДО злиття — **104/104 стенди · 2074/2074 перевірки**.
Після злиття з #1017 — стенди, яких воно торкнулось: **8/8 · 181/181**
(`comment-as-page 19/19` · `night-silence 14/14` · `questions 105/105` ·
`docs-fresh 20/20` · `docs-refs 10/10` · `conflict-markers` · `dead-css` ·
`css-alive`). Повний набір не повторював свідомо: цей діф **не змінює жодного
клієнтського файлу**, а 25 хвилин прогону над незмінним бандлом нічого не
доводять.

🔴 **ЗЛИТТЯ ПРИНЕСЛО ЧУЖЕ — і перевірка це спіймала першою дією.** Поки йшов
прогін, другий акаунт злив **#1017** («Стрічка: голос спільноти»). Конфлікти в
трьох документах зведені руками, `-X ours/-X theirs` не використовував:

- `SESSION_STATE_VOVA.md` — **обидві** секції збережені цілком (їхня «сесія c»
  зверху як свіжіша, моя «сесія b» під нею). Тут конфлікт означає ВТРАТУ.
- `CLAUDE.md` — рядок стендів обидва правили в ОДНОМУ місці, кожен вписував свій
  опис. Зведено: обидва описи, лічильник **103 → 105**.
- `START_HERE.md` — **не конфліктував, і саме тому був небезпечний**: git тихо
  взяв одну сторону, і вийшло б **104 при 105 на диску**. Знайшлось лише тому,
  що я перерахував стенди на диску, а не повірив жодному з двох чисел.
- `NOW.md` — за правилом узято ЧУЖУ версію і прозу переписано ЗАНОВО.

⚠️ **CACHE_NAME не чіпав.** Мій діф клієнтського коду не змінює, а `cstl-20260825-1122`
приїхав із #1017 і мусить лишитись їхнім. Наслідок для Вови: після цього
деплою на телефоні **візуально нічого не зміниться** — це очікувано, не збій.

⚠️ **Живу сторінку з контейнера перевірити не вдалось** (`curl` до
`castlelife.org` рветься на проксі, код 56). Доказ деплою — зелені роботи
GitHub Actions, а не мій запит.


---

## 🔴 ПОМИЛКА В ПІДПИСАХ ТИПІВ — ЗНАЙДЕНА Й ВИПРАВЛЕНА (25.08, після фінішу)

Вова спитав: «Що ще нам залишилось у вкладці питання?» — і, звіряючи §12 з
кодом, я знайшов помилку **у власній документації**, зробленій за годину до того.

**Що було не так.** Блоки в `send-answer-push` ідуть за ПРІОРИТЕТОМ — §10 каже
`2 → 1 → 3` (людина отримує рівно один push, найсильніший з тих, що їй
належать). А підписав я їх **за порядком у файлі**: 1, 2, 3. Через це:

| блок у файлі | шле насправді | номер за §10 | як я підписав |
|---|---|---|---|
| перший (`c.reply_to_id`) | `answer-reply` | **2** | «тип 1 «вам відповіли»» ❌ |
| другий (автор запису) | `answer-new` | **1** | «тип 2 (автору запису)» ❌ |
| третій (цікавить) | `answer-interest` | **3** | 3 ✅ |

🔑 **КОД БУВ ПРАВИЛЬНИЙ.** §12 вимагає тишу для типів 1, 3, 4 і НЕ для 2 —
реалізовано саме так. Помилка жила виключно в назвах: у коментарях функції, у
двох перевірках сторожа, у пульсі, літописі, журналі, `CLAUDE.md` і в тілі
PR #1018. І фраза «типи 1 і 3» випадково лишалась правдивою, що робило помилку
ще непомітнішою.

🛑 **ЧОМУ ЦЕ НЕ ДРІБНИЦЯ.** Помилку в коментарі не ловить НІЩО: вона не падає,
не змінює поведінки, не з'являється в журналах. Вона просто лежить і чекає, поки
хтось піде «лагодити» справний код за брехливим підписом — і зламає працююче
правило, будучи впевненим, що виправляє ваду.

**Що зроблено:**
1. Кожен блок у функції тепер називає СВІЙ номер із §10 (`ТИП 2 за §10 —
   answer-reply` тощо), і поруч записано саму пастку — щоб наступний не
   повторив.
2. Перевірки сторожа перейменовані і називають тип разом з іменем у базі.
3. 🆕 **Три нові перевірки прив'язують підпис до `type:` МАШИННО.** Стенд шукає
   мітки `ТИП N за §10` і звіряє з ПЕРШИМ `type:` після кожної. Міряється не
   «слово поруч», а відрізок від мітки до наступної мітки — тож немає магічного
   числа символів, яке довелося б підкручувати після кожного дописаного абзацу
   (перша редакція саме на цьому й спіткнулась: вікно 1200 символів не діставало
   до `type:` у другому блоці, а до третього треба було 4173).
4. **Контроль-мутант:** переставити підписи місцями — рівно та помилка, яку я
   допустив, — → **16/18**, падають обидві перевірки підпису. Файл після заміру
   повернуто байт-у-байт.

Стенд: **14/14 → 18/18**.

⚠️ **Чого це НЕ виправляє:** повідомлення вже змерджених комітів і тіло
PR #1018 переписати не можна — вони лишаються в історії з перевернутими
підписами. Тіло PR оновлено окремо.
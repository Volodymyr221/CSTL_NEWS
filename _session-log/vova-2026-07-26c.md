# Журнал сесії — 2026-07-26 (сесія G), гілка `claude/startuem-ozunsb`

> Живий журнал. Дописую ПІСЛЯ КОЖНОЇ значущої дії, не наприкінці з пам'яті.
> Формат рядка: що зробив (файл/функція) · навіщо · що НЕ спрацювало · слова Вови дослівно · PR# · деплой# · CACHE_NAME.

---

## Старт сесії

- `git config user.name "Вова"` + `user.email vitocorleone191@outlook.com` — авторство комітів людиною.
  «Unverified» на GitHub — очікувано, ігнорую (HOT_RULES, шапка).
- `git fetch origin` + `git pull origin main` → **Already up to date**, HEAD = `97e4e570`
  («Документація: повний хендовер сесії F у новий чат», PR #657).
- Прочитано: `HOT_RULES.md` · `START_HERE.md` · `SESSION_STATE_VOVA.md` (хендовер сесії F) ·
  `AUDIT_2026-07.md` · `CSTL_BUGS.md` · `CLAUDE.md` · `ВОВА_ПРОФІЛЬ.md` · `BACKLOG.md` (Роби зараз) ·
  `BYYOU_PLAN.md`.
- **`/byyou` = `idle`** — незавершеного потоку нема, пересідати на іншу гілку не треба.
- Стан прода з хендовера: PR **#656**, `CACHE_NAME = cstl-20260726-1615`. Відкритих 🔴 багів нема.

## Очікує рішення Вови

- Що робимо сьогодні (запитано у першій відповіді).
- 🔴 Живий тест сесії F (два пристрої + клавіатура на iPhone) — з пісочниці недосяжно.

---

## Дії

### Крок 1 — ревізія AUDIT_2026-07.md (Вова: «Роби ревізію AUDIT_2026-07.md. Давай наведем повний порядок в документації, по нових денних»)

**Метод:** кожен пункт звірено по коду/даних, не з пам'яті. Мітки ✅/🗑/⬜/⏸️ + доказ (файл:рядок).

**Три зміни продукту, що перевернули цілі секції (знайдено на старті, до розбору пунктів):**
1. Таб-бар 22.07 = Стрічка·Обговорення·Громада·Дошка·Автобуси (`index.html:160-208`).
   `switchTab('news'|'events')` → `'shotam'` (`app.js:33`). Вкладок Новин і Подій НЕМА.
2. Новини = блок «Табло новин» у Громаді (`news.js:53-56`, `community.js:222-229`).
3. `initEvents()` не імпортується ніде — календар/чіпи Подій не рендеряться взагалі.
   Живий лише `openShotamModal` (`community-blocks.js:16`).

**Заміри наживо (а не з опису):**
- `data/schedule.json`: 6 унікальних маршрутів, ЖОДНОГО зворотного (Олика→Луцьк/Ківерці).
  При цьому `MARSHRUTI` (`parse_vopas.py:45-53`) зворотні ВЖЕ просить → винен не парсер, а VOPAS. (А-1)
- `data/events.json`: 5 подій, майбутніх 0 (остання 15.07, сьогодні 26.07). (ПД-1 підтверджено)
- `data/articles.json`: 400 статей. За 72 год: Волинь 103 (Н-1 було «0» → ВИРІШЕНО),
  Україна+Світ 63% (Н-2 було 83% → ВИРІШЕНО), Громада 4 (нова задача Н-17).
  Категорії: Суспільство 381/400 = 95% (Н-10 було 85% → ПОГІРШИЛОСЬ).
- `images/`+`icons/`: усі 5 мертвих файлів на місці, ~11.4МБ (СИ-2 підтверджено).
- `send-chat-push/index.ts`: `thread_user_state` — 0 входжень (П-3 підтверджено відкритим).
- `supabase.js:1207`: запобіжник 42703 живий (СТ-4 підтверджено відкритим).

**20 пунктів знайдено вже зробленими:** ПД-2 (кабінет вміє події, `can_events`+`source_article_id`) ·
Н-1 · Н-2 · Н-4 (кабінет редактора існує повністю: `editor_users`, `cms_articles`, `cms-sync.yml`) ·
Н-8 частково · Г-8 (`community.js:98-110`) · Г-9 (`account-ui.js:206`) · Г-10 (групи контактів +
РЕАЛЬНІ номери амбулаторії/сільради/ЦНАП) · Г-11 (2 графіки, `community-blocks.js:102,227-241`) ·
Г-13 (`community-blocks.js:684`) · Г-16 (`community-blocks.js:931`) · Г-21 (`sidebar.js:34-37`) ·
О-1…О-5 (Обговорення давно вкладка) · П-4 частково (`account-ui.js:226`) ·
СИ-4 (auto-merge.yml ВИДАЛЕНО — ризику нема) · АН-1 (аналітика в адмінці `admin.html:919`).
Плюс «порядок блоків Громади» з BACKLOG — виконано (`community.js:222-276`).

**Знайдено в проєкті, чого в аудиті не було взагалі:** кабінет редактора · конвеєр cms-sync ·
AI-агент новин (`ai_news_agent.py`) · аналітика + `ai-analytics-summary` · `send-page-push` ·
`send-comment-push` · `gitleaks.yml` · модуль «Стрічка» (`feed.js`, 177КБ).

**Цифри підсумку не вигадував** — порахував грепом по готовому файлу: закрито 65 (20 нових),
треба 45, застаріло 9, заблоковано 3, додано 5 (Н-17, СТ-5, СТ-6, СТ-7).
Перший варіант шапки написав на око (58/80/32) — виправив на реальні.

**Питання, які ревізія кладе на стіл Вові (сам вирішити не можу):**
- ПД-3: доля вкладки Подій — (а) закрити назавжди / (б) повернути вкладку / (в) розвивати блоком.
  Від цього залежить, чи оживає 5 пунктів календаря.
- Н-17: хто і як часто пише про Громаду (4 статті за 72 год із 291).
- Г-6: чи знімаємо правило «Громаду доробляти останньою» — джерел «зовні» майже не лишилось.
- Г-17: уточнення було «в Роми» — Роми нема, треба слово Вови.

### Крок 2 — чистка решти документації (Вова: «Роби, але так щоб нічого не поламати»)

**Безпека:** змінено ВИКЛЮЧНО `.md` (6 файлів). `git diff` по `sw.js`/`src/`/`index.html`/
`style/`/`bundle.js`/`admin.html` — порожній. `CACHE_NAME` НЕ чіпав (лишився `cstl-20260726-1615`,
як на проді) — за правилом «чисто документаційні зміни → кеш не бампати».
Звірив розміри до/після: усі файли ВИРОСЛИ (нічого не зрізано), нічого не видаляв —
закрите переносив із доказом.

**Знайдена і виправлена ВЛАСНА неточність із Кроку 1:** написав у ревізії «`GEO_FILTERS` у коді
більше нема» — формально правда, але вводить в оману: гео-чіпи не зникли, їх перейменовано на
`CM_NEWS_FILTERS = ['Громада','Волинь','Україна та Світ']` і перенесено у блок Громади
(`community-blocks.js:1155`). Виправив опис Н-5: перший рядок чіпів на місці, треба саме ДРУГИЙ.

**BACKLOG.md** — з розділу «🔴 Роби зараз» 5 блоків із 7 виявились зробленими:
- Кабінет редактора для Алли (=Н-4) · Захід 2 репутація (=Д-1, PR #267, 08.07 — це і був
  головний приклад мертвого беклогу) · Порядок блоків Громади · Фаза Б Google-акаунти (20.06) ·
  серверний антиспам (05.07→25.07 спільний двигун).
- «ДЛЯ ВОВИ: вручну запустити RSS Parser» — дія більше не потрібна, парсер працює сам
  (291 стаття за 72 год).
- Push Level B — усі 8 пунктів на проді, Firebase не знадобився.
- Фаза 3 «Світло» — закрито рішенням Вови 04.07 (С-1), позначив 🔒, не видалив.
- Google-авторизація — зроблена; питання «модель логіну» вирішилось на практиці: м'яка.
- Додано таблицю «✅ Закриті — знайдено ревізією 26.07» з доказом на кожен рядок.

**CSTL_BUGS.md:**
- B-14 — уточнив адресу: баг писався на `news.js`/`GEO_FILTERS`, а фільтри тепер у
  `community-blocks.js:1155`. Позначив заблокованим новим B-28.
- 🆕 **B-28 заведено** — класифікатор категорій: 381/400 = 95% «Суспільство» (було 85% → гірше).
  Блокує B-14/Н-5.
- Виявив: BACKLOG посилався на B-07 як на відкритий, хоча його закрито ще 12.05
  (`submit.js` видалено) — виправив у BACKLOG.

**EDITOR_CABINET_PLAN.md** — шапка казала «чекає зустрічі → схема → код»; насправді кабінет
побудований. Замінив на «✅ ПЛАН ВИКОНАНО» з переліком що саме є в коді/базі. Лишилось не
технічне, а людське (зустріч з Аллою по контенту).

**CHATS_NAV_PLAN.md** — шапка казала «код ще НЕ почато»; насправді Етапи 1-2 зроблені, а концепт
«Чати» розформовано 05.07. Позначив архівним + вписав актуальний таб-бар.

**START_HERE.md** — виправив головну пастку для нового чату: там стояло «5 вкладок: Громада ·
Новини · Події · Автобуси · Світло», тобто перший же файл сесії дезінформував.
Тепер: Стрічка · Обговорення · Громада · Дошка · Автобуси + пояснення куди поділись інші.
Таблицю обов'язкового читання перебудував: AUDIT піднято на 2-ге місце як джерело істини,
BACKLOG опущено на 6-те з поміткою «не джерело істини».

### Крок 3 — 4 рішення Вови + вихід Алли з проєкту

**Відповіді Вови (дослівно):**
1. ПД-3 → «Лишити блоком, ПД-3 закрити».
2. Н-17 → «Алла вийшла з проекту, але так агент пише, редактор публікує якщо є що +
   парсинг так як зараз».
3. Г-6 → «Зняти обмеження».
4. Г-17 → «Закрий це питання, воно вирішене».

**Внесено в AUDIT_2026-07.md:**
- ПД-3 ❌ закрито назавжди + 6 пунктів календаря (ПД-4, ПД-5, ПД-6, ПД-10, ПД-11, ПД-13)
  переведено з 🗑 у ❌ «не робимо». Наслідок: `events.js` лишається лише постачальником
  `openShotamModal`, решту можна прибирати при чистці.
- Н-17 — вписано затверджену схему: агент пише ЧЕРНЕТКИ (не автопублікація) → редактор
  публікує лише якщо є що → RSS як зараз. Позначено головний блокер: роль редактора вакантна.
- Г-6 ✅ обмеження знято → Г-2/Г-4/Г-5/Г-12/Г-19/Г-20 більше нічим не заморожені.
- Г-17 ❌ закрито.
- Підсумок перераховано: закрито 65, треба 43, закрито рішенням 8, застаріло 2, заблоковано 2.
- Додано секцію «🟢 Рішення Вови від 26.07.2026» вгорі файлу.

**НОВА ОБСТАВИНА — Алла вийшла з проєкту 26.07.2026.** Прогнав грепом: 18 файлів згадували її.
Розділив на історію і живі документи (як робили з Ромою 18.07).
- НЕ чіпав: `_archive/*`, `_session-log/*`, `BYYOU_ARCHIVE_*` — це історія.
- Централізовано зафіксовано там же, де вихід Роми: `CLAUDE.md` (+ окремий рядок, що роль
  редактора ВАКАНТНА і яка схема поки її нема), `NEW_SESSION_PROMPT.md`,
  `.claude/commands/startuem.md`. (`start.md` згадок не мав — він реюзає промпт.)
- Роль замість імені: `VISION.md`, `CONTENT_STRATEGY.md`, `NEWS_REDESIGN_PLAN.md`,
  `SHOTAM_PAGES_PLAN.md`, `RODMAP.md`, `BACKLOG.md`, `CLAUDE.md` (приклад «лист Аллі»).
- Архівні шапки: `LIST_ALLA.md` (лист-запрошення → лишив як ГОТОВИЙ ШАБЛОН для наступного
  куратора), `EDITOR_CABINET_ARCH.md` (статус «дизайн на узгодження» застарів — кабінет працює),
  `EDITOR_CABINET_PLAN.md` (позначено, що далі в тексті «Алла» читати як «редактор»).
- У `SHOTAM_PAGES_PLAN.md` був записаний ризик «якщо пише лише одна людина — це та сама одна
  стрічка». Дописав: станом на 26.07 кураторів НУЛЬ, ризик реалізувався.

**Безпека:** знову змінено виключно `.md` (13 файлів). `git diff` по sw.js/src/index.html/
style/bundle.js/admin.html/data/scripts/supabase — порожній. CACHE_NAME = cstl-20260726-1615.

### Крок 4 — СИ-2: видалення мертвих файлів (Вова обрав варіант 2)

**Ризик, який тримав у голові:** імена схожі (`cork.png` vs `cork2.png`,
`castle-icon2.png` vs `castle-icon.png`) — видалити не той файл легко.
Тому кожен із 5 перевіряв ОКРЕМО грепом по всьому репо (виключив `.git`, `backup`, `node_modules`).

**Результат перевірки:** усі 5 мають нуль посилань у коді — єдина згадка в самому AUDIT.
Для контрасту заміряв живі: cork2.png 11 посилань, castle-icon.png 4, bus-hero2.png 6,
logo.png 17. Жодного з 5 нема в `STATIC_ASSETS` (`sw.js:8-30`) — там із картинок лише
logo.png, castle-icon.png, cork2.png, тобто ті що лишаються.

**Видалено (git rm):** IMG_2321.png 3.7М · cork.png 3.1М · castle-icon2.png 1.7М ·
0C9D1101-….png 1.6М · 75777DAD-….png 1.3М.
**Заміряно: репо 34МБ → 23МБ** (images 16→6.9, icons 6.8→3.9).

**Перевірка після:** усі 9 живих файлів на місці · `node build.js` **exit=0**, check-imports
39 файлів clean · `bundle.js` перезібрано і git не бачить у ньому змін (тобто на код видалення
не вплинуло взагалі) · `sw.js` недоторканий.
⚠️ Спершу build падав з MODULE_NOT_FOUND — це була відсутність `node_modules` у пісочниці,
НЕ наслідок видалення. Поставив `npm install` і прогнав по-справжньому, щоб не звітувати
про неперевірене. `node_modules/` у .gitignore — у коміт не потрапляє.

**CACHE_NAME не бампав** і це свідомо: жоден видалений файл не був у precache і на нього
ніхто не посилався → у кешах браузерів його нема, оновлювати нічого.

**🔴 ЗНАЙДЕНО ШОСТИЙ МЕРТВИЙ ФАЙЛ — `images/bus-hero.png` (1.6МБ).** Нуль посилань у коді,
єдина згадка була в СИ-7, який називав його «активним» — це помилка аудиту.
НЕ видаляв: Вова просив СИ-2 (5 файлів), розширювати обсяг самому — проти HOT_RULES №9.
Вписав у СИ-7 як питання на рішення. Активний саме `bus-hero2.png` (6 посилань).

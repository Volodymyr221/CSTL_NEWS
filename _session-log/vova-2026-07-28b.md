# Журнал сесії — 2026-07-28 (сесія K)

**Гілка:** `claude/startuem-iwbjg9` · production: `main`
**Старт:** `CACHE_NAME = cstl-20260728-0901` · останній прод-PR **#689**

---

## Крок 0 — старт (`/startuem`)

- `git config user.name "Вова"` + `user.email vitocorleone191@outlook.com` — авторство комітів.
- `git fetch origin` + `git pull origin main` → **Already up to date** (гілка вже на свіжому `origin/main`).
- `git log --oneline -15`: верхівка `c18cc379 auto(news)`, перед нею PR **#689** (Дошка + `core/list-patch.js`).
- `BYYOU_PLAN.md` = `idle` → незавершеного потоку `/byyou` немає, пересідати на іншу гілку не треба.
- Заведено цей журнал.

## Прочитано

`HOT_RULES.md` (10 правил) · `START_HERE.md` · `SESSION_STATE_VOVA.md` (хендовер сесії J) ·
`AUDIT_2026-07.md` (шапка + ревізія 26.07 + Н-10/Н-12/Г-14) · `CSTL_BUGS.md` ·
`CSTL NEWS VOVA/CLAUDE.md` · `ВОВА_ПРОФІЛЬ.md` · `BYYOU_PLAN.md`.

## Стан на вході

- Відкритих 🔴 багів **немає** (`CSTL_BUGS.md`).
- 🟡 відкриті: B-14 (заблокований B-28), B-28 (=Н-10), B-25 (клавіатура iOS, Вова погодився лишити), B-22 (дедуп новин — чекає критеріїв Вови).
- Чекає ЖИВОГО тесту на iPhone: 10 пунктів з `BYYOU_PLAN.md` (Стрічка без перезавантаження, відкріплення, порядок закріплених, Дошка на місці).

## Знайдені розбіжності в документах (сказано Вові в першій відповіді)

1. **Локальний `main` — досі чужа гілка.** `git log origin/main..main` = **50** і `main..origin/main` = **50** (незв'язані історії після переклонування). Хендовер сесії I писав «борг закрито» — не відповідає дійсності, сесія J це вже зафіксувала. Лікується `git branch -f main origin/main` (тільки локальний вказівник) — **без дозволу Вови не роблю**.
2. **`START_HERE.md` відстає.** Секція «Поточний стан проекту» = 26.07 сесія F, `CACHE_NAME` 1615, PR #656. Насправді — 28.07 сесія J, `CACHE_NAME` 0901, PR #689. Сам файл посилає читати хендовер у `SESSION_STATE_VOVA.md`, тому на роботу не впливає, але число застаріле.

## Далі

Чекаю «Роби» від Вови. Нових завдань у документах немає; за пріоритетом `AUDIT_2026-07.md` наступні: **Н-10/B-28** (класифікатор новин, 95% «Суспільство»), **Н-12** (обрізані статті чужих RSS), **Г-14/ПД-1** (0 майбутніх подій — контент, не код).

---

## /byyou — Дошка, потік 1 (фундамент вигляду)

**Ціль від Вови (дослівно):** «Дошка... деякі виправлені, які мені не подобаються. До них
треба підійти системно і технічно і правильно з точки зору UEUX дизайну». Далі:
1. ціна — «опційна, але її треба вмістити правильно щоб все виглядало чітко, з нею та без неї»;
2. заголовок — «Прибрати. Зараз заголовок дошка оголошень ховається при скролі, а треба
   зробити, щоб ховалася оце поле воду з категоріями... щоб не займало багато місця.
   Але як ти думаєш користувач ідентифікує, що це дошка оголошень?»;
3. колір — «кожна сторінка має свій колір... це треба якось стандартизувати, мені подобаються
   кольори які в автобусах і в стрічці... Як ти думаєш, варто залишити тут цей такий темний
   колір?... Цю фотографію взагалі потрібно краще прибрати ні?»;
4. «давай все вводити по черзі».

### Прочитано (реальний код, не з пам'яті)
`src/tabs/board.js` — `renderBoardCard:151`, `renderCardFoot:75`, `renderHeader:383`,
`renderBody:469`, `syncBoardBodyOffset:1170`, `setupHeaderCollapse:1227` ·
`style/community.css` — `.board-bg:1791`, `.cm-block--board:608`, `.bm-preview-section:1828`,
`.bd-controls:2629`, `.bd-title:2762+2853`, `.board-vignette:3646` ·
`style/base.css:214-217` · `style/feed.css:1-17` · `style/buses.css:1-10` · `index.html:56-88` ·
`sw.js:8-30`.

### 🔎 ДВІ ЗНАХІДКИ, ЯКІ ЗМІНИЛИ ВІДПОВІДЬ
1. **`images/cork2.png` = 2.9 МБ і воно в `STATIC_ASSETS` (`sw.js:29`).** `CACHE_NAME`
   бампається при кожному деплої коду → кожен телефон перекачує 2.9 МБ фото корка при
   кожному деплої, навіть не заходячи на Дошку. Ще й блокує install (`Promise.all`).
   Видно цього фото ~38% — згори `rgba(24,14,8,0.62)`. Найважчий файл проєкту.
   → Це перетворює питання «чи прибирати фото» зі смаку на рахунок.
2. **Сірих фонів ЧОТИРИ, не один:** `#ECEEF1` (Стрічка) · `#ECEEF2` (`#page-buses`) ·
   `#F0F2F5` (`.app-main[data-tab=buses]`) · `#E7E1D2` (Обговорення). В САМИХ автобусах
   два різні сірі на вкладених шарах. Токена немає — тому й розійшлись. Вова відчув це
   правильно («плюс-мінус один колір, може чимось відрізняються»).

### 🔴 Пастка, знайдена до коду (а не після)
`syncBoardBodyOffset` ставить `.bd-body { padding-top }` = живій висоті шапки. Ховати
шапку через `max-height` (як зараз ховається заголовок) → тіло перерахує `padding-top`
ПІД ПАЛЬЦЕМ і список стрибне. Це HOT_RULE №10. Рішення: `transform: translateY(-100%)`,
`padding-top` не змінюється взагалі → контент не рухається ні на піксель.

### Рішення про розбиття
Ціна = колонка в базі + форма подачі + анатомія картки + фільтр → **окремий потік 2**.
Потік 1 (цей) не чіпає ні розмітку картки, ні схему Supabase → нульовий ризик для
`list-patch`/`keepScroll`, які Вова щойно приймав.

**Стан:** план на 12 кроків у `BYYOU_PLAN.md`, статус `active`. Брама старту — чекаю «ок».

### Виконання потоку 1 (кроки 1-11)

**Вова: «Роби будем дивитись»** → брама старту пройдена.

| Крок | Що | Коміт |
|---|---|---|
| 1 | `--app-bg`/`--app-glass` у base.css; 8 місць у buses.css+feed.css на токен | `20126dd9` |
| 2 | `.board-bg` = `var(--app-bg)`; знято фото/затемнення/inset-тіні/vignette з Дошки | `f99aa240` |
| 3+4 | корок віджета Громади і прев'ю подачі → CSS-градієнт; cork2.png з precache знято | `c410993c` |
| — | bundle.js | `d760150d` |
| 5 | скло шапки + біле поле пошуку (scoped `#board-content`) | `e347b683` |
| 6-9 | заголовок прибрано + `.sr-only` h2; шапка ховається цілком через translateY; пороги 110/70 | `1ff2e0cb` |
| 10-11 | стенд `tests/board-header.mjs` 14/14; прибрано мертвий `vignette--bottom` | `ed0cb3cf` |

### 🔴 Рішення Вови на 🔴-крокі 4
Питання: віджет Дошки в Громаді темний, а вкладка стала світлою — що робити.
Вибрав **варіант А**: «Темний, але фото → градієнт». Тобто Д-21 («віджет = той самий фон
що вкладка») свідомо скасовано, натомість звільнено 2.9 МБ. Прев'ю подачі — так само темне.

### 🔴 СТЕНД ЗБРЕХАВ (восьмий раз за три дні) — і винна була мірка
Перша версія `tests/board-header.mjs` завалила 2 з 13: «шапка не піднялась під шапку
застосунку» (78px замість 0) і «тінь не знята» (rgba замість none).
**Корінь:** у шапки є `transition: transform 0.28s / box-shadow 0.2s`, а стенд міряв
`getBoundingClientRect()` і `getComputedStyle()` ОДРАЗУ після додавання класу — тобто
ПЕРШИЙ КАДР анімації. Код був правильний із самого початку.
**Виправлення:** `SETTLE = 420мс` перед виміром кінцевого стану; зсув списку міряється
двічі — і на першому кадрі, і після доїзду (стрибок може статись у будь-якій точці).
**Це рівно те правило з CLAUDE.md:** якщо стенд «завалив» те, що виглядає правильно —
перша підозрювана мірка. Спрацювало.
Друга дрібниця: стенд спершу впав із SyntaxError — у CSS-комментарі всередині шаблонного
рядка стояли зворотні лапки, вони закрили сам шаблон. Записав попередження в код стенда.

### Заміряно
`npm test` 19 стендів / **227 перевірок** зелені · `node build.js` чисто ·
браузерний смоук `tests/tools/board-shot.mjs` на ЖИВОМУ застосунку:
`collapsed: true`, `transform: matrix(1,0,0,1,0,-78)`, `bgColor: rgb(236,238,241)`,
`bgImage: none`; помилки консолі лише зовнішні (403/404/tunnel від проксі пісочниці).
**Контраст шапки:** лічильник+локація 1.66:1 → **6.34:1**; бордові іконки 2.1:1 → **8.30:1**.
**Контроль стенда:** згортання через висоту дає −78px зсуву, новий спосіб — 0px.

### Стан
Брама ДЕПЛОЮ. Нічого не запушено, `CACHE_NAME = cstl-20260728-0954`. Скріншоти надіслані.

### ДЕПЛОЙ — Вова: «Деплой, І напиши мені, що ти саме змінив й простими словами»

- `git push -u origin claude/startuem-iwbjg9` → 9 комітів (`b57d0d46..54f0f451`).
- PR **#690** створено і змерджено squash у `main` → `e9838be2`.
- Деплой **run 3472 — completed / success** (10:37 UTC).

### ⚠️ ЧОГО НЕ ЗМІГ ПЕРЕВІРИТИ (і сказав це Вові прямо)
Фонову перевірку живого лічильника версії ставив на 12 спроб × 20с — **усі порожні**.
Причина: GitHub Pages **недосяжний із пісочниці**, `curl` дає `HTTP 000` (мережа не пускає).
Тобто перевірка була марною з самого початку. Підтвердження деплою взяте зі **статусу
GitHub Actions**, а не з живого сайту. Виправив себе в наступному повідомленні Вові, щоб
не створювати ілюзію, ніби я бачив сайт.

### Закриття потоку
- `BYYOU_PLAN.md` → архів `BYYOU_ARCHIVE_2026-07-28_board-bg-header.md`, live-файл скинуто
  в `idle` + вписано ГОТОВИЙ ДО СТАРТУ опис **потоку 2** (ціна + анатомія картки) і
  21 пункт живого тесту.
- `SESSION_STATE_VOVA.md` — новий хендовер сесії K зверху (5 речей про код + урок про мірку).
- `CLAUDE.md` — оновлено рядки `style/base.css` (новий, з токенами) і `src/tabs/board.js`;
  стенди 18/213 → **19/227**.

### ДОРОБКА після перевірки Вови: фон замало відрізнявся

**Вова дослівно:** «фон такий що верхня шапка і самі карточки воно трошки зливається
з заднім фоном, можливо його зробити трішечки насиченішим темнішим… щоб карточки
і шапка воно виділялися на фоні».

**Заміряно ДО:** картка `#FBFBF9` на `#ECEEF1` = **1.122:1** — рівно як Facebook, тобто
на самій межі. Вова відчув правильно.

**🔑 Знайдено ДРУГУ причину, яку Вова описав як одну.** `#FBFBF9` — це ТЕПЛИЙ, жовтуватий
білий (залишок від часів кремового корка), а новий фон — ХОЛОДНИЙ сірий. Тепла поверхня
на холодному тлі читається «брудно» навіть при формально достатній різниці яскравості.
Тобто лікувати треба було два дефекти: яскравість + температуру.

**Показано 4 варіанти скріншотами** (`tests/tools/board-bg-variants.mjs` — новий інструмент,
знімає ОДИН екран з різними фонами, щоб Вова вибирав оком, а не за числами).
**Вибір Вови: B + «тільки Дошку»** (Стрічку й Автобуси не чіпати).

**Зроблено:**
- `--board-bg: #E1E5EB` + `--board-glass` + `--board-glass-solid` у `base.css` —
  **названий ДРУГИЙ РІВЕНЬ поверхні**, не «ще один випадковий сірий». Причина в коді:
  Дошка єдиний екран, де контент лежить окремими картками у дві колонки.
  Різниця стала **1.264:1** (між Telegram 1.20 і виразним розділенням).
- Картка Дошки → чистий `#FFFFFF`. ⚠️ Scoped ТРЬОМА селекторами:
  `#board-content .cm-board-note:not(--official)` + `.cm-board-modal-note` +
  `.bm-preview-canvas .cm-board-note`. Причина: зум-модалка і прев'ю подачі живуть
  ПОЗА `#board-content` (створюються в `body`), а картка і її збільшена копія мусять
  бути одного кольору. Базове `#FBFBF9` лишено — його носять картки Обговорень, де
  фон теплий беж і холодний білий був би чужим.
- `.app-main[data-tab="board"] { background: var(--board-bg) }` — щоб при
  overscroll-bounce не блиснув кремовий `--bg-page`.
- Виправлено брехливий коментар у `sw.js` (казав `--app-bg` замість `--board-bg`).

**Перевірено:** `npm test` 19/19 · 227/227 · `node build.js` · живий смоук:
`bgOfBoard: rgb(225,229,235)` = `#E1E5EB` ✅ · `CACHE_NAME = cstl-20260728-1103`.

### ДОРОБКА 2: шапка Дошки зливалась із фоном → окрема плашка (варіант D)

**Вова:** «сам верхній блок цього пошуку зливається з фоном… може накласти накладку
як у Громаді у віджеті Дошка зверху, щоб трошки вирізнялось, але щоб було гармонічно
поєднано… може ти найдеш якесь оптимальне рішення».

**🔴 Чому НЕ скопіював накладку віджета буквально.** `.cmbw-head` = `rgba(0,0,0,0.22)`
+ світла лінія знизу. Це ЗАТЕМНЕННЯ, і воно працює тільки бо фон віджета темний, а
текст світлий (`#F2E7D0`). У Дошки текст ТЕМНИЙ на світлому → те саме затемнення дало б
темно-сіру плиту з гіршою читабельністю, і шапка виглядала б ВДАВЛЕНОЮ, а не піднятою.
Фізика та сама, знак протилежний: на темному «вище» = темніше з підсвіченим краєм,
на світлому «вище» = світліше з тінню під краєм.

**Показано 4 варіанти скріншотами** (`tests/tools/board-head-variants.mjs`):
A світліше скло · B градієнт+лінія · C біла плита · D плашка з полями.
**Вова: «Давай D попробуєм, якщо що повернемо».**

**D:** `left/right: 8px` · `background: rgba(255,255,255,0.78)` · `border 1px` ·
`border-radius: 16px` · тінь `0 6px 18px`. Шапка відділяється **формою, а не кольором** —
стає такою самою білою заокругленою плашкою, як картки. Нового прийому в інтерфейс не
додано, використано наявний — це і є «гармонічно поєднано».
**Ціна, сказана Вові ДО вибору:** шапка вужча за екран → у двох смужках по 8px з боків
видно картки, що проїжджають угору.

**🔑 Дві взаємодії, які врахував ДО коду (не після):**
1. **Згортання.** У плашки з'явились рамка і заокруглені НИЖНІ кути — при зсуві рівно
   на власну висоту вони сідали впритул до межі шапки застосунку і в кутах могла блиснути
   світла дуга. Тому `translateY(calc(-100% - 6px))` — запас замість розрахунку.
   Заміряно: `hiddenAboveHeader` = **−6px** (було 0).
2. **Сторож стенда впав би на ФОРМІ ЗАПИСУ.** Регулярка вимагала точного
   `translateY(-100%)`, а стало `calc(-100% - 6px)` — поведінка не змінилась ані на крок.
   Це рівно та помилка, що коштувала часу 27.07. Регулярку послаблено до
   `translateY([^)]*-100%`, наслідок міряють перевірки нижче.
3. Fallback без blur: напівпрозоре біле без розмиття показувало б крізь себе картки
   брудними смугами → `rgba(255,255,255,0.96)`.

**Перевірено:** `npm test` 19/19 · **227/227** · `node build.js` · живий смоук:
`transform: matrix(1,0,0,1,0,-86)` (78 висота + 6 запас + рамка), `bgOfBoard: rgb(225,229,235)`.
`CACHE_NAME = cstl-20260728-1129`.

### ДОРОБКА 3: варіант B замість D (Вова: «Давай ще B попробуємо»)

D прожив один деплой (run 3475). Вова захотів порівняти живцем → перейшли на B.

**B:** `linear-gradient(to bottom, rgba(255,255,255,0.86), rgba(255,255,255,0.62))` +
`border-bottom: 1px solid rgba(16,18,22,0.12)` + тінь `0 5px 16px`.
Шапка знову на ВСЮ ширину (смужок по 8px з боків більше нема), а «підняття» дає
вертикальний градієнт — світліше згори, темніше донизу, ніби на плашку падає світло.
**B — найточніший дзеркальний відповідник накладки віджета:** там на темному тлі
затемнення + світла лінія, тут на світлому — підсвітлення + темна лінія.

**🔑 Що перевірив, а не припустив:** чи потрібен ще запас 6px у `translateY`.
Його додавали під рамку і кути 16px варіанта D. У B рамка знизу лишилась (1px) і
базові нижні кути 12px → **ризик той самий, запас потрібен**. Не прибрав, і вписав це
в коментар прямо, щоб наступна сесія не прийняла його за залишок від D.
Заміряно: зсув `-85px` (79 висота + 6 запас).

**Історію вибору вписано в CSS** (D і B, чому кожен, де інструмент) — щоб наступна
сесія не переграла це наново.

**Перевірено:** `npm test` 19/19 · 227/227 · `node build.js` · живий смоук:
`bgOfBoard: rgb(225,229,235)`. `CACHE_NAME = cstl-20260728-1146`.

### Оновлення документації під фактичний стан (Вова: «Роби»)

Борг за чотири доробки: документи описували стан на момент PR #690.

- **`SESSION_STATE_VOVA.md`** — шапка хендовера: `CACHE_NAME 1203`, таблиця 5 PR з деплоями,
  **новий блок «ФАКТИЧНИЙ ВИГЛЯД ДОШКИ НА КІНЕЦЬ СЕСІЇ»** (щоб не збирати стан по PR).
  Пункт 1 переписано на ДВА РІВНІ ПОВЕРХНІ (`--app-bg` / `--board-bg`) з поясненням, чому
  це система, а не повернення хаосу, і чому картка стала чистим білим (яскравість 1.122 →
  1.264 + температура). Пункт 2 доповнено запасом 6px і застереженням «не прибирати як
  залишок від D» + чому регулярка сторожа послаблена. Новий пункт 5 — історія вибору
  вигляду шапки (чому накладку віджета не можна скопіювати буквально; D vs B).
- **`CLAUDE.md`** — рядок `style/base.css` (два рівні + числа контрасту), рядок
  `src/tabs/board.js` (пункти д і е: фактична шапка B з двошаровою тінню, картка білим і
  ЧОМУ три селектори), уточнено зсув згортання `calc(-100% - 6px)`.
- **`BYYOU_PLAN.md`** — чеклист живого тесту Дошки переписано під новий вигляд (10 пунктів
  замість 7), додано пункт «Автобуси і Стрічка НЕ мусили змінитись» і підказку, що варіанти
  A/C/D лежать у репозиторії.

Тільки `.md` — `CACHE_NAME` не чіпав.

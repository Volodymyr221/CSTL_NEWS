# Журнал 29.08.2026 — Вова

> ⚠️ **ДЕТАЛЬНИЙ ХІД ЦЬОГО ДНЯ ЛЕЖИТЬ У `vova-2026-08-27c.md`** (рядки 847+).
> Сесія почалась 27.08 і тривала три доби без розриву, тож журнал вівся одним
> файлом. Цей файл — вказівник за датою і підсумок дня, а не друга копія: два
> описи однієї роботи розійшлися б, і це вже коштувало проєкту окремих розборів.

**Гілка:** `claude/startuem-2mp937` · **усе в `main`** · деплой **v5211+**

---

## Що зроблено 29.08

| PR | що |
|---|---|
| [#1087](https://github.com/Volodymyr221/CSTL_NEWS/pull/1087) | відео з YouTube доходить у статтю: обкладинка в тілі, плеєр по тапу |
| [#1088](https://github.com/Volodymyr221/CSTL_NEWS/pull/1088) | чужі пошти з документів; карта акаунтів у `ВОВА_ПРОФІЛЬ.md` |
| [#1089](https://github.com/Volodymyr221/CSTL_NEWS/pull/1089) | карта акаунтів закрита повністю — модератор сторінки 4 теж тестовий профіль Вови |
| [#1090](https://github.com/Volodymyr221/CSTL_NEWS/pull/1090) | персональні дані з публічного репо: пошти → ролі, номер зі стенда, 23 знімки з git |
| [#1091](https://github.com/Volodymyr221/CSTL_NEWS/pull/1091) | **поправка Вови** — робоча пошта проєкту повернута в Політику і VAPID |

Раніше того ж дня: моніторинг громади для Olyka Castle, буфер із двох чернеток на
спільноту, акордеон питань в адмінці, чесна причина відмови публікації.

---

## 🔑 Три уроки дня — усі про перевірку, а не про код

**1. Хибна тривога, яку не закрили явно, живе далі як факт.** Модератора сторінки 4
розбирали ТРИЧІ (18.08, 20.08 і я сам 29.08) — щоразу дарма. Закриваєш тривогу —
виправляй **усі** місця, де вона записана.

**2. `.gitignore` без `git rm --cached` — це напис на відчинених дверях.** Рядок
`tests/tools/_out/` стояв із 28.08, а 23 знімки живого застосунку лежали в
репозиторії, бо `.gitignore` не діє на вже відстежувані файли. Захист виглядав
поставленим і не діяв жодного дня.

**3. Контакт у правовому документі мусить бути РОБОЧИМ на момент публікації.**
Чистячи ПД, я замінив пошту проєкту на `privacy@castlelife.org`, якої не існує —
і тим зламав право людини вимагати видалення своїх даних (ст. 12 ЗУ №2297-VI).
Слова Вови: «це пошта проекту, вона робоча». 🔑 Помилка була в класифікації:
адреса ОРГАНІЗАЦІЇ — не персональні дані, ховати її нема від чого.

---

## 🚧 Відкрите (свідомо відкладено Вовою)

1. **`cstl-proxy.<пошта Вови>.workers.dev`** — особиста адреса в субдомені живого
   проксі Cloudflare (`parse_rss.py`, 4 місця). Грепом по «@» не знаходиться.
   Потрібен новий worker з нейтральним іменем — дія в панелі Cloudflare, після неї
   заміна в коді одним рядком.
2. **Пошта в підписі комітів** — у файлах скілів і в авторі кожного коміту.
3. **Історія git** — усе прибране лишається в старих комітах. Лікує лише
   `git filter-repo` + force-push, що вбʼє посилання на коміти в PR-ах і журналах.

---

## 🕐 29.08 вечір — РОЗКЛАД ПАРСЕРА ПЕРЕЇХАВ ІЗ GITHUB У SUPABASE

**Скарга Вови:** «чого парсер вчора запустився тільки 1 раз. Нам треба хоча б раз
в годину, в нас теж має бути актуальна інформація».

### 📐 Замір: розклад не «повільний», а зламався в конкретний день

| період | медіана між прогонами |
|---|---|
| до 26.08 14:37 | **55 хв** — «раз на годину» працювало |
| після | **120…733 хв**, найбільша діра 12 годин |

Просимо 48 запусків на добу — отримуємо **8.6**.

🛑 **Розведення хвилин НЕ пробували вдруге, і це головне рішення заходу.** Ми вже
робили це для `cms-sync` 28.08; заміряно 29.08 — за розкладом у нього **10
прогонів із 30**, живе він на події `push`. Той самий засіб від тієї самої
хвороби вже провалився, тож пропонувати його знову означало б лікувати наосліп.

📐 Заразом заміряно навантаження: репозиторій просить **220 запусків на добу**, і
парсер новин та парсер автобусів стоять на ТІЙ САМІЙ хвилині (`0,30` і `*/30` —
це один запис), тобто щоразу конкурують між собою в пікову хвилину платформи.

### ✅ Що зроблено

`pg_cron` → Edge Function `nudge-parser` → GitHub `workflow_dispatch`.

🔑 **Чому саме pg_cron:** він у цьому ж проєкті возить `send-bus-push`
**щохвилини** і не пропускає. Надійний планувальник у нас уже був — розклад
просто стояв не там.

🛑 **Що можна будити — вписано в КОД функції**, не приймається із запиту: інакше
той, хто дістався б секрету, запускав би будь-який наш воркфлов, зокрема деплой
чи бекап бази. Те саме рішення вже стоїть у `publish-now`.
🔑 Два рубежі, як у `send-unanswered-push`: `verify_jwt` + спільний секрет
`x-cstl-push-secret`, який читається з `app_secrets` **у момент виклику** — тож
ротація не вимагає чіпати `cron.job`.

⚠️ **Розклад у GitHub свідомо НЕ чіпали.** Спершу доводимо нове, потім знімаємо
старе: якби зняли `0,30` одразу і pg_cron не запрацював, новини стали б **рідшими,
ніж були**. Тепер `0,30` лишається безкоштовною страховкою.
⚠️ Хвилина **:47** теж не випадкова — у воркфлову стоїть `cancel-in-progress: true`,
і збіг із «рівною» хвилиною скасовував би один із двох прогонів.

### ✅ Доведено наслідком, а не словами функції

```
select public.nudge_rss_parser();
→ 200 {"nudged":true,"workflow":"rss-parser.yml"}
→ GitHub: прогін #1955 workflow_dispatch стартував о 15:13:21 —
  через ДВІ секунди після виклику о 15:13:19
```

🔑 `nudged:true` саме по собі довело б лише те, що GitHub **прийняв запит**.
Перевіряли, що прогін справді біжить.

### 🚧 Що це НЕ полагодило

Мій доступ до запуску воркфловів лишається закритим (`403 Resource not accessible
by integration` — бракує дозволу GitHub App, а його видає не власник репозиторію).
Токен лежить у секретах Supabase і доступний **функціям**, не мені.

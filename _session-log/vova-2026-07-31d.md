# Живий журнал сесії — 2026-07-31 (сесія S, гілка `claude/startuem-blvhny`)

> Ведеться ПО ХОДУ, не з пам'яті. Комітиться кожні кілька кроків (HOT_RULES №11).

---

## 1. Старт `/startuem`

- `git config user.name "Вова"` + `user.email «робоча пошта Вови (підпис комітів)»` — авторство Вови.
- Гілка сесії: `claude/startuem-blvhny` (авто-гілка старту).
- `git fetch origin` + `git pull origin main` → **Already up to date**.
- Звірка за правилом №11:
  - `git log --oneline origin/main ^HEAD | grep -v "auto("` → **порожньо**
  - `git log --oneline HEAD ^origin/main` → **порожньо**
  - Тобто локальний стан = `origin/main` = `a860a441`.
- Прочитано: `HOT_RULES.md` (11 правил), `START_HERE.md`, `SESSION_STATE_VOVA.md`
  (хендовери сесій R/Q/P), `AUDIT_2026-07.md`, `CSTL_BUGS.md`, `CLAUDE.md`,
  `_ai-tools/BYYOU_PLAN.md`.

## 2. Що знайшов на старті

- **Відкритих 🔴 багів нема** (`CSTL_BUGS.md`).
- Прод: PR **#731** → `main` (`f60dea4a`), деплой **#3596**,
  `CACHE_NAME = cstl-20260731-1227`.
- **Два відкриті потоки, живий файл один:**
  1. `BYYOU_PLAN.md` (живий, статус `paused`) — «Новини: єдина картка + дайджест».
     Брама пройдена, рішення Вови є (табло = дайджест), наступний по суті — **крок 2**
     (звести картку в ОДИН компонент `.nc-*`, знести 33 scoped-перевизначення).
  2. `_archive/BYYOU_PLAN_2026-07-30_аудит-безпеки-paused.md` — аудит безпеки,
     5/14 кроків, попереду 6-10 (сміттєвий ввід форм, XSS у 154 `innerHTML`,
     подвійні тапи, сирі системні помилки).
- ⚠️ `BYYOU_PLAN.md` вказує гілку `claude/startuem-devlock-ui-mc1dnt`, але сам план
  уже в `main`, а моя гілка від `main` — пересідати нікуди не треба, план читається
  звідси. Код у тому потоці НЕ чіпався.
- **Розбіжностей у документації не знайшов** — хендовери R і Q узгоджені між собою.

## 3. Стан коду — не чіпав

Жодного файлу не змінено. Чекаю «Роби» від Вови.

---

## 4. Потік `/byyou` — новини: єдина картка + дайджест (кроки 1-10)

Вова продублював замовлення дослівно і сказав «byyou» → режим ПРОДОВЖЕННЯ,
статус плану `paused` → `active`, гілка `claude/startuem-blvhny`.

### Крок 2 — корінь: одна картка замість двох трактовок · `5519c6c7`
- Прочитав РЕАЛЬНИЙ код: `news.js`, `news-hub.js/css`, `community-blocks.js`,
  `community.css`, `news.css`.
- Новий `style/news-card.css` — компонент `.nc` з варіантами `lead/row/mini`.
  Екран задає ЛИШЕ токени `--nc-*`.
- `renderRow` + `renderFeatured` → одна `renderCard(a, variant)`.
- Знесено 20 перевизначень у `news-hub.css` і 13 у `community.css`.
- Прибрані інлайнові `style="background:…"` у міток разом із `GEO_COLORS`,
  `geoColor()`, `catColor()` — обидва екрани цю заливку однаково гасили через
  `background: none !important`, тобто розмітка писала фарбу, яку ніхто не бачив.
- **Пастка, яку побачив завчасно:** `.nc-img { display: block }` перебило б
  `display: flex` у `.img-fallback` (файл підключається пізніше при рівній вазі),
  і замок 🏰 поїхав би в кут. Тому правило звужене до `img.nc-img`.
- Стенди після рефактора: **31/31 · 452/452** — рефактор доведено нейтральним.

### Крок 3 — обідок як у Дошки · `00acbbca`
Вова: «фон залишити, просто такий ободок зробити, як в дошці… у вигляді карточки,
а не просто лініями». Перенесено КРИТЕРІЙ, не hex:
- обідок ↔ фон = **1.399** (у Дошки 1.398);
- натиск ↔ картка = **1.274** (у Дошки 1.274 — цифра в цифру);
- ⚠️ `--news-press` довелось ПЕРЕРАХУВАТИ (`#D2D4D7` → `#E2E4E7`): старе значення
  підбиралось під сірий фон і на білій картці давало 1.485, тобто натиск раптом
  став би різкішим за той, що на Дошці.
- Стара підсвітка `::before` з токеном `--nc-bleed` видалена — вона існувала лише
  тому, що в рядка не було поверхні.

### Крок 4 — «щоб кожна новина несла сенс» · `00acbbca`
🔬 Не на око: написав вимір `tests/tools/_measure-meta.mjs`. Виявилось, що у
«Волині» **19 з 20 карток (95%)** мали ПОРОЖНІЙ верхній рядок, у «Громаді» 55%.
Причина — два чесні рішення, що зійшлись: гео-мітка знімається як повтор вкладки,
категорія «Суспільство» не малюється (94.5% статей, B-28). Картка починалась
одразу із заголовка — це і була «стіна».
➡️ Джерело переїхало нагору, час униз: **звідки → що → коли**. Після зміни
порожніх верхніх рядків **0%** у всіх трьох розділах.

### Крок 5 — табло = дайджест
По одній новині з кожного розділу + мітка розділу. Пряма відповідь на «неясно,
що робиться в світі і в Україні».

### Крок 6 — великі літери
«УСІ НОВИНИ» і назви розділів капсом. Вкладки 13 → 11px, розрядка 0.3px.
🔬 Заміряно на 320/390/430 (`tests/tools/_measure-tabs.mjs`): при розрядці 0.6px
«УКРАЇНА ТА СВІТ» просило 108px у клітинці 107px — на око добре, а на старому
айфоні напис був би підрізаний. При 0.3px літери займають 98/107.
⚠️ Заразом виправив саму МІРКУ: `scrollWidth` у вузла з `overflow: hidden` ніколи
не менший за `clientWidth`, тож він каже «обрізано чи ні», але не каже про запас.
Перейшов на `Range.getBoundingClientRect()`.

### Крок 7 — одна мова до самої статті
Мітки статті переведені на ті самі `.nc-badge` і те саме правило про приховану
категорію за замовчуванням (до цього стаття писала «СУСПІЛЬСТВО», а картка мовчала).

### Заразом — справжній дефект, знайдений СКРІНШОТОМ
Биті чужі фото в ТАБЛІ показували системну іконку «зламане зображення»:
`handleImgError` висів на хабі й на модалці статті, а на віджеті — ні.
Полагоджено спільним обробником, копії не робив.

### Крок 8 — вибір Вови зі скріншотів
Зняв живцем обидва варіанти табла (`tests/tools/news-shots.mjs`):
- **A — велика перша з фото:** 547px = **74.8%** видимої зони;
- **B — три однакові рядки:** 406px = **55.5%**.
Тобто велика перша коштує 133px і повертає віджет майже туди, звідки потік його
витягав уранці (було 567px = 77.6%). Уперся у власний сторож попередньої сесії
(«віджет не з'їдає головний екран < 480px») — і це було правильне місце спитати.
**Вова вибрав B.** Підсумок: **422px = 57.7%** (на 15px більше за 407px — це ціна
мітки розділу на кожній картці, тобто рівно те, що він просив).

### Крок 9 — сторож проти повернення хвороби
`tests/news-widget.mjs`: розбір CSS шукає правила, де контейнер екрана чіпає
ВЛАСТИВОСТІ картки (токени `--nc-*` дозволені). Два КОНТРОЛІ: підкинуте
`.nh-list .nc-title { font-size: 19px }` МУСИТЬ спійматись, а самі токени —
НЕ мусять. Без контролю перевірка була б зелена і на порожньому файлі.

### Крок 10 — прогін
`node build.js` exit 0 · **npm test: 31/31 стендів, 462/462 перевірки.**
Баланс дужок у чотирьох змінених CSS зійшовся. Залишків старих класів у `src/`
нема (`.news-badge` в `events.js` — законний користувач, не чіпав).

### ⬜ Відкрите (не чіпав за HOT_RULES №9)
Легасі-CSS у `style/news.css` (`.news-card-row*`, `.news-card-featured*`,
`.news-card-geo`, `.news-card-category`, `.exclusive-badge`) — розмітки під нього
більше нема, ~110 рядків. Чекаю рішення Вови.

### Стан
Кроки 1-10 зроблені, усе ЛОКАЛЬНИМИ комітами. **Нічого не запушено** — брама
деплою /byyou чекає слова «деплой». `CACHE_NAME` піднято до `cstl-20260731-2155`.

---

## 5. ✅ Деплой

Вова: «Деплой».

- Пуш гілки `claude/startuem-blvhny` (4 коміти) → **PR #732** → squash-мердж у `main`.
- Коміт у `main`: **`bedffb5d`**. Деплой **#3600**.
- `CACHE_NAME = cstl-20260731-2155` (код мінявся — обовʼязково за HOT_RULES №2).
- Перед пушем: `node --check` трьох змінених `.js` + `node build.js` exit 0 +
  `npm test` **31/31 стендів, 462/462 перевірки**.
- ⚠️ Хук про «Unverified» коміти — проігноровано за правилом проєкту (підпис-ключ
  на Claude, `user.email` навмисно людина). Авторство «Вова» не ламав.

## 6. Фаза 5 — закриття потоку

- `BYYOU_PLAN.md` → `_archive/BYYOU_PLAN_2026-07-31c_єдина-картка-новин.md`,
  живий файл скинуто в шаблон `idle` зі списком завершених і незавершених потоків
  (щоб наступний чат бачив, що аудит безпеки досі чекає).
- `SESSION_STATE_VOVA.md` — новий хендовер сесії S угорі.
- `START_HERE.md` — блок «Поточний стан» оновлено (у сесії O він відставав на
  4 дні, тому цього разу оновив одразу).

## 7. ⬜ Що лишається відкритим

1. **Легасі-CSS у `style/news.css`** (~110 рядків) — розмітки під нього нема.
   Не чіпав за HOT_RULES №9. ⚠️ `.news-badge` чіпати НЕ можна: `events.js`.
2. **B-28** — 94.5% статей «Суспільство», корінь у `scripts/parse_rss.py`.
3. **`data/articles.json` 1.31 МБ** — розділення на «свіже + архів».
4. **Аудит безпеки** — 5 із 14 кроків, лежить в `_archive/`.
5. **Нові CSS не в `STATIC_ASSETS`** `sw.js` (`news-card.css`, `news-hub.css`,
   `dev-lock.css`, `crop.css`, `install.css`) — офлайн-fallback їх не має.

# Журнал сесії — 15.08.2026 (Вова)

**Гілка:** `claude/startuem-s3yr33` · production: `main`
**CACHE_NAME на старті:** `cstl-20260815-0609`
**Старт:** `/startuem`

---

## Крок 0 — старт сесії

- `git config user.name "Вова"` + `user.email vitocorleone191@outlook.com` — виконано.
- `git fetch origin` — виконано. `git log origin/main ^HEAD` порожній, `HEAD ^origin/main` порожній
  → гілка рівно на `origin/main`, нічого не загубилось, нічого незапушеного.
- Останній змістовний коміт у `main`: `76f944e1` — «Іконка "Стрічка": просвіти дорівнюють
  товщині лінії + аудит руху в JS (#915)». Решта згори — авто-коміти парсерів (news/vopas).
- Прочитано: `HOT_RULES.md` · `START_HERE.md` · `SESSION_STATE_VOVA.md` · `AUDIT_2026-07.md` ·
  `BACKLOG_VOVA_2026-08.md` · `CSTL_BUGS.md` · `CLAUDE.md` · `NEW_SESSION_PROMPT.md`.
- `BYYOU_PLAN.md` — статус `working` (не `paused`), потік правової відповідності від 13-14.08;
  кроки 1-9 зроблено, роботу змерджено в `main` 14.08. Пересідати на іншу гілку не треба.

## Стан на вході

- Відкритих 🔴 критичних багів **немає** (`CSTL_BUGS.md`).
- Незакриті 🟡: B-28 (класифікатор категорій, 93.2% «Суспільство») → блокує B-14
  (тематичний фільтр); B-25 (смикання при підйомі клавіатури, дві спроби провалились);
  B-22 (семантичні дублі новин).
- `plans/001-003` — специфікації аудиту руху від 14.08, **код за ними не застосований**,
  чекають слова «Роби». Порядок виконання: 002 → 003 → 001.
- Черга `BACKLOG_VOVA_2026-08.md`: п.1 віджет зборів — заблокований даними від Вови
  (потрібен перший справжній збір: назва · хто збирає · посилання на банку · ціль).

## Що робив далі

### Supabase — стартове попередження було ХИБНЕ

Я написав у першій відповіді, що конектор не авторизований (так казав системний
реміндер на старті). Вова: «Супабейс авторизований в цій сесії». Перевірив живим
запитом `list_projects` — працює: `Olyka Castle` `uabyfecseqnemvcqhdem`,
`ACTIVE_HEALTHY`, Postgres 17. Другий проєкт `Olyka Tea` — `INACTIVE`, не наш.

### 🔴 Вова працюватиме ПАРАЛЕЛЬНО з іншого акаунта

**Слова Вови дослівно:** *«я буду працювати ще з іншого акаунта, щоб не наробити
біди. Я там буду виправляти. Всякі такі які будуть помічати баги в роботі тобто
десь вкладка так звертається, десь щось не так виглядає, і так далі»*.

➡️ **Наслідок для мене цією сесією:** тримати дотик вузьким і коміти дрібними,
`git fetch` перед кожним пушем, у `main` напряму не лізти. Головний ризик —
не конфлікт тексту, а `bundle.js`: його комітять обидві сторони, і він
перегенерований цілком, тож конфлікт у ньому розводиться НЕ руками, а
`node build.js` після зведення `src/`.

### План `plans/002` — `ease-in` із закриття банера автобусів ✅

**Зроблено (`src/tabs/buses.js`):**
1. Додано `import { SHEET_EASE } from '../core/sheet-motion.js'` у наявну групу
   імпортів `../core/` (файл звідти ще нічого не імпортував).
2. Рядок 2099: `'transform 0.25s cubic-bezier(0.4,0,1,1)'` →
   `` `transform 250ms ${SHEET_EASE}` `` (`cubic-bezier(0.32, 0.72, 0, 1)`).
3. `setTimeout(… , 260)` → `250` — прибирання зрівняне з тривалістю переходу
   (банер 10мс висів уже нерухомий).

🔑 **Це не нова знахідка, а пропущена половина старої:** рівно цю криву прибрали
зі `style/buses.css:1642` 12.08 (PR #906), але той прохід дивився CSS і не
дивився JS.

⚠️ **Дрібниця, на якій себе спіймав:** перший варіант коментаря містив криву
дослівно — і контрольний `grep -c "cubic-bezier(0.4,0,1,1)"` із плану дав **1**
замість 0. Тобто перевірка червоніла б на КОМЕНТАРІ про прибрану ваду. Переписав
коментар на `0.4,0,1,1` без префікса → grep дає **0**.

**Перевірено:**
- `node --check src/tabs/buses.js` → OK · `node build.js` → exit 0
  (спершу впав `Cannot find module 'esbuild'` — у свіжому контейнері не було
  `node_modules`, вилікувано `npm install`).
- `grep -c "cubic-bezier(0.4,0,1,1)" src/tabs/buses.js` → **0**;
  у `bundle.js` живий рядок `transform 250ms ${SHEET_EASE}` (рядок 10218).
- `node tests/motion.mjs` → **17/17**.
- `CACHE_NAME` `cstl-20260815-0609` → **`cstl-20260815-0804`** (змінився код).

🛑 **Межі плану дотримані:** гілку `else` (повернення банера на місце) не чіпав,
`hideBanner()` / логіку відстеження / push не чіпав, `style/buses.css:442,483`
(шкала руху) не чіпав.

📋 **У звіт, як вимагає крок 4 плану — інші криві, вписані числом у `buses.js`
(НЕ чіпав, поза межами 002):**
- `buses.js:1768` і `:1810` — `cubic-bezier(0.25, 0.46, 0.45, 0.94)` (тижнева
  смужка днів, `track`). Це `ease-in-out` sine — не `ease-in`, вади «стоїть на
  старті» тут немає, але крива теж не з токена.
- `buses.js:2111` — `cubic-bezier(0.22,1,0.36,1)` у гілці `else` (snap-back).
  План прямо забороняє чіпати.

⚠️ **Стенда на цей банер у проєкті НЕМА** (`grep` по `tests/*.mjs` на `.btb-`
дає 0). Тобто зміна доведена збіркою і `motion.mjs`, але живого сторожа саме на
банер відстеження рейсу не існує — перевірка відчуттям на iPhone за Вовою.

### План `plans/003` — гейт `:hover` від дотику ✅

**Питання Вови:** *«Продовжуй, але що мені перевірити? Що ти зробив?»* — відповів
окремо: обидві зміни лежать на ГІЛЦІ, на проді їх ще нема, тобто до `/finish`
перевіряти на телефоні нічого.

**Зроблено (`style/board.css:88`):** правило
`.cm-board-note:hover { transform: rotate(0deg) scale(1.02); z-index: 5 }`
обгорнуто в `@media (hover: hover) and (pointer: fine)`. Значення руху НЕ
змінені — змінено лише те, на яких пристроях воно діє.

🔑 **Чому це видно не на Дошці:** у списку самої Дошки рух уже скасовано окремим
правилом (`#board-content .bd-ad:hover { transform: none }`, `board.css:2472`,
не чіпав). Клас `.cm-board-note` носять ще ТРИ поверхні поза `#board-content` —
віджет Дошки на Громаді, зум-модалка картки, прев'ю подачі оголошення. Саме там
фальшивий hover і лишав картку збільшеною після тапу.

📐 **ЗАМІРЯНО ПРИЛАДОМ, з контролем** (разовий скрипт у скретчпаді, у репозиторій
не клав: підіймає ЖИВИЙ `style/board.css` у двох контекстах і читає обчислений
`transform` картки; «до» береться з `git show HEAD:style/board.css`):

| сценарій | до зміни | після зміни |
|---|---|---|
| ДОТИК (iPhone 390×844, `hasTouch`) | **1.02** | **1** |
| МИША (десктоп 1280) | 1.02 | **1.02** |

Тобто вада була реальна (картка справді лишалась на 1.02 після тапу), а наведення
мишею не втрачене. Без рядка «до зміни» прилад не доводив би нічого.

**Перевірено:** `node build.js` exit 0 · `board-cream` **38/38** ·
`board-card-row` **23/23** · `drift` **23/23** ·
`grep -c "hover: hover" style/board.css` → **1**.
`CACHE_NAME` → **`cstl-20260815-0820`**.

⚠️ **ТА САМА ПАСТКА ВДРУГЕ ЗА СЕСІЮ.** Перший варіант коментаря містив
`hover: hover` дослівно — і контрольний grep із плану дав **2** замість 1.
Переписав на «перша умова медіа-запиту». 🔑 **Правило, яке варто винести:
коментар, що ЦИТУЄ прибрану ваду, ламає grep-перевірку на цю ваду.** У проєкті
контроль часто саме `grep -c` — отже описуй ваду словами, не рядком.

⚠️ **Хук `pre-edit-read-check.js` спрацював і був правий:** я дивився `board.css`
через `sed` у Bash, а не через Read tool. Прочитав як належить — далі редагував.

📋 **У звіт (не чіпав, поза межами плану):** єдиний інший `:hover` у стилях —
`style/feed.css:452` `.fd-screen-menu-item:hover { background: … }`. Це КОЛІР, не
рух; план прямо забороняє гейтити такі — фальшивий hover на кольорі нешкідливий.

### План `plans/001` — свайп модалки статті на спільний механізм ✅

**Слова Вови:** *«Роби це і деплой все разом»*.

**Зроблено (`src/app.js`):** тіло `initModalSwipe()` (73 рядки власного жесту на
`touchstart`/`touchmove`/`touchend`/`touchcancel` + `requestAnimationFrame`)
замінено викликом спільного `attachSheetDismiss()` з `core/sheet-motion.js`.
Це було ОСТАННЄ місце в застосунку з самописним свайпом-закриттям.

Закрито чотири вади разом: крива з повільним стартом на закритті · крива вписана
числом двічі · поріг закриття тільки по відстані · таймер прибирання на 10мс
коротший за сам рух.

🔎 **Знахідка поза планом:** у старому коді був **прапорець без оголошення**
(`startedAtTop` — ні `let`, ні `const`), тобто він писався в ГЛОБАЛЬНУ область.
Помилки не було видно тільки тому, що в `bundle.js` немає суворого режиму
(`grep -c "use strict" bundle.js` → 0). Зник разом зі старим тілом функції.

📐 **ЗАМІРЯНО ПРИЛАДОМ, з контролем на коді ДО зміни** (жест шлеться через CDP
`Input.dispatchTouchEvent` — справжні довірені дотики; прилад ще й САМ рахує
швидкість тією ж формулою, що `createDragTracker`, і друкує її):

**Палець на рисочці (12px) — ізолює саме поріг швидкості:**

| жест | ДО (bundle з HEAD) | ПІСЛЯ |
|---|---|---|
| кидок 40px (швидкість 3.8 px/мс) | **лишилась** ❌ | **закрилась** ✅ |
| повільно 100px | закрилась ✅ | закрилась ✅ |
| повільно 30px | лишилась ✅ | лишилась ✅ |
| | **2/3** | **3/3** |

**Палець на 30px від верху — знахідка, якої в плані не було:**
старий жест вимагав влучити **рівно в рисочку**, а вона заввишки **24px**
(заміряно `getBoundingClientRect`). На 30px під пальцем уже бейдж, і старий код
не реагував **узагалі** — навіть на повільні 100px (контроль **1/3**). Новий
`headerZone: 64` дає тягнути за всю верхню смугу. Тобто зона хапання зросла
**24 → 64px**, і це помітніше за саму криву.

🔴 **ПРИЛАД ЧЕРВОНІВ ДВІЧІ, І ОБИДВА РАЗИ ВИНЕН БУВ ПРИЛАД, А НЕ КОД** —
рівно те, від чого застерігає `CLAUDE.md` («міру перевіряти так само, як код»):
1. Перший «кидок» 20px за 48мс = 0.417 px/мс, після згладжування **0.363** при
   порозі `FLICK_V = 0.45`. Тобто за задумом механізму це НЕ кидок.
2. Друга спроба (без пауз) дала **0.102 px/мс**: кожен обмін по CDP коштує ~65мс,
   і прилад упирався у ВЛАСНУ затримку. Лікування — ставити всі кадри руху в
   чергу протоколу разом, без очікування кожної відповіді. Стало 3.8-10.7 px/мс.
➡️ Обидва рази врятувало те, що прилад **друкує виміряну швидкість**. Якби він
казав просто «❌ не закрилась», я поліз би правити справний код.

⚠️ Дрібна неточність у самому плані: блок «Target» імпортує ще й `sheetRemaining`,
але ніде його не вживає — імпортував лише `attachSheetDismiss` (`check-imports`
у `node build.js` мовчить, бо файл справді збирається).

**Перевірено:** `node --check` OK · `node build.js` exit 0 ·
`modal-scroll-lock` **8/8** · `tabbar-icons` **18/18** · `motion` **17/17** ·
контролі «Done when» — `cubic-bezier` **0**, крива з повільним стартом **0**,
`addEventListener('touch` **0**, таймер 240 **0**.
`CACHE_NAME` → **`cstl-20260815-0837`**.

🛑 Прилад у репозиторій НЕ клав (лежить у скретчпаді сесії): він піднімає живий
застосунок і шле дотики через CDP — це радше інструмент разового доведення, ніж
сторож. **Постійного сторожа на свайп модалки статті в проєкті так і немає.**

# Журнал сесії — 27.07.2026 (сесія J)

**Гілка:** `claude/startuem-0u7daz` · production: `main`
**Старт:** 20:48 (Київ ~23:48 — сервер у UTC)

---

## Крок 0 — старт `/startuem`

- `git config user.name "Вова"` + `user.email vitocorleone191@outlook.com` — авторство комітів.
- `git fetch origin` + `git pull origin main` → **Already up to date**, локальний `main` синхронний
  з `origin/main` (борг попередніх сесій закрито 27.07, підтверджено).
- Останній коміт: `f21f866f` — «Стенди переїхали в репозиторій: npm test (#685)».
- `BYYOU_PLAN.md` = **`idle`**, незавершених потоків нема.
- Прочитано: HOT_RULES · START_HERE · SESSION_STATE_VOVA (хендовер сесії I) ·
  AUDIT_2026-07 · CSTL_BUGS · CLAUDE.md · ВОВА_ПРОФІЛЬ.
- Заведено цей журнал (`vova-2026-07-27b.md`); за сесію I журналу в git нема — лишився
  тільки хендовер у `SESSION_STATE_VOVA.md`.

**Стан на вході:** `CACHE_NAME = cstl-20260727-2045`, останній деплой run 3449 ✅,
відкритих 🔴 багів нема. Чекає живого тесту Вови на iPhone: тости (#682), закріплення
постів (#680/#681), чернетка композера (#679), закриття модалок (#678).

---

## Потік /byyou — «стрічка не перезавантажується після дії над постом»

**Слова Вови (переслав промпт від ChatGPT):** «після редагування… стрічка повністю
перерендерюється… екран помітно блимає… користувача автоматично повертає на самий верх…
якщо редагувався пост десь посередині або внизу, після завершення дії його потрібно
знову шукати». Вимога: «максимально схоже на Instagram, Telegram або Facebook»,
«при цьому нічого не поламай і логіка має зберігатись». Брама старту → **«Ок»**.

**Діагноз (по коду, не з пам'яті):** корінь один на чотири місця `src/tabs/feed.js` —
композер (2563), закріплення (2837), видалення (2849), збереження сторінки (2773):
`.fd-screen` зноситься → `renderFeed()` → `openPageScreen(pageId, true)`. Плюс
`applyPostEvent` (750) робив `renderFeed()` на чужу правку. Взірець лікування був поруч
у тому ж файлі — `patchLike`.

**Кроки і коміти:**
1. `b5162a4e` — `pagePostsOf(pageId)` + `screen.dataset.page`. Екран тримав заморожену
   копію списку в замиканні; після точкової зміни перемикач «Дописи|Події» показав би
   стан на момент відкриття.
2. `b4c3a66d` — `keepScroll(scroller, fn, skipId)`.
3. `0febf272` — `patchPostCard(id)` + збереження кадру каруселі через один rAF
   (бо `wireGalleries` своїм rAF ставить трек на перший слайд).
4. `5836ca2c` — `insertPostCard` / `removePostCard`. Виняток: угорі списку якір НЕ
   ставимо, інакше щойно опублікований пост опинився б над видимою областю.
5-6. `61167b1c` — композер: редагування → патч, публікація → вставка.
7. `01ba4475` — закріплення: патч + `reorderPagePosts` (`insertBefore`, вузол не
   перестворюється, фото не перезавантажуються).
8. `67e432e5` — видалення.
9. `90c9d014` — realtime: чужа правка/видалення + чуже закріплення переставляє вузол.
10. `ee6fb2a4` — стенд №15 `tests/patch-scroll.mjs`, 11 перевірок.
11. `53ce7eb9` — `npm test` 15/15 · 168 перевірок · `node build.js` зелений.
12. `b260d9d5` — `tests/tools/feed-smoke.mjs` (браузерний смоук, 2 чужі помилки).
13. `CACHE_NAME = cstl-20260727-2126` + документація.

**🔴 ЩО НЕ СПРАЦЮВАЛО (найважливіше):**
- **Стенд збрехав.** Перша версія `patch-scroll` дала дрейф **0 і в контролі** — тобто
  «проблеми не існує». Причина: у Chromium є вбудований якір `overflow-anchor`, а в
  **Safari його НЕМА взагалі**. Стенд міряв браузер, якого у Вови нема. Після
  `overflow-anchor: none` контроль показав 150 / 458 / −368 px. Це **сьома** брехлива
  мірка за два дні.
- **Перевірка «повний рендер скидає scrollTop на 0» не відтворилась** у синтетиці:
  `innerHTML` в одній задачі не дає проміжного розкладу. Замінено на дві чесні:
  (а) екран, зібраний заново, справді починає з 0; (б) повний рендер перестворює всі
  14 вузлів, патч — рівно один (це і є «блимання»).
- **Стенд `pin` впав** на перевірці `/const pagePosts = orderPinned\(/` — тобто на ФОРМІ
  ЗАПИСУ, хоч поведінка не змінилась. Переписано на наслідок: ганяємо `pagePostsOf`.

**Свідомо не чіпав:** збереження сторінки (назва/аватар/банер — там масштаб липкого
заголовка міряється один раз при відкритті) і пігулку «↑ N нових публікацій» (підйом
угору там навмисний).

**Статус:** усе локально, у `main` нічого не пішло. Чекаю «деплой».

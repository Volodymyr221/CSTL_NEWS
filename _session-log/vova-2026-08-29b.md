# Журнал сесії — 29.08.2026 «b»

Гілка: `claude/startuem-h0iocf` · production: `main`

---

## Крок 0 — старт `/startuem`

- `git config user.name "Вова"` + `user.email vitocorleone191@outlook.com` — ✅
- `git fetch origin` — локальна гілка **синхронна з `origin/main`** (обидві звірки
  на дві крапки порожні: нічого не бракує і нічого не зависло незапушеним).
- Останній коміт у `main`: `a3f7b289` — PR **#1094** «Розклад парсера новин переїхав
  із GitHub у Supabase (pg_cron щогодини)».
- `CACHE_NAME` = `cstl-20260829-1012`.
- `_ai-tools/BYYOU_PLAN.md` — статус `idle`, незавершеного потоку немає.
- Журнал за 29.08 уже існував (сесія без літери) → цей заведено з літерою **b**.

## Прочитано (обов'язковий список зі скіла)

`NOW.md` · `HOT_RULES.md` · `CSTL_BUGS.md` (відкриті) ·
`_ai-tools/BACKLOG_VOVA_2026-08.md` · `ВОВА_ПРОФІЛЬ.md` ·
`_ai-tools/NEW_SESSION_PROMPT.md`.

## Розходження, знайдені при читанні

🔴 **`NOW.md` відстав від `main` на два PR.** У пульсі написано «останній PR **#1091**»
і в «НЕ ДОРОБЛЕНО» стоїть рядок «Розклад GitHub кидає прогони → синк будить push у
`main`; токен свідомо не робимо». Але **#1094** уже переніс розклад парсера в Supabase
`pg_cron` (щогодини) — тобто обхідний шлях описаний як актуальний стан, хоч його вже
замінили. Питання винесено Вові в першу відповідь.

## Далі

Чекаю «Роби». Код не чіпаю.

---

## 29.08 ~16:20 — `sourceUrl` ДОЧИЩЕНО (Вова: «Дочищаємо sourceUrl»)

### 🛑 Пастка, знайдена ДО написання коду

Спокуса була очевидна — переписати `sourceUrl` на кінцеву адресу. **Не можна:**
`sourceUrl` це ще й **ключ дедуплікації** — `seen_urls` збирається з нього
(`parse_rss.py:3053`), а звірка йде проти адреси, яку дає RSS (`if link in seen_urls`,
2723). Підмінили б — наступний прогін не впізнав би вже збережену статтю.
📐 Заміряно: **38** статей мають кінцеву адресу, відмінну від адреси запиту → рівно
стільки дублів у стрічці.

➡️ Рішення: `sourceUrl` не чіпаємо, заводимо окреме поле **`finalUrl`**, і лише
коли редирект СПРАВДІ був. Клієнт бере `finalUrl || sourceUrl`.

### Зміни

- `src/tabs/news.js` — `адресаОригіналу = article.finalUrl || article.sourceUrl`,
  застосовано у **трьох** виходах: підпис джерела, «Читати повністю», «Читати оригінал».
  🔑 Саме три, бо 27.08 підпис виправили в одному місці й решта лишилась старою.
- `scripts/parse_rss.py` — кінцева адреса лягає в `finalUrl` у свіжому розборі
  і в переборі збережених; `RICH_ALGO_VERSION` **6 → 7**, щоб наявні статті перебрались.
- `tests/article-links.mjs` — сцена двох адрес + **окрема стаття без `finalUrl`**
  (запасний варіант). 22 → **32** перевірки.
- `sw.js` — `CACHE_NAME` = `cstl-20260829-1623`.

### Доведено

```
article-links      32/32  ✅
МУТАНТ (адресаОригіналу = article.sourceUrl) → 29/32 — три нові червоніють ✅
контроль запасного варіанту лишився зеленим (він і не мав реагувати)
test_rich_html     91/91  ✅
test_markup_polish 42/42  ✅
node build.js      exit 0 ✅
```

### ⚠️ Ціна, названа чесно

Бамп версії перебирає **всі** статті по 40 за прогін → повний прохід ≈ 10 годин при
щогодинному кроні. Дотягнути кінцеві адреси звідси не вийшло: проксі не пускає до
`pravda.com.ua` (**403 Tunnel connection failed**), а вгадувати адресу з тіла статті —
це гадання, не факт.

### 🔬 Інцидент — наступив на записану пастку

`НОВИЙ=$(date ...)` у bash → **код 127**, і `sed` записав у `sw.js` літерал `$НОВИЙ`.
Це рівно те, про що попереджає `NOW.md` («кирилиця в іменах змінних bash»). Виправлено
латинською назвою, файл перевірено.

---

## 29.08 ~17:00 — ОНБОРДИНГ БРАУЗЕРНОГО ГОСТЯ (Вова: «схеми, роби»)

Замовлення зі скріна: два банери внизу одночасно · дрібний хрестик · слабка іконка ·
«Як встановити» показує один рядок, що «геть нічого не пояснює». Розбір GPT Вова
переслав; узяв з нього чергу і покрокову модалку, **відхилив** знімки Safari.

### Що зроблено

1. **Черга.** `consent.js` → `consentPending()` + подія `cstl-consent-accepted`.
   `install-banner.js` чекає її і показується через **400 мс** (банер згоди їде вниз
   240 мс — поява в ту саму мить читалась би як «одне перетворилось на інше»).
2. **✕ 44×44.** 📐 **Знайшов у геометрії пастку:** картка була ~68px, кнопка по
   центру, тож область 44×44 у правому куті **накрила б праву половину кнопки** —
   «зручний хрестик» украв би тапи в головної дії. ➡️ Картка розведена **на два
   ряди**: текст і ✕ угорі, кнопка на всю ширину знизу. Перетин **0 px²**.
3. **Іконка** 📲 → `icons/icon-192.png` — та сама, що стане на екран.
4. **Модалка 1/3 → 2/3 → 3/3** з SVG-схемами, крапки прогресу, «Готово».
   Банер при відкритті **зникає**, а не висить позаду.
5. **Не-Safari на iPhone.** 🔴 `isIOS()` дивиться на СИСТЕМУ, а «На початковий
   екран» уміє лише Safari. У Chrome/Instagram показували б кроки, яких у людини
   немає, — той самий клас, що вада посилань. Тепер окремий аркуш «Потрібен Safari»
   + копіювання адреси (і кнопка каже, чи копіювання вийшло).
6. **«Зігнорував» = відповідь.** Другий показ поспіль без дії → та сама пауза 7 днів.

### 🛑 Чому схеми, а не знімки (аргумент, який переважив)

Знімки Safari переконливіші рівно один сезон: Apple перемальовує панель майже щороку,
і застаріла картинка **гірша за текст** — вона впевнено показує те, чого немає.
Плюс ~600 KB у передкеші проти кількох кілобайт і чужий інтерфейс у ПУБЛІЧНОМУ репо.
⚠️ Тому крок 1 названо ширше за один значок: на iOS 18+ низ Safari це `‹ › [адреса] ⋯`
і «Поділитись» лежить УСЕРЕДИНІ ⋯ (видно на скріні Вови), на старших — окремо.
Інструкція називає **обидва** входи.

### Доведено

```
install-onboarding  19/19 ✅  (новий стенд)
МУТАНТ 1: прибрав чергу      → 18/19, «НЕ показується, поки висить згода» ❌
МУТАНТ 2: ✕ повернув на 22px → 18/19, «область натиску не менша за 44×44» ❌
сусідні: modal-surface 10/10 · legal-privacy 31/31 · home 80/80 · dev-lock 44/44
node build.js exit 0 · CACHE_NAME cstl-20260829-1707
```

### 🔬 Два інциденти по ходу

1. **`check-imports` спіткнувся об мішані імена.** `цеSafariНаIOS` він читає як виклик
   `Safari()`, `відкритиПроSafari` — так само. Збірка падала. ➡️ Перейменовано на
   `isIOSSafari` (латиницею, як сусідні `isIOS`/`isStandalone`) і `відкритиПроБраузер`.
   **Правило: не мішати кирилицю з латиницею в ОДНОМУ імені.**
2. **Схема кроку 3 накладалась написами** — видно на першому знімку: «Скасувати»,
   заголовок і «Додати» наїхали один на одного, кружок обрізав кнопку. Шрифт у SVG
   виявився ширшим за мій розрахунок. ➡️ Декоративні підписи прибрано, лишилось те,
   що несе зміст; обвід — прямокутний по формі кнопки, а не кружок.
   🔑 Спіймано **знімком**, не тестом: сторож міряв «схема одна», а не «чи не наїхало».

---

## 29.08 ~20:40 — ЖИВА ПЕРЕВІРКА НА iPHONE ВИЯВИЛА, ЩО ІНСТРУКЦІЯ БРЕХАЛА

Вова поставив застосунок на екран за нашою інструкцією і надіслав три знімки.

### 🔴 Головне: кроків ЧОТИРИ, а не три

На знімку відкритого меню ⋯ видно пункти: «Поширити · Додати до папки «Закладки» ·
Додати закладку до… · Нова вкладка · Нова приватна вкладка». **Пункту «На Початковий
екран» там НЕМАЄ** — він на крок глибше, в аркуші «Поширити».

🛑 Тобто моя інструкція впевнено називала кнопку, якої на тому екрані немає — **рівно
та вада, заради якої ми й розводили Safari з рештою браузерів**. Іронія повна: того ж
дня я написав сторож проти «показуємо одне, а веде в нікуди», і сам зробив це на крок
далі.
🔑 **Урок ширший за цей екран:** схему чужого інтерфейсу, намальовану з памʼяті,
доводить лише знімок із живого пристрою. Мій «здогад про очевидне» був хибний, і
жоден стенд його спіймати не міг — він міряв нашу розмітку, а не чужий Safari.

### Що ще попросив Вова (усе зроблено)

1. **Сказати, що це веб-версія.** Дослівно: «треба вказати, що на даний момент поки
   можлива веб-версія додатку PWA, і тоді вже інструкція». ➡️ У шапці модалки:
   «Поки що CSTL LIFE — це **веб-версія** (PWA, застосунок із браузера): у магазинах
   застосунків його ще немає». Без цього людина, яка чекала App Store, на кроці
   «Поширити» вирішить, що її обманули.
2. **Свайп між кроками.** Поріг 40px, перевага горизонталі (`|dx| > |dy|`), щоб
   прокрутка аркуша не гортала сторінки. 🛑 На КРАЯХ свайп навмисно нічого не робить:
   закрити аркуш жестом, схожим на гортання, означало б завершити встановлення так,
   що людина не зрозуміє, що сталося.
3. **Панель браузера намальована неправильно.** Вова: «зліва кружечок, знизу поле
   строки, типу поле для силки і справа три крапки». ➡️ СХЕМА_1 перемальована за
   знімком: кружок ‹ ліворуч, поле адреси посередині, обведений кружок ··· праворуч.
4. **Справжня іконка в останньому кроці.** Вова: «можна туди вставити нашу іконку
   додатку… це буде більш правдоподібно. І написати CSTL LIFE в назві. А все остальне,
   рядки, залишити таким блюром». ➡️ `<image href="icons/icon-192.png">` усередині SVG
   + підпис «CSTL LIFE»; решта лишилась заглушками.

### Доведено

```
install-onboarding  19 → 28/28 ✅
  нові перевірки: 4 кроки · крок «Поширити» на місці · усі чотири різні ·
  сказано про веб-версію · свайп вперед/назад · свайп на краю нічого не робить ·
  рух коротший за поріг не гортає
node build.js exit 0
```

---

## 29.08 ~22:15 — БАНЕРИ НАД ТАБ-БАРОМ І ВИДИМИЙ ХРЕСТИК

Вова обвів на знімку місце, де банер наїжджає на таб-бар.

### 1. Полиця для технічних сповіщень — один токен на всі

📐 **Чому 76px не вистачало:** я рахував по висоті бару (`--tabbar-h: 56px`), а
кружок «Громади» піднімається над його низом на **83px** (`.tab-item--home::before`
— `bottom: 25px` плюс діаметр 58px). Банер згоди взагалі стояв на 10px, тобто просто
поверх бару.
➡️ `--notice-bottom: calc(96px + safe-area)` у `tokens.css`, обидва банери беруть його.
🔑 **Токен один НАВМИСНО:** доти кожен банер тримав власне число, і полагодити один
означало лишити другий зламаним — рівно це й сталося. Вова просив ширше: «взагалі
такі такого роду технічні сповіщення, незалежно чи це веб версія чи PWA чи нативний».

### 2. Хрестик: це були ДВІ вади, а я полагодив одну

🔴 44×44 лікує **попадання** пальцем. Видимість — це **розмір і контраст знака**.
Вова: «кнопку закриття Х погано видно, бо вона маленька». Людина, яка не бачить
кнопки, до неї й не тягнеться, скільки б та не ловила.
➡️ Знак 15 → **18px**, колір світло-сірий → `#6B4A50`, під ним видимий кружок 34px
через `radial-gradient` — так область натиску лишається всі 44×44, нічого не звузилось
заради вигляду. Те саме в хрестику інструкції, щоб мова була одна.

### Доведено

```
install-onboarding  28 → 32/32 ✅
  нові: банер над таб-баром (зазор 13px) · банер згоди теж · знак ≥17px · підкладка є
МУТАНТ: --notice-bottom назад на 76px → 30/32, обидва «над таб-баром» червоніють
  (зазор -7px — тобто прилад міряє саме перекриття, а не число в CSS)
```

---

## 29.08 ~22:40 — ЗАТЕМНЕННЯ ПІД ТЕХНІЧНИМИ СПОВІЩЕННЯМИ

🗣️ Вова: «добавити легеньке затемнення… а так воно зливається зі всією інформацією,
не зрозуміло, що саме оце надо». **Діагноз його і він точний** — банер напівпрозорий
і світлий поверх строкатої стрічки читався як ще одна картка.

### 🔴 Головне рішення: ДВА різні шари, і різниця — не оформлення, а право людини

| шар | натиски | чому саме так |
|---|---|---|
| `.notice-scrim` (встановлення) | **ловить**, тап повз = відмова | пропозиція необовʼязкова, тож тап повз неї — чесне «ні». Заразом «хрестик» завбільшки з екран |
| `.notice-scrim--soft` (згода) | **не ловить** | текст банера каже «**КОРИСТУЮЧИСЬ** CSTL LIFE, ви погоджуєтесь» — згода дається фактом користування. Заблокувати означало б зробити з повідомлення **браму, не змінивши в ньому жодного слова**. Рішення Вови: «залишаємо як зараз» |

🛑 **Пастка, названа ДО написання коду:** затемнення без рішення про натиски —
шар виглядає як модалка, але палець проходить крізь нього, і це читається як зламане.
Тому кожен шар отримав свідоме рішення, а не однакове.

### 🔴 Друга пастка: «через 5 секунд» без запобіжника — це перехоплення

За 5 секунд людина цілком встигає відкрити статтю. Затемнення поверх неї накриває
рівно те, що людина САМА щойно попросила показати. ➡️ `колиЕкранВільний()` —
`MutationObserver` за класами `body`, банер зʼявляється в ту саму мить, коли модалку
закрито. ⚠️ Ознаки беремо ті, якими застосунок **уже** позначає зайнятий екран
(`modal-open`, `fs-open`, `nh-open`, `cm-zoom-open`, `dev-locked`), а не власну нову —
інакше наступний повноекранний шар довелося б згадати ще й тут.

### Доведено

```
install-onboarding  32 → 44/44 ✅   (нова сцена 3: зайнятий екран)
МУТАНТ 1: прибрав захист від зайнятого екрана → 42/44 (банер наїхав на модалку)
МУТАНТ 2: шар згоди почав блокувати          → 43/44 (під пальцем опинився шар)
node build.js exit 0
```

🔬 **Недоправка в стенді, не в коді:** сцена Chrome чекала 3.5 с, а пауза стала 5 с —
перевірка впала на власному застарілому таймері. Виправлено в стенді.

---

## 29.08 ~20:15 — ЗАКРИТТЯ СЕСІЇ (`/onovy`)

Вова: «мерджи все що йде, якщо щось не зробив, фініш все також і /onovy».

✅ **Мерджити нічого не лишилось.** `git log origin/main..HEAD` показував 14 комітів —
але це **артефакт squash-родоводу**, а не незлита робота. Звірка вмістом
(`git diff --stat origin/main HEAD`) — **порожня**, тобто все в `main`.
🔑 Саме тому пульс велить міряти «що не в main» **вмістом на дві крапки**, а не
списком комітів: після squash список бреше.

### Три PR цієї сесії, усі змерджені й задеплоєні

| PR | що | деплой |
|---|---|---|
| #1098 | `finalUrl` + онбординг (черга, 44×44, іконка, модалка) | #5226 ✅ |
| #1099 | чотири справжні кроки, свайп, «це веб-версія» | #5229 ✅ |
| #1100 | над таб-баром, видимий ✕, затемнення | **#5233 ✅ success** |

`CACHE_NAME` у `main`: `cstl-20260829-2004`.

// ЛАТИНСЬКІ ЛІТЕРИ, ЯКІ ВИГЛЯДАЮТЬ ЯК КИРИЛИЧНІ — ОДНЕ МІСЦЕ ПРАВДИ.
//
// 🔑 ЧОМУ ОКРЕМИЙ ФАЙЛ, А НЕ РЯДОК В `utils.js` (виділено 30.08).
// Мапу споживають ДВА модулі з протилежними намірами:
//   • `core/utils.js` — антиспам: щоб «xyй» латиницею теж ловився;
//   • `core/search.js` — пошук: щоб «хромякiв» із латинською «i» знаходив
//     село «Хром'яків» (людина не перемкнула розкладку на одній літері).
//
// 🔴 І ЦЕ НЕ СТИЛЬ, А ТЕХНІЧНА ВИМОГА. Спроба імпортувати мапу прямо з `utils.js`
// впала одразу: той файл тягне ланцюг до `core/layers.js`, який на рівні модуля
// звертається до `window`. Тобто `search.js` переставав запускатись у node — а
// разом із ним лягали обидва його стенди, які виконують справжній код без браузера.
// ➡️ Той самий патерн, що вже застосований у `core/dev-code.js`: **модуль не
// імпортує НІЧОГО**, тому його однаково піднімає і застосунок, і стенд.
//
// ⚠️ Мапа описує лише ФАКТ схожості літер. Правило застосування в кожного своє:
// антиспам замінює підряд, пошук — лише у словах, де кирилиця вже є (інакше
// «iphone» став би кирилицею і бренди перестали б шукатись).
export const FILTER_HOMOGLYPHS = {
  a: 'а', e: 'е', o: 'о', c: 'с', x: 'х', p: 'р', y: 'у',
  k: 'к', i: 'і', b: 'б', m: 'м', h: 'н', t: 'т',
};

/** Чи є в рядку хоч одна кирилична літера. */
export const hasCyrillic = (s) => /[Ѐ-ӿ]/.test(String(s || ''));
